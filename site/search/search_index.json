{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Pytigon GUI","text":"<p>Version: 0.260511 \u00b7 License: LGPL 3.0 \u00b7 Author: S\u0142awomir Cho\u0142aj</p>"},{"location":"#overview","title":"Overview","text":"<p>Pytigon GUI is the wxPython desktop frontend for the Pytigon framework. It provides a rich set of GUI components \u2013 frames, pages, notebooks, toolbars, grids, form controls, and popups \u2013 all integrated with the Django web backend.</p>"},{"location":"#module-map","title":"Module Map","text":"Module Purpose <code>pytigon</code> Main application entry point (~50K) <code>wxauto</code> wxPython automation utilities <code>guictrl</code> GUI controls \u2013 tags, buttons, grids, inputs, popups <code>guiframe</code> Application frames \u2013 app, browser, form, notebook, page <code>guilib</code> GUI library \u2013 events, images, login, threading, WebSocket <code>toolbar</code> Toolbars \u2013 modern, standard, tree, generic, menu bar"},{"location":"#quick-start","title":"Quick Start","text":"<pre><code>from pytigon_gui.pytigon import main\nmain()\n</code></pre>"},{"location":"#key-design-patterns","title":"Key Design Patterns","text":"<ul> <li>Tag-based UI \u2013 XML-like tag syntax (<code>&lt;ctrl&gt;</code>, <code>&lt;button&gt;</code>, <code>&lt;grid&gt;</code>) parsed into wxPython widgets</li> <li>Device Context \u2013 Shared rendering abstraction with <code>pytigon_lib.schhtml</code></li> <li>Notebook/Page \u2013 Tabbed document interface with dynamic page creation</li> <li>Signal System \u2013 Event-driven communication between components</li> <li>WebSocket Bridge \u2013 Real-time communication with the Django backend</li> </ul>"},{"location":"HOWTO/","title":"How to Use the Pytigon GUI Documentation","text":"<p>Generate and serve API documentation for <code>pytigon_gui</code> using MkDocs with the Material for MkDocs theme and mkdocstrings.</p>"},{"location":"HOWTO/#quick-start","title":"Quick Start","text":"<pre><code>cd pytigon_gui\n./gen_docs.sh install    # one-time\n./gen_docs.sh serve      # http://127.0.0.1:8000\n./gen_docs.sh build      # static HTML \u2192 site/\n</code></pre>"},{"location":"HOWTO/#file-structure","title":"File Structure","text":"<pre><code>pytigon_gui/\n\u251c\u2500\u2500 mkdocs.yml                    # Configuration\n\u251c\u2500\u2500 gen_docs.sh                   # Build/serve/clean/install/deploy\n\u251c\u2500\u2500 docs/\n\u2502   \u251c\u2500\u2500 index.md                  # Landing page\n\u2502   \u251c\u2500\u2500 HOWTO.md                  # This file\n\u2502   \u251c\u2500\u2500 getting-started/\n\u2502   \u2502   \u251c\u2500\u2500 overview.md           # Architecture + tag system\n\u2502   \u2502   \u2514\u2500\u2500 quick-import.md       # Import cheat sheet\n\u2502   \u2514\u2500\u2500 api/                      # 43 API reference pages\n\u2502       \u251c\u2500\u2500 pytigon_gui.md        # Root package\n\u2502       \u251c\u2500\u2500 pytigon_main.md       # Main app\n\u2502       \u251c\u2500\u2500 wxauto.md             # Automation\n\u2502       \u251c\u2500\u2500 guictrl*.md           # 16 control pages\n\u2502       \u251c\u2500\u2500 guiframe*.md          # 9 frame pages\n\u2502       \u251c\u2500\u2500 guilib*.md            # 10 library pages\n\u2502       \u2514\u2500\u2500 toolbar*.md           # 8 toolbar pages\n\u2514\u2500\u2500 site/                         # Built output\n</code></pre>"},{"location":"HOWTO/#commands","title":"Commands","text":"Command Description <code>./gen_docs.sh install</code> Install mkdocs + material + mkdocstrings <code>./gen_docs.sh build</code> Generate static HTML <code>./gen_docs.sh serve</code> Live-reload dev server on :8000 <code>./gen_docs.sh clean</code> Remove site/ <code>./gen_docs.sh deploy</code> GitHub Pages deploy"},{"location":"HOWTO/#adding-a-module","title":"Adding a Module","text":"<ol> <li>Create <code>docs/api/new_module.md</code> with <code>pytigon_gui.new_module</code></li> <li>Add to <code>mkdocs.yml</code> \u2192 <code>nav</code> section</li> <li>Rebuild: <code>./gen_docs.sh build</code></li> </ol>"},{"location":"HOWTO/#note","title":"Note","text":"<p>Documentation covers all subfolders of <code>pytigon_gui/</code>, including the complete <code>guictrl/</code>, <code>guiframe/</code>, <code>guilib/</code>, and <code>toolbar/</code> packages. Non-Python content (locale/ translations) is excluded.</p>"},{"location":"api/guictrl/","title":"guictrl \u2013 GUI Controls Package","text":"<p>Control system that bridges XML-like tag syntax to wxPython widgets.</p>"},{"location":"api/guictrl/#pytigon_gui.guictrl","title":"guictrl","text":"<p>GUI control widgets for the Pytigon SchForm framework.</p> <p>Provides base control classes, widget factories, display controls, panels, grid/table controls, popup controls, and tag-to-widget parsing infrastructure.</p>"},{"location":"api/guictrl_basectrl/","title":"guictrl.basectrl \u2013 Base Control Classes","text":"<p>Base classes for all GUI controls providing common functionality like attribute handling, layout, and event binding.</p>"},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl","title":"basectrl","text":"<p>Base control class for the SchForm GUI framework.</p> <p>Defines SchBaseCtrl -- the mixin base class for all wxPython widgets managed by the SchForm framework. Provides attribute initialization from HTML tag kwargs, parent form navigation, data loading from server, and keyboard event handling.</p> <p>Also provides the handle_best_size decorator for auto-sizing widgets.</p>"},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl-attributes","title":"Attributes","text":""},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl.logger","title":"logger  <code>module-attribute</code>","text":"<pre><code>logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl-classes","title":"Classes","text":""},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl.SchBaseCtrl","title":"SchBaseCtrl","text":"<pre><code>SchBaseCtrl(parent, kwds)\n</code></pre> <p>Base mixin class for all SchForm wxPython widgets.</p> <p>Provides: - Attribute initialization from HTML tag keyword arguments - Parent form/page navigation - Server data loading for tdata (table data) and ldata (list data) - Common keyboard handling (Escape, Tab) - Widget lifecycle: after_create signal, unique name management</p> <p>Initialize base control attributes.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent wxPython window.</p> required <code>kwds</code> <p>Keyword argument dict parsed from HTML tag attributes.</p> required Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def __init__(self, parent, kwds):\n    \"\"\"Initialize base control attributes.\n\n    Args:\n        parent: Parent wxPython window.\n        kwds: Keyword argument dict parsed from HTML tag attributes.\n    \"\"\"\n    self.unique_name = None\n    self.init_base(kwds, parent)\n    self.accept_focus = True\n    self.acc_tab_bind = False\n    form = self.get_parent_form()\n    if form:\n        form.signal_from_child(self, \"__init__\")\n</code></pre>"},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl.SchBaseCtrl-functions","title":"Methods:","text":""},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl.SchBaseCtrl.CanAcceptFocus","title":"CanAcceptFocus","text":"<pre><code>CanAcceptFocus()\n</code></pre> <p>Return True if the control can accept keyboard focus.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def CanAcceptFocus(self):\n    \"\"\"Return True if the control can accept keyboard focus.\"\"\"\n    if self.IsShown() and self.IsEnabled():\n        return self.accept_focus\n    return False\n</code></pre>"},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl.SchBaseCtrl.CanAcceptFocusFromKeyboard","title":"CanAcceptFocusFromKeyboard","text":"<pre><code>CanAcceptFocusFromKeyboard()\n</code></pre> <p>Return True if the control accepts focus from keyboard navigation.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def CanAcceptFocusFromKeyboard(self):\n    \"\"\"Return True if the control accepts focus from keyboard navigation.\"\"\"\n    return self.CanAcceptFocus()\n</code></pre>"},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl.SchBaseCtrl.after_create","title":"after_create","text":"<pre><code>after_create()\n</code></pre> <p>Called immediately after the widget is fully constructed.</p> <p>Binds keyboard events, executes the 'onload' script (if any), calls the ext_init hook if present, and signals the parent form that this child is ready.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def after_create(self):\n    \"\"\"Called immediately after the widget is fully constructed.\n\n    Binds keyboard events, executes the 'onload' script (if any),\n    calls the __ext_init__ hook if present, and signals the parent\n    form that this child is ready.\n    \"\"\"\n    self.Bind(wx.EVT_KEY_DOWN, self.on_key_down_base)\n    if self.onload:\n        d = {\"wx\": wx, \"self\": self}\n        try:\n            exec(self.onload, d)\n        except Exception:\n            logger.exception(\"Error executing onload script for %s\", self.GetName())\n\n    if hasattr(self, \"__ext_init__\"):\n        self.__ext_init__()\n\n    self.get_parent_form().signal_from_child(self, \"after_create\")\n</code></pre>"},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl.SchBaseCtrl.get_ldata","title":"get_ldata","text":"<pre><code>get_ldata()\n</code></pre> <p>Return hierarchical list data, loading from server if necessary.</p> <p>Returns:</p> Type Description <p>List data (nested tree structure), or None if unavailable.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_ldata(self):\n    \"\"\"Return hierarchical list data, loading from server if necessary.\n\n    Returns:\n        List data (nested tree structure), or None if unavailable.\n    \"\"\"\n    if self.ldata is not None:\n        return self.ldata\n    if self.ldatabuf is not None:\n        return self.ldatabuf\n    if self.src:\n        self.refresh_ldata()\n    return self.ldatabuf\n</code></pre>"},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl.SchBaseCtrl.get_parent_form","title":"get_parent_form","text":"<pre><code>get_parent_form()\n</code></pre> <p>Walk up the widget hierarchy to find the parent SchForm.</p> <p>Returns:</p> Type Description <p>The SchForm ancestor, or None if not found.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_parent_form(self):\n    \"\"\"Walk up the widget hierarchy to find the parent SchForm.\n\n    Returns:\n        The SchForm ancestor, or None if not found.\n    \"\"\"\n    parent = self.parent\n    while parent is not None:\n        if type(parent).__name__ == \"SchForm\":\n            return parent\n        parent = parent.GetParent()\n    return None\n</code></pre>"},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl.SchBaseCtrl.get_parent_page","title":"get_parent_page","text":"<pre><code>get_parent_page()\n</code></pre> <p>Return the parent page that contains this control's form.</p> <p>Returns:</p> Type Description <p>The parent SchPage, or None if not found.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_parent_page(self):\n    \"\"\"Return the parent page that contains this control's form.\n\n    Returns:\n        The parent SchPage, or None if not found.\n    \"\"\"\n    form = self.get_parent_form()\n    if form:\n        return form.get_parent_page()\n    return None\n</code></pre>"},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl.SchBaseCtrl.get_tdata","title":"get_tdata","text":"<pre><code>get_tdata()\n</code></pre> <p>Return table data, loading from server if necessary.</p> <p>Returns:</p> Type Description <p>Table data (list of rows), or None if unavailable.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_tdata(self):\n    \"\"\"Return table data, loading from server if necessary.\n\n    Returns:\n        Table data (list of rows), or None if unavailable.\n    \"\"\"\n    if self.tdata is not None:\n        return self.tdata\n    if self.tdatabuf is not None:\n        return self.tdatabuf\n    if self.src:\n        self.refresh_tdata()\n    return self.tdatabuf\n</code></pre>"},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl.SchBaseCtrl.get_unique_name","title":"get_unique_name","text":"<pre><code>get_unique_name()\n</code></pre> <p>Return the unique name identifier.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_unique_name(self):\n    \"\"\"Return the unique name identifier.\"\"\"\n    return self.unique_name\n</code></pre>"},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl.SchBaseCtrl.init_base","title":"init_base","text":"<pre><code>init_base(kwds, parent=None)\n</code></pre> <p>Initialize all control attributes from kwds.</p> <p>Extracts well-known attributes (href, label, id, target, value, valuetype, defaultvalue, length, maxlength, readonly, hidden, src, onload, tdata, ldata, data, param, style) from kwds and assigns them as instance attributes.</p> <p>Parameters:</p> Name Type Description Default <code>kwds</code> <p>Keyword argument dict parsed from HTML tag attributes.</p> required <code>parent</code> <p>Optional parent window reference.</p> <code>None</code> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def init_base(self, kwds, parent=None):\n    \"\"\"Initialize all control attributes from kwds.\n\n    Extracts well-known attributes (href, label, id, target, value,\n    valuetype, defaultvalue, length, maxlength, readonly, hidden,\n    src, onload, tdata, ldata, data, param, style) from kwds\n    and assigns them as instance attributes.\n\n    Args:\n        kwds: Keyword argument dict parsed from HTML tag attributes.\n        parent: Optional parent window reference.\n    \"\"\"\n    if parent:\n        self.parent = parent\n\n    self.ldatabuf = None\n\n    self.tag = None\n    if \"param\" in kwds and \"tag\" in kwds[\"param\"]:\n        self.tag = kwds[\"param\"][\"tag\"]\n\n    self.href = _extract_kwarg(kwds, \"href\", None)\n    if self.href is None:\n        self.href = \"\"\n\n    self.label = _extract_kwarg(kwds, \"label\", None)\n    self.nr_id = _extract_kwarg(kwds, \"id\", None)\n    self.target = _extract_kwarg(kwds, \"target\", \"_blank\")\n    self.value = _extract_kwarg(kwds, \"value\", None)\n    self.valuetype = _extract_kwarg(kwds, \"valuetype\", \"data\")\n    self.defaultvalue = _extract_kwarg(kwds, \"defaultvalue\", None)\n    self.length = int(_extract_kwarg(kwds, \"length\", 0))\n    self.maxlength = int(_extract_kwarg(kwds, \"maxlength\", 0))\n    self.readonly = _extract_kwarg(kwds, \"readonly\", False)\n    self.hidden = True if _extract_kwarg(kwds, \"hidden\", False) else False\n    self.src = _extract_kwarg(kwds, \"src\", None)\n    self.onload = _extract_kwarg(kwds, \"onload\", None)\n\n    # tdata / tdatabuf: table data (already parsed) or lazily loaded\n    self.tdata = _extract_kwarg(kwds, \"tdata\", None)\n    self.tdatabuf = None\n    if self.tdata is not None:\n        self.tdatabuf = None\n\n    # ldata: hierarchical list data (already parsed) or lazily loaded\n    self.ldata = _extract_kwarg(kwds, \"ldata\", None)\n\n    # data: URL-encoded string data\n    raw_data = _extract_kwarg(kwds, \"data\", None)\n    self.data = unquote(raw_data) if raw_data is not None else None\n\n    self.param = _extract_kwarg(kwds, \"param\", None)\n    self.style = _extract_kwarg(kwds, \"style\", None)\n\n    # Invoke any registered ctrl_process hooks for this tag type\n    ctrl_process = wx.GetApp().ctrl_process\n    if self.tag in ctrl_process:\n        for fun in ctrl_process[self.tag]:\n            fun(self)\n</code></pre>"},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl.SchBaseCtrl.is_ctrl_block","title":"is_ctrl_block","text":"<pre><code>is_ctrl_block()\n</code></pre> <p>Return True if this control is a block-level element.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def is_ctrl_block(self):\n    \"\"\"Return True if this control is a block-level element.\"\"\"\n    return self.GetParent().is_ctrl_block(self)\n</code></pre>"},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl.SchBaseCtrl.load_data_from_server","title":"load_data_from_server","text":"<pre><code>load_data_from_server(path)\n</code></pre> <p>Load raw binary data from a server URL.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <p>URL path to fetch.</p> required <p>Returns:</p> Type Description <p>Raw response data (bytes).</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def load_data_from_server(self, path):\n    \"\"\"Load raw binary data from a server URL.\n\n    Args:\n        path: URL path to fetch.\n\n    Returns:\n        Raw response data (bytes).\n    \"\"\"\n    http = wx.GetApp().get_http(self.parent)\n    response = http.get(self, str(path))\n    return response.ptr()\n</code></pre>"},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl.SchBaseCtrl.load_string_from_server","title":"load_string_from_server","text":"<pre><code>load_string_from_server(path)\n</code></pre> <p>Load a text response from a server URL.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <p>URL path to fetch.</p> required <p>Returns:</p> Type Description <p>Response body as a string.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def load_string_from_server(self, path):\n    \"\"\"Load a text response from a server URL.\n\n    Args:\n        path: URL path to fetch.\n\n    Returns:\n        Response body as a string.\n    \"\"\"\n    http = wx.GetApp().get_http(self.parent)\n    response = http.get(self, str(path))\n    return response.str()\n</code></pre>"},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl.SchBaseCtrl.on_key_down_base","title":"on_key_down_base","text":"<pre><code>on_key_down_base(event)\n</code></pre> <p>Default key-down handler for Escape and Tab navigation.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>wx.KeyEvent.</p> required Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def on_key_down_base(self, event):\n    \"\"\"Default key-down handler for Escape and Tab navigation.\n\n    Args:\n        event: wx.KeyEvent.\n    \"\"\"\n    if event.GetKeyCode() == wx.WXK_ESCAPE:\n        wx.CallAfter(self.GetParent().cancel)\n    elif event.GetKeyCode() == wx.WXK_TAB:\n        if event.ShiftDown():\n            self.GetParent().Navigate(self, True)\n        else:\n            self.GetParent().Navigate(self, False)\n    else:\n        event.Skip()\n</code></pre>"},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl.SchBaseCtrl.refresh_ldata","title":"refresh_ldata","text":"<pre><code>refresh_ldata()\n</code></pre> <p>Refresh hierarchical list data from the server.</p> <p>Loads from self.src and parses into self.ldatabuf using TreeParser.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def refresh_ldata(self):\n    \"\"\"Refresh hierarchical list data from the server.\n\n    Loads from self.src and parses into self.ldatabuf using TreeParser.\n    \"\"\"\n    if self.ldata is None and self.src:\n        lista = self.load_data_from_server(self.src)\n        mp = TreeParser()\n        mp.feed(lista)\n        mp.close()\n        self.ldatabuf = mp.tree_parent[0][1]\n</code></pre>"},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl.SchBaseCtrl.refresh_tdata","title":"refresh_tdata","text":"<pre><code>refresh_tdata(html_src=None)\n</code></pre> <p>Refresh table data from the server or provided HTML source.</p> <p>Parameters:</p> Name Type Description Default <code>html_src</code> <p>Optional HTML string containing table data. If None, loads from self.src.</p> <code>None</code> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def refresh_tdata(self, html_src=None):\n    \"\"\"Refresh table data from the server or provided HTML source.\n\n    Args:\n        html_src: Optional HTML string containing table data.\n            If None, loads from self.src.\n    \"\"\"\n    if self.tdata is None and self.src:\n        if html_src:\n            tables = html_src\n        else:\n            tables = self.load_data_from_server(self.src).decode(\"utf-8\")\n        self.tdatabuf = tdata_from_html(tables, wx.GetApp().http)\n</code></pre>"},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl.SchBaseCtrl.set_acc_key_tab","title":"set_acc_key_tab","text":"<pre><code>set_acc_key_tab(tab)\n</code></pre> <p>Register an accelerator table for this widget.</p> <p>Parameters:</p> Name Type Description Default <code>tab</code> <p>List or tuple of accelerator elements. Each element has structure: (flag, keycode, callback function).</p> required <p>Returns:</p> Type Description <p>Result from the parent's set_acc_key_tab method.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def set_acc_key_tab(self, tab):\n    \"\"\"Register an accelerator table for this widget.\n\n    Args:\n        tab: List or tuple of accelerator elements. Each element has\n            structure: (flag, keycode, callback function).\n\n    Returns:\n        Result from the parent's set_acc_key_tab method.\n    \"\"\"\n    return self.GetParent().set_acc_key_tab(self, tab)\n</code></pre>"},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl.SchBaseCtrl.set_unique_name","title":"set_unique_name","text":"<pre><code>set_unique_name(name)\n</code></pre> <p>Assign a unique name identifier to this control.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def set_unique_name(self, name):\n    \"\"\"Assign a unique name identifier to this control.\"\"\"\n    self.unique_name = name\n</code></pre>"},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl-functions","title":"Functions:","text":""},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl._extract_kwarg","title":"_extract_kwarg","text":"<pre><code>_extract_kwarg(kwds, key, default=None)\n</code></pre> <p>Remove and return key from kwds, or return default if absent.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def _extract_kwarg(kwds, key, default=None):\n    \"\"\"Remove and return key from kwds, or return default if absent.\"\"\"\n    if key in kwds:\n        val = kwds[key]\n        del kwds[key]\n        return val\n    return default\n</code></pre>"},{"location":"api/guictrl_basectrl/#pytigon_gui.guictrl.basectrl.handle_best_size","title":"handle_best_size","text":"<pre><code>handle_best_size(base_class)\n</code></pre> <p>Decorator that adds GetBestSize() using param width/height hints.</p> <p>When applied to a widget class, this makes GetBestSize() first check for explicit 'width' and 'height' values in self.param, falling back to the widget's default best size.</p> <p>Parameters:</p> Name Type Description Default <code>base_class</code> <p>A wxPython widget class to wrap.</p> required <p>Returns:</p> Type Description <p>A derived class with custom GetBestSize() logic.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def handle_best_size(base_class):\n    \"\"\"Decorator that adds GetBestSize() using param width/height hints.\n\n    When applied to a widget class, this makes GetBestSize() first check\n    for explicit 'width' and 'height' values in self.param, falling back\n    to the widget's default best size.\n\n    Args:\n        base_class: A wxPython widget class to wrap.\n\n    Returns:\n        A derived class with custom GetBestSize() logic.\n    \"\"\"\n\n    class _Derived(base_class):\n        def GetBestSize(self):\n            w = 0\n            h = 0\n            if self.param and \"width\" in self.param:\n                try:\n                    w = int(self.param[\"width\"])\n                except (ValueError, TypeError):\n                    pass\n            if self.param and \"height\" in self.param:\n                try:\n                    h = int(self.param[\"height\"])\n                except (ValueError, TypeError):\n                    pass\n            if w == 0 or h == 0:\n                base_size = super().GetBestSize()\n                if w == 0:\n                    w = base_size[0]\n                if h == 0:\n                    h = base_size[1]\n            return (w, h)\n\n    return _Derived\n</code></pre>"},{"location":"api/guictrl_button/","title":"guictrl.button \u2013 Button Controls","text":"<p>Button control implementations including standard buttons and toolbar-integrated buttons.</p>"},{"location":"api/guictrl_button/#pytigon_gui.guictrl.button","title":"button","text":"<p>Button widgets for the SchForm GUI framework.</p> <p>Provides button base classes, toolbar button variants, and bitmap/nobg/plate/menu button implementations.</p>"},{"location":"api/guictrl_button/#pytigon_gui.guictrl.button-modules","title":"Modules","text":""},{"location":"api/guictrl_button/#pytigon_gui.guictrl.button.base","title":"base","text":"<p>Button widget classes for the SchForm GUI framework.</p> <p>Provides factory functions that create wxPython button subclasses integrated with the SchBaseCtrl mixin. All button classes are created dynamically through factory functions to reduce code duplication while allowing each button type to customize its behavior (bitmap handling, close behavior, menu handling, etc.).</p> Factory functions <p>_make_button_class, _make_menu_button_class</p>"},{"location":"api/guictrl_button/#pytigon_gui.guictrl.button.base-attributes","title":"Attributes","text":""},{"location":"api/guictrl_button/#pytigon_gui.guictrl.button.base.BITMAPBUTTON","title":"BITMAPBUTTON  <code>module-attribute</code>","text":"<pre><code>BITMAPBUTTON = _make_button_class(wx.BitmapButton, is_bitmap_button=True)\n</code></pre> <p>Button with a bitmap, based on wx.BitmapButton.</p>"},{"location":"api/guictrl_button/#pytigon_gui.guictrl.button.base.CLOSEBUTTON","title":"CLOSEBUTTON  <code>module-attribute</code>","text":"<pre><code>CLOSEBUTTON = _make_button_class(\n    BitmapTextButton, is_bitmap_button=True, is_close_button=True\n)\n</code></pre> <p>Close button that cancels the child form when clicked.</p>"},{"location":"api/guictrl_button/#pytigon_gui.guictrl.button.base.GENBITMAPBUTTON","title":"GENBITMAPBUTTON  <code>module-attribute</code>","text":"<pre><code>GENBITMAPBUTTON = _make_button_class(buttons.GenBitmapButton, is_bitmap_button=True)\n</code></pre> <p>Generic bitmap button with no border.</p>"},{"location":"api/guictrl_button/#pytigon_gui.guictrl.button.base.GENBITMAPBUTTONTXT","title":"GENBITMAPBUTTONTXT  <code>module-attribute</code>","text":"<pre><code>GENBITMAPBUTTONTXT = _make_button_class(BitmapTextButton, is_bitmap_button=True)\n</code></pre> <p>Bitmap text button (normal icon size).</p>"},{"location":"api/guictrl_button/#pytigon_gui.guictrl.button.base.GENBITMAPBUTTONTXT_SMALL","title":"GENBITMAPBUTTONTXT_SMALL  <code>module-attribute</code>","text":"<pre><code>GENBITMAPBUTTONTXT_SMALL = _make_button_class(\n    BitmapTextButton, is_bitmap_button=True, icon_size=1\n)\n</code></pre> <p>Bitmap text button (small icon size).</p>"},{"location":"api/guictrl_button/#pytigon_gui.guictrl.button.base.GENBITMAPTEXTBUTTON","title":"GENBITMAPTEXTBUTTON  <code>module-attribute</code>","text":"<pre><code>GENBITMAPTEXTBUTTON = _make_button_class(\n    buttons.GenBitmapTextButton, is_bitmap_button=True\n)\n</code></pre> <p>Generic bitmap button with text label.</p>"},{"location":"api/guictrl_button/#pytigon_gui.guictrl.button.base.MENUBUTTON","title":"MENUBUTTON  <code>module-attribute</code>","text":"<pre><code>MENUBUTTON = _make_menu_button_class(SIMPLE_BUTTON)\n</code></pre> <p>Menu button based on a simple push button.</p>"},{"location":"api/guictrl_button/#pytigon_gui.guictrl.button.base.MENUTOOLBARBUTTON","title":"MENUTOOLBARBUTTON  <code>module-attribute</code>","text":"<pre><code>MENUTOOLBARBUTTON = _make_menu_button_class(GENBITMAPBUTTONTXT)\n</code></pre> <p>Menu button styled as a toolbar button with bitmap and text.</p>"},{"location":"api/guictrl_button/#pytigon_gui.guictrl.button.base.NOBG_BUTTON","title":"NOBG_BUTTON  <code>module-attribute</code>","text":"<pre><code>NOBG_BUTTON = GENBITMAPBUTTONTXT\n</code></pre> <p>Alias for a button without background (same as GENBITMAPBUTTONTXT).</p>"},{"location":"api/guictrl_button/#pytigon_gui.guictrl.button.base.NOBG_BUTTON_TXT","title":"NOBG_BUTTON_TXT  <code>module-attribute</code>","text":"<pre><code>NOBG_BUTTON_TXT = GENBITMAPBUTTONTXT\n</code></pre> <p>Alias for a text button without background.</p>"},{"location":"api/guictrl_button/#pytigon_gui.guictrl.button.base.PLATEBUTTON","title":"PLATEBUTTON  <code>module-attribute</code>","text":"<pre><code>PLATEBUTTON = _make_button_class(platebtn.PlateButton, is_bitmap_button=True)\n</code></pre> <p>Styled plate button with gradient background and bitmap.</p>"},{"location":"api/guictrl_button/#pytigon_gui.guictrl.button.base.SIMPLE_BUTTON","title":"SIMPLE_BUTTON  <code>module-attribute</code>","text":"<pre><code>SIMPLE_BUTTON = _make_button_class(wx.Button)\n</code></pre> <p>Standard push button without bitmap.</p>"},{"location":"api/guictrl_button/#pytigon_gui.guictrl.button.base-classes","title":"Classes","text":""},{"location":"api/guictrl_button/#pytigon_gui.guictrl.button.base-functions","title":"Functions:","text":""},{"location":"api/guictrl_button/#pytigon_gui.guictrl.button.toolbarbutton","title":"toolbarbutton","text":"<p>Module contains helper classes for button widgets</p>"},{"location":"api/guictrl_button/#pytigon_gui.guictrl.button.toolbarbutton-classes","title":"Classes","text":""},{"location":"api/guictrl_button/#pytigon_gui.guictrl.button.toolbarbutton.BitmapTextButton","title":"BitmapTextButton","text":"<pre><code>BitmapTextButton(\n    parent,\n    id=-1,\n    bitmap=NullBitmap,\n    label=\"\",\n    pos=DefaultPosition,\n    size=DefaultSize,\n    style=0,\n    validator=DefaultValidator,\n    name=\"bitmaptextbutton\",\n)\n</code></pre> <p>               Bases: <code>GenBitmapButton</code></p> <p>Button for toolbars</p> <p>Constructor</p> Source code in <code>pytigon_gui/guictrl/button/toolbarbutton.py</code> <pre><code>def __init__(\n    self,\n    parent,\n    id=-1,\n    bitmap=wx.NullBitmap,\n    label=\"\",\n    pos=wx.DefaultPosition,\n    size=wx.DefaultSize,\n    style=0,\n    validator=wx.DefaultValidator,\n    name=\"bitmaptextbutton\",\n):\n    \"\"\"Constructor\"\"\"\n\n    self._art = RB.RibbonMSWArtProvider()\n    # self._art = RB.RibbonArtProvider()\n    wx.lib.buttons.GenBitmapButton.__init__(\n        self, parent, id, bitmap, pos, size, style | wx.BU_EXACTFIT, validator, name\n    )\n    self.SetLabel(label)\n    self.enter_state = False\n    self.Bind(wx.EVT_ENTER_WINDOW, self.on_enter)\n    self.Bind(wx.EVT_LEAVE_WINDOW, self.on_leave)\n    image = self.bmpLabel.ConvertToImage()\n    imageutils.grayOut(image)\n    self.bmpLabel2 = wx.Bitmap(image)\n    self.SetBackgroundColour(wx.SystemSettings.GetColour(wx.SYS_COLOUR_3DFACE))\n</code></pre> Methods:\u00b6"},{"location":"api/guictrl_ctrl/","title":"guictrl.ctrl \u2013 Control System","text":"<p>Core control orchestration \u2013 manages the control tree, command routing, and interaction between controls and frames.</p>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl","title":"ctrl","text":"<p>Backward-compatibility shim for pytigon_gui.guictrl.ctrl.</p> <p>This module re-exports all widget classes and factory functions from the refactored submodules so that existing code relying on:</p> <pre><code>import pytigon_gui.guictrl.ctrl as schctrl\n</code></pre> <p>continues to work unchanged. The actual implementations now live in:</p> <pre><code>guictrl/button/base.py     - Button classes and factories\nguictrl/input/text.py      - TEXT, PASSWORD, SEARCH, STYLEDTEXT, MASKTEXT\nguictrl/input/numeric.py   - NUM, AMOUNT, FLOAT, SPIN, SLIDER, GAUGE, TICKER\nguictrl/input/choice.py    - CHECKBOX, CHECKLISTBOX, LISTBOX, LIST, CHECKLIST,\n                             RADIOBOX, RADIOBUTTON\nguictrl/input/combo.py     - BITMAPCOMBOBOX, CHOICE, DBCHOICE, DBCHOICE_EXT\nguictrl/input/datetime.py  - CALENDAR, DATEPICKER, DATETIMEPICKER, TIME\nguictrl/display.py         - STATICTEXT, ERRORLIST, TREE, TREELIST,\n                             COLOURSELECT, GENERICDIR, EDITABLELISTBOX,\n                             FILEBROWSEBUTTON, IMAGEBROWSEBUTTON,\n                             HTMLLISTBOX, POPUPHTML\nguictrl/panels.py          - HTML, NOTEBOOK, COLLAPSIBLE_PANEL, CompositePanel\nguictrl/grids.py           - TABLE, GRID, UPDATEGRIDBUTTON\nguictrl/factory.py         - SELECT, BUTTON, TEXTAREA, SELECT2,\n                             COMPOSITE, COMPONENT\n</code></pre> <p>NOTE: This module's namespace is dynamically extended by plugins at runtime (e.g. HTML2, COMPONENT, STYLEDTEXT, AUTOCOMPLETE, etc. may be replaced). Keep this file as a thin shim to preserve that capability.</p>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl-attributes","title":"Attributes","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.AUTOCOMPLETE","title":"AUTOCOMPLETE  <code>module-attribute</code>","text":"<pre><code>AUTOCOMPLETE = STYLEDTEXT\n</code></pre> <p>Alias for STYLEDTEXT; may be replaced by plugins at runtime.</p>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.BITMAPBUTTON","title":"BITMAPBUTTON  <code>module-attribute</code>","text":"<pre><code>BITMAPBUTTON = _make_button_class(wx.BitmapButton, is_bitmap_button=True)\n</code></pre> <p>Button with a bitmap, based on wx.BitmapButton.</p>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CLOSEBUTTON","title":"CLOSEBUTTON  <code>module-attribute</code>","text":"<pre><code>CLOSEBUTTON = _make_button_class(\n    BitmapTextButton, is_bitmap_button=True, is_close_button=True\n)\n</code></pre> <p>Close button that cancels the child form when clicked.</p>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.GENBITMAPBUTTON","title":"GENBITMAPBUTTON  <code>module-attribute</code>","text":"<pre><code>GENBITMAPBUTTON = _make_button_class(buttons.GenBitmapButton, is_bitmap_button=True)\n</code></pre> <p>Generic bitmap button with no border.</p>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.GENBITMAPBUTTONTXT","title":"GENBITMAPBUTTONTXT  <code>module-attribute</code>","text":"<pre><code>GENBITMAPBUTTONTXT = _make_button_class(BitmapTextButton, is_bitmap_button=True)\n</code></pre> <p>Bitmap text button (normal icon size).</p>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.GENBITMAPBUTTONTXT_SMALL","title":"GENBITMAPBUTTONTXT_SMALL  <code>module-attribute</code>","text":"<pre><code>GENBITMAPBUTTONTXT_SMALL = _make_button_class(\n    BitmapTextButton, is_bitmap_button=True, icon_size=1\n)\n</code></pre> <p>Bitmap text button (small icon size).</p>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.GENBITMAPTEXTBUTTON","title":"GENBITMAPTEXTBUTTON  <code>module-attribute</code>","text":"<pre><code>GENBITMAPTEXTBUTTON = _make_button_class(\n    buttons.GenBitmapTextButton, is_bitmap_button=True\n)\n</code></pre> <p>Generic bitmap button with text label.</p>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.HTML2","title":"HTML2  <code>module-attribute</code>","text":"<pre><code>HTML2 = None\n</code></pre> <p>Set by webview plugins (cef, wxwebview) to provide an HTML2 viewer class.</p>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.MENUBUTTON","title":"MENUBUTTON  <code>module-attribute</code>","text":"<pre><code>MENUBUTTON = _make_menu_button_class(SIMPLE_BUTTON)\n</code></pre> <p>Menu button based on a simple push button.</p>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.MENUTOOLBARBUTTON","title":"MENUTOOLBARBUTTON  <code>module-attribute</code>","text":"<pre><code>MENUTOOLBARBUTTON = _make_menu_button_class(GENBITMAPBUTTONTXT)\n</code></pre> <p>Menu button styled as a toolbar button with bitmap and text.</p>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.NOBG_BUTTON","title":"NOBG_BUTTON  <code>module-attribute</code>","text":"<pre><code>NOBG_BUTTON = GENBITMAPBUTTONTXT\n</code></pre> <p>Alias for a button without background (same as GENBITMAPBUTTONTXT).</p>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.NOBG_BUTTON_TXT","title":"NOBG_BUTTON_TXT  <code>module-attribute</code>","text":"<pre><code>NOBG_BUTTON_TXT = GENBITMAPBUTTONTXT\n</code></pre> <p>Alias for a text button without background.</p>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.PLATEBUTTON","title":"PLATEBUTTON  <code>module-attribute</code>","text":"<pre><code>PLATEBUTTON = _make_button_class(platebtn.PlateButton, is_bitmap_button=True)\n</code></pre> <p>Styled plate button with gradient background and bitmap.</p>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SIMPLE_BUTTON","title":"SIMPLE_BUTTON  <code>module-attribute</code>","text":"<pre><code>SIMPLE_BUTTON = _make_button_class(wx.Button)\n</code></pre> <p>Standard push button without bitmap.</p>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.STANDARDSTYLEDTEXT","title":"STANDARDSTYLEDTEXT  <code>module-attribute</code>","text":"<pre><code>STANDARDSTYLEDTEXT = STYLEDTEXT\n</code></pre> <p>Alias for STYLEDTEXT; the standard styled text implementation.</p>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl-classes","title":"Classes","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.AMOUNT","title":"AMOUNT","text":"<pre><code>AMOUNT(parent, **kwds)\n</code></pre> <p>               Bases: <code>SpinCtrlDouble</code>, <code>SchBaseCtrl</code></p> <p>Currency/amount spin control with double precision (2 digits).</p> <p>Handles ctrlamount tag. Supports min/max bounds from param, process_enter, readonly mode, and arrow key navigation.</p> Tag arguments <p>Initialize the amount spin control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.SpinCtrlDouble with increment of 1, 2 decimal digits, and configurable bounds.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/numeric.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the amount spin control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.SpinCtrlDouble with increment\n            of 1, 2 decimal digits, and configurable bounds.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    if self.param and \"process_enter\" in self.param:\n        kwds[\"style\"] = wx.TE_PROCESS_ENTER\n    if self.readonly:\n        style = 0\n        if \"style\" in kwds:\n            style = kwds[\"style\"]\n        style = style | wx.TE_READONLY\n        kwds[\"style\"] = style\n\n    kwds[\"inc\"] = 1\n\n    if self.param and \"min\" in self.param:\n        kwds[\"min\"] = float(self.param[\"min\"])\n    else:\n        kwds[\"min\"] = -100000000\n    if self.param and \"max\" in self.param:\n        kwds[\"max\"] = float(self.param[\"max\"])\n    else:\n        kwds[\"max\"] = 100000000\n\n    kwds[\"style\"] = wx.SP_ARROW_KEYS | wx.ALIGN_RIGHT\n\n    wx.SpinCtrlDouble.__init__(self, parent, **kwds)\n    self.SetDigits(2)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.AMOUNT-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.BITMAPCOMBOBOX","title":"BITMAPCOMBOBOX","text":"<pre><code>BITMAPCOMBOBOX(parent, **kwds)\n</code></pre> <p>               Bases: <code>BitmapComboBox</code>, <code>SchBaseCtrl</code></p> <p>Bitmap combo box with icon support.</p> <p>Handles ctrlbitmapcombobox tag. Displays a dropdown with optional icons loaded from filesystem paths. Supports embedded wxArt icons, PNG icons from static files, and Fork Awesome font icons.</p> Tag arguments <p>Initialize the bitmap combo box.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to BitmapComboBox.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the bitmap combo box.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to BitmapComboBox.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    BitmapComboBox.__init__(self, parent, **kwds)\n    self.init_default_icons = False\n    self._first_use = True\n    self.accept_focus = False\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.BITMAPCOMBOBOX-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.BITMAPCOMBOBOX.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Get the current combo box value.</p> <p>Returns:</p> Type Description <p>The current value string, or empty string if None.</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get the current combo box value.\n\n    Returns:\n        The current value string, or empty string if None.\n    \"\"\"\n    ret = super().GetValue()\n    if ret:\n        return ret\n    else:\n        return \"\"\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.BITMAPCOMBOBOX.SetValue","title":"SetValue","text":"<pre><code>SetValue(value)\n</code></pre> <p>Set the combo box value.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>Value to select. Empty/None sets empty string.</p> required Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def SetValue(self, value):\n    \"\"\"Set the combo box value.\n\n    Args:\n        value: Value to select. Empty/None sets empty string.\n    \"\"\"\n    if value:\n        ret = super().SetValue(value)\n    else:\n        ret = super().SetValue(\"\")\n    return ret\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.BITMAPCOMBOBOX.after_create","title":"after_create","text":"<pre><code>after_create()\n</code></pre> <p>Load icons after the control is fully created.</p> <p>Uses wx.CallAfter to defer icon loading until the event loop is idle, ensuring the control is properly sized and ready.</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def after_create(self):\n    \"\"\"Load icons after the control is fully created.\n\n    Uses wx.CallAfter to defer icon loading until the\n    event loop is idle, ensuring the control is properly\n    sized and ready.\n    \"\"\"\n    super().after_create()\n    if self.init_default_icons:\n\n        def _load_icons():\n            self.init_embeded_icons()\n            self.init_fa_icons()\n\n        wx.CallAfter(_load_icons)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.BITMAPCOMBOBOX.init_embeded_icons","title":"init_embeded_icons","text":"<pre><code>init_embeded_icons()\n</code></pre> <p>Load embedded PNG icons from the static/icons/22x22 directory.</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def init_embeded_icons(self):\n    \"\"\"Load embedded PNG icons from the static/icons/22x22 directory.\"\"\"\n    base_path = str(wx.GetApp().src_path) + \"/static/icons/22x22/\"\n    return self._init_icons(base_path, \"png://\")\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.BITMAPCOMBOBOX.init_extern_icons","title":"init_extern_icons","text":"<pre><code>init_extern_icons(base_path, prefix)\n</code></pre> <p>Load icons from an external directory.</p> <p>Parameters:</p> Name Type Description Default <code>base_path</code> <p>Filesystem path to the icon directory.</p> required <code>prefix</code> <p>URL-style prefix for icon identifiers (e.g. 'png://').</p> required Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def init_extern_icons(self, base_path, prefix):\n    \"\"\"Load icons from an external directory.\n\n    Args:\n        base_path: Filesystem path to the icon directory.\n        prefix: URL-style prefix for icon identifiers (e.g. 'png://').\n    \"\"\"\n    return self._init_icons(base_path, prefix)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.BITMAPCOMBOBOX.init_fa_icons","title":"init_fa_icons","text":"<pre><code>init_fa_icons()\n</code></pre> <p>Load Fork Awesome font icons from the static/fonts directory.</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def init_fa_icons(self):\n    \"\"\"Load Fork Awesome font icons from the static/fonts directory.\"\"\"\n    base_path = (\n        str(wx.GetApp().src_path) + \"/static/fonts/fork-awesome/fonts/22x22/\"\n    )\n    return self._init_icons(base_path, \"fa://\")\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.BITMAPCOMBOBOX.init_wx_icons","title":"init_wx_icons","text":"<pre><code>init_wx_icons()\n</code></pre> <p>Load standard wxArtProvider icons into the combo box.</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def init_wx_icons(self):\n    \"\"\"Load standard wxArtProvider icons into the combo box.\"\"\"\n    for art_id in sorted([pos for pos in dir(wx) if pos.startswith(\"ART_\")]):\n        artid = getattr(wx, art_id)\n        bmp = wx.ArtProvider.GetBitmap(artid, wx.ART_TOOLBAR, (22, 22))\n        if bmp.IsOk():\n            self.Append(art_id, bmp, art_id)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.BitmapTextButton","title":"BitmapTextButton","text":"<pre><code>BitmapTextButton(\n    parent,\n    id=-1,\n    bitmap=NullBitmap,\n    label=\"\",\n    pos=DefaultPosition,\n    size=DefaultSize,\n    style=0,\n    validator=DefaultValidator,\n    name=\"bitmaptextbutton\",\n)\n</code></pre> <p>               Bases: <code>GenBitmapButton</code></p> <p>Button for toolbars</p> <p>Constructor</p> Source code in <code>pytigon_gui/guictrl/button/toolbarbutton.py</code> <pre><code>def __init__(\n    self,\n    parent,\n    id=-1,\n    bitmap=wx.NullBitmap,\n    label=\"\",\n    pos=wx.DefaultPosition,\n    size=wx.DefaultSize,\n    style=0,\n    validator=wx.DefaultValidator,\n    name=\"bitmaptextbutton\",\n):\n    \"\"\"Constructor\"\"\"\n\n    self._art = RB.RibbonMSWArtProvider()\n    # self._art = RB.RibbonArtProvider()\n    wx.lib.buttons.GenBitmapButton.__init__(\n        self, parent, id, bitmap, pos, size, style | wx.BU_EXACTFIT, validator, name\n    )\n    self.SetLabel(label)\n    self.enter_state = False\n    self.Bind(wx.EVT_ENTER_WINDOW, self.on_enter)\n    self.Bind(wx.EVT_LEAVE_WINDOW, self.on_leave)\n    image = self.bmpLabel.ConvertToImage()\n    imageutils.grayOut(image)\n    self.bmpLabel2 = wx.Bitmap(image)\n    self.SetBackgroundColour(wx.SystemSettings.GetColour(wx.SYS_COLOUR_3DFACE))\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.BitmapTextButton-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CALENDAR","title":"CALENDAR","text":"<pre><code>CALENDAR(parent, **kwds)\n</code></pre> <p>               Bases: <code>CalendarCtrl</code>, <code>SchBaseCtrl</code></p> <p>Calendar control for date selection.</p> <p>Handles ctrlcalendar tag. Displays a monthly calendar grid with Monday as the first day of the week, holiday highlighting, and sequential month selection.</p> Tag arguments <p>Initialize the calendar control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to CalendarCtrl with platform-appropriate style flags (wx.adv vs wx.calendar namespace).</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/datetime.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the calendar control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to CalendarCtrl with platform-appropriate\n            style flags (wx.adv vs wx.calendar namespace).\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    try:\n        CalendarCtrl.__init__(\n            self,\n            parent,\n            style=wx.adv.CAL_MONDAY_FIRST\n            | wx.adv.CAL_SHOW_HOLIDAYS\n            | wx.adv.CAL_SEQUENTIAL_MONTH_SELECTION,\n            **kwds,\n        )\n    except AttributeError:\n        CalendarCtrl.__init__(\n            self,\n            parent,\n            style=wx.calendar.CAL_MONDAY_FIRST\n            | wx.calendar.CAL_SHOW_HOLIDAYS\n            | wx.calendar.CAL_SEQUENTIAL_MONTH_SELECTION,\n            **kwds,\n        )\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CALENDAR-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CHECKBOX","title":"CHECKBOX","text":"<pre><code>CHECKBOX(parent, **kwds)\n</code></pre> <p>               Bases: <code>CheckBox</code>, <code>SchBaseCtrl</code></p> <p>Checkbox control with optional custom value.</p> <p>Handles ctrlcheckbox tag. When the checkbox is checked, GetValue returns either the configured custom value or True. When unchecked, returns None.</p> Tag arguments <p>Initialize the checkbox control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.CheckBox. If 'checked' is present, the checkbox starts checked. If no label is provided, adds ALIGN_RIGHT style.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the checkbox control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.CheckBox. If 'checked' is\n            present, the checkbox starts checked. If no label\n            is provided, adds ALIGN_RIGHT style.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    if \"checked\" in kwds:\n        del kwds[\"checked\"]\n        checked = True\n    else:\n        checked = False\n\n    if self.label:\n        kwds[\"label\"] = self.label\n\n    if \"value\" in kwds:\n        self.value = kwds[\"value\"]\n        del kwds[\"value\"]\n    else:\n        self.value = None\n\n    if not self.label:\n        if \"style\" in kwds:\n            kwds[\"style\"] |= wx.ALIGN_RIGHT\n        else:\n            kwds[\"style\"] = wx.ALIGN_RIGHT\n\n    wx.CheckBox.__init__(self, parent, **kwds)\n    if checked:\n        self.SetValue(True)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CHECKBOX-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CHECKBOX.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Get the current checkbox value.</p> <p>Returns:</p> Type Description <p>If checked and a custom value is set, returns str(value).</p> <p>If checked and no custom value, returns True.</p> <p>If unchecked, returns None.</p> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get the current checkbox value.\n\n    Returns:\n        If checked and a custom value is set, returns str(value).\n        If checked and no custom value, returns True.\n        If unchecked, returns None.\n    \"\"\"\n    checked = wx.CheckBox.GetValue(self)\n    if checked:\n        if self.value:\n            return str(self.value)\n        else:\n            return True\n    else:\n        return None\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CHECKBOX.SetValue","title":"SetValue","text":"<pre><code>SetValue(value)\n</code></pre> <p>Set the checkbox state.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>If bool, sets the check state directly. Otherwise stores as the custom value.</p> required Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def SetValue(self, value):\n    \"\"\"Set the checkbox state.\n\n    Args:\n        value: If bool, sets the check state directly.\n            Otherwise stores as the custom value.\n    \"\"\"\n    if isinstance(value, bool):\n        return wx.CheckBox.SetValue(self, value)\n    else:\n        self.value = value\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CHECKBOX.process_refr_data","title":"process_refr_data","text":"<pre><code>process_refr_data(**kwds)\n</code></pre> <p>Refresh the checkbox with new data.</p> <p>Parameters:</p> Name Type Description Default <code>**kwds</code> <p>New keyword arguments. Supports 'checked' and 'value' keys.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def process_refr_data(self, **kwds):\n    \"\"\"Refresh the checkbox with new data.\n\n    Args:\n        **kwds: New keyword arguments. Supports 'checked'\n            and 'value' keys.\n    \"\"\"\n    self.init_base(kwds)\n    if \"checked\" in kwds:\n        del kwds[\"checked\"]\n        checked = True\n    else:\n        checked = False\n    if \"value\" in kwds:\n        self.value = kwds[\"value\"]\n        del kwds[\"value\"]\n    else:\n        self.value = None\n\n    if checked:\n        self.SetValue(True)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CHECKLIST","title":"CHECKLIST","text":"<pre><code>CHECKLIST(parent, **kwds)\n</code></pre> <p>               Bases: <code>LIST</code></p> <p>List control with checkboxes for each item.</p> <p>Extends LIST to enable item checkboxes. Items can be toggled by activating them (Enter or double-click).</p> Tag arguments <p>Initialize the checklist control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to LIST. Enables checkboxes and binds item activation for toggling.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the checklist control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to LIST. Enables checkboxes and\n            binds item activation for toggling.\n    \"\"\"\n    super().__init__(parent, **kwds)\n    self.EnableCheckBoxes()\n    self.Bind(wx.EVT_LIST_ITEM_ACTIVATED, self._on_item_activated)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CHECKLIST-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CHECKLIST.GetBestSize","title":"GetBestSize","text":"<pre><code>GetBestSize()\n</code></pre> <p>Return the best size for the control.</p> <p>Returns:</p> Type Description <p>Tuple of (400, 48).</p> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def GetBestSize(self):\n    \"\"\"Return the best size for the control.\n\n    Returns:\n        Tuple of (400, 48).\n    \"\"\"\n    return (400, 48)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CHECKLIST.ToggleItem","title":"ToggleItem","text":"<pre><code>ToggleItem(index)\n</code></pre> <p>Toggle the checked state of an item.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <p>Zero-based item index.</p> required Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def ToggleItem(self, index):\n    \"\"\"Toggle the checked state of an item.\n\n    Args:\n        index: Zero-based item index.\n    \"\"\"\n    toggle = not self.IsItemChecked(index)\n    self.CheckItem(index, toggle)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CHECKLISTBOX","title":"CHECKLISTBOX","text":"<pre><code>CHECKLISTBOX(parent, **kwds)\n</code></pre> <p>               Bases: <code>CheckListBox</code>, <code>SchBaseCtrl</code></p> <p>Check list box allowing multiple selections with checkboxes.</p> <p>Handles ctrlchecklistbox tag. Provides a list where each item has an associated checkbox. GetValue returns a list of values for checked items.</p> Tag arguments <p>Initialize the check list box.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.CheckListBox with WANTS_CHARS style.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the check list box.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.CheckListBox with WANTS_CHARS style.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n\n    if \"style\" in kwds:\n        kwds[\"style\"] |= wx.WANTS_CHARS\n    else:\n        kwds[\"style\"] = wx.WANTS_CHARS\n    wx.CheckListBox.__init__(self, parent, **kwds)\n    self._init_data(self.get_tdata())\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CHECKLISTBOX-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CHECKLISTBOX.GetBestSize","title":"GetBestSize","text":"<pre><code>GetBestSize()\n</code></pre> <p>Return the best size for the control.</p> <p>Returns:</p> Type Description <p>Tuple of (300, 68).</p> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def GetBestSize(self):\n    \"\"\"Return the best size for the control.\n\n    Returns:\n        Tuple of (300, 68).\n    \"\"\"\n    return (300, 68)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CHECKLISTBOX.GetMinSize","title":"GetMinSize","text":"<pre><code>GetMinSize()\n</code></pre> <p>Return the minimum size (same as best size).</p> <p>Returns:</p> Type Description <p>Tuple of (300, 68).</p> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def GetMinSize(self):\n    \"\"\"Return the minimum size (same as best size).\n\n    Returns:\n        Tuple of (300, 68).\n    \"\"\"\n    return self.GetBestSize()\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CHECKLISTBOX.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Get values of all checked items.</p> <p>Returns:</p> Type Description <p>List of values for checked items. Uses the 'value'</p> <p>attribute from item attrs if present, otherwise the</p> <p>item's display text.</p> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get values of all checked items.\n\n    Returns:\n        List of values for checked items. Uses the 'value'\n        attribute from item attrs if present, otherwise the\n        item's display text.\n    \"\"\"\n    ret = []\n    data = self.get_tdata()\n    items = self.GetCheckedItems()\n    for item in items:\n        if hasattr(data[item][0], \"attrs\") and \"value\" in data[item][0].attrs:\n            ret.append(data[item][0].attrs[\"value\"])\n        else:\n            ret.append(data[item][0].data)\n    return ret\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CHECKLISTBOX.process_refr_data","title":"process_refr_data","text":"<pre><code>process_refr_data(**kwds)\n</code></pre> <p>Refresh the list with new data.</p> <p>Parameters:</p> Name Type Description Default <code>**kwds</code> <p>New keyword arguments.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def process_refr_data(self, **kwds):\n    \"\"\"Refresh the list with new data.\n\n    Args:\n        **kwds: New keyword arguments.\n    \"\"\"\n    self.init_base(kwds)\n    self.Clear()\n    self._init_data(self.get_tdata())\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CHOICE","title":"CHOICE","text":"<pre><code>CHOICE(parent, **kwds)\n</code></pre> <p>               Bases: <code>POPUPHTML</code></p> <p>Choice control with popup-based selection dialog.</p> <p>Handles ctrlchoice tag. Extends POPUPHTML to provide a dropdown choice with optional extended popup dialog. Supports keyboard accelerator (F2) for the extended dialog.</p> Tag arguments <p>Initialize the choice control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to POPUPHTML. Sets default href to the list dialog if not provided.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the choice control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to POPUPHTML. Sets default href\n            to the list dialog if not provided.\n    \"\"\"\n    if \"href\" not in kwds:\n        kwds[\"href\"] = wx.GetApp().make_href(\"/schsys/listdialog/\")\n\n    if \"style\" in kwds:\n        kwds[\"style\"] = kwds[\"style\"] | wx.WANTS_CHARS\n    else:\n        kwds[\"style\"] = wx.WANTS_CHARS\n\n    if \"size\" not in kwds:\n        kwds[\"size\"] = wx.Size(250, -1)\n    self.height = 250\n    kwds[\"dialog_with_value\"] = False\n    POPUPHTML.__init__(self, parent, **kwds)\n\n    self._init_choices(self.get_tdata())\n\n    accel_table = [\n        (0, wx.WXK_F2, self._on_ext_button_click),\n    ]\n    self.set_acc_key_tab(accel_table)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CHOICE-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CHOICE.GetBestSize","title":"GetBestSize","text":"<pre><code>GetBestSize()\n</code></pre> <p>Return the best size, delegating to POPUPHTML.</p> <p>Returns:</p> Type Description <p>Tuple of (width, height).</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def GetBestSize(self):\n    \"\"\"Return the best size, delegating to POPUPHTML.\n\n    Returns:\n        Tuple of (width, height).\n    \"\"\"\n    dx, dy = POPUPHTML.GetBestSize(self)\n    return (dx, dy)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CHOICE.GetMaxSize","title":"GetMaxSize","text":"<pre><code>GetMaxSize()\n</code></pre> <p>Return the maximum size (same as best size).</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def GetMaxSize(self):\n    \"\"\"Return the maximum size (same as best size).\"\"\"\n    return self.GetBestSize()\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CHOICE.GetMinSize","title":"GetMinSize","text":"<pre><code>GetMinSize()\n</code></pre> <p>Return the minimum size (same as best size).</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def GetMinSize(self):\n    \"\"\"Return the minimum size (same as best size).\"\"\"\n    return self.GetBestSize()\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CHOICE.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Get the current choice value.</p> <p>When readonly, returns the value from the stored record. Otherwise delegates to POPUPHTML.</p> <p>Returns:</p> Type Description <p>The selected value or None.</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get the current choice value.\n\n    When readonly, returns the value from the stored record.\n    Otherwise delegates to POPUPHTML.\n\n    Returns:\n        The selected value or None.\n    \"\"\"\n    if self.readonly:\n        value = self.get_rec()\n        return value[0]\n    else:\n        return POPUPHTML.GetValue(self)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CHOICE.OnButtonClick","title":"OnButtonClick","text":"<pre><code>OnButtonClick()\n</code></pre> <p>Handle the dropdown button click.</p> <p>Opens the popup dialog and populates it with current choices.</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def OnButtonClick(self):\n    \"\"\"Handle the dropdown button click.\n\n    Opens the popup dialog and populates it with current choices.\n    \"\"\"\n    if self.simpleDialog:\n        ret = POPUPHTML.OnButtonClick(self)\n        self.popup.html.body.choices = self.choices\n        wx.CallAfter(self.popup.html.body.refr)\n    else:\n        ret = POPUPHTML.OnButtonClick(self)\n        self.page.body.choices = self.choices\n        wx.CallAfter(self.page.body.refr)\n    return ret\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.COLLAPSIBLE_PANEL","title":"COLLAPSIBLE_PANEL","text":"<pre><code>COLLAPSIBLE_PANEL(parent, **kwds)\n</code></pre> <p>               Bases: <code>CollapsiblePane</code>, <code>SchBaseCtrl</code></p> <p>Collapsible panel that can show/hide HTML content.</p> <p>Handles ctrlcollapsible_panel tag. Content is loaded from param['data'] as HTML and rendered in a SchPage child.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    kwds[\"label\"] = self.label\n    kwds[\"style\"] = wx.CP_DEFAULT_STYLE | wx.CP_NO_TLW_RESIZE\n    if \"size\" in kwds:\n        tmp = kwds[\"size\"]\n        self._size = [tmp[0], tmp[1]]\n        del kwds[\"size\"]\n    else:\n        self._size = [400, 300]\n    self._height = 0\n\n    if self.param and \"collapse_height\" in self.param:\n        self._size[1] = int(self.param[\"collapse_height\"])\n\n    wx.CollapsiblePane.__init__(self, parent, **kwds)\n    try:\n        self.GetPane().SetBackgroundStyle(wx.BG_STYLE_ERASE)\n    except Exception:\n        self.GetPane().SetBackgroundStyle(wx.BG_STYLE_CUSTOM)\n\n    if self.param and \"data\" in self.param and self.param[\"data\"].strip():\n        self.set_html(self.param[\"data\"])\n    else:\n        self.html = None\n        self.SetSize((0, 0))\n        self.Hide()\n    self.Bind(wx.EVT_COLLAPSIBLEPANE_CHANGED, self._on_pane_changed)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.COLLAPSIBLE_PANEL-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.COLLAPSIBLE_PANEL.GetBestSize","title":"GetBestSize","text":"<pre><code>GetBestSize()\n</code></pre> <p>Return best size accounting for expanded content height.</p> <p>Returns:</p> Type Description <p>Tuple of (width, height).</p> Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def GetBestSize(self):\n    \"\"\"Return best size accounting for expanded content height.\n\n    Returns:\n        Tuple of (width, height).\n    \"\"\"\n    ret = wx.CollapsiblePane.GetBestSize(self)\n    if self.IsExpanded():\n        return (ret[0], ret[1] + self._height)\n    elif not self.IsShown():\n        return (0, 0)\n    else:\n        if ret[1] - self._height &gt; 0:\n            return (ret[0], ret[1] - self._height)\n        else:\n            return (ret[0], ret[1])\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.COLLAPSIBLE_PANEL.SetValue","title":"SetValue","text":"<pre><code>SetValue(value)\n</code></pre> <p>Set value (placeholder - HTML is set via set_html).</p> Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def SetValue(self, value):\n    \"\"\"Set value (placeholder - HTML is set via set_html).\"\"\"\n    if not self.html:\n        pass\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.COLLAPSIBLE_PANEL.process_refr_data","title":"process_refr_data","text":"<pre><code>process_refr_data(**kwds)\n</code></pre> <p>Refresh from new data.</p> <p>Parameters:</p> Name Type Description Default <code>**kwds</code> <p>New keyword arguments. If param['data'] is non-empty, shows the panel and sets HTML content.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def process_refr_data(self, **kwds):\n    \"\"\"Refresh from new data.\n\n    Args:\n        **kwds: New keyword arguments. If param['data'] is\n            non-empty, shows the panel and sets HTML content.\n    \"\"\"\n    if \"param\" in kwds:\n        self.param = kwds[\"param\"]\n        if \"data\" in self.param and self.param[\"data\"].strip():\n            self.Show(True)\n            self.set_html(self.param[\"data\"])\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.COLLAPSIBLE_PANEL.set_html","title":"set_html","text":"<pre><code>set_html(html_txt)\n</code></pre> <p>Parse and display HTML content in the pane.</p> <p>Parameters:</p> Name Type Description Default <code>html_txt</code> <p>HTML string to render.</p> required Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def set_html(self, html_txt):\n    \"\"\"Parse and display HTML content in the pane.\n\n    Args:\n        html_txt: HTML string to render.\n    \"\"\"\n    mp = ShtmlParser()\n    mp.process(\n        \"&lt;html&gt;&lt;body&gt;\" + self.param[\"data\"].decode(\"utf-8\") + \"&lt;/body&gt;&lt;/html&gt;\"\n    )\n    mp.address = None\n    if not self.html:\n        self.html = page.SchPage(self.GetPane(), mp, {})\n        self.html.init_frame()\n        self.html.activate_page()\n        width = self._size[0] if self._size[0] &gt; 0 else 400\n        self._height = self._size[1] if self._size[1] &gt; 0 else 300\n        self.html.SetSize((width, self._height))\n        sizer = wx.BoxSizer()\n        sizer.Add(self.html, 1, wx.EXPAND | wx.ALL, 5)\n        self.GetPane().SetSizer(sizer)\n        self._on_pane_changed(None)\n    else:\n        self.html.set_adr_and_param(mp, None)\n        self._on_pane_changed(None)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.COLOURSELECT","title":"COLOURSELECT","text":"<pre><code>COLOURSELECT(parent, **kwds)\n</code></pre> <p>               Bases: <code>ColourSelect</code>, <code>SchBaseCtrl</code></p> <p>Colour selection dropdown.</p> <p>Handles ctrlcolourselect tag.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    if \"name\" in kwds:\n        del kwds[\"name\"]\n    colourselect.ColourSelect.__init__(self, parent, id=-1, **kwds)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CompositePanel","title":"CompositePanel","text":"<pre><code>CompositePanel(parent, **kwds)\n</code></pre> <p>               Bases: <code>Panel</code>, <code>SchBaseCtrl</code></p> <p>Composite panel that wraps a primary control.</p> <p>Used by SELECT2 to embed a Select2Base control alongside optional action buttons in a horizontal layout.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    wx.Panel.__init__(self, parent, **kwds)\n    self.ctrl = None\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CompositePanel-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CompositePanel.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Get value from the wrapped control.</p> <p>Returns:</p> Type Description <p>String value or None.</p> Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get value from the wrapped control.\n\n    Returns:\n        String value or None.\n    \"\"\"\n    if self.ctrl:\n        return str(self.ctrl.GetValue())\n    else:\n        return None\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.CompositePanel.set_main_ctrl","title":"set_main_ctrl","text":"<pre><code>set_main_ctrl(ctrl)\n</code></pre> <p>Set the primary wrapped control.</p> <p>Parameters:</p> Name Type Description Default <code>ctrl</code> <p>The main wx control to embed.</p> required Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def set_main_ctrl(self, ctrl):\n    \"\"\"Set the primary wrapped control.\n\n    Args:\n        ctrl: The main wx control to embed.\n    \"\"\"\n    self.ctrl = ctrl\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.DATEPICKER","title":"DATEPICKER","text":"<pre><code>DATEPICKER(parent, **kwds)\n</code></pre> <p>               Bases: <code>DatePickerCtrl</code>, <code>SchBaseCtrl</code></p> <p>Date picker control (non-Linux: native).</p> <p>Uses the platform's native date picker control with dropdown calendar, century display, and allow-none support.</p> Tag arguments <p>Initialize the native date picker.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to DatePickerCtrl with dropdown, show-century, and allow-none styles.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/datetime.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the native date picker.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to DatePickerCtrl with dropdown,\n            show-century, and allow-none styles.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    kwds[\"size\"] = (120, -1)\n    try:\n        kwds[\"style\"] = (\n            wx.adv.DP_DROPDOWN | wx.adv.DP_SHOWCENTURY | wx.adv.DP_ALLOWNONE\n        )\n    except AttributeError:\n        kwds[\"style\"] = wx.DP_DROPDOWN | wx.DP_SHOWCENTURY | wx.DP_ALLOWNONE\n    wx.adv.DatePickerCtrl.__init__(self, parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.DATEPICKER-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.DATEPICKER.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Get the current date in ISO format.</p> <p>Returns:</p> Type Description <p>Date string in YYYY-MM-DD format, or None.</p> Source code in <code>pytigon_gui/guictrl/input/datetime.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get the current date in ISO format.\n\n    Returns:\n        Date string in YYYY-MM-DD format, or None.\n    \"\"\"\n    value = wx.adv.DatePickerCtrl.GetValue(self)\n    if value:\n        return value.FormatISODate()\n    else:\n        return None\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.DATEPICKER.SetValue","title":"SetValue","text":"<pre><code>SetValue(value)\n</code></pre> <p>Set the date value from a string.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>Date string in parseable format, or None to clear the selection.</p> required Source code in <code>pytigon_gui/guictrl/input/datetime.py</code> <pre><code>def SetValue(self, value):\n    \"\"\"Set the date value from a string.\n\n    Args:\n        value: Date string in parseable format, or None\n            to clear the selection.\n    \"\"\"\n    if value:\n        date = wx.DateTime()\n        date.ParseDate(value)\n        wx.adv.DatePickerCtrl.SetValue(self, date)\n    else:\n        wx.adv.DatePickerCtrl.SetValue(self, None)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.DATETIMEPICKER","title":"DATETIMEPICKER","text":"<pre><code>DATETIMEPICKER(parent, **kwds)\n</code></pre> <p>               Bases: <code>SchBaseCtrl</code></p> <p>Date-time picker control with popup dialog.</p> <p>Handles ctrldatetimepicker tag. Provides combined date and time selection with a masked input in 'YYYY-MM-DD HH:MM' format.</p> Tag arguments <p>Initialize the date-time picker.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to the underlying POPUPHTML control.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/datetime.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the date-time picker.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to the underlying POPUPHTML control.\n    \"\"\"\n    from pytigon_gui.guictrl.display import POPUPHTML\n\n    kwds[\"href\"] = wx.GetApp().make_href(\"/schsys/datedialog/\")\n\n    if \"style\" in kwds:\n        kwds[\"style\"] = kwds[\"style\"] | wx.WANTS_CHARS\n    else:\n        kwds[\"style\"] = wx.WANTS_CHARS\n\n    if \"size\" in kwds:\n        kwds[\"size\"] = wx.Size(200, kwds[\"size\"].height)\n    else:\n        kwds[\"size\"] = wx.Size(200, -1)\n\n    SchBaseCtrl.__init__(self, parent, kwds)\n    POPUPHTML.__init__(self, parent, **kwds)\n\n    self.to_masked(\n        mask=\"####-##-## ##:##\",\n        formatcodes=\"F\",\n        validRegex=r\"\\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}\",\n    )\n\n    if self.value:\n        self.set_rec(self.value, Td(self.value))\n    else:\n        now = datetime.datetime.now().isoformat().replace(\"T\", \" \")[:16]\n        self.set_rec(now, Td(now), False)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.DATETIMEPICKER-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.DATETIMEPICKER.GetBestSize","title":"GetBestSize","text":"<pre><code>GetBestSize()\n</code></pre> <p>Return the best size for the date-time picker.</p> <p>Returns:</p> Type Description <p>Tuple of (180, height).</p> Source code in <code>pytigon_gui/guictrl/input/datetime.py</code> <pre><code>def GetBestSize(self):\n    \"\"\"Return the best size for the date-time picker.\n\n    Returns:\n        Tuple of (180, height).\n    \"\"\"\n    from pytigon_gui.guictrl.display import POPUPHTML\n\n    dx, dy = POPUPHTML.GetBestSize(self)\n    return (180, dy)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.DATETIMEPICKER.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Get the current date-time value.</p> <p>Returns:</p> Type Description <p>Date-time string from the record, or parsed from</p> <p>the text control if the record is empty.</p> Source code in <code>pytigon_gui/guictrl/input/datetime.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get the current date-time value.\n\n    Returns:\n        Date-time string from the record, or parsed from\n        the text control if the record is empty.\n    \"\"\"\n    from pytigon_gui.guictrl.display import POPUPHTML\n\n    value = self.get_rec()\n    value2 = self.GetTextCtrl().GetValue()\n    if value2 and value2[0] != \" \":\n        if value.data:\n            return value.data\n        else:\n            return [\n                datetime.datetime.strptime(value2, \"%Y-%m-%d %H:%M\"),\n            ]\n    return None\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.DATETIMEPICKER.set_rec","title":"set_rec","text":"<pre><code>set_rec(value, value_rec, dismiss=False)\n</code></pre> <p>Set the record value, padding time if needed.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>Date-time string.</p> required <code>value_rec</code> <p>Td record object.</p> required <code>dismiss</code> <p>Whether to dismiss the popup.</p> <code>False</code> <p>Returns:</p> Type Description <p>Result from parent set_rec.</p> Source code in <code>pytigon_gui/guictrl/input/datetime.py</code> <pre><code>def set_rec(self, value, value_rec, dismiss=False):\n    \"\"\"Set the record value, padding time if needed.\n\n    Args:\n        value: Date-time string.\n        value_rec: Td record object.\n        dismiss: Whether to dismiss the popup.\n\n    Returns:\n        Result from parent set_rec.\n    \"\"\"\n    from pytigon_gui.guictrl.display import POPUPHTML\n\n    if len(value) == 16:\n        return POPUPHTML.set_rec(self, value, value_rec, dismiss)\n    elif len(value) == 10:\n        value_rec.data = value_rec.data + \" 00:00\"\n        return POPUPHTML.set_rec(self, value + \" 00:00\", value_rec, dismiss)\n    else:\n        return POPUPHTML.set_rec(self, \"0000.00.00 00:00\", value_rec, dismiss)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.DBCHOICE","title":"DBCHOICE","text":"<pre><code>DBCHOICE(parent, **kwds)\n</code></pre> <p>               Bases: <code>CHOICE</code></p> <p>Database-bound choice control.</p> <p>Handles ctrldbchoice tag. Extends CHOICE to return the 'value' attribute from the selected item's attrs when in readonly mode, making it suitable for database key selection.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the choice control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to POPUPHTML. Sets default href\n            to the list dialog if not provided.\n    \"\"\"\n    if \"href\" not in kwds:\n        kwds[\"href\"] = wx.GetApp().make_href(\"/schsys/listdialog/\")\n\n    if \"style\" in kwds:\n        kwds[\"style\"] = kwds[\"style\"] | wx.WANTS_CHARS\n    else:\n        kwds[\"style\"] = wx.WANTS_CHARS\n\n    if \"size\" not in kwds:\n        kwds[\"size\"] = wx.Size(250, -1)\n    self.height = 250\n    kwds[\"dialog_with_value\"] = False\n    POPUPHTML.__init__(self, parent, **kwds)\n\n    self._init_choices(self.get_tdata())\n\n    accel_table = [\n        (0, wx.WXK_F2, self._on_ext_button_click),\n    ]\n    self.set_acc_key_tab(accel_table)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.DBCHOICE-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.DBCHOICE.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Get the database value of the selected item.</p> <p>When readonly, returns the 'value' attribute from the selected item's attrs dict (typically a database key). Otherwise delegates to POPUPHTML.</p> <p>Returns:</p> Type Description <p>The database value string or None.</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get the database value of the selected item.\n\n    When readonly, returns the 'value' attribute from the\n    selected item's attrs dict (typically a database key).\n    Otherwise delegates to POPUPHTML.\n\n    Returns:\n        The database value string or None.\n    \"\"\"\n    if self.readonly:\n        value = self.get_rec()\n        if value:\n            if hasattr(value, \"attrs\") and \"value\" in value.attrs:\n                return value.attrs[\"value\"]\n            else:\n                return value.data\n        else:\n            return None\n    else:\n        return POPUPHTML.GetValue(self)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.DBCHOICE_EXT","title":"DBCHOICE_EXT","text":"<pre><code>DBCHOICE_EXT(parent, **kwds)\n</code></pre> <p>               Bases: <code>POPUPHTML</code></p> <p>Extended database choice control with read-only style.</p> <p>Handles ctrldbchoice_ext tag. Similar to DBCHOICE but always uses CB_READONLY style. Supports setting values from dicts with 'value', 'title', 'selected' keys, or from '!!'-delimited strings.</p> Tag arguments <p>Initialize the extended database choice.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to POPUPHTML with CB_READONLY style.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the extended database choice.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to POPUPHTML with CB_READONLY style.\n    \"\"\"\n    if \"style\" in kwds:\n        kwds[\"style\"] = kwds[\"style\"] | wx.CB_READONLY\n    else:\n        kwds[\"style\"] = wx.CB_READONLY\n\n    POPUPHTML.__init__(self, parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.DBCHOICE_EXT-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.DBCHOICE_EXT.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Get the current choice value.</p> <p>When readonly, extracts the stored record value. Otherwise delegates to POPUPHTML.</p> <p>Returns:</p> Type Description <p>The selected value or None.</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get the current choice value.\n\n    When readonly, extracts the stored record value.\n    Otherwise delegates to POPUPHTML.\n\n    Returns:\n        The selected value or None.\n    \"\"\"\n    if self.readonly:\n        value = self.get_rec()\n        if isinstance(value, tuple) and len(value) &gt; 0:\n            return value[1][0]\n        if len(value) &gt; 0:\n            return value[0]\n        else:\n            return None\n    else:\n        return POPUPHTML.GetValue(self)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.DBCHOICE_EXT.SetValue","title":"SetValue","text":"<pre><code>SetValue(value)\n</code></pre> <p>Set the choice value.</p> <p>Supports multiple input formats: - dict with 'value', 'title', 'selected' keys - '!!'-delimited string: 'id:name!!' format</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>The value to set, in one of the supported formats.</p> required Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def SetValue(self, value):\n    \"\"\"Set the choice value.\n\n    Supports multiple input formats:\n    - dict with 'value', 'title', 'selected' keys\n    - '!!'-delimited string: 'id:name!!' format\n\n    Args:\n        value: The value to set, in one of the supported formats.\n    \"\"\"\n    if isinstance(value, dict):\n        val = value[\"value\"]\n        title = value[\"title\"]\n        sel = value[\"selected\"]\n        if sel:\n            self.set_rec(val, Td(title, {\"value\": val}), dismiss=False)\n    else:\n        if \"!!\" in value:\n            id_part = value.split(\":\")[0]\n            name = value[len(id_part) + 1 :].replace(\"!!\", \"\")\n            self.set_rec(name, [id_part, name], dismiss=False)\n\n    POPUPHTML.SetValue(self, value)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.DBCHOICE_EXT.get_parm","title":"get_parm","text":"<pre><code>get_parm(parm)\n</code></pre> <p>Get a parameter from the stored record value.</p> <p>Used for URL parameter encoding (base64).</p> <p>Parameters:</p> Name Type Description Default <code>parm</code> <p>Parameter name ('value' supported).</p> required <p>Returns:</p> Type Description <p>Base64-encoded value or None.</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def get_parm(self, parm):\n    \"\"\"Get a parameter from the stored record value.\n\n    Used for URL parameter encoding (base64).\n\n    Args:\n        parm: Parameter name ('value' supported).\n\n    Returns:\n        Base64-encoded value or None.\n    \"\"\"\n    if len(self.rec_value) &gt; 0:\n        id_val = self.rec_value[0]\n        if parm == \"value\":\n            return b64encode(str(id_val).encode(\"utf-8\"))\n        return None\n    else:\n        return None\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.EDITABLELISTBOX","title":"EDITABLELISTBOX","text":"<pre><code>EDITABLELISTBOX(parent, **kwds)\n</code></pre> <p>               Bases: <code>EditableListBox</code>, <code>SchBaseCtrl</code></p> <p>Editable list box with add/remove buttons.</p> <p>Handles ctrleditablelistbox tag.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    wx.adv.EditableListBox.__init__(self, parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.ERRORLIST","title":"ERRORLIST","text":"<pre><code>ERRORLIST(parent, **kwds)\n</code></pre> <p>               Bases: <code>BitmapTextButton</code>, <code>SchBaseCtrl</code></p> <p>Error indicator button with tooltip.</p> <p>Handles ctrlerrorlist tag. Displays an error icon with a tooltip showing the error message from ldata or param['data'].</p> Tag arguments Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    self.bmp = bitmap_from_href(\"client://status/dialog-error.png\", 1)\n    kwds[\"style\"] = wx.NO_BORDER\n    kwds[\"bitmap\"] = self.bmp\n    BitmapTextButton.__init__(self, parent, **kwds)\n    if self.ldata:\n        self.SetToolTip(wx.ToolTip(self.ldata[0][0]))\n    elif self.param and \"data\" in self.param:\n        self.SetToolTip(wx.ToolTip(self.param[\"data\"]))\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.FILEBROWSEBUTTON","title":"FILEBROWSEBUTTON","text":"<pre><code>FILEBROWSEBUTTON(parent, **kwds)\n</code></pre> <p>               Bases: <code>FileBrowseButton</code>, <code>SchBaseCtrl</code></p> <p>File browse button with text field and Browse button.</p> <p>Handles ctrlfilebrowsebutton tag.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    kwds[\"labelText\"] = \"\"\n    kwds[\"buttonText\"] = str(_(\"Browse\"))\n    kwds[\"size\"] = (400, -1)\n    filebrowsebutton.FileBrowseButton.__init__(self, parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.FILEBROWSEBUTTON-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.FILEBROWSEBUTTON.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Return the file path prefixed with '@'.</p> Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def GetValue(self):\n    \"\"\"Return the file path prefixed with '@'.\"\"\"\n    return \"@\" + super(FILEBROWSEBUTTON, self).GetValue()\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.FLOAT","title":"FLOAT","text":"<pre><code>FLOAT(parent, **kwds)\n</code></pre> <p>               Bases: <code>SpinCtrlDouble</code>, <code>SchBaseCtrl</code></p> <p>Floating-point spin control with high precision (6 digits).</p> <p>Handles ctrlfloat tag. Similar to AMOUNT but with 6 decimal digits for scientific/general float input.</p> Tag arguments <p>Initialize the float spin control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.SpinCtrlDouble with increment of 1, 6 decimal digits, and configurable bounds.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/numeric.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the float spin control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.SpinCtrlDouble with increment\n            of 1, 6 decimal digits, and configurable bounds.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    if self.param and \"process_enter\" in self.param:\n        kwds[\"style\"] = wx.TE_PROCESS_ENTER\n    if self.readonly:\n        style = 0\n        if \"style\" in kwds:\n            style = kwds[\"style\"]\n        style = style | wx.TE_READONLY\n        kwds[\"style\"] = style\n\n    kwds[\"inc\"] = 1\n\n    if self.param and \"min\" in self.param:\n        kwds[\"min\"] = float(self.param[\"min\"])\n    else:\n        kwds[\"min\"] = -100000000\n    if self.param and \"max\" in self.param:\n        kwds[\"max\"] = float(self.param[\"max\"])\n    else:\n        kwds[\"max\"] = 100000000\n\n    kwds[\"style\"] = wx.SP_ARROW_KEYS | wx.ALIGN_RIGHT\n\n    wx.SpinCtrlDouble.__init__(self, parent, **kwds)\n    self.SetDigits(6)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.FLOAT-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.GAUGE","title":"GAUGE","text":"<pre><code>GAUGE(parent, **kwds)\n</code></pre> <p>               Bases: <code>Gauge</code>, <code>SchBaseCtrl</code></p> <p>Progress gauge (progress bar) control.</p> <p>Handles ctrlgauge tag. Displays progress as a horizontal bar. Value can be set via process_refr_data.</p> Tag arguments <p>Initialize the gauge control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.Gauge.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/numeric.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the gauge control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.Gauge.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    wx.Gauge.__init__(self, parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.GAUGE-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.GAUGE.process_refr_data","title":"process_refr_data","text":"<pre><code>process_refr_data(**kwds)\n</code></pre> <p>Refresh the gauge with new data.</p> <p>Parameters:</p> Name Type Description Default <code>**kwds</code> <p>New keyword arguments. If 'value' is present in the underlying self.value, updates the gauge.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/numeric.py</code> <pre><code>def process_refr_data(self, **kwds):\n    \"\"\"Refresh the gauge with new data.\n\n    Args:\n        **kwds: New keyword arguments. If 'value' is present\n            in the underlying self.value, updates the gauge.\n    \"\"\"\n    self.init_base(kwds)\n    if self.value:\n        self.SetValue(self.value)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.GENERICDIR","title":"GENERICDIR","text":"<pre><code>GENERICDIR(parent, **kwds)\n</code></pre> <p>               Bases: <code>GenericDirCtrl</code>, <code>SchBaseCtrl</code></p> <p>Directory browser tree.</p> <p>Handles ctrlgenericdir tag.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    wx.GenericDirCtrl.__init__(self, parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.GRID","title":"GRID","text":"<pre><code>GRID(parent, **kwds)\n</code></pre> <p>               Bases: <code>SchTableGrid</code>, <code>SchBaseCtrl</code></p> <p>Data-bound grid connected to a server data proxy.</p> <p>Handles ctrlgrid tag. Uses a DataProxy to fetch and display paginated server data. Supports readonly mode and auto-refresh on parent parameter changes.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    parm = createparm.create_parm(self.src, parent.get_parm_obj())\n    if parm:\n        self.proxy = tabproxy.DataProxy(wx.GetApp().get_http(parent), str(parm[0]))\n        self.proxy.set_address_parm(parm[2])\n    else:\n        self.proxy = tabproxy.DataProxy(wx.GetApp().get_http(parent), str(self.src))\n    table = gridtable_from_proxy.DataSource(self.proxy)\n\n    if self.readonly:\n        kwds[\"typ\"] = self.VIEW\n        table.set_read_only(True)\n    else:\n        table.set_read_only(False)\n\n    super().__init__(table, self.src, parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.GRID-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.GRID.OnSize","title":"OnSize","text":"<pre><code>OnSize(event=None)\n</code></pre> <p>Handle resize event, adjusting height for toolbar.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>wx.SizeEvent or None.</p> <code>None</code> Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def OnSize(self, event=None):\n    \"\"\"Handle resize event, adjusting height for toolbar.\n\n    Args:\n        event: wx.SizeEvent or None.\n    \"\"\"\n    if event is not None:\n        size = event.GetSize()\n        old = self.GetSize()\n        if old[1] != size[1] - 8:\n            self.SetSize((size[0], max(0, size[1] - 8)))\n            self.GetParent().RefrPage()\n        event.Skip()\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.GRID.process_refr_data","title":"process_refr_data","text":"<pre><code>process_refr_data(**kwds)\n</code></pre> <p>Refresh grid with new data source.</p> <p>Parameters:</p> Name Type Description Default <code>**kwds</code> <p>New keyword arguments.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def process_refr_data(self, **kwds):\n    \"\"\"Refresh grid with new data source.\n\n    Args:\n        **kwds: New keyword arguments.\n    \"\"\"\n    self.init_base(kwds)\n\n    parm = createparm.create_parm(self.src, self.GetParent().get_parm_obj())\n    if parm:\n        self.proxy = tabproxy.DataProxy(wx.GetApp().get_http(self), str(parm[0]))\n        self.proxy.SetAddressParm(parm[2])\n    else:\n        self.proxy = tabproxy.DataProxy(wx.GetApp().get_http(self), str(self.src))\n    table = gridtable_from_proxy.DataSource(self.proxy)\n    self.SetTable(table)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.GRID.refr_obj","title":"refr_obj","text":"<pre><code>refr_obj()\n</code></pre> <p>Refresh grid if parent is visible and parameters changed.</p> Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def refr_obj(self):\n    \"\"\"Refresh grid if parent is visible and parameters changed.\"\"\"\n    if self.GetParent().IsShown():\n        parm = createparm.create_parm(self.src, self.GetParent().get_parm_obj())\n        if parm:\n            if self.proxy.set_address_parm(parm[2]):\n                self.GetTable().refresh(False)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.HTML","title":"HTML","text":"<pre><code>HTML(parent, **kwds)\n</code></pre> <p>               Bases: <code>SchPage</code>, <code>SchBaseCtrl</code></p> <p>HTML content panel.</p> <p>Handles ctrlhtml tag. Renders HTML content loaded from a server source or provided inline via param['data'].</p> Tag arguments Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    page.SchPage.__init__(self, parent, self.src, None)\n    if self.src:\n        value = self.load_string_from_server(self.src)\n        self._set_value(value, False)\n    elif self.param and \"data\" in self.param and len(self.param[\"data\"]) &gt; 0:\n        self._set_value(self.param[\"data\"], False)\n    else:\n        self._value = None\n    self.body.Refresh()\n    self.body.Update()\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.HTML-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.HTML.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Return the raw HTML string.</p> Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def GetValue(self):\n    \"\"\"Return the raw HTML string.\"\"\"\n    return self._value\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.HTML.SetValue","title":"SetValue","text":"<pre><code>SetValue(value)\n</code></pre> <p>Set the HTML content value.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>HTML string to display.</p> required Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def SetValue(self, value):\n    \"\"\"Set the HTML content value.\n\n    Args:\n        value: HTML string to display.\n    \"\"\"\n    self._set_value(value)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.HTMLLISTBOX","title":"HTMLLISTBOX","text":"<pre><code>HTMLLISTBOX(parent, **kwds)\n</code></pre> <p>               Bases: <code>VListBox</code>, <code>SchBaseCtrl</code></p> <p>HTML-rendered virtual list box.</p> <p>Handles ctrlhtmllist tag. Uses HtmlViewerParser to render each item as HTML within a virtual scrolling list.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    self.choices = []\n    self.getItemFunct = None\n    self.h = None\n\n    if self.param and \"multiple\" in self.param:\n        style = kwds.get(\"style\", 0)\n        style |= wx.LB_MULTIPLE\n        kwds[\"style\"] = style\n\n    if \"size\" in kwds:\n        size = kwds[\"size\"]\n        if size[1] == -1:\n            kwds[\"size\"] = (size[0], 100)\n\n    tdata = self.get_tdata()\n    if tdata:\n        for row in tdata:\n            if hasattr(row[0], \"attrs\") and \"value\" in row[0].attrs:\n                selected = \"selected\" in row[0].attrs\n                self._append_html(row[0].attrs[\"value\"], selected, row[0].data)\n            else:\n                self._append_html(-1, False, row[0].data)\n\n    wx.VListBox.__init__(self, parent, **kwds)\n    self.SetItemCount(len(self.choices))\n    for i, choice in enumerate(self.choices):\n        if choice[1]:\n            self.SetSelection(i)\n    self.ScrollToRow(0)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.HTMLLISTBOX-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.HTMLLISTBOX.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Return selected item IDs (multi-select only).</p> Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def GetValue(self):\n    \"\"\"Return selected item IDs (multi-select only).\"\"\"\n    ret = []\n    if self.HasMultipleSelection():\n        item, cookie = self.GetFirstSelected()\n        while item != wx.NOT_FOUND:\n            ret.append(self.choices[item][0])\n            item, cookie = self.GetNextSelected(cookie)\n    return ret\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.HTMLLISTBOX.OnDrawItem","title":"OnDrawItem","text":"<pre><code>OnDrawItem(dc, rect, n)\n</code></pre> <p>Draw item n.</p> Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def OnDrawItem(self, dc, rect, n):\n    \"\"\"Draw item n.\"\"\"\n    self._calc_or_draw(n, dc, rect, False)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.HTMLLISTBOX.OnMeasureItem","title":"OnMeasureItem","text":"<pre><code>OnMeasureItem(n)\n</code></pre> <p>Measure item n height.</p> Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def OnMeasureItem(self, n):\n    \"\"\"Measure item n height.\"\"\"\n    dc = wx.ClientDC(self)\n    w, h = self._calc_or_draw(n, dc, None, True)\n    return h\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.HTMLLISTBOX.append_html","title":"append_html","text":"<pre><code>append_html(html_txt)\n</code></pre> <p>Append a plain HTML item.</p> Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def append_html(self, html_txt):\n    \"\"\"Append a plain HTML item.\"\"\"\n    self._append_html(-1, False, html_txt)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.HTMLLISTBOX.append_text","title":"append_text","text":"<pre><code>append_text(txt)\n</code></pre> <p>Append plain text (newlines stripped).</p> Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def append_text(self, txt):\n    \"\"\"Append plain text (newlines stripped).\"\"\"\n    self.append_html(txt.replace(\"\\n\", \"\"))\n    self.SetItemCount(len(self.choices))\n    self.Refresh()\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.HTMLLISTBOX.append_texts","title":"append_texts","text":"<pre><code>append_texts(txt_list)\n</code></pre> <p>Append multiple text items.</p> Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def append_texts(self, txt_list):\n    \"\"\"Append multiple text items.\"\"\"\n    for txt in txt_list:\n        self.append_html(txt.replace(\"\\n\", \"\"))\n    self.SetItemCount(len(self.choices))\n    self.SetSelection(len(self.choices) - 1)\n    self.Refresh()\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.HTMLLISTBOX.process_refr_data","title":"process_refr_data","text":"<pre><code>process_refr_data(**kwds)\n</code></pre> <p>Refresh from new data.</p> Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def process_refr_data(self, **kwds):\n    \"\"\"Refresh from new data.\"\"\"\n    self.init_base(kwds)\n    self.choices = []\n    tdata = self.get_tdata()\n    if tdata:\n        for row in tdata:\n            if hasattr(row[0], \"attrs\") and \"value\" in row[0].attrs:\n                selected = \"selected\" in row[0].attrs\n                self._append_html(row[0].attrs[\"value\"], selected, row[0].data)\n            else:\n                self._append_html(-1, False, row[0].data)\n    self.SetItemCount(len(self.choices))\n    for i, choice in enumerate(self.choices):\n        if choice[1]:\n            self.SetSelection(i)\n    self.ScrollToRow(0)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.HTMLLISTBOX.scroll_to_line","title":"scroll_to_line","text":"<pre><code>scroll_to_line(line_no)\n</code></pre> <p>Scroll to and select line_no.</p> Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def scroll_to_line(self, line_no):\n    \"\"\"Scroll to and select line_no.\"\"\"\n    nr = line_no\n    count = self.GetItemCount()\n    if count &gt; 0:\n        if nr &gt;= count:\n            nr = count - 1\n        if nr &lt; 0:\n            nr = 0\n        self.SetSelection(nr)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.IMAGEBROWSEBUTTON","title":"IMAGEBROWSEBUTTON","text":"<pre><code>IMAGEBROWSEBUTTON(parent, **kwds)\n</code></pre> <p>               Bases: <code>FILEBROWSEBUTTON</code>, <code>SchBaseCtrl</code></p> <p>Image browse button with preview dialog.</p> <p>Handles ctrlimagebrowsebutton tag. Uses ImageDialog for visual image selection.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    kwds[\"labelText\"] = \"\"\n    kwds[\"buttonText\"] = str(_(\"Browse\"))\n    kwds[\"size\"] = (400, -1)\n    filebrowsebutton.FileBrowseButton.__init__(self, parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.IMAGEBROWSEBUTTON-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.IMAGEBROWSEBUTTON.OnBrowse","title":"OnBrowse","text":"<pre><code>OnBrowse(event=None)\n</code></pre> <p>Open the image browser dialog.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>Button event (optional).</p> <code>None</code> Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def OnBrowse(self, event=None):\n    \"\"\"Open the image browser dialog.\n\n    Args:\n        event: Button event (optional).\n    \"\"\"\n    current = self.GetValue()\n    directory = Path(current).parent\n    if Path(current).is_dir():\n        directory = current\n        current = \"\"\n    elif directory and Path(directory).is_dir():\n        current = Path(current).name\n        directory = str(directory)\n    else:\n        directory = self.startDirectory\n        current = \"\"\n\n    dlg = wx.lib.imagebrowser.ImageDialog(self, current)\n    if dlg.ShowModal() == wx.ID_OK:\n        self.SetValue(dlg.GetFile())\n    dlg.Destroy()\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.LIST","title":"LIST","text":"<pre><code>LIST(parent, **kwds)\n</code></pre> <p>               Bases: <code>ListCtrl</code>, <code>SchBaseCtrl</code></p> <p>List control with multiple columns (report view).</p> <p>Handles ctrllist tag. Displays tabular data in report mode with auto-sized columns. First row of tdata provides column headers.</p> Tag arguments <p>Initialize the list control in report mode.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.ListCtrl with LC_REPORT style.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the list control in report mode.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.ListCtrl with LC_REPORT style.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n\n    if \"style\" in kwds:\n        kwds[\"style\"] = kwds[\"style\"] | wx.LC_REPORT\n    else:\n        kwds[\"style\"] = wx.LC_REPORT\n    kwds[\"size\"] = wx.Size(-1, -1)\n\n    wx.ListCtrl.__init__(self, parent, **kwds)\n\n    tdata = self.get_tdata()\n    if tdata:\n        first_row = True\n        num_cols = 0\n        for row in tdata:\n            if first_row:\n                first_row = False\n                num_cols = len(row)\n                for i in range(num_cols):\n                    self.InsertColumn(i, row[i].data)\n            else:\n                index = self.InsertItem(0, row[0].data)\n                for i in range(1, num_cols):\n                    self.SetItem(index, i, row[i].data)\n\n        for i in range(num_cols):\n            self.SetColumnWidth(i, -1)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.LIST-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.LIST.process_refr_data","title":"process_refr_data","text":"<pre><code>process_refr_data(**kwds)\n</code></pre> <p>Refresh the list with new data.</p> <p>Parameters:</p> Name Type Description Default <code>**kwds</code> <p>New keyword arguments.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def process_refr_data(self, **kwds):\n    \"\"\"Refresh the list with new data.\n\n    Args:\n        **kwds: New keyword arguments.\n    \"\"\"\n    self.init_base(kwds)\n    self.ClearAll()\n    tdata = self.get_tdata()\n    if tdata:\n        first_row = True\n        num_cols = 0\n        for row in tdata:\n            if first_row:\n                first_row = False\n                num_cols = len(row)\n                for i in range(num_cols):\n                    self.InsertColumn(i, row[i].data)\n            else:\n                index = self.InsertStringItem(0, row[0].data)\n                for i in range(1, num_cols):\n                    self.SetStringItem(index, i, row[i].data)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.LISTBOX","title":"LISTBOX","text":"<pre><code>LISTBOX(parent, **kwds)\n</code></pre> <p>               Bases: <code>ListBox</code>, <code>SchBaseCtrl</code></p> <p>Standard list box for single or multiple selection.</p> <p>Handles ctrllistbox tag. Supports multiple selection mode via the 'multiple' parameter. Tracks selected items and their associated values.</p> Tag arguments <p>Initialize the list box.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.ListBox. Enables LB_MULTIPLE if 'multiple' is in the param dict.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the list box.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.ListBox. Enables LB_MULTIPLE\n            if 'multiple' is in the param dict.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    self._norefresh = False\n\n    if self.param and \"multiple\" in self.param:\n        style = 0\n        if \"style\" in kwds:\n            style = kwds[\"style\"]\n        style = style | wx.LB_MULTIPLE\n        kwds[\"style\"] = style\n\n    wx.ListBox.__init__(self, parent, **kwds)\n\n    self._init_choices(self.get_tdata())\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.LISTBOX-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.LISTBOX.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Get values of all selected items.</p> <p>Returns:</p> Type Description <p>List of values corresponding to selected items.</p> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get values of all selected items.\n\n    Returns:\n        List of values corresponding to selected items.\n    \"\"\"\n    ret = []\n    sel = self.GetSelections()\n    for s in sel:\n        ret.append(self.choices[s])\n    return ret\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.LISTBOX.block_refresh","title":"block_refresh","text":"<pre><code>block_refresh()\n</code></pre> <p>Prevent automatic refresh on process_refr_data calls.</p> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def block_refresh(self):\n    \"\"\"Prevent automatic refresh on process_refr_data calls.\"\"\"\n    self._norefresh = True\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.LISTBOX.find_and_select","title":"find_and_select","text":"<pre><code>find_and_select(item)\n</code></pre> <p>Find the first occurrence of item and select it.</p> <p>Matches items that start with the given string.</p> <p>Parameters:</p> Name Type Description Default <code>item</code> <p>String prefix to search for.</p> required Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def find_and_select(self, item):\n    \"\"\"Find the first occurrence of item and select it.\n\n    Matches items that start with the given string.\n\n    Args:\n        item: String prefix to search for.\n    \"\"\"\n    strings = self.GetStrings()\n    for i, s in enumerate(strings):\n        if s.startswith(item):\n            self.SetSelection(i)\n            return\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.LISTBOX.process_refr_data","title":"process_refr_data","text":"<pre><code>process_refr_data(**kwds)\n</code></pre> <p>Refresh the list with new data.</p> <p>Parameters:</p> Name Type Description Default <code>**kwds</code> <p>New keyword arguments. Skips refresh if _norefresh is True.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def process_refr_data(self, **kwds):\n    \"\"\"Refresh the list with new data.\n\n    Args:\n        **kwds: New keyword arguments. Skips refresh if\n            _norefresh is True.\n    \"\"\"\n    self.init_base(kwds)\n    if not self._norefresh:\n        self.Clear()\n        self.refresh_tdata()\n        self._init_choices(self.get_tdata())\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.MASKTEXT","title":"MASKTEXT","text":"<pre><code>MASKTEXT(parent, **kwds)\n</code></pre> <p>               Bases: <code>TextCtrl</code>, <code>SchBaseCtrl</code></p> <p>Masked text input control with format validation.</p> <p>Handles ctrlmasktext tag. Supports mask patterns and autoformat strings via the 'src' attribute. When src starts with '!', it is treated as an autoformat string; otherwise it is used as a mask pattern.</p> Tag arguments <p>Initialize the masked text control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to masked.TextCtrl. If valuetype and src are set, configures mask or autoformat.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/text.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the masked text control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to masked.TextCtrl. If valuetype\n            and src are set, configures mask or autoformat.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    if self.valuetype:\n        if self.src:\n            if self.src.startswith(\"!\"):\n                kwds[\"autoformat\"] = self.src[1:]\n            else:\n                kwds[\"mask\"] = self.src\n    masked.TextCtrl.__init__(self, parent, **kwds)\n    self._autofit = False\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.MASKTEXT-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.NOTEBOOK","title":"NOTEBOOK","text":"<pre><code>NOTEBOOK(parent, **kwds)\n</code></pre> <p>               Bases: <code>Notebook</code>, <code>SchBaseCtrl</code></p> <p>Tabbed notebook container.</p> <p>Handles ctrlnotebook tag. Each row in tdata creates a tab page: row[0].data is the tab label, row[1].data is the content href.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    self.children = []\n    wx.Notebook.__init__(self, parent, **kwds)\n    if self.tdata:\n        for row in self.tdata:\n            h = page.SchPage(self, row[1].data, {})\n            self.AddPage(h, row[0].data)\n            self.children.append(h)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.NUM","title":"NUM","text":"<pre><code>NUM(parent, **kwds)\n</code></pre> <p>               Bases: <code>SpinCtrl</code>, <code>SchBaseCtrl</code></p> <p>Numeric spin control with optional readonly and process_enter.</p> <p>Handles ctrlnum tag. Similar to SPIN but supports process_enter key handling and readonly mode via TE_READONLY style.</p> Tag arguments <p>Initialize the numeric spin control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.SpinCtrl with optional TE_PROCESS_ENTER and TE_READONLY styles.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/numeric.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the numeric spin control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.SpinCtrl with optional\n            TE_PROCESS_ENTER and TE_READONLY styles.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    if self.param and \"process_enter\" in self.param:\n        kwds[\"style\"] = wx.TE_PROCESS_ENTER\n    if self.readonly:\n        style = 0\n        if \"style\" in kwds:\n            style = kwds[\"style\"]\n        style = style | wx.TE_READONLY\n        kwds[\"style\"] = style\n    wx.SpinCtrl.__init__(self, parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.NUM-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.PASSWORD","title":"PASSWORD","text":"<pre><code>PASSWORD(parent, **kwds)\n</code></pre> <p>               Bases: <code>TEXT</code>, <code>SchBaseCtrl</code></p> <p>Password entry control with masked input.</p> <p>Handles ctrlpassword tag. Inherits from TEXT but adds the wx.TE_PASSWORD style for masked character display.</p> Tag arguments <p>Initialize the password control with masked style.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to TEXT after adding TE_PASSWORD style.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/text.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the password control with masked style.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to TEXT after adding TE_PASSWORD style.\n    \"\"\"\n    if \"style\" in kwds:\n        kwds[\"style\"] = kwds[\"style\"] | wx.TE_PASSWORD\n    else:\n        kwds[\"style\"] = wx.TE_PASSWORD\n\n    TEXT.__init__(self, parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.PASSWORD-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.POPUPHTML","title":"POPUPHTML","text":"<pre><code>POPUPHTML(parent, **kwds)\n</code></pre> <p>               Bases: <code>DataPopupControl</code>, <code>SchBaseCtrl</code></p> <p>Popup HTML control with data-driven dialog.</p> <p>Handles ctrlpopuphtml tag. A text field with a dropdown button that opens an HTML popup for selection. Content is loaded from an href endpoint.</p> Tag arguments The href base address schema <p>href + \"dialog\"       - address of dialog window href + \"test?value=\"  - address for autocomplete records</p> Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    DataPopupControl.__init__(self, parent, **kwds)\n    if self.param and \"IN_NEW_WIN\" in self.param:\n        self.simpleDialog = False\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.RADIOBOX","title":"RADIOBOX","text":"<pre><code>RADIOBOX(parent, **kwds)\n</code></pre> <p>               Bases: <code>RadioBox</code>, <code>SchBaseCtrl</code></p> <p>Radio box with a group of mutually exclusive choices.</p> <p>Handles ctrlradiobox tag. Choices are populated from tdata.</p> Tag arguments <p>Initialize the radio box.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.RadioBox with 'choices' populated from tdata.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the radio box.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.RadioBox with 'choices'\n            populated from tdata.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    tdata = self.get_tdata()\n    if tdata:\n        choices = []\n        for row in tdata:\n            choices.append(row[0].data)\n        kwds[\"choices\"] = choices\n    wx.RadioBox.__init__(self, parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.RADIOBOX-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.RADIOBOX.process_refr_data","title":"process_refr_data","text":"<pre><code>process_refr_data(**kwds)\n</code></pre> <p>Refresh the radio box with new choices.</p> <p>Parameters:</p> Name Type Description Default <code>**kwds</code> <p>New keyword arguments.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def process_refr_data(self, **kwds):\n    \"\"\"Refresh the radio box with new choices.\n\n    Args:\n        **kwds: New keyword arguments.\n    \"\"\"\n    self.init_base(kwds)\n    self.Clear()\n    self.refresh_tdata()\n    tdata = self.get_tdata()\n    if tdata:\n        for row in tdata:\n            self.Append(row[0].data)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.RADIOBUTTON","title":"RADIOBUTTON","text":"<pre><code>RADIOBUTTON(parent, **kwds)\n</code></pre> <p>               Bases: <code>RadioButton</code>, <code>SchBaseCtrl</code></p> <p>Individual radio button for use in radio groups.</p> <p>Handles ctrlradiobutton tag. The first button with a given name prefix automatically gets the RB_GROUP style to start a new radio group.</p> Tag arguments <p>Initialize the radio button.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.RadioButton. Adds RB_GROUP style if this is the first button with this name prefix (before '__').</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the radio button.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.RadioButton. Adds RB_GROUP\n            style if this is the first button with this name\n            prefix (before '__').\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    if \"name\" in kwds:\n        name = kwds[\"name\"]\n        # If no sibling with same name group exists, make this the group start\n        if not hasattr(parent, name.split(\"__\")[0]):\n            if \"style\" in kwds:\n                kwds[\"style\"] |= wx.RB_GROUP\n            else:\n                kwds[\"style\"] = wx.RB_GROUP\n\n    if \"checked\" in kwds:\n        del kwds[\"checked\"]\n        checked = True\n    else:\n        checked = False\n    if \"value\" in kwds:\n        self.value = kwds[\"value\"]\n        del kwds[\"value\"]\n    else:\n        self.value = None\n\n    wx.RadioButton.__init__(self, parent, **kwds)\n    if checked:\n        self.SetValue(True)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.RADIOBUTTON-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.RADIOBUTTON.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Get the radio button value.</p> <p>Returns:</p> Type Description <p>If selected and custom value is set, returns str(value).</p> <p>If selected without custom value, returns None.</p> <p>If not selected, returns None.</p> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get the radio button value.\n\n    Returns:\n        If selected and custom value is set, returns str(value).\n        If selected without custom value, returns None.\n        If not selected, returns None.\n    \"\"\"\n    selected = wx.RadioButton.GetValue(self)\n    if selected:\n        if self.value:\n            return str(self.value)\n        else:\n            return None\n    else:\n        return None\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.RADIOBUTTON.SetValue","title":"SetValue","text":"<pre><code>SetValue(value)\n</code></pre> <p>Set the radio button state.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>If bool, sets the selection state directly. Otherwise stores as the custom value.</p> required Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def SetValue(self, value):\n    \"\"\"Set the radio button state.\n\n    Args:\n        value: If bool, sets the selection state directly.\n            Otherwise stores as the custom value.\n    \"\"\"\n    if isinstance(value, bool):\n        return wx.RadioButton.SetValue(self, value)\n    else:\n        self.value = value\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SEARCH","title":"SEARCH","text":"<pre><code>SEARCH(parent, **kwds)\n</code></pre> <p>               Bases: <code>SearchCtrl</code>, <code>SchBaseCtrl</code></p> <p>Search text control with platform-specific sizing.</p> <p>Handles ctrlsearch tag. Wraps wx.SearchCtrl with adjusted default sizes for GTK and other platforms. Binds key-down events on the internal text control for GTK and MSW platforms.</p> Tag arguments <p>Initialize the search control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.SearchCtrl with platform-adjusted size and optional TE_PROCESS_ENTER style.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/text.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the search control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.SearchCtrl with platform-adjusted\n            size and optional TE_PROCESS_ENTER style.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    if wx.Platform in (\"__WXGTK__\",):\n        kwds[\"size\"] = (200, 28)\n    else:\n        kwds[\"size\"] = (200, -1)\n    if self.param and \"process_enter\" in self.param:\n        kwds[\"style\"] = wx.TE_PROCESS_ENTER\n\n    wx.SearchCtrl.__init__(self, parent, **kwds)\n\n    # Bind key-down on the internal TextCtrl child for GTK/MSW\n    if wx.Platform in (\"__WXGTK__\", \"__WXMSW__\"):\n        for child in list(self.GetChildren()):\n            if isinstance(child, wx.TextCtrl):\n                child.Bind(wx.EVT_KEY_DOWN, self.on_key_down_base)\n                break\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SEARCH-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SLIDER","title":"SLIDER","text":"<pre><code>SLIDER(parent, **kwds)\n</code></pre> <p>               Bases: <code>Slider</code>, <code>SchBaseCtrl</code></p> <p>Slider control for selecting a value from a range.</p> <p>Handles ctrlslider tag. Thin wrapper around wx.Slider.</p> Tag arguments <p>Initialize the slider control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.Slider.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/numeric.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the slider control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.Slider.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    wx.Slider.__init__(self, parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SLIDER-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SPIN","title":"SPIN","text":"<pre><code>SPIN(parent, **kwds)\n</code></pre> <p>               Bases: <code>SpinCtrl</code>, <code>SchBaseCtrl</code></p> <p>Integer spin control.</p> <p>Handles ctrlspinbutton tag. Wraps wx.SpinCtrl for integer value selection with up/down arrows.</p> Tag arguments <p>Initialize the integer spin control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.SpinCtrl.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/numeric.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the integer spin control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.SpinCtrl.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    wx.SpinCtrl.__init__(self, parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SPIN-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.STATICTEXT","title":"STATICTEXT","text":"<pre><code>STATICTEXT(parent, **kwds)\n</code></pre> <p>               Bases: <code>StaticText</code>, <code>SchBaseCtrl</code></p> <p>Static non-editable text label.</p> <p>Handles ctrlstatictext tag. Displays read-only text.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    wx.StaticText.__init__(self, parent, **kwds)\n    if self.label:\n        self.SetLabel(self.label)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.STYLEDTEXT","title":"STYLEDTEXT","text":"<pre><code>STYLEDTEXT(parent, **kwds)\n</code></pre> <p>               Bases: <code>TextCtrl</code>, <code>SchBaseCtrl</code></p> <p>Multi-line styled text control.</p> <p>Handles ctrlstyledtext tag. Always uses wx.TE_MULTILINE style. Supports initial data from the 'data' parameter.</p> Tag arguments <p>Initialize the styled (multi-line) text control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.TextCtrl with TE_MULTILINE style.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/text.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the styled (multi-line) text control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.TextCtrl with TE_MULTILINE style.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    if \"style\" in kwds:\n        style = kwds[\"style\"]\n        style = style | wx.TE_MULTILINE\n        kwds[\"style\"] = style\n    else:\n        kwds[\"style\"] = wx.TE_MULTILINE\n    wx.TextCtrl.__init__(self, parent, **kwds)\n    if self.param and \"data\" in self.param:\n        self.SetValue(self.param[\"data\"])\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.STYLEDTEXT-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.STYLEDTEXT.SetValue","title":"SetValue","text":"<pre><code>SetValue(value)\n</code></pre> <p>Set the text value, stripping a leading newline if present.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>String or bytes value to set. A single leading newline is stripped for cleaner display.</p> required Source code in <code>pytigon_gui/guictrl/input/text.py</code> <pre><code>def SetValue(self, value):\n    \"\"\"Set the text value, stripping a leading newline if present.\n\n    Args:\n        value: String or bytes value to set. A single leading\n            newline is stripped for cleaner display.\n    \"\"\"\n    if value.startswith(\"\\n\"):\n        value2 = value[1:]\n    else:\n        value2 = value\n    if isinstance(value2, str):\n        return wx.TextCtrl.SetValue(self, value2)\n    else:\n        return wx.TextCtrl.SetValue(self, value2.decode(\"utf-8\"))\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SchBaseCtrl","title":"SchBaseCtrl","text":"<pre><code>SchBaseCtrl(parent, kwds)\n</code></pre> <p>Base mixin class for all SchForm wxPython widgets.</p> <p>Provides: - Attribute initialization from HTML tag keyword arguments - Parent form/page navigation - Server data loading for tdata (table data) and ldata (list data) - Common keyboard handling (Escape, Tab) - Widget lifecycle: after_create signal, unique name management</p> <p>Initialize base control attributes.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent wxPython window.</p> required <code>kwds</code> <p>Keyword argument dict parsed from HTML tag attributes.</p> required Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def __init__(self, parent, kwds):\n    \"\"\"Initialize base control attributes.\n\n    Args:\n        parent: Parent wxPython window.\n        kwds: Keyword argument dict parsed from HTML tag attributes.\n    \"\"\"\n    self.unique_name = None\n    self.init_base(kwds, parent)\n    self.accept_focus = True\n    self.acc_tab_bind = False\n    form = self.get_parent_form()\n    if form:\n        form.signal_from_child(self, \"__init__\")\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SchBaseCtrl-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SchBaseCtrl.CanAcceptFocus","title":"CanAcceptFocus","text":"<pre><code>CanAcceptFocus()\n</code></pre> <p>Return True if the control can accept keyboard focus.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def CanAcceptFocus(self):\n    \"\"\"Return True if the control can accept keyboard focus.\"\"\"\n    if self.IsShown() and self.IsEnabled():\n        return self.accept_focus\n    return False\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SchBaseCtrl.CanAcceptFocusFromKeyboard","title":"CanAcceptFocusFromKeyboard","text":"<pre><code>CanAcceptFocusFromKeyboard()\n</code></pre> <p>Return True if the control accepts focus from keyboard navigation.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def CanAcceptFocusFromKeyboard(self):\n    \"\"\"Return True if the control accepts focus from keyboard navigation.\"\"\"\n    return self.CanAcceptFocus()\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SchBaseCtrl.after_create","title":"after_create","text":"<pre><code>after_create()\n</code></pre> <p>Called immediately after the widget is fully constructed.</p> <p>Binds keyboard events, executes the 'onload' script (if any), calls the ext_init hook if present, and signals the parent form that this child is ready.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def after_create(self):\n    \"\"\"Called immediately after the widget is fully constructed.\n\n    Binds keyboard events, executes the 'onload' script (if any),\n    calls the __ext_init__ hook if present, and signals the parent\n    form that this child is ready.\n    \"\"\"\n    self.Bind(wx.EVT_KEY_DOWN, self.on_key_down_base)\n    if self.onload:\n        d = {\"wx\": wx, \"self\": self}\n        try:\n            exec(self.onload, d)\n        except Exception:\n            logger.exception(\"Error executing onload script for %s\", self.GetName())\n\n    if hasattr(self, \"__ext_init__\"):\n        self.__ext_init__()\n\n    self.get_parent_form().signal_from_child(self, \"after_create\")\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SchBaseCtrl.get_ldata","title":"get_ldata","text":"<pre><code>get_ldata()\n</code></pre> <p>Return hierarchical list data, loading from server if necessary.</p> <p>Returns:</p> Type Description <p>List data (nested tree structure), or None if unavailable.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_ldata(self):\n    \"\"\"Return hierarchical list data, loading from server if necessary.\n\n    Returns:\n        List data (nested tree structure), or None if unavailable.\n    \"\"\"\n    if self.ldata is not None:\n        return self.ldata\n    if self.ldatabuf is not None:\n        return self.ldatabuf\n    if self.src:\n        self.refresh_ldata()\n    return self.ldatabuf\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SchBaseCtrl.get_parent_form","title":"get_parent_form","text":"<pre><code>get_parent_form()\n</code></pre> <p>Walk up the widget hierarchy to find the parent SchForm.</p> <p>Returns:</p> Type Description <p>The SchForm ancestor, or None if not found.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_parent_form(self):\n    \"\"\"Walk up the widget hierarchy to find the parent SchForm.\n\n    Returns:\n        The SchForm ancestor, or None if not found.\n    \"\"\"\n    parent = self.parent\n    while parent is not None:\n        if type(parent).__name__ == \"SchForm\":\n            return parent\n        parent = parent.GetParent()\n    return None\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SchBaseCtrl.get_parent_page","title":"get_parent_page","text":"<pre><code>get_parent_page()\n</code></pre> <p>Return the parent page that contains this control's form.</p> <p>Returns:</p> Type Description <p>The parent SchPage, or None if not found.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_parent_page(self):\n    \"\"\"Return the parent page that contains this control's form.\n\n    Returns:\n        The parent SchPage, or None if not found.\n    \"\"\"\n    form = self.get_parent_form()\n    if form:\n        return form.get_parent_page()\n    return None\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SchBaseCtrl.get_tdata","title":"get_tdata","text":"<pre><code>get_tdata()\n</code></pre> <p>Return table data, loading from server if necessary.</p> <p>Returns:</p> Type Description <p>Table data (list of rows), or None if unavailable.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_tdata(self):\n    \"\"\"Return table data, loading from server if necessary.\n\n    Returns:\n        Table data (list of rows), or None if unavailable.\n    \"\"\"\n    if self.tdata is not None:\n        return self.tdata\n    if self.tdatabuf is not None:\n        return self.tdatabuf\n    if self.src:\n        self.refresh_tdata()\n    return self.tdatabuf\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SchBaseCtrl.get_unique_name","title":"get_unique_name","text":"<pre><code>get_unique_name()\n</code></pre> <p>Return the unique name identifier.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_unique_name(self):\n    \"\"\"Return the unique name identifier.\"\"\"\n    return self.unique_name\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SchBaseCtrl.init_base","title":"init_base","text":"<pre><code>init_base(kwds, parent=None)\n</code></pre> <p>Initialize all control attributes from kwds.</p> <p>Extracts well-known attributes (href, label, id, target, value, valuetype, defaultvalue, length, maxlength, readonly, hidden, src, onload, tdata, ldata, data, param, style) from kwds and assigns them as instance attributes.</p> <p>Parameters:</p> Name Type Description Default <code>kwds</code> <p>Keyword argument dict parsed from HTML tag attributes.</p> required <code>parent</code> <p>Optional parent window reference.</p> <code>None</code> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def init_base(self, kwds, parent=None):\n    \"\"\"Initialize all control attributes from kwds.\n\n    Extracts well-known attributes (href, label, id, target, value,\n    valuetype, defaultvalue, length, maxlength, readonly, hidden,\n    src, onload, tdata, ldata, data, param, style) from kwds\n    and assigns them as instance attributes.\n\n    Args:\n        kwds: Keyword argument dict parsed from HTML tag attributes.\n        parent: Optional parent window reference.\n    \"\"\"\n    if parent:\n        self.parent = parent\n\n    self.ldatabuf = None\n\n    self.tag = None\n    if \"param\" in kwds and \"tag\" in kwds[\"param\"]:\n        self.tag = kwds[\"param\"][\"tag\"]\n\n    self.href = _extract_kwarg(kwds, \"href\", None)\n    if self.href is None:\n        self.href = \"\"\n\n    self.label = _extract_kwarg(kwds, \"label\", None)\n    self.nr_id = _extract_kwarg(kwds, \"id\", None)\n    self.target = _extract_kwarg(kwds, \"target\", \"_blank\")\n    self.value = _extract_kwarg(kwds, \"value\", None)\n    self.valuetype = _extract_kwarg(kwds, \"valuetype\", \"data\")\n    self.defaultvalue = _extract_kwarg(kwds, \"defaultvalue\", None)\n    self.length = int(_extract_kwarg(kwds, \"length\", 0))\n    self.maxlength = int(_extract_kwarg(kwds, \"maxlength\", 0))\n    self.readonly = _extract_kwarg(kwds, \"readonly\", False)\n    self.hidden = True if _extract_kwarg(kwds, \"hidden\", False) else False\n    self.src = _extract_kwarg(kwds, \"src\", None)\n    self.onload = _extract_kwarg(kwds, \"onload\", None)\n\n    # tdata / tdatabuf: table data (already parsed) or lazily loaded\n    self.tdata = _extract_kwarg(kwds, \"tdata\", None)\n    self.tdatabuf = None\n    if self.tdata is not None:\n        self.tdatabuf = None\n\n    # ldata: hierarchical list data (already parsed) or lazily loaded\n    self.ldata = _extract_kwarg(kwds, \"ldata\", None)\n\n    # data: URL-encoded string data\n    raw_data = _extract_kwarg(kwds, \"data\", None)\n    self.data = unquote(raw_data) if raw_data is not None else None\n\n    self.param = _extract_kwarg(kwds, \"param\", None)\n    self.style = _extract_kwarg(kwds, \"style\", None)\n\n    # Invoke any registered ctrl_process hooks for this tag type\n    ctrl_process = wx.GetApp().ctrl_process\n    if self.tag in ctrl_process:\n        for fun in ctrl_process[self.tag]:\n            fun(self)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SchBaseCtrl.is_ctrl_block","title":"is_ctrl_block","text":"<pre><code>is_ctrl_block()\n</code></pre> <p>Return True if this control is a block-level element.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def is_ctrl_block(self):\n    \"\"\"Return True if this control is a block-level element.\"\"\"\n    return self.GetParent().is_ctrl_block(self)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SchBaseCtrl.load_data_from_server","title":"load_data_from_server","text":"<pre><code>load_data_from_server(path)\n</code></pre> <p>Load raw binary data from a server URL.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <p>URL path to fetch.</p> required <p>Returns:</p> Type Description <p>Raw response data (bytes).</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def load_data_from_server(self, path):\n    \"\"\"Load raw binary data from a server URL.\n\n    Args:\n        path: URL path to fetch.\n\n    Returns:\n        Raw response data (bytes).\n    \"\"\"\n    http = wx.GetApp().get_http(self.parent)\n    response = http.get(self, str(path))\n    return response.ptr()\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SchBaseCtrl.load_string_from_server","title":"load_string_from_server","text":"<pre><code>load_string_from_server(path)\n</code></pre> <p>Load a text response from a server URL.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <p>URL path to fetch.</p> required <p>Returns:</p> Type Description <p>Response body as a string.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def load_string_from_server(self, path):\n    \"\"\"Load a text response from a server URL.\n\n    Args:\n        path: URL path to fetch.\n\n    Returns:\n        Response body as a string.\n    \"\"\"\n    http = wx.GetApp().get_http(self.parent)\n    response = http.get(self, str(path))\n    return response.str()\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SchBaseCtrl.on_key_down_base","title":"on_key_down_base","text":"<pre><code>on_key_down_base(event)\n</code></pre> <p>Default key-down handler for Escape and Tab navigation.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>wx.KeyEvent.</p> required Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def on_key_down_base(self, event):\n    \"\"\"Default key-down handler for Escape and Tab navigation.\n\n    Args:\n        event: wx.KeyEvent.\n    \"\"\"\n    if event.GetKeyCode() == wx.WXK_ESCAPE:\n        wx.CallAfter(self.GetParent().cancel)\n    elif event.GetKeyCode() == wx.WXK_TAB:\n        if event.ShiftDown():\n            self.GetParent().Navigate(self, True)\n        else:\n            self.GetParent().Navigate(self, False)\n    else:\n        event.Skip()\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SchBaseCtrl.refresh_ldata","title":"refresh_ldata","text":"<pre><code>refresh_ldata()\n</code></pre> <p>Refresh hierarchical list data from the server.</p> <p>Loads from self.src and parses into self.ldatabuf using TreeParser.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def refresh_ldata(self):\n    \"\"\"Refresh hierarchical list data from the server.\n\n    Loads from self.src and parses into self.ldatabuf using TreeParser.\n    \"\"\"\n    if self.ldata is None and self.src:\n        lista = self.load_data_from_server(self.src)\n        mp = TreeParser()\n        mp.feed(lista)\n        mp.close()\n        self.ldatabuf = mp.tree_parent[0][1]\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SchBaseCtrl.refresh_tdata","title":"refresh_tdata","text":"<pre><code>refresh_tdata(html_src=None)\n</code></pre> <p>Refresh table data from the server or provided HTML source.</p> <p>Parameters:</p> Name Type Description Default <code>html_src</code> <p>Optional HTML string containing table data. If None, loads from self.src.</p> <code>None</code> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def refresh_tdata(self, html_src=None):\n    \"\"\"Refresh table data from the server or provided HTML source.\n\n    Args:\n        html_src: Optional HTML string containing table data.\n            If None, loads from self.src.\n    \"\"\"\n    if self.tdata is None and self.src:\n        if html_src:\n            tables = html_src\n        else:\n            tables = self.load_data_from_server(self.src).decode(\"utf-8\")\n        self.tdatabuf = tdata_from_html(tables, wx.GetApp().http)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SchBaseCtrl.set_acc_key_tab","title":"set_acc_key_tab","text":"<pre><code>set_acc_key_tab(tab)\n</code></pre> <p>Register an accelerator table for this widget.</p> <p>Parameters:</p> Name Type Description Default <code>tab</code> <p>List or tuple of accelerator elements. Each element has structure: (flag, keycode, callback function).</p> required <p>Returns:</p> Type Description <p>Result from the parent's set_acc_key_tab method.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def set_acc_key_tab(self, tab):\n    \"\"\"Register an accelerator table for this widget.\n\n    Args:\n        tab: List or tuple of accelerator elements. Each element has\n            structure: (flag, keycode, callback function).\n\n    Returns:\n        Result from the parent's set_acc_key_tab method.\n    \"\"\"\n    return self.GetParent().set_acc_key_tab(self, tab)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SchBaseCtrl.set_unique_name","title":"set_unique_name","text":"<pre><code>set_unique_name(name)\n</code></pre> <p>Assign a unique name identifier to this control.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def set_unique_name(self, name):\n    \"\"\"Assign a unique name identifier to this control.\"\"\"\n    self.unique_name = name\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.TABLE","title":"TABLE","text":"<pre><code>TABLE(parent, **kwds)\n</code></pre> <p>               Bases: <code>SchGridPanel</code>, <code>SchBaseCtrl</code></p> <p>Table grid panel for viewing/editing HTML table data.</p> <p>Handles ctrltable tag. Renders data from tdata as a grid with toolbar actions. Supports row insert/update/delete signals and server-side data refresh.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n\n    if self.param and \"table_lp\" in self.param:\n        self._table_lp = int(self.param[\"table_lp\"])\n    else:\n        self._table_lp = 0\n\n    name = kwds.get(\"name\", \"LIST\")\n\n    if \"size\" in kwds:\n        SchGridPanel.__init__(self, parent, size=kwds[\"size\"], name=name)\n    else:\n        SchGridPanel.__init__(self, parent, name=name)\n\n    tdata = self.get_tdata()\n    if not tdata:\n        logger.warning(\"no tdata: href=%s src=%s\", self.href, self.src)\n    if tdata:\n        table = SimpleDataTable(self, tdata)\n        if self.param and \"no_actions\" in self.param:\n            table.set_no_actions(True)\n    else:\n        table = None\n\n    self.grid = grid.SchTableGrid(\n        table,\n        \"\",\n        self,\n        typ=grid.SchTableGrid.VIEW,\n        style=wx.TAB_TRAVERSAL | wx.FULL_REPAINT_ON_RESIZE,\n    )\n    self.create_toolbar(self.grid)\n    self.Bind(wx.EVT_CLOSE, self._on_close)\n    self._table = table\n    for signal_name in (\"update_row_ok\", \"new_row_ok\", \"delete_row_ok\"):\n        self.get_parent_page().register_signal(self, signal_name)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.TABLE-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.TABLE.delete_row_ok","title":"delete_row_ok","text":"<pre><code>delete_row_ok(data)\n</code></pre> <p>Handle delete_row_ok signal.</p> <p>Hides the current row and moves cursor appropriately.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <p>Delete signal data (unused).</p> required <p>Returns:</p> Type Description <p>True.</p> Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def delete_row_ok(self, data):\n    \"\"\"Handle delete_row_ok signal.\n\n    Hides the current row and moves cursor appropriately.\n\n    Args:\n        data: Delete signal data (unused).\n\n    Returns:\n        True.\n    \"\"\"\n    row_id = self.grid.GetGridCursorRow()\n    self.grid.HideRow(row_id)\n    row_id += 1\n    if row_id &gt;= self.grid.GetNumberRows():\n        self.grid.goto_last_row()\n    else:\n        self.grid.SetGridCursor(row_id, 0)\n        self.grid.MakeCellVisible(row_id, 0)\n    return True\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.TABLE.do_refresh","title":"do_refresh","text":"<pre><code>do_refresh(tdata)\n</code></pre> <p>Replace table data and restore cursor position.</p> <p>Parameters:</p> Name Type Description Default <code>tdata</code> <p>New table data to display.</p> required Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def do_refresh(self, tdata):\n    \"\"\"Replace table data and restore cursor position.\n\n    Args:\n        tdata: New table data to display.\n    \"\"\"\n    oldRow = self.grid.GetGridCursorRow()\n    self._table.replace_tab(tdata)\n    if self.grid.last_action == \"insert\":\n        newRow = self.grid.GetGridCursorRow() + 1\n        if newRow &lt; self.grid.GetTable().GetNumberRows():\n            self.grid.SetGridCursor(newRow, 0)\n            self.grid.MakeCellVisible(newRow, 0)\n    else:\n        if oldRow &lt; self.grid.GetTable().GetNumberRows():\n            if oldRow &lt; 0:\n                oldRow = 0\n            self.grid.SetGridCursor(oldRow, 0)\n            self.grid.MakeCellVisible(oldRow, 0)\n        else:\n            self.grid.goto_last_row()\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.TABLE.new_row_ok","title":"new_row_ok","text":"<pre><code>new_row_ok(data)\n</code></pre> <p>Handle new_row_ok signal.</p> Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def new_row_ok(self, data):\n    \"\"\"Handle new_row_ok signal.\"\"\"\n    return self._row_ok(data, insert=True)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.TABLE.process_refr_data","title":"process_refr_data","text":"<pre><code>process_refr_data(**kwds)\n</code></pre> <p>Refresh the table with new data.</p> <p>Parameters:</p> Name Type Description Default <code>**kwds</code> <p>New keyword arguments.</p> <code>{}</code> <p>Returns:</p> Type Description <p>Result from do_refresh.</p> Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def process_refr_data(self, **kwds):\n    \"\"\"Refresh the table with new data.\n\n    Args:\n        **kwds: New keyword arguments.\n\n    Returns:\n        Result from do_refresh.\n    \"\"\"\n    self.grid.last_action = \"\"\n    self.init_base(kwds)\n    tdata = self.get_tdata()\n    return self.do_refresh(tdata)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.TABLE.refresh_from_source","title":"refresh_from_source","text":"<pre><code>refresh_from_source(html_src)\n</code></pre> <p>Refresh table from raw HTML source.</p> <p>Parameters:</p> Name Type Description Default <code>html_src</code> <p>Raw HTML containing table data.</p> required <p>Returns:</p> Type Description <p>Result from do_refresh.</p> Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def refresh_from_source(self, html_src):\n    \"\"\"Refresh table from raw HTML source.\n\n    Args:\n        html_src: Raw HTML containing table data.\n\n    Returns:\n        Result from do_refresh.\n    \"\"\"\n    self.refresh_tdata(html_src)\n    tdata = self.get_tdata()\n    return self.do_refresh(tdata)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.TABLE.update_row_ok","title":"update_row_ok","text":"<pre><code>update_row_ok(data)\n</code></pre> <p>Handle update_row_ok signal.</p> Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def update_row_ok(self, data):\n    \"\"\"Handle update_row_ok signal.\"\"\"\n    return self._row_ok(data, insert=False)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.TEXT","title":"TEXT","text":"<pre><code>TEXT(parent, **kwds)\n</code></pre> <p>               Bases: <code>SchBaseCtrl</code>, <code>TextCtrl</code></p> <p>Single-line text input control.</p> <p>Handles ctrltext tag. Supports process_enter style for Enter key handling, hidden state, and maxlength constraint.</p> Tag arguments <p>Initialize the text control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.TextCtrl after processing 'param' for process_enter flag.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/text.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the text control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.TextCtrl after processing\n            'param' for process_enter flag.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    if self.param and \"process_enter\" in self.param:\n        kwds[\"style\"] = wx.TE_PROCESS_ENTER\n    wx.TextCtrl.__init__(self, parent, **kwds)\n    if self.hidden:\n        self.Enable(False)\n    if self.maxlength:\n        self.SetMaxLength(int(self.maxlength))\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.TEXT-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.TEXT.GetBestSize","title":"GetBestSize","text":"<pre><code>GetBestSize()\n</code></pre> <p>Return a wider best size than the default.</p> <p>The width is multiplied by 4 to give text fields more horizontal space.</p> <p>Returns:</p> Type Description <p>Tuple of (width * 4, height).</p> Source code in <code>pytigon_gui/guictrl/input/text.py</code> <pre><code>def GetBestSize(self):\n    \"\"\"Return a wider best size than the default.\n\n    The width is multiplied by 4 to give text fields more\n    horizontal space.\n\n    Returns:\n        Tuple of (width * 4, height).\n    \"\"\"\n    size = super().GetBestSize()\n    return (4 * size[0], size[1])\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.TEXT.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Get the current text value.</p> <p>Returns:</p> Type Description <p>The text content as a string.</p> Source code in <code>pytigon_gui/guictrl/input/text.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get the current text value.\n\n    Returns:\n        The text content as a string.\n    \"\"\"\n    return super().GetValue()\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.TEXT.SetValue","title":"SetValue","text":"<pre><code>SetValue(value)\n</code></pre> <p>Set the text value, converting bytes to str if needed.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>String or bytes value to set.</p> required Source code in <code>pytigon_gui/guictrl/input/text.py</code> <pre><code>def SetValue(self, value):\n    \"\"\"Set the text value, converting bytes to str if needed.\n\n    Args:\n        value: String or bytes value to set.\n    \"\"\"\n    if isinstance(value, str):\n        return wx.TextCtrl.SetValue(self, value)\n    else:\n        return wx.TextCtrl.SetValue(self, value.decode(\"utf-8\"))\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.TICKER","title":"TICKER","text":"<pre><code>TICKER(parent, **kwds)\n</code></pre> <p>               Bases: <code>Ticker</code>, <code>SchBaseCtrl</code></p> <p>Scrolling ticker text control.</p> <p>Handles ctrlticker tag. Displays scrolling text. Stops the ticker when the control is closed.</p> Tag arguments <p>Initialize the ticker control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.lib.ticker.Ticker.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/numeric.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the ticker control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.lib.ticker.Ticker.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    wx_ticker.Ticker.__init__(self, parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.TICKER-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.TICKER.CanClose","title":"CanClose","text":"<pre><code>CanClose()\n</code></pre> <p>Stop the ticker before closing.</p> <p>Returns:</p> Type Description <p>True always, after stopping the ticker animation.</p> Source code in <code>pytigon_gui/guictrl/input/numeric.py</code> <pre><code>def CanClose(self):\n    \"\"\"Stop the ticker before closing.\n\n    Returns:\n        True always, after stopping the ticker animation.\n    \"\"\"\n    self.Stop()\n    return True\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.TICKER.SetValue","title":"SetValue","text":"<pre><code>SetValue(value)\n</code></pre> <p>Set the scrolling text value.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>Text string to display.</p> required Source code in <code>pytigon_gui/guictrl/input/numeric.py</code> <pre><code>def SetValue(self, value):\n    \"\"\"Set the scrolling text value.\n\n    Args:\n        value: Text string to display.\n    \"\"\"\n    self.SetText(value)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.TIME","title":"TIME","text":"<pre><code>TIME(parent, **kwds)\n</code></pre> <p>               Bases: <code>TimeCtrl</code>, <code>SchBaseCtrl</code></p> <p>Time input control with mask validation.</p> <p>Handles ctrltime tag. Uses wx.lib.masked.TimeCtrl for formatted time entry with validation.</p> Tag arguments <p>Initialize the time control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to masked.TimeCtrl.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/datetime.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the time control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to masked.TimeCtrl.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    masked.TimeCtrl.__init__(self, parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.TIME-functions","title":"Methods:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.TREE","title":"TREE","text":"<pre><code>TREE(parent, **kwds)\n</code></pre> <p>               Bases: <code>TreeCtrl</code>, <code>SchBaseCtrl</code></p> <p>Tree control with hierarchical data.</p> <p>Handles ctrltree tag. Builds a tree from ldata with folder/file icons and supports activation (navigation) on click.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    kwds[\"style\"] = wx.TR_HIDE_ROOT | wx.TR_DEFAULT_STYLE\n    wx.TreeCtrl.__init__(self, parent, **kwds)\n\n    isz = (16, 16)\n    il = wx.ImageList(isz[0], isz[1])\n    self.fldridx = il.Add(\n        wx.ArtProvider.GetBitmap(wx.ART_FOLDER, wx.ART_OTHER, isz)\n    )\n    self.fldropenidx = il.Add(\n        wx.ArtProvider.GetBitmap(wx.ART_FILE_OPEN, wx.ART_OTHER, isz)\n    )\n    self.fileidx = il.Add(\n        wx.ArtProvider.GetBitmap(wx.ART_NORMAL_FILE, wx.ART_OTHER, isz)\n    )\n    self.fileidxmark = il.Add(\n        wx.ArtProvider.GetBitmap(wx.ART_TICK_MARK, wx.ART_OTHER, isz)\n    )\n    self.SetImageList(il)\n    self.il = il\n    self.root = self.AddRoot(\"/\")\n\n    ldata = self.get_ldata()\n    if ldata:\n        self._append_list(self.root, ldata)\n    self.Bind(wx.EVT_TREE_ITEM_ACTIVATED, self._on_activated)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.TREELIST","title":"TREELIST","text":"<pre><code>TREELIST(parent, **kwds)\n</code></pre> <p>               Bases: <code>HyperTreeList</code>, <code>SchBaseCtrl</code></p> <p>Tree list with multiple columns.</p> <p>Handles ctrltreelist tag. Supports '||' separated column data and folder/file icons.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    kwds[\"agwStyle\"] = wx.TR_HIDE_ROOT\n    TreeListCtrl.__init__(self, parent, **kwds)\n\n    isz = (16, 16)\n    il = wx.ImageList(isz[0], isz[1])\n    self.fldridx = il.Add(\n        wx.ArtProvider.GetBitmap(wx.ART_FOLDER, wx.ART_OTHER, isz)\n    )\n    self.fldropenidx = il.Add(\n        wx.ArtProvider.GetBitmap(wx.ART_FILE_OPEN, wx.ART_OTHER, isz)\n    )\n    self.fileidx = il.Add(\n        wx.ArtProvider.GetBitmap(wx.ART_NORMAL_FILE, wx.ART_OTHER, isz)\n    )\n    self.fileidxmark = il.Add(\n        wx.ArtProvider.GetBitmap(wx.ART_TICK_MARK, wx.ART_OTHER, isz)\n    )\n    self.SetImageList(il)\n    self.il = il\n\n    columns = self.label.split(\"||\")\n    for col_title in columns:\n        try:\n            self.AppendColumn(col_title)\n        except AttributeError:\n            self.AddColumn(col_title)\n    self.SetColumnWidth(0, 175)\n    self.root = self.AddRoot(\"/\")\n\n    ldata = self.get_ldata()\n    if ldata:\n        self._append_list(self.root, ldata)\n    self.Refresh()\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.UPDATEGRIDBUTTON","title":"UPDATEGRIDBUTTON","text":"<pre><code>UPDATEGRIDBUTTON(parent, **kwds)\n</code></pre> <p>               Bases: <code>Button</code>, <code>SchBaseCtrl</code></p> <p>Button that submits form data to update a parent grid.</p> <p>Handles ctrlupdategridbutton tag. Collects values from all sibling controls and sends them to the parent grid's OnUpRecFromForm method.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    wx.Button.__init__(self, parent, **kwds)\n    self.Bind(wx.EVT_BUTTON, self._on_click)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl-functions","title":"Functions:","text":""},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.BUTTON","title":"BUTTON","text":"<pre><code>BUTTON(parent, **kwds)\n</code></pre> <p>Dispatch function for ctrlbutton tag.</p> <p>Routes to the appropriate button class based on the 'btn-class' parameter or the presence of a 'src' attribute (bitmap vs text button).</p> Supported btn-class values <p>SIMPLE_BUTTON, BITMAPBUTTON, PLATEBUTTON, GENBITMAPBUTTON, GENBITMAPBUTTONTXT, GENBITMAPBUTTONTXT_SMALL, NOBG_BUTTON, NOBG_BUTTON_TXT, CLOSEBUTTON, MENUBUTTON, MENUTOOLBARBUTTON</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Tag keyword arguments including optional 'param' with 'btn-class' key, and optional 'src' for bitmap.</p> <code>{}</code> <p>Returns:</p> Type Description <p>A button control instance.</p> Source code in <code>pytigon_gui/guictrl/factory.py</code> <pre><code>def BUTTON(parent, **kwds):\n    \"\"\"Dispatch function for ctrlbutton tag.\n\n    Routes to the appropriate button class based on the\n    'btn-class' parameter or the presence of a 'src' attribute\n    (bitmap vs text button).\n\n    Supported btn-class values:\n        SIMPLE_BUTTON, BITMAPBUTTON, PLATEBUTTON,\n        GENBITMAPBUTTON, GENBITMAPBUTTONTXT,\n        GENBITMAPBUTTONTXT_SMALL, NOBG_BUTTON,\n        NOBG_BUTTON_TXT, CLOSEBUTTON, MENUBUTTON,\n        MENUTOOLBARBUTTON\n\n    Args:\n        parent: Parent window.\n        **kwds: Tag keyword arguments including optional 'param'\n            with 'btn-class' key, and optional 'src' for bitmap.\n\n    Returns:\n        A button control instance.\n    \"\"\"\n    if \"param\" in kwds and \"btn-class\" in kwds[\"param\"]:\n        btn_class = kwds[\"param\"][\"btn-class\"]\n        class_map = {\n            \"SIMPLE_BUTTON\": SIMPLE_BUTTON,\n            \"BITMAPBUTTON\": BITMAPBUTTON,\n            \"PLATEBUTTON\": PLATEBUTTON,\n            \"GENBITMAPBUTTON\": GENBITMAPBUTTON,\n            \"GENBITMAPBUTTONTXT\": GENBITMAPBUTTONTXT,\n            \"GENBITMAPBUTTONTXT_SMALL\": GENBITMAPBUTTONTXT_SMALL,\n            \"NOBG_BUTTON\": NOBG_BUTTON,\n            \"NOBG_BUTTON_TXT\": NOBG_BUTTON_TXT,\n            \"CLOSEBUTTON\": CLOSEBUTTON,\n            \"MENUBUTTON\": MENUBUTTON,\n            \"MENUTOOLBARBUTTON\": MENUTOOLBARBUTTON,\n        }\n        if btn_class in class_map:\n            return class_map[btn_class](parent, **kwds)\n\n    if \"src\" in kwds:\n        return BITMAPBUTTON(parent, **kwds)\n    else:\n        return SIMPLE_BUTTON(parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.COMPONENT","title":"COMPONENT","text":"<pre><code>COMPONENT(parent, **kwds)\n</code></pre> <p>Create a web component widget.</p> <p>Loads a web component via HTTP, replaces placeholder tokens, and renders it using the HTML2 viewer if available.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Tag keyword arguments with 'param' containing 'data' (component name).</p> <code>{}</code> <p>Returns:</p> Type Description <p>An HTML2 viewer instance or None if HTML2 is not available.</p> Source code in <code>pytigon_gui/guictrl/factory.py</code> <pre><code>def COMPONENT(parent, **kwds):\n    \"\"\"Create a web component widget.\n\n    Loads a web component via HTTP, replaces placeholder tokens,\n    and renders it using the HTML2 viewer if available.\n\n    Args:\n        parent: Parent window.\n        **kwds: Tag keyword arguments with 'param' containing\n            'data' (component name).\n\n    Returns:\n        An HTML2 viewer instance or None if HTML2 is not available.\n    \"\"\"\n    # HTML2 is dynamically set by plugins at runtime\n    from pytigon_gui.guictrl import ctrl as schctrl\n\n    http = wx.GetApp().get_http(parent)\n    response = http.get(\n        parent,\n        wx.GetApp().make_href(\"/schsys/widget_web?browser_type=1\"),\n    )\n    buf = response.str()\n\n    colour = wx.SystemSettings.GetColour(wx.SYS_COLOUR_3DFACE).GetAsString(\n        flags=wx.C2S_HTML_SYNTAX\n    )\n    buf = buf.replace(\"&lt;component&gt;\", kwds[\"param\"][\"data\"]).replace(\"[[color]]\", colour)\n\n    if schctrl.HTML2:\n        kwds[\"component\"] = True\n        obj = schctrl.HTML2(parent, **kwds)\n        obj.load_str(buf, \"http://127.0.0.2\")\n        return obj\n    else:\n        return None\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.COMPOSITE","title":"COMPOSITE","text":"<pre><code>COMPOSITE(parent, **kwds)\n</code></pre> <p>Dispatch function for ctrlcomposite tag.</p> <p>Looks up the widget class by the 'class' parameter (uppercased) in the global namespace and instantiates it.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Tag keyword arguments with 'param' containing 'class' key.</p> <code>{}</code> <p>Returns:</p> Type Description <p>An instance of the requested widget class.</p> Source code in <code>pytigon_gui/guictrl/factory.py</code> <pre><code>def COMPOSITE(parent, **kwds):\n    \"\"\"Dispatch function for ctrlcomposite tag.\n\n    Looks up the widget class by the 'class' parameter (uppercased)\n    in the global namespace and instantiates it.\n\n    Args:\n        parent: Parent window.\n        **kwds: Tag keyword arguments with 'param' containing\n            'class' key.\n\n    Returns:\n        An instance of the requested widget class.\n    \"\"\"\n    cls_name = kwds[\"param\"][\"class\"].upper()\n    if cls_name in globals():\n        return globals()[cls_name](parent, **kwds)\n    return None\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SELECT","title":"SELECT","text":"<pre><code>SELECT(parent, **kwds)\n</code></pre> <p>Dispatch function for ctrlselect tag.</p> <p>Routes to the appropriate widget based on parameters: - HeavySelect2Field / ModelSelect2Field types: SELECT2 - Single selection (size=1): DBCHOICE (readonly combo) - Multi selection (size&gt;1 or 'multiple' param): CHECKLISTBOX</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Tag keyword arguments including 'param' dict.</p> <code>{}</code> <p>Returns:</p> Type Description <p>A wx control instance appropriate for the selection mode.</p> Source code in <code>pytigon_gui/guictrl/factory.py</code> <pre><code>def SELECT(parent, **kwds):\n    \"\"\"Dispatch function for ctrlselect tag.\n\n    Routes to the appropriate widget based on parameters:\n    - HeavySelect2Field / ModelSelect2Field types: SELECT2\n    - Single selection (size=1): DBCHOICE (readonly combo)\n    - Multi selection (size&gt;1 or 'multiple' param): CHECKLISTBOX\n\n    Args:\n        parent: Parent window.\n        **kwds: Tag keyword arguments including 'param' dict.\n\n    Returns:\n        A wx control instance appropriate for the selection mode.\n    \"\"\"\n    size = 1\n    multiple = False\n\n    if \"param\" in kwds:\n        param = kwds[\"param\"]\n        if \"multiple\" in param:\n            multiple = True\n        if \"size\" in param:\n            size = int(param[\"size\"])\n        if multiple and size == 1:\n            size = 10\n\n        if \"schtype\" in param and param[\"schtype\"] in (\n            \"HeavySelect2Field\",\n            \"HeavySelect2MultipleField\",\n            \"ModelSelect2Field\",\n            \"ModelSelect2MultipleField\",\n        ):\n            kwds[\"param\"][\"data\"] = [kwds[\"param\"]]\n            kwds[\"param\"][\"data\"][0][\"attrs\"] = kwds[\"param\"]\n            return SELECT2(parent, **kwds)\n\n    if size == 1:\n        kwds[\"readonly\"] = True\n        return DBCHOICE(parent, **kwds)\n    else:\n        kwds[\"length\"] = size\n        return CHECKLISTBOX(parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.SELECT2","title":"SELECT2","text":"<pre><code>SELECT2(parent, **kwds)\n</code></pre> <p>Create a Select2 composite control with optional action buttons.</p> <p>Builds a CompositePanel containing a Select2Base control and up to two action buttons from the composite parameter data.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Tag keyword arguments with 'param' containing 'data' (list of parameter nodes).</p> <code>{}</code> <p>Returns:</p> Type Description <p>A CompositePanel containing the Select2 control and buttons.</p> Source code in <code>pytigon_gui/guictrl/factory.py</code> <pre><code>def SELECT2(parent, **kwds):\n    \"\"\"Create a Select2 composite control with optional action buttons.\n\n    Builds a CompositePanel containing a Select2Base control\n    and up to two action buttons from the composite parameter data.\n\n    Args:\n        parent: Parent window.\n        **kwds: Tag keyword arguments with 'param' containing\n            'data' (list of parameter nodes).\n\n    Returns:\n        A CompositePanel containing the Select2 control and buttons.\n    \"\"\"\n    data = kwds[\"param\"][\"data\"]\n    panel = CompositePanel(parent, size=(460, -1))\n    ctrl = Select2Base(panel, **kwds)\n    panel.set_main_ctrl(ctrl)\n\n    # Build optional action buttons from composite data\n    button1 = None\n    button2 = None\n    if (\n        len(data) &gt; 1\n        and \"children\" in data[1]\n        and data[1][\"children\"]\n        and len(data[1][\"children\"]) &gt; 0\n    ):\n        button1 = _button_from_parm(panel, param=data[1][\"children\"][0])\n    if (\n        len(data) &gt; 1\n        and \"children\" in data[1]\n        and data[1][\"children\"]\n        and len(data[1][\"children\"]) &gt; 1\n    ):\n        button2 = _button_from_parm(panel, param=data[1][\"children\"][1])\n\n    ctrl.init(button1, button2)\n\n    # Shared callback for select2 and buttons\n    def ret_ok(id, title):\n        ctrl.set_value(id, title)\n        wx.CallAfter(ctrl.SetFocus)\n\n    ctrl.ret_ok = ret_ok\n    if button1:\n        button1.ret_ok = ret_ok\n    if button2:\n        button2.ret_ok = ret_ok\n\n    # Layout: horizontal box with ctrl + buttons\n    box = wx.BoxSizer(wx.HORIZONTAL)\n    box.Add(ctrl, 0, wx.EXPAND)\n    if button1:\n        box.Add(button1)\n    if button2:\n        box.Add(button2)\n\n    panel.SetSizer(box)\n    panel.SetAutoLayout(True)\n    panel.Fit()\n\n    return panel\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.TEXTAREA","title":"TEXTAREA","text":"<pre><code>TEXTAREA(parent, **kwds)\n</code></pre> <p>Dispatch function for ctrltextarea tag.</p> <p>Handles sizing from 'cols' and 'rows' attributes and routes to STYLEDTEXT or AUTOCOMPLETE based on the 'src' attribute (code languages get STYLEDTEXT, others get AUTOCOMPLETE for code completion).</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Tag keyword arguments with optional 'param', 'cols', 'rows', 'src'.</p> <code>{}</code> <p>Returns:</p> Type Description <p>A STYLEDTEXT or AUTOCOMPLETE instance.</p> Source code in <code>pytigon_gui/guictrl/factory.py</code> <pre><code>def TEXTAREA(parent, **kwds):\n    \"\"\"Dispatch function for ctrltextarea tag.\n\n    Handles sizing from 'cols' and 'rows' attributes and\n    routes to STYLEDTEXT or AUTOCOMPLETE based on the 'src'\n    attribute (code languages get STYLEDTEXT, others get\n    AUTOCOMPLETE for code completion).\n\n    Args:\n        parent: Parent window.\n        **kwds: Tag keyword arguments with optional 'param',\n            'cols', 'rows', 'src'.\n\n    Returns:\n        A STYLEDTEXT or AUTOCOMPLETE instance.\n    \"\"\"\n    if \"param\" in kwds:\n        if \"data\" in kwds[\"param\"]:\n            data = kwds[\"param\"][\"data\"].replace(\"\\r\", \"\")\n            if data.startswith(\"\\n\"):\n                data = data[1:]\n                kwds[\"param\"][\"data\"] = data\n\n    size = [-1, -1]\n\n    if \"param\" in kwds and \"width\" not in kwds[\"param\"]:\n        if \"cols\" in kwds[\"param\"]:\n            width = 7 * int(is_null(kwds[\"param\"][\"cols\"], \"80\"))\n            if width &gt; 480:\n                width = 480\n            kwds[\"param\"][\"width\"] = str(width)\n            size[0] = width\n        else:\n            kwds[\"param\"][\"width\"] = \"100%\"\n    if \"param\" in kwds and \"height\" not in kwds[\"param\"]:\n        if \"rows\" in kwds[\"param\"]:\n            size[1] = 14 + 14 * int(is_null(kwds[\"param\"][\"rows\"], \"3\"))\n            kwds[\"param\"][\"height\"] = str(size[1])\n        else:\n            kwds[\"param\"][\"height\"] = \"100%\"\n\n    kwds[\"size\"] = size\n\n    kwds.pop(\"cols\", None)\n    kwds.pop(\"rows\", None)\n\n    if \"src\" in kwds:\n        if kwds[\"src\"] in (\"c\", \"python\", \"html\"):\n            return STYLEDTEXT(parent, **kwds)\n        else:\n            return AUTOCOMPLETE(parent, **kwds)\n    else:\n        return STYLEDTEXT(parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_ctrl/#pytigon_gui.guictrl.ctrl.handle_best_size","title":"handle_best_size","text":"<pre><code>handle_best_size(base_class)\n</code></pre> <p>Decorator that adds GetBestSize() using param width/height hints.</p> <p>When applied to a widget class, this makes GetBestSize() first check for explicit 'width' and 'height' values in self.param, falling back to the widget's default best size.</p> <p>Parameters:</p> Name Type Description Default <code>base_class</code> <p>A wxPython widget class to wrap.</p> required <p>Returns:</p> Type Description <p>A derived class with custom GetBestSize() logic.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def handle_best_size(base_class):\n    \"\"\"Decorator that adds GetBestSize() using param width/height hints.\n\n    When applied to a widget class, this makes GetBestSize() first check\n    for explicit 'width' and 'height' values in self.param, falling back\n    to the widget's default best size.\n\n    Args:\n        base_class: A wxPython widget class to wrap.\n\n    Returns:\n        A derived class with custom GetBestSize() logic.\n    \"\"\"\n\n    class _Derived(base_class):\n        def GetBestSize(self):\n            w = 0\n            h = 0\n            if self.param and \"width\" in self.param:\n                try:\n                    w = int(self.param[\"width\"])\n                except (ValueError, TypeError):\n                    pass\n            if self.param and \"height\" in self.param:\n                try:\n                    h = int(self.param[\"height\"])\n                except (ValueError, TypeError):\n                    pass\n            if w == 0 or h == 0:\n                base_size = super().GetBestSize()\n                if w == 0:\n                    w = base_size[0]\n                if h == 0:\n                    h = base_size[1]\n            return (w, h)\n\n    return _Derived\n</code></pre>"},{"location":"api/guictrl_display/","title":"guictrl.display \u2013 Display Rendering","text":"<p>Rendering engine for drawing controls onto wxPython device contexts using the shared DC abstraction from pytigon_lib.schhtml.</p>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display","title":"display","text":"<p>Display and popup widget classes for the SchForm GUI framework.</p> <p>Provides wxPython display, navigation, and popup controls: static text, error indicator, tree, tree list, colour selector, directory browser, editable list, file/image browse, HTML list, and popup HTML control.</p>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display-attributes","title":"Attributes","text":""},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.INIT_CSS_STR","title":"INIT_CSS_STR  <code>module-attribute</code>","text":"<pre><code>INIT_CSS_STR = \"\\nbody {font-family:sans-serif;font-size:150%; padding:1;}\\ntable {border:0;vertical-align:top; padding:1;}\\ntd table { padding: 1; }\\nth {border:0; cellpadding:1;}\\ntd {border:0; vertical-align:top; cellpadding:1;}\\nstrong,b {font-weight:bold;}\\np { cellpadding:1; border:0; width:100%; }\\n\"\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.logger","title":"logger  <code>module-attribute</code>","text":"<pre><code>logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display-classes","title":"Classes","text":""},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.BitmapTextButton","title":"BitmapTextButton","text":"<pre><code>BitmapTextButton(\n    parent,\n    id=-1,\n    bitmap=NullBitmap,\n    label=\"\",\n    pos=DefaultPosition,\n    size=DefaultSize,\n    style=0,\n    validator=DefaultValidator,\n    name=\"bitmaptextbutton\",\n)\n</code></pre> <p>               Bases: <code>GenBitmapButton</code></p> <p>Button for toolbars</p> <p>Constructor</p> Source code in <code>pytigon_gui/guictrl/button/toolbarbutton.py</code> <pre><code>def __init__(\n    self,\n    parent,\n    id=-1,\n    bitmap=wx.NullBitmap,\n    label=\"\",\n    pos=wx.DefaultPosition,\n    size=wx.DefaultSize,\n    style=0,\n    validator=wx.DefaultValidator,\n    name=\"bitmaptextbutton\",\n):\n    \"\"\"Constructor\"\"\"\n\n    self._art = RB.RibbonMSWArtProvider()\n    # self._art = RB.RibbonArtProvider()\n    wx.lib.buttons.GenBitmapButton.__init__(\n        self, parent, id, bitmap, pos, size, style | wx.BU_EXACTFIT, validator, name\n    )\n    self.SetLabel(label)\n    self.enter_state = False\n    self.Bind(wx.EVT_ENTER_WINDOW, self.on_enter)\n    self.Bind(wx.EVT_LEAVE_WINDOW, self.on_leave)\n    image = self.bmpLabel.ConvertToImage()\n    imageutils.grayOut(image)\n    self.bmpLabel2 = wx.Bitmap(image)\n    self.SetBackgroundColour(wx.SystemSettings.GetColour(wx.SYS_COLOUR_3DFACE))\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.BitmapTextButton-functions","title":"Methods:","text":""},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.COLOURSELECT","title":"COLOURSELECT","text":"<pre><code>COLOURSELECT(parent, **kwds)\n</code></pre> <p>               Bases: <code>ColourSelect</code>, <code>SchBaseCtrl</code></p> <p>Colour selection dropdown.</p> <p>Handles ctrlcolourselect tag.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    if \"name\" in kwds:\n        del kwds[\"name\"]\n    colourselect.ColourSelect.__init__(self, parent, id=-1, **kwds)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.DataPopupControl","title":"DataPopupControl","text":"<pre><code>DataPopupControl(*args, **kwds)\n</code></pre> <p>               Bases: <code>ComboCtrl</code></p> <p>Server-backed combo control with popup data selection.</p> <p>Provides a ComboCtrl that uses server endpoints for: - /size/  : fetching popup dimensions - /test/  : validating/looking up typed values - /dialog/: opening an extended selection dialog</p> <p>Supports an optional secondary href (separated by ';') for the extended dialog endpoint.</p> <p>Initialize the popup control.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <p>Positional arguments passed to ComboCtrl.</p> <code>()</code> <code>**kwds</code> <p>Keyword arguments. Accepts 'dialog_with_value' (bool, default True) and 'style'.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def __init__(self, *args, **kwds):\n    \"\"\"Initialize the popup control.\n\n    Args:\n        *args: Positional arguments passed to ComboCtrl.\n        **kwds: Keyword arguments. Accepts 'dialog_with_value'\n            (bool, default True) and 'style'.\n    \"\"\"\n    if \"style\" in kwds:\n        kwds[\"style\"] |= wx.TE_PROCESS_ENTER\n    else:\n        kwds[\"style\"] = wx.TE_PROCESS_ENTER\n\n    if \"dialog_with_value\" in kwds:\n        self.dialog_with_value = kwds[\"dialog_with_value\"]\n        del kwds[\"dialog_with_value\"]\n    else:\n        self.dialog_with_value = True\n    ComboCtrl.__init__(self, *args, **kwds)\n\n    self.win = None\n    self.html = None\n    self.rec_value = []\n    self.event_object = None\n    self.page = None\n    self.popup = None\n    self.start_value = \"\"\n\n    if self.defaultvalue:\n        self.clear_str = self.defaultvalue\n    else:\n        self.clear_str = \"\"\n\n    # Support dual-href: primary href for list/size/test,\n    # secondary (after ';') for extended dialog\n    href_parts = self.href.split(\";\")\n    self.href = href_parts[0]\n    self.href2 = href_parts[1] if len(href_parts) &gt; 1 else None\n\n    self.http = wx.GetApp().get_http(self)\n    response = self.http.get(self, str(self.href) + \"size/\")\n    self.size = schjson.loads(response.str())\n\n    self.simpleDialog = True\n    if self.GetTextCtrl():\n        self.GetTextCtrl().SetForegroundColour(wx.Colour(0, 0, 0))\n\n    self.UseAltPopupWindow(enable=True)\n\n    popup_ctrl = self._create_popoup()\n    self.SetPopupControl(popup_ctrl)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.DataPopupControl-functions","title":"Methods:","text":""},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.DataPopupControl.Dismiss","title":"Dismiss","text":"<pre><code>Dismiss()\n</code></pre> <p>Dismiss the popup or extended dialog page.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def Dismiss(self):\n    \"\"\"Dismiss the popup or extended dialog page.\"\"\"\n    if self.page:\n        self.page.body.old_any_parent_command(\"on_cancel\", None)\n        self.page = None\n    else:\n        super().Dismiss()\n    self.SetFocus()\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.DataPopupControl.GetTextCtrl","title":"GetTextCtrl","text":"<pre><code>GetTextCtrl()\n</code></pre> <p>Return the text control, preferring the masked override if set.</p> <p>Returns:</p> Type Description <p>wx.TextCtrl or masked.TextCtrl instance.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def GetTextCtrl(self):\n    \"\"\"Return the text control, preferring the masked override if set.\n\n    Returns:\n        wx.TextCtrl or masked.TextCtrl instance.\n    \"\"\"\n    if self.win:\n        return self.win\n    return ComboCtrl.GetTextCtrl(self)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.DataPopupControl.KillFocus","title":"KillFocus","text":"<pre><code>KillFocus()\n</code></pre> <p>Handle focus loss: trigger focus_out validation if readonly.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def KillFocus(self):\n    \"\"\"Handle focus loss: trigger focus_out validation if readonly.\"\"\"\n    value = ComboCtrl.GetValue(self)\n    if self.readonly:\n        self.focus_out(value)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.DataPopupControl.alternate_button_click","title":"alternate_button_click","text":"<pre><code>alternate_button_click()\n</code></pre> <p>Handle alternative button (e.g. F2) to open extended dialog.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def alternate_button_click(self):\n    \"\"\"Handle alternative button (e.g. F2) to open extended dialog.\"\"\"\n    if self.event_object and hasattr(self.event_object, \"on_before_button_click\"):\n        self.event_object.on_before_button_click()\n\n    self.run_ext_dialog()\n\n    if self.event_object and hasattr(self.event_object, \"OnButtonClick\"):\n        self.event_object.OnButtonClick()\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.DataPopupControl.any_parent_command","title":"any_parent_command","text":"<pre><code>any_parent_command(command, *args, **kwds)\n</code></pre> <p>Walk up the widget tree to find and call a named command.</p> <p>Parameters:</p> Name Type Description Default <code>command</code> <p>Method name to search for.</p> required <code>*args</code> <p>Positional arguments.</p> <code>()</code> <code>**kwds</code> <p>Keyword arguments.</p> <code>{}</code> <p>Returns:</p> Type Description <p>Result of the command, or None if not found.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def any_parent_command(self, command, *args, **kwds):\n    \"\"\"Walk up the widget tree to find and call a named command.\n\n    Args:\n        command: Method name to search for.\n        *args: Positional arguments.\n        **kwds: Keyword arguments.\n\n    Returns:\n        Result of the command, or None if not found.\n    \"\"\"\n    parent = self\n    while parent is not None:\n        if hasattr(parent, command):\n            return getattr(parent, command)(*args, **kwds)\n        parent = parent.GetParent()\n    return None\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.DataPopupControl.clear_rec","title":"clear_rec","text":"<pre><code>clear_rec()\n</code></pre> <p>Clear the current selection and reset to the default string.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def clear_rec(self):\n    \"\"\"Clear the current selection and reset to the default string.\"\"\"\n    self.start_value = \"\"\n    self.SetValue(self.clear_str)\n    self.rec_value = []\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.DataPopupControl.focus_in","title":"focus_in","text":"<pre><code>focus_in(value)\n</code></pre> <p>Handle focus-in event (no-op by default).</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>Current field value.</p> required Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def focus_in(self, value):\n    \"\"\"Handle focus-in event (no-op by default).\n\n    Args:\n        value: Current field value.\n    \"\"\"\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.DataPopupControl.focus_out","title":"focus_out","text":"<pre><code>focus_out(value)\n</code></pre> <p>Validate typed value against the server when focus leaves.</p> <p>Compares current value with start_value. If changed and non-empty, posts the value to the server's /test/ endpoint.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>The current field value.</p> required Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def focus_out(self, value):\n    \"\"\"Validate typed value against the server when focus leaves.\n\n    Compares current value with start_value. If changed and non-empty,\n    posts the value to the server's /test/ endpoint.\n\n    Args:\n        value: The current field value.\n    \"\"\"\n    if str(value) != self.start_value and str(value) != \"\":\n        self.http = wx.GetApp().get_http(self)\n        x = bencode(value)\n        response = self.http.post(self, str(self.href) + \"test/\", {\"value\": x})\n        tab = schjson.loads(response.str())\n        ret = tab[0]\n\n        if ret != 1:\n            if ret == 2:\n                self.OnButtonClick()\n            else:\n                self.clear_rec()\n        else:\n            self.set_rec(tab[1], tab[2], False)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.DataPopupControl.get_last_control_with_focus","title":"get_last_control_with_focus","text":"<pre><code>get_last_control_with_focus()\n</code></pre> <p>Return self as the last focused control.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def get_last_control_with_focus(self):\n    \"\"\"Return self as the last focused control.\"\"\"\n    return self\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.DataPopupControl.get_parm","title":"get_parm","text":"<pre><code>get_parm(parm)\n</code></pre> <p>Get a parameter value for URL encoding.</p> <p>Parameters:</p> Name Type Description Default <code>parm</code> <p>Parameter name ('value' supported).</p> required <p>Returns:</p> Type Description <p>Bencoded field value, or None.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def get_parm(self, parm):\n    \"\"\"Get a parameter value for URL encoding.\n\n    Args:\n        parm: Parameter name ('value' supported).\n\n    Returns:\n        Bencoded field value, or None.\n    \"\"\"\n    return bencode(ComboCtrl.GetValue(self)) if parm == \"value\" else None\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.DataPopupControl.get_rec","title":"get_rec","text":"<pre><code>get_rec()\n</code></pre> <p>Return the currently selected record.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def get_rec(self):\n    \"\"\"Return the currently selected record.\"\"\"\n    return self.rec_value\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.DataPopupControl.has_parm","title":"has_parm","text":"<pre><code>has_parm(parm)\n</code></pre> <p>Check if a parameter is supported.</p> <p>Parameters:</p> Name Type Description Default <code>parm</code> <p>Parameter name.</p> required <p>Returns:</p> Type Description <p>True if parm is 'value', False otherwise.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def has_parm(self, parm):\n    \"\"\"Check if a parameter is supported.\n\n    Args:\n        parm: Parameter name.\n\n    Returns:\n        True if parm is 'value', False otherwise.\n    \"\"\"\n    return parm == \"value\"\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.DataPopupControl.on_create","title":"on_create","text":"<pre><code>on_create(parent)\n</code></pre> <p>Create the embedded SchPage for popup content.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window for the SchPage.</p> required <p>Returns:</p> Type Description <p>The created SchPage instance.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def on_create(self, parent):\n    \"\"\"Create the embedded SchPage for popup content.\n\n    Args:\n        parent: Parent window for the SchPage.\n\n    Returns:\n        The created SchPage instance.\n    \"\"\"\n    from pytigon_gui.guiframe.page import SchPage\n\n    href = (\n        self.href + \"dialog/|value\"\n        if self.dialog_with_value\n        else self.href + \"dialog/\"\n    )\n    self.html = SchPage(parent, href, self)\n    self.html.body.parent_combo = self\n    return self.html\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.DataPopupControl.on_popoup","title":"on_popoup","text":"<pre><code>on_popoup()\n</code></pre> <p>Called when the dropdown popup is shown.</p> <p>Refreshes the embedded HTML page and positions it.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def on_popoup(self):\n    \"\"\"Called when the dropdown popup is shown.\n\n    Refreshes the embedded HTML page and positions it.\n    \"\"\"\n    if self.html:\n        wx.BeginBusyCursor()\n        self.html.body.Hide()\n\n        def _after():\n            self.html.refresh_html()\n            self.html.SetFocus()\n            self.html.on_size(None)\n            self.html.body.init()\n\n            def _after2():\n                self.html.body.refr(self.start_value)\n                self.html.body.Show()\n\n            wx.CallAfter(_after2)\n            wx.EndBusyCursor()\n\n        wx.CallAfter(_after)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.DataPopupControl.run_ext_dialog","title":"run_ext_dialog","text":"<pre><code>run_ext_dialog()\n</code></pre> <p>Open the extended selection dialog as a child page.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def run_ext_dialog(self):\n    \"\"\"Open the extended selection dialog as a child page.\"\"\"\n    self.GetTextCtrl().SetFocus()\n\n    parm = {\"value\": self.get_parm(\"value\")}\n\n    href = self.href2 if self.href2 else self.href\n\n    _href = href + \"dialog/|value\" if self.dialog_with_value else href + \"dialog/\"\n    self.page = self.GetParent().new_child_page(str(_href), \"Select\", parm)\n    self.page.body.old_any_parent_command = self.page.body.any_parent_command\n    self.page.body.any_parent_command = self.any_parent_command\n    self.page.body.parent_combo = self\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.DataPopupControl.set_new_href","title":"set_new_href","text":"<pre><code>set_new_href(href)\n</code></pre> <p>Update the server endpoint URL.</p> <p>Parameters:</p> Name Type Description Default <code>href</code> <p>New base URL. May contain ';' to separate primary and secondary (dialog) endpoints.</p> required Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def set_new_href(self, href):\n    \"\"\"Update the server endpoint URL.\n\n    Args:\n        href: New base URL. May contain ';' to separate primary\n            and secondary (dialog) endpoints.\n    \"\"\"\n    self.href = href\n\n    href_parts = self.href.split(\";\")\n    if len(href_parts) &gt; 1:\n        self.href = href_parts[0]\n        self.href2 = href_parts[1]\n    else:\n        self.href2 = None\n\n    href3 = self.href2 if self.href2 else self.href\n\n    if self.dialog_with_value:\n        href3 += \"dialog/|value\"\n    else:\n        href3 += \"dialog/\"\n\n    if self.popup:\n        self.popup.set_new_href(href3)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.DataPopupControl.set_rec","title":"set_rec","text":"<pre><code>set_rec(value, value_rec, dismiss=False)\n</code></pre> <p>Set the field value from a server response record.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>Element id.</p> required <code>value_rec</code> <p>Element data (Td object or similar).</p> required <code>dismiss</code> <p>If True, dismisses the popup after setting.</p> <code>False</code> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def set_rec(self, value, value_rec, dismiss=False):\n    \"\"\"Set the field value from a server response record.\n\n    Args:\n        value: Element id.\n        value_rec: Element data (Td object or similar).\n        dismiss: If True, dismisses the popup after setting.\n    \"\"\"\n    value2 = value_rec.data\n\n    if self.event_object and hasattr(self.event_object, \"set_rec\"):\n        value2 = self.event_object.set_rec(value, value_rec, dismiss)\n\n    self.start_value = value2\n    self.SetValue(value2)\n    self.rec_value = value_rec\n\n    if dismiss:\n        self.Dismiss()\n\n    parent = self.GetParent()\n    if hasattr(parent, \"on_popup_control_change_value\"):\n        parent.on_popup_control_change_value(self)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.DataPopupControl.to_masked","title":"to_masked","text":"<pre><code>to_masked(**kwds)\n</code></pre> <p>Convert the internal text control to a masked.TextCtrl.</p> <p>Parameters:</p> Name Type Description Default <code>**kwds</code> <p>Parameters forwarded to SetCtrlParameters.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def to_masked(self, **kwds):\n    \"\"\"Convert the internal text control to a masked.TextCtrl.\n\n    Args:\n        **kwds: Parameters forwarded to SetCtrlParameters.\n    \"\"\"\n    self.win = ComboCtrl.GetTextCtrl(self)\n    self.win.__class__ = masked.TextCtrl\n    self.win._PostInit(setupEventHandling=True, name=\"maskedTextCtrl\", value=\"\")\n    self.win.SetCtrlParameters(**kwds)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.EDITABLELISTBOX","title":"EDITABLELISTBOX","text":"<pre><code>EDITABLELISTBOX(parent, **kwds)\n</code></pre> <p>               Bases: <code>EditableListBox</code>, <code>SchBaseCtrl</code></p> <p>Editable list box with add/remove buttons.</p> <p>Handles ctrleditablelistbox tag.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    wx.adv.EditableListBox.__init__(self, parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.ERRORLIST","title":"ERRORLIST","text":"<pre><code>ERRORLIST(parent, **kwds)\n</code></pre> <p>               Bases: <code>BitmapTextButton</code>, <code>SchBaseCtrl</code></p> <p>Error indicator button with tooltip.</p> <p>Handles ctrlerrorlist tag. Displays an error icon with a tooltip showing the error message from ldata or param['data'].</p> Tag arguments Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    self.bmp = bitmap_from_href(\"client://status/dialog-error.png\", 1)\n    kwds[\"style\"] = wx.NO_BORDER\n    kwds[\"bitmap\"] = self.bmp\n    BitmapTextButton.__init__(self, parent, **kwds)\n    if self.ldata:\n        self.SetToolTip(wx.ToolTip(self.ldata[0][0]))\n    elif self.param and \"data\" in self.param:\n        self.SetToolTip(wx.ToolTip(self.param[\"data\"]))\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.FILEBROWSEBUTTON","title":"FILEBROWSEBUTTON","text":"<pre><code>FILEBROWSEBUTTON(parent, **kwds)\n</code></pre> <p>               Bases: <code>FileBrowseButton</code>, <code>SchBaseCtrl</code></p> <p>File browse button with text field and Browse button.</p> <p>Handles ctrlfilebrowsebutton tag.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    kwds[\"labelText\"] = \"\"\n    kwds[\"buttonText\"] = str(_(\"Browse\"))\n    kwds[\"size\"] = (400, -1)\n    filebrowsebutton.FileBrowseButton.__init__(self, parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.FILEBROWSEBUTTON-functions","title":"Methods:","text":""},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.FILEBROWSEBUTTON.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Return the file path prefixed with '@'.</p> Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def GetValue(self):\n    \"\"\"Return the file path prefixed with '@'.\"\"\"\n    return \"@\" + super(FILEBROWSEBUTTON, self).GetValue()\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.GENERICDIR","title":"GENERICDIR","text":"<pre><code>GENERICDIR(parent, **kwds)\n</code></pre> <p>               Bases: <code>GenericDirCtrl</code>, <code>SchBaseCtrl</code></p> <p>Directory browser tree.</p> <p>Handles ctrlgenericdir tag.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    wx.GenericDirCtrl.__init__(self, parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.HTMLLISTBOX","title":"HTMLLISTBOX","text":"<pre><code>HTMLLISTBOX(parent, **kwds)\n</code></pre> <p>               Bases: <code>VListBox</code>, <code>SchBaseCtrl</code></p> <p>HTML-rendered virtual list box.</p> <p>Handles ctrlhtmllist tag. Uses HtmlViewerParser to render each item as HTML within a virtual scrolling list.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    self.choices = []\n    self.getItemFunct = None\n    self.h = None\n\n    if self.param and \"multiple\" in self.param:\n        style = kwds.get(\"style\", 0)\n        style |= wx.LB_MULTIPLE\n        kwds[\"style\"] = style\n\n    if \"size\" in kwds:\n        size = kwds[\"size\"]\n        if size[1] == -1:\n            kwds[\"size\"] = (size[0], 100)\n\n    tdata = self.get_tdata()\n    if tdata:\n        for row in tdata:\n            if hasattr(row[0], \"attrs\") and \"value\" in row[0].attrs:\n                selected = \"selected\" in row[0].attrs\n                self._append_html(row[0].attrs[\"value\"], selected, row[0].data)\n            else:\n                self._append_html(-1, False, row[0].data)\n\n    wx.VListBox.__init__(self, parent, **kwds)\n    self.SetItemCount(len(self.choices))\n    for i, choice in enumerate(self.choices):\n        if choice[1]:\n            self.SetSelection(i)\n    self.ScrollToRow(0)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.HTMLLISTBOX-functions","title":"Methods:","text":""},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.HTMLLISTBOX.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Return selected item IDs (multi-select only).</p> Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def GetValue(self):\n    \"\"\"Return selected item IDs (multi-select only).\"\"\"\n    ret = []\n    if self.HasMultipleSelection():\n        item, cookie = self.GetFirstSelected()\n        while item != wx.NOT_FOUND:\n            ret.append(self.choices[item][0])\n            item, cookie = self.GetNextSelected(cookie)\n    return ret\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.HTMLLISTBOX.OnDrawItem","title":"OnDrawItem","text":"<pre><code>OnDrawItem(dc, rect, n)\n</code></pre> <p>Draw item n.</p> Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def OnDrawItem(self, dc, rect, n):\n    \"\"\"Draw item n.\"\"\"\n    self._calc_or_draw(n, dc, rect, False)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.HTMLLISTBOX.OnMeasureItem","title":"OnMeasureItem","text":"<pre><code>OnMeasureItem(n)\n</code></pre> <p>Measure item n height.</p> Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def OnMeasureItem(self, n):\n    \"\"\"Measure item n height.\"\"\"\n    dc = wx.ClientDC(self)\n    w, h = self._calc_or_draw(n, dc, None, True)\n    return h\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.HTMLLISTBOX.append_html","title":"append_html","text":"<pre><code>append_html(html_txt)\n</code></pre> <p>Append a plain HTML item.</p> Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def append_html(self, html_txt):\n    \"\"\"Append a plain HTML item.\"\"\"\n    self._append_html(-1, False, html_txt)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.HTMLLISTBOX.append_text","title":"append_text","text":"<pre><code>append_text(txt)\n</code></pre> <p>Append plain text (newlines stripped).</p> Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def append_text(self, txt):\n    \"\"\"Append plain text (newlines stripped).\"\"\"\n    self.append_html(txt.replace(\"\\n\", \"\"))\n    self.SetItemCount(len(self.choices))\n    self.Refresh()\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.HTMLLISTBOX.append_texts","title":"append_texts","text":"<pre><code>append_texts(txt_list)\n</code></pre> <p>Append multiple text items.</p> Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def append_texts(self, txt_list):\n    \"\"\"Append multiple text items.\"\"\"\n    for txt in txt_list:\n        self.append_html(txt.replace(\"\\n\", \"\"))\n    self.SetItemCount(len(self.choices))\n    self.SetSelection(len(self.choices) - 1)\n    self.Refresh()\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.HTMLLISTBOX.process_refr_data","title":"process_refr_data","text":"<pre><code>process_refr_data(**kwds)\n</code></pre> <p>Refresh from new data.</p> Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def process_refr_data(self, **kwds):\n    \"\"\"Refresh from new data.\"\"\"\n    self.init_base(kwds)\n    self.choices = []\n    tdata = self.get_tdata()\n    if tdata:\n        for row in tdata:\n            if hasattr(row[0], \"attrs\") and \"value\" in row[0].attrs:\n                selected = \"selected\" in row[0].attrs\n                self._append_html(row[0].attrs[\"value\"], selected, row[0].data)\n            else:\n                self._append_html(-1, False, row[0].data)\n    self.SetItemCount(len(self.choices))\n    for i, choice in enumerate(self.choices):\n        if choice[1]:\n            self.SetSelection(i)\n    self.ScrollToRow(0)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.HTMLLISTBOX.scroll_to_line","title":"scroll_to_line","text":"<pre><code>scroll_to_line(line_no)\n</code></pre> <p>Scroll to and select line_no.</p> Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def scroll_to_line(self, line_no):\n    \"\"\"Scroll to and select line_no.\"\"\"\n    nr = line_no\n    count = self.GetItemCount()\n    if count &gt; 0:\n        if nr &gt;= count:\n            nr = count - 1\n        if nr &lt; 0:\n            nr = 0\n        self.SetSelection(nr)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.IMAGEBROWSEBUTTON","title":"IMAGEBROWSEBUTTON","text":"<pre><code>IMAGEBROWSEBUTTON(parent, **kwds)\n</code></pre> <p>               Bases: <code>FILEBROWSEBUTTON</code>, <code>SchBaseCtrl</code></p> <p>Image browse button with preview dialog.</p> <p>Handles ctrlimagebrowsebutton tag. Uses ImageDialog for visual image selection.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    kwds[\"labelText\"] = \"\"\n    kwds[\"buttonText\"] = str(_(\"Browse\"))\n    kwds[\"size\"] = (400, -1)\n    filebrowsebutton.FileBrowseButton.__init__(self, parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.IMAGEBROWSEBUTTON-functions","title":"Methods:","text":""},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.IMAGEBROWSEBUTTON.OnBrowse","title":"OnBrowse","text":"<pre><code>OnBrowse(event=None)\n</code></pre> <p>Open the image browser dialog.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>Button event (optional).</p> <code>None</code> Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def OnBrowse(self, event=None):\n    \"\"\"Open the image browser dialog.\n\n    Args:\n        event: Button event (optional).\n    \"\"\"\n    current = self.GetValue()\n    directory = Path(current).parent\n    if Path(current).is_dir():\n        directory = current\n        current = \"\"\n    elif directory and Path(directory).is_dir():\n        current = Path(current).name\n        directory = str(directory)\n    else:\n        directory = self.startDirectory\n        current = \"\"\n\n    dlg = wx.lib.imagebrowser.ImageDialog(self, current)\n    if dlg.ShowModal() == wx.ID_OK:\n        self.SetValue(dlg.GetFile())\n    dlg.Destroy()\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.POPUPHTML","title":"POPUPHTML","text":"<pre><code>POPUPHTML(parent, **kwds)\n</code></pre> <p>               Bases: <code>DataPopupControl</code>, <code>SchBaseCtrl</code></p> <p>Popup HTML control with data-driven dialog.</p> <p>Handles ctrlpopuphtml tag. A text field with a dropdown button that opens an HTML popup for selection. Content is loaded from an href endpoint.</p> Tag arguments The href base address schema <p>href + \"dialog\"       - address of dialog window href + \"test?value=\"  - address for autocomplete records</p> Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    DataPopupControl.__init__(self, parent, **kwds)\n    if self.param and \"IN_NEW_WIN\" in self.param:\n        self.simpleDialog = False\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.STATICTEXT","title":"STATICTEXT","text":"<pre><code>STATICTEXT(parent, **kwds)\n</code></pre> <p>               Bases: <code>StaticText</code>, <code>SchBaseCtrl</code></p> <p>Static non-editable text label.</p> <p>Handles ctrlstatictext tag. Displays read-only text.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    wx.StaticText.__init__(self, parent, **kwds)\n    if self.label:\n        self.SetLabel(self.label)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.SchBaseCtrl","title":"SchBaseCtrl","text":"<pre><code>SchBaseCtrl(parent, kwds)\n</code></pre> <p>Base mixin class for all SchForm wxPython widgets.</p> <p>Provides: - Attribute initialization from HTML tag keyword arguments - Parent form/page navigation - Server data loading for tdata (table data) and ldata (list data) - Common keyboard handling (Escape, Tab) - Widget lifecycle: after_create signal, unique name management</p> <p>Initialize base control attributes.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent wxPython window.</p> required <code>kwds</code> <p>Keyword argument dict parsed from HTML tag attributes.</p> required Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def __init__(self, parent, kwds):\n    \"\"\"Initialize base control attributes.\n\n    Args:\n        parent: Parent wxPython window.\n        kwds: Keyword argument dict parsed from HTML tag attributes.\n    \"\"\"\n    self.unique_name = None\n    self.init_base(kwds, parent)\n    self.accept_focus = True\n    self.acc_tab_bind = False\n    form = self.get_parent_form()\n    if form:\n        form.signal_from_child(self, \"__init__\")\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.SchBaseCtrl-functions","title":"Methods:","text":""},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.SchBaseCtrl.CanAcceptFocus","title":"CanAcceptFocus","text":"<pre><code>CanAcceptFocus()\n</code></pre> <p>Return True if the control can accept keyboard focus.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def CanAcceptFocus(self):\n    \"\"\"Return True if the control can accept keyboard focus.\"\"\"\n    if self.IsShown() and self.IsEnabled():\n        return self.accept_focus\n    return False\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.SchBaseCtrl.CanAcceptFocusFromKeyboard","title":"CanAcceptFocusFromKeyboard","text":"<pre><code>CanAcceptFocusFromKeyboard()\n</code></pre> <p>Return True if the control accepts focus from keyboard navigation.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def CanAcceptFocusFromKeyboard(self):\n    \"\"\"Return True if the control accepts focus from keyboard navigation.\"\"\"\n    return self.CanAcceptFocus()\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.SchBaseCtrl.after_create","title":"after_create","text":"<pre><code>after_create()\n</code></pre> <p>Called immediately after the widget is fully constructed.</p> <p>Binds keyboard events, executes the 'onload' script (if any), calls the ext_init hook if present, and signals the parent form that this child is ready.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def after_create(self):\n    \"\"\"Called immediately after the widget is fully constructed.\n\n    Binds keyboard events, executes the 'onload' script (if any),\n    calls the __ext_init__ hook if present, and signals the parent\n    form that this child is ready.\n    \"\"\"\n    self.Bind(wx.EVT_KEY_DOWN, self.on_key_down_base)\n    if self.onload:\n        d = {\"wx\": wx, \"self\": self}\n        try:\n            exec(self.onload, d)\n        except Exception:\n            logger.exception(\"Error executing onload script for %s\", self.GetName())\n\n    if hasattr(self, \"__ext_init__\"):\n        self.__ext_init__()\n\n    self.get_parent_form().signal_from_child(self, \"after_create\")\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.SchBaseCtrl.get_ldata","title":"get_ldata","text":"<pre><code>get_ldata()\n</code></pre> <p>Return hierarchical list data, loading from server if necessary.</p> <p>Returns:</p> Type Description <p>List data (nested tree structure), or None if unavailable.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_ldata(self):\n    \"\"\"Return hierarchical list data, loading from server if necessary.\n\n    Returns:\n        List data (nested tree structure), or None if unavailable.\n    \"\"\"\n    if self.ldata is not None:\n        return self.ldata\n    if self.ldatabuf is not None:\n        return self.ldatabuf\n    if self.src:\n        self.refresh_ldata()\n    return self.ldatabuf\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.SchBaseCtrl.get_parent_form","title":"get_parent_form","text":"<pre><code>get_parent_form()\n</code></pre> <p>Walk up the widget hierarchy to find the parent SchForm.</p> <p>Returns:</p> Type Description <p>The SchForm ancestor, or None if not found.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_parent_form(self):\n    \"\"\"Walk up the widget hierarchy to find the parent SchForm.\n\n    Returns:\n        The SchForm ancestor, or None if not found.\n    \"\"\"\n    parent = self.parent\n    while parent is not None:\n        if type(parent).__name__ == \"SchForm\":\n            return parent\n        parent = parent.GetParent()\n    return None\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.SchBaseCtrl.get_parent_page","title":"get_parent_page","text":"<pre><code>get_parent_page()\n</code></pre> <p>Return the parent page that contains this control's form.</p> <p>Returns:</p> Type Description <p>The parent SchPage, or None if not found.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_parent_page(self):\n    \"\"\"Return the parent page that contains this control's form.\n\n    Returns:\n        The parent SchPage, or None if not found.\n    \"\"\"\n    form = self.get_parent_form()\n    if form:\n        return form.get_parent_page()\n    return None\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.SchBaseCtrl.get_tdata","title":"get_tdata","text":"<pre><code>get_tdata()\n</code></pre> <p>Return table data, loading from server if necessary.</p> <p>Returns:</p> Type Description <p>Table data (list of rows), or None if unavailable.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_tdata(self):\n    \"\"\"Return table data, loading from server if necessary.\n\n    Returns:\n        Table data (list of rows), or None if unavailable.\n    \"\"\"\n    if self.tdata is not None:\n        return self.tdata\n    if self.tdatabuf is not None:\n        return self.tdatabuf\n    if self.src:\n        self.refresh_tdata()\n    return self.tdatabuf\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.SchBaseCtrl.get_unique_name","title":"get_unique_name","text":"<pre><code>get_unique_name()\n</code></pre> <p>Return the unique name identifier.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_unique_name(self):\n    \"\"\"Return the unique name identifier.\"\"\"\n    return self.unique_name\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.SchBaseCtrl.init_base","title":"init_base","text":"<pre><code>init_base(kwds, parent=None)\n</code></pre> <p>Initialize all control attributes from kwds.</p> <p>Extracts well-known attributes (href, label, id, target, value, valuetype, defaultvalue, length, maxlength, readonly, hidden, src, onload, tdata, ldata, data, param, style) from kwds and assigns them as instance attributes.</p> <p>Parameters:</p> Name Type Description Default <code>kwds</code> <p>Keyword argument dict parsed from HTML tag attributes.</p> required <code>parent</code> <p>Optional parent window reference.</p> <code>None</code> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def init_base(self, kwds, parent=None):\n    \"\"\"Initialize all control attributes from kwds.\n\n    Extracts well-known attributes (href, label, id, target, value,\n    valuetype, defaultvalue, length, maxlength, readonly, hidden,\n    src, onload, tdata, ldata, data, param, style) from kwds\n    and assigns them as instance attributes.\n\n    Args:\n        kwds: Keyword argument dict parsed from HTML tag attributes.\n        parent: Optional parent window reference.\n    \"\"\"\n    if parent:\n        self.parent = parent\n\n    self.ldatabuf = None\n\n    self.tag = None\n    if \"param\" in kwds and \"tag\" in kwds[\"param\"]:\n        self.tag = kwds[\"param\"][\"tag\"]\n\n    self.href = _extract_kwarg(kwds, \"href\", None)\n    if self.href is None:\n        self.href = \"\"\n\n    self.label = _extract_kwarg(kwds, \"label\", None)\n    self.nr_id = _extract_kwarg(kwds, \"id\", None)\n    self.target = _extract_kwarg(kwds, \"target\", \"_blank\")\n    self.value = _extract_kwarg(kwds, \"value\", None)\n    self.valuetype = _extract_kwarg(kwds, \"valuetype\", \"data\")\n    self.defaultvalue = _extract_kwarg(kwds, \"defaultvalue\", None)\n    self.length = int(_extract_kwarg(kwds, \"length\", 0))\n    self.maxlength = int(_extract_kwarg(kwds, \"maxlength\", 0))\n    self.readonly = _extract_kwarg(kwds, \"readonly\", False)\n    self.hidden = True if _extract_kwarg(kwds, \"hidden\", False) else False\n    self.src = _extract_kwarg(kwds, \"src\", None)\n    self.onload = _extract_kwarg(kwds, \"onload\", None)\n\n    # tdata / tdatabuf: table data (already parsed) or lazily loaded\n    self.tdata = _extract_kwarg(kwds, \"tdata\", None)\n    self.tdatabuf = None\n    if self.tdata is not None:\n        self.tdatabuf = None\n\n    # ldata: hierarchical list data (already parsed) or lazily loaded\n    self.ldata = _extract_kwarg(kwds, \"ldata\", None)\n\n    # data: URL-encoded string data\n    raw_data = _extract_kwarg(kwds, \"data\", None)\n    self.data = unquote(raw_data) if raw_data is not None else None\n\n    self.param = _extract_kwarg(kwds, \"param\", None)\n    self.style = _extract_kwarg(kwds, \"style\", None)\n\n    # Invoke any registered ctrl_process hooks for this tag type\n    ctrl_process = wx.GetApp().ctrl_process\n    if self.tag in ctrl_process:\n        for fun in ctrl_process[self.tag]:\n            fun(self)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.SchBaseCtrl.is_ctrl_block","title":"is_ctrl_block","text":"<pre><code>is_ctrl_block()\n</code></pre> <p>Return True if this control is a block-level element.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def is_ctrl_block(self):\n    \"\"\"Return True if this control is a block-level element.\"\"\"\n    return self.GetParent().is_ctrl_block(self)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.SchBaseCtrl.load_data_from_server","title":"load_data_from_server","text":"<pre><code>load_data_from_server(path)\n</code></pre> <p>Load raw binary data from a server URL.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <p>URL path to fetch.</p> required <p>Returns:</p> Type Description <p>Raw response data (bytes).</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def load_data_from_server(self, path):\n    \"\"\"Load raw binary data from a server URL.\n\n    Args:\n        path: URL path to fetch.\n\n    Returns:\n        Raw response data (bytes).\n    \"\"\"\n    http = wx.GetApp().get_http(self.parent)\n    response = http.get(self, str(path))\n    return response.ptr()\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.SchBaseCtrl.load_string_from_server","title":"load_string_from_server","text":"<pre><code>load_string_from_server(path)\n</code></pre> <p>Load a text response from a server URL.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <p>URL path to fetch.</p> required <p>Returns:</p> Type Description <p>Response body as a string.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def load_string_from_server(self, path):\n    \"\"\"Load a text response from a server URL.\n\n    Args:\n        path: URL path to fetch.\n\n    Returns:\n        Response body as a string.\n    \"\"\"\n    http = wx.GetApp().get_http(self.parent)\n    response = http.get(self, str(path))\n    return response.str()\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.SchBaseCtrl.on_key_down_base","title":"on_key_down_base","text":"<pre><code>on_key_down_base(event)\n</code></pre> <p>Default key-down handler for Escape and Tab navigation.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>wx.KeyEvent.</p> required Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def on_key_down_base(self, event):\n    \"\"\"Default key-down handler for Escape and Tab navigation.\n\n    Args:\n        event: wx.KeyEvent.\n    \"\"\"\n    if event.GetKeyCode() == wx.WXK_ESCAPE:\n        wx.CallAfter(self.GetParent().cancel)\n    elif event.GetKeyCode() == wx.WXK_TAB:\n        if event.ShiftDown():\n            self.GetParent().Navigate(self, True)\n        else:\n            self.GetParent().Navigate(self, False)\n    else:\n        event.Skip()\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.SchBaseCtrl.refresh_ldata","title":"refresh_ldata","text":"<pre><code>refresh_ldata()\n</code></pre> <p>Refresh hierarchical list data from the server.</p> <p>Loads from self.src and parses into self.ldatabuf using TreeParser.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def refresh_ldata(self):\n    \"\"\"Refresh hierarchical list data from the server.\n\n    Loads from self.src and parses into self.ldatabuf using TreeParser.\n    \"\"\"\n    if self.ldata is None and self.src:\n        lista = self.load_data_from_server(self.src)\n        mp = TreeParser()\n        mp.feed(lista)\n        mp.close()\n        self.ldatabuf = mp.tree_parent[0][1]\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.SchBaseCtrl.refresh_tdata","title":"refresh_tdata","text":"<pre><code>refresh_tdata(html_src=None)\n</code></pre> <p>Refresh table data from the server or provided HTML source.</p> <p>Parameters:</p> Name Type Description Default <code>html_src</code> <p>Optional HTML string containing table data. If None, loads from self.src.</p> <code>None</code> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def refresh_tdata(self, html_src=None):\n    \"\"\"Refresh table data from the server or provided HTML source.\n\n    Args:\n        html_src: Optional HTML string containing table data.\n            If None, loads from self.src.\n    \"\"\"\n    if self.tdata is None and self.src:\n        if html_src:\n            tables = html_src\n        else:\n            tables = self.load_data_from_server(self.src).decode(\"utf-8\")\n        self.tdatabuf = tdata_from_html(tables, wx.GetApp().http)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.SchBaseCtrl.set_acc_key_tab","title":"set_acc_key_tab","text":"<pre><code>set_acc_key_tab(tab)\n</code></pre> <p>Register an accelerator table for this widget.</p> <p>Parameters:</p> Name Type Description Default <code>tab</code> <p>List or tuple of accelerator elements. Each element has structure: (flag, keycode, callback function).</p> required <p>Returns:</p> Type Description <p>Result from the parent's set_acc_key_tab method.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def set_acc_key_tab(self, tab):\n    \"\"\"Register an accelerator table for this widget.\n\n    Args:\n        tab: List or tuple of accelerator elements. Each element has\n            structure: (flag, keycode, callback function).\n\n    Returns:\n        Result from the parent's set_acc_key_tab method.\n    \"\"\"\n    return self.GetParent().set_acc_key_tab(self, tab)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.SchBaseCtrl.set_unique_name","title":"set_unique_name","text":"<pre><code>set_unique_name(name)\n</code></pre> <p>Assign a unique name identifier to this control.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def set_unique_name(self, name):\n    \"\"\"Assign a unique name identifier to this control.\"\"\"\n    self.unique_name = name\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.TREE","title":"TREE","text":"<pre><code>TREE(parent, **kwds)\n</code></pre> <p>               Bases: <code>TreeCtrl</code>, <code>SchBaseCtrl</code></p> <p>Tree control with hierarchical data.</p> <p>Handles ctrltree tag. Builds a tree from ldata with folder/file icons and supports activation (navigation) on click.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    kwds[\"style\"] = wx.TR_HIDE_ROOT | wx.TR_DEFAULT_STYLE\n    wx.TreeCtrl.__init__(self, parent, **kwds)\n\n    isz = (16, 16)\n    il = wx.ImageList(isz[0], isz[1])\n    self.fldridx = il.Add(\n        wx.ArtProvider.GetBitmap(wx.ART_FOLDER, wx.ART_OTHER, isz)\n    )\n    self.fldropenidx = il.Add(\n        wx.ArtProvider.GetBitmap(wx.ART_FILE_OPEN, wx.ART_OTHER, isz)\n    )\n    self.fileidx = il.Add(\n        wx.ArtProvider.GetBitmap(wx.ART_NORMAL_FILE, wx.ART_OTHER, isz)\n    )\n    self.fileidxmark = il.Add(\n        wx.ArtProvider.GetBitmap(wx.ART_TICK_MARK, wx.ART_OTHER, isz)\n    )\n    self.SetImageList(il)\n    self.il = il\n    self.root = self.AddRoot(\"/\")\n\n    ldata = self.get_ldata()\n    if ldata:\n        self._append_list(self.root, ldata)\n    self.Bind(wx.EVT_TREE_ITEM_ACTIVATED, self._on_activated)\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.TREELIST","title":"TREELIST","text":"<pre><code>TREELIST(parent, **kwds)\n</code></pre> <p>               Bases: <code>HyperTreeList</code>, <code>SchBaseCtrl</code></p> <p>Tree list with multiple columns.</p> <p>Handles ctrltreelist tag. Supports '||' separated column data and folder/file icons.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/display.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    kwds[\"agwStyle\"] = wx.TR_HIDE_ROOT\n    TreeListCtrl.__init__(self, parent, **kwds)\n\n    isz = (16, 16)\n    il = wx.ImageList(isz[0], isz[1])\n    self.fldridx = il.Add(\n        wx.ArtProvider.GetBitmap(wx.ART_FOLDER, wx.ART_OTHER, isz)\n    )\n    self.fldropenidx = il.Add(\n        wx.ArtProvider.GetBitmap(wx.ART_FILE_OPEN, wx.ART_OTHER, isz)\n    )\n    self.fileidx = il.Add(\n        wx.ArtProvider.GetBitmap(wx.ART_NORMAL_FILE, wx.ART_OTHER, isz)\n    )\n    self.fileidxmark = il.Add(\n        wx.ArtProvider.GetBitmap(wx.ART_TICK_MARK, wx.ART_OTHER, isz)\n    )\n    self.SetImageList(il)\n    self.il = il\n\n    columns = self.label.split(\"||\")\n    for col_title in columns:\n        try:\n            self.AppendColumn(col_title)\n        except AttributeError:\n            self.AddColumn(col_title)\n    self.SetColumnWidth(0, 175)\n    self.root = self.AddRoot(\"/\")\n\n    ldata = self.get_ldata()\n    if ldata:\n        self._append_list(self.root, ldata)\n    self.Refresh()\n</code></pre>"},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display-functions","title":"Functions:","text":""},{"location":"api/guictrl_display/#pytigon_gui.guictrl.display.bitmap_from_href","title":"bitmap_from_href","text":"<pre><code>bitmap_from_href(href, size_type=SIZE_DEFAULT)\n</code></pre> <p>Load a resource and convert it to a wx.Bitmap.</p> <p>Parameters:</p> Name Type Description Default <code>href</code> <p>Resource identifier. Supported formats: - ArtProvider id, e.g. 'wx.ART_COPY' - Font Awesome id, e.g. 'fa://icon_name' or 'fa://bus' - Local library image, e.g. 'png://image_name.png' - Client-side image, e.g. 'client://image_name.png' - Inline SVG data, e.g. 'data:image/svg+xml,...' - Remote HTTP/HTTPS URL, e.g. 'http://www.pytigon.cloud/test.png'</p> required <code>size_type</code> <p>One of SIZE_DEFAULT, SIZE_SMALL, SIZE_MEDIUM, SIZE_LARGE.</p> <code>SIZE_DEFAULT</code> <p>Returns:</p> Type Description <p>wx.Bitmap. If the resource cannot be loaded, ART_MISSING_IMAGE is</p> <p>returned.</p> Source code in <code>pytigon_gui/guilib/image.py</code> <pre><code>def bitmap_from_href(href, size_type=SIZE_DEFAULT):\n    \"\"\"Load a resource and convert it to a wx.Bitmap.\n\n    Args:\n        href: Resource identifier. Supported formats:\n            - ArtProvider id, e.g. 'wx.ART_COPY'\n            - Font Awesome id, e.g. 'fa://icon_name' or 'fa://bus'\n            - Local library image, e.g. 'png://image_name.png'\n            - Client-side image, e.g. 'client://image_name.png'\n            - Inline SVG data, e.g. 'data:image/svg+xml,...'\n            - Remote HTTP/HTTPS URL, e.g. 'http://www.pytigon.cloud/test.png'\n        size_type: One of SIZE_DEFAULT, SIZE_SMALL, SIZE_MEDIUM, SIZE_LARGE.\n\n    Returns:\n        wx.Bitmap. If the resource cannot be loaded, ART_MISSING_IMAGE is\n        returned.\n    \"\"\"\n    size_type2 = size_type\n    if \"?\" in href:\n        x = href.split(\"?\")\n        href2 = x[0]\n        if \"=\" in x[1]:\n            try:\n                size_type2 = int(x[1].split(\"=\")[1])\n            except (ValueError, IndexError):\n                size_type2 = 1\n    else:\n        href2 = href\n\n    if size_type2 == SIZE_SMALL:\n        icon_size = 16\n    elif size_type2 == SIZE_MEDIUM:\n        icon_size = 22\n    else:\n        icon_size = 32\n\n    if href2[:3] == \"wx.\":\n        if (art_id := _resolve_art_id(href2)) is not None:\n            bmp = wx.ArtProvider.GetBitmap(\n                art_id, wx.ART_TOOLBAR, (icon_size, icon_size)\n            )\n        else:\n            bmp = wx.ArtProvider.GetBitmap(\n                wx.ART_MISSING_IMAGE, wx.ART_TOOLBAR, (icon_size, icon_size)\n            )\n    elif href.startswith(\"client://\"):\n        image = wx.Image(\n            str(wx.GetApp().src_path)\n            + f\"/static/icons/{icon_size}x{icon_size}/\"\n            + href2[9:]\n        )\n        bmp = wx.Bitmap(image)\n    elif href.startswith(\"png://\"):\n        image = wx.Image(\n            str(wx.GetApp().src_path)\n            + f\"/static/icons/{icon_size}x{icon_size}/\"\n            + href2[6:]\n        )\n        bmp = wx.Bitmap(image)\n    elif href.startswith(\"fa://\"):\n        suffix = \"\" if \".png\" in href.lower() else \".png\"\n        try:\n            image = wx.Image(\n                str(wx.GetApp().src_path)\n                + f\"/static/fonts/fork-awesome/fonts/{icon_size}x{icon_size}/\"\n                + href2[5:].replace(\"fa-\", \"\")\n                + suffix\n            )\n            image = image.AdjustChannels(1, 1, 1, 0.55)\n            bmp = wx.Bitmap(image)\n        except Exception:\n            logger.warning(\"Cannot load image: %s\", href2)\n            bmp = wx.Bitmap()\n    elif href.startswith(\"data:image/svg+xml\"):\n        x = href.split(\",\", 1)\n        svg_code = x[1]\n        svg = SVGimage.CreateFromBytes(svg_code.encode(\"utf-8\"))\n        bmp = svg.ConvertToBitmap(width=icon_size, height=icon_size)\n    else:\n        if href.lower().startswith(\"http\"):\n            http = wx.GetApp().get_http(None)\n            response = http.get(None, str(href))\n            if response.ret_code != 404:\n                s = response.ptr()\n                stream = BytesIO(s)\n                bmp = wx.Bitmap(wx.Image(stream))\n            else:\n                bmp = wx.ArtProvider.GetBitmap(\n                    wx.ART_MISSING_IMAGE, wx.ART_TOOLBAR, (32, 32)\n                )\n        else:\n            bmp = wx.ArtProvider.GetBitmap(\n                wx.ART_MISSING_IMAGE, wx.ART_TOOLBAR, (32, 32)\n            )\n    return bmp\n</code></pre>"},{"location":"api/guictrl_factory/","title":"guictrl.factory \u2013 Control Factory","text":"<p>Factory pattern implementation for creating control instances from tag definitions, with registration and lookup support.</p>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory","title":"factory","text":"<p>Factory and dispatch functions for the SchForm GUI framework.</p> <p>These UPPERCASE functions serve as the primary interface for creating widget instances from HTML tag handlers (tag.py). They dispatch to the appropriate widget class based on tag attributes, parameters, and configuration.</p>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory-attributes","title":"Attributes","text":""},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.AUTOCOMPLETE","title":"AUTOCOMPLETE  <code>module-attribute</code>","text":"<pre><code>AUTOCOMPLETE = STYLEDTEXT\n</code></pre> <p>Alias for STYLEDTEXT; may be replaced by plugins at runtime.</p>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.BITMAPBUTTON","title":"BITMAPBUTTON  <code>module-attribute</code>","text":"<pre><code>BITMAPBUTTON = _make_button_class(wx.BitmapButton, is_bitmap_button=True)\n</code></pre> <p>Button with a bitmap, based on wx.BitmapButton.</p>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.CLOSEBUTTON","title":"CLOSEBUTTON  <code>module-attribute</code>","text":"<pre><code>CLOSEBUTTON = _make_button_class(\n    BitmapTextButton, is_bitmap_button=True, is_close_button=True\n)\n</code></pre> <p>Close button that cancels the child form when clicked.</p>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.GENBITMAPBUTTON","title":"GENBITMAPBUTTON  <code>module-attribute</code>","text":"<pre><code>GENBITMAPBUTTON = _make_button_class(buttons.GenBitmapButton, is_bitmap_button=True)\n</code></pre> <p>Generic bitmap button with no border.</p>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.GENBITMAPBUTTONTXT","title":"GENBITMAPBUTTONTXT  <code>module-attribute</code>","text":"<pre><code>GENBITMAPBUTTONTXT = _make_button_class(BitmapTextButton, is_bitmap_button=True)\n</code></pre> <p>Bitmap text button (normal icon size).</p>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.GENBITMAPBUTTONTXT_SMALL","title":"GENBITMAPBUTTONTXT_SMALL  <code>module-attribute</code>","text":"<pre><code>GENBITMAPBUTTONTXT_SMALL = _make_button_class(\n    BitmapTextButton, is_bitmap_button=True, icon_size=1\n)\n</code></pre> <p>Bitmap text button (small icon size).</p>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.GENBITMAPTEXTBUTTON","title":"GENBITMAPTEXTBUTTON  <code>module-attribute</code>","text":"<pre><code>GENBITMAPTEXTBUTTON = _make_button_class(\n    buttons.GenBitmapTextButton, is_bitmap_button=True\n)\n</code></pre> <p>Generic bitmap button with text label.</p>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.MENUBUTTON","title":"MENUBUTTON  <code>module-attribute</code>","text":"<pre><code>MENUBUTTON = _make_menu_button_class(SIMPLE_BUTTON)\n</code></pre> <p>Menu button based on a simple push button.</p>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.MENUTOOLBARBUTTON","title":"MENUTOOLBARBUTTON  <code>module-attribute</code>","text":"<pre><code>MENUTOOLBARBUTTON = _make_menu_button_class(GENBITMAPBUTTONTXT)\n</code></pre> <p>Menu button styled as a toolbar button with bitmap and text.</p>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.NOBG_BUTTON","title":"NOBG_BUTTON  <code>module-attribute</code>","text":"<pre><code>NOBG_BUTTON = GENBITMAPBUTTONTXT\n</code></pre> <p>Alias for a button without background (same as GENBITMAPBUTTONTXT).</p>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.NOBG_BUTTON_TXT","title":"NOBG_BUTTON_TXT  <code>module-attribute</code>","text":"<pre><code>NOBG_BUTTON_TXT = GENBITMAPBUTTONTXT\n</code></pre> <p>Alias for a text button without background.</p>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.PLATEBUTTON","title":"PLATEBUTTON  <code>module-attribute</code>","text":"<pre><code>PLATEBUTTON = _make_button_class(platebtn.PlateButton, is_bitmap_button=True)\n</code></pre> <p>Styled plate button with gradient background and bitmap.</p>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.SIMPLE_BUTTON","title":"SIMPLE_BUTTON  <code>module-attribute</code>","text":"<pre><code>SIMPLE_BUTTON = _make_button_class(wx.Button)\n</code></pre> <p>Standard push button without bitmap.</p>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory-classes","title":"Classes","text":""},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.CHECKLISTBOX","title":"CHECKLISTBOX","text":"<pre><code>CHECKLISTBOX(parent, **kwds)\n</code></pre> <p>               Bases: <code>CheckListBox</code>, <code>SchBaseCtrl</code></p> <p>Check list box allowing multiple selections with checkboxes.</p> <p>Handles ctrlchecklistbox tag. Provides a list where each item has an associated checkbox. GetValue returns a list of values for checked items.</p> Tag arguments <p>Initialize the check list box.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.CheckListBox with WANTS_CHARS style.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the check list box.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.CheckListBox with WANTS_CHARS style.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n\n    if \"style\" in kwds:\n        kwds[\"style\"] |= wx.WANTS_CHARS\n    else:\n        kwds[\"style\"] = wx.WANTS_CHARS\n    wx.CheckListBox.__init__(self, parent, **kwds)\n    self._init_data(self.get_tdata())\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.CHECKLISTBOX-functions","title":"Methods:","text":""},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.CHECKLISTBOX.GetBestSize","title":"GetBestSize","text":"<pre><code>GetBestSize()\n</code></pre> <p>Return the best size for the control.</p> <p>Returns:</p> Type Description <p>Tuple of (300, 68).</p> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def GetBestSize(self):\n    \"\"\"Return the best size for the control.\n\n    Returns:\n        Tuple of (300, 68).\n    \"\"\"\n    return (300, 68)\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.CHECKLISTBOX.GetMinSize","title":"GetMinSize","text":"<pre><code>GetMinSize()\n</code></pre> <p>Return the minimum size (same as best size).</p> <p>Returns:</p> Type Description <p>Tuple of (300, 68).</p> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def GetMinSize(self):\n    \"\"\"Return the minimum size (same as best size).\n\n    Returns:\n        Tuple of (300, 68).\n    \"\"\"\n    return self.GetBestSize()\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.CHECKLISTBOX.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Get values of all checked items.</p> <p>Returns:</p> Type Description <p>List of values for checked items. Uses the 'value'</p> <p>attribute from item attrs if present, otherwise the</p> <p>item's display text.</p> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get values of all checked items.\n\n    Returns:\n        List of values for checked items. Uses the 'value'\n        attribute from item attrs if present, otherwise the\n        item's display text.\n    \"\"\"\n    ret = []\n    data = self.get_tdata()\n    items = self.GetCheckedItems()\n    for item in items:\n        if hasattr(data[item][0], \"attrs\") and \"value\" in data[item][0].attrs:\n            ret.append(data[item][0].attrs[\"value\"])\n        else:\n            ret.append(data[item][0].data)\n    return ret\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.CHECKLISTBOX.process_refr_data","title":"process_refr_data","text":"<pre><code>process_refr_data(**kwds)\n</code></pre> <p>Refresh the list with new data.</p> <p>Parameters:</p> Name Type Description Default <code>**kwds</code> <p>New keyword arguments.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def process_refr_data(self, **kwds):\n    \"\"\"Refresh the list with new data.\n\n    Args:\n        **kwds: New keyword arguments.\n    \"\"\"\n    self.init_base(kwds)\n    self.Clear()\n    self._init_data(self.get_tdata())\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.CompositePanel","title":"CompositePanel","text":"<pre><code>CompositePanel(parent, **kwds)\n</code></pre> <p>               Bases: <code>Panel</code>, <code>SchBaseCtrl</code></p> <p>Composite panel that wraps a primary control.</p> <p>Used by SELECT2 to embed a Select2Base control alongside optional action buttons in a horizontal layout.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    wx.Panel.__init__(self, parent, **kwds)\n    self.ctrl = None\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.CompositePanel-functions","title":"Methods:","text":""},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.CompositePanel.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Get value from the wrapped control.</p> <p>Returns:</p> Type Description <p>String value or None.</p> Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get value from the wrapped control.\n\n    Returns:\n        String value or None.\n    \"\"\"\n    if self.ctrl:\n        return str(self.ctrl.GetValue())\n    else:\n        return None\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.CompositePanel.set_main_ctrl","title":"set_main_ctrl","text":"<pre><code>set_main_ctrl(ctrl)\n</code></pre> <p>Set the primary wrapped control.</p> <p>Parameters:</p> Name Type Description Default <code>ctrl</code> <p>The main wx control to embed.</p> required Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def set_main_ctrl(self, ctrl):\n    \"\"\"Set the primary wrapped control.\n\n    Args:\n        ctrl: The main wx control to embed.\n    \"\"\"\n    self.ctrl = ctrl\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.DBCHOICE","title":"DBCHOICE","text":"<pre><code>DBCHOICE(parent, **kwds)\n</code></pre> <p>               Bases: <code>CHOICE</code></p> <p>Database-bound choice control.</p> <p>Handles ctrldbchoice tag. Extends CHOICE to return the 'value' attribute from the selected item's attrs when in readonly mode, making it suitable for database key selection.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the choice control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to POPUPHTML. Sets default href\n            to the list dialog if not provided.\n    \"\"\"\n    if \"href\" not in kwds:\n        kwds[\"href\"] = wx.GetApp().make_href(\"/schsys/listdialog/\")\n\n    if \"style\" in kwds:\n        kwds[\"style\"] = kwds[\"style\"] | wx.WANTS_CHARS\n    else:\n        kwds[\"style\"] = wx.WANTS_CHARS\n\n    if \"size\" not in kwds:\n        kwds[\"size\"] = wx.Size(250, -1)\n    self.height = 250\n    kwds[\"dialog_with_value\"] = False\n    POPUPHTML.__init__(self, parent, **kwds)\n\n    self._init_choices(self.get_tdata())\n\n    accel_table = [\n        (0, wx.WXK_F2, self._on_ext_button_click),\n    ]\n    self.set_acc_key_tab(accel_table)\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.DBCHOICE-functions","title":"Methods:","text":""},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.DBCHOICE.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Get the database value of the selected item.</p> <p>When readonly, returns the 'value' attribute from the selected item's attrs dict (typically a database key). Otherwise delegates to POPUPHTML.</p> <p>Returns:</p> Type Description <p>The database value string or None.</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get the database value of the selected item.\n\n    When readonly, returns the 'value' attribute from the\n    selected item's attrs dict (typically a database key).\n    Otherwise delegates to POPUPHTML.\n\n    Returns:\n        The database value string or None.\n    \"\"\"\n    if self.readonly:\n        value = self.get_rec()\n        if value:\n            if hasattr(value, \"attrs\") and \"value\" in value.attrs:\n                return value.attrs[\"value\"]\n            else:\n                return value.data\n        else:\n            return None\n    else:\n        return POPUPHTML.GetValue(self)\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.STYLEDTEXT","title":"STYLEDTEXT","text":"<pre><code>STYLEDTEXT(parent, **kwds)\n</code></pre> <p>               Bases: <code>TextCtrl</code>, <code>SchBaseCtrl</code></p> <p>Multi-line styled text control.</p> <p>Handles ctrlstyledtext tag. Always uses wx.TE_MULTILINE style. Supports initial data from the 'data' parameter.</p> Tag arguments <p>Initialize the styled (multi-line) text control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.TextCtrl with TE_MULTILINE style.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/text.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the styled (multi-line) text control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.TextCtrl with TE_MULTILINE style.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    if \"style\" in kwds:\n        style = kwds[\"style\"]\n        style = style | wx.TE_MULTILINE\n        kwds[\"style\"] = style\n    else:\n        kwds[\"style\"] = wx.TE_MULTILINE\n    wx.TextCtrl.__init__(self, parent, **kwds)\n    if self.param and \"data\" in self.param:\n        self.SetValue(self.param[\"data\"])\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.STYLEDTEXT-functions","title":"Methods:","text":""},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.STYLEDTEXT.SetValue","title":"SetValue","text":"<pre><code>SetValue(value)\n</code></pre> <p>Set the text value, stripping a leading newline if present.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>String or bytes value to set. A single leading newline is stripped for cleaner display.</p> required Source code in <code>pytigon_gui/guictrl/input/text.py</code> <pre><code>def SetValue(self, value):\n    \"\"\"Set the text value, stripping a leading newline if present.\n\n    Args:\n        value: String or bytes value to set. A single leading\n            newline is stripped for cleaner display.\n    \"\"\"\n    if value.startswith(\"\\n\"):\n        value2 = value[1:]\n    else:\n        value2 = value\n    if isinstance(value2, str):\n        return wx.TextCtrl.SetValue(self, value2)\n    else:\n        return wx.TextCtrl.SetValue(self, value2.decode(\"utf-8\"))\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.SchBaseCtrl","title":"SchBaseCtrl","text":"<pre><code>SchBaseCtrl(parent, kwds)\n</code></pre> <p>Base mixin class for all SchForm wxPython widgets.</p> <p>Provides: - Attribute initialization from HTML tag keyword arguments - Parent form/page navigation - Server data loading for tdata (table data) and ldata (list data) - Common keyboard handling (Escape, Tab) - Widget lifecycle: after_create signal, unique name management</p> <p>Initialize base control attributes.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent wxPython window.</p> required <code>kwds</code> <p>Keyword argument dict parsed from HTML tag attributes.</p> required Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def __init__(self, parent, kwds):\n    \"\"\"Initialize base control attributes.\n\n    Args:\n        parent: Parent wxPython window.\n        kwds: Keyword argument dict parsed from HTML tag attributes.\n    \"\"\"\n    self.unique_name = None\n    self.init_base(kwds, parent)\n    self.accept_focus = True\n    self.acc_tab_bind = False\n    form = self.get_parent_form()\n    if form:\n        form.signal_from_child(self, \"__init__\")\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.SchBaseCtrl-functions","title":"Methods:","text":""},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.SchBaseCtrl.CanAcceptFocus","title":"CanAcceptFocus","text":"<pre><code>CanAcceptFocus()\n</code></pre> <p>Return True if the control can accept keyboard focus.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def CanAcceptFocus(self):\n    \"\"\"Return True if the control can accept keyboard focus.\"\"\"\n    if self.IsShown() and self.IsEnabled():\n        return self.accept_focus\n    return False\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.SchBaseCtrl.CanAcceptFocusFromKeyboard","title":"CanAcceptFocusFromKeyboard","text":"<pre><code>CanAcceptFocusFromKeyboard()\n</code></pre> <p>Return True if the control accepts focus from keyboard navigation.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def CanAcceptFocusFromKeyboard(self):\n    \"\"\"Return True if the control accepts focus from keyboard navigation.\"\"\"\n    return self.CanAcceptFocus()\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.SchBaseCtrl.after_create","title":"after_create","text":"<pre><code>after_create()\n</code></pre> <p>Called immediately after the widget is fully constructed.</p> <p>Binds keyboard events, executes the 'onload' script (if any), calls the ext_init hook if present, and signals the parent form that this child is ready.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def after_create(self):\n    \"\"\"Called immediately after the widget is fully constructed.\n\n    Binds keyboard events, executes the 'onload' script (if any),\n    calls the __ext_init__ hook if present, and signals the parent\n    form that this child is ready.\n    \"\"\"\n    self.Bind(wx.EVT_KEY_DOWN, self.on_key_down_base)\n    if self.onload:\n        d = {\"wx\": wx, \"self\": self}\n        try:\n            exec(self.onload, d)\n        except Exception:\n            logger.exception(\"Error executing onload script for %s\", self.GetName())\n\n    if hasattr(self, \"__ext_init__\"):\n        self.__ext_init__()\n\n    self.get_parent_form().signal_from_child(self, \"after_create\")\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.SchBaseCtrl.get_ldata","title":"get_ldata","text":"<pre><code>get_ldata()\n</code></pre> <p>Return hierarchical list data, loading from server if necessary.</p> <p>Returns:</p> Type Description <p>List data (nested tree structure), or None if unavailable.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_ldata(self):\n    \"\"\"Return hierarchical list data, loading from server if necessary.\n\n    Returns:\n        List data (nested tree structure), or None if unavailable.\n    \"\"\"\n    if self.ldata is not None:\n        return self.ldata\n    if self.ldatabuf is not None:\n        return self.ldatabuf\n    if self.src:\n        self.refresh_ldata()\n    return self.ldatabuf\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.SchBaseCtrl.get_parent_form","title":"get_parent_form","text":"<pre><code>get_parent_form()\n</code></pre> <p>Walk up the widget hierarchy to find the parent SchForm.</p> <p>Returns:</p> Type Description <p>The SchForm ancestor, or None if not found.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_parent_form(self):\n    \"\"\"Walk up the widget hierarchy to find the parent SchForm.\n\n    Returns:\n        The SchForm ancestor, or None if not found.\n    \"\"\"\n    parent = self.parent\n    while parent is not None:\n        if type(parent).__name__ == \"SchForm\":\n            return parent\n        parent = parent.GetParent()\n    return None\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.SchBaseCtrl.get_parent_page","title":"get_parent_page","text":"<pre><code>get_parent_page()\n</code></pre> <p>Return the parent page that contains this control's form.</p> <p>Returns:</p> Type Description <p>The parent SchPage, or None if not found.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_parent_page(self):\n    \"\"\"Return the parent page that contains this control's form.\n\n    Returns:\n        The parent SchPage, or None if not found.\n    \"\"\"\n    form = self.get_parent_form()\n    if form:\n        return form.get_parent_page()\n    return None\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.SchBaseCtrl.get_tdata","title":"get_tdata","text":"<pre><code>get_tdata()\n</code></pre> <p>Return table data, loading from server if necessary.</p> <p>Returns:</p> Type Description <p>Table data (list of rows), or None if unavailable.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_tdata(self):\n    \"\"\"Return table data, loading from server if necessary.\n\n    Returns:\n        Table data (list of rows), or None if unavailable.\n    \"\"\"\n    if self.tdata is not None:\n        return self.tdata\n    if self.tdatabuf is not None:\n        return self.tdatabuf\n    if self.src:\n        self.refresh_tdata()\n    return self.tdatabuf\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.SchBaseCtrl.get_unique_name","title":"get_unique_name","text":"<pre><code>get_unique_name()\n</code></pre> <p>Return the unique name identifier.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_unique_name(self):\n    \"\"\"Return the unique name identifier.\"\"\"\n    return self.unique_name\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.SchBaseCtrl.init_base","title":"init_base","text":"<pre><code>init_base(kwds, parent=None)\n</code></pre> <p>Initialize all control attributes from kwds.</p> <p>Extracts well-known attributes (href, label, id, target, value, valuetype, defaultvalue, length, maxlength, readonly, hidden, src, onload, tdata, ldata, data, param, style) from kwds and assigns them as instance attributes.</p> <p>Parameters:</p> Name Type Description Default <code>kwds</code> <p>Keyword argument dict parsed from HTML tag attributes.</p> required <code>parent</code> <p>Optional parent window reference.</p> <code>None</code> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def init_base(self, kwds, parent=None):\n    \"\"\"Initialize all control attributes from kwds.\n\n    Extracts well-known attributes (href, label, id, target, value,\n    valuetype, defaultvalue, length, maxlength, readonly, hidden,\n    src, onload, tdata, ldata, data, param, style) from kwds\n    and assigns them as instance attributes.\n\n    Args:\n        kwds: Keyword argument dict parsed from HTML tag attributes.\n        parent: Optional parent window reference.\n    \"\"\"\n    if parent:\n        self.parent = parent\n\n    self.ldatabuf = None\n\n    self.tag = None\n    if \"param\" in kwds and \"tag\" in kwds[\"param\"]:\n        self.tag = kwds[\"param\"][\"tag\"]\n\n    self.href = _extract_kwarg(kwds, \"href\", None)\n    if self.href is None:\n        self.href = \"\"\n\n    self.label = _extract_kwarg(kwds, \"label\", None)\n    self.nr_id = _extract_kwarg(kwds, \"id\", None)\n    self.target = _extract_kwarg(kwds, \"target\", \"_blank\")\n    self.value = _extract_kwarg(kwds, \"value\", None)\n    self.valuetype = _extract_kwarg(kwds, \"valuetype\", \"data\")\n    self.defaultvalue = _extract_kwarg(kwds, \"defaultvalue\", None)\n    self.length = int(_extract_kwarg(kwds, \"length\", 0))\n    self.maxlength = int(_extract_kwarg(kwds, \"maxlength\", 0))\n    self.readonly = _extract_kwarg(kwds, \"readonly\", False)\n    self.hidden = True if _extract_kwarg(kwds, \"hidden\", False) else False\n    self.src = _extract_kwarg(kwds, \"src\", None)\n    self.onload = _extract_kwarg(kwds, \"onload\", None)\n\n    # tdata / tdatabuf: table data (already parsed) or lazily loaded\n    self.tdata = _extract_kwarg(kwds, \"tdata\", None)\n    self.tdatabuf = None\n    if self.tdata is not None:\n        self.tdatabuf = None\n\n    # ldata: hierarchical list data (already parsed) or lazily loaded\n    self.ldata = _extract_kwarg(kwds, \"ldata\", None)\n\n    # data: URL-encoded string data\n    raw_data = _extract_kwarg(kwds, \"data\", None)\n    self.data = unquote(raw_data) if raw_data is not None else None\n\n    self.param = _extract_kwarg(kwds, \"param\", None)\n    self.style = _extract_kwarg(kwds, \"style\", None)\n\n    # Invoke any registered ctrl_process hooks for this tag type\n    ctrl_process = wx.GetApp().ctrl_process\n    if self.tag in ctrl_process:\n        for fun in ctrl_process[self.tag]:\n            fun(self)\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.SchBaseCtrl.is_ctrl_block","title":"is_ctrl_block","text":"<pre><code>is_ctrl_block()\n</code></pre> <p>Return True if this control is a block-level element.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def is_ctrl_block(self):\n    \"\"\"Return True if this control is a block-level element.\"\"\"\n    return self.GetParent().is_ctrl_block(self)\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.SchBaseCtrl.load_data_from_server","title":"load_data_from_server","text":"<pre><code>load_data_from_server(path)\n</code></pre> <p>Load raw binary data from a server URL.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <p>URL path to fetch.</p> required <p>Returns:</p> Type Description <p>Raw response data (bytes).</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def load_data_from_server(self, path):\n    \"\"\"Load raw binary data from a server URL.\n\n    Args:\n        path: URL path to fetch.\n\n    Returns:\n        Raw response data (bytes).\n    \"\"\"\n    http = wx.GetApp().get_http(self.parent)\n    response = http.get(self, str(path))\n    return response.ptr()\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.SchBaseCtrl.load_string_from_server","title":"load_string_from_server","text":"<pre><code>load_string_from_server(path)\n</code></pre> <p>Load a text response from a server URL.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <p>URL path to fetch.</p> required <p>Returns:</p> Type Description <p>Response body as a string.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def load_string_from_server(self, path):\n    \"\"\"Load a text response from a server URL.\n\n    Args:\n        path: URL path to fetch.\n\n    Returns:\n        Response body as a string.\n    \"\"\"\n    http = wx.GetApp().get_http(self.parent)\n    response = http.get(self, str(path))\n    return response.str()\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.SchBaseCtrl.on_key_down_base","title":"on_key_down_base","text":"<pre><code>on_key_down_base(event)\n</code></pre> <p>Default key-down handler for Escape and Tab navigation.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>wx.KeyEvent.</p> required Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def on_key_down_base(self, event):\n    \"\"\"Default key-down handler for Escape and Tab navigation.\n\n    Args:\n        event: wx.KeyEvent.\n    \"\"\"\n    if event.GetKeyCode() == wx.WXK_ESCAPE:\n        wx.CallAfter(self.GetParent().cancel)\n    elif event.GetKeyCode() == wx.WXK_TAB:\n        if event.ShiftDown():\n            self.GetParent().Navigate(self, True)\n        else:\n            self.GetParent().Navigate(self, False)\n    else:\n        event.Skip()\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.SchBaseCtrl.refresh_ldata","title":"refresh_ldata","text":"<pre><code>refresh_ldata()\n</code></pre> <p>Refresh hierarchical list data from the server.</p> <p>Loads from self.src and parses into self.ldatabuf using TreeParser.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def refresh_ldata(self):\n    \"\"\"Refresh hierarchical list data from the server.\n\n    Loads from self.src and parses into self.ldatabuf using TreeParser.\n    \"\"\"\n    if self.ldata is None and self.src:\n        lista = self.load_data_from_server(self.src)\n        mp = TreeParser()\n        mp.feed(lista)\n        mp.close()\n        self.ldatabuf = mp.tree_parent[0][1]\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.SchBaseCtrl.refresh_tdata","title":"refresh_tdata","text":"<pre><code>refresh_tdata(html_src=None)\n</code></pre> <p>Refresh table data from the server or provided HTML source.</p> <p>Parameters:</p> Name Type Description Default <code>html_src</code> <p>Optional HTML string containing table data. If None, loads from self.src.</p> <code>None</code> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def refresh_tdata(self, html_src=None):\n    \"\"\"Refresh table data from the server or provided HTML source.\n\n    Args:\n        html_src: Optional HTML string containing table data.\n            If None, loads from self.src.\n    \"\"\"\n    if self.tdata is None and self.src:\n        if html_src:\n            tables = html_src\n        else:\n            tables = self.load_data_from_server(self.src).decode(\"utf-8\")\n        self.tdatabuf = tdata_from_html(tables, wx.GetApp().http)\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.SchBaseCtrl.set_acc_key_tab","title":"set_acc_key_tab","text":"<pre><code>set_acc_key_tab(tab)\n</code></pre> <p>Register an accelerator table for this widget.</p> <p>Parameters:</p> Name Type Description Default <code>tab</code> <p>List or tuple of accelerator elements. Each element has structure: (flag, keycode, callback function).</p> required <p>Returns:</p> Type Description <p>Result from the parent's set_acc_key_tab method.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def set_acc_key_tab(self, tab):\n    \"\"\"Register an accelerator table for this widget.\n\n    Args:\n        tab: List or tuple of accelerator elements. Each element has\n            structure: (flag, keycode, callback function).\n\n    Returns:\n        Result from the parent's set_acc_key_tab method.\n    \"\"\"\n    return self.GetParent().set_acc_key_tab(self, tab)\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.SchBaseCtrl.set_unique_name","title":"set_unique_name","text":"<pre><code>set_unique_name(name)\n</code></pre> <p>Assign a unique name identifier to this control.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def set_unique_name(self, name):\n    \"\"\"Assign a unique name identifier to this control.\"\"\"\n    self.unique_name = name\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.Select2Base","title":"Select2Base","text":"<pre><code>Select2Base(parent, **kwds)\n</code></pre> <p>               Bases: <code>ComboCtrl</code>, <code>SchBaseCtrl</code></p> <p>Server-backed autocomplete combo using Select2 protocol.</p> <p>Provides a wx.ComboCtrl that communicates with a Select2-compatible server endpoint for autocomplete search and selection.</p> <p>Supports multiple selection mode and action buttons (F2/Insert keys).</p> <p>Initialize the Select2 control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Keyword arguments forwarded to wx.ComboCtrl and SchBaseCtrl. Expects 'param' containing 'data' with Select2 configuration attrs.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the Select2 control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Keyword arguments forwarded to wx.ComboCtrl and\n            SchBaseCtrl. Expects 'param' containing 'data' with\n            Select2 configuration attrs.\n    \"\"\"\n    self.popup = None\n    self.button1 = None\n    self.button2 = None\n    self._popup_shown = False\n\n    SchBaseCtrl.__init__(self, parent, kwds)\n\n    data = self.param[\"data\"][0][\"attrs\"]\n    self.multiple = \"multiple\" in data\n\n    if \"style\" in kwds:\n        kwds[\"style\"] |= wx.TE_PROCESS_ENTER\n    else:\n        kwds[\"style\"] = wx.TE_PROCESS_ENTER\n\n    if \"item_id\" in self.param and self.param[\"item_id\"] != \"None\":\n        self.item_id = [int(self.param[\"item_id\"])]\n        self.item_str = [self.param[\"item_str\"]]\n    else:\n        self.item_id = [-1]\n        self.item_str = [\"\"]\n\n    kwds[\"size\"] = (438, -1)\n\n    wx.ComboCtrl.__init__(self, parent, **kwds)\n\n    if self.GetTextCtrl():\n        self.GetTextCtrl().SetForegroundColour(wx.Colour(0, 0, 0))\n\n    self.Bind(wx.EVT_CHAR, self.on_char)\n\n    if self.item_str:\n        self.SetValue(self.item_str[0])\n\n    self.Bind(wx.EVT_KEY_DOWN, self.on_key_down_base)\n\n    self.GetParent().get_parent_page().register_signal(self, \"return_new_row\")\n    self.GetParent().get_parent_page().register_signal(self, \"return_updated_row\")\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.Select2Base-functions","title":"Methods:","text":""},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.Select2Base.Dismiss","title":"Dismiss","text":"<pre><code>Dismiss()\n</code></pre> <p>Close the popup and return focus.</p> Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def Dismiss(self):\n    \"\"\"Close the popup and return focus.\"\"\"\n    if self.popup:\n        self.popup.Close()\n        self.popup = None\n    self.SetFocus()\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.Select2Base.DoSetPopupControl","title":"DoSetPopupControl","text":"<pre><code>DoSetPopupControl(popup)\n</code></pre> <p>Placeholder for popup control setup (not used).</p> Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def DoSetPopupControl(self, popup):\n    \"\"\"Placeholder for popup control setup (not used).\"\"\"\n    pass\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.Select2Base.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Return the selected value (item_id or list of item_ids).</p> <p>Returns:</p> Type Description <p>int or list of int item IDs.</p> Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def GetValue(self):\n    \"\"\"Return the selected value (item_id or list of item_ids).\n\n    Returns:\n        int or list of int item IDs.\n    \"\"\"\n    if self.multiple:\n        return self.item_id\n    return self.item_id[0]\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.Select2Base.OnButtonClick","title":"OnButtonClick","text":"<pre><code>OnButtonClick()\n</code></pre> <p>Handle the dropdown button click: open popup and clear search.</p> Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def OnButtonClick(self):\n    \"\"\"Handle the dropdown button click: open popup and clear search.\"\"\"\n    ret = self._on_button_click()\n    self.popup.edit_ctrl.SetValue(\"\")\n    wx.CallAfter(self.popup.edit_ctrl.SetFocus)\n    return ret\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.Select2Base.init","title":"init","text":"<pre><code>init(button1, button2)\n</code></pre> <p>Register optional action buttons (F2 and Insert shortcuts).</p> <p>Parameters:</p> Name Type Description Default <code>button1</code> <p>Button triggered by F2.</p> required <code>button2</code> <p>Button triggered by Insert.</p> required Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def init(self, button1, button2):\n    \"\"\"Register optional action buttons (F2 and Insert shortcuts).\n\n    Args:\n        button1: Button triggered by F2.\n        button2: Button triggered by Insert.\n    \"\"\"\n    self.button1 = button1\n    self.button2 = button2\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.Select2Base.on_char","title":"on_char","text":"<pre><code>on_char(event)\n</code></pre> <p>Handle character input: forward to the popup search field.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>wx.KeyEvent.</p> required Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def on_char(self, event):\n    \"\"\"Handle character input: forward to the popup search field.\n\n    Args:\n        event: wx.KeyEvent.\n    \"\"\"\n    try:\n        c = chr(event.GetUnicodeKey())\n        if c in string.printable:\n            if not self._popup_shown:\n                self._on_button_click()\n            self.popup.edit_ctrl.AppendText(c)\n    except (ValueError, OverflowError):\n        pass\n    if event.KeyCode in (wx.WXK_DELETE, wx.WXK_BACK):\n        self.item_id = [-1]\n        self.item_str = [\"\"]\n        self.Clear()\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.Select2Base.on_key_down_base","title":"on_key_down_base","text":"<pre><code>on_key_down_base(event)\n</code></pre> <p>Handle global keyboard shortcuts.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>wx.KeyEvent.</p> required Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def on_key_down_base(self, event):\n    \"\"\"Handle global keyboard shortcuts.\n\n    Args:\n        event: wx.KeyEvent.\n    \"\"\"\n    if event.GetKeyCode() == wx.WXK_TAB:\n        if event.ShiftDown():\n            self.GetParent().GetParent().Navigate(self.GetParent(), True)\n        else:\n            self.GetParent().GetParent().Navigate(self.GetParent(), False)\n    elif event.GetKeyCode() == wx.WXK_F2:\n        self.button1.on_click(event)\n    elif event.GetKeyCode() == wx.WXK_INSERT:\n        self.button2.on_click(event)\n    else:\n        event.Skip()\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.Select2Base.on_popup_hidden","title":"on_popup_hidden","text":"<pre><code>on_popup_hidden()\n</code></pre> <p>Called when the popup is hidden.</p> Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def on_popup_hidden(self):\n    \"\"\"Called when the popup is hidden.\"\"\"\n    self._popup_shown = False\n    self.SetFocus()\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.Select2Base.return_new_row","title":"return_new_row","text":"<pre><code>return_new_row(**argv)\n</code></pre> <p>Handle return_new_row signal by updating the value.</p> Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def return_new_row(self, **argv):\n    \"\"\"Handle return_new_row signal by updating the value.\"\"\"\n    self.set_value(argv[\"id\"], argv[\"title\"])\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.Select2Base.return_updated_row","title":"return_updated_row","text":"<pre><code>return_updated_row(**argv)\n</code></pre> <p>Handle return_updated_row signal by updating the value.</p> Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def return_updated_row(self, **argv):\n    \"\"\"Handle return_updated_row signal by updating the value.\"\"\"\n    self.set_value(argv[\"id\"], argv[\"title\"])\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.Select2Base.set_value","title":"set_value","text":"<pre><code>set_value(item_id, item_str)\n</code></pre> <p>Set the selected value.</p> <p>Parameters:</p> Name Type Description Default <code>item_id</code> <p>Table row ID (database key).</p> required <code>item_str</code> <p>String representation of the row.</p> required Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def set_value(self, item_id, item_str):\n    \"\"\"Set the selected value.\n\n    Args:\n        item_id: Table row ID (database key).\n        item_str: String representation of the row.\n    \"\"\"\n    if self.multiple:\n        if item_id not in self.item_id:\n            self.item_id.append(item_id)\n            self.item_str.append(item_str)\n        self.SetValue(\"; \".join(self.item_str)[2:])\n    else:\n        self.item_id = [item_id]\n        self.item_str = [item_str]\n        self.SetValue(item_str)\n\n    def _finish():\n        self.SelectNone()\n        self.SetInsertionPointEnd()\n\n    wx.CallAfter(_finish)\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory-functions","title":"Functions:","text":""},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.BUTTON","title":"BUTTON","text":"<pre><code>BUTTON(parent, **kwds)\n</code></pre> <p>Dispatch function for ctrlbutton tag.</p> <p>Routes to the appropriate button class based on the 'btn-class' parameter or the presence of a 'src' attribute (bitmap vs text button).</p> Supported btn-class values <p>SIMPLE_BUTTON, BITMAPBUTTON, PLATEBUTTON, GENBITMAPBUTTON, GENBITMAPBUTTONTXT, GENBITMAPBUTTONTXT_SMALL, NOBG_BUTTON, NOBG_BUTTON_TXT, CLOSEBUTTON, MENUBUTTON, MENUTOOLBARBUTTON</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Tag keyword arguments including optional 'param' with 'btn-class' key, and optional 'src' for bitmap.</p> <code>{}</code> <p>Returns:</p> Type Description <p>A button control instance.</p> Source code in <code>pytigon_gui/guictrl/factory.py</code> <pre><code>def BUTTON(parent, **kwds):\n    \"\"\"Dispatch function for ctrlbutton tag.\n\n    Routes to the appropriate button class based on the\n    'btn-class' parameter or the presence of a 'src' attribute\n    (bitmap vs text button).\n\n    Supported btn-class values:\n        SIMPLE_BUTTON, BITMAPBUTTON, PLATEBUTTON,\n        GENBITMAPBUTTON, GENBITMAPBUTTONTXT,\n        GENBITMAPBUTTONTXT_SMALL, NOBG_BUTTON,\n        NOBG_BUTTON_TXT, CLOSEBUTTON, MENUBUTTON,\n        MENUTOOLBARBUTTON\n\n    Args:\n        parent: Parent window.\n        **kwds: Tag keyword arguments including optional 'param'\n            with 'btn-class' key, and optional 'src' for bitmap.\n\n    Returns:\n        A button control instance.\n    \"\"\"\n    if \"param\" in kwds and \"btn-class\" in kwds[\"param\"]:\n        btn_class = kwds[\"param\"][\"btn-class\"]\n        class_map = {\n            \"SIMPLE_BUTTON\": SIMPLE_BUTTON,\n            \"BITMAPBUTTON\": BITMAPBUTTON,\n            \"PLATEBUTTON\": PLATEBUTTON,\n            \"GENBITMAPBUTTON\": GENBITMAPBUTTON,\n            \"GENBITMAPBUTTONTXT\": GENBITMAPBUTTONTXT,\n            \"GENBITMAPBUTTONTXT_SMALL\": GENBITMAPBUTTONTXT_SMALL,\n            \"NOBG_BUTTON\": NOBG_BUTTON,\n            \"NOBG_BUTTON_TXT\": NOBG_BUTTON_TXT,\n            \"CLOSEBUTTON\": CLOSEBUTTON,\n            \"MENUBUTTON\": MENUBUTTON,\n            \"MENUTOOLBARBUTTON\": MENUTOOLBARBUTTON,\n        }\n        if btn_class in class_map:\n            return class_map[btn_class](parent, **kwds)\n\n    if \"src\" in kwds:\n        return BITMAPBUTTON(parent, **kwds)\n    else:\n        return SIMPLE_BUTTON(parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.COMPONENT","title":"COMPONENT","text":"<pre><code>COMPONENT(parent, **kwds)\n</code></pre> <p>Create a web component widget.</p> <p>Loads a web component via HTTP, replaces placeholder tokens, and renders it using the HTML2 viewer if available.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Tag keyword arguments with 'param' containing 'data' (component name).</p> <code>{}</code> <p>Returns:</p> Type Description <p>An HTML2 viewer instance or None if HTML2 is not available.</p> Source code in <code>pytigon_gui/guictrl/factory.py</code> <pre><code>def COMPONENT(parent, **kwds):\n    \"\"\"Create a web component widget.\n\n    Loads a web component via HTTP, replaces placeholder tokens,\n    and renders it using the HTML2 viewer if available.\n\n    Args:\n        parent: Parent window.\n        **kwds: Tag keyword arguments with 'param' containing\n            'data' (component name).\n\n    Returns:\n        An HTML2 viewer instance or None if HTML2 is not available.\n    \"\"\"\n    # HTML2 is dynamically set by plugins at runtime\n    from pytigon_gui.guictrl import ctrl as schctrl\n\n    http = wx.GetApp().get_http(parent)\n    response = http.get(\n        parent,\n        wx.GetApp().make_href(\"/schsys/widget_web?browser_type=1\"),\n    )\n    buf = response.str()\n\n    colour = wx.SystemSettings.GetColour(wx.SYS_COLOUR_3DFACE).GetAsString(\n        flags=wx.C2S_HTML_SYNTAX\n    )\n    buf = buf.replace(\"&lt;component&gt;\", kwds[\"param\"][\"data\"]).replace(\"[[color]]\", colour)\n\n    if schctrl.HTML2:\n        kwds[\"component\"] = True\n        obj = schctrl.HTML2(parent, **kwds)\n        obj.load_str(buf, \"http://127.0.0.2\")\n        return obj\n    else:\n        return None\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.COMPOSITE","title":"COMPOSITE","text":"<pre><code>COMPOSITE(parent, **kwds)\n</code></pre> <p>Dispatch function for ctrlcomposite tag.</p> <p>Looks up the widget class by the 'class' parameter (uppercased) in the global namespace and instantiates it.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Tag keyword arguments with 'param' containing 'class' key.</p> <code>{}</code> <p>Returns:</p> Type Description <p>An instance of the requested widget class.</p> Source code in <code>pytigon_gui/guictrl/factory.py</code> <pre><code>def COMPOSITE(parent, **kwds):\n    \"\"\"Dispatch function for ctrlcomposite tag.\n\n    Looks up the widget class by the 'class' parameter (uppercased)\n    in the global namespace and instantiates it.\n\n    Args:\n        parent: Parent window.\n        **kwds: Tag keyword arguments with 'param' containing\n            'class' key.\n\n    Returns:\n        An instance of the requested widget class.\n    \"\"\"\n    cls_name = kwds[\"param\"][\"class\"].upper()\n    if cls_name in globals():\n        return globals()[cls_name](parent, **kwds)\n    return None\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.SELECT","title":"SELECT","text":"<pre><code>SELECT(parent, **kwds)\n</code></pre> <p>Dispatch function for ctrlselect tag.</p> <p>Routes to the appropriate widget based on parameters: - HeavySelect2Field / ModelSelect2Field types: SELECT2 - Single selection (size=1): DBCHOICE (readonly combo) - Multi selection (size&gt;1 or 'multiple' param): CHECKLISTBOX</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Tag keyword arguments including 'param' dict.</p> <code>{}</code> <p>Returns:</p> Type Description <p>A wx control instance appropriate for the selection mode.</p> Source code in <code>pytigon_gui/guictrl/factory.py</code> <pre><code>def SELECT(parent, **kwds):\n    \"\"\"Dispatch function for ctrlselect tag.\n\n    Routes to the appropriate widget based on parameters:\n    - HeavySelect2Field / ModelSelect2Field types: SELECT2\n    - Single selection (size=1): DBCHOICE (readonly combo)\n    - Multi selection (size&gt;1 or 'multiple' param): CHECKLISTBOX\n\n    Args:\n        parent: Parent window.\n        **kwds: Tag keyword arguments including 'param' dict.\n\n    Returns:\n        A wx control instance appropriate for the selection mode.\n    \"\"\"\n    size = 1\n    multiple = False\n\n    if \"param\" in kwds:\n        param = kwds[\"param\"]\n        if \"multiple\" in param:\n            multiple = True\n        if \"size\" in param:\n            size = int(param[\"size\"])\n        if multiple and size == 1:\n            size = 10\n\n        if \"schtype\" in param and param[\"schtype\"] in (\n            \"HeavySelect2Field\",\n            \"HeavySelect2MultipleField\",\n            \"ModelSelect2Field\",\n            \"ModelSelect2MultipleField\",\n        ):\n            kwds[\"param\"][\"data\"] = [kwds[\"param\"]]\n            kwds[\"param\"][\"data\"][0][\"attrs\"] = kwds[\"param\"]\n            return SELECT2(parent, **kwds)\n\n    if size == 1:\n        kwds[\"readonly\"] = True\n        return DBCHOICE(parent, **kwds)\n    else:\n        kwds[\"length\"] = size\n        return CHECKLISTBOX(parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.SELECT2","title":"SELECT2","text":"<pre><code>SELECT2(parent, **kwds)\n</code></pre> <p>Create a Select2 composite control with optional action buttons.</p> <p>Builds a CompositePanel containing a Select2Base control and up to two action buttons from the composite parameter data.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Tag keyword arguments with 'param' containing 'data' (list of parameter nodes).</p> <code>{}</code> <p>Returns:</p> Type Description <p>A CompositePanel containing the Select2 control and buttons.</p> Source code in <code>pytigon_gui/guictrl/factory.py</code> <pre><code>def SELECT2(parent, **kwds):\n    \"\"\"Create a Select2 composite control with optional action buttons.\n\n    Builds a CompositePanel containing a Select2Base control\n    and up to two action buttons from the composite parameter data.\n\n    Args:\n        parent: Parent window.\n        **kwds: Tag keyword arguments with 'param' containing\n            'data' (list of parameter nodes).\n\n    Returns:\n        A CompositePanel containing the Select2 control and buttons.\n    \"\"\"\n    data = kwds[\"param\"][\"data\"]\n    panel = CompositePanel(parent, size=(460, -1))\n    ctrl = Select2Base(panel, **kwds)\n    panel.set_main_ctrl(ctrl)\n\n    # Build optional action buttons from composite data\n    button1 = None\n    button2 = None\n    if (\n        len(data) &gt; 1\n        and \"children\" in data[1]\n        and data[1][\"children\"]\n        and len(data[1][\"children\"]) &gt; 0\n    ):\n        button1 = _button_from_parm(panel, param=data[1][\"children\"][0])\n    if (\n        len(data) &gt; 1\n        and \"children\" in data[1]\n        and data[1][\"children\"]\n        and len(data[1][\"children\"]) &gt; 1\n    ):\n        button2 = _button_from_parm(panel, param=data[1][\"children\"][1])\n\n    ctrl.init(button1, button2)\n\n    # Shared callback for select2 and buttons\n    def ret_ok(id, title):\n        ctrl.set_value(id, title)\n        wx.CallAfter(ctrl.SetFocus)\n\n    ctrl.ret_ok = ret_ok\n    if button1:\n        button1.ret_ok = ret_ok\n    if button2:\n        button2.ret_ok = ret_ok\n\n    # Layout: horizontal box with ctrl + buttons\n    box = wx.BoxSizer(wx.HORIZONTAL)\n    box.Add(ctrl, 0, wx.EXPAND)\n    if button1:\n        box.Add(button1)\n    if button2:\n        box.Add(button2)\n\n    panel.SetSizer(box)\n    panel.SetAutoLayout(True)\n    panel.Fit()\n\n    return panel\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.TEXTAREA","title":"TEXTAREA","text":"<pre><code>TEXTAREA(parent, **kwds)\n</code></pre> <p>Dispatch function for ctrltextarea tag.</p> <p>Handles sizing from 'cols' and 'rows' attributes and routes to STYLEDTEXT or AUTOCOMPLETE based on the 'src' attribute (code languages get STYLEDTEXT, others get AUTOCOMPLETE for code completion).</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Tag keyword arguments with optional 'param', 'cols', 'rows', 'src'.</p> <code>{}</code> <p>Returns:</p> Type Description <p>A STYLEDTEXT or AUTOCOMPLETE instance.</p> Source code in <code>pytigon_gui/guictrl/factory.py</code> <pre><code>def TEXTAREA(parent, **kwds):\n    \"\"\"Dispatch function for ctrltextarea tag.\n\n    Handles sizing from 'cols' and 'rows' attributes and\n    routes to STYLEDTEXT or AUTOCOMPLETE based on the 'src'\n    attribute (code languages get STYLEDTEXT, others get\n    AUTOCOMPLETE for code completion).\n\n    Args:\n        parent: Parent window.\n        **kwds: Tag keyword arguments with optional 'param',\n            'cols', 'rows', 'src'.\n\n    Returns:\n        A STYLEDTEXT or AUTOCOMPLETE instance.\n    \"\"\"\n    if \"param\" in kwds:\n        if \"data\" in kwds[\"param\"]:\n            data = kwds[\"param\"][\"data\"].replace(\"\\r\", \"\")\n            if data.startswith(\"\\n\"):\n                data = data[1:]\n                kwds[\"param\"][\"data\"] = data\n\n    size = [-1, -1]\n\n    if \"param\" in kwds and \"width\" not in kwds[\"param\"]:\n        if \"cols\" in kwds[\"param\"]:\n            width = 7 * int(is_null(kwds[\"param\"][\"cols\"], \"80\"))\n            if width &gt; 480:\n                width = 480\n            kwds[\"param\"][\"width\"] = str(width)\n            size[0] = width\n        else:\n            kwds[\"param\"][\"width\"] = \"100%\"\n    if \"param\" in kwds and \"height\" not in kwds[\"param\"]:\n        if \"rows\" in kwds[\"param\"]:\n            size[1] = 14 + 14 * int(is_null(kwds[\"param\"][\"rows\"], \"3\"))\n            kwds[\"param\"][\"height\"] = str(size[1])\n        else:\n            kwds[\"param\"][\"height\"] = \"100%\"\n\n    kwds[\"size\"] = size\n\n    kwds.pop(\"cols\", None)\n    kwds.pop(\"rows\", None)\n\n    if \"src\" in kwds:\n        if kwds[\"src\"] in (\"c\", \"python\", \"html\"):\n            return STYLEDTEXT(parent, **kwds)\n        else:\n            return AUTOCOMPLETE(parent, **kwds)\n    else:\n        return STYLEDTEXT(parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory._button_from_parm","title":"_button_from_parm","text":"<pre><code>_button_from_parm(parent, param)\n</code></pre> <p>Create a button from a composite parameter node.</p> <p>Used internally by SELECT2 to create action buttons alongside the Select2 control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>param</code> <p>Parameter dict with 'children' and 'attrs'.</p> required <p>Returns:</p> Type Description <p>A BUTTON instance or None.</p> Source code in <code>pytigon_gui/guictrl/factory.py</code> <pre><code>def _button_from_parm(parent, param):\n    \"\"\"Create a button from a composite parameter node.\n\n    Used internally by SELECT2 to create action buttons\n    alongside the Select2 control.\n\n    Args:\n        parent: Parent window.\n        param: Parameter dict with 'children' and 'attrs'.\n\n    Returns:\n        A BUTTON instance or None.\n    \"\"\"\n    if \"children\" in param and param[\"children\"]:\n        icon = param[\"children\"][0][\"attrs\"][\"class\"]\n        href = param[\"attrs\"][\"href\"]\n        button = BUTTON(parent, src=\"fa://\" + icon + \"?size=0\", href=href)\n        return button\n    return None\n</code></pre>"},{"location":"api/guictrl_factory/#pytigon_gui.guictrl.factory.bitmap_from_href","title":"bitmap_from_href","text":"<pre><code>bitmap_from_href(href, size_type=SIZE_DEFAULT)\n</code></pre> <p>Load a resource and convert it to a wx.Bitmap.</p> <p>Parameters:</p> Name Type Description Default <code>href</code> <p>Resource identifier. Supported formats: - ArtProvider id, e.g. 'wx.ART_COPY' - Font Awesome id, e.g. 'fa://icon_name' or 'fa://bus' - Local library image, e.g. 'png://image_name.png' - Client-side image, e.g. 'client://image_name.png' - Inline SVG data, e.g. 'data:image/svg+xml,...' - Remote HTTP/HTTPS URL, e.g. 'http://www.pytigon.cloud/test.png'</p> required <code>size_type</code> <p>One of SIZE_DEFAULT, SIZE_SMALL, SIZE_MEDIUM, SIZE_LARGE.</p> <code>SIZE_DEFAULT</code> <p>Returns:</p> Type Description <p>wx.Bitmap. If the resource cannot be loaded, ART_MISSING_IMAGE is</p> <p>returned.</p> Source code in <code>pytigon_gui/guilib/image.py</code> <pre><code>def bitmap_from_href(href, size_type=SIZE_DEFAULT):\n    \"\"\"Load a resource and convert it to a wx.Bitmap.\n\n    Args:\n        href: Resource identifier. Supported formats:\n            - ArtProvider id, e.g. 'wx.ART_COPY'\n            - Font Awesome id, e.g. 'fa://icon_name' or 'fa://bus'\n            - Local library image, e.g. 'png://image_name.png'\n            - Client-side image, e.g. 'client://image_name.png'\n            - Inline SVG data, e.g. 'data:image/svg+xml,...'\n            - Remote HTTP/HTTPS URL, e.g. 'http://www.pytigon.cloud/test.png'\n        size_type: One of SIZE_DEFAULT, SIZE_SMALL, SIZE_MEDIUM, SIZE_LARGE.\n\n    Returns:\n        wx.Bitmap. If the resource cannot be loaded, ART_MISSING_IMAGE is\n        returned.\n    \"\"\"\n    size_type2 = size_type\n    if \"?\" in href:\n        x = href.split(\"?\")\n        href2 = x[0]\n        if \"=\" in x[1]:\n            try:\n                size_type2 = int(x[1].split(\"=\")[1])\n            except (ValueError, IndexError):\n                size_type2 = 1\n    else:\n        href2 = href\n\n    if size_type2 == SIZE_SMALL:\n        icon_size = 16\n    elif size_type2 == SIZE_MEDIUM:\n        icon_size = 22\n    else:\n        icon_size = 32\n\n    if href2[:3] == \"wx.\":\n        if (art_id := _resolve_art_id(href2)) is not None:\n            bmp = wx.ArtProvider.GetBitmap(\n                art_id, wx.ART_TOOLBAR, (icon_size, icon_size)\n            )\n        else:\n            bmp = wx.ArtProvider.GetBitmap(\n                wx.ART_MISSING_IMAGE, wx.ART_TOOLBAR, (icon_size, icon_size)\n            )\n    elif href.startswith(\"client://\"):\n        image = wx.Image(\n            str(wx.GetApp().src_path)\n            + f\"/static/icons/{icon_size}x{icon_size}/\"\n            + href2[9:]\n        )\n        bmp = wx.Bitmap(image)\n    elif href.startswith(\"png://\"):\n        image = wx.Image(\n            str(wx.GetApp().src_path)\n            + f\"/static/icons/{icon_size}x{icon_size}/\"\n            + href2[6:]\n        )\n        bmp = wx.Bitmap(image)\n    elif href.startswith(\"fa://\"):\n        suffix = \"\" if \".png\" in href.lower() else \".png\"\n        try:\n            image = wx.Image(\n                str(wx.GetApp().src_path)\n                + f\"/static/fonts/fork-awesome/fonts/{icon_size}x{icon_size}/\"\n                + href2[5:].replace(\"fa-\", \"\")\n                + suffix\n            )\n            image = image.AdjustChannels(1, 1, 1, 0.55)\n            bmp = wx.Bitmap(image)\n        except Exception:\n            logger.warning(\"Cannot load image: %s\", href2)\n            bmp = wx.Bitmap()\n    elif href.startswith(\"data:image/svg+xml\"):\n        x = href.split(\",\", 1)\n        svg_code = x[1]\n        svg = SVGimage.CreateFromBytes(svg_code.encode(\"utf-8\"))\n        bmp = svg.ConvertToBitmap(width=icon_size, height=icon_size)\n    else:\n        if href.lower().startswith(\"http\"):\n            http = wx.GetApp().get_http(None)\n            response = http.get(None, str(href))\n            if response.ret_code != 404:\n                s = response.ptr()\n                stream = BytesIO(s)\n                bmp = wx.Bitmap(wx.Image(stream))\n            else:\n                bmp = wx.ArtProvider.GetBitmap(\n                    wx.ART_MISSING_IMAGE, wx.ART_TOOLBAR, (32, 32)\n                )\n        else:\n            bmp = wx.ArtProvider.GetBitmap(\n                wx.ART_MISSING_IMAGE, wx.ART_TOOLBAR, (32, 32)\n            )\n    return bmp\n</code></pre>"},{"location":"api/guictrl_grid/","title":"guictrl.grid \u2013 Grid Subpackage","text":"<p>Data grid components: grid widget, panel wrapper, table base classes (from HTML tables and data proxies), cell editors, and renderers.</p>"},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid","title":"grid","text":"<p>Grid and table widgets for the SchForm GUI framework.</p> <p>Provides table grid, data grid, grid panels, table data sources, popup cell editors, renderers, and tab proxy for server data.</p>"},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid-modules","title":"Modules","text":""},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.grid","title":"grid","text":""},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.grid-classes","title":"Classes","text":""},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.grid-modules","title":"Modules","text":""},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.gridpanel","title":"gridpanel","text":"<p>Grid panel with toolbar for the SchForm framework.</p> <p>Provides SchGridPanel: a wx.Panel wrapper that hosts a SchTableGrid alongside a configurable toolbar with action buttons (edit, delete, view row) and dynamic per-row action buttons.</p>"},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.gridpanel-classes","title":"Classes","text":""},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.gridpanel.SchGridPanel","title":"SchGridPanel","text":"<pre><code>SchGridPanel(*args, **argv)\n</code></pre> <p>               Bases: <code>Panel</code></p> <p>Panel for grid widget with toolbar</p> Source code in <code>pytigon_gui/guictrl/grid/gridpanel.py</code> <pre><code>def __init__(self, *args, **argv):\n    self.grid = None\n    self.icon_size = 2\n    wx.Panel.__init__(self, *args, **argv)\n    self.vertical = False\n    self._bitmaps = {\n        \"edit\": \"wx.ART_FILE_OPEN\",\n        \"edit_inline\": \"wx.ART_FILE_OPEN\",\n        \"delete\": \"wx.ART_DELETE\",\n        \"view_row\": \"wx.ART_INFORMATION\",\n    }\n    self._menu_buttons = collections.OrderedDict()\n</code></pre>"},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.gridpanel-functions","title":"Functions:","text":""},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.gridtable_base","title":"gridtable_base","text":"<p>Base grid table class for the SchForm framework.</p> <p>Provides SchGridTableBase, the abstract base for all grid table data sources used by SchTableGrid. Implements common functionality: row selection, sorting, filtering, read-only state, dirty tracking, and cell attribute management.</p>"},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.gridtable_from_html_table","title":"gridtable_from_html_table","text":"<p>Grid table data source from server-rendered HTML tables.</p> <p>Provides SimpleDataTable and PageData: a SchGridTableBase implementation that loads paginated table data from the server as HTML and parses it into grid rows. Supports sorting, column sizing, cell-level attributes, and copy/paste.</p>"},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.gridtable_from_html_table-functions","title":"Functions:","text":""},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.gridtable_from_proxy","title":"gridtable_from_proxy","text":"<p>Grid table data source from a server data proxy.</p> <p>Provides DataSource: a SchGridTableBase implementation that fetches paginated data via a DataProxy (tabproxy.py) from a Django backend. Supports lazy page loading, dirty tracking, sorting, filtering, and commit/sync of CRUD operations.</p>"},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.gridtable_from_proxy-classes","title":"Classes","text":""},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.gridtable_from_proxy.DataSource","title":"DataSource","text":"<pre><code>DataSource(proxy)\n</code></pre> <p>               Bases: <code>SchGridTableBase</code></p> Source code in <code>pytigon_gui/guictrl/grid/gridtable_from_proxy.py</code> <pre><code>def __init__(self, proxy):\n    SchGridTableBase.__init__(self)\n    self.proxy = proxy\n    self.proxy.set_parent(self)\n    self.last_page_id = -1\n    self.pages_nr = [-1, -1, -1, -1, -1]\n    self.pages = [None, None, None, None, None]\n    self.last_page = None\n    self.max_count = self.proxy.get_max_count()\n    self.rec_count = self.max_count\n\n    self.default_rec = self.proxy.get_default_rec()\n\n    self.attrs = {}\n    self.setparm = False\n\n    self.can_append = 1\n    self.read_only = False\n    self.is_valid = True\n\n    self.last_row_count = self.max_count + self.can_append\n</code></pre> Methods:\u00b6 <code></code> auto_update \u00b6 <pre><code>auto_update(col_name, col_names, rec)\n</code></pre> <p>Return transformed row after current row is changed</p> Source code in <code>pytigon_gui/guictrl/grid/gridtable_from_proxy.py</code> <pre><code>def auto_update(self, col_name, col_names, rec):\n    \"\"\"Return transformed row after current row is changed\"\"\"\n    return self.proxy.auto_update(col_name, col_names, rec)\n</code></pre>"},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.popupcelleditors","title":"popupcelleditors","text":"<p>Popup cell editor classes for grid cells.</p> <p>Provides wx.Grid cell editors that use server-backed DataPopupControl for data selection within grid cells: generic popup, date picker, list picker, and a base popup data editor.</p>"},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.popupcelleditors-classes","title":"Classes","text":""},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.popupcelleditors.DatePopupDataCellEditor","title":"DatePopupDataCellEditor","text":"<pre><code>DatePopupDataCellEditor()\n</code></pre> <p>               Bases: <code>PopupDataCellEditor</code></p> <p>Date picker cell editor with masked input format.</p> <p>Uses the server's datedialog endpoint with a masked text control for YYYY-MM-DD date entry.</p> <p>Initialize with the datedialog server endpoint.</p> Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def __init__(self):\n    \"\"\"Initialize with the datedialog server endpoint.\"\"\"\n    PopupDataCellEditor.__init__(self)\n    self.address = wx.GetApp().make_href(\"/schsys/datedialog/\")\n</code></pre> Methods:\u00b6 <code></code> Clone \u00b6 <pre><code>Clone()\n</code></pre> <p>Return a copy of this date editor.</p> <p>Returns:</p> Type Description <p>A new DatePopupDataCellEditor instance.</p> Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def Clone(self):\n    \"\"\"Return a copy of this date editor.\n\n    Returns:\n        A new DatePopupDataCellEditor instance.\n    \"\"\"\n    ret = DatePopupDataCellEditor()\n    ret.address = self.address\n    ret.SetParameters(self.param)\n    return ret\n</code></pre> <code></code> Create \u00b6 <pre><code>Create(parent, id, evt_handler)\n</code></pre> <p>Create the editor and set masked date format.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>id</code> <p>Window ID.</p> required <code>evt_handler</code> <p>Event handler.</p> required Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def Create(self, parent, id, evt_handler):\n    \"\"\"Create the editor and set masked date format.\n\n    Args:\n        parent: Parent window.\n        id: Window ID.\n        evt_handler: Event handler.\n    \"\"\"\n    PopupDataCellEditor.Create(self, parent, id, evt_handler)\n    self._tc.to_masked(autoformat=\"EUDATEYYYYMMDD.\")\n</code></pre>"},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.popupcelleditors.GenericPopupCellEditor","title":"GenericPopupCellEditor","text":"<pre><code>GenericPopupCellEditor()\n</code></pre> <p>               Bases: <code>PopupDataCellEditor</code></p> <p>Generic popup cell editor with configurable server endpoint.</p> <p>The address is set via set_parameters, allowing reuse across different data types.</p> <p>Initialize with a default datedialog endpoint.</p> Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def __init__(self):\n    \"\"\"Initialize with a default datedialog endpoint.\"\"\"\n    PopupDataCellEditor.__init__(self)\n    self.address = wx.GetApp().make_href(\"/schsys/datedialog/\")\n</code></pre> Methods:\u00b6 <code></code> Clone \u00b6 <pre><code>Clone()\n</code></pre> <p>Return a copy of this editor.</p> <p>Returns:</p> Type Description <p>A new GenericPopupCellEditor instance.</p> Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def Clone(self):\n    \"\"\"Return a copy of this editor.\n\n    Returns:\n        A new GenericPopupCellEditor instance.\n    \"\"\"\n    ret = GenericPopupCellEditor()\n    ret.SetParameters(self.address)\n    return ret\n</code></pre> <code></code> set_parameters \u00b6 <pre><code>set_parameters(params)\n</code></pre> <p>Set the server address from parameters.</p> <p>Parameters:</p> Name Type Description Default <code>params</code> <p>URL string for the data endpoint.</p> required Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def set_parameters(self, params):\n    \"\"\"Set the server address from parameters.\n\n    Args:\n        params: URL string for the data endpoint.\n    \"\"\"\n    self.address = params\n</code></pre>"},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.popupcelleditors.ListPopupCellEditor","title":"ListPopupCellEditor","text":"<pre><code>ListPopupCellEditor()\n</code></pre> <p>               Bases: <code>PopupDataCellEditor</code></p> <p>List picker cell editor that loads choices from the cell type.</p> <p>Parses a JSON-encoded choice list from the cell's type name and presents it in the popup dialog for selection.</p> <p>Initialize with the listdialog server endpoint.</p> Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def __init__(self):\n    \"\"\"Initialize with the listdialog server endpoint.\"\"\"\n    PopupDataCellEditor.__init__(self)\n    self.address = wx.GetApp().make_href(\"/schsys/listdialog/\")\n</code></pre> Methods:\u00b6 <code></code> BeginEdit \u00b6 <pre><code>BeginEdit(row, col, grid)\n</code></pre> <p>Begin editing: parse choice list from cell type name.</p> <p>Parameters:</p> Name Type Description Default <code>row</code> <p>Grid row index.</p> required <code>col</code> <p>Grid column index.</p> required <code>grid</code> <p>The parent wx.Grid.</p> required Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def BeginEdit(self, row, col, grid):\n    \"\"\"Begin editing: parse choice list from cell type name.\n\n    Args:\n        row: Grid row index.\n        col: Grid column index.\n        grid: The parent wx.Grid.\n    \"\"\"\n    typ = grid.GetTable().GetTypeName(row, col)\n    sep = typ.find(\":\")\n    self.choices = schjson.loads(typ[sep + 1 :])\n    PopupDataCellEditor.BeginEdit(self, row, col, grid)\n</code></pre> <code></code> Clone \u00b6 <pre><code>Clone()\n</code></pre> <p>Return a copy of this list editor.</p> <p>Returns:</p> Type Description <p>A new ListPopupCellEditor instance.</p> Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def Clone(self):\n    \"\"\"Return a copy of this list editor.\n\n    Returns:\n        A new ListPopupCellEditor instance.\n    \"\"\"\n    ret = ListPopupCellEditor()\n    ret.address = self.address\n    ret.SetParameters(self.param)\n    return ret\n</code></pre> <code></code> Create \u00b6 <pre><code>Create(parent, id, evt_handler)\n</code></pre> <p>Create the editor and set the event object.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>id</code> <p>Window ID.</p> required <code>evt_handler</code> <p>Event handler.</p> required Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def Create(self, parent, id, evt_handler):\n    \"\"\"Create the editor and set the event object.\n\n    Args:\n        parent: Parent window.\n        id: Window ID.\n        evt_handler: Event handler.\n    \"\"\"\n    PopupDataCellEditor.Create(self, parent, id, evt_handler)\n    self._tc.SetEventObject(self)\n</code></pre> <code></code> OnButtonClick \u00b6 <pre><code>OnButtonClick()\n</code></pre> <p>Populate the popup dialog with parsed choices.</p> Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def OnButtonClick(self):\n    \"\"\"Populate the popup dialog with parsed choices.\"\"\"\n    if self._tc.simpleDialog:\n        self._tc.popup.html.choices = self.choices\n        self._tc.popup.html.refr()\n    else:\n        self._tc.page.body.choices = self.choices\n        self._tc.page.body.refr()\n</code></pre> <code></code> set_rec \u00b6 <pre><code>set_rec(value, value_rec, dismiss=True)\n</code></pre> <p>Match typed value against choice list.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>Typed value to match.</p> required <code>value_rec</code> <p>Record object.</p> required <code>dismiss</code> <p>Dismiss popup after setting.</p> <code>True</code> <p>Returns:</p> Type Description <p>Matched 'id:name' string, or empty string.</p> Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def set_rec(self, value, value_rec, dismiss=True):\n    \"\"\"Match typed value against choice list.\n\n    Args:\n        value: Typed value to match.\n        value_rec: Record object.\n        dismiss: Dismiss popup after setting.\n\n    Returns:\n        Matched 'id:name' string, or empty string.\n    \"\"\"\n    if len(value) &gt; 2 and value[1] == \":\":\n        return value\n    for choice in self.choices:\n        if choice[1].lower().startswith(value.lower()):\n            return choice[0] + \":\" + choice[1]\n    return \"\"\n</code></pre>"},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.popupcelleditors.PopupDataCellControl","title":"PopupDataCellControl","text":"<pre><code>PopupDataCellControl(href, *args, **kwds)\n</code></pre> <p>               Bases: <code>DataPopupControl</code></p> <p>Thin wrapper around DataPopupControl for use as a grid cell control.</p> <p>Initialize with a server URL.</p> <p>Parameters:</p> Name Type Description Default <code>href</code> <p>Server URL for the popup endpoint.</p> required <code>*args</code> <p>Positional arguments for DataPopupControl.</p> <code>()</code> <code>**kwds</code> <p>Keyword arguments for DataPopupControl.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def __init__(self, href, *args, **kwds):\n    \"\"\"Initialize with a server URL.\n\n    Args:\n        href: Server URL for the popup endpoint.\n        *args: Positional arguments for DataPopupControl.\n        **kwds: Keyword arguments for DataPopupControl.\n    \"\"\"\n    self.href = href\n    self.defaultvalue = None\n    popuphtml.DataPopupControl.__init__(self, *args, **kwds)\n</code></pre> Methods:\u00b6"},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.popupcelleditors.PopupDataCellEditor","title":"PopupDataCellEditor","text":"<pre><code>PopupDataCellEditor()\n</code></pre> <p>               Bases: <code>GridCellEditor</code></p> <p>Grid cell editor using a server-backed DataPopupControl.</p> <p>Provides inline editing for grid cells that need data lookup via server endpoints. The address can be set via property or parameters.</p> <p>Initialize the editor with no address or params.</p> Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def __init__(self):\n    \"\"\"Initialize the editor with no address or params.\"\"\"\n    GridCellEditor.__init__(self)\n    self.address = None\n    self.param = None\n</code></pre> Methods:\u00b6 <code></code> ApplyEdit \u00b6 <pre><code>ApplyEdit(row, col, grid)\n</code></pre> <p>Apply the edit value to the table.</p> <p>Parameters:</p> Name Type Description Default <code>row</code> <p>Grid row index.</p> required <code>col</code> <p>Grid column index.</p> required <code>grid</code> <p>The parent wx.Grid.</p> required Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def ApplyEdit(self, row, col, grid):\n    \"\"\"Apply the edit value to the table.\n\n    Args:\n        row: Grid row index.\n        col: Grid column index.\n        grid: The parent wx.Grid.\n    \"\"\"\n    val = self._tc.GetValue()\n    grid.GetTable().SetValue(row, col, val)\n    self.start_value = \"\"\n    self._tc.SetValue(\"\")\n</code></pre> <code></code> BeginEdit \u00b6 <pre><code>BeginEdit(row, col, grid)\n</code></pre> <p>Begin editing a cell.</p> <p>Parameters:</p> Name Type Description Default <code>row</code> <p>Grid row index.</p> required <code>col</code> <p>Grid column index.</p> required <code>grid</code> <p>The parent wx.Grid.</p> required Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def BeginEdit(self, row, col, grid):\n    \"\"\"Begin editing a cell.\n\n    Args:\n        row: Grid row index.\n        col: Grid column index.\n        grid: The parent wx.Grid.\n    \"\"\"\n    self.grid = grid\n    self.start_value = None\n    value = grid.GetTable().GetValue(row, col)\n\n    if isinstance(value, datetime.date):\n        self.start_value = value.isoformat()\n    elif isinstance(value, str):\n        self.start_value = value\n    else:\n        self.start_value = str(value)\n\n    self._tc.focus_in(self.start_value)\n    if self.start_value:\n        self._tc.set_rec(self.start_value, (self.start_value,))\n    self._tc.SetInsertionPointEnd()\n    self._tc.GetTextCtrl().SetSelection(0, -1)\n    self._tc.GetTextCtrl().SetFocus()\n</code></pre> <code></code> Clone \u00b6 <pre><code>Clone()\n</code></pre> <p>Return a copy of this editor.</p> <p>Returns:</p> Type Description <p>A new PopupDataCellEditor instance.</p> Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def Clone(self):\n    \"\"\"Return a copy of this editor.\n\n    Returns:\n        A new PopupDataCellEditor instance.\n    \"\"\"\n    ret = PopupDataCellEditor()\n    ret.SetParameters(self.param)\n    return ret\n</code></pre> <code></code> Create \u00b6 <pre><code>Create(parent, id, evt_handler)\n</code></pre> <p>Create the cell editor control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>id</code> <p>Window ID.</p> required <code>evt_handler</code> <p>Event handler for focus events.</p> required Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def Create(self, parent, id, evt_handler):\n    \"\"\"Create the cell editor control.\n\n    Args:\n        parent: Parent window.\n        id: Window ID.\n        evt_handler: Event handler for focus events.\n    \"\"\"\n    self.Parent = parent\n    self._tc = PopupDataCellControl(self.address, parent, id)\n    self._tc.DismissObject = self\n    self.SetControl(self._tc)\n    self.evtHandler = evt_handler\n\n    if evt_handler:\n        self._tc.PushEventHandler(self.evtHandler)\n        evt_handler.Bind(wx.EVT_KILL_FOCUS, self.on_kill_focus)\n</code></pre> <code></code> EndEdit \u00b6 <pre><code>EndEdit(row, col, grid, old_value)\n</code></pre> <p>End editing and commit changes if value changed.</p> <p>Parameters:</p> Name Type Description Default <code>row</code> <p>Grid row index.</p> required <code>col</code> <p>Grid column index.</p> required <code>grid</code> <p>The parent wx.Grid.</p> required <code>old_value</code> <p>The previous cell value.</p> required <p>Returns:</p> Type Description <p>True if the value changed, False otherwise.</p> Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def EndEdit(self, row, col, grid, old_value):\n    \"\"\"End editing and commit changes if value changed.\n\n    Args:\n        row: Grid row index.\n        col: Grid column index.\n        grid: The parent wx.Grid.\n        old_value: The previous cell value.\n\n    Returns:\n        True if the value changed, False otherwise.\n    \"\"\"\n    self._tc.focus_out(self._tc.GetValue())\n    changed = False\n    value = self._tc.GetValue()\n    if str(value) != str(self.start_value):\n        changed = True\n        grid.GetTable().SetValue(row, col, value)\n    grid.SetFocus()\n    return changed\n</code></pre> <code></code> IsAcceptedKey \u00b6 <pre><code>IsAcceptedKey(evt)\n</code></pre> <p>Check if a key event should start cell editing.</p> <p>Parameters:</p> Name Type Description Default <code>evt</code> <p>wx.KeyEvent.</p> required <p>Returns:</p> Type Description <p>True if the key is accepted for starting edit.</p> Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def IsAcceptedKey(self, evt):\n    \"\"\"Check if a key event should start cell editing.\n\n    Args:\n        evt: wx.KeyEvent.\n\n    Returns:\n        True if the key is accepted for starting edit.\n    \"\"\"\n    return (\n        not (evt.ControlDown() or evt.AltDown())\n        and evt.GetKeyCode() != wx.WXK_SHIFT\n    )\n</code></pre> <code></code> PaintBackground \u00b6 <pre><code>PaintBackground(rect, attr)\n</code></pre> <p>No-op background paint for the editor.</p> Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def PaintBackground(self, rect, attr):\n    \"\"\"No-op background paint for the editor.\"\"\"\n</code></pre> <code></code> Reset \u00b6 <pre><code>Reset()\n</code></pre> <p>Reset the editor to the initial value.</p> Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def Reset(self):\n    \"\"\"Reset the editor to the initial value.\"\"\"\n    self._tc.SetValue(self.start_value)\n</code></pre> <code></code> SetSize \u00b6 <pre><code>SetSize(rect)\n</code></pre> <p>Set the size of the editor control.</p> <p>Parameters:</p> Name Type Description Default <code>rect</code> <p>wx.Rect defining position and size.</p> required Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def SetSize(self, rect):\n    \"\"\"Set the size of the editor control.\n\n    Args:\n        rect: wx.Rect defining position and size.\n    \"\"\"\n    self._tc.SetSize(\n        rect.x, rect.y, rect.width + 2, rect.height + 2, wx.SIZE_ALLOW_MINUS_ONE\n    )\n</code></pre> <code></code> Show \u00b6 <pre><code>Show(show, attr)\n</code></pre> <p>Show or hide the editor.</p> <p>Parameters:</p> Name Type Description Default <code>show</code> <p>Boolean visibility flag.</p> required <code>attr</code> <p>Cell attributes.</p> required Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def Show(self, show, attr):\n    \"\"\"Show or hide the editor.\n\n    Args:\n        show: Boolean visibility flag.\n        attr: Cell attributes.\n    \"\"\"\n    GridCellEditor.Show(self, show, attr)\n</code></pre> <code></code> StartingClick \u00b6 <pre><code>StartingClick()\n</code></pre> <p>Handle click to start editing (no-op).</p> Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def StartingClick(self):\n    \"\"\"Handle click to start editing (no-op).\"\"\"\n</code></pre> <code></code> StartingKey \u00b6 <pre><code>StartingKey(evt)\n</code></pre> <p>Handle the initiating keystroke for cell editing.</p> <p>Parameters:</p> Name Type Description Default <code>evt</code> <p>wx.KeyEvent.</p> required Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def StartingKey(self, evt):\n    \"\"\"Handle the initiating keystroke for cell editing.\n\n    Args:\n        evt: wx.KeyEvent.\n    \"\"\"\n    key = evt.GetKeyCode()\n    ch = None\n    if 0 &lt;= key &lt; 256 and chr(key) in string.printable:\n        ch = chr(key)\n    if ch is not None:\n        self._tc.start_value = \"\"\n        self._tc.SetValue(ch)\n        if self._tc.popup:\n            self._tc.popup.set_string_value(ch)\n        self._tc.rec_value = (ch,)\n        if self._tc.popup:\n            self._tc.popup.Dismiss()\n        self._tc.SetInsertionPointEnd()\n    else:\n        evt.Skip()\n</code></pre> <code></code> get_address \u00b6 <pre><code>get_address()\n</code></pre> <p>Return the server address for data lookup.</p> Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def get_address(self):\n    \"\"\"Return the server address for data lookup.\"\"\"\n    return self._address\n</code></pre> <code></code> on_kill_focus \u00b6 <pre><code>on_kill_focus(event)\n</code></pre> <p>Handle kill-focus event (no-op).</p> Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def on_kill_focus(self, event):\n    \"\"\"Handle kill-focus event (no-op).\"\"\"\n</code></pre> <code></code> set_address \u00b6 <pre><code>set_address(address)\n</code></pre> <p>Set the server address.</p> <p>Parameters:</p> Name Type Description Default <code>address</code> <p>URL string for the data endpoint.</p> required Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def set_address(self, address):\n    \"\"\"Set the server address.\n\n    Args:\n        address: URL string for the data endpoint.\n    \"\"\"\n    self._address = address\n</code></pre> <code></code> set_parameters \u00b6 <pre><code>set_parameters(params)\n</code></pre> <p>Set editor parameters.</p> <p>Parameters:</p> Name Type Description Default <code>params</code> <p>Parameter dict or value.</p> required Source code in <code>pytigon_gui/guictrl/grid/popupcelleditors.py</code> <pre><code>def set_parameters(self, params):\n    \"\"\"Set editor parameters.\n\n    Args:\n        params: Parameter dict or value.\n    \"\"\"\n    self.param = params\n</code></pre>"},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.popupcelleditors-modules","title":"Modules","text":""},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.renderers","title":"renderers","text":"<p>Module contains helper classes for grid renderers</p>"},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.renderers-classes","title":"Classes","text":""},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.renderers.DateTimeRenderer","title":"DateTimeRenderer","text":"<pre><code>DateTimeRenderer()\n</code></pre> <p>               Bases: <code>GridCellRenderer</code></p> <p>DateTime renderer</p> Source code in <code>pytigon_gui/guictrl/grid/renderers.py</code> <pre><code>def __init__(self):\n    wx.grid.GridCellRenderer.__init__(self)\n    self.best_size = None\n</code></pre>"},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.renderers.ExtStringRenderer","title":"ExtStringRenderer","text":"<pre><code>ExtStringRenderer()\n</code></pre> <p>               Bases: <code>GridCellRenderer</code></p> <p>Enhanced version of string renderer</p> Source code in <code>pytigon_gui/guictrl/grid/renderers.py</code> <pre><code>def __init__(self):\n    \"\"\"Enhanced version of string renderer\"\"\"\n    wx.grid.GridCellRenderer.__init__(self)\n</code></pre> Methods:\u00b6"},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.renderers.IconAndStringRenderer","title":"IconAndStringRenderer","text":"<pre><code>IconAndStringRenderer()\n</code></pre> <p>               Bases: <code>MultiLineStringRenderer</code></p> <p>String renderer extended for rendering icons</p> Source code in <code>pytigon_gui/guictrl/grid/renderers.py</code> <pre><code>def __init__(self):\n    MultiLineStringRenderer.__init__(self, 64)\n    self.cache = {}\n</code></pre>"},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.renderers.MultiLineStringRenderer","title":"MultiLineStringRenderer","text":"<pre><code>MultiLineStringRenderer(width)\n</code></pre> <p>               Bases: <code>ExtStringRenderer</code></p> <p>Multiline string renderer</p> Source code in <code>pytigon_gui/guictrl/grid/renderers.py</code> <pre><code>def __init__(self, width):\n    self.width = width\n    ExtStringRenderer.__init__(self)\n</code></pre>"},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.tabproxy","title":"tabproxy","text":"<p>Server data proxy for grid tables.</p> <p>Provides DataProxy: a communication layer that fetches paginated data from a Django backend via HTTP, supporting CRUD operations, sorting, autocomplete, and column metadata retrieval.</p>"},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.tabproxy-classes","title":"Classes","text":""},{"location":"api/guictrl_grid/#pytigon_gui.guictrl.grid.tabproxy.DataProxy","title":"DataProxy","text":"<pre><code>DataProxy(http, tabaddress)\n</code></pre> Source code in <code>pytigon_gui/guictrl/grid/tabproxy.py</code> <pre><code>def __init__(self, http, tabaddress):\n    self.max_count = 1000000\n    self.var_count = -1\n    self.tabaddress = tabaddress\n    self.tabaddress0 = self.tabaddress\n    self.parm = \"\"\n    self.parent = None\n    self.http = http\n    self.col_types2 = []\n    self.parm = dict()\n    self.is_valid = True\n\n    response = self.http.post(\n        self.parent,\n        self.tabaddress,\n        process_post_parm(\n            {\n                \"cmd\": CMD_INFO,\n            }\n        ),\n    )\n    ret = schjson.loads(response.str())\n\n    self.col_names = ret[\"col_names\"]\n    self.col_types = ret[\"col_types\"]\n    self.default_rec = ret[\"default_rec\"]\n    self.col_size = ret[\"col_length\"]\n\n    for i in range(0, len(self.col_size)):\n        if self.col_size[i] &gt; 32:\n            self.col_size[i] = 32\n\n    self.auto_cols = ret[\"auto_cols\"]\n\n    for col in self.col_types:\n        self.col_types2.append(col.split(\":\")[0])\n\n    self.tab_conw = {\n        \"long\": self.conw_long,\n        \"string\": self.conw_none,\n        \"double\": self.conw_float,\n        \"bool\": self.conw_bool,\n        \"choice\": self.conw_none,\n        \"x\": self.conw_none,\n    }\n</code></pre> Methods:\u00b6 <code></code> auto_update \u00b6 <pre><code>auto_update(col_name, col_names, rec)\n</code></pre> <p>Return transformed row after current row is changed</p> Source code in <code>pytigon_gui/guictrl/grid/tabproxy.py</code> <pre><code>def auto_update(self, col_name, col_names, rec):\n    \"\"\"Return transformed row after current row is changed\"\"\"\n\n    col_name2 = schjson.dumps(col_name)\n    col_names2 = schjson.dumps(col_names)\n    rec2 = schjson.dumps(rec)\n\n    c = {\n        \"cmd\": CMD_AUTO,\n        \"col_name\": col_name2,\n        \"col_names\": col_names2,\n        \"rec\": rec2,\n    }\n\n    response = self.http.post(self.parent, self.tabaddress, process_post_parm(c))\n    ret = schjson.loads(response.str())\n    if ret == None:\n        return rec\n    else:\n        return ret[\"rec\"]\n</code></pre>"},{"location":"api/guictrl_grids/","title":"guictrl.grids \u2013 Grid Container","text":"<p>Grid layout container for arranging child controls in a wxGrid-compatible structure.</p>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids","title":"grids","text":"<p>Grid widget classes for the SchForm GUI framework.</p> <p>Provides wxPython grid/table controls integrated with SchBaseCtrl: table grid, data grid, and update-grid-button.</p>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids-attributes","title":"Attributes","text":""},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.logger","title":"logger  <code>module-attribute</code>","text":"<pre><code>logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids-classes","title":"Classes","text":""},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.GRID","title":"GRID","text":"<pre><code>GRID(parent, **kwds)\n</code></pre> <p>               Bases: <code>SchTableGrid</code>, <code>SchBaseCtrl</code></p> <p>Data-bound grid connected to a server data proxy.</p> <p>Handles ctrlgrid tag. Uses a DataProxy to fetch and display paginated server data. Supports readonly mode and auto-refresh on parent parameter changes.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    parm = createparm.create_parm(self.src, parent.get_parm_obj())\n    if parm:\n        self.proxy = tabproxy.DataProxy(wx.GetApp().get_http(parent), str(parm[0]))\n        self.proxy.set_address_parm(parm[2])\n    else:\n        self.proxy = tabproxy.DataProxy(wx.GetApp().get_http(parent), str(self.src))\n    table = gridtable_from_proxy.DataSource(self.proxy)\n\n    if self.readonly:\n        kwds[\"typ\"] = self.VIEW\n        table.set_read_only(True)\n    else:\n        table.set_read_only(False)\n\n    super().__init__(table, self.src, parent, **kwds)\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.GRID-functions","title":"Methods:","text":""},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.GRID.OnSize","title":"OnSize","text":"<pre><code>OnSize(event=None)\n</code></pre> <p>Handle resize event, adjusting height for toolbar.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>wx.SizeEvent or None.</p> <code>None</code> Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def OnSize(self, event=None):\n    \"\"\"Handle resize event, adjusting height for toolbar.\n\n    Args:\n        event: wx.SizeEvent or None.\n    \"\"\"\n    if event is not None:\n        size = event.GetSize()\n        old = self.GetSize()\n        if old[1] != size[1] - 8:\n            self.SetSize((size[0], max(0, size[1] - 8)))\n            self.GetParent().RefrPage()\n        event.Skip()\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.GRID.process_refr_data","title":"process_refr_data","text":"<pre><code>process_refr_data(**kwds)\n</code></pre> <p>Refresh grid with new data source.</p> <p>Parameters:</p> Name Type Description Default <code>**kwds</code> <p>New keyword arguments.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def process_refr_data(self, **kwds):\n    \"\"\"Refresh grid with new data source.\n\n    Args:\n        **kwds: New keyword arguments.\n    \"\"\"\n    self.init_base(kwds)\n\n    parm = createparm.create_parm(self.src, self.GetParent().get_parm_obj())\n    if parm:\n        self.proxy = tabproxy.DataProxy(wx.GetApp().get_http(self), str(parm[0]))\n        self.proxy.SetAddressParm(parm[2])\n    else:\n        self.proxy = tabproxy.DataProxy(wx.GetApp().get_http(self), str(self.src))\n    table = gridtable_from_proxy.DataSource(self.proxy)\n    self.SetTable(table)\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.GRID.refr_obj","title":"refr_obj","text":"<pre><code>refr_obj()\n</code></pre> <p>Refresh grid if parent is visible and parameters changed.</p> Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def refr_obj(self):\n    \"\"\"Refresh grid if parent is visible and parameters changed.\"\"\"\n    if self.GetParent().IsShown():\n        parm = createparm.create_parm(self.src, self.GetParent().get_parm_obj())\n        if parm:\n            if self.proxy.set_address_parm(parm[2]):\n                self.GetTable().refresh(False)\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.SchBaseCtrl","title":"SchBaseCtrl","text":"<pre><code>SchBaseCtrl(parent, kwds)\n</code></pre> <p>Base mixin class for all SchForm wxPython widgets.</p> <p>Provides: - Attribute initialization from HTML tag keyword arguments - Parent form/page navigation - Server data loading for tdata (table data) and ldata (list data) - Common keyboard handling (Escape, Tab) - Widget lifecycle: after_create signal, unique name management</p> <p>Initialize base control attributes.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent wxPython window.</p> required <code>kwds</code> <p>Keyword argument dict parsed from HTML tag attributes.</p> required Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def __init__(self, parent, kwds):\n    \"\"\"Initialize base control attributes.\n\n    Args:\n        parent: Parent wxPython window.\n        kwds: Keyword argument dict parsed from HTML tag attributes.\n    \"\"\"\n    self.unique_name = None\n    self.init_base(kwds, parent)\n    self.accept_focus = True\n    self.acc_tab_bind = False\n    form = self.get_parent_form()\n    if form:\n        form.signal_from_child(self, \"__init__\")\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.SchBaseCtrl-functions","title":"Methods:","text":""},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.SchBaseCtrl.CanAcceptFocus","title":"CanAcceptFocus","text":"<pre><code>CanAcceptFocus()\n</code></pre> <p>Return True if the control can accept keyboard focus.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def CanAcceptFocus(self):\n    \"\"\"Return True if the control can accept keyboard focus.\"\"\"\n    if self.IsShown() and self.IsEnabled():\n        return self.accept_focus\n    return False\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.SchBaseCtrl.CanAcceptFocusFromKeyboard","title":"CanAcceptFocusFromKeyboard","text":"<pre><code>CanAcceptFocusFromKeyboard()\n</code></pre> <p>Return True if the control accepts focus from keyboard navigation.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def CanAcceptFocusFromKeyboard(self):\n    \"\"\"Return True if the control accepts focus from keyboard navigation.\"\"\"\n    return self.CanAcceptFocus()\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.SchBaseCtrl.after_create","title":"after_create","text":"<pre><code>after_create()\n</code></pre> <p>Called immediately after the widget is fully constructed.</p> <p>Binds keyboard events, executes the 'onload' script (if any), calls the ext_init hook if present, and signals the parent form that this child is ready.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def after_create(self):\n    \"\"\"Called immediately after the widget is fully constructed.\n\n    Binds keyboard events, executes the 'onload' script (if any),\n    calls the __ext_init__ hook if present, and signals the parent\n    form that this child is ready.\n    \"\"\"\n    self.Bind(wx.EVT_KEY_DOWN, self.on_key_down_base)\n    if self.onload:\n        d = {\"wx\": wx, \"self\": self}\n        try:\n            exec(self.onload, d)\n        except Exception:\n            logger.exception(\"Error executing onload script for %s\", self.GetName())\n\n    if hasattr(self, \"__ext_init__\"):\n        self.__ext_init__()\n\n    self.get_parent_form().signal_from_child(self, \"after_create\")\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.SchBaseCtrl.get_ldata","title":"get_ldata","text":"<pre><code>get_ldata()\n</code></pre> <p>Return hierarchical list data, loading from server if necessary.</p> <p>Returns:</p> Type Description <p>List data (nested tree structure), or None if unavailable.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_ldata(self):\n    \"\"\"Return hierarchical list data, loading from server if necessary.\n\n    Returns:\n        List data (nested tree structure), or None if unavailable.\n    \"\"\"\n    if self.ldata is not None:\n        return self.ldata\n    if self.ldatabuf is not None:\n        return self.ldatabuf\n    if self.src:\n        self.refresh_ldata()\n    return self.ldatabuf\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.SchBaseCtrl.get_parent_form","title":"get_parent_form","text":"<pre><code>get_parent_form()\n</code></pre> <p>Walk up the widget hierarchy to find the parent SchForm.</p> <p>Returns:</p> Type Description <p>The SchForm ancestor, or None if not found.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_parent_form(self):\n    \"\"\"Walk up the widget hierarchy to find the parent SchForm.\n\n    Returns:\n        The SchForm ancestor, or None if not found.\n    \"\"\"\n    parent = self.parent\n    while parent is not None:\n        if type(parent).__name__ == \"SchForm\":\n            return parent\n        parent = parent.GetParent()\n    return None\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.SchBaseCtrl.get_parent_page","title":"get_parent_page","text":"<pre><code>get_parent_page()\n</code></pre> <p>Return the parent page that contains this control's form.</p> <p>Returns:</p> Type Description <p>The parent SchPage, or None if not found.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_parent_page(self):\n    \"\"\"Return the parent page that contains this control's form.\n\n    Returns:\n        The parent SchPage, or None if not found.\n    \"\"\"\n    form = self.get_parent_form()\n    if form:\n        return form.get_parent_page()\n    return None\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.SchBaseCtrl.get_tdata","title":"get_tdata","text":"<pre><code>get_tdata()\n</code></pre> <p>Return table data, loading from server if necessary.</p> <p>Returns:</p> Type Description <p>Table data (list of rows), or None if unavailable.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_tdata(self):\n    \"\"\"Return table data, loading from server if necessary.\n\n    Returns:\n        Table data (list of rows), or None if unavailable.\n    \"\"\"\n    if self.tdata is not None:\n        return self.tdata\n    if self.tdatabuf is not None:\n        return self.tdatabuf\n    if self.src:\n        self.refresh_tdata()\n    return self.tdatabuf\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.SchBaseCtrl.get_unique_name","title":"get_unique_name","text":"<pre><code>get_unique_name()\n</code></pre> <p>Return the unique name identifier.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_unique_name(self):\n    \"\"\"Return the unique name identifier.\"\"\"\n    return self.unique_name\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.SchBaseCtrl.init_base","title":"init_base","text":"<pre><code>init_base(kwds, parent=None)\n</code></pre> <p>Initialize all control attributes from kwds.</p> <p>Extracts well-known attributes (href, label, id, target, value, valuetype, defaultvalue, length, maxlength, readonly, hidden, src, onload, tdata, ldata, data, param, style) from kwds and assigns them as instance attributes.</p> <p>Parameters:</p> Name Type Description Default <code>kwds</code> <p>Keyword argument dict parsed from HTML tag attributes.</p> required <code>parent</code> <p>Optional parent window reference.</p> <code>None</code> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def init_base(self, kwds, parent=None):\n    \"\"\"Initialize all control attributes from kwds.\n\n    Extracts well-known attributes (href, label, id, target, value,\n    valuetype, defaultvalue, length, maxlength, readonly, hidden,\n    src, onload, tdata, ldata, data, param, style) from kwds\n    and assigns them as instance attributes.\n\n    Args:\n        kwds: Keyword argument dict parsed from HTML tag attributes.\n        parent: Optional parent window reference.\n    \"\"\"\n    if parent:\n        self.parent = parent\n\n    self.ldatabuf = None\n\n    self.tag = None\n    if \"param\" in kwds and \"tag\" in kwds[\"param\"]:\n        self.tag = kwds[\"param\"][\"tag\"]\n\n    self.href = _extract_kwarg(kwds, \"href\", None)\n    if self.href is None:\n        self.href = \"\"\n\n    self.label = _extract_kwarg(kwds, \"label\", None)\n    self.nr_id = _extract_kwarg(kwds, \"id\", None)\n    self.target = _extract_kwarg(kwds, \"target\", \"_blank\")\n    self.value = _extract_kwarg(kwds, \"value\", None)\n    self.valuetype = _extract_kwarg(kwds, \"valuetype\", \"data\")\n    self.defaultvalue = _extract_kwarg(kwds, \"defaultvalue\", None)\n    self.length = int(_extract_kwarg(kwds, \"length\", 0))\n    self.maxlength = int(_extract_kwarg(kwds, \"maxlength\", 0))\n    self.readonly = _extract_kwarg(kwds, \"readonly\", False)\n    self.hidden = True if _extract_kwarg(kwds, \"hidden\", False) else False\n    self.src = _extract_kwarg(kwds, \"src\", None)\n    self.onload = _extract_kwarg(kwds, \"onload\", None)\n\n    # tdata / tdatabuf: table data (already parsed) or lazily loaded\n    self.tdata = _extract_kwarg(kwds, \"tdata\", None)\n    self.tdatabuf = None\n    if self.tdata is not None:\n        self.tdatabuf = None\n\n    # ldata: hierarchical list data (already parsed) or lazily loaded\n    self.ldata = _extract_kwarg(kwds, \"ldata\", None)\n\n    # data: URL-encoded string data\n    raw_data = _extract_kwarg(kwds, \"data\", None)\n    self.data = unquote(raw_data) if raw_data is not None else None\n\n    self.param = _extract_kwarg(kwds, \"param\", None)\n    self.style = _extract_kwarg(kwds, \"style\", None)\n\n    # Invoke any registered ctrl_process hooks for this tag type\n    ctrl_process = wx.GetApp().ctrl_process\n    if self.tag in ctrl_process:\n        for fun in ctrl_process[self.tag]:\n            fun(self)\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.SchBaseCtrl.is_ctrl_block","title":"is_ctrl_block","text":"<pre><code>is_ctrl_block()\n</code></pre> <p>Return True if this control is a block-level element.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def is_ctrl_block(self):\n    \"\"\"Return True if this control is a block-level element.\"\"\"\n    return self.GetParent().is_ctrl_block(self)\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.SchBaseCtrl.load_data_from_server","title":"load_data_from_server","text":"<pre><code>load_data_from_server(path)\n</code></pre> <p>Load raw binary data from a server URL.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <p>URL path to fetch.</p> required <p>Returns:</p> Type Description <p>Raw response data (bytes).</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def load_data_from_server(self, path):\n    \"\"\"Load raw binary data from a server URL.\n\n    Args:\n        path: URL path to fetch.\n\n    Returns:\n        Raw response data (bytes).\n    \"\"\"\n    http = wx.GetApp().get_http(self.parent)\n    response = http.get(self, str(path))\n    return response.ptr()\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.SchBaseCtrl.load_string_from_server","title":"load_string_from_server","text":"<pre><code>load_string_from_server(path)\n</code></pre> <p>Load a text response from a server URL.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <p>URL path to fetch.</p> required <p>Returns:</p> Type Description <p>Response body as a string.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def load_string_from_server(self, path):\n    \"\"\"Load a text response from a server URL.\n\n    Args:\n        path: URL path to fetch.\n\n    Returns:\n        Response body as a string.\n    \"\"\"\n    http = wx.GetApp().get_http(self.parent)\n    response = http.get(self, str(path))\n    return response.str()\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.SchBaseCtrl.on_key_down_base","title":"on_key_down_base","text":"<pre><code>on_key_down_base(event)\n</code></pre> <p>Default key-down handler for Escape and Tab navigation.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>wx.KeyEvent.</p> required Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def on_key_down_base(self, event):\n    \"\"\"Default key-down handler for Escape and Tab navigation.\n\n    Args:\n        event: wx.KeyEvent.\n    \"\"\"\n    if event.GetKeyCode() == wx.WXK_ESCAPE:\n        wx.CallAfter(self.GetParent().cancel)\n    elif event.GetKeyCode() == wx.WXK_TAB:\n        if event.ShiftDown():\n            self.GetParent().Navigate(self, True)\n        else:\n            self.GetParent().Navigate(self, False)\n    else:\n        event.Skip()\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.SchBaseCtrl.refresh_ldata","title":"refresh_ldata","text":"<pre><code>refresh_ldata()\n</code></pre> <p>Refresh hierarchical list data from the server.</p> <p>Loads from self.src and parses into self.ldatabuf using TreeParser.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def refresh_ldata(self):\n    \"\"\"Refresh hierarchical list data from the server.\n\n    Loads from self.src and parses into self.ldatabuf using TreeParser.\n    \"\"\"\n    if self.ldata is None and self.src:\n        lista = self.load_data_from_server(self.src)\n        mp = TreeParser()\n        mp.feed(lista)\n        mp.close()\n        self.ldatabuf = mp.tree_parent[0][1]\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.SchBaseCtrl.refresh_tdata","title":"refresh_tdata","text":"<pre><code>refresh_tdata(html_src=None)\n</code></pre> <p>Refresh table data from the server or provided HTML source.</p> <p>Parameters:</p> Name Type Description Default <code>html_src</code> <p>Optional HTML string containing table data. If None, loads from self.src.</p> <code>None</code> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def refresh_tdata(self, html_src=None):\n    \"\"\"Refresh table data from the server or provided HTML source.\n\n    Args:\n        html_src: Optional HTML string containing table data.\n            If None, loads from self.src.\n    \"\"\"\n    if self.tdata is None and self.src:\n        if html_src:\n            tables = html_src\n        else:\n            tables = self.load_data_from_server(self.src).decode(\"utf-8\")\n        self.tdatabuf = tdata_from_html(tables, wx.GetApp().http)\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.SchBaseCtrl.set_acc_key_tab","title":"set_acc_key_tab","text":"<pre><code>set_acc_key_tab(tab)\n</code></pre> <p>Register an accelerator table for this widget.</p> <p>Parameters:</p> Name Type Description Default <code>tab</code> <p>List or tuple of accelerator elements. Each element has structure: (flag, keycode, callback function).</p> required <p>Returns:</p> Type Description <p>Result from the parent's set_acc_key_tab method.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def set_acc_key_tab(self, tab):\n    \"\"\"Register an accelerator table for this widget.\n\n    Args:\n        tab: List or tuple of accelerator elements. Each element has\n            structure: (flag, keycode, callback function).\n\n    Returns:\n        Result from the parent's set_acc_key_tab method.\n    \"\"\"\n    return self.GetParent().set_acc_key_tab(self, tab)\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.SchBaseCtrl.set_unique_name","title":"set_unique_name","text":"<pre><code>set_unique_name(name)\n</code></pre> <p>Assign a unique name identifier to this control.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def set_unique_name(self, name):\n    \"\"\"Assign a unique name identifier to this control.\"\"\"\n    self.unique_name = name\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.SchGridPanel","title":"SchGridPanel","text":"<pre><code>SchGridPanel(*args, **argv)\n</code></pre> <p>               Bases: <code>Panel</code></p> <p>Panel for grid widget with toolbar</p> Source code in <code>pytigon_gui/guictrl/grid/gridpanel.py</code> <pre><code>def __init__(self, *args, **argv):\n    self.grid = None\n    self.icon_size = 2\n    wx.Panel.__init__(self, *args, **argv)\n    self.vertical = False\n    self._bitmaps = {\n        \"edit\": \"wx.ART_FILE_OPEN\",\n        \"edit_inline\": \"wx.ART_FILE_OPEN\",\n        \"delete\": \"wx.ART_DELETE\",\n        \"view_row\": \"wx.ART_INFORMATION\",\n    }\n    self._menu_buttons = collections.OrderedDict()\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.SimpleDataTable","title":"SimpleDataTable","text":"<pre><code>SimpleDataTable(parent, tab)\n</code></pre> <p>               Bases: <code>SchGridTableBase</code></p> Source code in <code>pytigon_gui/guictrl/grid/gridtable_from_html_table.py</code> <pre><code>def __init__(self, parent, tab):\n    SchGridTableBase.__init__(self)\n    self._parent = parent\n    self.init_data(tab)\n    # self.last_row_count = len(self.data)\n    self.last_row_count = self.count\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.TABLE","title":"TABLE","text":"<pre><code>TABLE(parent, **kwds)\n</code></pre> <p>               Bases: <code>SchGridPanel</code>, <code>SchBaseCtrl</code></p> <p>Table grid panel for viewing/editing HTML table data.</p> <p>Handles ctrltable tag. Renders data from tdata as a grid with toolbar actions. Supports row insert/update/delete signals and server-side data refresh.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n\n    if self.param and \"table_lp\" in self.param:\n        self._table_lp = int(self.param[\"table_lp\"])\n    else:\n        self._table_lp = 0\n\n    name = kwds.get(\"name\", \"LIST\")\n\n    if \"size\" in kwds:\n        SchGridPanel.__init__(self, parent, size=kwds[\"size\"], name=name)\n    else:\n        SchGridPanel.__init__(self, parent, name=name)\n\n    tdata = self.get_tdata()\n    if not tdata:\n        logger.warning(\"no tdata: href=%s src=%s\", self.href, self.src)\n    if tdata:\n        table = SimpleDataTable(self, tdata)\n        if self.param and \"no_actions\" in self.param:\n            table.set_no_actions(True)\n    else:\n        table = None\n\n    self.grid = grid.SchTableGrid(\n        table,\n        \"\",\n        self,\n        typ=grid.SchTableGrid.VIEW,\n        style=wx.TAB_TRAVERSAL | wx.FULL_REPAINT_ON_RESIZE,\n    )\n    self.create_toolbar(self.grid)\n    self.Bind(wx.EVT_CLOSE, self._on_close)\n    self._table = table\n    for signal_name in (\"update_row_ok\", \"new_row_ok\", \"delete_row_ok\"):\n        self.get_parent_page().register_signal(self, signal_name)\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.TABLE-functions","title":"Methods:","text":""},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.TABLE.delete_row_ok","title":"delete_row_ok","text":"<pre><code>delete_row_ok(data)\n</code></pre> <p>Handle delete_row_ok signal.</p> <p>Hides the current row and moves cursor appropriately.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <p>Delete signal data (unused).</p> required <p>Returns:</p> Type Description <p>True.</p> Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def delete_row_ok(self, data):\n    \"\"\"Handle delete_row_ok signal.\n\n    Hides the current row and moves cursor appropriately.\n\n    Args:\n        data: Delete signal data (unused).\n\n    Returns:\n        True.\n    \"\"\"\n    row_id = self.grid.GetGridCursorRow()\n    self.grid.HideRow(row_id)\n    row_id += 1\n    if row_id &gt;= self.grid.GetNumberRows():\n        self.grid.goto_last_row()\n    else:\n        self.grid.SetGridCursor(row_id, 0)\n        self.grid.MakeCellVisible(row_id, 0)\n    return True\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.TABLE.do_refresh","title":"do_refresh","text":"<pre><code>do_refresh(tdata)\n</code></pre> <p>Replace table data and restore cursor position.</p> <p>Parameters:</p> Name Type Description Default <code>tdata</code> <p>New table data to display.</p> required Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def do_refresh(self, tdata):\n    \"\"\"Replace table data and restore cursor position.\n\n    Args:\n        tdata: New table data to display.\n    \"\"\"\n    oldRow = self.grid.GetGridCursorRow()\n    self._table.replace_tab(tdata)\n    if self.grid.last_action == \"insert\":\n        newRow = self.grid.GetGridCursorRow() + 1\n        if newRow &lt; self.grid.GetTable().GetNumberRows():\n            self.grid.SetGridCursor(newRow, 0)\n            self.grid.MakeCellVisible(newRow, 0)\n    else:\n        if oldRow &lt; self.grid.GetTable().GetNumberRows():\n            if oldRow &lt; 0:\n                oldRow = 0\n            self.grid.SetGridCursor(oldRow, 0)\n            self.grid.MakeCellVisible(oldRow, 0)\n        else:\n            self.grid.goto_last_row()\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.TABLE.new_row_ok","title":"new_row_ok","text":"<pre><code>new_row_ok(data)\n</code></pre> <p>Handle new_row_ok signal.</p> Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def new_row_ok(self, data):\n    \"\"\"Handle new_row_ok signal.\"\"\"\n    return self._row_ok(data, insert=True)\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.TABLE.process_refr_data","title":"process_refr_data","text":"<pre><code>process_refr_data(**kwds)\n</code></pre> <p>Refresh the table with new data.</p> <p>Parameters:</p> Name Type Description Default <code>**kwds</code> <p>New keyword arguments.</p> <code>{}</code> <p>Returns:</p> Type Description <p>Result from do_refresh.</p> Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def process_refr_data(self, **kwds):\n    \"\"\"Refresh the table with new data.\n\n    Args:\n        **kwds: New keyword arguments.\n\n    Returns:\n        Result from do_refresh.\n    \"\"\"\n    self.grid.last_action = \"\"\n    self.init_base(kwds)\n    tdata = self.get_tdata()\n    return self.do_refresh(tdata)\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.TABLE.refresh_from_source","title":"refresh_from_source","text":"<pre><code>refresh_from_source(html_src)\n</code></pre> <p>Refresh table from raw HTML source.</p> <p>Parameters:</p> Name Type Description Default <code>html_src</code> <p>Raw HTML containing table data.</p> required <p>Returns:</p> Type Description <p>Result from do_refresh.</p> Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def refresh_from_source(self, html_src):\n    \"\"\"Refresh table from raw HTML source.\n\n    Args:\n        html_src: Raw HTML containing table data.\n\n    Returns:\n        Result from do_refresh.\n    \"\"\"\n    self.refresh_tdata(html_src)\n    tdata = self.get_tdata()\n    return self.do_refresh(tdata)\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.TABLE.update_row_ok","title":"update_row_ok","text":"<pre><code>update_row_ok(data)\n</code></pre> <p>Handle update_row_ok signal.</p> Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def update_row_ok(self, data):\n    \"\"\"Handle update_row_ok signal.\"\"\"\n    return self._row_ok(data, insert=False)\n</code></pre>"},{"location":"api/guictrl_grids/#pytigon_gui.guictrl.grids.UPDATEGRIDBUTTON","title":"UPDATEGRIDBUTTON","text":"<pre><code>UPDATEGRIDBUTTON(parent, **kwds)\n</code></pre> <p>               Bases: <code>Button</code>, <code>SchBaseCtrl</code></p> <p>Button that submits form data to update a parent grid.</p> <p>Handles ctrlupdategridbutton tag. Collects values from all sibling controls and sends them to the parent grid's OnUpRecFromForm method.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/grids.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    wx.Button.__init__(self, parent, **kwds)\n    self.Bind(wx.EVT_BUTTON, self._on_click)\n</code></pre>"},{"location":"api/guictrl_input/","title":"guictrl.input \u2013 Input Controls","text":"<p>Form input controls: text, choice, combo, numeric, and date/time pickers.</p>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input","title":"input","text":"<p>Input widgets for the SchForm GUI framework.</p> <p>Provides text input, numeric, choice/combo, and date/time controls.</p>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input-modules","title":"Modules","text":""},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.choice","title":"choice","text":"<p>Choice/selection widget classes for the SchForm GUI framework.</p> <p>Provides wxPython selection controls integrated with SchBaseCtrl: checkbox, checklistbox, listbox, list control, radiobox, radio button.</p>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.choice-classes","title":"Classes","text":""},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.choice.CHECKBOX","title":"CHECKBOX","text":"<pre><code>CHECKBOX(parent, **kwds)\n</code></pre> <p>               Bases: <code>CheckBox</code>, <code>SchBaseCtrl</code></p> <p>Checkbox control with optional custom value.</p> <p>Handles ctrlcheckbox tag. When the checkbox is checked, GetValue returns either the configured custom value or True. When unchecked, returns None.</p> Tag arguments <p>Initialize the checkbox control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.CheckBox. If 'checked' is present, the checkbox starts checked. If no label is provided, adds ALIGN_RIGHT style.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the checkbox control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.CheckBox. If 'checked' is\n            present, the checkbox starts checked. If no label\n            is provided, adds ALIGN_RIGHT style.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    if \"checked\" in kwds:\n        del kwds[\"checked\"]\n        checked = True\n    else:\n        checked = False\n\n    if self.label:\n        kwds[\"label\"] = self.label\n\n    if \"value\" in kwds:\n        self.value = kwds[\"value\"]\n        del kwds[\"value\"]\n    else:\n        self.value = None\n\n    if not self.label:\n        if \"style\" in kwds:\n            kwds[\"style\"] |= wx.ALIGN_RIGHT\n        else:\n            kwds[\"style\"] = wx.ALIGN_RIGHT\n\n    wx.CheckBox.__init__(self, parent, **kwds)\n    if checked:\n        self.SetValue(True)\n</code></pre> Methods:\u00b6 <code></code> GetValue \u00b6 <pre><code>GetValue()\n</code></pre> <p>Get the current checkbox value.</p> <p>Returns:</p> Type Description <p>If checked and a custom value is set, returns str(value).</p> <p>If checked and no custom value, returns True.</p> <p>If unchecked, returns None.</p> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get the current checkbox value.\n\n    Returns:\n        If checked and a custom value is set, returns str(value).\n        If checked and no custom value, returns True.\n        If unchecked, returns None.\n    \"\"\"\n    checked = wx.CheckBox.GetValue(self)\n    if checked:\n        if self.value:\n            return str(self.value)\n        else:\n            return True\n    else:\n        return None\n</code></pre> <code></code> SetValue \u00b6 <pre><code>SetValue(value)\n</code></pre> <p>Set the checkbox state.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>If bool, sets the check state directly. Otherwise stores as the custom value.</p> required Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def SetValue(self, value):\n    \"\"\"Set the checkbox state.\n\n    Args:\n        value: If bool, sets the check state directly.\n            Otherwise stores as the custom value.\n    \"\"\"\n    if isinstance(value, bool):\n        return wx.CheckBox.SetValue(self, value)\n    else:\n        self.value = value\n</code></pre> <code></code> process_refr_data \u00b6 <pre><code>process_refr_data(**kwds)\n</code></pre> <p>Refresh the checkbox with new data.</p> <p>Parameters:</p> Name Type Description Default <code>**kwds</code> <p>New keyword arguments. Supports 'checked' and 'value' keys.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def process_refr_data(self, **kwds):\n    \"\"\"Refresh the checkbox with new data.\n\n    Args:\n        **kwds: New keyword arguments. Supports 'checked'\n            and 'value' keys.\n    \"\"\"\n    self.init_base(kwds)\n    if \"checked\" in kwds:\n        del kwds[\"checked\"]\n        checked = True\n    else:\n        checked = False\n    if \"value\" in kwds:\n        self.value = kwds[\"value\"]\n        del kwds[\"value\"]\n    else:\n        self.value = None\n\n    if checked:\n        self.SetValue(True)\n</code></pre>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.choice.CHECKLIST","title":"CHECKLIST","text":"<pre><code>CHECKLIST(parent, **kwds)\n</code></pre> <p>               Bases: <code>LIST</code></p> <p>List control with checkboxes for each item.</p> <p>Extends LIST to enable item checkboxes. Items can be toggled by activating them (Enter or double-click).</p> Tag arguments <p>Initialize the checklist control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to LIST. Enables checkboxes and binds item activation for toggling.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the checklist control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to LIST. Enables checkboxes and\n            binds item activation for toggling.\n    \"\"\"\n    super().__init__(parent, **kwds)\n    self.EnableCheckBoxes()\n    self.Bind(wx.EVT_LIST_ITEM_ACTIVATED, self._on_item_activated)\n</code></pre> Methods:\u00b6 <code></code> GetBestSize \u00b6 <pre><code>GetBestSize()\n</code></pre> <p>Return the best size for the control.</p> <p>Returns:</p> Type Description <p>Tuple of (400, 48).</p> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def GetBestSize(self):\n    \"\"\"Return the best size for the control.\n\n    Returns:\n        Tuple of (400, 48).\n    \"\"\"\n    return (400, 48)\n</code></pre> <code></code> ToggleItem \u00b6 <pre><code>ToggleItem(index)\n</code></pre> <p>Toggle the checked state of an item.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <p>Zero-based item index.</p> required Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def ToggleItem(self, index):\n    \"\"\"Toggle the checked state of an item.\n\n    Args:\n        index: Zero-based item index.\n    \"\"\"\n    toggle = not self.IsItemChecked(index)\n    self.CheckItem(index, toggle)\n</code></pre>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.choice.CHECKLISTBOX","title":"CHECKLISTBOX","text":"<pre><code>CHECKLISTBOX(parent, **kwds)\n</code></pre> <p>               Bases: <code>CheckListBox</code>, <code>SchBaseCtrl</code></p> <p>Check list box allowing multiple selections with checkboxes.</p> <p>Handles ctrlchecklistbox tag. Provides a list where each item has an associated checkbox. GetValue returns a list of values for checked items.</p> Tag arguments <p>Initialize the check list box.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.CheckListBox with WANTS_CHARS style.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the check list box.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.CheckListBox with WANTS_CHARS style.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n\n    if \"style\" in kwds:\n        kwds[\"style\"] |= wx.WANTS_CHARS\n    else:\n        kwds[\"style\"] = wx.WANTS_CHARS\n    wx.CheckListBox.__init__(self, parent, **kwds)\n    self._init_data(self.get_tdata())\n</code></pre> Methods:\u00b6 <code></code> GetBestSize \u00b6 <pre><code>GetBestSize()\n</code></pre> <p>Return the best size for the control.</p> <p>Returns:</p> Type Description <p>Tuple of (300, 68).</p> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def GetBestSize(self):\n    \"\"\"Return the best size for the control.\n\n    Returns:\n        Tuple of (300, 68).\n    \"\"\"\n    return (300, 68)\n</code></pre> <code></code> GetMinSize \u00b6 <pre><code>GetMinSize()\n</code></pre> <p>Return the minimum size (same as best size).</p> <p>Returns:</p> Type Description <p>Tuple of (300, 68).</p> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def GetMinSize(self):\n    \"\"\"Return the minimum size (same as best size).\n\n    Returns:\n        Tuple of (300, 68).\n    \"\"\"\n    return self.GetBestSize()\n</code></pre> <code></code> GetValue \u00b6 <pre><code>GetValue()\n</code></pre> <p>Get values of all checked items.</p> <p>Returns:</p> Type Description <p>List of values for checked items. Uses the 'value'</p> <p>attribute from item attrs if present, otherwise the</p> <p>item's display text.</p> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get values of all checked items.\n\n    Returns:\n        List of values for checked items. Uses the 'value'\n        attribute from item attrs if present, otherwise the\n        item's display text.\n    \"\"\"\n    ret = []\n    data = self.get_tdata()\n    items = self.GetCheckedItems()\n    for item in items:\n        if hasattr(data[item][0], \"attrs\") and \"value\" in data[item][0].attrs:\n            ret.append(data[item][0].attrs[\"value\"])\n        else:\n            ret.append(data[item][0].data)\n    return ret\n</code></pre> <code></code> process_refr_data \u00b6 <pre><code>process_refr_data(**kwds)\n</code></pre> <p>Refresh the list with new data.</p> <p>Parameters:</p> Name Type Description Default <code>**kwds</code> <p>New keyword arguments.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def process_refr_data(self, **kwds):\n    \"\"\"Refresh the list with new data.\n\n    Args:\n        **kwds: New keyword arguments.\n    \"\"\"\n    self.init_base(kwds)\n    self.Clear()\n    self._init_data(self.get_tdata())\n</code></pre>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.choice.LIST","title":"LIST","text":"<pre><code>LIST(parent, **kwds)\n</code></pre> <p>               Bases: <code>ListCtrl</code>, <code>SchBaseCtrl</code></p> <p>List control with multiple columns (report view).</p> <p>Handles ctrllist tag. Displays tabular data in report mode with auto-sized columns. First row of tdata provides column headers.</p> Tag arguments <p>Initialize the list control in report mode.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.ListCtrl with LC_REPORT style.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the list control in report mode.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.ListCtrl with LC_REPORT style.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n\n    if \"style\" in kwds:\n        kwds[\"style\"] = kwds[\"style\"] | wx.LC_REPORT\n    else:\n        kwds[\"style\"] = wx.LC_REPORT\n    kwds[\"size\"] = wx.Size(-1, -1)\n\n    wx.ListCtrl.__init__(self, parent, **kwds)\n\n    tdata = self.get_tdata()\n    if tdata:\n        first_row = True\n        num_cols = 0\n        for row in tdata:\n            if first_row:\n                first_row = False\n                num_cols = len(row)\n                for i in range(num_cols):\n                    self.InsertColumn(i, row[i].data)\n            else:\n                index = self.InsertItem(0, row[0].data)\n                for i in range(1, num_cols):\n                    self.SetItem(index, i, row[i].data)\n\n        for i in range(num_cols):\n            self.SetColumnWidth(i, -1)\n</code></pre> Methods:\u00b6 <code></code> process_refr_data \u00b6 <pre><code>process_refr_data(**kwds)\n</code></pre> <p>Refresh the list with new data.</p> <p>Parameters:</p> Name Type Description Default <code>**kwds</code> <p>New keyword arguments.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def process_refr_data(self, **kwds):\n    \"\"\"Refresh the list with new data.\n\n    Args:\n        **kwds: New keyword arguments.\n    \"\"\"\n    self.init_base(kwds)\n    self.ClearAll()\n    tdata = self.get_tdata()\n    if tdata:\n        first_row = True\n        num_cols = 0\n        for row in tdata:\n            if first_row:\n                first_row = False\n                num_cols = len(row)\n                for i in range(num_cols):\n                    self.InsertColumn(i, row[i].data)\n            else:\n                index = self.InsertStringItem(0, row[0].data)\n                for i in range(1, num_cols):\n                    self.SetStringItem(index, i, row[i].data)\n</code></pre>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.choice.LISTBOX","title":"LISTBOX","text":"<pre><code>LISTBOX(parent, **kwds)\n</code></pre> <p>               Bases: <code>ListBox</code>, <code>SchBaseCtrl</code></p> <p>Standard list box for single or multiple selection.</p> <p>Handles ctrllistbox tag. Supports multiple selection mode via the 'multiple' parameter. Tracks selected items and their associated values.</p> Tag arguments <p>Initialize the list box.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.ListBox. Enables LB_MULTIPLE if 'multiple' is in the param dict.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the list box.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.ListBox. Enables LB_MULTIPLE\n            if 'multiple' is in the param dict.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    self._norefresh = False\n\n    if self.param and \"multiple\" in self.param:\n        style = 0\n        if \"style\" in kwds:\n            style = kwds[\"style\"]\n        style = style | wx.LB_MULTIPLE\n        kwds[\"style\"] = style\n\n    wx.ListBox.__init__(self, parent, **kwds)\n\n    self._init_choices(self.get_tdata())\n</code></pre> Methods:\u00b6 <code></code> GetValue \u00b6 <pre><code>GetValue()\n</code></pre> <p>Get values of all selected items.</p> <p>Returns:</p> Type Description <p>List of values corresponding to selected items.</p> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get values of all selected items.\n\n    Returns:\n        List of values corresponding to selected items.\n    \"\"\"\n    ret = []\n    sel = self.GetSelections()\n    for s in sel:\n        ret.append(self.choices[s])\n    return ret\n</code></pre> <code></code> block_refresh \u00b6 <pre><code>block_refresh()\n</code></pre> <p>Prevent automatic refresh on process_refr_data calls.</p> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def block_refresh(self):\n    \"\"\"Prevent automatic refresh on process_refr_data calls.\"\"\"\n    self._norefresh = True\n</code></pre> <code></code> find_and_select \u00b6 <pre><code>find_and_select(item)\n</code></pre> <p>Find the first occurrence of item and select it.</p> <p>Matches items that start with the given string.</p> <p>Parameters:</p> Name Type Description Default <code>item</code> <p>String prefix to search for.</p> required Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def find_and_select(self, item):\n    \"\"\"Find the first occurrence of item and select it.\n\n    Matches items that start with the given string.\n\n    Args:\n        item: String prefix to search for.\n    \"\"\"\n    strings = self.GetStrings()\n    for i, s in enumerate(strings):\n        if s.startswith(item):\n            self.SetSelection(i)\n            return\n</code></pre> <code></code> process_refr_data \u00b6 <pre><code>process_refr_data(**kwds)\n</code></pre> <p>Refresh the list with new data.</p> <p>Parameters:</p> Name Type Description Default <code>**kwds</code> <p>New keyword arguments. Skips refresh if _norefresh is True.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def process_refr_data(self, **kwds):\n    \"\"\"Refresh the list with new data.\n\n    Args:\n        **kwds: New keyword arguments. Skips refresh if\n            _norefresh is True.\n    \"\"\"\n    self.init_base(kwds)\n    if not self._norefresh:\n        self.Clear()\n        self.refresh_tdata()\n        self._init_choices(self.get_tdata())\n</code></pre>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.choice.RADIOBOX","title":"RADIOBOX","text":"<pre><code>RADIOBOX(parent, **kwds)\n</code></pre> <p>               Bases: <code>RadioBox</code>, <code>SchBaseCtrl</code></p> <p>Radio box with a group of mutually exclusive choices.</p> <p>Handles ctrlradiobox tag. Choices are populated from tdata.</p> Tag arguments <p>Initialize the radio box.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.RadioBox with 'choices' populated from tdata.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the radio box.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.RadioBox with 'choices'\n            populated from tdata.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    tdata = self.get_tdata()\n    if tdata:\n        choices = []\n        for row in tdata:\n            choices.append(row[0].data)\n        kwds[\"choices\"] = choices\n    wx.RadioBox.__init__(self, parent, **kwds)\n</code></pre> Methods:\u00b6 <code></code> process_refr_data \u00b6 <pre><code>process_refr_data(**kwds)\n</code></pre> <p>Refresh the radio box with new choices.</p> <p>Parameters:</p> Name Type Description Default <code>**kwds</code> <p>New keyword arguments.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def process_refr_data(self, **kwds):\n    \"\"\"Refresh the radio box with new choices.\n\n    Args:\n        **kwds: New keyword arguments.\n    \"\"\"\n    self.init_base(kwds)\n    self.Clear()\n    self.refresh_tdata()\n    tdata = self.get_tdata()\n    if tdata:\n        for row in tdata:\n            self.Append(row[0].data)\n</code></pre>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.choice.RADIOBUTTON","title":"RADIOBUTTON","text":"<pre><code>RADIOBUTTON(parent, **kwds)\n</code></pre> <p>               Bases: <code>RadioButton</code>, <code>SchBaseCtrl</code></p> <p>Individual radio button for use in radio groups.</p> <p>Handles ctrlradiobutton tag. The first button with a given name prefix automatically gets the RB_GROUP style to start a new radio group.</p> Tag arguments <p>Initialize the radio button.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.RadioButton. Adds RB_GROUP style if this is the first button with this name prefix (before '__').</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the radio button.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.RadioButton. Adds RB_GROUP\n            style if this is the first button with this name\n            prefix (before '__').\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    if \"name\" in kwds:\n        name = kwds[\"name\"]\n        # If no sibling with same name group exists, make this the group start\n        if not hasattr(parent, name.split(\"__\")[0]):\n            if \"style\" in kwds:\n                kwds[\"style\"] |= wx.RB_GROUP\n            else:\n                kwds[\"style\"] = wx.RB_GROUP\n\n    if \"checked\" in kwds:\n        del kwds[\"checked\"]\n        checked = True\n    else:\n        checked = False\n    if \"value\" in kwds:\n        self.value = kwds[\"value\"]\n        del kwds[\"value\"]\n    else:\n        self.value = None\n\n    wx.RadioButton.__init__(self, parent, **kwds)\n    if checked:\n        self.SetValue(True)\n</code></pre> Methods:\u00b6 <code></code> GetValue \u00b6 <pre><code>GetValue()\n</code></pre> <p>Get the radio button value.</p> <p>Returns:</p> Type Description <p>If selected and custom value is set, returns str(value).</p> <p>If selected without custom value, returns None.</p> <p>If not selected, returns None.</p> Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get the radio button value.\n\n    Returns:\n        If selected and custom value is set, returns str(value).\n        If selected without custom value, returns None.\n        If not selected, returns None.\n    \"\"\"\n    selected = wx.RadioButton.GetValue(self)\n    if selected:\n        if self.value:\n            return str(self.value)\n        else:\n            return None\n    else:\n        return None\n</code></pre> <code></code> SetValue \u00b6 <pre><code>SetValue(value)\n</code></pre> <p>Set the radio button state.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>If bool, sets the selection state directly. Otherwise stores as the custom value.</p> required Source code in <code>pytigon_gui/guictrl/input/choice.py</code> <pre><code>def SetValue(self, value):\n    \"\"\"Set the radio button state.\n\n    Args:\n        value: If bool, sets the selection state directly.\n            Otherwise stores as the custom value.\n    \"\"\"\n    if isinstance(value, bool):\n        return wx.RadioButton.SetValue(self, value)\n    else:\n        self.value = value\n</code></pre>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.choice-functions","title":"Functions:","text":""},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.combo","title":"combo","text":"<p>Combo box and choice widget classes for the SchForm GUI framework.</p> <p>Provides wxPython combo/choice controls integrated with SchBaseCtrl: bitmap combo box, choice (popup-based), database choice, and extended database choice.</p>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.combo-classes","title":"Classes","text":""},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.combo.BITMAPCOMBOBOX","title":"BITMAPCOMBOBOX","text":"<pre><code>BITMAPCOMBOBOX(parent, **kwds)\n</code></pre> <p>               Bases: <code>BitmapComboBox</code>, <code>SchBaseCtrl</code></p> <p>Bitmap combo box with icon support.</p> <p>Handles ctrlbitmapcombobox tag. Displays a dropdown with optional icons loaded from filesystem paths. Supports embedded wxArt icons, PNG icons from static files, and Fork Awesome font icons.</p> Tag arguments <p>Initialize the bitmap combo box.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to BitmapComboBox.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the bitmap combo box.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to BitmapComboBox.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    BitmapComboBox.__init__(self, parent, **kwds)\n    self.init_default_icons = False\n    self._first_use = True\n    self.accept_focus = False\n</code></pre> Methods:\u00b6 <code></code> GetValue \u00b6 <pre><code>GetValue()\n</code></pre> <p>Get the current combo box value.</p> <p>Returns:</p> Type Description <p>The current value string, or empty string if None.</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get the current combo box value.\n\n    Returns:\n        The current value string, or empty string if None.\n    \"\"\"\n    ret = super().GetValue()\n    if ret:\n        return ret\n    else:\n        return \"\"\n</code></pre> <code></code> SetValue \u00b6 <pre><code>SetValue(value)\n</code></pre> <p>Set the combo box value.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>Value to select. Empty/None sets empty string.</p> required Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def SetValue(self, value):\n    \"\"\"Set the combo box value.\n\n    Args:\n        value: Value to select. Empty/None sets empty string.\n    \"\"\"\n    if value:\n        ret = super().SetValue(value)\n    else:\n        ret = super().SetValue(\"\")\n    return ret\n</code></pre> <code></code> after_create \u00b6 <pre><code>after_create()\n</code></pre> <p>Load icons after the control is fully created.</p> <p>Uses wx.CallAfter to defer icon loading until the event loop is idle, ensuring the control is properly sized and ready.</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def after_create(self):\n    \"\"\"Load icons after the control is fully created.\n\n    Uses wx.CallAfter to defer icon loading until the\n    event loop is idle, ensuring the control is properly\n    sized and ready.\n    \"\"\"\n    super().after_create()\n    if self.init_default_icons:\n\n        def _load_icons():\n            self.init_embeded_icons()\n            self.init_fa_icons()\n\n        wx.CallAfter(_load_icons)\n</code></pre> <code></code> init_embeded_icons \u00b6 <pre><code>init_embeded_icons()\n</code></pre> <p>Load embedded PNG icons from the static/icons/22x22 directory.</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def init_embeded_icons(self):\n    \"\"\"Load embedded PNG icons from the static/icons/22x22 directory.\"\"\"\n    base_path = str(wx.GetApp().src_path) + \"/static/icons/22x22/\"\n    return self._init_icons(base_path, \"png://\")\n</code></pre> <code></code> init_extern_icons \u00b6 <pre><code>init_extern_icons(base_path, prefix)\n</code></pre> <p>Load icons from an external directory.</p> <p>Parameters:</p> Name Type Description Default <code>base_path</code> <p>Filesystem path to the icon directory.</p> required <code>prefix</code> <p>URL-style prefix for icon identifiers (e.g. 'png://').</p> required Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def init_extern_icons(self, base_path, prefix):\n    \"\"\"Load icons from an external directory.\n\n    Args:\n        base_path: Filesystem path to the icon directory.\n        prefix: URL-style prefix for icon identifiers (e.g. 'png://').\n    \"\"\"\n    return self._init_icons(base_path, prefix)\n</code></pre> <code></code> init_fa_icons \u00b6 <pre><code>init_fa_icons()\n</code></pre> <p>Load Fork Awesome font icons from the static/fonts directory.</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def init_fa_icons(self):\n    \"\"\"Load Fork Awesome font icons from the static/fonts directory.\"\"\"\n    base_path = (\n        str(wx.GetApp().src_path) + \"/static/fonts/fork-awesome/fonts/22x22/\"\n    )\n    return self._init_icons(base_path, \"fa://\")\n</code></pre> <code></code> init_wx_icons \u00b6 <pre><code>init_wx_icons()\n</code></pre> <p>Load standard wxArtProvider icons into the combo box.</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def init_wx_icons(self):\n    \"\"\"Load standard wxArtProvider icons into the combo box.\"\"\"\n    for art_id in sorted([pos for pos in dir(wx) if pos.startswith(\"ART_\")]):\n        artid = getattr(wx, art_id)\n        bmp = wx.ArtProvider.GetBitmap(artid, wx.ART_TOOLBAR, (22, 22))\n        if bmp.IsOk():\n            self.Append(art_id, bmp, art_id)\n</code></pre>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.combo.CHOICE","title":"CHOICE","text":"<pre><code>CHOICE(parent, **kwds)\n</code></pre> <p>               Bases: <code>POPUPHTML</code></p> <p>Choice control with popup-based selection dialog.</p> <p>Handles ctrlchoice tag. Extends POPUPHTML to provide a dropdown choice with optional extended popup dialog. Supports keyboard accelerator (F2) for the extended dialog.</p> Tag arguments <p>Initialize the choice control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to POPUPHTML. Sets default href to the list dialog if not provided.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the choice control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to POPUPHTML. Sets default href\n            to the list dialog if not provided.\n    \"\"\"\n    if \"href\" not in kwds:\n        kwds[\"href\"] = wx.GetApp().make_href(\"/schsys/listdialog/\")\n\n    if \"style\" in kwds:\n        kwds[\"style\"] = kwds[\"style\"] | wx.WANTS_CHARS\n    else:\n        kwds[\"style\"] = wx.WANTS_CHARS\n\n    if \"size\" not in kwds:\n        kwds[\"size\"] = wx.Size(250, -1)\n    self.height = 250\n    kwds[\"dialog_with_value\"] = False\n    POPUPHTML.__init__(self, parent, **kwds)\n\n    self._init_choices(self.get_tdata())\n\n    accel_table = [\n        (0, wx.WXK_F2, self._on_ext_button_click),\n    ]\n    self.set_acc_key_tab(accel_table)\n</code></pre> Methods:\u00b6 <code></code> GetBestSize \u00b6 <pre><code>GetBestSize()\n</code></pre> <p>Return the best size, delegating to POPUPHTML.</p> <p>Returns:</p> Type Description <p>Tuple of (width, height).</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def GetBestSize(self):\n    \"\"\"Return the best size, delegating to POPUPHTML.\n\n    Returns:\n        Tuple of (width, height).\n    \"\"\"\n    dx, dy = POPUPHTML.GetBestSize(self)\n    return (dx, dy)\n</code></pre> <code></code> GetMaxSize \u00b6 <pre><code>GetMaxSize()\n</code></pre> <p>Return the maximum size (same as best size).</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def GetMaxSize(self):\n    \"\"\"Return the maximum size (same as best size).\"\"\"\n    return self.GetBestSize()\n</code></pre> <code></code> GetMinSize \u00b6 <pre><code>GetMinSize()\n</code></pre> <p>Return the minimum size (same as best size).</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def GetMinSize(self):\n    \"\"\"Return the minimum size (same as best size).\"\"\"\n    return self.GetBestSize()\n</code></pre> <code></code> GetValue \u00b6 <pre><code>GetValue()\n</code></pre> <p>Get the current choice value.</p> <p>When readonly, returns the value from the stored record. Otherwise delegates to POPUPHTML.</p> <p>Returns:</p> Type Description <p>The selected value or None.</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get the current choice value.\n\n    When readonly, returns the value from the stored record.\n    Otherwise delegates to POPUPHTML.\n\n    Returns:\n        The selected value or None.\n    \"\"\"\n    if self.readonly:\n        value = self.get_rec()\n        return value[0]\n    else:\n        return POPUPHTML.GetValue(self)\n</code></pre> <code></code> OnButtonClick \u00b6 <pre><code>OnButtonClick()\n</code></pre> <p>Handle the dropdown button click.</p> <p>Opens the popup dialog and populates it with current choices.</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def OnButtonClick(self):\n    \"\"\"Handle the dropdown button click.\n\n    Opens the popup dialog and populates it with current choices.\n    \"\"\"\n    if self.simpleDialog:\n        ret = POPUPHTML.OnButtonClick(self)\n        self.popup.html.body.choices = self.choices\n        wx.CallAfter(self.popup.html.body.refr)\n    else:\n        ret = POPUPHTML.OnButtonClick(self)\n        self.page.body.choices = self.choices\n        wx.CallAfter(self.page.body.refr)\n    return ret\n</code></pre>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.combo.DBCHOICE","title":"DBCHOICE","text":"<pre><code>DBCHOICE(parent, **kwds)\n</code></pre> <p>               Bases: <code>CHOICE</code></p> <p>Database-bound choice control.</p> <p>Handles ctrldbchoice tag. Extends CHOICE to return the 'value' attribute from the selected item's attrs when in readonly mode, making it suitable for database key selection.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the choice control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to POPUPHTML. Sets default href\n            to the list dialog if not provided.\n    \"\"\"\n    if \"href\" not in kwds:\n        kwds[\"href\"] = wx.GetApp().make_href(\"/schsys/listdialog/\")\n\n    if \"style\" in kwds:\n        kwds[\"style\"] = kwds[\"style\"] | wx.WANTS_CHARS\n    else:\n        kwds[\"style\"] = wx.WANTS_CHARS\n\n    if \"size\" not in kwds:\n        kwds[\"size\"] = wx.Size(250, -1)\n    self.height = 250\n    kwds[\"dialog_with_value\"] = False\n    POPUPHTML.__init__(self, parent, **kwds)\n\n    self._init_choices(self.get_tdata())\n\n    accel_table = [\n        (0, wx.WXK_F2, self._on_ext_button_click),\n    ]\n    self.set_acc_key_tab(accel_table)\n</code></pre> Methods:\u00b6 <code></code> GetValue \u00b6 <pre><code>GetValue()\n</code></pre> <p>Get the database value of the selected item.</p> <p>When readonly, returns the 'value' attribute from the selected item's attrs dict (typically a database key). Otherwise delegates to POPUPHTML.</p> <p>Returns:</p> Type Description <p>The database value string or None.</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get the database value of the selected item.\n\n    When readonly, returns the 'value' attribute from the\n    selected item's attrs dict (typically a database key).\n    Otherwise delegates to POPUPHTML.\n\n    Returns:\n        The database value string or None.\n    \"\"\"\n    if self.readonly:\n        value = self.get_rec()\n        if value:\n            if hasattr(value, \"attrs\") and \"value\" in value.attrs:\n                return value.attrs[\"value\"]\n            else:\n                return value.data\n        else:\n            return None\n    else:\n        return POPUPHTML.GetValue(self)\n</code></pre>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.combo.DBCHOICE_EXT","title":"DBCHOICE_EXT","text":"<pre><code>DBCHOICE_EXT(parent, **kwds)\n</code></pre> <p>               Bases: <code>POPUPHTML</code></p> <p>Extended database choice control with read-only style.</p> <p>Handles ctrldbchoice_ext tag. Similar to DBCHOICE but always uses CB_READONLY style. Supports setting values from dicts with 'value', 'title', 'selected' keys, or from '!!'-delimited strings.</p> Tag arguments <p>Initialize the extended database choice.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to POPUPHTML with CB_READONLY style.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the extended database choice.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to POPUPHTML with CB_READONLY style.\n    \"\"\"\n    if \"style\" in kwds:\n        kwds[\"style\"] = kwds[\"style\"] | wx.CB_READONLY\n    else:\n        kwds[\"style\"] = wx.CB_READONLY\n\n    POPUPHTML.__init__(self, parent, **kwds)\n</code></pre> Methods:\u00b6 <code></code> GetValue \u00b6 <pre><code>GetValue()\n</code></pre> <p>Get the current choice value.</p> <p>When readonly, extracts the stored record value. Otherwise delegates to POPUPHTML.</p> <p>Returns:</p> Type Description <p>The selected value or None.</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get the current choice value.\n\n    When readonly, extracts the stored record value.\n    Otherwise delegates to POPUPHTML.\n\n    Returns:\n        The selected value or None.\n    \"\"\"\n    if self.readonly:\n        value = self.get_rec()\n        if isinstance(value, tuple) and len(value) &gt; 0:\n            return value[1][0]\n        if len(value) &gt; 0:\n            return value[0]\n        else:\n            return None\n    else:\n        return POPUPHTML.GetValue(self)\n</code></pre> <code></code> SetValue \u00b6 <pre><code>SetValue(value)\n</code></pre> <p>Set the choice value.</p> <p>Supports multiple input formats: - dict with 'value', 'title', 'selected' keys - '!!'-delimited string: 'id:name!!' format</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>The value to set, in one of the supported formats.</p> required Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def SetValue(self, value):\n    \"\"\"Set the choice value.\n\n    Supports multiple input formats:\n    - dict with 'value', 'title', 'selected' keys\n    - '!!'-delimited string: 'id:name!!' format\n\n    Args:\n        value: The value to set, in one of the supported formats.\n    \"\"\"\n    if isinstance(value, dict):\n        val = value[\"value\"]\n        title = value[\"title\"]\n        sel = value[\"selected\"]\n        if sel:\n            self.set_rec(val, Td(title, {\"value\": val}), dismiss=False)\n    else:\n        if \"!!\" in value:\n            id_part = value.split(\":\")[0]\n            name = value[len(id_part) + 1 :].replace(\"!!\", \"\")\n            self.set_rec(name, [id_part, name], dismiss=False)\n\n    POPUPHTML.SetValue(self, value)\n</code></pre> <code></code> get_parm \u00b6 <pre><code>get_parm(parm)\n</code></pre> <p>Get a parameter from the stored record value.</p> <p>Used for URL parameter encoding (base64).</p> <p>Parameters:</p> Name Type Description Default <code>parm</code> <p>Parameter name ('value' supported).</p> required <p>Returns:</p> Type Description <p>Base64-encoded value or None.</p> Source code in <code>pytigon_gui/guictrl/input/combo.py</code> <pre><code>def get_parm(self, parm):\n    \"\"\"Get a parameter from the stored record value.\n\n    Used for URL parameter encoding (base64).\n\n    Args:\n        parm: Parameter name ('value' supported).\n\n    Returns:\n        Base64-encoded value or None.\n    \"\"\"\n    if len(self.rec_value) &gt; 0:\n        id_val = self.rec_value[0]\n        if parm == \"value\":\n            return b64encode(str(id_val).encode(\"utf-8\"))\n        return None\n    else:\n        return None\n</code></pre>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.datetime","title":"datetime","text":"<p>Date and time widget classes for the SchForm GUI framework.</p> <p>Provides wxPython date/time controls integrated with SchBaseCtrl: calendar, date picker (platform-specific), date-time picker, and time control.</p>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.datetime-classes","title":"Classes","text":""},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.datetime.CALENDAR","title":"CALENDAR","text":"<pre><code>CALENDAR(parent, **kwds)\n</code></pre> <p>               Bases: <code>CalendarCtrl</code>, <code>SchBaseCtrl</code></p> <p>Calendar control for date selection.</p> <p>Handles ctrlcalendar tag. Displays a monthly calendar grid with Monday as the first day of the week, holiday highlighting, and sequential month selection.</p> Tag arguments <p>Initialize the calendar control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to CalendarCtrl with platform-appropriate style flags (wx.adv vs wx.calendar namespace).</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/datetime.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the calendar control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to CalendarCtrl with platform-appropriate\n            style flags (wx.adv vs wx.calendar namespace).\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    try:\n        CalendarCtrl.__init__(\n            self,\n            parent,\n            style=wx.adv.CAL_MONDAY_FIRST\n            | wx.adv.CAL_SHOW_HOLIDAYS\n            | wx.adv.CAL_SEQUENTIAL_MONTH_SELECTION,\n            **kwds,\n        )\n    except AttributeError:\n        CalendarCtrl.__init__(\n            self,\n            parent,\n            style=wx.calendar.CAL_MONDAY_FIRST\n            | wx.calendar.CAL_SHOW_HOLIDAYS\n            | wx.calendar.CAL_SEQUENTIAL_MONTH_SELECTION,\n            **kwds,\n        )\n</code></pre> Methods:\u00b6"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.datetime.DATEPICKER","title":"DATEPICKER","text":"<pre><code>DATEPICKER(parent, **kwds)\n</code></pre> <p>               Bases: <code>DatePickerCtrl</code>, <code>SchBaseCtrl</code></p> <p>Date picker control (non-Linux: native).</p> <p>Uses the platform's native date picker control with dropdown calendar, century display, and allow-none support.</p> Tag arguments <p>Initialize the native date picker.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to DatePickerCtrl with dropdown, show-century, and allow-none styles.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/datetime.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the native date picker.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to DatePickerCtrl with dropdown,\n            show-century, and allow-none styles.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    kwds[\"size\"] = (120, -1)\n    try:\n        kwds[\"style\"] = (\n            wx.adv.DP_DROPDOWN | wx.adv.DP_SHOWCENTURY | wx.adv.DP_ALLOWNONE\n        )\n    except AttributeError:\n        kwds[\"style\"] = wx.DP_DROPDOWN | wx.DP_SHOWCENTURY | wx.DP_ALLOWNONE\n    wx.adv.DatePickerCtrl.__init__(self, parent, **kwds)\n</code></pre> Methods:\u00b6 <code></code> GetValue \u00b6 <pre><code>GetValue()\n</code></pre> <p>Get the current date in ISO format.</p> <p>Returns:</p> Type Description <p>Date string in YYYY-MM-DD format, or None.</p> Source code in <code>pytigon_gui/guictrl/input/datetime.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get the current date in ISO format.\n\n    Returns:\n        Date string in YYYY-MM-DD format, or None.\n    \"\"\"\n    value = wx.adv.DatePickerCtrl.GetValue(self)\n    if value:\n        return value.FormatISODate()\n    else:\n        return None\n</code></pre> <code></code> SetValue \u00b6 <pre><code>SetValue(value)\n</code></pre> <p>Set the date value from a string.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>Date string in parseable format, or None to clear the selection.</p> required Source code in <code>pytigon_gui/guictrl/input/datetime.py</code> <pre><code>def SetValue(self, value):\n    \"\"\"Set the date value from a string.\n\n    Args:\n        value: Date string in parseable format, or None\n            to clear the selection.\n    \"\"\"\n    if value:\n        date = wx.DateTime()\n        date.ParseDate(value)\n        wx.adv.DatePickerCtrl.SetValue(self, date)\n    else:\n        wx.adv.DatePickerCtrl.SetValue(self, None)\n</code></pre>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.datetime.DATETIMEPICKER","title":"DATETIMEPICKER","text":"<pre><code>DATETIMEPICKER(parent, **kwds)\n</code></pre> <p>               Bases: <code>SchBaseCtrl</code></p> <p>Date-time picker control with popup dialog.</p> <p>Handles ctrldatetimepicker tag. Provides combined date and time selection with a masked input in 'YYYY-MM-DD HH:MM' format.</p> Tag arguments <p>Initialize the date-time picker.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to the underlying POPUPHTML control.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/datetime.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the date-time picker.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to the underlying POPUPHTML control.\n    \"\"\"\n    from pytigon_gui.guictrl.display import POPUPHTML\n\n    kwds[\"href\"] = wx.GetApp().make_href(\"/schsys/datedialog/\")\n\n    if \"style\" in kwds:\n        kwds[\"style\"] = kwds[\"style\"] | wx.WANTS_CHARS\n    else:\n        kwds[\"style\"] = wx.WANTS_CHARS\n\n    if \"size\" in kwds:\n        kwds[\"size\"] = wx.Size(200, kwds[\"size\"].height)\n    else:\n        kwds[\"size\"] = wx.Size(200, -1)\n\n    SchBaseCtrl.__init__(self, parent, kwds)\n    POPUPHTML.__init__(self, parent, **kwds)\n\n    self.to_masked(\n        mask=\"####-##-## ##:##\",\n        formatcodes=\"F\",\n        validRegex=r\"\\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}\",\n    )\n\n    if self.value:\n        self.set_rec(self.value, Td(self.value))\n    else:\n        now = datetime.datetime.now().isoformat().replace(\"T\", \" \")[:16]\n        self.set_rec(now, Td(now), False)\n</code></pre> Methods:\u00b6 <code></code> GetBestSize \u00b6 <pre><code>GetBestSize()\n</code></pre> <p>Return the best size for the date-time picker.</p> <p>Returns:</p> Type Description <p>Tuple of (180, height).</p> Source code in <code>pytigon_gui/guictrl/input/datetime.py</code> <pre><code>def GetBestSize(self):\n    \"\"\"Return the best size for the date-time picker.\n\n    Returns:\n        Tuple of (180, height).\n    \"\"\"\n    from pytigon_gui.guictrl.display import POPUPHTML\n\n    dx, dy = POPUPHTML.GetBestSize(self)\n    return (180, dy)\n</code></pre> <code></code> GetValue \u00b6 <pre><code>GetValue()\n</code></pre> <p>Get the current date-time value.</p> <p>Returns:</p> Type Description <p>Date-time string from the record, or parsed from</p> <p>the text control if the record is empty.</p> Source code in <code>pytigon_gui/guictrl/input/datetime.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get the current date-time value.\n\n    Returns:\n        Date-time string from the record, or parsed from\n        the text control if the record is empty.\n    \"\"\"\n    from pytigon_gui.guictrl.display import POPUPHTML\n\n    value = self.get_rec()\n    value2 = self.GetTextCtrl().GetValue()\n    if value2 and value2[0] != \" \":\n        if value.data:\n            return value.data\n        else:\n            return [\n                datetime.datetime.strptime(value2, \"%Y-%m-%d %H:%M\"),\n            ]\n    return None\n</code></pre> <code></code> set_rec \u00b6 <pre><code>set_rec(value, value_rec, dismiss=False)\n</code></pre> <p>Set the record value, padding time if needed.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>Date-time string.</p> required <code>value_rec</code> <p>Td record object.</p> required <code>dismiss</code> <p>Whether to dismiss the popup.</p> <code>False</code> <p>Returns:</p> Type Description <p>Result from parent set_rec.</p> Source code in <code>pytigon_gui/guictrl/input/datetime.py</code> <pre><code>def set_rec(self, value, value_rec, dismiss=False):\n    \"\"\"Set the record value, padding time if needed.\n\n    Args:\n        value: Date-time string.\n        value_rec: Td record object.\n        dismiss: Whether to dismiss the popup.\n\n    Returns:\n        Result from parent set_rec.\n    \"\"\"\n    from pytigon_gui.guictrl.display import POPUPHTML\n\n    if len(value) == 16:\n        return POPUPHTML.set_rec(self, value, value_rec, dismiss)\n    elif len(value) == 10:\n        value_rec.data = value_rec.data + \" 00:00\"\n        return POPUPHTML.set_rec(self, value + \" 00:00\", value_rec, dismiss)\n    else:\n        return POPUPHTML.set_rec(self, \"0000.00.00 00:00\", value_rec, dismiss)\n</code></pre>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.datetime.TIME","title":"TIME","text":"<pre><code>TIME(parent, **kwds)\n</code></pre> <p>               Bases: <code>TimeCtrl</code>, <code>SchBaseCtrl</code></p> <p>Time input control with mask validation.</p> <p>Handles ctrltime tag. Uses wx.lib.masked.TimeCtrl for formatted time entry with validation.</p> Tag arguments <p>Initialize the time control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to masked.TimeCtrl.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/datetime.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the time control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to masked.TimeCtrl.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    masked.TimeCtrl.__init__(self, parent, **kwds)\n</code></pre> Methods:\u00b6"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.numeric","title":"numeric","text":"<p>Numeric input widget classes for the SchForm GUI framework.</p> <p>Provides wxPython numeric entry controls integrated with SchBaseCtrl: integer spin, float spin, currency amount, slider, gauge (progress bar), and ticker (scrolling text).</p>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.numeric-classes","title":"Classes","text":""},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.numeric.AMOUNT","title":"AMOUNT","text":"<pre><code>AMOUNT(parent, **kwds)\n</code></pre> <p>               Bases: <code>SpinCtrlDouble</code>, <code>SchBaseCtrl</code></p> <p>Currency/amount spin control with double precision (2 digits).</p> <p>Handles ctrlamount tag. Supports min/max bounds from param, process_enter, readonly mode, and arrow key navigation.</p> Tag arguments <p>Initialize the amount spin control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.SpinCtrlDouble with increment of 1, 2 decimal digits, and configurable bounds.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/numeric.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the amount spin control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.SpinCtrlDouble with increment\n            of 1, 2 decimal digits, and configurable bounds.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    if self.param and \"process_enter\" in self.param:\n        kwds[\"style\"] = wx.TE_PROCESS_ENTER\n    if self.readonly:\n        style = 0\n        if \"style\" in kwds:\n            style = kwds[\"style\"]\n        style = style | wx.TE_READONLY\n        kwds[\"style\"] = style\n\n    kwds[\"inc\"] = 1\n\n    if self.param and \"min\" in self.param:\n        kwds[\"min\"] = float(self.param[\"min\"])\n    else:\n        kwds[\"min\"] = -100000000\n    if self.param and \"max\" in self.param:\n        kwds[\"max\"] = float(self.param[\"max\"])\n    else:\n        kwds[\"max\"] = 100000000\n\n    kwds[\"style\"] = wx.SP_ARROW_KEYS | wx.ALIGN_RIGHT\n\n    wx.SpinCtrlDouble.__init__(self, parent, **kwds)\n    self.SetDigits(2)\n</code></pre> Methods:\u00b6"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.numeric.FLOAT","title":"FLOAT","text":"<pre><code>FLOAT(parent, **kwds)\n</code></pre> <p>               Bases: <code>SpinCtrlDouble</code>, <code>SchBaseCtrl</code></p> <p>Floating-point spin control with high precision (6 digits).</p> <p>Handles ctrlfloat tag. Similar to AMOUNT but with 6 decimal digits for scientific/general float input.</p> Tag arguments <p>Initialize the float spin control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.SpinCtrlDouble with increment of 1, 6 decimal digits, and configurable bounds.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/numeric.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the float spin control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.SpinCtrlDouble with increment\n            of 1, 6 decimal digits, and configurable bounds.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    if self.param and \"process_enter\" in self.param:\n        kwds[\"style\"] = wx.TE_PROCESS_ENTER\n    if self.readonly:\n        style = 0\n        if \"style\" in kwds:\n            style = kwds[\"style\"]\n        style = style | wx.TE_READONLY\n        kwds[\"style\"] = style\n\n    kwds[\"inc\"] = 1\n\n    if self.param and \"min\" in self.param:\n        kwds[\"min\"] = float(self.param[\"min\"])\n    else:\n        kwds[\"min\"] = -100000000\n    if self.param and \"max\" in self.param:\n        kwds[\"max\"] = float(self.param[\"max\"])\n    else:\n        kwds[\"max\"] = 100000000\n\n    kwds[\"style\"] = wx.SP_ARROW_KEYS | wx.ALIGN_RIGHT\n\n    wx.SpinCtrlDouble.__init__(self, parent, **kwds)\n    self.SetDigits(6)\n</code></pre> Methods:\u00b6"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.numeric.GAUGE","title":"GAUGE","text":"<pre><code>GAUGE(parent, **kwds)\n</code></pre> <p>               Bases: <code>Gauge</code>, <code>SchBaseCtrl</code></p> <p>Progress gauge (progress bar) control.</p> <p>Handles ctrlgauge tag. Displays progress as a horizontal bar. Value can be set via process_refr_data.</p> Tag arguments <p>Initialize the gauge control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.Gauge.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/numeric.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the gauge control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.Gauge.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    wx.Gauge.__init__(self, parent, **kwds)\n</code></pre> Methods:\u00b6 <code></code> process_refr_data \u00b6 <pre><code>process_refr_data(**kwds)\n</code></pre> <p>Refresh the gauge with new data.</p> <p>Parameters:</p> Name Type Description Default <code>**kwds</code> <p>New keyword arguments. If 'value' is present in the underlying self.value, updates the gauge.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/numeric.py</code> <pre><code>def process_refr_data(self, **kwds):\n    \"\"\"Refresh the gauge with new data.\n\n    Args:\n        **kwds: New keyword arguments. If 'value' is present\n            in the underlying self.value, updates the gauge.\n    \"\"\"\n    self.init_base(kwds)\n    if self.value:\n        self.SetValue(self.value)\n</code></pre>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.numeric.NUM","title":"NUM","text":"<pre><code>NUM(parent, **kwds)\n</code></pre> <p>               Bases: <code>SpinCtrl</code>, <code>SchBaseCtrl</code></p> <p>Numeric spin control with optional readonly and process_enter.</p> <p>Handles ctrlnum tag. Similar to SPIN but supports process_enter key handling and readonly mode via TE_READONLY style.</p> Tag arguments <p>Initialize the numeric spin control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.SpinCtrl with optional TE_PROCESS_ENTER and TE_READONLY styles.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/numeric.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the numeric spin control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.SpinCtrl with optional\n            TE_PROCESS_ENTER and TE_READONLY styles.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    if self.param and \"process_enter\" in self.param:\n        kwds[\"style\"] = wx.TE_PROCESS_ENTER\n    if self.readonly:\n        style = 0\n        if \"style\" in kwds:\n            style = kwds[\"style\"]\n        style = style | wx.TE_READONLY\n        kwds[\"style\"] = style\n    wx.SpinCtrl.__init__(self, parent, **kwds)\n</code></pre> Methods:\u00b6"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.numeric.SLIDER","title":"SLIDER","text":"<pre><code>SLIDER(parent, **kwds)\n</code></pre> <p>               Bases: <code>Slider</code>, <code>SchBaseCtrl</code></p> <p>Slider control for selecting a value from a range.</p> <p>Handles ctrlslider tag. Thin wrapper around wx.Slider.</p> Tag arguments <p>Initialize the slider control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.Slider.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/numeric.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the slider control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.Slider.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    wx.Slider.__init__(self, parent, **kwds)\n</code></pre> Methods:\u00b6"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.numeric.SPIN","title":"SPIN","text":"<pre><code>SPIN(parent, **kwds)\n</code></pre> <p>               Bases: <code>SpinCtrl</code>, <code>SchBaseCtrl</code></p> <p>Integer spin control.</p> <p>Handles ctrlspinbutton tag. Wraps wx.SpinCtrl for integer value selection with up/down arrows.</p> Tag arguments <p>Initialize the integer spin control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.SpinCtrl.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/numeric.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the integer spin control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.SpinCtrl.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    wx.SpinCtrl.__init__(self, parent, **kwds)\n</code></pre> Methods:\u00b6"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.numeric.TICKER","title":"TICKER","text":"<pre><code>TICKER(parent, **kwds)\n</code></pre> <p>               Bases: <code>Ticker</code>, <code>SchBaseCtrl</code></p> <p>Scrolling ticker text control.</p> <p>Handles ctrlticker tag. Displays scrolling text. Stops the ticker when the control is closed.</p> Tag arguments <p>Initialize the ticker control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.lib.ticker.Ticker.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/numeric.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the ticker control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.lib.ticker.Ticker.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    wx_ticker.Ticker.__init__(self, parent, **kwds)\n</code></pre> Methods:\u00b6 <code></code> CanClose \u00b6 <pre><code>CanClose()\n</code></pre> <p>Stop the ticker before closing.</p> <p>Returns:</p> Type Description <p>True always, after stopping the ticker animation.</p> Source code in <code>pytigon_gui/guictrl/input/numeric.py</code> <pre><code>def CanClose(self):\n    \"\"\"Stop the ticker before closing.\n\n    Returns:\n        True always, after stopping the ticker animation.\n    \"\"\"\n    self.Stop()\n    return True\n</code></pre> <code></code> SetValue \u00b6 <pre><code>SetValue(value)\n</code></pre> <p>Set the scrolling text value.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>Text string to display.</p> required Source code in <code>pytigon_gui/guictrl/input/numeric.py</code> <pre><code>def SetValue(self, value):\n    \"\"\"Set the scrolling text value.\n\n    Args:\n        value: Text string to display.\n    \"\"\"\n    self.SetText(value)\n</code></pre>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.text","title":"text","text":"<p>Text input widget classes for the SchForm GUI framework.</p> <p>Provides wxPython text entry controls integrated with SchBaseCtrl: single-line text, password, search, styled (multi-line) text, and masked input widgets.</p>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.text-attributes","title":"Attributes","text":""},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.text.AUTOCOMPLETE","title":"AUTOCOMPLETE  <code>module-attribute</code>","text":"<pre><code>AUTOCOMPLETE = STYLEDTEXT\n</code></pre> <p>Alias for STYLEDTEXT; may be replaced by plugins at runtime.</p>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.text.STANDARDSTYLEDTEXT","title":"STANDARDSTYLEDTEXT  <code>module-attribute</code>","text":"<pre><code>STANDARDSTYLEDTEXT = STYLEDTEXT\n</code></pre> <p>Alias for STYLEDTEXT; the standard styled text implementation.</p>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.text-classes","title":"Classes","text":""},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.text.MASKTEXT","title":"MASKTEXT","text":"<pre><code>MASKTEXT(parent, **kwds)\n</code></pre> <p>               Bases: <code>TextCtrl</code>, <code>SchBaseCtrl</code></p> <p>Masked text input control with format validation.</p> <p>Handles ctrlmasktext tag. Supports mask patterns and autoformat strings via the 'src' attribute. When src starts with '!', it is treated as an autoformat string; otherwise it is used as a mask pattern.</p> Tag arguments <p>Initialize the masked text control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to masked.TextCtrl. If valuetype and src are set, configures mask or autoformat.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/text.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the masked text control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to masked.TextCtrl. If valuetype\n            and src are set, configures mask or autoformat.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    if self.valuetype:\n        if self.src:\n            if self.src.startswith(\"!\"):\n                kwds[\"autoformat\"] = self.src[1:]\n            else:\n                kwds[\"mask\"] = self.src\n    masked.TextCtrl.__init__(self, parent, **kwds)\n    self._autofit = False\n</code></pre> Methods:\u00b6"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.text.PASSWORD","title":"PASSWORD","text":"<pre><code>PASSWORD(parent, **kwds)\n</code></pre> <p>               Bases: <code>TEXT</code>, <code>SchBaseCtrl</code></p> <p>Password entry control with masked input.</p> <p>Handles ctrlpassword tag. Inherits from TEXT but adds the wx.TE_PASSWORD style for masked character display.</p> Tag arguments <p>Initialize the password control with masked style.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to TEXT after adding TE_PASSWORD style.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/text.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the password control with masked style.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to TEXT after adding TE_PASSWORD style.\n    \"\"\"\n    if \"style\" in kwds:\n        kwds[\"style\"] = kwds[\"style\"] | wx.TE_PASSWORD\n    else:\n        kwds[\"style\"] = wx.TE_PASSWORD\n\n    TEXT.__init__(self, parent, **kwds)\n</code></pre> Methods:\u00b6"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.text.SEARCH","title":"SEARCH","text":"<pre><code>SEARCH(parent, **kwds)\n</code></pre> <p>               Bases: <code>SearchCtrl</code>, <code>SchBaseCtrl</code></p> <p>Search text control with platform-specific sizing.</p> <p>Handles ctrlsearch tag. Wraps wx.SearchCtrl with adjusted default sizes for GTK and other platforms. Binds key-down events on the internal text control for GTK and MSW platforms.</p> Tag arguments <p>Initialize the search control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.SearchCtrl with platform-adjusted size and optional TE_PROCESS_ENTER style.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/text.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the search control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.SearchCtrl with platform-adjusted\n            size and optional TE_PROCESS_ENTER style.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    if wx.Platform in (\"__WXGTK__\",):\n        kwds[\"size\"] = (200, 28)\n    else:\n        kwds[\"size\"] = (200, -1)\n    if self.param and \"process_enter\" in self.param:\n        kwds[\"style\"] = wx.TE_PROCESS_ENTER\n\n    wx.SearchCtrl.__init__(self, parent, **kwds)\n\n    # Bind key-down on the internal TextCtrl child for GTK/MSW\n    if wx.Platform in (\"__WXGTK__\", \"__WXMSW__\"):\n        for child in list(self.GetChildren()):\n            if isinstance(child, wx.TextCtrl):\n                child.Bind(wx.EVT_KEY_DOWN, self.on_key_down_base)\n                break\n</code></pre> Methods:\u00b6"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.text.STYLEDTEXT","title":"STYLEDTEXT","text":"<pre><code>STYLEDTEXT(parent, **kwds)\n</code></pre> <p>               Bases: <code>TextCtrl</code>, <code>SchBaseCtrl</code></p> <p>Multi-line styled text control.</p> <p>Handles ctrlstyledtext tag. Always uses wx.TE_MULTILINE style. Supports initial data from the 'data' parameter.</p> Tag arguments <p>Initialize the styled (multi-line) text control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.TextCtrl with TE_MULTILINE style.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/text.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the styled (multi-line) text control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.TextCtrl with TE_MULTILINE style.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    if \"style\" in kwds:\n        style = kwds[\"style\"]\n        style = style | wx.TE_MULTILINE\n        kwds[\"style\"] = style\n    else:\n        kwds[\"style\"] = wx.TE_MULTILINE\n    wx.TextCtrl.__init__(self, parent, **kwds)\n    if self.param and \"data\" in self.param:\n        self.SetValue(self.param[\"data\"])\n</code></pre> Methods:\u00b6 <code></code> SetValue \u00b6 <pre><code>SetValue(value)\n</code></pre> <p>Set the text value, stripping a leading newline if present.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>String or bytes value to set. A single leading newline is stripped for cleaner display.</p> required Source code in <code>pytigon_gui/guictrl/input/text.py</code> <pre><code>def SetValue(self, value):\n    \"\"\"Set the text value, stripping a leading newline if present.\n\n    Args:\n        value: String or bytes value to set. A single leading\n            newline is stripped for cleaner display.\n    \"\"\"\n    if value.startswith(\"\\n\"):\n        value2 = value[1:]\n    else:\n        value2 = value\n    if isinstance(value2, str):\n        return wx.TextCtrl.SetValue(self, value2)\n    else:\n        return wx.TextCtrl.SetValue(self, value2.decode(\"utf-8\"))\n</code></pre>"},{"location":"api/guictrl_input/#pytigon_gui.guictrl.input.text.TEXT","title":"TEXT","text":"<pre><code>TEXT(parent, **kwds)\n</code></pre> <p>               Bases: <code>SchBaseCtrl</code>, <code>TextCtrl</code></p> <p>Single-line text input control.</p> <p>Handles ctrltext tag. Supports process_enter style for Enter key handling, hidden state, and maxlength constraint.</p> Tag arguments <p>Initialize the text control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Forwarded to wx.TextCtrl after processing 'param' for process_enter flag.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/input/text.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the text control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Forwarded to wx.TextCtrl after processing\n            'param' for process_enter flag.\n    \"\"\"\n    SchBaseCtrl.__init__(self, parent, kwds)\n    if self.param and \"process_enter\" in self.param:\n        kwds[\"style\"] = wx.TE_PROCESS_ENTER\n    wx.TextCtrl.__init__(self, parent, **kwds)\n    if self.hidden:\n        self.Enable(False)\n    if self.maxlength:\n        self.SetMaxLength(int(self.maxlength))\n</code></pre> Methods:\u00b6 <code></code> GetBestSize \u00b6 <pre><code>GetBestSize()\n</code></pre> <p>Return a wider best size than the default.</p> <p>The width is multiplied by 4 to give text fields more horizontal space.</p> <p>Returns:</p> Type Description <p>Tuple of (width * 4, height).</p> Source code in <code>pytigon_gui/guictrl/input/text.py</code> <pre><code>def GetBestSize(self):\n    \"\"\"Return a wider best size than the default.\n\n    The width is multiplied by 4 to give text fields more\n    horizontal space.\n\n    Returns:\n        Tuple of (width * 4, height).\n    \"\"\"\n    size = super().GetBestSize()\n    return (4 * size[0], size[1])\n</code></pre> <code></code> GetValue \u00b6 <pre><code>GetValue()\n</code></pre> <p>Get the current text value.</p> <p>Returns:</p> Type Description <p>The text content as a string.</p> Source code in <code>pytigon_gui/guictrl/input/text.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get the current text value.\n\n    Returns:\n        The text content as a string.\n    \"\"\"\n    return super().GetValue()\n</code></pre> <code></code> SetValue \u00b6 <pre><code>SetValue(value)\n</code></pre> <p>Set the text value, converting bytes to str if needed.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>String or bytes value to set.</p> required Source code in <code>pytigon_gui/guictrl/input/text.py</code> <pre><code>def SetValue(self, value):\n    \"\"\"Set the text value, converting bytes to str if needed.\n\n    Args:\n        value: String or bytes value to set.\n    \"\"\"\n    if isinstance(value, str):\n        return wx.TextCtrl.SetValue(self, value)\n    else:\n        return wx.TextCtrl.SetValue(self, value.decode(\"utf-8\"))\n</code></pre>"},{"location":"api/guictrl_panels/","title":"guictrl.panels \u2013 Panel Container","text":"<p>Panel-based layout container with scrolling and docking support for organizing controls.</p>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels","title":"panels","text":"<p>Panel and container widget classes for the SchForm GUI framework.</p> <p>Provides wxPython container controls integrated with SchBaseCtrl: HTML panel, notebook (tab control), collapsible pane, and composite panel.</p>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels-classes","title":"Classes","text":""},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.COLLAPSIBLE_PANEL","title":"COLLAPSIBLE_PANEL","text":"<pre><code>COLLAPSIBLE_PANEL(parent, **kwds)\n</code></pre> <p>               Bases: <code>CollapsiblePane</code>, <code>SchBaseCtrl</code></p> <p>Collapsible panel that can show/hide HTML content.</p> <p>Handles ctrlcollapsible_panel tag. Content is loaded from param['data'] as HTML and rendered in a SchPage child.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    kwds[\"label\"] = self.label\n    kwds[\"style\"] = wx.CP_DEFAULT_STYLE | wx.CP_NO_TLW_RESIZE\n    if \"size\" in kwds:\n        tmp = kwds[\"size\"]\n        self._size = [tmp[0], tmp[1]]\n        del kwds[\"size\"]\n    else:\n        self._size = [400, 300]\n    self._height = 0\n\n    if self.param and \"collapse_height\" in self.param:\n        self._size[1] = int(self.param[\"collapse_height\"])\n\n    wx.CollapsiblePane.__init__(self, parent, **kwds)\n    try:\n        self.GetPane().SetBackgroundStyle(wx.BG_STYLE_ERASE)\n    except Exception:\n        self.GetPane().SetBackgroundStyle(wx.BG_STYLE_CUSTOM)\n\n    if self.param and \"data\" in self.param and self.param[\"data\"].strip():\n        self.set_html(self.param[\"data\"])\n    else:\n        self.html = None\n        self.SetSize((0, 0))\n        self.Hide()\n    self.Bind(wx.EVT_COLLAPSIBLEPANE_CHANGED, self._on_pane_changed)\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.COLLAPSIBLE_PANEL-functions","title":"Methods:","text":""},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.COLLAPSIBLE_PANEL.GetBestSize","title":"GetBestSize","text":"<pre><code>GetBestSize()\n</code></pre> <p>Return best size accounting for expanded content height.</p> <p>Returns:</p> Type Description <p>Tuple of (width, height).</p> Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def GetBestSize(self):\n    \"\"\"Return best size accounting for expanded content height.\n\n    Returns:\n        Tuple of (width, height).\n    \"\"\"\n    ret = wx.CollapsiblePane.GetBestSize(self)\n    if self.IsExpanded():\n        return (ret[0], ret[1] + self._height)\n    elif not self.IsShown():\n        return (0, 0)\n    else:\n        if ret[1] - self._height &gt; 0:\n            return (ret[0], ret[1] - self._height)\n        else:\n            return (ret[0], ret[1])\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.COLLAPSIBLE_PANEL.SetValue","title":"SetValue","text":"<pre><code>SetValue(value)\n</code></pre> <p>Set value (placeholder - HTML is set via set_html).</p> Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def SetValue(self, value):\n    \"\"\"Set value (placeholder - HTML is set via set_html).\"\"\"\n    if not self.html:\n        pass\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.COLLAPSIBLE_PANEL.process_refr_data","title":"process_refr_data","text":"<pre><code>process_refr_data(**kwds)\n</code></pre> <p>Refresh from new data.</p> <p>Parameters:</p> Name Type Description Default <code>**kwds</code> <p>New keyword arguments. If param['data'] is non-empty, shows the panel and sets HTML content.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def process_refr_data(self, **kwds):\n    \"\"\"Refresh from new data.\n\n    Args:\n        **kwds: New keyword arguments. If param['data'] is\n            non-empty, shows the panel and sets HTML content.\n    \"\"\"\n    if \"param\" in kwds:\n        self.param = kwds[\"param\"]\n        if \"data\" in self.param and self.param[\"data\"].strip():\n            self.Show(True)\n            self.set_html(self.param[\"data\"])\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.COLLAPSIBLE_PANEL.set_html","title":"set_html","text":"<pre><code>set_html(html_txt)\n</code></pre> <p>Parse and display HTML content in the pane.</p> <p>Parameters:</p> Name Type Description Default <code>html_txt</code> <p>HTML string to render.</p> required Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def set_html(self, html_txt):\n    \"\"\"Parse and display HTML content in the pane.\n\n    Args:\n        html_txt: HTML string to render.\n    \"\"\"\n    mp = ShtmlParser()\n    mp.process(\n        \"&lt;html&gt;&lt;body&gt;\" + self.param[\"data\"].decode(\"utf-8\") + \"&lt;/body&gt;&lt;/html&gt;\"\n    )\n    mp.address = None\n    if not self.html:\n        self.html = page.SchPage(self.GetPane(), mp, {})\n        self.html.init_frame()\n        self.html.activate_page()\n        width = self._size[0] if self._size[0] &gt; 0 else 400\n        self._height = self._size[1] if self._size[1] &gt; 0 else 300\n        self.html.SetSize((width, self._height))\n        sizer = wx.BoxSizer()\n        sizer.Add(self.html, 1, wx.EXPAND | wx.ALL, 5)\n        self.GetPane().SetSizer(sizer)\n        self._on_pane_changed(None)\n    else:\n        self.html.set_adr_and_param(mp, None)\n        self._on_pane_changed(None)\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.CompositePanel","title":"CompositePanel","text":"<pre><code>CompositePanel(parent, **kwds)\n</code></pre> <p>               Bases: <code>Panel</code>, <code>SchBaseCtrl</code></p> <p>Composite panel that wraps a primary control.</p> <p>Used by SELECT2 to embed a Select2Base control alongside optional action buttons in a horizontal layout.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    wx.Panel.__init__(self, parent, **kwds)\n    self.ctrl = None\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.CompositePanel-functions","title":"Methods:","text":""},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.CompositePanel.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Get value from the wrapped control.</p> <p>Returns:</p> Type Description <p>String value or None.</p> Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def GetValue(self):\n    \"\"\"Get value from the wrapped control.\n\n    Returns:\n        String value or None.\n    \"\"\"\n    if self.ctrl:\n        return str(self.ctrl.GetValue())\n    else:\n        return None\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.CompositePanel.set_main_ctrl","title":"set_main_ctrl","text":"<pre><code>set_main_ctrl(ctrl)\n</code></pre> <p>Set the primary wrapped control.</p> <p>Parameters:</p> Name Type Description Default <code>ctrl</code> <p>The main wx control to embed.</p> required Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def set_main_ctrl(self, ctrl):\n    \"\"\"Set the primary wrapped control.\n\n    Args:\n        ctrl: The main wx control to embed.\n    \"\"\"\n    self.ctrl = ctrl\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.HTML","title":"HTML","text":"<pre><code>HTML(parent, **kwds)\n</code></pre> <p>               Bases: <code>SchPage</code>, <code>SchBaseCtrl</code></p> <p>HTML content panel.</p> <p>Handles ctrlhtml tag. Renders HTML content loaded from a server source or provided inline via param['data'].</p> Tag arguments Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    page.SchPage.__init__(self, parent, self.src, None)\n    if self.src:\n        value = self.load_string_from_server(self.src)\n        self._set_value(value, False)\n    elif self.param and \"data\" in self.param and len(self.param[\"data\"]) &gt; 0:\n        self._set_value(self.param[\"data\"], False)\n    else:\n        self._value = None\n    self.body.Refresh()\n    self.body.Update()\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.HTML-functions","title":"Methods:","text":""},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.HTML.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Return the raw HTML string.</p> Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def GetValue(self):\n    \"\"\"Return the raw HTML string.\"\"\"\n    return self._value\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.HTML.SetValue","title":"SetValue","text":"<pre><code>SetValue(value)\n</code></pre> <p>Set the HTML content value.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>HTML string to display.</p> required Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def SetValue(self, value):\n    \"\"\"Set the HTML content value.\n\n    Args:\n        value: HTML string to display.\n    \"\"\"\n    self._set_value(value)\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.NOTEBOOK","title":"NOTEBOOK","text":"<pre><code>NOTEBOOK(parent, **kwds)\n</code></pre> <p>               Bases: <code>Notebook</code>, <code>SchBaseCtrl</code></p> <p>Tabbed notebook container.</p> <p>Handles ctrlnotebook tag. Each row in tdata creates a tab page: row[0].data is the tab label, row[1].data is the content href.</p> Tag arguments Source code in <code>pytigon_gui/guictrl/panels.py</code> <pre><code>def __init__(self, parent, **kwds):\n    SchBaseCtrl.__init__(self, parent, kwds)\n    self.children = []\n    wx.Notebook.__init__(self, parent, **kwds)\n    if self.tdata:\n        for row in self.tdata:\n            h = page.SchPage(self, row[1].data, {})\n            self.AddPage(h, row[0].data)\n            self.children.append(h)\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.SchBaseCtrl","title":"SchBaseCtrl","text":"<pre><code>SchBaseCtrl(parent, kwds)\n</code></pre> <p>Base mixin class for all SchForm wxPython widgets.</p> <p>Provides: - Attribute initialization from HTML tag keyword arguments - Parent form/page navigation - Server data loading for tdata (table data) and ldata (list data) - Common keyboard handling (Escape, Tab) - Widget lifecycle: after_create signal, unique name management</p> <p>Initialize base control attributes.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent wxPython window.</p> required <code>kwds</code> <p>Keyword argument dict parsed from HTML tag attributes.</p> required Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def __init__(self, parent, kwds):\n    \"\"\"Initialize base control attributes.\n\n    Args:\n        parent: Parent wxPython window.\n        kwds: Keyword argument dict parsed from HTML tag attributes.\n    \"\"\"\n    self.unique_name = None\n    self.init_base(kwds, parent)\n    self.accept_focus = True\n    self.acc_tab_bind = False\n    form = self.get_parent_form()\n    if form:\n        form.signal_from_child(self, \"__init__\")\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.SchBaseCtrl-functions","title":"Methods:","text":""},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.SchBaseCtrl.CanAcceptFocus","title":"CanAcceptFocus","text":"<pre><code>CanAcceptFocus()\n</code></pre> <p>Return True if the control can accept keyboard focus.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def CanAcceptFocus(self):\n    \"\"\"Return True if the control can accept keyboard focus.\"\"\"\n    if self.IsShown() and self.IsEnabled():\n        return self.accept_focus\n    return False\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.SchBaseCtrl.CanAcceptFocusFromKeyboard","title":"CanAcceptFocusFromKeyboard","text":"<pre><code>CanAcceptFocusFromKeyboard()\n</code></pre> <p>Return True if the control accepts focus from keyboard navigation.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def CanAcceptFocusFromKeyboard(self):\n    \"\"\"Return True if the control accepts focus from keyboard navigation.\"\"\"\n    return self.CanAcceptFocus()\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.SchBaseCtrl.after_create","title":"after_create","text":"<pre><code>after_create()\n</code></pre> <p>Called immediately after the widget is fully constructed.</p> <p>Binds keyboard events, executes the 'onload' script (if any), calls the ext_init hook if present, and signals the parent form that this child is ready.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def after_create(self):\n    \"\"\"Called immediately after the widget is fully constructed.\n\n    Binds keyboard events, executes the 'onload' script (if any),\n    calls the __ext_init__ hook if present, and signals the parent\n    form that this child is ready.\n    \"\"\"\n    self.Bind(wx.EVT_KEY_DOWN, self.on_key_down_base)\n    if self.onload:\n        d = {\"wx\": wx, \"self\": self}\n        try:\n            exec(self.onload, d)\n        except Exception:\n            logger.exception(\"Error executing onload script for %s\", self.GetName())\n\n    if hasattr(self, \"__ext_init__\"):\n        self.__ext_init__()\n\n    self.get_parent_form().signal_from_child(self, \"after_create\")\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.SchBaseCtrl.get_ldata","title":"get_ldata","text":"<pre><code>get_ldata()\n</code></pre> <p>Return hierarchical list data, loading from server if necessary.</p> <p>Returns:</p> Type Description <p>List data (nested tree structure), or None if unavailable.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_ldata(self):\n    \"\"\"Return hierarchical list data, loading from server if necessary.\n\n    Returns:\n        List data (nested tree structure), or None if unavailable.\n    \"\"\"\n    if self.ldata is not None:\n        return self.ldata\n    if self.ldatabuf is not None:\n        return self.ldatabuf\n    if self.src:\n        self.refresh_ldata()\n    return self.ldatabuf\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.SchBaseCtrl.get_parent_form","title":"get_parent_form","text":"<pre><code>get_parent_form()\n</code></pre> <p>Walk up the widget hierarchy to find the parent SchForm.</p> <p>Returns:</p> Type Description <p>The SchForm ancestor, or None if not found.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_parent_form(self):\n    \"\"\"Walk up the widget hierarchy to find the parent SchForm.\n\n    Returns:\n        The SchForm ancestor, or None if not found.\n    \"\"\"\n    parent = self.parent\n    while parent is not None:\n        if type(parent).__name__ == \"SchForm\":\n            return parent\n        parent = parent.GetParent()\n    return None\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.SchBaseCtrl.get_parent_page","title":"get_parent_page","text":"<pre><code>get_parent_page()\n</code></pre> <p>Return the parent page that contains this control's form.</p> <p>Returns:</p> Type Description <p>The parent SchPage, or None if not found.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_parent_page(self):\n    \"\"\"Return the parent page that contains this control's form.\n\n    Returns:\n        The parent SchPage, or None if not found.\n    \"\"\"\n    form = self.get_parent_form()\n    if form:\n        return form.get_parent_page()\n    return None\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.SchBaseCtrl.get_tdata","title":"get_tdata","text":"<pre><code>get_tdata()\n</code></pre> <p>Return table data, loading from server if necessary.</p> <p>Returns:</p> Type Description <p>Table data (list of rows), or None if unavailable.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_tdata(self):\n    \"\"\"Return table data, loading from server if necessary.\n\n    Returns:\n        Table data (list of rows), or None if unavailable.\n    \"\"\"\n    if self.tdata is not None:\n        return self.tdata\n    if self.tdatabuf is not None:\n        return self.tdatabuf\n    if self.src:\n        self.refresh_tdata()\n    return self.tdatabuf\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.SchBaseCtrl.get_unique_name","title":"get_unique_name","text":"<pre><code>get_unique_name()\n</code></pre> <p>Return the unique name identifier.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_unique_name(self):\n    \"\"\"Return the unique name identifier.\"\"\"\n    return self.unique_name\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.SchBaseCtrl.init_base","title":"init_base","text":"<pre><code>init_base(kwds, parent=None)\n</code></pre> <p>Initialize all control attributes from kwds.</p> <p>Extracts well-known attributes (href, label, id, target, value, valuetype, defaultvalue, length, maxlength, readonly, hidden, src, onload, tdata, ldata, data, param, style) from kwds and assigns them as instance attributes.</p> <p>Parameters:</p> Name Type Description Default <code>kwds</code> <p>Keyword argument dict parsed from HTML tag attributes.</p> required <code>parent</code> <p>Optional parent window reference.</p> <code>None</code> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def init_base(self, kwds, parent=None):\n    \"\"\"Initialize all control attributes from kwds.\n\n    Extracts well-known attributes (href, label, id, target, value,\n    valuetype, defaultvalue, length, maxlength, readonly, hidden,\n    src, onload, tdata, ldata, data, param, style) from kwds\n    and assigns them as instance attributes.\n\n    Args:\n        kwds: Keyword argument dict parsed from HTML tag attributes.\n        parent: Optional parent window reference.\n    \"\"\"\n    if parent:\n        self.parent = parent\n\n    self.ldatabuf = None\n\n    self.tag = None\n    if \"param\" in kwds and \"tag\" in kwds[\"param\"]:\n        self.tag = kwds[\"param\"][\"tag\"]\n\n    self.href = _extract_kwarg(kwds, \"href\", None)\n    if self.href is None:\n        self.href = \"\"\n\n    self.label = _extract_kwarg(kwds, \"label\", None)\n    self.nr_id = _extract_kwarg(kwds, \"id\", None)\n    self.target = _extract_kwarg(kwds, \"target\", \"_blank\")\n    self.value = _extract_kwarg(kwds, \"value\", None)\n    self.valuetype = _extract_kwarg(kwds, \"valuetype\", \"data\")\n    self.defaultvalue = _extract_kwarg(kwds, \"defaultvalue\", None)\n    self.length = int(_extract_kwarg(kwds, \"length\", 0))\n    self.maxlength = int(_extract_kwarg(kwds, \"maxlength\", 0))\n    self.readonly = _extract_kwarg(kwds, \"readonly\", False)\n    self.hidden = True if _extract_kwarg(kwds, \"hidden\", False) else False\n    self.src = _extract_kwarg(kwds, \"src\", None)\n    self.onload = _extract_kwarg(kwds, \"onload\", None)\n\n    # tdata / tdatabuf: table data (already parsed) or lazily loaded\n    self.tdata = _extract_kwarg(kwds, \"tdata\", None)\n    self.tdatabuf = None\n    if self.tdata is not None:\n        self.tdatabuf = None\n\n    # ldata: hierarchical list data (already parsed) or lazily loaded\n    self.ldata = _extract_kwarg(kwds, \"ldata\", None)\n\n    # data: URL-encoded string data\n    raw_data = _extract_kwarg(kwds, \"data\", None)\n    self.data = unquote(raw_data) if raw_data is not None else None\n\n    self.param = _extract_kwarg(kwds, \"param\", None)\n    self.style = _extract_kwarg(kwds, \"style\", None)\n\n    # Invoke any registered ctrl_process hooks for this tag type\n    ctrl_process = wx.GetApp().ctrl_process\n    if self.tag in ctrl_process:\n        for fun in ctrl_process[self.tag]:\n            fun(self)\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.SchBaseCtrl.is_ctrl_block","title":"is_ctrl_block","text":"<pre><code>is_ctrl_block()\n</code></pre> <p>Return True if this control is a block-level element.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def is_ctrl_block(self):\n    \"\"\"Return True if this control is a block-level element.\"\"\"\n    return self.GetParent().is_ctrl_block(self)\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.SchBaseCtrl.load_data_from_server","title":"load_data_from_server","text":"<pre><code>load_data_from_server(path)\n</code></pre> <p>Load raw binary data from a server URL.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <p>URL path to fetch.</p> required <p>Returns:</p> Type Description <p>Raw response data (bytes).</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def load_data_from_server(self, path):\n    \"\"\"Load raw binary data from a server URL.\n\n    Args:\n        path: URL path to fetch.\n\n    Returns:\n        Raw response data (bytes).\n    \"\"\"\n    http = wx.GetApp().get_http(self.parent)\n    response = http.get(self, str(path))\n    return response.ptr()\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.SchBaseCtrl.load_string_from_server","title":"load_string_from_server","text":"<pre><code>load_string_from_server(path)\n</code></pre> <p>Load a text response from a server URL.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <p>URL path to fetch.</p> required <p>Returns:</p> Type Description <p>Response body as a string.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def load_string_from_server(self, path):\n    \"\"\"Load a text response from a server URL.\n\n    Args:\n        path: URL path to fetch.\n\n    Returns:\n        Response body as a string.\n    \"\"\"\n    http = wx.GetApp().get_http(self.parent)\n    response = http.get(self, str(path))\n    return response.str()\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.SchBaseCtrl.on_key_down_base","title":"on_key_down_base","text":"<pre><code>on_key_down_base(event)\n</code></pre> <p>Default key-down handler for Escape and Tab navigation.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>wx.KeyEvent.</p> required Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def on_key_down_base(self, event):\n    \"\"\"Default key-down handler for Escape and Tab navigation.\n\n    Args:\n        event: wx.KeyEvent.\n    \"\"\"\n    if event.GetKeyCode() == wx.WXK_ESCAPE:\n        wx.CallAfter(self.GetParent().cancel)\n    elif event.GetKeyCode() == wx.WXK_TAB:\n        if event.ShiftDown():\n            self.GetParent().Navigate(self, True)\n        else:\n            self.GetParent().Navigate(self, False)\n    else:\n        event.Skip()\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.SchBaseCtrl.refresh_ldata","title":"refresh_ldata","text":"<pre><code>refresh_ldata()\n</code></pre> <p>Refresh hierarchical list data from the server.</p> <p>Loads from self.src and parses into self.ldatabuf using TreeParser.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def refresh_ldata(self):\n    \"\"\"Refresh hierarchical list data from the server.\n\n    Loads from self.src and parses into self.ldatabuf using TreeParser.\n    \"\"\"\n    if self.ldata is None and self.src:\n        lista = self.load_data_from_server(self.src)\n        mp = TreeParser()\n        mp.feed(lista)\n        mp.close()\n        self.ldatabuf = mp.tree_parent[0][1]\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.SchBaseCtrl.refresh_tdata","title":"refresh_tdata","text":"<pre><code>refresh_tdata(html_src=None)\n</code></pre> <p>Refresh table data from the server or provided HTML source.</p> <p>Parameters:</p> Name Type Description Default <code>html_src</code> <p>Optional HTML string containing table data. If None, loads from self.src.</p> <code>None</code> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def refresh_tdata(self, html_src=None):\n    \"\"\"Refresh table data from the server or provided HTML source.\n\n    Args:\n        html_src: Optional HTML string containing table data.\n            If None, loads from self.src.\n    \"\"\"\n    if self.tdata is None and self.src:\n        if html_src:\n            tables = html_src\n        else:\n            tables = self.load_data_from_server(self.src).decode(\"utf-8\")\n        self.tdatabuf = tdata_from_html(tables, wx.GetApp().http)\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.SchBaseCtrl.set_acc_key_tab","title":"set_acc_key_tab","text":"<pre><code>set_acc_key_tab(tab)\n</code></pre> <p>Register an accelerator table for this widget.</p> <p>Parameters:</p> Name Type Description Default <code>tab</code> <p>List or tuple of accelerator elements. Each element has structure: (flag, keycode, callback function).</p> required <p>Returns:</p> Type Description <p>Result from the parent's set_acc_key_tab method.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def set_acc_key_tab(self, tab):\n    \"\"\"Register an accelerator table for this widget.\n\n    Args:\n        tab: List or tuple of accelerator elements. Each element has\n            structure: (flag, keycode, callback function).\n\n    Returns:\n        Result from the parent's set_acc_key_tab method.\n    \"\"\"\n    return self.GetParent().set_acc_key_tab(self, tab)\n</code></pre>"},{"location":"api/guictrl_panels/#pytigon_gui.guictrl.panels.SchBaseCtrl.set_unique_name","title":"set_unique_name","text":"<pre><code>set_unique_name(name)\n</code></pre> <p>Assign a unique name identifier to this control.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def set_unique_name(self, name):\n    \"\"\"Assign a unique name identifier to this control.\"\"\"\n    self.unique_name = name\n</code></pre>"},{"location":"api/guictrl_popup/","title":"guictrl.popup \u2013 Popup Dialogs","text":"<p>Popup dialog implementations: HTML-based popups and Select2-style dropdown selectors.</p>"},{"location":"api/guictrl_popup/#pytigon_gui.guictrl.popup","title":"popup","text":"<p>Popup widgets for the SchForm GUI framework.</p> <p>Provides popup HTML and Select2-style dropdown controls.</p>"},{"location":"api/guictrl_popup/#pytigon_gui.guictrl.popup-modules","title":"Modules","text":""},{"location":"api/guictrl_popup/#pytigon_gui.guictrl.popup.popuphtml","title":"popuphtml","text":"<p>Popup control classes for server-backed data selection.</p> <p>Provides a ComboCtrl-based popup control (DataPopupControl) that interacts with server endpoints for data lookup, selection, and dialog display.</p> Server API <p>test(value):     Return element matching value, or empty string if no match.     Response format: (element id, string representation of element).</p> <p>dialog(value):     Return an HTML form to choose an element.     Response format: (element id, string representation of element).</p>"},{"location":"api/guictrl_popup/#pytigon_gui.guictrl.popup.popuphtml-classes","title":"Classes","text":""},{"location":"api/guictrl_popup/#pytigon_gui.guictrl.popup.popuphtml.DataPopup","title":"DataPopup","text":"<pre><code>DataPopup(size, combo, href)\n</code></pre> <p>               Bases: <code>ComboPopup</code></p> <p>Popup window that embeds an HTML page for data selection.</p> <p>Used by DataPopupControl to display a SchPage inside a combo dropdown. Handles ESC dismissal and size negotiation.</p> <p>Initialize the popup.</p> <p>Parameters:</p> Name Type Description Default <code>size</code> <p>Preferred size (width, height).</p> required <code>combo</code> <p>The parent DataPopupControl.</p> required <code>href</code> <p>Server URL for the popup content.</p> required Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def __init__(self, size, combo, href):\n    \"\"\"Initialize the popup.\n\n    Args:\n        size: Preferred size (width, height).\n        combo: The parent DataPopupControl.\n        href: Server URL for the popup content.\n    \"\"\"\n    self.href = href\n    self.combo = combo\n    self.html = None\n    self.size = size\n    wx.ComboPopup.__init__(self)\n</code></pre> Methods:\u00b6 <code></code> Create \u00b6 <pre><code>Create(parent)\n</code></pre> <p>Create the popup content (SchPage) and layout.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>The popup parent window.</p> required <p>Returns:</p> Type Description <p>True on success.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def Create(self, parent):\n    \"\"\"Create the popup content (SchPage) and layout.\n\n    Args:\n        parent: The popup parent window.\n\n    Returns:\n        True on success.\n    \"\"\"\n    self.html = self.combo.on_create(parent)\n    parent.Bind(wx.EVT_KEY_DOWN, self.on_key_down)\n    box = wx.BoxSizer(wx.VERTICAL)\n    box.Add(self.html, 1, wx.ALL | wx.GROW, 1)\n    parent.SetSizer(box)\n    parent.SetAutoLayout(True)\n    parent.Fit()\n    return True\n</code></pre> <code></code> GetAdjustedSize \u00b6 <pre><code>GetAdjustedSize(minWidth, prefHeight, maxHeight)\n</code></pre> <p>Return the popup's adjusted size.</p> <p>Parameters:</p> Name Type Description Default <code>minWidth</code> <p>Minimum width.</p> required <code>prefHeight</code> <p>Preferred height.</p> required <code>maxHeight</code> <p>Maximum height.</p> required <p>Returns:</p> Type Description <p>Adjusted (width, height) tuple.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def GetAdjustedSize(self, minWidth, prefHeight, maxHeight):\n    \"\"\"Return the popup's adjusted size.\n\n    Args:\n        minWidth: Minimum width.\n        prefHeight: Preferred height.\n        maxHeight: Maximum height.\n\n    Returns:\n        Adjusted (width, height) tuple.\n    \"\"\"\n    width = self.size[0]\n    height = self.size[1]\n    return wx.ComboPopup.GetAdjustedSize(self, width, height, maxHeight)\n</code></pre> <code></code> GetControl \u00b6 <pre><code>GetControl()\n</code></pre> <p>Return the embedded HTML control.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def GetControl(self):\n    \"\"\"Return the embedded HTML control.\"\"\"\n    return self.html\n</code></pre> <code></code> GetStringValue \u00b6 <pre><code>GetStringValue()\n</code></pre> <p>Return the combo's start value as the string representation.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def GetStringValue(self):\n    \"\"\"Return the combo's start value as the string representation.\"\"\"\n    return self.combo.start_value\n</code></pre> <code></code> OnPopup \u00b6 <pre><code>OnPopup()\n</code></pre> <p>Called when the popup is shown.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def OnPopup(self):\n    \"\"\"Called when the popup is shown.\"\"\"\n    self.combo.on_popoup()\n    return wx.ComboPopup.OnPopup(self)\n</code></pre> <code></code> on_key_down \u00b6 <pre><code>on_key_down(event)\n</code></pre> <p>Handle ESC key to dismiss the popup.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>wx.KeyEvent.</p> required Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def on_key_down(self, event):\n    \"\"\"Handle ESC key to dismiss the popup.\n\n    Args:\n        event: wx.KeyEvent.\n    \"\"\"\n    if event.KeyCode == wx.WXK_ESCAPE:\n        self.Dismiss()\n    else:\n        event.Skip()\n</code></pre> <code></code> set_new_href \u00b6 <pre><code>set_new_href(href)\n</code></pre> <p>Update the popup's server URL and propagate to the HTML page.</p> <p>Parameters:</p> Name Type Description Default <code>href</code> <p>New server URL.</p> required Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def set_new_href(self, href):\n    \"\"\"Update the popup's server URL and propagate to the HTML page.\n\n    Args:\n        href: New server URL.\n    \"\"\"\n    self.href = href\n    if self.html:\n        self.html.set_new_href(href)\n</code></pre>"},{"location":"api/guictrl_popup/#pytigon_gui.guictrl.popup.popuphtml.DataPopupControl","title":"DataPopupControl","text":"<pre><code>DataPopupControl(*args, **kwds)\n</code></pre> <p>               Bases: <code>ComboCtrl</code></p> <p>Server-backed combo control with popup data selection.</p> <p>Provides a ComboCtrl that uses server endpoints for: - /size/  : fetching popup dimensions - /test/  : validating/looking up typed values - /dialog/: opening an extended selection dialog</p> <p>Supports an optional secondary href (separated by ';') for the extended dialog endpoint.</p> <p>Initialize the popup control.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <p>Positional arguments passed to ComboCtrl.</p> <code>()</code> <code>**kwds</code> <p>Keyword arguments. Accepts 'dialog_with_value' (bool, default True) and 'style'.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def __init__(self, *args, **kwds):\n    \"\"\"Initialize the popup control.\n\n    Args:\n        *args: Positional arguments passed to ComboCtrl.\n        **kwds: Keyword arguments. Accepts 'dialog_with_value'\n            (bool, default True) and 'style'.\n    \"\"\"\n    if \"style\" in kwds:\n        kwds[\"style\"] |= wx.TE_PROCESS_ENTER\n    else:\n        kwds[\"style\"] = wx.TE_PROCESS_ENTER\n\n    if \"dialog_with_value\" in kwds:\n        self.dialog_with_value = kwds[\"dialog_with_value\"]\n        del kwds[\"dialog_with_value\"]\n    else:\n        self.dialog_with_value = True\n    ComboCtrl.__init__(self, *args, **kwds)\n\n    self.win = None\n    self.html = None\n    self.rec_value = []\n    self.event_object = None\n    self.page = None\n    self.popup = None\n    self.start_value = \"\"\n\n    if self.defaultvalue:\n        self.clear_str = self.defaultvalue\n    else:\n        self.clear_str = \"\"\n\n    # Support dual-href: primary href for list/size/test,\n    # secondary (after ';') for extended dialog\n    href_parts = self.href.split(\";\")\n    self.href = href_parts[0]\n    self.href2 = href_parts[1] if len(href_parts) &gt; 1 else None\n\n    self.http = wx.GetApp().get_http(self)\n    response = self.http.get(self, str(self.href) + \"size/\")\n    self.size = schjson.loads(response.str())\n\n    self.simpleDialog = True\n    if self.GetTextCtrl():\n        self.GetTextCtrl().SetForegroundColour(wx.Colour(0, 0, 0))\n\n    self.UseAltPopupWindow(enable=True)\n\n    popup_ctrl = self._create_popoup()\n    self.SetPopupControl(popup_ctrl)\n</code></pre> Methods:\u00b6 <code></code> Dismiss \u00b6 <pre><code>Dismiss()\n</code></pre> <p>Dismiss the popup or extended dialog page.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def Dismiss(self):\n    \"\"\"Dismiss the popup or extended dialog page.\"\"\"\n    if self.page:\n        self.page.body.old_any_parent_command(\"on_cancel\", None)\n        self.page = None\n    else:\n        super().Dismiss()\n    self.SetFocus()\n</code></pre> <code></code> GetTextCtrl \u00b6 <pre><code>GetTextCtrl()\n</code></pre> <p>Return the text control, preferring the masked override if set.</p> <p>Returns:</p> Type Description <p>wx.TextCtrl or masked.TextCtrl instance.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def GetTextCtrl(self):\n    \"\"\"Return the text control, preferring the masked override if set.\n\n    Returns:\n        wx.TextCtrl or masked.TextCtrl instance.\n    \"\"\"\n    if self.win:\n        return self.win\n    return ComboCtrl.GetTextCtrl(self)\n</code></pre> <code></code> KillFocus \u00b6 <pre><code>KillFocus()\n</code></pre> <p>Handle focus loss: trigger focus_out validation if readonly.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def KillFocus(self):\n    \"\"\"Handle focus loss: trigger focus_out validation if readonly.\"\"\"\n    value = ComboCtrl.GetValue(self)\n    if self.readonly:\n        self.focus_out(value)\n</code></pre> <code></code> alternate_button_click \u00b6 <pre><code>alternate_button_click()\n</code></pre> <p>Handle alternative button (e.g. F2) to open extended dialog.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def alternate_button_click(self):\n    \"\"\"Handle alternative button (e.g. F2) to open extended dialog.\"\"\"\n    if self.event_object and hasattr(self.event_object, \"on_before_button_click\"):\n        self.event_object.on_before_button_click()\n\n    self.run_ext_dialog()\n\n    if self.event_object and hasattr(self.event_object, \"OnButtonClick\"):\n        self.event_object.OnButtonClick()\n</code></pre> <code></code> any_parent_command \u00b6 <pre><code>any_parent_command(command, *args, **kwds)\n</code></pre> <p>Walk up the widget tree to find and call a named command.</p> <p>Parameters:</p> Name Type Description Default <code>command</code> <p>Method name to search for.</p> required <code>*args</code> <p>Positional arguments.</p> <code>()</code> <code>**kwds</code> <p>Keyword arguments.</p> <code>{}</code> <p>Returns:</p> Type Description <p>Result of the command, or None if not found.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def any_parent_command(self, command, *args, **kwds):\n    \"\"\"Walk up the widget tree to find and call a named command.\n\n    Args:\n        command: Method name to search for.\n        *args: Positional arguments.\n        **kwds: Keyword arguments.\n\n    Returns:\n        Result of the command, or None if not found.\n    \"\"\"\n    parent = self\n    while parent is not None:\n        if hasattr(parent, command):\n            return getattr(parent, command)(*args, **kwds)\n        parent = parent.GetParent()\n    return None\n</code></pre> <code></code> clear_rec \u00b6 <pre><code>clear_rec()\n</code></pre> <p>Clear the current selection and reset to the default string.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def clear_rec(self):\n    \"\"\"Clear the current selection and reset to the default string.\"\"\"\n    self.start_value = \"\"\n    self.SetValue(self.clear_str)\n    self.rec_value = []\n</code></pre> <code></code> focus_in \u00b6 <pre><code>focus_in(value)\n</code></pre> <p>Handle focus-in event (no-op by default).</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>Current field value.</p> required Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def focus_in(self, value):\n    \"\"\"Handle focus-in event (no-op by default).\n\n    Args:\n        value: Current field value.\n    \"\"\"\n</code></pre> <code></code> focus_out \u00b6 <pre><code>focus_out(value)\n</code></pre> <p>Validate typed value against the server when focus leaves.</p> <p>Compares current value with start_value. If changed and non-empty, posts the value to the server's /test/ endpoint.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>The current field value.</p> required Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def focus_out(self, value):\n    \"\"\"Validate typed value against the server when focus leaves.\n\n    Compares current value with start_value. If changed and non-empty,\n    posts the value to the server's /test/ endpoint.\n\n    Args:\n        value: The current field value.\n    \"\"\"\n    if str(value) != self.start_value and str(value) != \"\":\n        self.http = wx.GetApp().get_http(self)\n        x = bencode(value)\n        response = self.http.post(self, str(self.href) + \"test/\", {\"value\": x})\n        tab = schjson.loads(response.str())\n        ret = tab[0]\n\n        if ret != 1:\n            if ret == 2:\n                self.OnButtonClick()\n            else:\n                self.clear_rec()\n        else:\n            self.set_rec(tab[1], tab[2], False)\n</code></pre> <code></code> get_last_control_with_focus \u00b6 <pre><code>get_last_control_with_focus()\n</code></pre> <p>Return self as the last focused control.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def get_last_control_with_focus(self):\n    \"\"\"Return self as the last focused control.\"\"\"\n    return self\n</code></pre> <code></code> get_parm \u00b6 <pre><code>get_parm(parm)\n</code></pre> <p>Get a parameter value for URL encoding.</p> <p>Parameters:</p> Name Type Description Default <code>parm</code> <p>Parameter name ('value' supported).</p> required <p>Returns:</p> Type Description <p>Bencoded field value, or None.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def get_parm(self, parm):\n    \"\"\"Get a parameter value for URL encoding.\n\n    Args:\n        parm: Parameter name ('value' supported).\n\n    Returns:\n        Bencoded field value, or None.\n    \"\"\"\n    return bencode(ComboCtrl.GetValue(self)) if parm == \"value\" else None\n</code></pre> <code></code> get_rec \u00b6 <pre><code>get_rec()\n</code></pre> <p>Return the currently selected record.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def get_rec(self):\n    \"\"\"Return the currently selected record.\"\"\"\n    return self.rec_value\n</code></pre> <code></code> has_parm \u00b6 <pre><code>has_parm(parm)\n</code></pre> <p>Check if a parameter is supported.</p> <p>Parameters:</p> Name Type Description Default <code>parm</code> <p>Parameter name.</p> required <p>Returns:</p> Type Description <p>True if parm is 'value', False otherwise.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def has_parm(self, parm):\n    \"\"\"Check if a parameter is supported.\n\n    Args:\n        parm: Parameter name.\n\n    Returns:\n        True if parm is 'value', False otherwise.\n    \"\"\"\n    return parm == \"value\"\n</code></pre> <code></code> on_create \u00b6 <pre><code>on_create(parent)\n</code></pre> <p>Create the embedded SchPage for popup content.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window for the SchPage.</p> required <p>Returns:</p> Type Description <p>The created SchPage instance.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def on_create(self, parent):\n    \"\"\"Create the embedded SchPage for popup content.\n\n    Args:\n        parent: Parent window for the SchPage.\n\n    Returns:\n        The created SchPage instance.\n    \"\"\"\n    from pytigon_gui.guiframe.page import SchPage\n\n    href = (\n        self.href + \"dialog/|value\"\n        if self.dialog_with_value\n        else self.href + \"dialog/\"\n    )\n    self.html = SchPage(parent, href, self)\n    self.html.body.parent_combo = self\n    return self.html\n</code></pre> <code></code> on_popoup \u00b6 <pre><code>on_popoup()\n</code></pre> <p>Called when the dropdown popup is shown.</p> <p>Refreshes the embedded HTML page and positions it.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def on_popoup(self):\n    \"\"\"Called when the dropdown popup is shown.\n\n    Refreshes the embedded HTML page and positions it.\n    \"\"\"\n    if self.html:\n        wx.BeginBusyCursor()\n        self.html.body.Hide()\n\n        def _after():\n            self.html.refresh_html()\n            self.html.SetFocus()\n            self.html.on_size(None)\n            self.html.body.init()\n\n            def _after2():\n                self.html.body.refr(self.start_value)\n                self.html.body.Show()\n\n            wx.CallAfter(_after2)\n            wx.EndBusyCursor()\n\n        wx.CallAfter(_after)\n</code></pre> <code></code> run_ext_dialog \u00b6 <pre><code>run_ext_dialog()\n</code></pre> <p>Open the extended selection dialog as a child page.</p> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def run_ext_dialog(self):\n    \"\"\"Open the extended selection dialog as a child page.\"\"\"\n    self.GetTextCtrl().SetFocus()\n\n    parm = {\"value\": self.get_parm(\"value\")}\n\n    href = self.href2 if self.href2 else self.href\n\n    _href = href + \"dialog/|value\" if self.dialog_with_value else href + \"dialog/\"\n    self.page = self.GetParent().new_child_page(str(_href), \"Select\", parm)\n    self.page.body.old_any_parent_command = self.page.body.any_parent_command\n    self.page.body.any_parent_command = self.any_parent_command\n    self.page.body.parent_combo = self\n</code></pre> <code></code> set_new_href \u00b6 <pre><code>set_new_href(href)\n</code></pre> <p>Update the server endpoint URL.</p> <p>Parameters:</p> Name Type Description Default <code>href</code> <p>New base URL. May contain ';' to separate primary and secondary (dialog) endpoints.</p> required Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def set_new_href(self, href):\n    \"\"\"Update the server endpoint URL.\n\n    Args:\n        href: New base URL. May contain ';' to separate primary\n            and secondary (dialog) endpoints.\n    \"\"\"\n    self.href = href\n\n    href_parts = self.href.split(\";\")\n    if len(href_parts) &gt; 1:\n        self.href = href_parts[0]\n        self.href2 = href_parts[1]\n    else:\n        self.href2 = None\n\n    href3 = self.href2 if self.href2 else self.href\n\n    if self.dialog_with_value:\n        href3 += \"dialog/|value\"\n    else:\n        href3 += \"dialog/\"\n\n    if self.popup:\n        self.popup.set_new_href(href3)\n</code></pre> <code></code> set_rec \u00b6 <pre><code>set_rec(value, value_rec, dismiss=False)\n</code></pre> <p>Set the field value from a server response record.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>Element id.</p> required <code>value_rec</code> <p>Element data (Td object or similar).</p> required <code>dismiss</code> <p>If True, dismisses the popup after setting.</p> <code>False</code> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def set_rec(self, value, value_rec, dismiss=False):\n    \"\"\"Set the field value from a server response record.\n\n    Args:\n        value: Element id.\n        value_rec: Element data (Td object or similar).\n        dismiss: If True, dismisses the popup after setting.\n    \"\"\"\n    value2 = value_rec.data\n\n    if self.event_object and hasattr(self.event_object, \"set_rec\"):\n        value2 = self.event_object.set_rec(value, value_rec, dismiss)\n\n    self.start_value = value2\n    self.SetValue(value2)\n    self.rec_value = value_rec\n\n    if dismiss:\n        self.Dismiss()\n\n    parent = self.GetParent()\n    if hasattr(parent, \"on_popup_control_change_value\"):\n        parent.on_popup_control_change_value(self)\n</code></pre> <code></code> to_masked \u00b6 <pre><code>to_masked(**kwds)\n</code></pre> <p>Convert the internal text control to a masked.TextCtrl.</p> <p>Parameters:</p> Name Type Description Default <code>**kwds</code> <p>Parameters forwarded to SetCtrlParameters.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/popup/popuphtml.py</code> <pre><code>def to_masked(self, **kwds):\n    \"\"\"Convert the internal text control to a masked.TextCtrl.\n\n    Args:\n        **kwds: Parameters forwarded to SetCtrlParameters.\n    \"\"\"\n    self.win = ComboCtrl.GetTextCtrl(self)\n    self.win.__class__ = masked.TextCtrl\n    self.win._PostInit(setupEventHandling=True, name=\"maskedTextCtrl\", value=\"\")\n    self.win.SetCtrlParameters(**kwds)\n</code></pre>"},{"location":"api/guictrl_popup/#pytigon_gui.guictrl.popup.select2","title":"select2","text":"<p>Select2-style autocomplete widget classes.</p> <p>Provides server-backed autocomplete with popup selection list, inspired by the Select2 JavaScript library (https://select2.github.io/).</p>"},{"location":"api/guictrl_popup/#pytigon_gui.guictrl.popup.select2-classes","title":"Classes","text":""},{"location":"api/guictrl_popup/#pytigon_gui.guictrl.popup.select2.ListBoxNoFocus","title":"ListBoxNoFocus","text":"<p>               Bases: <code>ListBox</code></p> <p>ListBox that never accepts keyboard focus.</p> Methods:\u00b6 <code></code> CanAcceptFocus \u00b6 <pre><code>CanAcceptFocus()\n</code></pre> <p>Return False always -- this listbox cannot receive focus.</p> Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def CanAcceptFocus(self):\n    \"\"\"Return False always -- this listbox cannot receive focus.\"\"\"\n    return False\n</code></pre>"},{"location":"api/guictrl_popup/#pytigon_gui.guictrl.popup.select2.Select2Base","title":"Select2Base","text":"<pre><code>Select2Base(parent, **kwds)\n</code></pre> <p>               Bases: <code>ComboCtrl</code>, <code>SchBaseCtrl</code></p> <p>Server-backed autocomplete combo using Select2 protocol.</p> <p>Provides a wx.ComboCtrl that communicates with a Select2-compatible server endpoint for autocomplete search and selection.</p> <p>Supports multiple selection mode and action buttons (F2/Insert keys).</p> <p>Initialize the Select2 control.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>**kwds</code> <p>Keyword arguments forwarded to wx.ComboCtrl and SchBaseCtrl. Expects 'param' containing 'data' with Select2 configuration attrs.</p> <code>{}</code> Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def __init__(self, parent, **kwds):\n    \"\"\"Initialize the Select2 control.\n\n    Args:\n        parent: Parent window.\n        **kwds: Keyword arguments forwarded to wx.ComboCtrl and\n            SchBaseCtrl. Expects 'param' containing 'data' with\n            Select2 configuration attrs.\n    \"\"\"\n    self.popup = None\n    self.button1 = None\n    self.button2 = None\n    self._popup_shown = False\n\n    SchBaseCtrl.__init__(self, parent, kwds)\n\n    data = self.param[\"data\"][0][\"attrs\"]\n    self.multiple = \"multiple\" in data\n\n    if \"style\" in kwds:\n        kwds[\"style\"] |= wx.TE_PROCESS_ENTER\n    else:\n        kwds[\"style\"] = wx.TE_PROCESS_ENTER\n\n    if \"item_id\" in self.param and self.param[\"item_id\"] != \"None\":\n        self.item_id = [int(self.param[\"item_id\"])]\n        self.item_str = [self.param[\"item_str\"]]\n    else:\n        self.item_id = [-1]\n        self.item_str = [\"\"]\n\n    kwds[\"size\"] = (438, -1)\n\n    wx.ComboCtrl.__init__(self, parent, **kwds)\n\n    if self.GetTextCtrl():\n        self.GetTextCtrl().SetForegroundColour(wx.Colour(0, 0, 0))\n\n    self.Bind(wx.EVT_CHAR, self.on_char)\n\n    if self.item_str:\n        self.SetValue(self.item_str[0])\n\n    self.Bind(wx.EVT_KEY_DOWN, self.on_key_down_base)\n\n    self.GetParent().get_parent_page().register_signal(self, \"return_new_row\")\n    self.GetParent().get_parent_page().register_signal(self, \"return_updated_row\")\n</code></pre> Methods:\u00b6 <code></code> Dismiss \u00b6 <pre><code>Dismiss()\n</code></pre> <p>Close the popup and return focus.</p> Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def Dismiss(self):\n    \"\"\"Close the popup and return focus.\"\"\"\n    if self.popup:\n        self.popup.Close()\n        self.popup = None\n    self.SetFocus()\n</code></pre> <code></code> DoSetPopupControl \u00b6 <pre><code>DoSetPopupControl(popup)\n</code></pre> <p>Placeholder for popup control setup (not used).</p> Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def DoSetPopupControl(self, popup):\n    \"\"\"Placeholder for popup control setup (not used).\"\"\"\n    pass\n</code></pre> <code></code> GetValue \u00b6 <pre><code>GetValue()\n</code></pre> <p>Return the selected value (item_id or list of item_ids).</p> <p>Returns:</p> Type Description <p>int or list of int item IDs.</p> Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def GetValue(self):\n    \"\"\"Return the selected value (item_id or list of item_ids).\n\n    Returns:\n        int or list of int item IDs.\n    \"\"\"\n    if self.multiple:\n        return self.item_id\n    return self.item_id[0]\n</code></pre> <code></code> OnButtonClick \u00b6 <pre><code>OnButtonClick()\n</code></pre> <p>Handle the dropdown button click: open popup and clear search.</p> Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def OnButtonClick(self):\n    \"\"\"Handle the dropdown button click: open popup and clear search.\"\"\"\n    ret = self._on_button_click()\n    self.popup.edit_ctrl.SetValue(\"\")\n    wx.CallAfter(self.popup.edit_ctrl.SetFocus)\n    return ret\n</code></pre> <code></code> init \u00b6 <pre><code>init(button1, button2)\n</code></pre> <p>Register optional action buttons (F2 and Insert shortcuts).</p> <p>Parameters:</p> Name Type Description Default <code>button1</code> <p>Button triggered by F2.</p> required <code>button2</code> <p>Button triggered by Insert.</p> required Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def init(self, button1, button2):\n    \"\"\"Register optional action buttons (F2 and Insert shortcuts).\n\n    Args:\n        button1: Button triggered by F2.\n        button2: Button triggered by Insert.\n    \"\"\"\n    self.button1 = button1\n    self.button2 = button2\n</code></pre> <code></code> on_char \u00b6 <pre><code>on_char(event)\n</code></pre> <p>Handle character input: forward to the popup search field.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>wx.KeyEvent.</p> required Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def on_char(self, event):\n    \"\"\"Handle character input: forward to the popup search field.\n\n    Args:\n        event: wx.KeyEvent.\n    \"\"\"\n    try:\n        c = chr(event.GetUnicodeKey())\n        if c in string.printable:\n            if not self._popup_shown:\n                self._on_button_click()\n            self.popup.edit_ctrl.AppendText(c)\n    except (ValueError, OverflowError):\n        pass\n    if event.KeyCode in (wx.WXK_DELETE, wx.WXK_BACK):\n        self.item_id = [-1]\n        self.item_str = [\"\"]\n        self.Clear()\n</code></pre> <code></code> on_key_down_base \u00b6 <pre><code>on_key_down_base(event)\n</code></pre> <p>Handle global keyboard shortcuts.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>wx.KeyEvent.</p> required Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def on_key_down_base(self, event):\n    \"\"\"Handle global keyboard shortcuts.\n\n    Args:\n        event: wx.KeyEvent.\n    \"\"\"\n    if event.GetKeyCode() == wx.WXK_TAB:\n        if event.ShiftDown():\n            self.GetParent().GetParent().Navigate(self.GetParent(), True)\n        else:\n            self.GetParent().GetParent().Navigate(self.GetParent(), False)\n    elif event.GetKeyCode() == wx.WXK_F2:\n        self.button1.on_click(event)\n    elif event.GetKeyCode() == wx.WXK_INSERT:\n        self.button2.on_click(event)\n    else:\n        event.Skip()\n</code></pre> <code></code> on_popup_hidden \u00b6 <pre><code>on_popup_hidden()\n</code></pre> <p>Called when the popup is hidden.</p> Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def on_popup_hidden(self):\n    \"\"\"Called when the popup is hidden.\"\"\"\n    self._popup_shown = False\n    self.SetFocus()\n</code></pre> <code></code> return_new_row \u00b6 <pre><code>return_new_row(**argv)\n</code></pre> <p>Handle return_new_row signal by updating the value.</p> Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def return_new_row(self, **argv):\n    \"\"\"Handle return_new_row signal by updating the value.\"\"\"\n    self.set_value(argv[\"id\"], argv[\"title\"])\n</code></pre> <code></code> return_updated_row \u00b6 <pre><code>return_updated_row(**argv)\n</code></pre> <p>Handle return_updated_row signal by updating the value.</p> Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def return_updated_row(self, **argv):\n    \"\"\"Handle return_updated_row signal by updating the value.\"\"\"\n    self.set_value(argv[\"id\"], argv[\"title\"])\n</code></pre> <code></code> set_value \u00b6 <pre><code>set_value(item_id, item_str)\n</code></pre> <p>Set the selected value.</p> <p>Parameters:</p> Name Type Description Default <code>item_id</code> <p>Table row ID (database key).</p> required <code>item_str</code> <p>String representation of the row.</p> required Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def set_value(self, item_id, item_str):\n    \"\"\"Set the selected value.\n\n    Args:\n        item_id: Table row ID (database key).\n        item_str: String representation of the row.\n    \"\"\"\n    if self.multiple:\n        if item_id not in self.item_id:\n            self.item_id.append(item_id)\n            self.item_str.append(item_str)\n        self.SetValue(\"; \".join(self.item_str)[2:])\n    else:\n        self.item_id = [item_id]\n        self.item_str = [item_str]\n        self.SetValue(item_str)\n\n    def _finish():\n        self.SelectNone()\n        self.SetInsertionPointEnd()\n\n    wx.CallAfter(_finish)\n</code></pre>"},{"location":"api/guictrl_popup/#pytigon_gui.guictrl.popup.select2.Select2Popup","title":"Select2Popup","text":"<pre><code>Select2Popup(\n    parent,\n    id,\n    title,\n    pos,\n    size,\n    style,\n    combo,\n    field_id,\n    url=None,\n    minimum_input_length=2,\n)\n</code></pre> <p>               Bases: <code>MiniFrame</code></p> <p>Popup window for Select2-style autocomplete search.</p> <p>Displays a text entry field above a list of matching results. Supports keyboard navigation (arrows, Enter, Escape, Tab).</p> <p>Initialize the Select2 popup.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>id</code> <p>Window ID.</p> required <code>title</code> <p>Window title.</p> required <code>pos</code> <p>Screen position.</p> required <code>size</code> <p>Window size.</p> required <code>style</code> <p>Window style flags.</p> required <code>combo</code> <p>The parent Select2Base control.</p> required <code>field_id</code> <p>Django field identifier for the AJAX endpoint.</p> required <code>url</code> <p>Optional override URL for the autocomplete endpoint.</p> <code>None</code> <code>minimum_input_length</code> <p>Minimum characters before search triggers.</p> <code>2</code> Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def __init__(\n    self,\n    parent,\n    id,\n    title,\n    pos,\n    size,\n    style,\n    combo,\n    field_id,\n    url=None,\n    minimum_input_length=2,\n):\n    \"\"\"Initialize the Select2 popup.\n\n    Args:\n        parent: Parent window.\n        id: Window ID.\n        title: Window title.\n        pos: Screen position.\n        size: Window size.\n        style: Window style flags.\n        combo: The parent Select2Base control.\n        field_id: Django field identifier for the AJAX endpoint.\n        url: Optional override URL for the autocomplete endpoint.\n        minimum_input_length: Minimum characters before search triggers.\n    \"\"\"\n    from pytigon_gui.guiframe.page import SchPage\n\n    self.combo = combo\n    self.point = pos\n    self.field_id = field_id\n    self.url = url\n    self.minimum_input_length = minimum_input_length if minimum_input_length else 0\n\n    wx.MiniFrame.__init__(self, parent, id, title, pos, size, wx.RESIZE_BORDER)\n\n    self.edit_ctrl = wx.TextCtrl(\n        self, size=(440, -1), style=wx.TE_PROCESS_ENTER | wx.TE_PROCESS_TAB\n    )\n    self.list_ctrl = ListBoxNoFocus(self, size=(440, 200), style=wx.LB_SINGLE)\n\n    self.edit_ctrl.Bind(wx.EVT_KEY_DOWN, self.on_key_down)\n    self.edit_ctrl.Bind(wx.EVT_TEXT_ENTER, self.on_enter)\n    self.list_ctrl.Bind(wx.EVT_LISTBOX_DCLICK, self.on_enter)\n    self.list_ctrl.Bind(wx.EVT_LISTBOX, self.on_enter)\n\n    self.Bind(wx.EVT_ACTIVATE, self.on_activate)\n    self.Bind(wx.EVT_TEXT, self.on_text)\n\n    box = wx.BoxSizer(wx.VERTICAL)\n    box.Add(self.edit_ctrl)\n    box.Add(self.list_ctrl, 1, wx.ALL | wx.GROW, 1)\n    self.SetSizer(box)\n    self.SetAutoLayout(True)\n    self.Fit()\n</code></pre> Methods:\u00b6 <code></code> Dismiss \u00b6 <pre><code>Dismiss()\n</code></pre> <p>Hide the popup and return focus to the combo control.</p> Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def Dismiss(self):\n    \"\"\"Hide the popup and return focus to the combo control.\"\"\"\n    self.Hide()\n    self.combo.SetFocus()\n</code></pre> <code></code> Hide \u00b6 <pre><code>Hide()\n</code></pre> <p>Notify the parent combo and hide the window.</p> Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def Hide(self):\n    \"\"\"Notify the parent combo and hide the window.\"\"\"\n    self.combo.on_popup_hidden()\n    super().Hide()\n</code></pre> <code></code> Popup \u00b6 <pre><code>Popup()\n</code></pre> <p>Show the popup at the stored position.</p> Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def Popup(self):\n    \"\"\"Show the popup at the stored position.\"\"\"\n    self.Show()\n    self.Move(self.point)\n</code></pre> <code></code> clear \u00b6 <pre><code>clear()\n</code></pre> <p>Clear the search field and results list.</p> Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def clear(self):\n    \"\"\"Clear the search field and results list.\"\"\"\n    self.edit_ctrl.ChangeValue(\"\")\n    self.list_ctrl.Clear()\n</code></pre> <code></code> on_activate \u00b6 <pre><code>on_activate(event)\n</code></pre> <p>Hide popup and return focus when deactivated.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>The wx.ActivateEvent.</p> required Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def on_activate(self, event):\n    \"\"\"Hide popup and return focus when deactivated.\n\n    Args:\n        event: The wx.ActivateEvent.\n    \"\"\"\n    if not event.GetActive():\n        self.Hide()\n        self.combo.SetFocus()\n    event.Skip()\n</code></pre> <code></code> on_enter \u00b6 <pre><code>on_enter(event)\n</code></pre> <p>Handle Enter/DoubleClick on a list item: dismiss and set value.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>The wx event.</p> required Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def on_enter(self, event):\n    \"\"\"Handle Enter/DoubleClick on a list item: dismiss and set value.\n\n    Args:\n        event: The wx event.\n    \"\"\"\n    sel = self.list_ctrl.GetSelection()\n    if sel != wx.NOT_FOUND:\n        self.Dismiss()\n        item_id = self.list_ctrl.GetClientData(sel)\n        item_str = self.list_ctrl.GetString(sel)\n        self.combo.set_value(item_id, item_str)\n</code></pre> <code></code> on_key_down \u00b6 <pre><code>on_key_down(event)\n</code></pre> <p>Handle keyboard navigation within the popup.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>wx.KeyEvent.</p> required Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def on_key_down(self, event):\n    \"\"\"Handle keyboard navigation within the popup.\n\n    Args:\n        event: wx.KeyEvent.\n    \"\"\"\n    if event.KeyCode == wx.WXK_ESCAPE:\n        self.Dismiss()\n    elif event.KeyCode == wx.WXK_DOWN or (\n        event.AltDown() and event.KeyCode == ord(\"J\")\n    ):\n        sel = self.list_ctrl.GetSelection()\n        if sel != wx.NOT_FOUND and sel &lt; self.list_ctrl.GetCount() - 1:\n            self.list_ctrl.SetSelection(sel + 1)\n    elif event.KeyCode == wx.WXK_UP or (\n        event.AltDown() and event.KeyCode == ord(\"K\")\n    ):\n        sel = self.list_ctrl.GetSelection()\n        if sel != wx.NOT_FOUND and sel &gt; 0:\n            self.list_ctrl.SetSelection(sel - 1)\n    elif event.KeyCode == wx.WXK_TAB:\n        return self.on_enter(event)\n    else:\n        event.Skip()\n</code></pre> <code></code> on_text \u00b6 <pre><code>on_text(event)\n</code></pre> <p>Handle text input: query the server for autocomplete results.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>wx.CommandEvent with the text string.</p> required Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def on_text(self, event):\n    \"\"\"Handle text input: query the server for autocomplete results.\n\n    Args:\n        event: wx.CommandEvent with the text string.\n    \"\"\"\n    event.Skip()\n    s = event.GetString()\n    if len(s) &lt; self.minimum_input_length:\n        return\n\n    base_url = self.url if self.url else \"/select2/fields/auto.json\"\n    url = f\"{base_url}?term={s}&amp;page=1&amp;context=&amp;field_id={self.field_id}\"\n\n    try:\n        http = wx.GetApp().get_http(self.combo)\n        response = http.get(self, url)\n        tab = schjson.loads(response.str())\n    except Exception:\n        logger.warning(\"Failed to load select2 data from: %s\", url, exc_info=True)\n        return\n\n    err = tab.get(\"err\")\n    if err is None or err == \"nil\":\n        self.list_ctrl.Clear()\n        if len(tab[\"results\"]) &gt; 0:\n            for pos in tab[\"results\"]:\n                self.list_ctrl.Append(pos[\"text\"], pos[\"id\"])\n            if s:\n                self.list_ctrl.SetSelection(0)\n</code></pre> <code></code> set_position \u00b6 <pre><code>set_position(point)\n</code></pre> <p>Store the target screen position for the popup.</p> <p>Parameters:</p> Name Type Description Default <code>point</code> <p>(x, y) tuple in screen coordinates.</p> required Source code in <code>pytigon_gui/guictrl/popup/select2.py</code> <pre><code>def set_position(self, point):\n    \"\"\"Store the target screen position for the popup.\n\n    Args:\n        point: (x, y) tuple in screen coordinates.\n    \"\"\"\n    self.point = point\n</code></pre>"},{"location":"api/guictrl_tag/","title":"guictrl.tag \u2013 Tag Parser","text":"<p>Parsing infrastructure for the XML-like UI tag language used throughout Pytigon GUI.</p>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag","title":"tag","text":"<p>Backward-compatibility shim for pytigon_gui.guictrl.tag.</p> <p>This module re-exports all tag parser classes and preprocess functions from the refactored submodules. The actual implementations now live in:</p> <pre><code>guictrl/tag_parsers.py     - TreeList, TreeUl, TreeLi, Data,\n                             OptionTag, CompositeChildTag\nguictrl/tag_ctrltag.py     - CtrlTag, ComponentTag + register_tag_map calls\nguictrl/tag_preprocess.py  - All preprocess functions + register calls\n</code></pre> <p>Importing this module triggers all register_tag_map and register_tag_preprocess_map calls needed by the HTML parser.</p>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag-attributes","title":"Attributes","text":""},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.HIDDEN_DIVS","title":"HIDDEN_DIVS  <code>module-attribute</code>","text":"<pre><code>HIDDEN_DIVS = ['ajax-region', 'td_information', 'td_action']\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.SCHTYPE_MAP","title":"SCHTYPE_MAP  <code>module-attribute</code>","text":"<pre><code>SCHTYPE_MAP = {\n    \"FloatField\": \"float\",\n    \"IntegerField\": \"num\",\n    \"DecimalField\": \"amount\",\n    \"TimeField\": \"time\",\n    \"DateTimeField\": \"datetimepicker\",\n}\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag._ATTRIBUTES","title":"_ATTRIBUTES  <code>module-attribute</code>","text":"<pre><code>_ATTRIBUTES = (\n    \"name\",\n    \"href\",\n    \"label\",\n    \"target\",\n    \"valuetype\",\n    \"defaultvalue\",\n    \"length\",\n    \"maxlength\",\n    \"readonly\",\n    \"src\",\n    \"param\",\n    \"fields\",\n    \"hidden\",\n    \"checked\",\n    \"style\",\n    \"id\",\n    \"onload\",\n)\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag-classes","title":"Classes","text":""},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.ComponentTag","title":"ComponentTag","text":"<pre><code>ComponentTag(parent, parser, tag, attrs)\n</code></pre> <p>               Bases: <code>CtrlTag</code></p> <p>Handler for _component tags with nested child tracking.</p> <p>Extends CtrlTag to support web components that contain nested HTML. Tracks tag depth via a counter and manages the close_tag for proper nesting.</p> <p>Window dimensions from 'window-width' and 'window-height' attributes are forwarded as 'width' and 'height'.</p> <p>Initialize the component tag handler.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent HTML element parser.</p> required <code>parser</code> <p>The HTML parser instance.</p> required <code>tag</code> <p>Tag name.</p> required <code>attrs</code> <p>Tag attributes dict.</p> required Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def __init__(self, parent, parser, tag, attrs):\n    \"\"\"Initialize the component tag handler.\n\n    Args:\n        parent: Parent HTML element parser.\n        parser: The HTML parser instance.\n        tag: Tag name.\n        attrs: Tag attributes dict.\n    \"\"\"\n    super().__init__(parent, parser, tag, attrs)\n    self.data.append(make_start_tag(attrs[\"_tag\"], attrs))\n    self.count = 1\n    self._close_tag = None\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.ComponentTag-functions","title":"Methods:","text":""},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.ComponentTag.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Close the component tag, forwarding window dimensions.</p> <p>Returns:</p> Type Description <p>Result from parent close().</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def close(self):\n    \"\"\"Close the component tag, forwarding window dimensions.\n\n    Returns:\n        Result from parent close().\n    \"\"\"\n    if \"window-width\" in self.attrs:\n        self.attrs[\"width\"] = self.attrs[\"window-width\"]\n    if \"window-height\" in self.attrs:\n        self.attrs[\"height\"] = self.attrs[\"window-height\"]\n    return super().close()\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.ComponentTag.handle_endtag","title":"handle_endtag","text":"<pre><code>handle_endtag(tag)\n</code></pre> <p>Handle nested end tags with depth tracking.</p> <p>Decrements the counter. When it reaches zero, restores the original close_tag and delegates to the parent.</p> <p>Parameters:</p> Name Type Description Default <code>tag</code> <p>The closing tag name.</p> required <p>Returns:</p> Type Description <p>self while nested, or parent result when complete.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def handle_endtag(self, tag):\n    \"\"\"Handle nested end tags with depth tracking.\n\n    Decrements the counter. When it reaches zero, restores\n    the original close_tag and delegates to the parent.\n\n    Args:\n        tag: The closing tag name.\n\n    Returns:\n        self while nested, or parent result when complete.\n    \"\"\"\n    self.data.append(\"&lt;/\" + tag + \"&gt;\")\n    self.count -= 1\n    if self.count == 0:\n        if self._close_tag:\n            self.close_tag = self._close_tag\n        return super().handle_endtag(tag)\n    else:\n        return self\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.ComponentTag.handle_starttag","title":"handle_starttag","text":"<pre><code>handle_starttag(parser, tag, attrs)\n</code></pre> <p>Handle nested start tags within component.</p> <p>Uses _tag attribute for the actual tag name if present. Increments the depth counter and returns self for continued nesting.</p> <p>Parameters:</p> Name Type Description Default <code>parser</code> <p>The HTML parser.</p> required <code>tag</code> <p>Tag name.</p> required <code>attrs</code> <p>Tag attributes dict.</p> required <p>Returns:</p> Type Description <p>self to continue nesting.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def handle_starttag(self, parser, tag, attrs):\n    \"\"\"Handle nested start tags within component.\n\n    Uses _tag attribute for the actual tag name if present.\n    Increments the depth counter and returns self for\n    continued nesting.\n\n    Args:\n        parser: The HTML parser.\n        tag: Tag name.\n        attrs: Tag attributes dict.\n\n    Returns:\n        self to continue nesting.\n    \"\"\"\n    tag2 = attrs.get(\"_tag\", tag)\n    self.data.append(make_start_tag(tag2, attrs))\n    if not self._close_tag:\n        self._close_tag = self.close_tag\n    self.count += 1\n    return self\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.CompositeChildTag","title":"CompositeChildTag","text":"<pre><code>CompositeChildTag(parent, parser, tag, attrs)\n</code></pre> <p>               Bases: <code>BaseHtmlElemParser</code></p> <p>Parser for nested tags within ctrl-composite.</p> <p>Builds a composite_data dict tree that mirrors the HTML structure inside a composite control. Each nested tag becomes a child node with tag, attrs, data, and children.</p> <p>Initialize composite child tag parser.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser (CompositeChildTag or CtrlTag).</p> required <code>parser</code> <p>The HTML parser.</p> required <code>tag</code> <p>Tag name.</p> required <code>attrs</code> <p>Tag attributes dict.</p> required Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def __init__(self, parent, parser, tag, attrs):\n    \"\"\"Initialize composite child tag parser.\n\n    Args:\n        parent: Parent parser (CompositeChildTag or CtrlTag).\n        parser: The HTML parser.\n        tag: Tag name.\n        attrs: Tag attributes dict.\n    \"\"\"\n    BaseHtmlElemParser.__init__(self, parent, parser, tag, attrs)\n    self.composite_data = {\n        \"tag\": tag,\n        \"attrs\": attrs,\n        \"data\": None,\n        \"children\": [],\n    }\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.CompositeChildTag-functions","title":"Methods:","text":""},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.CompositeChildTag.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>On close, attach composite data to parent.</p> Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def close(self):\n    \"\"\"On close, attach composite data to parent.\"\"\"\n    if self.data:\n        self.composite_data[\"data\"] = self.data\n    if isinstance(self.parent, CompositeChildTag):\n        self.parent.composite_data[\"children\"].append(self.composite_data)\n    elif isinstance(self.parent, CtrlTag):\n        self.parent.data2.append(self.composite_data)\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.CompositeChildTag.handle_starttag","title":"handle_starttag","text":"<pre><code>handle_starttag(parser, tag, attrs)\n</code></pre> <p>Handle nested tags within composite.</p> <p>Parameters:</p> Name Type Description Default <code>parser</code> <p>The HTML parser.</p> required <code>tag</code> <p>Tag name.</p> required <code>attrs</code> <p>Tag attributes dict.</p> required <p>Returns:</p> Type Description <p>A new CompositeChildTag for the nested tag.</p> Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def handle_starttag(self, parser, tag, attrs):\n    \"\"\"Handle nested tags within composite.\n\n    Args:\n        parser: The HTML parser.\n        tag: Tag name.\n        attrs: Tag attributes dict.\n\n    Returns:\n        A new CompositeChildTag for the nested tag.\n    \"\"\"\n    return CompositeChildTag(self, parser, tag, attrs)\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.CtrlTag","title":"CtrlTag","text":"<pre><code>CtrlTag(parent, parser, tag, attrs)\n</code></pre> <p>               Bases: <code>TableTag</code></p> <p>Handler for all tags starting with 'ctrl-'.</p> <p>Parses the HTML tag attributes, collects child data (tdata, ldata), calculates relative sizes, and creates the corresponding wxPython widget via schctrl (pytigon_gui.guictrl.ctrl).</p> Lifecycle <ol> <li>init - Parse attributes, calculate sizes</li> <li>Child tags populate tdata/ldata via helper parsers</li> <li>close() - Build kwargs, create or update widget</li> <li>render() - Position and size the widget in the layout</li> </ol> <p>Initialize the ctrl tag handler.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent HTML element parser.</p> required <code>parser</code> <p>The HTML parser instance.</p> required <code>tag</code> <p>Full tag name (e.g. 'ctrl-text', 'ctrl-button').</p> required <code>attrs</code> <p>Tag attributes dict.</p> required Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def __init__(self, parent, parser, tag, attrs):\n    \"\"\"Initialize the ctrl tag handler.\n\n    Args:\n        parent: Parent HTML element parser.\n        parser: The HTML parser instance.\n        tag: Full tag name (e.g. 'ctrl-text', 'ctrl-button').\n        attrs: Tag attributes dict.\n    \"\"\"\n    TableTag.__init__(self, parent, parser, tag, attrs)\n\n    self.list = None\n    self.child_tags += [\"ul\", \"li\", \"data\", \"option\", \"div\"]\n    self.subtab = True\n    self.kwargs = {}\n    self.classObj = None\n    self.data2 = None\n    self.obj = None\n    self.width_best = -1\n    self.width_min = -1\n    self.width_max = -1\n    self.tdata = []\n\n    if self.parser.parse_only:\n        return\n    self._calc_relative_size()\n\n    # Initialize composite data storage\n    if self.tag == \"ctrl-composite\":\n        self.data2 = []\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.CtrlTag-functions","title":"Methods:","text":""},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.CtrlTag.append_to_tree","title":"append_to_tree","text":"<pre><code>append_to_tree(elem)\n</code></pre> <p>Append a tree element (from <ul>/<li> parsing). <p>Creates a TreeList on first call.</p> <p>Parameters:</p> Name Type Description Default <code>elem</code> <p>A tree data element.</p> required Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def append_to_tree(self, elem):\n    \"\"\"Append a tree element (from &lt;ul&gt;/&lt;li&gt; parsing).\n\n    Creates a TreeList on first call.\n\n    Args:\n        elem: A tree data element.\n    \"\"\"\n    from pytigon_gui.guictrl.tag_parsers import TreeList\n\n    if not self.list:\n        self.list = TreeList()\n    self.list.append_to_tree(elem)\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.CtrlTag.calc_col_sizes","title":"calc_col_sizes","text":"<pre><code>calc_col_sizes()\n</code></pre> <p>Calculate column sizes for table layout.</p> <p>Falls back to width 400 if best size is negative.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def calc_col_sizes(self):\n    \"\"\"Calculate column sizes for table layout.\n\n    Falls back to width 400 if best size is negative.\n    \"\"\"\n    sizes = self.calc_width()\n    if sizes[0] &gt;= 0:\n        self.width = sizes[0] + self.extra_space[0] + self.extra_space[1]\n    else:\n        self.width = 400\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.CtrlTag.calc_height","title":"calc_height","text":"<pre><code>calc_height()\n</code></pre> <p>Calculate height from widget best size.</p> <p>Returns:</p> Type Description <p>Integer height including extra space.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def calc_height(self):\n    \"\"\"Calculate height from widget best size.\n\n    Returns:\n        Integer height including extra space.\n    \"\"\"\n    if self.height &gt;= 0:\n        return self.height\n    if self.obj:\n        self.width_best, self.height_best = self.obj.GetBestSize()\n    else:\n        self.height_best = 30\n    self.height = self.height_best + self.extra_space[2] + self.extra_space[3]\n    return self.height\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.CtrlTag.calc_width","title":"calc_width","text":"<pre><code>calc_width()\n</code></pre> <p>Calculate width from widget best/min/max sizes.</p> <p>Returns:</p> Type Description <p>Tuple of (best_width, min_width, max_width).</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def calc_width(self):\n    \"\"\"Calculate width from widget best/min/max sizes.\n\n    Returns:\n        Tuple of (best_width, min_width, max_width).\n    \"\"\"\n    d = self.extra_space[0] + self.extra_space[1]\n    if self.width &lt; 0:\n        if self.obj:\n            self.width_best, self.height_best = self.obj.GetBestSize()\n            self.width_min, height_min = self.obj.GetMinSize()\n            self.width_max, height_max = self.obj.GetMaxSize()\n            return (\n                self.width_best + d,\n                self.width_min + d,\n                self.width_max + d,\n            )\n        else:\n            return (d, d, d)\n    else:\n        return (self.width, self.width, self.width)\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.CtrlTag.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Finalize tag parsing and create/update the widget.</p> <p>In parse_only mode, registers tdata for later use. Otherwise, calls handle_ctrl to instantiate the widget.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def close(self):\n    \"\"\"Finalize tag parsing and create/update the widget.\n\n    In parse_only mode, registers tdata for later use.\n    Otherwise, calls handle_ctrl to instantiate the widget.\n    \"\"\"\n    self._append_to_tdata()\n    if self.parser.parse_only:\n        if hasattr(self.parser, \"register_tdata\"):\n            self.parser.register_tdata(self.tdata, self.tag, self.attrs)\n    else:\n        self.handle_ctrl(self.tag)\n    return TableTag.close(self)\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.CtrlTag.draw_atom","title":"draw_atom","text":"<pre><code>draw_atom(dc, style, x, y, dx, dy)\n</code></pre> <p>Draw the widget atomically at the given position.</p> <p>Parameters:</p> Name Type Description Default <code>dc</code> <p>Device context.</p> required <code>style</code> <p>Drawing style (unused).</p> required <code>x, y</code> <p>Position.</p> required <code>dx, dy</code> <p>Size hints (unused).</p> required Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def draw_atom(self, dc, style, x, y, dx, dy):\n    \"\"\"Draw the widget atomically at the given position.\n\n    Args:\n        dc: Device context.\n        style: Drawing style (unused).\n        x, y: Position.\n        dx, dy: Size hints (unused).\n    \"\"\"\n    dc2 = dc.subdc(x, y, self.width, self.height)\n    cont = True\n    while cont:\n        dy, cont = self.render(dc2)\n        dc2.y += dy\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.CtrlTag.get_context","title":"get_context","text":"<pre><code>get_context()\n</code></pre> <p>Return layout context with button sizes and top offset.</p> <p>Returns:</p> Type Description <p>Dict with button_size_dx, button_size_dy, and top keys.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def get_context(self):\n    \"\"\"Return layout context with button sizes and top offset.\n\n    Returns:\n        Dict with button_size_dx, button_size_dy, and top keys.\n    \"\"\"\n    if self.parent and hasattr(self.parent, \"y\"):\n        _y = self.parent.y + 3\n    else:\n        _y = 1\n    return {\n        \"button_size_dx\": wx.Button.GetDefaultSize()[0] + 14,\n        \"button_size_dy\": wx.Button.GetDefaultSize()[1] + 14,\n        \"top\": _y,\n    }\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.CtrlTag.get_height","title":"get_height","text":"<pre><code>get_height()\n</code></pre> <p>Calculate and return the widget height.</p> <p>Returns:</p> Type Description <p>Integer height value, minimum 200.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def get_height(self):\n    \"\"\"Calculate and return the widget height.\n\n    Returns:\n        Integer height value, minimum 200.\n    \"\"\"\n    height = self.calc_height()\n    if height &gt;= 0:\n        self.height = height + self.extra_space[2] + self.extra_space[3]\n    else:\n        self.height = 200\n    return self.height\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.CtrlTag.get_parent_window","title":"get_parent_window","text":"<pre><code>get_parent_window()\n</code></pre> <p>Get the wxPython parent window for widget creation.</p> <p>Returns:</p> Type Description <p>The parent window from the parser.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def get_parent_window(self):\n    \"\"\"Get the wxPython parent window for widget creation.\n\n    Returns:\n        The parent window from the parser.\n    \"\"\"\n    return self.parser.get_parent_window()\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.CtrlTag.handle_ctrl","title":"handle_ctrl","text":"<pre><code>handle_ctrl(tag)\n</code></pre> <p>Create or update the wxPython widget for this ctrl tag.</p> <p>This is the main dispatch method that: 1. Resolves the widget name (with deduplication) 2. Builds kwargs from tag attributes 3. Looks up the widget class from schctrl 4. Pops an existing widget or creates a new one 5. Sets the initial value and triggers after_create</p> <p>Parameters:</p> Name Type Description Default <code>tag</code> <p>The ctrl tag name (e.g. 'ctrl-text').</p> required Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def handle_ctrl(self, tag):\n    \"\"\"Create or update the wxPython widget for this ctrl tag.\n\n    This is the main dispatch method that:\n    1. Resolves the widget name (with deduplication)\n    2. Builds kwargs from tag attributes\n    3. Looks up the widget class from schctrl\n    4. Pops an existing widget or creates a new one\n    5. Sets the initial value and triggers after_create\n\n    Args:\n        tag: The ctrl tag name (e.g. 'ctrl-text').\n    \"\"\"\n    # Resolve widget name\n    if \"name\" in self.attrs:\n        name_reg = is_null(self.attrs[\"name\"], \"\")\n        parent = self.parent\n        while parent:\n            if hasattr(parent, \"reg_field\"):\n                name_reg = parent.reg_field(self.attrs[\"name\"])\n                break\n            parent = parent.parent\n        name = name_reg\n    else:\n        name = tag[4:]\n    self.attrs[\"name\"] = name\n\n    parent = self.get_parent_window()\n    if parent:\n        gparent = parent.GetParent()\n        tmp = name\n        i = 1\n        while gparent.test_ctrl(tmp):\n            tmp = name + \"__\" + str(i)\n            i += 1\n        name = tmp\n\n    # Build kwargs from attributes\n    for atrybut in _ATTRIBUTES:\n        if atrybut in self.attrs:\n            attr_val = self.attrs[atrybut]\n            self.kwargs[atrybut] = \"\" if attr_val is None else attr_val\n\n    # Resolve value\n    valuetype = \"data\"\n    if \"valuetype\" in self.attrs:\n        if self.attrs[\"valuetype\"] == \"ref\":\n            valuetype = \"ref\"\n        if self.attrs[\"valuetype\"] == \"str\":\n            valuetype = \"str\"\n    if \"value\" in self.attrs:\n        if valuetype == \"str\":\n            value = unquote(self.attrs[\"value\"])\n        else:\n            try:\n                value = ast.literal_eval(self.attrs[\"value\"])\n            except (ValueError, SyntaxError):\n                logger.warning(\"Value error: %s\", self.attrs[\"value\"])\n                value = None\n    else:\n        value = None\n    if \"strvalue\" in self.attrs:\n        value = self.attrs[\"strvalue\"]\n\n    # Set size from calculated dimensions\n    self.kwargs[\"size\"] = wx.Size(\n        max(0, int((self.width - self.extra_space[0]) - self.extra_space[1])),\n        max(0, int((self.height - self.extra_space[2]) - self.extra_space[3])),\n    )\n\n    # Look up widget class by tag name\n    try:\n        if tag.upper().startswith(\"CTRL-\"):\n            self.classObj = getattr(schctrl, tag[5:].upper())\n        elif tag.startswith(\"_\"):\n            self.classObj = getattr(schctrl, tag[1:].upper())\n        else:\n            self.classObj = getattr(schctrl, tag.upper())\n    except AttributeError:\n        return True\n\n    # Attach tdata and ldata to kwargs\n    if len(self.tdata) &gt; 0:\n        self.kwargs[\"tdata\"] = self.tdata\n    self.kwargs[\"param\"] = dict(self.attrs)\n    self.kwargs[\"param\"][\"table_lp\"] = str(self.parser.table_lp)\n    self.kwargs[\"param\"][\"tag\"] = tag.lower()\n    if self.data2 is not None:\n        self.kwargs[\"param\"][\"data\"] = is_null(self.data2, \"\")\n    else:\n        self.kwargs[\"param\"][\"data\"] = \"\".join(self.data)\n    if self.list:\n        l = self.list.get_list()\n        self.kwargs[\"ldata\"] = l\n\n    # Create or update widget\n    if parent:\n        gparent = parent.GetParent()\n        obj = gparent.pop_ctrl(name)\n        if obj:\n            if parent.update_controls:\n                if hasattr(obj, \"process_refr_data\"):\n                    obj.process_refr_data(**self.kwargs)\n                elif not (hasattr(obj, \"is_ctrl_block\") and obj.is_ctrl_block()):\n                    obj.SetValue(value)\n            parent.append_ctrl(obj)\n        else:\n            obj = self.classObj(parent, **self.kwargs)\n            if not obj:\n                logger.error(\"ERROR: %s\", self.classObj)\n            obj.set_unique_name(name)\n            if value is not None and valuetype in (\"data\", \"str\"):\n                obj.SetValue(value)\n            obj.after_create()\n            parent.append_ctrl(obj)\n        self.obj = obj\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.CtrlTag.handle_data","title":"handle_data","text":"<pre><code>handle_data(data)\n</code></pre> <p>Process raw text data.</p> <p>For ctrl-checkbox, text data is suppressed.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <p>Raw text data.</p> required <p>Returns:</p> Type Description <p>Result from parent handle_data.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def handle_data(self, data):\n    \"\"\"Process raw text data.\n\n    For ctrl-checkbox, text data is suppressed.\n\n    Args:\n        data: Raw text data.\n\n    Returns:\n        Result from parent handle_data.\n    \"\"\"\n    if self.tag == \"ctrl-checkbox\":\n        pass\n    return BaseHtmlElemParser.handle_data(self, data)\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.CtrlTag.handle_starttag","title":"handle_starttag","text":"<pre><code>handle_starttag(parser, tag, attrs)\n</code></pre> <p>Dispatch child tag to the appropriate parser.</p> <p>Routes <ul>, <li>, ,  to their helper parsers. For ctrl-composite, delegates to CompositeChildTag. <p>Parameters:</p> Name Type Description Default <code>parser</code> <p>The HTML parser.</p> required <code>tag</code> <p>Child tag name.</p> required <code>attrs</code> <p>Child tag attributes.</p> required <p>Returns:</p> Type Description <p>A parser instance for the child tag, or None.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def handle_starttag(self, parser, tag, attrs):\n    \"\"\"Dispatch child tag to the appropriate parser.\n\n    Routes &lt;ul&gt;, &lt;li&gt;, &lt;data&gt;, &lt;option&gt; to their helper parsers.\n    For ctrl-composite, delegates to CompositeChildTag.\n\n    Args:\n        parser: The HTML parser.\n        tag: Child tag name.\n        attrs: Child tag attributes.\n\n    Returns:\n        A parser instance for the child tag, or None.\n    \"\"\"\n    from pytigon_gui.guictrl.tag_parsers import (\n        TreeUl,\n        TreeLi,\n        Data,\n        OptionTag,\n        CompositeChildTag,\n    )\n\n    if self.tag == \"ctrl-checkbox\":\n        pass\n\n    if self.tag == \"ctrl-composite\":\n        return CompositeChildTag(self, parser, tag, attrs)\n\n    if tag[:3] + \"*\" in self.child_tags:\n        return self.class_from_tag_name(tag[:3] + \"*\")(self, parser, tag, attrs)\n    elif tag == \"ul\":\n        return TreeUl(self, parser, tag, attrs)\n    elif tag == \"li\":\n        return TreeLi(self, parser, tag, attrs)\n    elif tag == \"data\":\n        return Data(self, parser, tag, attrs)\n    elif tag == \"option\":\n        return OptionTag(self, parser, tag, attrs)\n    elif tag in self.child_tags:\n        o = self.class_from_tag_name(tag)\n        if o:\n            return o(self, parser, tag, attrs)\n        return None\n    return None\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.CtrlTag.render","title":"render","text":"<pre><code>render(dc)\n</code></pre> <p>Position and size the widget within the layout.</p> <p>Parameters:</p> Name Type Description Default <code>dc</code> <p>Device context for rendering.</p> required <p>Returns:</p> Type Description <p>Tuple of (height, False) for layout continuation.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def render(self, dc):\n    \"\"\"Position and size the widget within the layout.\n\n    Args:\n        dc: Device context for rendering.\n\n    Returns:\n        Tuple of (height, False) for layout continuation.\n    \"\"\"\n    if dc.rec and self.obj:\n        self.obj.SetSize(\n            int(dc.x + self.extra_space[0]),\n            int(dc.y + self.extra_space[2]),\n            int((self.width - self.extra_space[0]) - self.extra_space[1]),\n            int((self.height - self.extra_space[2]) - self.extra_space[3]),\n        )\n    return (self.height, False)\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.Data","title":"Data","text":"<pre><code>Data(parent, parser, tag, attrs)\n</code></pre> <p>               Bases: <code>BaseHtmlElemParser</code></p> <p>Parser for  tags with base64-encoded content. <p>Decodes base64 content and stores it in the parent's data2 attribute for use by CtrlTag.</p> Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def __init__(self, parent, parser, tag, attrs):\n    BaseHtmlElemParser.__init__(self, parent, parser, tag, attrs)\n    self.data = None\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.Data-functions","title":"Methods:","text":""},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.Data.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Decode base64 data and store in parent.data2.</p> Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def close(self):\n    \"\"\"Decode base64 data and store in parent.data2.\"\"\"\n    if self.data:\n        self.parent.data2 = b64decode(self.data.encode(\"utf-8\")).decode(\"utf-8\")\n    else:\n        self.parent.data2 = \"\"\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.Data.handle_data","title":"handle_data","text":"<pre><code>handle_data(data)\n</code></pre> <p>Accumulate text data, stripping trailing whitespace.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <p>Raw text data chunk.</p> required Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def handle_data(self, data):\n    \"\"\"Accumulate text data, stripping trailing whitespace.\n\n    Args:\n        data: Raw text data chunk.\n    \"\"\"\n    if self.data:\n        self.data += data.rstrip(\" \\n\")\n    else:\n        self.data = data.rstrip(\" \\n\")\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.OptionTag","title":"OptionTag","text":"<pre><code>OptionTag(parent, parser, tag, attrs)\n</code></pre> <p>               Bases: <code>BaseHtmlElemParser</code></p> <p>Parser for  tags that populate tdata. <p>Collects option text and attributes into Td objects for use by choice/select widgets.</p> Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def __init__(self, parent, parser, tag, attrs):\n    BaseHtmlElemParser.__init__(self, parent, parser, tag, attrs)\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.OptionTag-functions","title":"Methods:","text":""},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.OptionTag.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>On close, create a Td row and append to parent.tdata.</p> Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def close(self):\n    \"\"\"On close, create a Td row and append to parent.tdata.\"\"\"\n    if self.data and len(self.data) &gt; 0:\n        data = \"\".join(self.data).replace(\"\\n\", \"\").replace(\"\\r\", \"\").strip()\n        self.parent.tdata.append([Td(data, self.attrs)])\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.TreeLi","title":"TreeLi","text":"<pre><code>TreeLi(parent, parser, tag, attrs)\n</code></pre> <p>               Bases: <code>BaseHtmlElemParser</code></p> <p>Parser for <li> tags within tree structures. <p>Collects text content and optional nested <ul> children, producing a [label, children, attrs] row. If the <li> contains an  or , its href is captured in the attrs. Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def __init__(self, parent, parser, tag, attrs):\n    BaseHtmlElemParser.__init__(self, parent, parser, tag, attrs)\n    self.child = None\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.TreeLi-functions","title":"Methods:","text":""},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.TreeLi.append_to_tree","title":"append_to_tree","text":"<pre><code>append_to_tree(elem)\n</code></pre> <p>Store a nested tree element as a pending child.</p> <p>Parameters:</p> Name Type Description Default <code>elem</code> <p>A child tree element.</p> required Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def append_to_tree(self, elem):\n    \"\"\"Store a nested tree element as a pending child.\n\n    Args:\n        elem: A child tree element.\n    \"\"\"\n    self.child = elem\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.TreeLi.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>On close, push the collected data as a tree row.</p> Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def close(self):\n    \"\"\"On close, push the collected data as a tree row.\"\"\"\n    if self.child:\n        self.child[0] = superstrip(\"\".join(self.data))\n        self.child[2] = self.attrs\n        self.parent.append_to_tree(self.child)\n    else:\n        self.parent.append_to_tree([superstrip(\"\".join(self.data)), [], self.attrs])\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.TreeLi.handle_starttag","title":"handle_starttag","text":"<pre><code>handle_starttag(parser, tag, attrs)\n</code></pre> <p>Handle nested tags.</p> <ul> starts a new sub-tree.  or  captures href into attrs.   <p>Parameters:</p> Name Type Description Default <code>parser</code> <p>The HTML parser.</p> required <code>tag</code> <p>Tag name.</p> required <code>attrs</code> <p>Tag attributes dict.</p> required <p>Returns:</p> Type Description <p>A TreeUl for <ul>, or None. Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def handle_starttag(self, parser, tag, attrs):\n    \"\"\"Handle nested tags.\n\n    &lt;ul&gt; starts a new sub-tree.\n    &lt;a&gt; or &lt;ctrl-button&gt; captures href into attrs.\n\n    Args:\n        parser: The HTML parser.\n        tag: Tag name.\n        attrs: Tag attributes dict.\n\n    Returns:\n        A TreeUl for &lt;ul&gt;, or None.\n    \"\"\"\n    if tag == \"ul\":\n        return TreeUl(self, parser, tag, attrs)\n    elif tag in (\"a\", \"ctrl-button\"):\n        self.attrs[\"href\"] = attrs[\"href\"]\n    return None\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.TreeList","title":"TreeList","text":"<pre><code>TreeList()\n</code></pre> <p>Simple accumulator for tree-structured data.</p> <p>Collects child elements that form a nested tree representation. Used by TreeUl and TreeLi to build hierarchical data for tree widgets (TREE, TREELIST).</p> Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def __init__(self):\n    self.children = []\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.TreeList-functions","title":"Methods:","text":""},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.TreeList.append_to_tree","title":"append_to_tree","text":"<pre><code>append_to_tree(elem)\n</code></pre> <p>Append a child element to the tree.</p> <p>Parameters:</p> Name Type Description Default <code>elem</code> <p>A child element (typically a list/tuple row).</p> required Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def append_to_tree(self, elem):\n    \"\"\"Append a child element to the tree.\n\n    Args:\n        elem: A child element (typically a list/tuple row).\n    \"\"\"\n    self.children.append(elem)\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.TreeList.get_list","title":"get_list","text":"<pre><code>get_list()\n</code></pre> <p>Return the accumulated tree children.</p> <p>Returns:</p> Type Description <p>List of child elements.</p> Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def get_list(self):\n    \"\"\"Return the accumulated tree children.\n\n    Returns:\n        List of child elements.\n    \"\"\"\n    return self.children\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.TreeUl","title":"TreeUl","text":"<pre><code>TreeUl(parent, parser, tag, attrs)\n</code></pre> <p>               Bases: <code>BaseHtmlElemParser</code></p> <p>Parser for <ul> tags that builds tree data structures. <p>Collects <li> children into a nested list suitable for tree widgets (TREE, TREELIST). Each <li> becomes a row of [label, children, attrs]. Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def __init__(self, parent, parser, tag, attrs):\n    BaseHtmlElemParser.__init__(self, parent, parser, tag, attrs)\n    self.child_tags += [\"li\"]\n    self.children = []\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.TreeUl-functions","title":"Methods:","text":""},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.TreeUl.append_to_tree","title":"append_to_tree","text":"<pre><code>append_to_tree(elem)\n</code></pre> <p>Accept a child element into this tree level.</p> <p>Parameters:</p> Name Type Description Default <code>elem</code> <p>A child element to append.</p> required Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def append_to_tree(self, elem):\n    \"\"\"Accept a child element into this tree level.\n\n    Args:\n        elem: A child element to append.\n    \"\"\"\n    self.children.append(elem)\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.TreeUl.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>On close, push the collected children to the parent.</p> Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def close(self):\n    \"\"\"On close, push the collected children to the parent.\"\"\"\n    self.parent.append_to_tree([\"\", self.children, self.attrs])\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.TreeUl.handle_starttag","title":"handle_starttag","text":"<pre><code>handle_starttag(parser, tag, attrs)\n</code></pre> <p>Handle nested <li> tags. <p>Parameters:</p> Name Type Description Default <code>parser</code> <p>The HTML parser.</p> required <code>tag</code> <p>Tag name.</p> required <code>attrs</code> <p>Tag attributes dict.</p> required <p>Returns:</p> Type Description <p>A TreeLi parser for <li>, or None for other tags. Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def handle_starttag(self, parser, tag, attrs):\n    \"\"\"Handle nested &lt;li&gt; tags.\n\n    Args:\n        parser: The HTML parser.\n        tag: Tag name.\n        attrs: Tag attributes dict.\n\n    Returns:\n        A TreeLi parser for &lt;li&gt;, or None for other tags.\n    \"\"\"\n    if tag == \"li\":\n        return TreeLi(self, parser, tag, attrs)\n    return None\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag-functions","title":"Functions:","text":""},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.a_to_button","title":"a_to_button","text":"<pre><code>a_to_button(parent, attrs)\n</code></pre> <p>Convert  tags with button-like classes to ctrl-button. <p>Converts  tags whose class contains 'popup', 'button', 'btn', or 'schbtn' into ctrl-button tags. The 'schbtn' class has special handling: inside a ctrl-table it remains as , otherwise it becomes a ctrl-button. <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser.</p> required <code>attrs</code> <p>Anchor attributes.</p> required <p>Returns:</p> Type Description <p>Tuple of ('ctrl-button', attrs) or ('a', attrs).</p> Source code in <code>pytigon_gui/guictrl/tag_preprocess.py</code> <pre><code>def a_to_button(parent, attrs):\n    \"\"\"Convert &lt;a&gt; tags with button-like classes to ctrl-button.\n\n    Converts &lt;a&gt; tags whose class contains 'popup', 'button', 'btn',\n    or 'schbtn' into ctrl-button tags. The 'schbtn' class has special\n    handling: inside a ctrl-table it remains as &lt;a&gt;, otherwise it\n    becomes a ctrl-button.\n\n    Args:\n        parent: Parent parser.\n        attrs: Anchor attributes.\n\n    Returns:\n        Tuple of ('ctrl-button', attrs) or ('a', attrs).\n    \"\"\"\n    if \"class\" not in attrs:\n        return (\"a\", attrs)\n\n    classes = attrs[\"class\"]\n    is_button_class = any(c in classes for c in (\"popup\", \"button\", \"btn\", \"schbtn\"))\n    if not is_button_class:\n        return (\"a\", attrs)\n\n    if \"schbtn\" in classes:\n        try:\n            if parent.parent.parent.tag == \"ctrl-table\":\n                return (\"ctrl-button\", attrs)\n        except AttributeError:\n            return (\"a\", attrs)\n    else:\n        try:\n            if parent.parent.parent.tag == \"ctrl-table\":\n                return (\"a\", attrs)\n        except AttributeError:\n            pass\n        return (\"ctrl-button\", attrs)\n\n    return (\"a\", attrs)\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.component_convert","title":"component_convert","text":"<pre><code>component_convert(parent, attrs)\n</code></pre> <p>Convert custom element tags (-) to _component.</p> <p>Used for web component custom elements.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser.</p> required <code>attrs</code> <p>Component attributes.</p> required <p>Returns:</p> Type Description <p>Tuple of ('_component', attrs).</p> Source code in <code>pytigon_gui/guictrl/tag_preprocess.py</code> <pre><code>def component_convert(parent, attrs):\n    \"\"\"Convert custom element tags (*-*) to _component.\n\n    Used for web component custom elements.\n\n    Args:\n        parent: Parent parser.\n        attrs: Component attributes.\n\n    Returns:\n        Tuple of ('_component', attrs).\n    \"\"\"\n    return (\"_component\", attrs)\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.div_convert","title":"div_convert","text":"<pre><code>div_convert(parent, attrs)\n</code></pre> <p>Convert  tags based on CSS class. <p>Mapping: - 'popup'                    \u2192  (unchanged) - 'form-group'               \u2192  - 'controls'                 \u2192  - 'tree'                     \u2192 ctrl-tree - 'select2'                  \u2192 ctrl-composite - 'checkbox'                 \u2192  (adds empty  before) - Classes in HIDDEN_DIVS     \u2192 'none' (removed from output) <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser.</p> required <code>attrs</code> <p>Div attributes.</p> required <p>Returns:</p> Type Description <p>Tuple of (new_tag, attrs).</p> Source code in <code>pytigon_gui/guictrl/tag_preprocess.py</code> <pre><code>def div_convert(parent, attrs):\n    \"\"\"Convert &lt;div&gt; tags based on CSS class.\n\n    Mapping:\n    - 'popup'                    \u2192 &lt;div&gt; (unchanged)\n    - 'form-group'               \u2192 &lt;tr&gt;\n    - 'controls'                 \u2192 &lt;td&gt;\n    - 'tree'                     \u2192 ctrl-tree\n    - 'select2'                  \u2192 ctrl-composite\n    - 'checkbox'                 \u2192 &lt;th&gt; (adds empty &lt;th&gt; before)\n    - Classes in HIDDEN_DIVS     \u2192 'none' (removed from output)\n\n    Args:\n        parent: Parent parser.\n        attrs: Div attributes.\n\n    Returns:\n        Tuple of (new_tag, attrs).\n    \"\"\"\n    if \"class\" not in attrs:\n        return (\"div\", attrs)\n\n    classes = attrs[\"class\"]\n\n    if \"popup\" in classes:\n        return (\"div\", attrs)\n    if \"form-group\" in classes:\n        return (\"tr\", attrs)\n    if \"controls\" in classes:\n        return (\"td\", attrs)\n    if \"tree\" in classes:\n        return (\"ctrl-tree\", attrs)\n    if \"select2\" in classes:\n        return (\"ctrl-composite\", attrs)\n    if \"checkbox\" in classes:\n        # Insert empty &lt;th&gt; before the checkbox cell\n        obj = parent.parent.handle_starttag(parent.parser, \"th\", {})\n        if obj:\n            obj.handle_endtag(\"th\")\n        return (\"th\", attrs)\n\n    for hidden_cls in HIDDEN_DIVS:\n        if hidden_cls in classes:\n            return (\"none\", attrs)\n\n    return (\"div\", attrs)\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.error_p_to_error","title":"error_p_to_error","text":"<pre><code>error_p_to_error(parent, attrs)\n</code></pre> <p>Convert <p> to a comment.</p> <p>Strips error message paragraphs from the output by converting them to comments.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser.</p> required <code>attrs</code> <p>Paragraph attributes.</p> required <p>Returns:</p> Type Description <p>Tuple of ('comment', attrs) or ('p', attrs).</p> Source code in <code>pytigon_gui/guictrl/tag_preprocess.py</code> <pre><code>def error_p_to_error(parent, attrs):\n    \"\"\"Convert &lt;p class=\"help-block\"&gt; to a comment.\n\n    Strips error message paragraphs from the output by converting\n    them to comments.\n\n    Args:\n        parent: Parent parser.\n        attrs: Paragraph attributes.\n\n    Returns:\n        Tuple of ('comment', attrs) or ('p', attrs).\n    \"\"\"\n    if \"class\" in attrs and attrs[\"class\"] == \"help-block\":\n        return (\"comment\", attrs)\n    return (\"p\", attrs)\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.error_span_to_error","title":"error_span_to_error","text":"<pre><code>error_span_to_error(parent, attrs)\n</code></pre> <p>Convert  to ctrl-errorlist. <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser.</p> required <code>attrs</code> <p>Span attributes.</p> required <p>Returns:</p> Type Description <p>Tuple of ('ctrl-errorlist', attrs) or ('span', attrs).</p> Source code in <code>pytigon_gui/guictrl/tag_preprocess.py</code> <pre><code>def error_span_to_error(parent, attrs):\n    \"\"\"Convert &lt;span class=\"help-block\"&gt; to ctrl-errorlist.\n\n    Args:\n        parent: Parent parser.\n        attrs: Span attributes.\n\n    Returns:\n        Tuple of ('ctrl-errorlist', attrs) or ('span', attrs).\n    \"\"\"\n    if \"class\" in attrs and attrs[\"class\"] == \"help-block\":\n        return (\"ctrl-errorlist\", attrs)\n    return (\"span\", attrs)\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.input_to_ctrltab","title":"input_to_ctrltab","text":"<pre><code>input_to_ctrltab(parent, attrs)\n</code></pre> <p>Convert  to the appropriate ctrl-* tag.</p> <p>Maps HTML input types to ctrl widget types based on: - schtype attribute (Django field type \u2192 SCHTYPE_MAP) - type attribute (text, button, email, hidden, password, radio,   checkbox, file, submit) - class attribute (dateinput, datetimeinput) - name attribute (date_ prefix \u2192 datepicker)</p> <p>Also handles size, maxlength, value, and label attribute propagation.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser.</p> required <code>attrs</code> <p>Input tag attributes.</p> required <p>Returns:</p> Type Description <p>Tuple of (new_tag, new_attrs).</p> Source code in <code>pytigon_gui/guictrl/tag_preprocess.py</code> <pre><code>def input_to_ctrltab(parent, attrs):\n    \"\"\"Convert &lt;input&gt; to the appropriate ctrl-* tag.\n\n    Maps HTML input types to ctrl widget types based on:\n    - schtype attribute (Django field type \u2192 SCHTYPE_MAP)\n    - type attribute (text, button, email, hidden, password, radio,\n      checkbox, file, submit)\n    - class attribute (dateinput, datetimeinput)\n    - name attribute (date_ prefix \u2192 datepicker)\n\n    Also handles size, maxlength, value, and label attribute propagation.\n\n    Args:\n        parent: Parent parser.\n        attrs: Input tag attributes.\n\n    Returns:\n        Tuple of (new_tag, new_attrs).\n    \"\"\"\n    attrs_ret = {}\n    ret = \"text\"\n    upload = False\n\n    input_type = attrs.get(\"type\", \"text\")\n    schtype = attrs.get(\"schtype\", \"text\")\n    classname = attrs.get(\"class\", \"text\")\n\n    # Determine widget type from schtype (Django field mapping)\n    if schtype in SCHTYPE_MAP:\n        ret = SCHTYPE_MAP[schtype]\n    elif input_type == \"text\":\n        if classname == \"dateinput\":\n            ret = \"datepicker\"\n        elif classname == \"datetimeinput\":\n            ret = \"datetimepicker\"\n        else:\n            ret = \"text\"\n    elif input_type == \"button\":\n        ret = \"button\"\n    elif input_type == \"email\":\n        ret = \"masktext\"\n        attrs_ret = {\"src\": \"!EMAIL\", \"width\": \"250\", \"valuetype\": \"str\"}\n        if \"value\" in attrs:\n            attrs_ret[\"value\"] = attrs[\"value\"]\n    elif input_type == \"hidden\":\n        ret = \"text\"\n        attrs_ret = {\"hidden\": \"1\", \"width\": \"0\", \"height\": \"0\"}\n    elif input_type == \"password\":\n        ret = \"password\"\n    elif input_type == \"radio\":\n        ret = \"radiobutton\"\n        if \"checked\" in attrs:\n            attrs_ret[\"checked\"] = \"1\"\n        if \"value\" in attrs:\n            attrs_ret[\"value\"] = attrs[\"value\"]\n            attrs_ret[\"valuetype\"] = \"str\"\n    elif input_type == \"checkbox\":\n        ret = \"checkbox\"\n        if \"checked\" in attrs:\n            attrs_ret[\"checked\"] = \"1\"\n        if \"value\" in attrs:\n            attrs_ret[\"value\"] = attrs[\"value\"]\n            attrs_ret[\"valuetype\"] = \"str\"\n    elif input_type == \"file\":\n        ret = \"filebrowsebutton\"\n        upload = True\n    elif input_type == \"submit\":\n        ret = \"button\"\n        target = attrs.get(\"target\", \"_parent_refr\")\n        attrs_ret = {\n            \"defaultvalue\": \"1\",\n            \"param\": \"post\",\n            \"target\": target,\n            \"id\": \"wx.ID_OK\",\n        }\n        href = \".\"\n        fields = None\n        p = parent\n        while p:\n            if hasattr(p, \"gethref\"):\n                href = p.gethref()\n                fields = p.get_fields()\n                if p.get_upload():\n                    attrs_ret[\"valuetype\"] = \"upload\"\n                break\n            p = p.parent\n        if href is None:\n            href = \"\"\n        attrs_ret[\"href\"] = href\n        if fields:\n            attrs_ret[\"fields\"] = fields\n\n    # Width from size attribute\n    if \"size\" in attrs and input_type in (\"text\", \"email\"):\n        width_text = 10 * int(is_null(attrs[\"size\"], 20))\n        if width_text &gt; 400:\n            width_text = 8 * int(is_null(attrs[\"size\"], 20))\n        if width_text &gt; 480:\n            width_text = 480\n        attrs_ret[\"width\"] = str(width_text)\n\n    # Maxlength handling: prefer maxlength, then max_length, then size\n    if \"maxlength\" in attrs and input_type in (\"text\",):\n        attrs_ret[\"maxlength\"] = str(is_null(attrs[\"maxlength\"], \"80\"))\n    elif \"max_length\" in attrs and input_type in (\"text\",):\n        attrs_ret[\"maxlength\"] = str(is_null(attrs[\"max_length\"], \"80\"))\n    elif \"size\" in attrs:\n        attrs_ret[\"maxlength\"] = str(int(is_null(attrs[\"size\"], \"80\")))\n\n    # Value propagation for text-like inputs\n    if \"value\" in attrs and input_type in (\"text\", \"hidden\", \"checkbox\"):\n        attrs_ret[\"value\"] = is_null(attrs[\"value\"], \"\")\n        attrs_ret[\"valuetype\"] = \"str\"\n\n    # Label from value for buttons\n    if \"value\" in attrs and input_type in (\"button\", \"submit\"):\n        attrs_ret[\"label\"] = is_null(attrs[\"value\"], \"\")\n\n    # Checkbox label from title\n    if \"title\" in attrs and input_type == \"checkbox\":\n        attrs_ret[\"label\"] = is_null(attrs[\"title\"], \"\")\n\n    # Upload flag propagation\n    if \"name\" in attrs:\n        p = parent\n        while p:\n            if p.form_obj:\n                if upload:\n                    p.form_obj.set_upload(upload)\n                break\n            p = p.parent\n        attrs_ret[\"name\"] = attrs[\"name\"]\n        # Auto-detect date fields by name prefix\n        if ret == \"text\" and \"date_\" in attrs[\"name\"]:\n            ret = \"datepicker\"\n\n    # Forward remaining attributes not already set\n    for attr in attrs:\n        if attr not in attrs_ret and attr != \"value\":\n            attrs_ret[attr] = attrs[attr]\n\n    return (\"ctrl-\" + ret, attrs_ret)\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.label_to_th","title":"label_to_th","text":"<pre><code>label_to_th(parent, attrs)\n</code></pre> <p>Convert  to  for table-based form layout. <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser.</p> required <code>attrs</code> <p>Label attributes.</p> required <p>Returns:</p> Type Description <p>Tuple of ('th', attrs).</p> Source code in <code>pytigon_gui/guictrl/tag_preprocess.py</code> <pre><code>def label_to_th(parent, attrs):\n    \"\"\"Convert &lt;label&gt; to &lt;th&gt; for table-based form layout.\n\n    Args:\n        parent: Parent parser.\n        attrs: Label attributes.\n\n    Returns:\n        Tuple of ('th', attrs).\n    \"\"\"\n    return (\"th\", attrs)\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.select_to_ctrltab","title":"select_to_ctrltab","text":"<pre><code>select_to_ctrltab(parent, attrs)\n</code></pre> <p>Convert  to ctrl-select. <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser.</p> required <code>attrs</code> <p>Select attributes.</p> required <p>Returns:</p> Type Description <p>Tuple of ('ctrl-select', attrs).</p> Source code in <code>pytigon_gui/guictrl/tag_preprocess.py</code> <pre><code>def select_to_ctrltab(parent, attrs):\n    \"\"\"Convert &lt;select&gt; to ctrl-select.\n\n    Args:\n        parent: Parent parser.\n        attrs: Select attributes.\n\n    Returns:\n        Tuple of ('ctrl-select', attrs).\n    \"\"\"\n    return (\"ctrl-select\", attrs)\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.table_to_ctrltab","title":"table_to_ctrltab","text":"<pre><code>table_to_ctrltab(parent, attrs)\n</code></pre> <p>Convert  to ctrl-table or ctrl-list based on CSS class. <ul> <li>'tabsort' class \u2192 ctrl-table with minheight 60px</li> <li>'listctrl' class \u2192 ctrl-list</li> <li>Otherwise \u2192 unchanged  <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser.</p> required <code>attrs</code> <p>Table tag attributes.</p> required <p>Returns:</p> Type Description <p>Tuple of (new_tag, new_attrs).</p> Source code in <code>pytigon_gui/guictrl/tag_preprocess.py</code> <pre><code>def table_to_ctrltab(parent, attrs):\n    \"\"\"Convert &lt;table&gt; to ctrl-table or ctrl-list based on CSS class.\n\n    - 'tabsort' class \u2192 ctrl-table with minheight 60px\n    - 'listctrl' class \u2192 ctrl-list\n    - Otherwise \u2192 unchanged &lt;table&gt;\n\n    Args:\n        parent: Parent parser.\n        attrs: Table tag attributes.\n\n    Returns:\n        Tuple of (new_tag, new_attrs).\n    \"\"\"\n    if \"class\" in attrs:\n        if \"tabsort\" in attrs[\"class\"]:\n            attrs[\"minheight\"] = \"60px\"\n            return (\"ctrl-table\", attrs)\n        if \"listctrl\" in attrs[\"class\"]:\n            return (\"ctrl-list\", attrs)\n    return (\"table\", attrs)\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.textarea_to_ctrltab","title":"textarea_to_ctrltab","text":"<pre><code>textarea_to_ctrltab(parent, attrs)\n</code></pre> <p>Convert  to ctrl-textarea. <p>Forwards cols, rows, src, and name attributes.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser.</p> required <code>attrs</code> <p>Textarea attributes.</p> required <p>Returns:</p> Type Description <p>Tuple of ('ctrl-textarea', new_attrs).</p> Source code in <code>pytigon_gui/guictrl/tag_preprocess.py</code> <pre><code>def textarea_to_ctrltab(parent, attrs):\n    \"\"\"Convert &lt;textarea&gt; to ctrl-textarea.\n\n    Forwards cols, rows, src, and name attributes.\n\n    Args:\n        parent: Parent parser.\n        attrs: Textarea attributes.\n\n    Returns:\n        Tuple of ('ctrl-textarea', new_attrs).\n    \"\"\"\n    attrs_ret = {}\n    for key in (\"cols\", \"rows\", \"src\", \"name\"):\n        if key in attrs:\n            attrs_ret[key] = attrs[key]\n    return (\"ctrl-textarea\", attrs_ret)\n</code></pre>"},{"location":"api/guictrl_tag/#pytigon_gui.guictrl.tag.ul_convert","title":"ul_convert","text":"<pre><code>ul_convert(parent, attrs)\n</code></pre> <p>Convert <ul> based on CSS class. <ul> <li>'root' class \u2192 ldata (tree data container)</li> <li>'errorlist' class \u2192 ctrl-errorlist</li> </ul> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser.</p> required <code>attrs</code> <p>UL attributes.</p> required <p>Returns:</p> Type Description <p>Tuple of (new_tag, attrs).</p> Source code in <code>pytigon_gui/guictrl/tag_preprocess.py</code> <pre><code>def ul_convert(parent, attrs):\n    \"\"\"Convert &lt;ul&gt; based on CSS class.\n\n    - 'root' class \u2192 ldata (tree data container)\n    - 'errorlist' class \u2192 ctrl-errorlist\n\n    Args:\n        parent: Parent parser.\n        attrs: UL attributes.\n\n    Returns:\n        Tuple of (new_tag, attrs).\n    \"\"\"\n    if \"class\" in attrs:\n        if \"root\" in attrs[\"class\"]:\n            return (\"ldata\", attrs)\n        if attrs[\"class\"] == \"errorlist\":\n            return (\"ctrl-errorlist\", attrs)\n    return (\"ul\", attrs)\n</code></pre>"},{"location":"api/guictrl_tag_ctrltag/","title":"guictrl.tag_ctrltag \u2013 Control Tag Processor","text":"<p>Converts parsed tag definitions into live wxPython control instances with attribute binding, event wiring, and layout management.</p>"},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag","title":"tag_ctrltag","text":"<p>Primary tag handler classes for the SchForm widget system.</p> <p>Converts HTML ctrl-* tags into wxPython widget instances by dispatching to the appropriate widget class from guictrl.ctrl.</p>"},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag-attributes","title":"Attributes","text":""},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag._ATTRIBUTES","title":"_ATTRIBUTES  <code>module-attribute</code>","text":"<pre><code>_ATTRIBUTES = (\n    \"name\",\n    \"href\",\n    \"label\",\n    \"target\",\n    \"valuetype\",\n    \"defaultvalue\",\n    \"length\",\n    \"maxlength\",\n    \"readonly\",\n    \"src\",\n    \"param\",\n    \"fields\",\n    \"hidden\",\n    \"checked\",\n    \"style\",\n    \"id\",\n    \"onload\",\n)\n</code></pre>"},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag.logger","title":"logger  <code>module-attribute</code>","text":"<pre><code>logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag-classes","title":"Classes","text":""},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag.ComponentTag","title":"ComponentTag","text":"<pre><code>ComponentTag(parent, parser, tag, attrs)\n</code></pre> <p>               Bases: <code>CtrlTag</code></p> <p>Handler for _component tags with nested child tracking.</p> <p>Extends CtrlTag to support web components that contain nested HTML. Tracks tag depth via a counter and manages the close_tag for proper nesting.</p> <p>Window dimensions from 'window-width' and 'window-height' attributes are forwarded as 'width' and 'height'.</p> <p>Initialize the component tag handler.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent HTML element parser.</p> required <code>parser</code> <p>The HTML parser instance.</p> required <code>tag</code> <p>Tag name.</p> required <code>attrs</code> <p>Tag attributes dict.</p> required Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def __init__(self, parent, parser, tag, attrs):\n    \"\"\"Initialize the component tag handler.\n\n    Args:\n        parent: Parent HTML element parser.\n        parser: The HTML parser instance.\n        tag: Tag name.\n        attrs: Tag attributes dict.\n    \"\"\"\n    super().__init__(parent, parser, tag, attrs)\n    self.data.append(make_start_tag(attrs[\"_tag\"], attrs))\n    self.count = 1\n    self._close_tag = None\n</code></pre>"},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag.ComponentTag-functions","title":"Methods:","text":""},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag.ComponentTag.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Close the component tag, forwarding window dimensions.</p> <p>Returns:</p> Type Description <p>Result from parent close().</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def close(self):\n    \"\"\"Close the component tag, forwarding window dimensions.\n\n    Returns:\n        Result from parent close().\n    \"\"\"\n    if \"window-width\" in self.attrs:\n        self.attrs[\"width\"] = self.attrs[\"window-width\"]\n    if \"window-height\" in self.attrs:\n        self.attrs[\"height\"] = self.attrs[\"window-height\"]\n    return super().close()\n</code></pre>"},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag.ComponentTag.handle_endtag","title":"handle_endtag","text":"<pre><code>handle_endtag(tag)\n</code></pre> <p>Handle nested end tags with depth tracking.</p> <p>Decrements the counter. When it reaches zero, restores the original close_tag and delegates to the parent.</p> <p>Parameters:</p> Name Type Description Default <code>tag</code> <p>The closing tag name.</p> required <p>Returns:</p> Type Description <p>self while nested, or parent result when complete.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def handle_endtag(self, tag):\n    \"\"\"Handle nested end tags with depth tracking.\n\n    Decrements the counter. When it reaches zero, restores\n    the original close_tag and delegates to the parent.\n\n    Args:\n        tag: The closing tag name.\n\n    Returns:\n        self while nested, or parent result when complete.\n    \"\"\"\n    self.data.append(\"&lt;/\" + tag + \"&gt;\")\n    self.count -= 1\n    if self.count == 0:\n        if self._close_tag:\n            self.close_tag = self._close_tag\n        return super().handle_endtag(tag)\n    else:\n        return self\n</code></pre>"},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag.ComponentTag.handle_starttag","title":"handle_starttag","text":"<pre><code>handle_starttag(parser, tag, attrs)\n</code></pre> <p>Handle nested start tags within component.</p> <p>Uses _tag attribute for the actual tag name if present. Increments the depth counter and returns self for continued nesting.</p> <p>Parameters:</p> Name Type Description Default <code>parser</code> <p>The HTML parser.</p> required <code>tag</code> <p>Tag name.</p> required <code>attrs</code> <p>Tag attributes dict.</p> required <p>Returns:</p> Type Description <p>self to continue nesting.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def handle_starttag(self, parser, tag, attrs):\n    \"\"\"Handle nested start tags within component.\n\n    Uses _tag attribute for the actual tag name if present.\n    Increments the depth counter and returns self for\n    continued nesting.\n\n    Args:\n        parser: The HTML parser.\n        tag: Tag name.\n        attrs: Tag attributes dict.\n\n    Returns:\n        self to continue nesting.\n    \"\"\"\n    tag2 = attrs.get(\"_tag\", tag)\n    self.data.append(make_start_tag(tag2, attrs))\n    if not self._close_tag:\n        self._close_tag = self.close_tag\n    self.count += 1\n    return self\n</code></pre>"},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag.CtrlTag","title":"CtrlTag","text":"<pre><code>CtrlTag(parent, parser, tag, attrs)\n</code></pre> <p>               Bases: <code>TableTag</code></p> <p>Handler for all tags starting with 'ctrl-'.</p> <p>Parses the HTML tag attributes, collects child data (tdata, ldata), calculates relative sizes, and creates the corresponding wxPython widget via schctrl (pytigon_gui.guictrl.ctrl).</p> Lifecycle <ol> <li>init - Parse attributes, calculate sizes</li> <li>Child tags populate tdata/ldata via helper parsers</li> <li>close() - Build kwargs, create or update widget</li> <li>render() - Position and size the widget in the layout</li> </ol> <p>Initialize the ctrl tag handler.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent HTML element parser.</p> required <code>parser</code> <p>The HTML parser instance.</p> required <code>tag</code> <p>Full tag name (e.g. 'ctrl-text', 'ctrl-button').</p> required <code>attrs</code> <p>Tag attributes dict.</p> required Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def __init__(self, parent, parser, tag, attrs):\n    \"\"\"Initialize the ctrl tag handler.\n\n    Args:\n        parent: Parent HTML element parser.\n        parser: The HTML parser instance.\n        tag: Full tag name (e.g. 'ctrl-text', 'ctrl-button').\n        attrs: Tag attributes dict.\n    \"\"\"\n    TableTag.__init__(self, parent, parser, tag, attrs)\n\n    self.list = None\n    self.child_tags += [\"ul\", \"li\", \"data\", \"option\", \"div\"]\n    self.subtab = True\n    self.kwargs = {}\n    self.classObj = None\n    self.data2 = None\n    self.obj = None\n    self.width_best = -1\n    self.width_min = -1\n    self.width_max = -1\n    self.tdata = []\n\n    if self.parser.parse_only:\n        return\n    self._calc_relative_size()\n\n    # Initialize composite data storage\n    if self.tag == \"ctrl-composite\":\n        self.data2 = []\n</code></pre>"},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag.CtrlTag-functions","title":"Methods:","text":""},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag.CtrlTag.append_to_tree","title":"append_to_tree","text":"<pre><code>append_to_tree(elem)\n</code></pre> <p>Append a tree element (from <ul>/<li> parsing). <p>Creates a TreeList on first call.</p> <p>Parameters:</p> Name Type Description Default <code>elem</code> <p>A tree data element.</p> required Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def append_to_tree(self, elem):\n    \"\"\"Append a tree element (from &lt;ul&gt;/&lt;li&gt; parsing).\n\n    Creates a TreeList on first call.\n\n    Args:\n        elem: A tree data element.\n    \"\"\"\n    from pytigon_gui.guictrl.tag_parsers import TreeList\n\n    if not self.list:\n        self.list = TreeList()\n    self.list.append_to_tree(elem)\n</code></pre>"},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag.CtrlTag.calc_col_sizes","title":"calc_col_sizes","text":"<pre><code>calc_col_sizes()\n</code></pre> <p>Calculate column sizes for table layout.</p> <p>Falls back to width 400 if best size is negative.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def calc_col_sizes(self):\n    \"\"\"Calculate column sizes for table layout.\n\n    Falls back to width 400 if best size is negative.\n    \"\"\"\n    sizes = self.calc_width()\n    if sizes[0] &gt;= 0:\n        self.width = sizes[0] + self.extra_space[0] + self.extra_space[1]\n    else:\n        self.width = 400\n</code></pre>"},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag.CtrlTag.calc_height","title":"calc_height","text":"<pre><code>calc_height()\n</code></pre> <p>Calculate height from widget best size.</p> <p>Returns:</p> Type Description <p>Integer height including extra space.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def calc_height(self):\n    \"\"\"Calculate height from widget best size.\n\n    Returns:\n        Integer height including extra space.\n    \"\"\"\n    if self.height &gt;= 0:\n        return self.height\n    if self.obj:\n        self.width_best, self.height_best = self.obj.GetBestSize()\n    else:\n        self.height_best = 30\n    self.height = self.height_best + self.extra_space[2] + self.extra_space[3]\n    return self.height\n</code></pre>"},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag.CtrlTag.calc_width","title":"calc_width","text":"<pre><code>calc_width()\n</code></pre> <p>Calculate width from widget best/min/max sizes.</p> <p>Returns:</p> Type Description <p>Tuple of (best_width, min_width, max_width).</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def calc_width(self):\n    \"\"\"Calculate width from widget best/min/max sizes.\n\n    Returns:\n        Tuple of (best_width, min_width, max_width).\n    \"\"\"\n    d = self.extra_space[0] + self.extra_space[1]\n    if self.width &lt; 0:\n        if self.obj:\n            self.width_best, self.height_best = self.obj.GetBestSize()\n            self.width_min, height_min = self.obj.GetMinSize()\n            self.width_max, height_max = self.obj.GetMaxSize()\n            return (\n                self.width_best + d,\n                self.width_min + d,\n                self.width_max + d,\n            )\n        else:\n            return (d, d, d)\n    else:\n        return (self.width, self.width, self.width)\n</code></pre>"},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag.CtrlTag.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Finalize tag parsing and create/update the widget.</p> <p>In parse_only mode, registers tdata for later use. Otherwise, calls handle_ctrl to instantiate the widget.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def close(self):\n    \"\"\"Finalize tag parsing and create/update the widget.\n\n    In parse_only mode, registers tdata for later use.\n    Otherwise, calls handle_ctrl to instantiate the widget.\n    \"\"\"\n    self._append_to_tdata()\n    if self.parser.parse_only:\n        if hasattr(self.parser, \"register_tdata\"):\n            self.parser.register_tdata(self.tdata, self.tag, self.attrs)\n    else:\n        self.handle_ctrl(self.tag)\n    return TableTag.close(self)\n</code></pre>"},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag.CtrlTag.draw_atom","title":"draw_atom","text":"<pre><code>draw_atom(dc, style, x, y, dx, dy)\n</code></pre> <p>Draw the widget atomically at the given position.</p> <p>Parameters:</p> Name Type Description Default <code>dc</code> <p>Device context.</p> required <code>style</code> <p>Drawing style (unused).</p> required <code>x, y</code> <p>Position.</p> required <code>dx, dy</code> <p>Size hints (unused).</p> required Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def draw_atom(self, dc, style, x, y, dx, dy):\n    \"\"\"Draw the widget atomically at the given position.\n\n    Args:\n        dc: Device context.\n        style: Drawing style (unused).\n        x, y: Position.\n        dx, dy: Size hints (unused).\n    \"\"\"\n    dc2 = dc.subdc(x, y, self.width, self.height)\n    cont = True\n    while cont:\n        dy, cont = self.render(dc2)\n        dc2.y += dy\n</code></pre>"},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag.CtrlTag.get_context","title":"get_context","text":"<pre><code>get_context()\n</code></pre> <p>Return layout context with button sizes and top offset.</p> <p>Returns:</p> Type Description <p>Dict with button_size_dx, button_size_dy, and top keys.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def get_context(self):\n    \"\"\"Return layout context with button sizes and top offset.\n\n    Returns:\n        Dict with button_size_dx, button_size_dy, and top keys.\n    \"\"\"\n    if self.parent and hasattr(self.parent, \"y\"):\n        _y = self.parent.y + 3\n    else:\n        _y = 1\n    return {\n        \"button_size_dx\": wx.Button.GetDefaultSize()[0] + 14,\n        \"button_size_dy\": wx.Button.GetDefaultSize()[1] + 14,\n        \"top\": _y,\n    }\n</code></pre>"},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag.CtrlTag.get_height","title":"get_height","text":"<pre><code>get_height()\n</code></pre> <p>Calculate and return the widget height.</p> <p>Returns:</p> Type Description <p>Integer height value, minimum 200.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def get_height(self):\n    \"\"\"Calculate and return the widget height.\n\n    Returns:\n        Integer height value, minimum 200.\n    \"\"\"\n    height = self.calc_height()\n    if height &gt;= 0:\n        self.height = height + self.extra_space[2] + self.extra_space[3]\n    else:\n        self.height = 200\n    return self.height\n</code></pre>"},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag.CtrlTag.get_parent_window","title":"get_parent_window","text":"<pre><code>get_parent_window()\n</code></pre> <p>Get the wxPython parent window for widget creation.</p> <p>Returns:</p> Type Description <p>The parent window from the parser.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def get_parent_window(self):\n    \"\"\"Get the wxPython parent window for widget creation.\n\n    Returns:\n        The parent window from the parser.\n    \"\"\"\n    return self.parser.get_parent_window()\n</code></pre>"},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag.CtrlTag.handle_ctrl","title":"handle_ctrl","text":"<pre><code>handle_ctrl(tag)\n</code></pre> <p>Create or update the wxPython widget for this ctrl tag.</p> <p>This is the main dispatch method that: 1. Resolves the widget name (with deduplication) 2. Builds kwargs from tag attributes 3. Looks up the widget class from schctrl 4. Pops an existing widget or creates a new one 5. Sets the initial value and triggers after_create</p> <p>Parameters:</p> Name Type Description Default <code>tag</code> <p>The ctrl tag name (e.g. 'ctrl-text').</p> required Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def handle_ctrl(self, tag):\n    \"\"\"Create or update the wxPython widget for this ctrl tag.\n\n    This is the main dispatch method that:\n    1. Resolves the widget name (with deduplication)\n    2. Builds kwargs from tag attributes\n    3. Looks up the widget class from schctrl\n    4. Pops an existing widget or creates a new one\n    5. Sets the initial value and triggers after_create\n\n    Args:\n        tag: The ctrl tag name (e.g. 'ctrl-text').\n    \"\"\"\n    # Resolve widget name\n    if \"name\" in self.attrs:\n        name_reg = is_null(self.attrs[\"name\"], \"\")\n        parent = self.parent\n        while parent:\n            if hasattr(parent, \"reg_field\"):\n                name_reg = parent.reg_field(self.attrs[\"name\"])\n                break\n            parent = parent.parent\n        name = name_reg\n    else:\n        name = tag[4:]\n    self.attrs[\"name\"] = name\n\n    parent = self.get_parent_window()\n    if parent:\n        gparent = parent.GetParent()\n        tmp = name\n        i = 1\n        while gparent.test_ctrl(tmp):\n            tmp = name + \"__\" + str(i)\n            i += 1\n        name = tmp\n\n    # Build kwargs from attributes\n    for atrybut in _ATTRIBUTES:\n        if atrybut in self.attrs:\n            attr_val = self.attrs[atrybut]\n            self.kwargs[atrybut] = \"\" if attr_val is None else attr_val\n\n    # Resolve value\n    valuetype = \"data\"\n    if \"valuetype\" in self.attrs:\n        if self.attrs[\"valuetype\"] == \"ref\":\n            valuetype = \"ref\"\n        if self.attrs[\"valuetype\"] == \"str\":\n            valuetype = \"str\"\n    if \"value\" in self.attrs:\n        if valuetype == \"str\":\n            value = unquote(self.attrs[\"value\"])\n        else:\n            try:\n                value = ast.literal_eval(self.attrs[\"value\"])\n            except (ValueError, SyntaxError):\n                logger.warning(\"Value error: %s\", self.attrs[\"value\"])\n                value = None\n    else:\n        value = None\n    if \"strvalue\" in self.attrs:\n        value = self.attrs[\"strvalue\"]\n\n    # Set size from calculated dimensions\n    self.kwargs[\"size\"] = wx.Size(\n        max(0, int((self.width - self.extra_space[0]) - self.extra_space[1])),\n        max(0, int((self.height - self.extra_space[2]) - self.extra_space[3])),\n    )\n\n    # Look up widget class by tag name\n    try:\n        if tag.upper().startswith(\"CTRL-\"):\n            self.classObj = getattr(schctrl, tag[5:].upper())\n        elif tag.startswith(\"_\"):\n            self.classObj = getattr(schctrl, tag[1:].upper())\n        else:\n            self.classObj = getattr(schctrl, tag.upper())\n    except AttributeError:\n        return True\n\n    # Attach tdata and ldata to kwargs\n    if len(self.tdata) &gt; 0:\n        self.kwargs[\"tdata\"] = self.tdata\n    self.kwargs[\"param\"] = dict(self.attrs)\n    self.kwargs[\"param\"][\"table_lp\"] = str(self.parser.table_lp)\n    self.kwargs[\"param\"][\"tag\"] = tag.lower()\n    if self.data2 is not None:\n        self.kwargs[\"param\"][\"data\"] = is_null(self.data2, \"\")\n    else:\n        self.kwargs[\"param\"][\"data\"] = \"\".join(self.data)\n    if self.list:\n        l = self.list.get_list()\n        self.kwargs[\"ldata\"] = l\n\n    # Create or update widget\n    if parent:\n        gparent = parent.GetParent()\n        obj = gparent.pop_ctrl(name)\n        if obj:\n            if parent.update_controls:\n                if hasattr(obj, \"process_refr_data\"):\n                    obj.process_refr_data(**self.kwargs)\n                elif not (hasattr(obj, \"is_ctrl_block\") and obj.is_ctrl_block()):\n                    obj.SetValue(value)\n            parent.append_ctrl(obj)\n        else:\n            obj = self.classObj(parent, **self.kwargs)\n            if not obj:\n                logger.error(\"ERROR: %s\", self.classObj)\n            obj.set_unique_name(name)\n            if value is not None and valuetype in (\"data\", \"str\"):\n                obj.SetValue(value)\n            obj.after_create()\n            parent.append_ctrl(obj)\n        self.obj = obj\n</code></pre>"},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag.CtrlTag.handle_data","title":"handle_data","text":"<pre><code>handle_data(data)\n</code></pre> <p>Process raw text data.</p> <p>For ctrl-checkbox, text data is suppressed.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <p>Raw text data.</p> required <p>Returns:</p> Type Description <p>Result from parent handle_data.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def handle_data(self, data):\n    \"\"\"Process raw text data.\n\n    For ctrl-checkbox, text data is suppressed.\n\n    Args:\n        data: Raw text data.\n\n    Returns:\n        Result from parent handle_data.\n    \"\"\"\n    if self.tag == \"ctrl-checkbox\":\n        pass\n    return BaseHtmlElemParser.handle_data(self, data)\n</code></pre>"},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag.CtrlTag.handle_starttag","title":"handle_starttag","text":"<pre><code>handle_starttag(parser, tag, attrs)\n</code></pre> <p>Dispatch child tag to the appropriate parser.</p> <p>Routes <ul>, <li>, ,  to their helper parsers. For ctrl-composite, delegates to CompositeChildTag. <p>Parameters:</p> Name Type Description Default <code>parser</code> <p>The HTML parser.</p> required <code>tag</code> <p>Child tag name.</p> required <code>attrs</code> <p>Child tag attributes.</p> required <p>Returns:</p> Type Description <p>A parser instance for the child tag, or None.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def handle_starttag(self, parser, tag, attrs):\n    \"\"\"Dispatch child tag to the appropriate parser.\n\n    Routes &lt;ul&gt;, &lt;li&gt;, &lt;data&gt;, &lt;option&gt; to their helper parsers.\n    For ctrl-composite, delegates to CompositeChildTag.\n\n    Args:\n        parser: The HTML parser.\n        tag: Child tag name.\n        attrs: Child tag attributes.\n\n    Returns:\n        A parser instance for the child tag, or None.\n    \"\"\"\n    from pytigon_gui.guictrl.tag_parsers import (\n        TreeUl,\n        TreeLi,\n        Data,\n        OptionTag,\n        CompositeChildTag,\n    )\n\n    if self.tag == \"ctrl-checkbox\":\n        pass\n\n    if self.tag == \"ctrl-composite\":\n        return CompositeChildTag(self, parser, tag, attrs)\n\n    if tag[:3] + \"*\" in self.child_tags:\n        return self.class_from_tag_name(tag[:3] + \"*\")(self, parser, tag, attrs)\n    elif tag == \"ul\":\n        return TreeUl(self, parser, tag, attrs)\n    elif tag == \"li\":\n        return TreeLi(self, parser, tag, attrs)\n    elif tag == \"data\":\n        return Data(self, parser, tag, attrs)\n    elif tag == \"option\":\n        return OptionTag(self, parser, tag, attrs)\n    elif tag in self.child_tags:\n        o = self.class_from_tag_name(tag)\n        if o:\n            return o(self, parser, tag, attrs)\n        return None\n    return None\n</code></pre>"},{"location":"api/guictrl_tag_ctrltag/#pytigon_gui.guictrl.tag_ctrltag.CtrlTag.render","title":"render","text":"<pre><code>render(dc)\n</code></pre> <p>Position and size the widget within the layout.</p> <p>Parameters:</p> Name Type Description Default <code>dc</code> <p>Device context for rendering.</p> required <p>Returns:</p> Type Description <p>Tuple of (height, False) for layout continuation.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def render(self, dc):\n    \"\"\"Position and size the widget within the layout.\n\n    Args:\n        dc: Device context for rendering.\n\n    Returns:\n        Tuple of (height, False) for layout continuation.\n    \"\"\"\n    if dc.rec and self.obj:\n        self.obj.SetSize(\n            int(dc.x + self.extra_space[0]),\n            int(dc.y + self.extra_space[2]),\n            int((self.width - self.extra_space[0]) - self.extra_space[1]),\n            int((self.height - self.extra_space[2]) - self.extra_space[3]),\n        )\n    return (self.height, False)\n</code></pre>"},{"location":"api/guictrl_tag_parsers/","title":"guictrl.tag_parsers \u2013 Tag Parsers","text":"<p>Individual tag parsers for <code>&lt;ctrl&gt;</code>, <code>&lt;button&gt;</code>, <code>&lt;grid&gt;</code>, <code>&lt;input&gt;</code>, and other UI elements in the Pytigon tag language.</p>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers","title":"tag_parsers","text":"<p>HTML helper parsers for tree-structured data and option handling.</p> <p>These classes support the CtrlTag parser in building nested tree data (ldata) and flat option data (tdata) from HTML tags like <ul>, <li>, , and ."},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers-classes","title":"Classes","text":""},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.CompositeChildTag","title":"CompositeChildTag","text":"<pre><code>CompositeChildTag(parent, parser, tag, attrs)\n</code></pre> <p>               Bases: <code>BaseHtmlElemParser</code></p> <p>Parser for nested tags within ctrl-composite.</p> <p>Builds a composite_data dict tree that mirrors the HTML structure inside a composite control. Each nested tag becomes a child node with tag, attrs, data, and children.</p> <p>Initialize composite child tag parser.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser (CompositeChildTag or CtrlTag).</p> required <code>parser</code> <p>The HTML parser.</p> required <code>tag</code> <p>Tag name.</p> required <code>attrs</code> <p>Tag attributes dict.</p> required Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def __init__(self, parent, parser, tag, attrs):\n    \"\"\"Initialize composite child tag parser.\n\n    Args:\n        parent: Parent parser (CompositeChildTag or CtrlTag).\n        parser: The HTML parser.\n        tag: Tag name.\n        attrs: Tag attributes dict.\n    \"\"\"\n    BaseHtmlElemParser.__init__(self, parent, parser, tag, attrs)\n    self.composite_data = {\n        \"tag\": tag,\n        \"attrs\": attrs,\n        \"data\": None,\n        \"children\": [],\n    }\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.CompositeChildTag-functions","title":"Methods:","text":""},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.CompositeChildTag.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>On close, attach composite data to parent.</p> Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def close(self):\n    \"\"\"On close, attach composite data to parent.\"\"\"\n    if self.data:\n        self.composite_data[\"data\"] = self.data\n    if isinstance(self.parent, CompositeChildTag):\n        self.parent.composite_data[\"children\"].append(self.composite_data)\n    elif isinstance(self.parent, CtrlTag):\n        self.parent.data2.append(self.composite_data)\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.CompositeChildTag.handle_starttag","title":"handle_starttag","text":"<pre><code>handle_starttag(parser, tag, attrs)\n</code></pre> <p>Handle nested tags within composite.</p> <p>Parameters:</p> Name Type Description Default <code>parser</code> <p>The HTML parser.</p> required <code>tag</code> <p>Tag name.</p> required <code>attrs</code> <p>Tag attributes dict.</p> required <p>Returns:</p> Type Description <p>A new CompositeChildTag for the nested tag.</p> Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def handle_starttag(self, parser, tag, attrs):\n    \"\"\"Handle nested tags within composite.\n\n    Args:\n        parser: The HTML parser.\n        tag: Tag name.\n        attrs: Tag attributes dict.\n\n    Returns:\n        A new CompositeChildTag for the nested tag.\n    \"\"\"\n    return CompositeChildTag(self, parser, tag, attrs)\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.CtrlTag","title":"CtrlTag","text":"<pre><code>CtrlTag(parent, parser, tag, attrs)\n</code></pre> <p>               Bases: <code>TableTag</code></p> <p>Handler for all tags starting with 'ctrl-'.</p> <p>Parses the HTML tag attributes, collects child data (tdata, ldata), calculates relative sizes, and creates the corresponding wxPython widget via schctrl (pytigon_gui.guictrl.ctrl).</p> Lifecycle <ol> <li>init - Parse attributes, calculate sizes</li> <li>Child tags populate tdata/ldata via helper parsers</li> <li>close() - Build kwargs, create or update widget</li> <li>render() - Position and size the widget in the layout</li> </ol> <p>Initialize the ctrl tag handler.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent HTML element parser.</p> required <code>parser</code> <p>The HTML parser instance.</p> required <code>tag</code> <p>Full tag name (e.g. 'ctrl-text', 'ctrl-button').</p> required <code>attrs</code> <p>Tag attributes dict.</p> required Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def __init__(self, parent, parser, tag, attrs):\n    \"\"\"Initialize the ctrl tag handler.\n\n    Args:\n        parent: Parent HTML element parser.\n        parser: The HTML parser instance.\n        tag: Full tag name (e.g. 'ctrl-text', 'ctrl-button').\n        attrs: Tag attributes dict.\n    \"\"\"\n    TableTag.__init__(self, parent, parser, tag, attrs)\n\n    self.list = None\n    self.child_tags += [\"ul\", \"li\", \"data\", \"option\", \"div\"]\n    self.subtab = True\n    self.kwargs = {}\n    self.classObj = None\n    self.data2 = None\n    self.obj = None\n    self.width_best = -1\n    self.width_min = -1\n    self.width_max = -1\n    self.tdata = []\n\n    if self.parser.parse_only:\n        return\n    self._calc_relative_size()\n\n    # Initialize composite data storage\n    if self.tag == \"ctrl-composite\":\n        self.data2 = []\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.CtrlTag-functions","title":"Methods:","text":""},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.CtrlTag.append_to_tree","title":"append_to_tree","text":"<pre><code>append_to_tree(elem)\n</code></pre> <p>Append a tree element (from <ul>/<li> parsing). <p>Creates a TreeList on first call.</p> <p>Parameters:</p> Name Type Description Default <code>elem</code> <p>A tree data element.</p> required Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def append_to_tree(self, elem):\n    \"\"\"Append a tree element (from &lt;ul&gt;/&lt;li&gt; parsing).\n\n    Creates a TreeList on first call.\n\n    Args:\n        elem: A tree data element.\n    \"\"\"\n    from pytigon_gui.guictrl.tag_parsers import TreeList\n\n    if not self.list:\n        self.list = TreeList()\n    self.list.append_to_tree(elem)\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.CtrlTag.calc_col_sizes","title":"calc_col_sizes","text":"<pre><code>calc_col_sizes()\n</code></pre> <p>Calculate column sizes for table layout.</p> <p>Falls back to width 400 if best size is negative.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def calc_col_sizes(self):\n    \"\"\"Calculate column sizes for table layout.\n\n    Falls back to width 400 if best size is negative.\n    \"\"\"\n    sizes = self.calc_width()\n    if sizes[0] &gt;= 0:\n        self.width = sizes[0] + self.extra_space[0] + self.extra_space[1]\n    else:\n        self.width = 400\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.CtrlTag.calc_height","title":"calc_height","text":"<pre><code>calc_height()\n</code></pre> <p>Calculate height from widget best size.</p> <p>Returns:</p> Type Description <p>Integer height including extra space.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def calc_height(self):\n    \"\"\"Calculate height from widget best size.\n\n    Returns:\n        Integer height including extra space.\n    \"\"\"\n    if self.height &gt;= 0:\n        return self.height\n    if self.obj:\n        self.width_best, self.height_best = self.obj.GetBestSize()\n    else:\n        self.height_best = 30\n    self.height = self.height_best + self.extra_space[2] + self.extra_space[3]\n    return self.height\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.CtrlTag.calc_width","title":"calc_width","text":"<pre><code>calc_width()\n</code></pre> <p>Calculate width from widget best/min/max sizes.</p> <p>Returns:</p> Type Description <p>Tuple of (best_width, min_width, max_width).</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def calc_width(self):\n    \"\"\"Calculate width from widget best/min/max sizes.\n\n    Returns:\n        Tuple of (best_width, min_width, max_width).\n    \"\"\"\n    d = self.extra_space[0] + self.extra_space[1]\n    if self.width &lt; 0:\n        if self.obj:\n            self.width_best, self.height_best = self.obj.GetBestSize()\n            self.width_min, height_min = self.obj.GetMinSize()\n            self.width_max, height_max = self.obj.GetMaxSize()\n            return (\n                self.width_best + d,\n                self.width_min + d,\n                self.width_max + d,\n            )\n        else:\n            return (d, d, d)\n    else:\n        return (self.width, self.width, self.width)\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.CtrlTag.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Finalize tag parsing and create/update the widget.</p> <p>In parse_only mode, registers tdata for later use. Otherwise, calls handle_ctrl to instantiate the widget.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def close(self):\n    \"\"\"Finalize tag parsing and create/update the widget.\n\n    In parse_only mode, registers tdata for later use.\n    Otherwise, calls handle_ctrl to instantiate the widget.\n    \"\"\"\n    self._append_to_tdata()\n    if self.parser.parse_only:\n        if hasattr(self.parser, \"register_tdata\"):\n            self.parser.register_tdata(self.tdata, self.tag, self.attrs)\n    else:\n        self.handle_ctrl(self.tag)\n    return TableTag.close(self)\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.CtrlTag.draw_atom","title":"draw_atom","text":"<pre><code>draw_atom(dc, style, x, y, dx, dy)\n</code></pre> <p>Draw the widget atomically at the given position.</p> <p>Parameters:</p> Name Type Description Default <code>dc</code> <p>Device context.</p> required <code>style</code> <p>Drawing style (unused).</p> required <code>x, y</code> <p>Position.</p> required <code>dx, dy</code> <p>Size hints (unused).</p> required Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def draw_atom(self, dc, style, x, y, dx, dy):\n    \"\"\"Draw the widget atomically at the given position.\n\n    Args:\n        dc: Device context.\n        style: Drawing style (unused).\n        x, y: Position.\n        dx, dy: Size hints (unused).\n    \"\"\"\n    dc2 = dc.subdc(x, y, self.width, self.height)\n    cont = True\n    while cont:\n        dy, cont = self.render(dc2)\n        dc2.y += dy\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.CtrlTag.get_context","title":"get_context","text":"<pre><code>get_context()\n</code></pre> <p>Return layout context with button sizes and top offset.</p> <p>Returns:</p> Type Description <p>Dict with button_size_dx, button_size_dy, and top keys.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def get_context(self):\n    \"\"\"Return layout context with button sizes and top offset.\n\n    Returns:\n        Dict with button_size_dx, button_size_dy, and top keys.\n    \"\"\"\n    if self.parent and hasattr(self.parent, \"y\"):\n        _y = self.parent.y + 3\n    else:\n        _y = 1\n    return {\n        \"button_size_dx\": wx.Button.GetDefaultSize()[0] + 14,\n        \"button_size_dy\": wx.Button.GetDefaultSize()[1] + 14,\n        \"top\": _y,\n    }\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.CtrlTag.get_height","title":"get_height","text":"<pre><code>get_height()\n</code></pre> <p>Calculate and return the widget height.</p> <p>Returns:</p> Type Description <p>Integer height value, minimum 200.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def get_height(self):\n    \"\"\"Calculate and return the widget height.\n\n    Returns:\n        Integer height value, minimum 200.\n    \"\"\"\n    height = self.calc_height()\n    if height &gt;= 0:\n        self.height = height + self.extra_space[2] + self.extra_space[3]\n    else:\n        self.height = 200\n    return self.height\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.CtrlTag.get_parent_window","title":"get_parent_window","text":"<pre><code>get_parent_window()\n</code></pre> <p>Get the wxPython parent window for widget creation.</p> <p>Returns:</p> Type Description <p>The parent window from the parser.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def get_parent_window(self):\n    \"\"\"Get the wxPython parent window for widget creation.\n\n    Returns:\n        The parent window from the parser.\n    \"\"\"\n    return self.parser.get_parent_window()\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.CtrlTag.handle_ctrl","title":"handle_ctrl","text":"<pre><code>handle_ctrl(tag)\n</code></pre> <p>Create or update the wxPython widget for this ctrl tag.</p> <p>This is the main dispatch method that: 1. Resolves the widget name (with deduplication) 2. Builds kwargs from tag attributes 3. Looks up the widget class from schctrl 4. Pops an existing widget or creates a new one 5. Sets the initial value and triggers after_create</p> <p>Parameters:</p> Name Type Description Default <code>tag</code> <p>The ctrl tag name (e.g. 'ctrl-text').</p> required Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def handle_ctrl(self, tag):\n    \"\"\"Create or update the wxPython widget for this ctrl tag.\n\n    This is the main dispatch method that:\n    1. Resolves the widget name (with deduplication)\n    2. Builds kwargs from tag attributes\n    3. Looks up the widget class from schctrl\n    4. Pops an existing widget or creates a new one\n    5. Sets the initial value and triggers after_create\n\n    Args:\n        tag: The ctrl tag name (e.g. 'ctrl-text').\n    \"\"\"\n    # Resolve widget name\n    if \"name\" in self.attrs:\n        name_reg = is_null(self.attrs[\"name\"], \"\")\n        parent = self.parent\n        while parent:\n            if hasattr(parent, \"reg_field\"):\n                name_reg = parent.reg_field(self.attrs[\"name\"])\n                break\n            parent = parent.parent\n        name = name_reg\n    else:\n        name = tag[4:]\n    self.attrs[\"name\"] = name\n\n    parent = self.get_parent_window()\n    if parent:\n        gparent = parent.GetParent()\n        tmp = name\n        i = 1\n        while gparent.test_ctrl(tmp):\n            tmp = name + \"__\" + str(i)\n            i += 1\n        name = tmp\n\n    # Build kwargs from attributes\n    for atrybut in _ATTRIBUTES:\n        if atrybut in self.attrs:\n            attr_val = self.attrs[atrybut]\n            self.kwargs[atrybut] = \"\" if attr_val is None else attr_val\n\n    # Resolve value\n    valuetype = \"data\"\n    if \"valuetype\" in self.attrs:\n        if self.attrs[\"valuetype\"] == \"ref\":\n            valuetype = \"ref\"\n        if self.attrs[\"valuetype\"] == \"str\":\n            valuetype = \"str\"\n    if \"value\" in self.attrs:\n        if valuetype == \"str\":\n            value = unquote(self.attrs[\"value\"])\n        else:\n            try:\n                value = ast.literal_eval(self.attrs[\"value\"])\n            except (ValueError, SyntaxError):\n                logger.warning(\"Value error: %s\", self.attrs[\"value\"])\n                value = None\n    else:\n        value = None\n    if \"strvalue\" in self.attrs:\n        value = self.attrs[\"strvalue\"]\n\n    # Set size from calculated dimensions\n    self.kwargs[\"size\"] = wx.Size(\n        max(0, int((self.width - self.extra_space[0]) - self.extra_space[1])),\n        max(0, int((self.height - self.extra_space[2]) - self.extra_space[3])),\n    )\n\n    # Look up widget class by tag name\n    try:\n        if tag.upper().startswith(\"CTRL-\"):\n            self.classObj = getattr(schctrl, tag[5:].upper())\n        elif tag.startswith(\"_\"):\n            self.classObj = getattr(schctrl, tag[1:].upper())\n        else:\n            self.classObj = getattr(schctrl, tag.upper())\n    except AttributeError:\n        return True\n\n    # Attach tdata and ldata to kwargs\n    if len(self.tdata) &gt; 0:\n        self.kwargs[\"tdata\"] = self.tdata\n    self.kwargs[\"param\"] = dict(self.attrs)\n    self.kwargs[\"param\"][\"table_lp\"] = str(self.parser.table_lp)\n    self.kwargs[\"param\"][\"tag\"] = tag.lower()\n    if self.data2 is not None:\n        self.kwargs[\"param\"][\"data\"] = is_null(self.data2, \"\")\n    else:\n        self.kwargs[\"param\"][\"data\"] = \"\".join(self.data)\n    if self.list:\n        l = self.list.get_list()\n        self.kwargs[\"ldata\"] = l\n\n    # Create or update widget\n    if parent:\n        gparent = parent.GetParent()\n        obj = gparent.pop_ctrl(name)\n        if obj:\n            if parent.update_controls:\n                if hasattr(obj, \"process_refr_data\"):\n                    obj.process_refr_data(**self.kwargs)\n                elif not (hasattr(obj, \"is_ctrl_block\") and obj.is_ctrl_block()):\n                    obj.SetValue(value)\n            parent.append_ctrl(obj)\n        else:\n            obj = self.classObj(parent, **self.kwargs)\n            if not obj:\n                logger.error(\"ERROR: %s\", self.classObj)\n            obj.set_unique_name(name)\n            if value is not None and valuetype in (\"data\", \"str\"):\n                obj.SetValue(value)\n            obj.after_create()\n            parent.append_ctrl(obj)\n        self.obj = obj\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.CtrlTag.handle_data","title":"handle_data","text":"<pre><code>handle_data(data)\n</code></pre> <p>Process raw text data.</p> <p>For ctrl-checkbox, text data is suppressed.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <p>Raw text data.</p> required <p>Returns:</p> Type Description <p>Result from parent handle_data.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def handle_data(self, data):\n    \"\"\"Process raw text data.\n\n    For ctrl-checkbox, text data is suppressed.\n\n    Args:\n        data: Raw text data.\n\n    Returns:\n        Result from parent handle_data.\n    \"\"\"\n    if self.tag == \"ctrl-checkbox\":\n        pass\n    return BaseHtmlElemParser.handle_data(self, data)\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.CtrlTag.handle_starttag","title":"handle_starttag","text":"<pre><code>handle_starttag(parser, tag, attrs)\n</code></pre> <p>Dispatch child tag to the appropriate parser.</p> <p>Routes <ul>, <li>, ,  to their helper parsers. For ctrl-composite, delegates to CompositeChildTag. <p>Parameters:</p> Name Type Description Default <code>parser</code> <p>The HTML parser.</p> required <code>tag</code> <p>Child tag name.</p> required <code>attrs</code> <p>Child tag attributes.</p> required <p>Returns:</p> Type Description <p>A parser instance for the child tag, or None.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def handle_starttag(self, parser, tag, attrs):\n    \"\"\"Dispatch child tag to the appropriate parser.\n\n    Routes &lt;ul&gt;, &lt;li&gt;, &lt;data&gt;, &lt;option&gt; to their helper parsers.\n    For ctrl-composite, delegates to CompositeChildTag.\n\n    Args:\n        parser: The HTML parser.\n        tag: Child tag name.\n        attrs: Child tag attributes.\n\n    Returns:\n        A parser instance for the child tag, or None.\n    \"\"\"\n    from pytigon_gui.guictrl.tag_parsers import (\n        TreeUl,\n        TreeLi,\n        Data,\n        OptionTag,\n        CompositeChildTag,\n    )\n\n    if self.tag == \"ctrl-checkbox\":\n        pass\n\n    if self.tag == \"ctrl-composite\":\n        return CompositeChildTag(self, parser, tag, attrs)\n\n    if tag[:3] + \"*\" in self.child_tags:\n        return self.class_from_tag_name(tag[:3] + \"*\")(self, parser, tag, attrs)\n    elif tag == \"ul\":\n        return TreeUl(self, parser, tag, attrs)\n    elif tag == \"li\":\n        return TreeLi(self, parser, tag, attrs)\n    elif tag == \"data\":\n        return Data(self, parser, tag, attrs)\n    elif tag == \"option\":\n        return OptionTag(self, parser, tag, attrs)\n    elif tag in self.child_tags:\n        o = self.class_from_tag_name(tag)\n        if o:\n            return o(self, parser, tag, attrs)\n        return None\n    return None\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.CtrlTag.render","title":"render","text":"<pre><code>render(dc)\n</code></pre> <p>Position and size the widget within the layout.</p> <p>Parameters:</p> Name Type Description Default <code>dc</code> <p>Device context for rendering.</p> required <p>Returns:</p> Type Description <p>Tuple of (height, False) for layout continuation.</p> Source code in <code>pytigon_gui/guictrl/tag_ctrltag.py</code> <pre><code>def render(self, dc):\n    \"\"\"Position and size the widget within the layout.\n\n    Args:\n        dc: Device context for rendering.\n\n    Returns:\n        Tuple of (height, False) for layout continuation.\n    \"\"\"\n    if dc.rec and self.obj:\n        self.obj.SetSize(\n            int(dc.x + self.extra_space[0]),\n            int(dc.y + self.extra_space[2]),\n            int((self.width - self.extra_space[0]) - self.extra_space[1]),\n            int((self.height - self.extra_space[2]) - self.extra_space[3]),\n        )\n    return (self.height, False)\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.Data","title":"Data","text":"<pre><code>Data(parent, parser, tag, attrs)\n</code></pre> <p>               Bases: <code>BaseHtmlElemParser</code></p> <p>Parser for  tags with base64-encoded content. <p>Decodes base64 content and stores it in the parent's data2 attribute for use by CtrlTag.</p> Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def __init__(self, parent, parser, tag, attrs):\n    BaseHtmlElemParser.__init__(self, parent, parser, tag, attrs)\n    self.data = None\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.Data-functions","title":"Methods:","text":""},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.Data.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Decode base64 data and store in parent.data2.</p> Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def close(self):\n    \"\"\"Decode base64 data and store in parent.data2.\"\"\"\n    if self.data:\n        self.parent.data2 = b64decode(self.data.encode(\"utf-8\")).decode(\"utf-8\")\n    else:\n        self.parent.data2 = \"\"\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.Data.handle_data","title":"handle_data","text":"<pre><code>handle_data(data)\n</code></pre> <p>Accumulate text data, stripping trailing whitespace.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <p>Raw text data chunk.</p> required Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def handle_data(self, data):\n    \"\"\"Accumulate text data, stripping trailing whitespace.\n\n    Args:\n        data: Raw text data chunk.\n    \"\"\"\n    if self.data:\n        self.data += data.rstrip(\" \\n\")\n    else:\n        self.data = data.rstrip(\" \\n\")\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.OptionTag","title":"OptionTag","text":"<pre><code>OptionTag(parent, parser, tag, attrs)\n</code></pre> <p>               Bases: <code>BaseHtmlElemParser</code></p> <p>Parser for  tags that populate tdata. <p>Collects option text and attributes into Td objects for use by choice/select widgets.</p> Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def __init__(self, parent, parser, tag, attrs):\n    BaseHtmlElemParser.__init__(self, parent, parser, tag, attrs)\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.OptionTag-functions","title":"Methods:","text":""},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.OptionTag.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>On close, create a Td row and append to parent.tdata.</p> Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def close(self):\n    \"\"\"On close, create a Td row and append to parent.tdata.\"\"\"\n    if self.data and len(self.data) &gt; 0:\n        data = \"\".join(self.data).replace(\"\\n\", \"\").replace(\"\\r\", \"\").strip()\n        self.parent.tdata.append([Td(data, self.attrs)])\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.TreeLi","title":"TreeLi","text":"<pre><code>TreeLi(parent, parser, tag, attrs)\n</code></pre> <p>               Bases: <code>BaseHtmlElemParser</code></p> <p>Parser for <li> tags within tree structures. <p>Collects text content and optional nested <ul> children, producing a [label, children, attrs] row. If the <li> contains an  or , its href is captured in the attrs. Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def __init__(self, parent, parser, tag, attrs):\n    BaseHtmlElemParser.__init__(self, parent, parser, tag, attrs)\n    self.child = None\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.TreeLi-functions","title":"Methods:","text":""},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.TreeLi.append_to_tree","title":"append_to_tree","text":"<pre><code>append_to_tree(elem)\n</code></pre> <p>Store a nested tree element as a pending child.</p> <p>Parameters:</p> Name Type Description Default <code>elem</code> <p>A child tree element.</p> required Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def append_to_tree(self, elem):\n    \"\"\"Store a nested tree element as a pending child.\n\n    Args:\n        elem: A child tree element.\n    \"\"\"\n    self.child = elem\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.TreeLi.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>On close, push the collected data as a tree row.</p> Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def close(self):\n    \"\"\"On close, push the collected data as a tree row.\"\"\"\n    if self.child:\n        self.child[0] = superstrip(\"\".join(self.data))\n        self.child[2] = self.attrs\n        self.parent.append_to_tree(self.child)\n    else:\n        self.parent.append_to_tree([superstrip(\"\".join(self.data)), [], self.attrs])\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.TreeLi.handle_starttag","title":"handle_starttag","text":"<pre><code>handle_starttag(parser, tag, attrs)\n</code></pre> <p>Handle nested tags.</p> <ul> starts a new sub-tree.  or  captures href into attrs.   <p>Parameters:</p> Name Type Description Default <code>parser</code> <p>The HTML parser.</p> required <code>tag</code> <p>Tag name.</p> required <code>attrs</code> <p>Tag attributes dict.</p> required <p>Returns:</p> Type Description <p>A TreeUl for <ul>, or None. Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def handle_starttag(self, parser, tag, attrs):\n    \"\"\"Handle nested tags.\n\n    &lt;ul&gt; starts a new sub-tree.\n    &lt;a&gt; or &lt;ctrl-button&gt; captures href into attrs.\n\n    Args:\n        parser: The HTML parser.\n        tag: Tag name.\n        attrs: Tag attributes dict.\n\n    Returns:\n        A TreeUl for &lt;ul&gt;, or None.\n    \"\"\"\n    if tag == \"ul\":\n        return TreeUl(self, parser, tag, attrs)\n    elif tag in (\"a\", \"ctrl-button\"):\n        self.attrs[\"href\"] = attrs[\"href\"]\n    return None\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.TreeList","title":"TreeList","text":"<pre><code>TreeList()\n</code></pre> <p>Simple accumulator for tree-structured data.</p> <p>Collects child elements that form a nested tree representation. Used by TreeUl and TreeLi to build hierarchical data for tree widgets (TREE, TREELIST).</p> Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def __init__(self):\n    self.children = []\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.TreeList-functions","title":"Methods:","text":""},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.TreeList.append_to_tree","title":"append_to_tree","text":"<pre><code>append_to_tree(elem)\n</code></pre> <p>Append a child element to the tree.</p> <p>Parameters:</p> Name Type Description Default <code>elem</code> <p>A child element (typically a list/tuple row).</p> required Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def append_to_tree(self, elem):\n    \"\"\"Append a child element to the tree.\n\n    Args:\n        elem: A child element (typically a list/tuple row).\n    \"\"\"\n    self.children.append(elem)\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.TreeList.get_list","title":"get_list","text":"<pre><code>get_list()\n</code></pre> <p>Return the accumulated tree children.</p> <p>Returns:</p> Type Description <p>List of child elements.</p> Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def get_list(self):\n    \"\"\"Return the accumulated tree children.\n\n    Returns:\n        List of child elements.\n    \"\"\"\n    return self.children\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.TreeUl","title":"TreeUl","text":"<pre><code>TreeUl(parent, parser, tag, attrs)\n</code></pre> <p>               Bases: <code>BaseHtmlElemParser</code></p> <p>Parser for <ul> tags that builds tree data structures. <p>Collects <li> children into a nested list suitable for tree widgets (TREE, TREELIST). Each <li> becomes a row of [label, children, attrs]. Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def __init__(self, parent, parser, tag, attrs):\n    BaseHtmlElemParser.__init__(self, parent, parser, tag, attrs)\n    self.child_tags += [\"li\"]\n    self.children = []\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.TreeUl-functions","title":"Methods:","text":""},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.TreeUl.append_to_tree","title":"append_to_tree","text":"<pre><code>append_to_tree(elem)\n</code></pre> <p>Accept a child element into this tree level.</p> <p>Parameters:</p> Name Type Description Default <code>elem</code> <p>A child element to append.</p> required Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def append_to_tree(self, elem):\n    \"\"\"Accept a child element into this tree level.\n\n    Args:\n        elem: A child element to append.\n    \"\"\"\n    self.children.append(elem)\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.TreeUl.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>On close, push the collected children to the parent.</p> Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def close(self):\n    \"\"\"On close, push the collected children to the parent.\"\"\"\n    self.parent.append_to_tree([\"\", self.children, self.attrs])\n</code></pre>"},{"location":"api/guictrl_tag_parsers/#pytigon_gui.guictrl.tag_parsers.TreeUl.handle_starttag","title":"handle_starttag","text":"<pre><code>handle_starttag(parser, tag, attrs)\n</code></pre> <p>Handle nested <li> tags. <p>Parameters:</p> Name Type Description Default <code>parser</code> <p>The HTML parser.</p> required <code>tag</code> <p>Tag name.</p> required <code>attrs</code> <p>Tag attributes dict.</p> required <p>Returns:</p> Type Description <p>A TreeLi parser for <li>, or None for other tags. Source code in <code>pytigon_gui/guictrl/tag_parsers.py</code> <pre><code>def handle_starttag(self, parser, tag, attrs):\n    \"\"\"Handle nested &lt;li&gt; tags.\n\n    Args:\n        parser: The HTML parser.\n        tag: Tag name.\n        attrs: Tag attributes dict.\n\n    Returns:\n        A TreeLi parser for &lt;li&gt;, or None for other tags.\n    \"\"\"\n    if tag == \"li\":\n        return TreeLi(self, parser, tag, attrs)\n    return None\n</code></pre>"},{"location":"api/guictrl_tag_preprocess/","title":"guictrl.tag_preprocess \u2013 Tag Preprocessor","text":"<p>Preprocessing pipeline for tag definitions: macro expansion, attribute inheritance, and template resolution.</p>"},{"location":"api/guictrl_tag_preprocess/#pytigon_gui.guictrl.tag_preprocess","title":"tag_preprocess","text":"<p>Tag preprocess functions for converting standard HTML tags to ctrl-* tags.</p> <p>These functions are registered via register_tag_preprocess_map and are called by the HTML parser before normal tag handling. They transform standard HTML elements (, , , , , etc.) into the corresponding ctrl-* tags that the CtrlTag class can handle. <p>Each function receives (parent, attrs) and returns (new_tag, new_attrs) or the original (tag, attrs) if no conversion is needed.</p> Constants <p>SCHTYPE_MAP, HIDDEN_DIVS</p>"},{"location":"api/guictrl_tag_preprocess/#pytigon_gui.guictrl.tag_preprocess-attributes","title":"Attributes","text":""},{"location":"api/guictrl_tag_preprocess/#pytigon_gui.guictrl.tag_preprocess.HIDDEN_DIVS","title":"HIDDEN_DIVS  <code>module-attribute</code>","text":"<pre><code>HIDDEN_DIVS = ['ajax-region', 'td_information', 'td_action']\n</code></pre>"},{"location":"api/guictrl_tag_preprocess/#pytigon_gui.guictrl.tag_preprocess.SCHTYPE_MAP","title":"SCHTYPE_MAP  <code>module-attribute</code>","text":"<pre><code>SCHTYPE_MAP = {\n    \"FloatField\": \"float\",\n    \"IntegerField\": \"num\",\n    \"DecimalField\": \"amount\",\n    \"TimeField\": \"time\",\n    \"DateTimeField\": \"datetimepicker\",\n}\n</code></pre>"},{"location":"api/guictrl_tag_preprocess/#pytigon_gui.guictrl.tag_preprocess-functions","title":"Functions:","text":""},{"location":"api/guictrl_tag_preprocess/#pytigon_gui.guictrl.tag_preprocess.a_to_button","title":"a_to_button","text":"<pre><code>a_to_button(parent, attrs)\n</code></pre> <p>Convert  tags with button-like classes to ctrl-button. <p>Converts  tags whose class contains 'popup', 'button', 'btn', or 'schbtn' into ctrl-button tags. The 'schbtn' class has special handling: inside a ctrl-table it remains as , otherwise it becomes a ctrl-button. <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser.</p> required <code>attrs</code> <p>Anchor attributes.</p> required <p>Returns:</p> Type Description <p>Tuple of ('ctrl-button', attrs) or ('a', attrs).</p> Source code in <code>pytigon_gui/guictrl/tag_preprocess.py</code> <pre><code>def a_to_button(parent, attrs):\n    \"\"\"Convert &lt;a&gt; tags with button-like classes to ctrl-button.\n\n    Converts &lt;a&gt; tags whose class contains 'popup', 'button', 'btn',\n    or 'schbtn' into ctrl-button tags. The 'schbtn' class has special\n    handling: inside a ctrl-table it remains as &lt;a&gt;, otherwise it\n    becomes a ctrl-button.\n\n    Args:\n        parent: Parent parser.\n        attrs: Anchor attributes.\n\n    Returns:\n        Tuple of ('ctrl-button', attrs) or ('a', attrs).\n    \"\"\"\n    if \"class\" not in attrs:\n        return (\"a\", attrs)\n\n    classes = attrs[\"class\"]\n    is_button_class = any(c in classes for c in (\"popup\", \"button\", \"btn\", \"schbtn\"))\n    if not is_button_class:\n        return (\"a\", attrs)\n\n    if \"schbtn\" in classes:\n        try:\n            if parent.parent.parent.tag == \"ctrl-table\":\n                return (\"ctrl-button\", attrs)\n        except AttributeError:\n            return (\"a\", attrs)\n    else:\n        try:\n            if parent.parent.parent.tag == \"ctrl-table\":\n                return (\"a\", attrs)\n        except AttributeError:\n            pass\n        return (\"ctrl-button\", attrs)\n\n    return (\"a\", attrs)\n</code></pre>"},{"location":"api/guictrl_tag_preprocess/#pytigon_gui.guictrl.tag_preprocess.component_convert","title":"component_convert","text":"<pre><code>component_convert(parent, attrs)\n</code></pre> <p>Convert custom element tags (-) to _component.</p> <p>Used for web component custom elements.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser.</p> required <code>attrs</code> <p>Component attributes.</p> required <p>Returns:</p> Type Description <p>Tuple of ('_component', attrs).</p> Source code in <code>pytigon_gui/guictrl/tag_preprocess.py</code> <pre><code>def component_convert(parent, attrs):\n    \"\"\"Convert custom element tags (*-*) to _component.\n\n    Used for web component custom elements.\n\n    Args:\n        parent: Parent parser.\n        attrs: Component attributes.\n\n    Returns:\n        Tuple of ('_component', attrs).\n    \"\"\"\n    return (\"_component\", attrs)\n</code></pre>"},{"location":"api/guictrl_tag_preprocess/#pytigon_gui.guictrl.tag_preprocess.div_convert","title":"div_convert","text":"<pre><code>div_convert(parent, attrs)\n</code></pre> <p>Convert  tags based on CSS class. <p>Mapping: - 'popup'                    \u2192  (unchanged) - 'form-group'               \u2192  - 'controls'                 \u2192  - 'tree'                     \u2192 ctrl-tree - 'select2'                  \u2192 ctrl-composite - 'checkbox'                 \u2192  (adds empty  before) - Classes in HIDDEN_DIVS     \u2192 'none' (removed from output) <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser.</p> required <code>attrs</code> <p>Div attributes.</p> required <p>Returns:</p> Type Description <p>Tuple of (new_tag, attrs).</p> Source code in <code>pytigon_gui/guictrl/tag_preprocess.py</code> <pre><code>def div_convert(parent, attrs):\n    \"\"\"Convert &lt;div&gt; tags based on CSS class.\n\n    Mapping:\n    - 'popup'                    \u2192 &lt;div&gt; (unchanged)\n    - 'form-group'               \u2192 &lt;tr&gt;\n    - 'controls'                 \u2192 &lt;td&gt;\n    - 'tree'                     \u2192 ctrl-tree\n    - 'select2'                  \u2192 ctrl-composite\n    - 'checkbox'                 \u2192 &lt;th&gt; (adds empty &lt;th&gt; before)\n    - Classes in HIDDEN_DIVS     \u2192 'none' (removed from output)\n\n    Args:\n        parent: Parent parser.\n        attrs: Div attributes.\n\n    Returns:\n        Tuple of (new_tag, attrs).\n    \"\"\"\n    if \"class\" not in attrs:\n        return (\"div\", attrs)\n\n    classes = attrs[\"class\"]\n\n    if \"popup\" in classes:\n        return (\"div\", attrs)\n    if \"form-group\" in classes:\n        return (\"tr\", attrs)\n    if \"controls\" in classes:\n        return (\"td\", attrs)\n    if \"tree\" in classes:\n        return (\"ctrl-tree\", attrs)\n    if \"select2\" in classes:\n        return (\"ctrl-composite\", attrs)\n    if \"checkbox\" in classes:\n        # Insert empty &lt;th&gt; before the checkbox cell\n        obj = parent.parent.handle_starttag(parent.parser, \"th\", {})\n        if obj:\n            obj.handle_endtag(\"th\")\n        return (\"th\", attrs)\n\n    for hidden_cls in HIDDEN_DIVS:\n        if hidden_cls in classes:\n            return (\"none\", attrs)\n\n    return (\"div\", attrs)\n</code></pre>"},{"location":"api/guictrl_tag_preprocess/#pytigon_gui.guictrl.tag_preprocess.error_p_to_error","title":"error_p_to_error","text":"<pre><code>error_p_to_error(parent, attrs)\n</code></pre> <p>Convert <p> to a comment.</p> <p>Strips error message paragraphs from the output by converting them to comments.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser.</p> required <code>attrs</code> <p>Paragraph attributes.</p> required <p>Returns:</p> Type Description <p>Tuple of ('comment', attrs) or ('p', attrs).</p> Source code in <code>pytigon_gui/guictrl/tag_preprocess.py</code> <pre><code>def error_p_to_error(parent, attrs):\n    \"\"\"Convert &lt;p class=\"help-block\"&gt; to a comment.\n\n    Strips error message paragraphs from the output by converting\n    them to comments.\n\n    Args:\n        parent: Parent parser.\n        attrs: Paragraph attributes.\n\n    Returns:\n        Tuple of ('comment', attrs) or ('p', attrs).\n    \"\"\"\n    if \"class\" in attrs and attrs[\"class\"] == \"help-block\":\n        return (\"comment\", attrs)\n    return (\"p\", attrs)\n</code></pre>"},{"location":"api/guictrl_tag_preprocess/#pytigon_gui.guictrl.tag_preprocess.error_span_to_error","title":"error_span_to_error","text":"<pre><code>error_span_to_error(parent, attrs)\n</code></pre> <p>Convert  to ctrl-errorlist. <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser.</p> required <code>attrs</code> <p>Span attributes.</p> required <p>Returns:</p> Type Description <p>Tuple of ('ctrl-errorlist', attrs) or ('span', attrs).</p> Source code in <code>pytigon_gui/guictrl/tag_preprocess.py</code> <pre><code>def error_span_to_error(parent, attrs):\n    \"\"\"Convert &lt;span class=\"help-block\"&gt; to ctrl-errorlist.\n\n    Args:\n        parent: Parent parser.\n        attrs: Span attributes.\n\n    Returns:\n        Tuple of ('ctrl-errorlist', attrs) or ('span', attrs).\n    \"\"\"\n    if \"class\" in attrs and attrs[\"class\"] == \"help-block\":\n        return (\"ctrl-errorlist\", attrs)\n    return (\"span\", attrs)\n</code></pre>"},{"location":"api/guictrl_tag_preprocess/#pytigon_gui.guictrl.tag_preprocess.input_to_ctrltab","title":"input_to_ctrltab","text":"<pre><code>input_to_ctrltab(parent, attrs)\n</code></pre> <p>Convert  to the appropriate ctrl-* tag.</p> <p>Maps HTML input types to ctrl widget types based on: - schtype attribute (Django field type \u2192 SCHTYPE_MAP) - type attribute (text, button, email, hidden, password, radio,   checkbox, file, submit) - class attribute (dateinput, datetimeinput) - name attribute (date_ prefix \u2192 datepicker)</p> <p>Also handles size, maxlength, value, and label attribute propagation.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser.</p> required <code>attrs</code> <p>Input tag attributes.</p> required <p>Returns:</p> Type Description <p>Tuple of (new_tag, new_attrs).</p> Source code in <code>pytigon_gui/guictrl/tag_preprocess.py</code> <pre><code>def input_to_ctrltab(parent, attrs):\n    \"\"\"Convert &lt;input&gt; to the appropriate ctrl-* tag.\n\n    Maps HTML input types to ctrl widget types based on:\n    - schtype attribute (Django field type \u2192 SCHTYPE_MAP)\n    - type attribute (text, button, email, hidden, password, radio,\n      checkbox, file, submit)\n    - class attribute (dateinput, datetimeinput)\n    - name attribute (date_ prefix \u2192 datepicker)\n\n    Also handles size, maxlength, value, and label attribute propagation.\n\n    Args:\n        parent: Parent parser.\n        attrs: Input tag attributes.\n\n    Returns:\n        Tuple of (new_tag, new_attrs).\n    \"\"\"\n    attrs_ret = {}\n    ret = \"text\"\n    upload = False\n\n    input_type = attrs.get(\"type\", \"text\")\n    schtype = attrs.get(\"schtype\", \"text\")\n    classname = attrs.get(\"class\", \"text\")\n\n    # Determine widget type from schtype (Django field mapping)\n    if schtype in SCHTYPE_MAP:\n        ret = SCHTYPE_MAP[schtype]\n    elif input_type == \"text\":\n        if classname == \"dateinput\":\n            ret = \"datepicker\"\n        elif classname == \"datetimeinput\":\n            ret = \"datetimepicker\"\n        else:\n            ret = \"text\"\n    elif input_type == \"button\":\n        ret = \"button\"\n    elif input_type == \"email\":\n        ret = \"masktext\"\n        attrs_ret = {\"src\": \"!EMAIL\", \"width\": \"250\", \"valuetype\": \"str\"}\n        if \"value\" in attrs:\n            attrs_ret[\"value\"] = attrs[\"value\"]\n    elif input_type == \"hidden\":\n        ret = \"text\"\n        attrs_ret = {\"hidden\": \"1\", \"width\": \"0\", \"height\": \"0\"}\n    elif input_type == \"password\":\n        ret = \"password\"\n    elif input_type == \"radio\":\n        ret = \"radiobutton\"\n        if \"checked\" in attrs:\n            attrs_ret[\"checked\"] = \"1\"\n        if \"value\" in attrs:\n            attrs_ret[\"value\"] = attrs[\"value\"]\n            attrs_ret[\"valuetype\"] = \"str\"\n    elif input_type == \"checkbox\":\n        ret = \"checkbox\"\n        if \"checked\" in attrs:\n            attrs_ret[\"checked\"] = \"1\"\n        if \"value\" in attrs:\n            attrs_ret[\"value\"] = attrs[\"value\"]\n            attrs_ret[\"valuetype\"] = \"str\"\n    elif input_type == \"file\":\n        ret = \"filebrowsebutton\"\n        upload = True\n    elif input_type == \"submit\":\n        ret = \"button\"\n        target = attrs.get(\"target\", \"_parent_refr\")\n        attrs_ret = {\n            \"defaultvalue\": \"1\",\n            \"param\": \"post\",\n            \"target\": target,\n            \"id\": \"wx.ID_OK\",\n        }\n        href = \".\"\n        fields = None\n        p = parent\n        while p:\n            if hasattr(p, \"gethref\"):\n                href = p.gethref()\n                fields = p.get_fields()\n                if p.get_upload():\n                    attrs_ret[\"valuetype\"] = \"upload\"\n                break\n            p = p.parent\n        if href is None:\n            href = \"\"\n        attrs_ret[\"href\"] = href\n        if fields:\n            attrs_ret[\"fields\"] = fields\n\n    # Width from size attribute\n    if \"size\" in attrs and input_type in (\"text\", \"email\"):\n        width_text = 10 * int(is_null(attrs[\"size\"], 20))\n        if width_text &gt; 400:\n            width_text = 8 * int(is_null(attrs[\"size\"], 20))\n        if width_text &gt; 480:\n            width_text = 480\n        attrs_ret[\"width\"] = str(width_text)\n\n    # Maxlength handling: prefer maxlength, then max_length, then size\n    if \"maxlength\" in attrs and input_type in (\"text\",):\n        attrs_ret[\"maxlength\"] = str(is_null(attrs[\"maxlength\"], \"80\"))\n    elif \"max_length\" in attrs and input_type in (\"text\",):\n        attrs_ret[\"maxlength\"] = str(is_null(attrs[\"max_length\"], \"80\"))\n    elif \"size\" in attrs:\n        attrs_ret[\"maxlength\"] = str(int(is_null(attrs[\"size\"], \"80\")))\n\n    # Value propagation for text-like inputs\n    if \"value\" in attrs and input_type in (\"text\", \"hidden\", \"checkbox\"):\n        attrs_ret[\"value\"] = is_null(attrs[\"value\"], \"\")\n        attrs_ret[\"valuetype\"] = \"str\"\n\n    # Label from value for buttons\n    if \"value\" in attrs and input_type in (\"button\", \"submit\"):\n        attrs_ret[\"label\"] = is_null(attrs[\"value\"], \"\")\n\n    # Checkbox label from title\n    if \"title\" in attrs and input_type == \"checkbox\":\n        attrs_ret[\"label\"] = is_null(attrs[\"title\"], \"\")\n\n    # Upload flag propagation\n    if \"name\" in attrs:\n        p = parent\n        while p:\n            if p.form_obj:\n                if upload:\n                    p.form_obj.set_upload(upload)\n                break\n            p = p.parent\n        attrs_ret[\"name\"] = attrs[\"name\"]\n        # Auto-detect date fields by name prefix\n        if ret == \"text\" and \"date_\" in attrs[\"name\"]:\n            ret = \"datepicker\"\n\n    # Forward remaining attributes not already set\n    for attr in attrs:\n        if attr not in attrs_ret and attr != \"value\":\n            attrs_ret[attr] = attrs[attr]\n\n    return (\"ctrl-\" + ret, attrs_ret)\n</code></pre>"},{"location":"api/guictrl_tag_preprocess/#pytigon_gui.guictrl.tag_preprocess.label_to_th","title":"label_to_th","text":"<pre><code>label_to_th(parent, attrs)\n</code></pre> <p>Convert  to  for table-based form layout. <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser.</p> required <code>attrs</code> <p>Label attributes.</p> required <p>Returns:</p> Type Description <p>Tuple of ('th', attrs).</p> Source code in <code>pytigon_gui/guictrl/tag_preprocess.py</code> <pre><code>def label_to_th(parent, attrs):\n    \"\"\"Convert &lt;label&gt; to &lt;th&gt; for table-based form layout.\n\n    Args:\n        parent: Parent parser.\n        attrs: Label attributes.\n\n    Returns:\n        Tuple of ('th', attrs).\n    \"\"\"\n    return (\"th\", attrs)\n</code></pre>"},{"location":"api/guictrl_tag_preprocess/#pytigon_gui.guictrl.tag_preprocess.select_to_ctrltab","title":"select_to_ctrltab","text":"<pre><code>select_to_ctrltab(parent, attrs)\n</code></pre> <p>Convert  to ctrl-select. <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser.</p> required <code>attrs</code> <p>Select attributes.</p> required <p>Returns:</p> Type Description <p>Tuple of ('ctrl-select', attrs).</p> Source code in <code>pytigon_gui/guictrl/tag_preprocess.py</code> <pre><code>def select_to_ctrltab(parent, attrs):\n    \"\"\"Convert &lt;select&gt; to ctrl-select.\n\n    Args:\n        parent: Parent parser.\n        attrs: Select attributes.\n\n    Returns:\n        Tuple of ('ctrl-select', attrs).\n    \"\"\"\n    return (\"ctrl-select\", attrs)\n</code></pre>"},{"location":"api/guictrl_tag_preprocess/#pytigon_gui.guictrl.tag_preprocess.table_to_ctrltab","title":"table_to_ctrltab","text":"<pre><code>table_to_ctrltab(parent, attrs)\n</code></pre> <p>Convert  to ctrl-table or ctrl-list based on CSS class. <ul> <li>'tabsort' class \u2192 ctrl-table with minheight 60px</li> <li>'listctrl' class \u2192 ctrl-list</li> <li>Otherwise \u2192 unchanged  <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser.</p> required <code>attrs</code> <p>Table tag attributes.</p> required <p>Returns:</p> Type Description <p>Tuple of (new_tag, new_attrs).</p> Source code in <code>pytigon_gui/guictrl/tag_preprocess.py</code> <pre><code>def table_to_ctrltab(parent, attrs):\n    \"\"\"Convert &lt;table&gt; to ctrl-table or ctrl-list based on CSS class.\n\n    - 'tabsort' class \u2192 ctrl-table with minheight 60px\n    - 'listctrl' class \u2192 ctrl-list\n    - Otherwise \u2192 unchanged &lt;table&gt;\n\n    Args:\n        parent: Parent parser.\n        attrs: Table tag attributes.\n\n    Returns:\n        Tuple of (new_tag, new_attrs).\n    \"\"\"\n    if \"class\" in attrs:\n        if \"tabsort\" in attrs[\"class\"]:\n            attrs[\"minheight\"] = \"60px\"\n            return (\"ctrl-table\", attrs)\n        if \"listctrl\" in attrs[\"class\"]:\n            return (\"ctrl-list\", attrs)\n    return (\"table\", attrs)\n</code></pre>"},{"location":"api/guictrl_tag_preprocess/#pytigon_gui.guictrl.tag_preprocess.textarea_to_ctrltab","title":"textarea_to_ctrltab","text":"<pre><code>textarea_to_ctrltab(parent, attrs)\n</code></pre> <p>Convert  to ctrl-textarea. <p>Forwards cols, rows, src, and name attributes.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser.</p> required <code>attrs</code> <p>Textarea attributes.</p> required <p>Returns:</p> Type Description <p>Tuple of ('ctrl-textarea', new_attrs).</p> Source code in <code>pytigon_gui/guictrl/tag_preprocess.py</code> <pre><code>def textarea_to_ctrltab(parent, attrs):\n    \"\"\"Convert &lt;textarea&gt; to ctrl-textarea.\n\n    Forwards cols, rows, src, and name attributes.\n\n    Args:\n        parent: Parent parser.\n        attrs: Textarea attributes.\n\n    Returns:\n        Tuple of ('ctrl-textarea', new_attrs).\n    \"\"\"\n    attrs_ret = {}\n    for key in (\"cols\", \"rows\", \"src\", \"name\"):\n        if key in attrs:\n            attrs_ret[key] = attrs[key]\n    return (\"ctrl-textarea\", attrs_ret)\n</code></pre>"},{"location":"api/guictrl_tag_preprocess/#pytigon_gui.guictrl.tag_preprocess.ul_convert","title":"ul_convert","text":"<pre><code>ul_convert(parent, attrs)\n</code></pre> <p>Convert <ul> based on CSS class. <ul> <li>'root' class \u2192 ldata (tree data container)</li> <li>'errorlist' class \u2192 ctrl-errorlist</li> </ul> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent parser.</p> required <code>attrs</code> <p>UL attributes.</p> required <p>Returns:</p> Type Description <p>Tuple of (new_tag, attrs).</p> Source code in <code>pytigon_gui/guictrl/tag_preprocess.py</code> <pre><code>def ul_convert(parent, attrs):\n    \"\"\"Convert &lt;ul&gt; based on CSS class.\n\n    - 'root' class \u2192 ldata (tree data container)\n    - 'errorlist' class \u2192 ctrl-errorlist\n\n    Args:\n        parent: Parent parser.\n        attrs: UL attributes.\n\n    Returns:\n        Tuple of (new_tag, attrs).\n    \"\"\"\n    if \"class\" in attrs:\n        if \"root\" in attrs[\"class\"]:\n            return (\"ldata\", attrs)\n        if attrs[\"class\"] == \"errorlist\":\n            return (\"ctrl-errorlist\", attrs)\n    return (\"ul\", attrs)\n</code></pre>"},{"location":"api/guiframe/","title":"guiframe \u2013 Frame Management","text":"<p>Application frame system: main app window, browser frame, form dialogs, notebook tabs, and page components.</p>"},{"location":"api/guiframe/#pytigon_gui.guiframe","title":"guiframe","text":"<p>Pytigon GUI frame layer -- window hierarchy and layout management.</p> Window hierarchy <p>appframe.SchAppFrame (or browserframe.SchBrowserFrame)     \u2514\u2500\u2500 Notebook (SchNotebook) -- manages tab pages         \u2514\u2500\u2500 SchNotebookPage -- one tab, hosts SchPage windows             \u2514\u2500\u2500 SchPage -- container for up to 4 SchForm instances                 \u251c\u2500\u2500 header  : SchForm (optional sash window at top)                 \u251c\u2500\u2500 body    : SchForm (mandatory, fills center)                 \u251c\u2500\u2500 footer  : SchForm (optional sash window at bottom)                 \u2514\u2500\u2500 panel   : SchForm (optional sash window at left)</p> <p>Modules:</p> Name Description <code>appframe       - SchAppFrame</code> <p>main desktop application window</p> <code>browserframe   - SchBrowserFrame</code> <p>embedded webview variant</p> <code>manager        - SChAuiManager / SChAuiBaseManager</code> <p>AUI helpers</p> <code>notebook       - SchNotebook</code> <p>tab container</p> <code>notebookpage   - SchNotebookPage</code> <p>single tab with split layout</p> <code>page           - SchPage</code> <p>manages body/header/footer/panel forms</p> <code>form           - SchForm</code> <p>HTML-rendered scrollable panel</p>"},{"location":"api/guiframe_appframe/","title":"guiframe.appframe \u2013 Application Frame","text":"<p>Main application window \u2013 wx.Frame subclass with Django embedding, toolbar integration, and notebook management.</p>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe","title":"appframe","text":"<p>This module contains SchAppFrame class. When pytigon application starts, new top frame window (class SchAppFrame) is created. During the initialisation process all defined for application plugins are started.</p>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe-attributes","title":"Attributes","text":""},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_BOOKMARK_ADD","title":"ID_BOOKMARK_ADD  <code>module-attribute</code>","text":"<pre><code>ID_BOOKMARK_ADD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_CLEAR_HISTORY","title":"ID_CLEAR_HISTORY  <code>module-attribute</code>","text":"<pre><code>ID_CLEAR_HISTORY = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_CLOSETAB","title":"ID_CLOSETAB  <code>module-attribute</code>","text":"<pre><code>ID_CLOSETAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_COPY","title":"ID_COPY  <code>module-attribute</code>","text":"<pre><code>ID_COPY = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_CUT","title":"ID_CUT  <code>module-attribute</code>","text":"<pre><code>ID_CUT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_DOWNLOAD_OPTIONS","title":"ID_DOWNLOAD_OPTIONS  <code>module-attribute</code>","text":"<pre><code>ID_DOWNLOAD_OPTIONS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_END","title":"ID_END  <code>module-attribute</code>","text":"<pre><code>ID_END = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_EXIT","title":"ID_EXIT  <code>module-attribute</code>","text":"<pre><code>ID_EXIT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_FIND","title":"ID_FIND  <code>module-attribute</code>","text":"<pre><code>ID_FIND = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_GENERAL_OPTIONS","title":"ID_GENERAL_OPTIONS  <code>module-attribute</code>","text":"<pre><code>ID_GENERAL_OPTIONS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_GOTODESKTOP","title":"ID_GOTODESKTOP  <code>module-attribute</code>","text":"<pre><code>ID_GOTODESKTOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_GOTOFOOTER","title":"ID_GOTOFOOTER  <code>module-attribute</code>","text":"<pre><code>ID_GOTOFOOTER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_GOTOHEADER","title":"ID_GOTOHEADER  <code>module-attribute</code>","text":"<pre><code>ID_GOTOHEADER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_GOTOPANEL","title":"ID_GOTOPANEL  <code>module-attribute</code>","text":"<pre><code>ID_GOTOPANEL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_HELP","title":"ID_HELP  <code>module-attribute</code>","text":"<pre><code>ID_HELP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_HISTORY_SHOW","title":"ID_HISTORY_SHOW  <code>module-attribute</code>","text":"<pre><code>ID_HISTORY_SHOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_LOAD","title":"ID_LOAD  <code>module-attribute</code>","text":"<pre><code>ID_LOAD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_NEXTPAGE","title":"ID_NEXTPAGE  <code>module-attribute</code>","text":"<pre><code>ID_NEXTPAGE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_NEXTTAB","title":"ID_NEXTTAB  <code>module-attribute</code>","text":"<pre><code>ID_NEXTTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_PAGE_BLOCK","title":"ID_PAGE_BLOCK  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_BLOCK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_PAGE_SOURCE","title":"ID_PAGE_SOURCE  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_SOURCE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_PAGE_VIEW","title":"ID_PAGE_VIEW  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_VIEW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_PASTE","title":"ID_PASTE  <code>module-attribute</code>","text":"<pre><code>ID_PASTE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_PREVPAGE","title":"ID_PREVPAGE  <code>module-attribute</code>","text":"<pre><code>ID_PREVPAGE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_PREVTAB","title":"ID_PREVTAB  <code>module-attribute</code>","text":"<pre><code>ID_PREVTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_PRINT","title":"ID_PRINT  <code>module-attribute</code>","text":"<pre><code>ID_PRINT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_PRINT_PREVIEW","title":"ID_PRINT_PREVIEW  <code>module-attribute</code>","text":"<pre><code>ID_PRINT_PREVIEW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_PRINT_SETTINGS","title":"ID_PRINT_SETTINGS  <code>module-attribute</code>","text":"<pre><code>ID_PRINT_SETTINGS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_REDO","title":"ID_REDO  <code>module-attribute</code>","text":"<pre><code>ID_REDO = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_REFRESHTAB","title":"ID_REFRESHTAB  <code>module-attribute</code>","text":"<pre><code>ID_REFRESHTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_REPLACE","title":"ID_REPLACE  <code>module-attribute</code>","text":"<pre><code>ID_REPLACE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_RESET","title":"ID_RESET  <code>module-attribute</code>","text":"<pre><code>ID_RESET = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_RETVALUE","title":"ID_RETVALUE  <code>module-attribute</code>","text":"<pre><code>ID_RETVALUE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_SAVE","title":"ID_SAVE  <code>module-attribute</code>","text":"<pre><code>ID_SAVE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_SAVE_AS","title":"ID_SAVE_AS  <code>module-attribute</code>","text":"<pre><code>ID_SAVE_AS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_SELALL","title":"ID_SELALL  <code>module-attribute</code>","text":"<pre><code>ID_SELALL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_SHOWDESKTOP","title":"ID_SHOWDESKTOP  <code>module-attribute</code>","text":"<pre><code>ID_SHOWDESKTOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_SHOWFOOTER","title":"ID_SHOWFOOTER  <code>module-attribute</code>","text":"<pre><code>ID_SHOWFOOTER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_SHOWHEADER","title":"ID_SHOWHEADER  <code>module-attribute</code>","text":"<pre><code>ID_SHOWHEADER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_SHOWPANEL","title":"ID_SHOWPANEL  <code>module-attribute</code>","text":"<pre><code>ID_SHOWPANEL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_SHOWSTATUSBAR","title":"ID_SHOWSTATUSBAR  <code>module-attribute</code>","text":"<pre><code>ID_SHOWSTATUSBAR = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_SHOWTOOLBAR1","title":"ID_SHOWTOOLBAR1  <code>module-attribute</code>","text":"<pre><code>ID_SHOWTOOLBAR1 = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_SHOWTOOLBAR2","title":"ID_SHOWTOOLBAR2  <code>module-attribute</code>","text":"<pre><code>ID_SHOWTOOLBAR2 = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_SHOW_BOOKMARKS","title":"ID_SHOW_BOOKMARKS  <code>module-attribute</code>","text":"<pre><code>ID_SHOW_BOOKMARKS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_SHOW_DOWNLOAD","title":"ID_SHOW_DOWNLOAD  <code>module-attribute</code>","text":"<pre><code>ID_SHOW_DOWNLOAD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_START","title":"ID_START  <code>module-attribute</code>","text":"<pre><code>ID_START = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_TASKBAR_CLOSE","title":"ID_TASKBAR_CLOSE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_CLOSE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_TASKBAR_HIDE","title":"ID_TASKBAR_HIDE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_HIDE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_TASKBAR_SHOW","title":"ID_TASKBAR_SHOW  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_SHOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_TASKBAR_TOGGLE","title":"ID_TASKBAR_TOGGLE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_TOGGLE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_UNDO","title":"ID_UNDO  <code>module-attribute</code>","text":"<pre><code>ID_UNDO = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_WEB_ADDBOOKMARK","title":"ID_WEB_ADDBOOKMARK  <code>module-attribute</code>","text":"<pre><code>ID_WEB_ADDBOOKMARK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_WEB_BACK","title":"ID_WEB_BACK  <code>module-attribute</code>","text":"<pre><code>ID_WEB_BACK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_WEB_DOWN","title":"ID_WEB_DOWN  <code>module-attribute</code>","text":"<pre><code>ID_WEB_DOWN = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_WEB_EDIT","title":"ID_WEB_EDIT  <code>module-attribute</code>","text":"<pre><code>ID_WEB_EDIT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_WEB_FORWARD","title":"ID_WEB_FORWARD  <code>module-attribute</code>","text":"<pre><code>ID_WEB_FORWARD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_WEB_NEW_WINDOW","title":"ID_WEB_NEW_WINDOW  <code>module-attribute</code>","text":"<pre><code>ID_WEB_NEW_WINDOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_WEB_REFRESH","title":"ID_WEB_REFRESH  <code>module-attribute</code>","text":"<pre><code>ID_WEB_REFRESH = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_WEB_SOURCE","title":"ID_WEB_SOURCE  <code>module-attribute</code>","text":"<pre><code>ID_WEB_SOURCE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_WEB_STOP","title":"ID_WEB_STOP  <code>module-attribute</code>","text":"<pre><code>ID_WEB_STOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_WEB_UP","title":"ID_WEB_UP  <code>module-attribute</code>","text":"<pre><code>ID_WEB_UP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ID_ZOOM","title":"ID_ZOOM  <code>module-attribute</code>","text":"<pre><code>ID_ZOOM = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe._","title":"_  <code>module-attribute</code>","text":"<pre><code>_ = wx.GetTranslation\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe._RECORD_VIDEO","title":"_RECORD_VIDEO  <code>module-attribute</code>","text":"<pre><code>_RECORD_VIDEO = 0\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe._RECORD_VIDEO_MONITOR","title":"_RECORD_VIDEO_MONITOR  <code>module-attribute</code>","text":"<pre><code>_RECORD_VIDEO_MONITOR = None\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe._RECORD_VIDEO_OUT","title":"_RECORD_VIDEO_OUT  <code>module-attribute</code>","text":"<pre><code>_RECORD_VIDEO_OUT = None\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe._RECORD_VIDEO_STRUCT","title":"_RECORD_VIDEO_STRUCT  <code>module-attribute</code>","text":"<pre><code>_RECORD_VIDEO_STRUCT = None\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.logger","title":"logger  <code>module-attribute</code>","text":"<pre><code>logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe-classes","title":"Classes","text":""},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ArtProviderFromIcon","title":"ArtProviderFromIcon","text":"<pre><code>ArtProviderFromIcon()\n</code></pre> <p>               Bases: <code>ArtProvider</code></p> <p>Extended ArtProvider that loads bitmaps from a local icon library.</p> <p>Reads icon identifiers from static/icons/ids.txt and caches bitmaps at 16px, 22px, and 32px sizes.</p> Source code in <code>pytigon_gui/guilib/image.py</code> <pre><code>def __init__(self):\n    wx.ArtProvider.__init__(self)\n    self.tab_16 = {}\n    self.tab_22 = {}\n    self.tab_32 = {}\n\n    ids_path = str(wx.GetApp().src_path) + \"/static/icons/ids.txt\"\n    try:\n        with open(ids_path) as ids:\n            for line in ids:\n                line = line.strip()\n                if not line:\n                    continue\n                l = line.split(\",\")\n                if len(l) &gt; 1:\n                    image_path = l[1]\n                    art_key = \"wx.ART_\" + l[0]\n                    if (art_id := _resolve_art_id(art_key)) is not None:\n                        self.tab_16[art_id] = [image_path, None]\n                        self.tab_22[art_id] = [image_path, None]\n                        self.tab_32[art_id] = [image_path, None]\n    except FileNotFoundError:\n        logger.warning(\"Icon id file not found: %s\", ids_path)\n    except Exception:\n        logger.exception(\"Error loading icon id file: %s\", ids_path)\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ArtProviderFromIcon-functions","title":"Methods:","text":""},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.ArtProviderFromIcon.CreateBitmap","title":"CreateBitmap","text":"<pre><code>CreateBitmap(artid2, client, size)\n</code></pre> <p>Create and cache a bitmap for the given art id and size.</p> <p>Parameters:</p> Name Type Description Default <code>artid2</code> <p>Art id (string).</p> required <code>client</code> <p>Client identifier (unused).</p> required <code>size</code> <p>Requested size in pixels.</p> required <p>Returns:</p> Type Description <p>wx.Bitmap or wx.NullBitmap.</p> Source code in <code>pytigon_gui/guilib/image.py</code> <pre><code>def CreateBitmap(self, artid2, client, size):\n    \"\"\"Create and cache a bitmap for the given art id and size.\n\n    Args:\n        artid2: Art id (string).\n        client: Client identifier (unused).\n        size: Requested size in pixels.\n\n    Returns:\n        wx.Bitmap or wx.NullBitmap.\n    \"\"\"\n    artid = artid2.encode(\"utf-8\")\n    if size not in (16, 22, 32):\n        size = 32\n    path = None\n    cached_table = None\n\n    if size == 16:\n        cached_table = self.tab_16\n        if artid in self.tab_16:\n            if self.tab_16[artid][1]:\n                return self.tab_16[artid][1]\n            path = \"png://\" + self.tab_16[artid][0] + \"?size=0\"\n    elif size == 22:\n        cached_table = self.tab_22\n        if artid in self.tab_22:\n            if self.tab_22[artid][1]:\n                return self.tab_22[artid][1]\n            path = \"png://\" + self.tab_22[artid][0] + \"?size=1\"\n    else:\n        cached_table = self.tab_32\n        if artid in self.tab_32:\n            if self.tab_32[artid][1]:\n                return self.tab_32[artid][1]\n            path = \"png://\" + self.tab_32[artid][0] + \"?size=2\"\n\n    if path is None:\n        return wx.NullBitmap\n\n    try:\n        bitmap = bitmap_from_href(path)\n    except Exception:\n        bitmap = None\n\n    if bitmap and cached_table and artid in cached_table:\n        cached_table[artid][1] = bitmap\n        return bitmap\n    return wx.NullBitmap\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SChAuiManager","title":"SChAuiManager","text":"<pre><code>SChAuiManager(*argi, **argv)\n</code></pre> <p>               Bases: <code>SChAuiBaseManager</code></p> <p>AUI manager that stores pane info on added windows via SetPanel.</p> Source code in <code>pytigon_gui/guiframe/manager.py</code> <pre><code>def __init__(self, *argi, **argv):\n    aui.AuiManager.__init__(self, *argi, **argv)\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SChAuiManager-functions","title":"Methods:","text":""},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SChAuiManager.ActivatePane","title":"ActivatePane","text":"<pre><code>ActivatePane(window)\n</code></pre> <p>Activate a pane, returning None on failure instead of raising.</p> <p>Parameters:</p> Name Type Description Default <code>window</code> <p>The wx.Window whose pane should be activated.</p> required <p>Returns:</p> Type Description <p>Result of parent ActivatePane, or None if it fails.</p> Source code in <code>pytigon_gui/guiframe/manager.py</code> <pre><code>def ActivatePane(self, window):\n    \"\"\"Activate a pane, returning None on failure instead of raising.\n\n    Args:\n        window: The wx.Window whose pane should be activated.\n\n    Returns:\n        Result of parent ActivatePane, or None if it fails.\n    \"\"\"\n    try:\n        return aui.AuiManager.ActivatePane(self, window)\n    except Exception:\n        logger.debug(\"ActivatePane failed for window: %s\", window, exc_info=True)\n        return None\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SChAuiManager.AddPane","title":"AddPane","text":"<pre><code>AddPane(window, arg1, *argi, **argv)\n</code></pre> <p>Add a pane and call SetPanel on the window if supported.</p> <p>Parameters:</p> Name Type Description Default <code>window</code> <p>The wx.Window to add as a pane.</p> required <code>arg1</code> <p>Pane info or direction argument.</p> required <code>*argi</code> <p>Additional positional arguments.</p> <code>()</code> <code>**argv</code> <p>Additional keyword arguments.</p> <code>{}</code> <p>Returns:</p> Type Description <p>Result of the parent AddPane call.</p> Source code in <code>pytigon_gui/guiframe/manager.py</code> <pre><code>def AddPane(self, window, arg1, *argi, **argv):\n    \"\"\"Add a pane and call SetPanel on the window if supported.\n\n    Args:\n        window: The wx.Window to add as a pane.\n        arg1: Pane info or direction argument.\n        *argi: Additional positional arguments.\n        **argv: Additional keyword arguments.\n\n    Returns:\n        Result of the parent AddPane call.\n    \"\"\"\n    ret = aui.AuiManager.AddPane(self, window, arg1, *argi, **argv)\n    if hasattr(window, \"SetPanel\"):\n        window.SetPanel(arg1)\n    return ret\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchAppFrame","title":"SchAppFrame","text":"<pre><code>SchAppFrame(\n    gui_style=\"tree(toolbar,statusbar)\",\n    title=\"\",\n    pos=DefaultPosition,\n    size=DefaultSize,\n    style=DEFAULT_FRAME_STYLE | CLIP_CHILDREN | WANTS_CHARS,\n    video_name=\"\",\n)\n</code></pre> <p>               Bases: <code>SchBaseFrame</code></p> <p>This is main window of pytigon application</p> <p>Constructor</p> <p>Parameters:</p> Name Type Description Default <code>List of key words</code> <p>dialog - application with one form</p> <p>one_form - some as dialog</p> <p>tray - application have system tray icon with some actions</p> <p>statusbar - show the status bar</p> <p>tree - menu in tree form</p> <p>standard - standard for platform interface</p> <p>modern - replace standard toolbar with ribbon bar</p> <p>toolbar - show the tool bar</p> required Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def __init__(\n    self,\n    gui_style=\"tree(toolbar,statusbar)\",\n    title=\"\",\n    pos=wx.DefaultPosition,\n    size=wx.DefaultSize,\n    style=wx.DEFAULT_FRAME_STYLE | wx.CLIP_CHILDREN | wx.WANTS_CHARS,\n    video_name=\"\",\n):\n    \"\"\"Constructor\n\n    Args:\n        gui_style - there is string with some key words. If specyfied key word exist some functionality of\n        SChAppFrame are turned on.\n\n        List of key words:\n\n            dialog - application with one form\n\n            one_form - some as dialog\n\n            tray - application have system tray icon with some actions\n\n            statusbar - show the status bar\n\n            tree - menu in tree form\n\n            standard - standard for platform interface\n\n            modern - replace standard toolbar with ribbon bar\n\n            toolbar - show the tool bar\n\n        title - the window title\n\n        pos - the window position\n\n        size - the window size\n\n        style - see wx.Frame constructor\n    \"\"\"\n\n    SchBaseFrame.__init__(\n        self, None, gui_style, wx.ID_ANY, title, pos, size, style, \"MainWindow\"\n    )\n    # self.SetBackgroundColour(wx.SystemSettings.GetColour(wx.SYS_COLOUR_BACKGROUND))\n\n    self._id = wx.ID_HIGHEST\n    self._perspectives = []\n    self._menu_bar_lp = 0\n    self._toolbar_bar_lp = 1\n    self._proc_mannager = None\n    self._video = video_name\n    self.gui_style = gui_style\n    self.idle_objects = []\n    self.command = {}\n    # self.command_enabled_always = []\n    self.last_pane = None\n    self.active_pane = None\n    self.active_page = None\n    self.active_child_ctrl = None\n\n    self.toolbar_interface = None\n    self.menubar_interface = None\n    self.statusbar_interface = None\n\n    self.destroy_fun_tab = []\n    self.after_init = False\n\n    self.sys_command = dict({\"EXIT\": self.on_exit, \"ABOUT\": self.on_about})\n\n    if \"dialog\" in self.gui_style or \"one_form\" in self.gui_style:\n        hide_on_single_page = True\n    else:\n        hide_on_single_page = False\n\n    self._panel = _SChMainPanel(self, self)\n    self._mgr = SChAuiManager()\n    self._mgr.SetManagedWindow(self._panel)\n\n    if not hasattr(self._mgr, \"SetAGWFlags\"):\n        self._mgr.SetAGWFlags = self._mgr.SetFlags\n        self._mgr.GetAGWFlags = self._mgr.GetFlags\n\n    self._mgr.SetAGWFlags(self._mgr.GetAGWFlags() ^ aui.AUI_MGR_ALLOW_ACTIVE_PANE)\n\n    self.desktop = self._create_notebook_ctrl(hide_on_single_page)\n    self.get_dock_art().SetMetric(aui.AUI_DOCKART_PANE_BORDER_SIZE, 0)\n\n    wx.ArtProvider.Push(ArtProviderFromIcon())\n\n    icon = wx.Icon()\n    b = wx.Bitmap(wx.Image(str(wx.GetApp().src_path) + \"/static/icons/schweb.png\"))\n    icon.CopyFromBitmap(b)\n    self.SetIcon(icon)\n\n    if \"tray\" in gui_style:\n        parent = self\n\n        class _TaskBarIcon(wx.adv.TaskBarIcon):\n            def CreatePopupMenu(self):\n                nonlocal parent\n                parent.PopupMenu(parent.menu_tray)\n                return None\n\n        self.tbIcon = _TaskBarIcon()\n        self.tbIcon.SetIcon(icon, \"Pytigon\")\n    else:\n        self.tbIcon = None\n\n    if \"statusbar\" in gui_style:\n        self.statusbar = self._create_status_bar()\n    else:\n        self.statusbar = None\n\n    wx.GetApp().SetTopWindow(self)\n\n    self.setup_frame()\n\n    if \"tree\" in gui_style:\n        self._sizer = wx.BoxSizer(wx.HORIZONTAL)\n    else:\n        self._sizer = wx.BoxSizer(wx.VERTICAL)\n\n    if \"standard\" in gui_style:\n        if len(wx.GetApp().get_tab(self._toolbar_bar_lp)) &gt; 1:\n            from pytigon_gui.toolbar import standardtoolbar\n\n            self.toolbar_interface = standardtoolbar.StandardToolbarBar(\n                self, gui_style\n            )\n            self._create_tool_bar()\n            self.toolbar_interface.create()\n        self._sizer.Add(self._panel, 1, wx.EXPAND)\n        self._mgr.Update()\n    elif \"generic\" in gui_style:\n        if len(wx.GetApp().get_tab(self._toolbar_bar_lp)) &gt; 1:\n            from pytigon_gui.toolbar import generictoolbar\n\n            self.toolbar_interface = generictoolbar.GenericToolbarBar(\n                self, gui_style\n            )\n            self._create_tool_bar()\n            self.toolbar_interface.create()\n        self._sizer.Add(self._panel, 1, wx.EXPAND)\n        self._mgr.Update()\n    elif \"modern\" in gui_style:\n        from pytigon_gui.toolbar.moderntoolbar import ModernToolbarBar\n\n        self.toolbar_interface = ModernToolbarBar(self, gui_style)\n        self._create_tool_bar()\n        # self.toolbar_interface.realize_bar()\n        self.toolbar_interface.create()\n        self._sizer.Add(self.toolbar_interface, 0, wx.EXPAND)\n        self._sizer.Add(self._panel, 1, wx.EXPAND)\n    elif \"tree\" in gui_style:\n        from pytigon_gui.toolbar.treetoolbar import TreeToolbarBar\n\n        self.toolbar_interface = TreeToolbarBar(self._panel, gui_style)\n        self._create_tool_bar()\n        self.toolbar_interface.create()\n        self._mgr.AddPane(\n            self.toolbar_interface,\n            self._create_pane_info(\"menu\", _(\"Menu\"))\n            .CaptionVisible(True)\n            .MinimizeButton(True)\n            .CloseButton(False)\n            .Left()\n            .BestSize((250, 40))\n            .MinSize((250, 40))\n            .Show(),\n        )\n        self._sizer.Add(self._panel, 1, wx.EXPAND)\n    else:\n        self._sizer.Add(self._panel, 1, wx.EXPAND)\n\n    self.SetSizer(self._sizer)\n\n    if wx.GetApp().get_tab(2) and len(wx.GetApp().get_tab(2)) &gt; 1:\n        self._menu_bar_lp = 2\n    else:\n        if wx.GetApp().menu_always:\n            self._menu_bar_lp = 1\n\n    if self._menu_bar_lp &gt; 0:\n        from pytigon_gui.toolbar import menubar\n\n        self.menubar_interface = menubar.MenuToolbarBar(self, gui_style)\n        self._create_menu_bar()\n\n    s_dx = wx.SystemSettings.GetMetric(wx.SYS_SCREEN_X)\n    s_dy = wx.SystemSettings.GetMetric(wx.SYS_SCREEN_Y)\n\n    self._mgr.AddPane(\n        self._create_notebook_ctrl(),\n        self._create_pane_info(\"panel\", _(\"Tools\"))\n        .CaptionVisible(True)\n        .Left()\n        .MinSize((400, int(s_dy / 2)))\n        .BestSize((int(s_dx / 2) - 50, s_dy - 100))\n        .Show(),\n    )\n    self._mgr.AddPane(\n        self._create_notebook_ctrl(),\n        self._create_pane_info(\"header\", _(\"Header\"))\n        .CaptionVisible(False)\n        .Top()\n        .MinSize((int(s_dx), int(s_dy / 10)))\n        .BestSize((int(s_dx), int(s_dy / 5)))\n        .Show(),\n    )\n    self._mgr.AddPane(\n        self._create_notebook_ctrl(),\n        self._create_pane_info(\"footer\", _(\"Footer\"))\n        .CaptionVisible(False)\n        .Bottom()\n        .MinSize((int(s_dx), int(s_dy / 10)))\n        .BestSize((int(s_dx), int(s_dy / 5)))\n        .Show(),\n    )\n    self._mgr.AddPane(\n        self.desktop,\n        self._create_pane_info(\"desktop\", _(\"Desktop\")).CenterPane().Show(),\n    )\n    perspective_notoolbar = self._mgr.SavePerspective()\n\n    if \"toolbar\" in gui_style and \"standard\" in gui_style:\n        i = 1\n        while True:\n            name = \"tb\" + str(i)\n            tbpanel = self._mgr.GetPane(name)\n            if tbpanel.IsOk():\n                tbpanel.Show()\n            else:\n                break\n            i += 1\n\n    perspective_all = self._mgr.SavePerspective()\n    all_panes = self._mgr.GetAllPanes()\n\n    for ii in range(len(all_panes)):\n        if not all_panes[ii].IsToolbar():\n            if all_panes[ii].name != \"menu\":\n                all_panes[ii].Hide()\n\n    self._mgr.GetPane(\"desktop\").Show()\n\n    perspective_default = self._mgr.SavePerspective()\n\n    size = self.desktop.GetPageCount()\n    for i in range(size):  # @UnusedVariable\n        self.desktop.DeletePage(0)\n\n    self._perspectives.append(perspective_default)\n    self._perspectives.append(perspective_all)\n    self._perspectives.append(perspective_notoolbar)\n\n    self.init_acc_keys()\n    self.init_plugins()\n    self.SetAcceleratorTable(wx.AcceleratorTable(self.aTable))\n\n    self._mgr.Update()\n\n    self.t1 = wx.Timer(self)\n    self.t1.Start(250)\n\n    self.Bind(wx.EVT_TIMER, self.on_timer, self.t1)\n    self.Bind(aui.EVT_AUI_PANE_ACTIVATED, self.on_pane_activated)\n    self.Bind(\n        wx.EVT_MENU_RANGE, self.on_show_elem, id=ID_SHOWHEADER, id2=ID_SHOWTOOLBAR2\n    )\n\n    if \"tray\" in gui_style:\n        self.Bind(wx.EVT_CLOSE, self.on_taskbar_hide)\n    else:\n        self.Bind(wx.EVT_CLOSE, self.on_close)\n\n    self._panel.Bind(wx.EVT_CHILD_FOCUS, self.on_child_focus)\n\n    self.bind_command(self.on_open, id=wx.ID_OPEN)\n    self.bind_command(self.on_exit, id=wx.ID_EXIT)\n\n    # if self.toolbar_interface:\n    # self.toolbar_interface.bind_ui(self.on_update_ui_command, wx.ID_ANY)\n\n    # for pos in [wx.ID_EXIT, ID_WEB_NEW_WINDOW, wx.ID_OPEN]:\n    #    self.command_enabled_always.append(pos)\n\n    self.bind_command(self.on_next_tab, id=ID_NEXTTAB)\n    self.bind_command(self.on_prev_tab, id=ID_PREVTAB)\n    self.bind_command(self.on_next_page, id=ID_NEXTPAGE)\n    self.bind_command(self.on_prev_page, id=ID_PREVPAGE)\n    self.bind_command(self.on_close_tab, id=ID_CLOSETAB)\n    self.bind_command(self.on_refresh_tab, id=ID_REFRESHTAB)\n\n    self.bind_command(self.on_goto_desktop, id=ID_GOTODESKTOP)\n    self.bind_command(self.on_goto_head, id=ID_GOTOHEADER)\n    self.bind_command(self.on_goto_panel, id=ID_GOTOPANEL)\n    self.bind_command(self.on_goto_footer, id=ID_GOTOFOOTER)\n\n    self.bind_command(self.on_command, id=ID_WEB_NEW_WINDOW)\n\n    self.bind_command(self.on_show_status_bar, id=ID_SHOWSTATUSBAR)\n\n    if self.menubar_interface:\n        self.SetMenuBar(self.menubar_interface)\n\n    if self.tbIcon:\n        # self.tbIcon.Bind(wx.adv.EVT_TASKBAR_LEFT_DCLICK, self.on_taskbar_toogle)\n        # self.tbIcon.Bind(wx.adv.EVT_TASKBAR_RIGHT_UP, self.on_taskbar_show_menu)\n\n        self.menu_tray = wx.Menu()\n        self.menu_tray.Append(ID_TASKBAR_SHOW, _(\"Show It\"))\n        self.Bind(wx.EVT_MENU, self.on_taskbar_show, id=ID_TASKBAR_SHOW)\n        self.menu_tray.Append(ID_TASKBAR_HIDE, _(\"Minimize It\"))\n        self.Bind(wx.EVT_MENU, self.on_taskbar_hide, id=ID_TASKBAR_HIDE)\n        self.menu_tray.AppendSeparator()\n        self.menu_tray.Append(ID_TASKBAR_CLOSE, _(\"Close It\"))\n        self.Bind(wx.EVT_MENU, self.on_close, id=ID_TASKBAR_CLOSE)\n\n    self.SetExtraStyle(wx.WS_EX_PROCESS_UI_UPDATES)\n    wx.UpdateUIEvent.SetUpdateInterval(50)\n    wx.UpdateUIEvent.SetMode(wx.UPDATE_UI_PROCESS_SPECIFIED)\n    wx.CallAfter(self.UpdateWindowUI)\n    self.Layout()\n    wx.CallAfter(self.Bind, wx.EVT_IDLE, self.on_idle)\n    wx.CallAfter(self.Show)\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchAppFrame-functions","title":"Methods:","text":""},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchAppFrame.bind_command","title":"bind_command","text":"<pre><code>bind_command(fun, id=ID_ANY)\n</code></pre> <p>bind command event, unlike wxPython Bind this function bind command to menu and toolbar interface</p> Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def bind_command(self, fun, id=wx.ID_ANY):\n    \"\"\"bind command event, unlike wxPython Bind this function bind command to menu and toolbar interface\"\"\"\n    self.Bind(wx.EVT_MENU, fun, id=id)\n    if self.toolbar_interface:\n        self.toolbar_interface.bind(fun, id)\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchAppFrame.bind_to_toolbar","title":"bind_to_toolbar","text":"<pre><code>bind_to_toolbar(funct, id)\n</code></pre> <p>bind function to toolbar button</p> Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def bind_to_toolbar(self, funct, id):\n    \"\"\"bind function to toolbar button\n\n    Args:\n        funct - function (event handler) to bind\n        id - toolbar button id\n    \"\"\"\n    if \"toolbar\" in self.gui_style:\n        self.toolbar_interface.bind(funct, id)\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchAppFrame.count_shown_panels","title":"count_shown_panels","text":"<pre><code>count_shown_panels(count_toolbars=True)\n</code></pre> <p>count visible panels</p> Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def count_shown_panels(self, count_toolbars=True):\n    \"\"\"count visible panels\"\"\"\n    count = 0\n    for panel in self._mgr.GetAllPanes():\n        if panel.IsShown():\n            if (\"Toolbar\" not in panel.caption) or count_toolbars:\n                count += 1\n    return count\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchAppFrame.download_data","title":"download_data","text":"<pre><code>download_data(response, title='', parameters=None)\n</code></pre> <p>Download response data to the user's Downloads folder and open it.</p> <p>Parameters:</p> Name Type Description Default <code>response</code> <p>HTTP response with binary content.</p> required <code>title</code> <p>Optional title (unused).</p> <code>''</code> <code>parameters</code> <p>Optional parameters (unused).</p> <code>None</code> <p>Returns:</p> Type Description <p>True on success.</p> Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def download_data(self, response, title=\"\", parameters=None):\n    \"\"\"Download response data to the user's Downloads folder and open it.\n\n    Args:\n        response: HTTP response with binary content.\n        title: Optional title (unused).\n        parameters: Optional parameters (unused).\n\n    Returns:\n        True on success.\n    \"\"\"\n    cd = response.response.headers.get(\"content-disposition\")\n    if cd:\n        name = cd.split(\"filename=\")[1].replace('\"', \"\")\n    else:\n        name = \"data.dat\"\n\n    p = response.ptr()\n\n    path = Path.home() / \"Downloads\"\n    file_path = path / name\n\n    try:\n        with open(file_path, \"wb\") as f:\n            f.write(p)\n    except OSError as e:\n        logger.error(\"Error saving download to %s: %s\", file_path, e)\n        return False\n\n    try:\n        if platform.system() == \"Windows\":\n            os.startfile(file_path)\n        elif platform.system() == \"Darwin\":\n            subprocess.Popen([\"open\", file_path])\n        else:\n            subprocess.Popen([\"xdg-open\", file_path])\n    except Exception as e:\n        logger.error(\"Error opening download %s: %s\", file_path, e)\n\n    return True\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchAppFrame.get_active_ctrl","title":"get_active_ctrl","text":"<pre><code>get_active_ctrl()\n</code></pre> <p>Get active widget</p> Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def get_active_ctrl(self):\n    \"\"\"Get active widget\"\"\"\n    ctrl = None\n    idn = self.desktop.GetSelection()\n    if idn &gt;= 0:\n        panel = self.desktop.GetPage(idn)\n        if panel:\n            count = panel.get_page_count()\n            if count &gt; 0:\n                htmlwin = panel.get_page(count - 1)\n                if htmlwin:\n                    ctrl = htmlwin.get_last_control_with_focus()\n            return ctrl\n    return None\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchAppFrame.get_active_panel","title":"get_active_panel","text":"<pre><code>get_active_panel()\n</code></pre> <p>get active form</p> Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def get_active_panel(self):\n    \"\"\"get active form\"\"\"\n    idn = self.desktop.GetSelection()\n    if idn &gt;= 0:\n        panel = self.desktop.GetPage(idn)\n        if panel:\n            count = panel.GetPageCount()\n            if count &gt; 0:\n                htmlwin = panel.GetPage(count - 1)\n                return htmlwin\n    return None\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchAppFrame.get_dock_art","title":"get_dock_art","text":"<pre><code>get_dock_art()\n</code></pre> <p>return art provider related to the asociated frame manager</p> Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def get_dock_art(self):\n    \"\"\"return art provider related to the asociated frame manager\"\"\"\n    return self._mgr.GetArtProvider()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchAppFrame.get_frame_manager","title":"get_frame_manager","text":"<pre><code>get_frame_manager()\n</code></pre> <p>returns frame manager - aui.framemanager.AuiManager derived object</p> Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def get_frame_manager(self):\n    \"\"\"returns frame manager - aui.framemanager.AuiManager derived object\"\"\"\n    return self._mgr\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchAppFrame.get_menu_bar","title":"get_menu_bar","text":"<pre><code>get_menu_bar()\n</code></pre> <p>return toolbar interface</p> Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def get_menu_bar(self):\n    \"\"\"return toolbar interface\"\"\"\n    return self.menubar_interface\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchAppFrame.get_tool_bar","title":"get_tool_bar","text":"<pre><code>get_tool_bar()\n</code></pre> <p>return toolbar interface</p> Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def get_tool_bar(self):\n    \"\"\"return toolbar interface\"\"\"\n    return self.toolbar_interface\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchAppFrame.goto_panel","title":"goto_panel","text":"<pre><code>goto_panel(panel_name)\n</code></pre> <p>Activate panel</p> <p>Parameters:</p> Name Type Description Default <code>panel_name</code> <p>name of panel to activate</p> required Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def goto_panel(self, panel_name):\n    \"\"\"Activate panel\n\n    Args:\n        panel_name: name of panel to activate\n    \"\"\"\n    panel = self._mgr.GetPane(panel_name)\n    if panel.window.GetPageCount() &gt; 0:\n        if not panel.IsShown():\n            panel.Show(True)\n            self._mgr.Update()\n        panel.window.SetFocus()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchAppFrame.new_main_page","title":"new_main_page","text":"<pre><code>new_main_page(\n    address_or_parser, title=\"\", parameters=None, view_in=\"desktop\", http=None\n)\n</code></pre> <p>Open a new page in main</p> <p>Parameters:</p> Name Type Description Default <code>address_or_parser</code> <p>can be: address of http page (str type) or</p> required <code></code> required <code>view_in - options are</code> <p>'desktop', 'panel', 'header', 'footer'</p> required Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def new_main_page(\n    self, address_or_parser, title=\"\", parameters=None, view_in=\"desktop\", http=None\n):\n    \"\"\"Open a new page in main\n\n    Args:\n        address_or_parser: can be: address of http page (str type) or\n        :class:'~pytigon_lib.schparser.html_parsers.ShtmlParser'\n        title - new tab title\n        parameters - parameters of http request\n        view_in - options are: 'desktop', 'panel', 'header', 'footer'\n    Returns:\n        created form :class:'~pytigon_gui.guiframe.htmlsash.SchPage'\n    \"\"\"\n\n    if type(address_or_parser) == str:\n        address = address_or_parser\n        if (\n            not address.startswith(\"^\")\n            and not address.startswith(\"file://\")\n            and view_in == \"desktop\"\n        ):\n            return self.open_page(address_or_parser, title, parameters, view_in)\n\n    elif type(address_or_parser) == HttpResponse:\n        address = address_or_parser.url\n    else:\n        address = address_or_parser.address\n\n    ext = address.split(\"?\")[0].split(\".\")[-1]\n    if (\n        (not view_in or \"browser\" not in view_in)\n        and ext\n        and ext\n        in (\n            \"pdf\",\n            \"spdf\",\n            \"ods\",\n            \"odp\",\n            \"odt\",\n            \"docx\",\n            \"xlsx\",\n            \"pptx\",\n            \"zip\",\n            \"avi\",\n            \"mpeg\",\n            \"mp3\",\n            \"mp4\",\n        )\n    ):\n        return self.open_page(address, title, parameters, view_in)\n\n    if view_in:\n        panel = view_in\n    else:\n        parm = split2(address, \"?\")\n        if len(parm) == 2:\n            panel = \"desktop\"\n            parm2 = parm[1].split(\",\")\n            parm3 = [pos.split(\"=\") for pos in parm2]\n            for pos in parm3:\n                if len(pos) == 2:\n                    if pos[0] == \"show_in\":\n                        panel = pos[1]\n                        break\n\n    if panel == \"pscript\":\n        if not http:\n            http = wx.GetApp().get_http(self)\n        response = http.get(self, address)\n        ptr = response.str()\n        exec(ptr)\n        return\n\n    if not address.startswith(\"^\"):\n        if (not view_in or view_in.startswith(\"browser\")) or (\n            address.startswith(\"http\")\n            and not address.startswith(wx.GetApp().base_address)\n        ):\n            if view_in and \"_\" in view_in:\n                panel = view_in.split(\"_\")[1]\n            else:\n                panel = \"desktop\"\n            ret = self.new_main_page(\n                \"^standard/webview/widget_web.html\", \"Empty page\", view_in=panel\n            )\n            if (\n                address.startswith(\"http://\")\n                or address.startswith(\"https://\")\n                or address.startswith(\"file://\")\n            ):\n\n                def _ret_fun():\n                    if type(address_or_parser) == str:\n                        ret.body.WEB.go(address)\n                    else:\n                        ret.body.WEB.load_str(address_or_parser.ptr())\n\n                wx.CallAfter(_ret_fun)\n            else:\n\n                def _ret_fun():\n                    if type(address_or_parser) == str:\n                        ret.body.WEB.go(wx.GetApp().base_path + address)\n                    else:\n                        ret.body.WEB.load_str(address_or_parser.ptr())\n\n                wx.CallAfter(_ret_fun)\n            return ret\n\n    if len(title) &lt; 32:\n        title2 = title\n    else:\n        title2 = title[:30] + \"...\"\n\n    if panel.startswith(\"toolbar\"):\n        name = panel[7:]\n        if name[0:1] == \"_\":\n            return self.toolbar_interface.create_html_win(\n                name[1:], address_or_parser, parameters\n            )\n        else:\n            return self.toolbar_interface.create_html_win(\n                None, address_or_parser, parameters\n            )\n    if panel == \"desktop2\":\n        n = self._mgr.GetPane(\"desktop\").window\n    else:\n        n = self._mgr.GetPane(panel).window\n\n    if not self._mgr.GetPane(panel).IsShown():\n        refr = True\n    else:\n        refr = False\n\n    if title2 in [pos.caption for pos in n._tabs._pages]:\n        for id, pos in enumerate(n._tabs._pages):\n            if pos.caption == title2:\n                n.SetSelection(id)\n                n.activate_page(pos.window)\n        if refr:\n            self._mgr.GetPane(panel).Show()\n            self._mgr.Update()\n        return None\n\n    page = SchNotebookPage(n)\n    if panel == \"desktop2\":\n        if title is None:\n            if type(address_or_parser) == str:\n                n.add_and_split(page, \"\", wx.RIGHT)\n            else:\n                n.add_and_split(page, address_or_parser.title, wx.RIGHT)\n        else:\n            n.add_and_split(page, title2, wx.RIGHT)\n    else:\n        if title is None:\n            if type(address_or_parser) == str:\n                n.AddPage(page, \"\", True)\n            else:\n                n.AddPage(page, address_or_parser.title, True)\n        else:\n            n.AddPage(page, title2, True)\n\n    if type(address_or_parser) == str:\n        address = address_or_parser\n    elif type(address_or_parser) == HttpResponse:\n        address = address_or_parser.url\n\n    else:\n        address = address_or_parser.address\n\n    if http:\n        page.http = http\n    else:\n        page.http = wx.GetApp().get_http_for_adr(address)\n\n    if refr:\n        self._mgr.GetPane(panel).Show()\n    self._mgr.Update()\n\n    return page.new_child_page(address_or_parser, None, parameters)\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchAppFrame.on_close_tab","title":"on_close_tab","text":"<pre><code>on_close_tab(evt)\n</code></pre> <p>close active tab in active notebook</p> Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def on_close_tab(self, evt):\n    \"\"\"close active tab in active notebook\"\"\"\n    win = wx.Window.FindFocus()\n    while win:\n        if win.__class__.__name__ == \"SchNotebook\":\n            id = win.GetSelection()\n            if id &gt;= 0:\n                panel = win.GetPage(id)\n                panel.close_child_page(True)\n            return\n        win = win.GetParent()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchAppFrame.on_next_page","title":"on_next_page","text":"<pre><code>on_next_page(evt)\n</code></pre> <p>scroll active form one page down</p> Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def on_next_page(self, evt):\n    \"\"\"scroll active form one page down\"\"\"\n    self._on_page_event(1)\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchAppFrame.on_next_tab","title":"on_next_tab","text":"<pre><code>on_next_tab(evt)\n</code></pre> <p>select a next tab in desktop notebook</p> Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def on_next_tab(self, evt):\n    \"\"\"select a next tab in desktop notebook\"\"\"\n    desktop = wx.GetApp().GetTopWindow().desktop\n    id = desktop.GetPageIndex(desktop.GetCurrentPage())\n    idn = 0\n    if id != None and id &gt;= 0:\n        idn = id + 1\n    if idn &gt;= desktop.GetPageCount():\n        idn = 0\n    desktop.SetSelection(idn)\n    return\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchAppFrame.on_prev_page","title":"on_prev_page","text":"<pre><code>on_prev_page(evt)\n</code></pre> <p>scroll active form one page up</p> Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def on_prev_page(self, evt):\n    \"\"\"scroll active form one page up\"\"\"\n    self._on_page_event(-1)\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchAppFrame.on_prev_tab","title":"on_prev_tab","text":"<pre><code>on_prev_tab(evt)\n</code></pre> <p>select a previous tab in desktop notebook</p> Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def on_prev_tab(self, evt):\n    \"\"\"select a previous tab in desktop notebook\"\"\"\n    desktop = wx.GetApp().GetTopWindow().desktop\n    id = desktop.GetSelection()\n    idn = 0\n    if id &gt;= 0:\n        idn = id - 1\n    if idn &lt; 0:\n        idn = desktop.GetPageCount() - 1\n    desktop.SetSelection(idn)\n    return\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchAppFrame.on_refresh_tab","title":"on_refresh_tab","text":"<pre><code>on_refresh_tab(evt)\n</code></pre> <p>Refresh content of active form</p> Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def on_refresh_tab(self, evt):\n    \"\"\"Refresh content of active form\"\"\"\n    desktop = wx.GetApp().GetTopWindow().desktop\n    idn = desktop.GetSelection()\n    if idn &gt;= 0:\n        panel = desktop.GetPage(idn)\n        panel.refresh_html()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchAppFrame.show_document","title":"show_document","text":"<pre><code>show_document(response, title='', parameters=None)\n</code></pre> <p>Open a document (odf, office, video, etc.) with the system handler.</p> <p>Saves the response content to a temp file and opens it with the default application for the file type.</p> <p>Parameters:</p> Name Type Description Default <code>response</code> <p>HTTP response with binary content.</p> required <code>title</code> <p>Optional title (unused).</p> <code>''</code> <code>parameters</code> <p>Optional parameters (unused).</p> <code>None</code> Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def show_document(self, response, title=\"\", parameters=None):\n    \"\"\"Open a document (odf, office, video, etc.) with the system handler.\n\n    Saves the response content to a temp file and opens it with the\n    default application for the file type.\n\n    Args:\n        response: HTTP response with binary content.\n        title: Optional title (unused).\n        parameters: Optional parameters (unused).\n    \"\"\"\n    cd = response.response.headers.get(\"content-disposition\")\n    if cd:\n        name = cd.split(\"filename=\")[1].replace('\"', \"\")\n    else:\n        name = \"data.dat\"\n\n    p = response.ptr()\n    path = gettempdir()\n\n    file_path = Path(path) / name\n\n    try:\n        with open(file_path, \"wb\") as f:\n            f.write(p)\n    except OSError as e:\n        logger.error(\"Error writing document to %s: %s\", file_path, e)\n        return\n\n    try:\n        if platform.system() == \"Windows\":\n            os.startfile(file_path)\n        elif platform.system() == \"Darwin\":\n            subprocess.Popen([\"open\", file_path])\n        else:\n            subprocess.Popen([\"xdg-open\", file_path])\n    except Exception as e:\n        logger.error(\"Error opening document %s: %s\", file_path, e)\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchAppFrame.show_pdf","title":"show_pdf","text":"<pre><code>show_pdf(response, title='', parameters=None)\n</code></pre> <p>show pdf downloaded from web server</p> <p>Parameters:</p> Name Type Description Default <code>page</code> <p>web page address</p> required Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def show_pdf(self, response, title=\"\", parameters=None):\n    \"\"\"show pdf downloaded from web server\n\n    Args:\n        page: web page address\n    \"\"\"\n\n    temp_filename = schfs.get_temp_filename(ext=\"pdf\")\n    with schfs.open_file(temp_filename, \"wb\") as f:\n        f.write(response.ptr())\n\n    form_frame = self.new_main_page(\n        \"file://\" + temp_filename, temp_filename, view_in=\"browser\"\n    )\n\n    def _after_init():\n        form_frame.body.WEB.execute_javascript(f\"document.title = '{title}';\")\n\n    wx.CallAfter(_after_init)\n\n    return form_frame\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchBaseFrame","title":"SchBaseFrame","text":"<pre><code>SchBaseFrame(\n    parent,\n    gui_style=\"tree(toolbar,statusbar)\",\n    id=-1,\n    title=\"\",\n    pos=DefaultPosition,\n    size=DefaultSize,\n    style=DEFAULT_FRAME_STYLE | CLIP_CHILDREN | WANTS_CHARS,\n    name=\"MainWindow\",\n)\n</code></pre> <p>               Bases: <code>Frame</code></p> <p>Base main window for Pytigon applications.</p> <p>Handles plugin initialization and close event management.</p> <p>Initialize the base frame.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window (usually None for top-level).</p> required <code>gui_style</code> <p>Style string describing UI layout (e.g. \"tree(toolbar,statusbar)\").</p> <code>'tree(toolbar,statusbar)'</code> <code>id</code> <p>Window identifier.</p> <code>-1</code> <code>title</code> <p>Window title.</p> <code>''</code> <code>pos</code> <p>Window position.</p> <code>DefaultPosition</code> <code>size</code> <p>Window size.</p> <code>DefaultSize</code> <code>style</code> <p>wx.Frame style flags.</p> <code>DEFAULT_FRAME_STYLE | CLIP_CHILDREN | WANTS_CHARS</code> <code>name</code> <p>Widget name.</p> <code>'MainWindow'</code> Source code in <code>pytigon_gui/guiframe/baseframe.py</code> <pre><code>def __init__(\n    self,\n    parent,\n    gui_style=\"tree(toolbar,statusbar)\",\n    id=-1,\n    title=\"\",\n    pos=wx.DefaultPosition,\n    size=wx.DefaultSize,\n    style=wx.DEFAULT_FRAME_STYLE | wx.CLIP_CHILDREN | wx.WANTS_CHARS,\n    name=\"MainWindow\",\n):\n    \"\"\"Initialize the base frame.\n\n    Args:\n        parent: Parent window (usually None for top-level).\n        gui_style: Style string describing UI layout (e.g. \"tree(toolbar,statusbar)\").\n        id: Window identifier.\n        title: Window title.\n        pos: Window position.\n        size: Window size.\n        style: wx.Frame style flags.\n        name: Widget name.\n    \"\"\"\n    wx.Frame.__init__(self, None, wx.ID_ANY, title, pos, size, style, \"MainWindow\")\n    self.run_on_close = []\n    self.destroy_fun_tab = []\n    self.Bind(wx.EVT_CLOSE, self.on_close)\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchBaseFrame-functions","title":"Methods:","text":""},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchBaseFrame.init_plugins","title":"init_plugins","text":"<pre><code>init_plugins()\n</code></pre> <p>Discover and initialize application plugins.</p> <p>Searches for plugins in: 1. Built-in pytigon appdata/plugins directory 2. User data plugins directory 3. Application-specific plugins directory</p> <p>Plugins in the 'auto' directory or listed in auto_plugins config are loaded automatically. Each plugin module may expose an <code>init_plugin</code> function that receives the application, frame, desktop notebook, frame manager, menu bar, toolbar interface, and accelerator table.</p> Source code in <code>pytigon_gui/guiframe/baseframe.py</code> <pre><code>def init_plugins(self):\n    \"\"\"Discover and initialize application plugins.\n\n    Searches for plugins in:\n    1. Built-in pytigon appdata/plugins directory\n    2. User data plugins directory\n    3. Application-specific plugins directory\n\n    Plugins in the 'auto' directory or listed in auto_plugins config\n    are loaded automatically. Each plugin module may expose an\n    ``init_plugin`` function that receives the application, frame,\n    desktop notebook, frame manager, menu bar, toolbar interface,\n    and accelerator table.\n    \"\"\"\n    home_dir = wx.GetApp().get_working_dir()\n    app_plugins = Path(wx.GetApp().cwd) / \"plugins\"\n    dirnames = [\n        Path(wx.GetApp().pytigon_path) / \"appdata\" / \"plugins\",\n        Path(wx.GetApp().data_path) / \"plugins\",\n        app_plugins,\n    ]\n\n    auto_plugins = []\n    try:\n        auto_plugins = (\n            wx.GetApp().config[\"Global settings\"].get(\"auto_plugins\", \"\").split(\";\")\n        )\n    except (KeyError, AttributeError):\n        pass\n\n    for dirname in dirnames:\n        if not Path(dirname).exists():\n            continue\n\n        for ff in sorted(os.listdir(dirname)):\n            dirname2 = Path(dirname) / ff\n            if not Path(dirname2).is_dir():\n                continue\n\n            files = sorted(os.listdir(dirname2))\n\n            for f in files:\n                if not (Path(dirname2) / f).is_dir():\n                    continue\n\n                mod_name = ff + \".\" + f\n                # Skip internal modules\n                if \".__\" in mod_name:\n                    break\n\n                x = mod_name.replace(\".\", \"/\")\n                if not (\n                    ff == \"auto\"\n                    or (wx.GetApp().plugins and x in wx.GetApp().plugins)\n                    or x in auto_plugins\n                ):\n                    continue\n\n                try:\n                    mod = import_plugin(mod_name)\n                except Exception:\n                    logger.exception(\"Error importing plugin: %s\", mod_name)\n                    continue\n\n                if not hasattr(mod, \"init_plugin\"):\n                    continue\n\n                try:\n                    destroy = mod.init_plugin(\n                        wx.GetApp(),\n                        self,\n                        getattr(self, \"desktop\", None),\n                        getattr(self, \"_mgr\", None),\n                        self.get_menu_bar(),\n                        getattr(self, \"toolbar_interface\", None),\n                        getattr(self, \"aTable\", None),\n                    )\n                except Exception:\n                    logger.exception(\"Error initializing plugin: %s\", mod_name)\n                    continue\n\n                if destroy is not None:\n                    self.destroy_fun_tab.append(destroy)\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchBaseFrame.on_close","title":"on_close","text":"<pre><code>on_close(event)\n</code></pre> <p>Handle the window close event.</p> <p>Calls all registered close handlers in order, then allows the event to propagate.</p> Source code in <code>pytigon_gui/guiframe/baseframe.py</code> <pre><code>def on_close(self, event):\n    \"\"\"Handle the window close event.\n\n    Calls all registered close handlers in order, then allows\n    the event to propagate.\n    \"\"\"\n    for fun in self.run_on_close:\n        try:\n            fun(self)\n        except Exception:\n            traceback.print_exc()\n    event.Skip()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebook","title":"SchNotebook","text":"<pre><code>SchNotebook(\n    parent,\n    pos=DefaultPosition,\n    size=DefaultSize,\n    style=BORDER_NONE | TAB_TRAVERSAL | WANTS_CHARS,\n)\n</code></pre> <p>               Bases: <code>AuiNotebook</code></p> <p>Custom AUI notebook for Pytigon applications.</p> <p>Manages tab pages within desktop, panel, header, and footer containers.  Supports page splitting, custom close behaviour, and wiki-based help on double-click of a tab.</p> <p>Construct a SchNotebook.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>pos</code> <p>Window position (default: wx.DefaultPosition).</p> <code>DefaultPosition</code> <code>size</code> <p>Window size (default: wx.DefaultSize).</p> <code>DefaultSize</code> <code>style</code> <p>Window style flags.</p> <code>BORDER_NONE | TAB_TRAVERSAL | WANTS_CHARS</code> Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def __init__(\n    self,\n    parent,\n    pos=wx.DefaultPosition,\n    size=wx.DefaultSize,\n    style=wx.BORDER_NONE | wx.TAB_TRAVERSAL | wx.WANTS_CHARS,\n):\n    \"\"\"Construct a SchNotebook.\n\n    Args:\n        parent: Parent window.\n        pos: Window position (default: wx.DefaultPosition).\n        size: Window size (default: wx.DefaultSize).\n        style: Window style flags.\n    \"\"\"\n    self._curpage = -1\n    self._tab_id_counter = AuiBaseTabCtrlId\n    self._dummy_wnd = None\n    self._hide_tabs = False\n    self._sash_dclick_unsplit = False\n    self._tab_ctrl_height = 20\n    self._requested_bmp_size = wx.Size(-1, -1)\n    self._requested_tabctrl_height = -1\n    self._textCtrl = None\n    self._tabBounds = (-1, -1)\n\n    self.panel = None\n    self.active = False\n    self.closing = False\n    self.last_active = None\n\n    # Deliberately call wx.Panel.__init__ instead of AuiNotebook's\n    # to keep control over the initialization sequence.\n    wx.Panel.__init__(self, parent, wx.ID_ANY, pos, size, style, name=\"SchNotebook\")\n    self._mgr = SChAuiBaseManager()\n\n    self._tabs = aui.AuiTabContainer(self)\n    self.InitNotebook(AUI_NB_DEFAULT_STYLE)\n\n    self.Bind(aui.EVT_AUINOTEBOOK_PAGE_CLOSE, self.on_closing)\n    self.Bind(aui.EVT_AUINOTEBOOK_TAB_DCLICK, self.on_dclick)\n    self.Bind(aui.EVT_AUINOTEBOOK_PAGE_CHANGED, self.on_changed)\n    self.Bind(aui.EVT_AUINOTEBOOK_PAGE_CHANGING, self.on_changing)\n    self.Bind(wx.EVT_NAVIGATION_KEY, self.on_navigate)\n\n    self.SetWindowStyleFlag(wx.WANTS_CHARS)\n    self.SetBackgroundColour(wx.SystemSettings.GetColour(wx.SYS_COLOUR_BACKGROUND))\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebook-functions","title":"Methods:","text":""},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebook.DeletePage","title":"DeletePage","text":"<pre><code>DeletePage(sel)\n</code></pre> <p>Delete the page at index sel.</p> <p>When the last page is removed, hides the parent panel and restores focus to a visible pane.</p> Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def DeletePage(self, sel):\n    \"\"\"Delete the page at index *sel*.\n\n    When the last page is removed, hides the parent panel and\n    restores focus to a visible pane.\n    \"\"\"\n    page = self.GetPage(sel)\n    if page == self.last_active:\n        self.last_active = None\n\n    def _close():\n        self.panel.Hide()\n        top = wx.GetApp().GetTopWindow()\n        if top:\n            top._mgr.Update()\n        for pane_name in (\"desktop\", \"panel\", \"menu\", \"header\", \"footer\"):\n            pane_info = top._mgr.GetPane(pane_name) if top else None\n            if pane_info and pane_info.IsOk() and pane_info.IsShown():\n                pane_info.window.SetFocus()\n\n    if len(self._tabs._pages) == 1:\n        wx.CallAfter(_close)\n\n    return aui.AuiNotebook.DeletePage(self, sel)\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebook.Freeze","title":"Freeze","text":"<pre><code>Freeze()\n</code></pre> <p>Prevent visual updates (no-op for this control).</p> Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def Freeze(self):\n    \"\"\"Prevent visual updates (no-op for this control).\"\"\"\n    pass\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebook.GetPanel","title":"GetPanel","text":"<pre><code>GetPanel()\n</code></pre> <p>Return the owning panel reference.</p> Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def GetPanel(self):\n    \"\"\"Return the owning panel reference.\"\"\"\n    return self.panel\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebook.SetPanel","title":"SetPanel","text":"<pre><code>SetPanel(panel)\n</code></pre> <p>Store a reference to the owning panel.</p> <p>Parameters:</p> Name Type Description Default <code>panel</code> <p>The parent panel object.</p> required Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def SetPanel(self, panel):\n    \"\"\"Store a reference to the owning panel.\n\n    Args:\n        panel: The parent panel object.\n    \"\"\"\n    self.panel = panel\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebook.Thaw","title":"Thaw","text":"<pre><code>Thaw()\n</code></pre> <p>Allow visual updates (no-op for this control).</p> Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def Thaw(self):\n    \"\"\"Allow visual updates (no-op for this control).\"\"\"\n    pass\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebook.activate_page","title":"activate_page","text":"<pre><code>activate_page(page)\n</code></pre> <p>Activate page and deactivate the previously active one.</p> <p>Parameters:</p> Name Type Description Default <code>page</code> <p>The SchNotebookPage to activate, or None.</p> required Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def activate_page(self, page):\n    \"\"\"Activate *page* and deactivate the previously active one.\n\n    Args:\n        page: The SchNotebookPage to activate, or None.\n    \"\"\"\n    if self.last_active:\n        self.last_active.deactivate_page()\n    self.last_active = page\n    if page:\n        page.activate_page()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebook.add_and_split","title":"add_and_split","text":"<pre><code>add_and_split(page, title, direction)\n</code></pre> <p>Add a page and split the notebook window.</p> <p>Parameters:</p> Name Type Description Default <code>page</code> <p>SchNotebookPage to add.</p> required <code>title</code> <p>Caption for the new tab.</p> required <code>direction</code> <p>wx.LEFT, wx.TOP, wx.RIGHT, or wx.BOTTOM.</p> required Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def add_and_split(self, page, title, direction):\n    \"\"\"Add a page and split the notebook window.\n\n    Args:\n        page: SchNotebookPage to add.\n        title: Caption for the new tab.\n        direction: wx.LEFT, wx.TOP, wx.RIGHT, or wx.BOTTOM.\n    \"\"\"\n    self.closing = False\n    if self.GetPageCount() &lt; 1:\n        return\n\n    cli_size = self.GetClientSize()\n    if self.GetPageCount() &gt; 2:\n        split_size = self.CalculateNewSplitSize()\n    else:\n        split_size = self.GetClientSize()\n        split_size.x = int(split_size.x / 2)\n        split_size.y = int(split_size.y / 2)\n\n    new_tabs = aui.TabFrame(self)\n    new_tabs.SetTabCtrlHeight(self._tab_ctrl_height)\n    self._tab_id_counter += 1\n    new_tabs._tabs = aui.AuiTabCtrl(self, self._tab_id_counter)\n    new_tabs._tabs.SetArtProvider(self._tabs.GetArtProvider().Clone())\n    new_tabs._tabs.SetAGWFlags(self._agwFlags)\n    dest_tabs = new_tabs._tabs\n\n    pane_info = (\n        framemanager.AuiPaneInfo()\n        .CaptionVisible(False)\n        .BestSize(split_size.GetWidth(), split_size.GetHeight())\n    )\n\n    if direction == wx.LEFT:\n        pane_info.Left()\n        mouse_pt = wx.Point(0, int(cli_size.y / 2))\n    elif direction == wx.RIGHT:\n        pane_info.Right()\n        mouse_pt = wx.Point(cli_size.x, int(cli_size.y / 2))\n    elif direction == wx.TOP:\n        pane_info.Top()\n        mouse_pt = wx.Point(int(cli_size.x / 2), 0)\n    elif direction == wx.BOTTOM:\n        pane_info.Bottom()\n        mouse_pt = wx.Point(int(cli_size.x / 2), cli_size.y)\n    else:\n        pane_info.Right()\n        mouse_pt = wx.Point(cli_size.x, int(cli_size.y / 2))\n\n    page_info = aui.AuiNotebookPage()\n    page_info.window = page\n    page_info.caption = title\n    page_info.active = False\n    page_info.control = None\n\n    idn = self.GetPageCount()\n    self._tabs.InsertPage(page, page_info, idn)\n    dest_tabs.AddPage(page, page_info)\n    self.SetPageTextColour(idn, wx.Colour(0, 96, 0))\n    page.Reparent(self)\n    self.DoSizing()\n    dest_tabs.Refresh()\n    self.UpdateHintWindowSize()\n\n    self._mgr.AddPane(new_tabs, pane_info, mouse_pt)\n    self._mgr.Update()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebook.on_changed","title":"on_changed","text":"<pre><code>on_changed(event)\n</code></pre> <p>Handle page selection change.</p> <p>Refreshes the previously selected and newly selected pages, then activates the new page.</p> Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def on_changed(self, event):\n    \"\"\"Handle page selection change.\n\n    Refreshes the previously selected and newly selected pages,\n    then activates the new page.\n    \"\"\"\n    sel = event.GetSelection()\n    old_sel = event.GetOldSelection()\n    if old_sel &gt;= 0:\n        self.GetPage(old_sel).Refresh()\n    if sel &gt;= 0:\n        self.GetPage(sel).Refresh()\n        self.activate_page(self.GetPage(sel))\n    event.Skip()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebook.on_changing","title":"on_changing","text":"<pre><code>on_changing(event)\n</code></pre> <p>Handle page selection about to change.</p> <p>When closing a tab, ensures the selection moves to a sensible neighbouring page.</p> Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def on_changing(self, event):\n    \"\"\"Handle page selection about to change.\n\n    When closing a tab, ensures the selection moves to a sensible\n    neighbouring page.\n    \"\"\"\n    if self.closing:\n        self.closing = False\n        sel = event.GetSelection()\n        oldsel = event.GetOldSelection()\n        if oldsel == -1:\n            new_page = self.GetPage(sel)\n            tab = self.FindTab(new_page)[0]\n            if tab:\n                count = tab.GetPageCount()\n                if count &gt; 0:\n                    new_sel = count - 1\n                    if new_sel &gt; sel:\n                        self.SetSelection(new_sel)\n                        event.Veto()\n                        return\n    event.Skip()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebook.on_closing","title":"on_closing","text":"<pre><code>on_closing(event)\n</code></pre> <p>Handle page close request.</p> <p>Vetoes the close if the page's <code>close_no_del</code> method indicates there are still child pages to close first.</p> Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def on_closing(self, event):\n    \"\"\"Handle page close request.\n\n    Vetoes the close if the page's ``close_no_del`` method\n    indicates there are still child pages to close first.\n    \"\"\"\n    idn = event.GetSelection()\n    if idn &gt;= 0:\n        page = self.GetPage(idn)\n        self.closing = True\n        if page.close_no_del(True):\n            self.GetParent().GetParent().after_close(self)\n            event.Veto()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebook.on_dclick","title":"on_dclick","text":"<pre><code>on_dclick(event)\n</code></pre> <p>Handle tab double-click.</p> <p>Opens the associated wiki help page in a new split pane.</p> Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def on_dclick(self, event):\n    \"\"\"Handle tab double-click.\n\n    Opens the associated wiki help page in a new split pane.\n    \"\"\"\n    try:\n        txt = self.GetPageText(event.GetSelection())\n        mp, adr = wx.GetApp().read_html(\n            self, \"/schwiki/help/\" + wiki_from_str(txt) + \"/view/\", None\n        )\n        if not txt.startswith(\"?\"):\n            wiki = wiki_from_str(txt)\n            wx.GetApp().GetTopWindow().new_main_page(\n                mp, \"?: \" + wiki, panel=\"desktop2\"\n            )\n    except Exception:\n        logger.debug(\"Failed to open wiki page for: %s\", txt, exc_info=True)\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebook.on_navigate","title":"on_navigate","text":"<pre><code>on_navigate(evt)\n</code></pre> <p>Handle navigation key events.</p> <p>Activates the current page and passes the event on.</p> Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def on_navigate(self, evt):\n    \"\"\"Handle navigation key events.\n\n    Activates the current page and passes the event on.\n    \"\"\"\n    forward = evt.GetDirection()\n    self.activate_page(self.GetCurrentPage())\n    evt.Skip()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebook.set_active","title":"set_active","text":"<pre><code>set_active(active)\n</code></pre> <p>Set the active state and refresh the current page.</p> <p>Parameters:</p> Name Type Description Default <code>active</code> <p>True to activate, False to deactivate.</p> required Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def set_active(self, active):\n    \"\"\"Set the active state and refresh the current page.\n\n    Args:\n        active: True to activate, False to deactivate.\n    \"\"\"\n    self.active = active\n    if active:\n        w = self.GetCurrentPage()\n        self.activate_page(w)\n        self.GetParent().GetParent().SetActive(self, w)\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebookPage","title":"SchNotebookPage","text":"<pre><code>SchNotebookPage(parent)\n</code></pre> <p>               Bases: <code>Window</code></p> <p>One tab in a SchNotebook, hosting one or more SchPage windows.</p> <p>Initialize the notebook page.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent SchNotebook window.</p> required Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def __init__(self, parent):\n    \"\"\"Initialize the notebook page.\n\n    Args:\n        parent: Parent SchNotebook window.\n    \"\"\"\n    wx.Window.__init__(\n        self,\n        parent,\n        style=wx.TAB_TRAVERSAL | wx.WANTS_CHARS,\n        name=\"SchNotebookPage\",\n    )\n    try:\n        self.SetBackgroundStyle(wx.BG_STYLE_ERASE)\n    except Exception:\n        self.SetBackgroundStyle(wx.BG_STYLE_CUSTOM)\n\n    self._start_pos = None\n    self._start_pos_x_y_dx_dy = None\n    self._last_x_y_dx_dy = None\n    self._best_x_y_dx_dy = None\n    self._layout_style = 0\n\n    self.child_panels = []\n    self.bestx = -1\n    self.orient = None\n    self.http = None\n    self.reverse_style = True\n\n    self.Bind(wx.EVT_SIZE, self.on_size)\n    self.Bind(wx.EVT_ERASE_BACKGROUND, self.on_erase_background)\n    self.Bind(wx.EVT_LEFT_DOWN, self.on_left_down)\n    self.Bind(wx.EVT_LEFT_UP, self.on_left_up)\n    self.Bind(wx.EVT_MOTION, self.on_move)\n    self.Bind(wx.EVT_SET_FOCUS, self.on_set_focus)\n\n    self.SetWindowStyleFlag(wx.WANTS_CHARS)\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebookPage-functions","title":"Methods:","text":""},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebookPage.activate_page","title":"activate_page","text":"<pre><code>activate_page()\n</code></pre> <p>Activate the last child page.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def activate_page(self):\n    \"\"\"Activate the last child page.\"\"\"\n    if self.get_page_count() &gt; 0:\n        self.get_page(-1).activate_page()\n    else:\n        self.SetFocus()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebookPage.add_page","title":"add_page","text":"<pre><code>add_page(page)\n</code></pre> <p>Append a child SchPage to this notebook page.</p> <p>If the new page has <code>disable_parent</code> set and there are existing pages, the last existing page's forms are disabled.</p> <p>Parameters:</p> Name Type Description Default <code>page</code> <p>The SchPage to add.</p> required Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def add_page(self, page):\n    \"\"\"Append a child SchPage to this notebook page.\n\n    If the new page has ``disable_parent`` set and there are\n    existing pages, the last existing page's forms are disabled.\n\n    Args:\n        page: The SchPage to add.\n    \"\"\"\n    if self.get_page_count() &gt; 0:\n        if page.disable_parent:\n            self.get_page(-1).enable_forms(False)\n    self.child_panels.append(page)\n    self._layout()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebookPage.close_child_page","title":"close_child_page","text":"<pre><code>close_child_page(close_without_refresh=True)\n</code></pre> <p>Close the last child page; remove this tab if it was the only page.</p> <p>Parameters:</p> Name Type Description Default <code>close_without_refresh</code> <p>Forwarded to :meth:<code>close_no_del</code>.</p> <code>True</code> <p>Returns:</p> Type Description <p>Result of :meth:<code>close_no_del</code>.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def close_child_page(self, close_without_refresh=True):\n    \"\"\"Close the last child page; remove this tab if it was the only page.\n\n    Args:\n        close_without_refresh: Forwarded to :meth:`close_no_del`.\n\n    Returns:\n        Result of :meth:`close_no_del`.\n    \"\"\"\n    parent = self.GetParent()\n    pages = parent._tabs._pages\n    for i, page_info in enumerate(pages):\n        if page_info.window == self:\n            ret = self.close_no_del(close_without_refresh)\n            if not ret:\n                parent.DeletePage(i)\n            return ret\n    return False\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebookPage.close_no_del","title":"close_no_del","text":"<pre><code>close_no_del(close_without_refresh=True)\n</code></pre> <p>Close the last child page without destroying this container.</p> <p>Parameters:</p> Name Type Description Default <code>close_without_refresh</code> <p>If True, signal 'child_canceled'; otherwise signal 'child_closed_with_ok'.</p> <code>True</code> <p>Returns:</p> Type Description <p>True if the close was handled (more pages remain or vetoed),</p> <p>False if this container itself should be closed.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def close_no_del(self, close_without_refresh=True):\n    \"\"\"Close the last child page without destroying this container.\n\n    Args:\n        close_without_refresh: If True, signal 'child_canceled';\n            otherwise signal 'child_closed_with_ok'.\n\n    Returns:\n        True if the close was handled (more pages remain or vetoed),\n        False if this container itself should be closed.\n    \"\"\"\n    count = self.get_page_count()\n    if count == 0:\n        return False\n\n    win = self.get_page(-1)\n    if not win.CanClose():\n        return True\n\n    del self.child_panels[-1]\n    if count &gt; 1:\n        self.get_page(-1).enable_forms(True)\n        self._layout()\n        self.get_page(-1).set_focus()\n        if close_without_refresh:\n            self.get_page(-1).signal(\"child_canceled\", win)\n        else:\n            self.get_page(-1).signal(\"child_closed_with_ok\", win)\n        win.Destroy()\n        return True\n\n    win.Destroy()\n    return False\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebookPage.deactivate_page","title":"deactivate_page","text":"<pre><code>deactivate_page()\n</code></pre> <p>Deactivate the last child page.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def deactivate_page(self):\n    \"\"\"Deactivate the last child page.\"\"\"\n    if self.get_page_count() &gt; 0:\n        self.get_page(-1).deactivate_page()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebookPage.get_app_http","title":"get_app_http","text":"<pre><code>get_app_http()\n</code></pre> <p>Return the HTTP client associated with this notebook page.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def get_app_http(self):\n    \"\"\"Return the HTTP client associated with this notebook page.\"\"\"\n    return self.http\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebookPage.get_margins","title":"get_margins","text":"<pre><code>get_margins()\n</code></pre> <p>Return the margin between child windows, in pixels.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def get_margins(self):\n    \"\"\"Return the margin between child windows, in pixels.\"\"\"\n    return 2\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebookPage.get_page","title":"get_page","text":"<pre><code>get_page(nr)\n</code></pre> <p>Return a child SchPage by index.</p> <p>Parameters:</p> Name Type Description Default <code>nr</code> <p>Index of the page (supports negative indexing).</p> required <p>Returns:</p> Type Description <p>SchPage instance.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def get_page(self, nr):\n    \"\"\"Return a child SchPage by index.\n\n    Args:\n        nr: Index of the page (supports negative indexing).\n\n    Returns:\n        SchPage instance.\n    \"\"\"\n    return self.child_panels[nr]\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebookPage.get_page_count","title":"get_page_count","text":"<pre><code>get_page_count()\n</code></pre> <p>Return number of child SchPage windows.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def get_page_count(self):\n    \"\"\"Return number of child SchPage windows.\"\"\"\n    return len(self.child_panels)\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebookPage.get_xy","title":"get_xy","text":"<pre><code>get_xy(size=None, page=-1)\n</code></pre> <p>Calculate the divider position for two child pages.</p> <p>Parameters:</p> Name Type Description Default <code>size</code> <p>Size of the container (uses self.GetSize() if None).</p> <code>None</code> <code>page</code> <p>Index of the page to measure (default: last page).</p> <code>-1</code> <p>Returns:</p> Type Description <p>Tuple (x, y, width, height) for the divider position.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def get_xy(self, size=None, page=-1):\n    \"\"\"Calculate the divider position for two child pages.\n\n    Args:\n        size: Size of the container (uses self.GetSize() if None).\n        page: Index of the page to measure (default: last page).\n\n    Returns:\n        Tuple (x, y, width, height) for the divider position.\n    \"\"\"\n    if size is None:\n        size = self.GetSize()\n    if self._best_x_y_dx_dy:\n        return self._best_x_y_dx_dy\n\n    bestx, besty = self.get_page(page).calculate_best_size()\n    self.bestx = bestx\n\n    if bestx &gt; size.GetWidth() - 20:\n        x = size.GetWidth() / 10\n    else:\n        x = (size.GetWidth() - 2) - bestx\n    if besty &gt; size.GetHeight() - 20:\n        y = size.GetHeight() / 10\n    else:\n        y = (size.GetHeight() - 2) - besty\n\n    self._last_x_y_dx_dy = (x, y, size.GetWidth(), size.GetHeight())\n    return self._last_x_y_dx_dy\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebookPage.new_child_page","title":"new_child_page","text":"<pre><code>new_child_page(address_or_parser, title='', parameters=None, callback=None)\n</code></pre> <p>Append a new SchPage as a child of this notebook page.</p> <p>Parameters:</p> Name Type Description Default <code>address_or_parser</code> <p>HTTP address (str) or ShtmlParser instance.</p> required <code>title</code> <p>Caption for the new page.</p> <code>''</code> <code>parameters</code> <p>Dictionary of request parameters.</p> <code>None</code> <code>callback</code> <p>Optional callback invoked after init.</p> <code>None</code> <p>Returns:</p> Type Description <p>The created SchPage.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def new_child_page(\n    self, address_or_parser, title=\"\", parameters=None, callback=None\n):\n    \"\"\"Append a new SchPage as a child of this notebook page.\n\n    Args:\n        address_or_parser: HTTP address (str) or ShtmlParser instance.\n        title: Caption for the new page.\n        parameters: Dictionary of request parameters.\n        callback: Optional callback invoked after init.\n\n    Returns:\n        The created SchPage.\n    \"\"\"\n    h = page.SchPage(self, address_or_parser, parameters)\n    if self.get_page_count() &gt; 0:\n        h.parent_page = self.get_page(-1)\n\n    nr = 0\n    if (header := h.header) is not None:\n        nr += 4\n    if (footer := h.footer) is not None:\n        nr += 2\n    if (panel := h.panel) is not None:\n        nr += 1\n\n    title2 = title if title else h.get_title()\n    self.add_page(h)\n\n    def init_page():\n        nonlocal h, callback\n        h.init_frame(callback)\n        h.activate_page()\n        top = wx.GetApp().GetTopWindow()\n        if top:\n            top._mgr.GetPane(\"desktop\").Show()\n        h.Update()\n\n    wx.CallAfter(init_page)\n    return h\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebookPage.new_main_page","title":"new_main_page","text":"<pre><code>new_main_page(address_or_parser, title='', parameters=None, view_in=None)\n</code></pre> <p>Create a new top-level page in a specific pane.</p> <p>Delegates to the application frame's <code>new_main_page</code>.</p> <p>Parameters:</p> Name Type Description Default <code>address_or_parser</code> <p>HTTP address (str) or ShtmlParser instance.</p> required <code>title</code> <p>Caption for the new page.</p> <code>''</code> <code>parameters</code> <p>Dictionary of request parameters.</p> <code>None</code> <code>view_in</code> <p>Pane name ('desktop', 'panel', 'header', 'footer').</p> <code>None</code> <p>Returns:</p> Type Description <p>Result of the application frame's new_main_page.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def new_main_page(self, address_or_parser, title=\"\", parameters=None, view_in=None):\n    \"\"\"Create a new top-level page in a specific pane.\n\n    Delegates to the application frame's ``new_main_page``.\n\n    Args:\n        address_or_parser: HTTP address (str) or ShtmlParser instance.\n        title: Caption for the new page.\n        parameters: Dictionary of request parameters.\n        view_in: Pane name ('desktop', 'panel', 'header', 'footer').\n\n    Returns:\n        Result of the application frame's new_main_page.\n    \"\"\"\n    top = wx.GetApp().GetTopWindow()\n    if view_in is None:\n        pp = top._mgr.GetPane(self.GetParent())\n        return top.new_main_page(address_or_parser, title, parameters, pp.name)\n    return top.new_main_page(address_or_parser, title, parameters, view_in)\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebookPage.on_child_form_cancel","title":"on_child_form_cancel","text":"<pre><code>on_child_form_cancel()\n</code></pre> <p>Called by child SchForm when Cancel is pushed.</p> <p>Closes the child without refreshing the parent.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def on_child_form_cancel(self):\n    \"\"\"Called by child SchForm when Cancel is pushed.\n\n    Closes the child without refreshing the parent.\n    \"\"\"\n    self.close_child_page(True)\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebookPage.on_child_form_ok","title":"on_child_form_ok","text":"<pre><code>on_child_form_ok()\n</code></pre> <p>Called by child SchForm when OK is pushed.</p> <p>Closes the child and refreshes the parent form.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def on_child_form_ok(self):\n    \"\"\"Called by child SchForm when OK is pushed.\n\n    Closes the child and refreshes the parent form.\n    \"\"\"\n    self.close_child_page(False)\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebookPage.on_erase_background","title":"on_erase_background","text":"<pre><code>on_erase_background(evt)\n</code></pre> <p>Draw borders between child pages and resize handles.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def on_erase_background(self, evt):\n    \"\"\"Draw borders between child pages and resize handles.\"\"\"\n    top = wx.GetApp().GetTopWindow()\n    if not top:\n        return\n\n    dc = wx.ClientDC(self)\n    dc.Clear()\n    margin = self.get_margins()\n\n    has_desktop = hasattr(top, \"desktop\")\n    tabs_count = len(top.desktop._mgr.GetAllPanes()) if has_desktop else 0\n    page_count = self.get_page_count()\n\n    show_border = (\n        (page_count &gt; 0 and tabs_count &gt; 2)\n        or page_count &gt; 1\n        or (top.count_shown_panels(count_toolbars=False) &gt; 1 and page_count &gt; 0)\n    )\n\n    if not show_border:\n        # Nothing to draw.\n        return\n\n    last_page = self.get_page(-1)\n    (dx, dy) = last_page.GetSize()\n    (x, y) = last_page.GetPosition()\n    x -= margin / 2\n    y -= margin / 2\n    dx += margin\n    dy += margin\n\n    parent = self.GetParent()\n    if parent.active and parent.GetCurrentPage() == self:\n        col = wx.SystemSettings.GetColour(wx.SYS_COLOUR_HIGHLIGHT)\n    else:\n        col = wx.SystemSettings.GetColour(wx.SYS_COLOUR_INACTIVEBORDER)\n    dc.SetPen(wx.Pen(col, margin))\n\n    # Border rectangle\n    dc.DrawLine(int(x), int(y), int(x + dx), int(y))\n    dc.DrawLine(int(x + dx), int(y), int(x + dx), int(y + dy))\n    dc.DrawLine(int(x), int(y - margin / 2), int(x), int(y + dy))\n    dc.DrawLine(int(x), int(y + dy), int(x + dx), int(y + dy))\n\n    # Resize handle lines\n    if self._layout_style &gt; 0:\n        col = wx.SystemSettings.GetColour(wx.SYS_COLOUR_3DSHADOW)\n        dc.SetPen(wx.Pen(col, 1))\n        self._draw_resize_handles(dc, x, y, dx, dy, margin)\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebookPage.on_left_down","title":"on_left_down","text":"<pre><code>on_left_down(event)\n</code></pre> <p>Begin splitter drag.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def on_left_down(self, event):\n    \"\"\"Begin splitter drag.\"\"\"\n    self._start_pos = event.GetPosition()\n    self._start_pos_x_y_dx_dy = self._last_x_y_dx_dy\n    self.SetCursor(wx.Cursor(wx.CURSOR_SIZING))\n    self.CaptureMouse()\n    event.Skip()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebookPage.on_left_up","title":"on_left_up","text":"<pre><code>on_left_up(event)\n</code></pre> <p>End splitter drag.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def on_left_up(self, event):\n    \"\"\"End splitter drag.\"\"\"\n    if self._start_pos:\n        self._start_pos = None\n        self._start_pos_x_y_dx_dy = None\n        self._best_x_y_dx_dy = None\n        self.ReleaseMouse()\n        self.SetCursor(wx.Cursor(wx.CURSOR_ARROW))\n    event.Skip()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebookPage.on_move","title":"on_move","text":"<pre><code>on_move(event)\n</code></pre> <p>Handle mouse movement during splitter drag.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def on_move(self, event):\n    \"\"\"Handle mouse movement during splitter drag.\"\"\"\n    if self._start_pos and self._start_pos_x_y_dx_dy:\n        pos = event.GetPosition()\n        dx = pos[0] - self._start_pos[0]\n        dy = pos[1] - self._start_pos[1]\n        if self.reverse_style:\n            dy = -dy\n        self._best_x_y_dx_dy = (\n            self._start_pos_x_y_dx_dy[0] + dx,\n            self._start_pos_x_y_dx_dy[1] + dy,\n            self._start_pos_x_y_dx_dy[2],\n            self._start_pos_x_y_dx_dy[3],\n        )\n        self._layout()\n    event.Skip()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebookPage.on_set_focus","title":"on_set_focus","text":"<pre><code>on_set_focus(evt)\n</code></pre> <p>Delegate focus to the last child page.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def on_set_focus(self, evt):\n    \"\"\"Delegate focus to the last child page.\"\"\"\n    if self.get_page_count() &gt; 0:\n        self.get_page(-1).SetFocus()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.SchNotebookPage.on_size","title":"on_size","text":"<pre><code>on_size(event)\n</code></pre> <p>Re-layout children when the container is resized.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def on_size(self, event):\n    \"\"\"Re-layout children when the container is resized.\"\"\"\n    size = event.GetSize()\n    self.bestx = -1\n    if size:\n        wx.CallAfter(self._layout, size)\n    event.Skip()\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe._SChMainPanel","title":"_SChMainPanel","text":"<pre><code>_SChMainPanel(app_frame, *argi, **argv)\n</code></pre> <p>               Bases: <code>Window</code></p> Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def __init__(self, app_frame, *argi, **argv):\n    argv[\"name\"] = \"schmainpanel\"\n    if \"style\" in argv:\n        argv[\"style\"] |= wx.WANTS_CHARS\n    self.app_frame = app_frame\n    wx.Window.__init__(self, *argi, **argv)\n    self.SetBackgroundColour(wx.SystemSettings.GetColour(wx.SYS_COLOUR_BACKGROUND))\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe-functions","title":"Functions:","text":""},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.bitmap_from_href","title":"bitmap_from_href","text":"<pre><code>bitmap_from_href(href, size_type=SIZE_DEFAULT)\n</code></pre> <p>Load a resource and convert it to a wx.Bitmap.</p> <p>Parameters:</p> Name Type Description Default <code>href</code> <p>Resource identifier. Supported formats: - ArtProvider id, e.g. 'wx.ART_COPY' - Font Awesome id, e.g. 'fa://icon_name' or 'fa://bus' - Local library image, e.g. 'png://image_name.png' - Client-side image, e.g. 'client://image_name.png' - Inline SVG data, e.g. 'data:image/svg+xml,...' - Remote HTTP/HTTPS URL, e.g. 'http://www.pytigon.cloud/test.png'</p> required <code>size_type</code> <p>One of SIZE_DEFAULT, SIZE_SMALL, SIZE_MEDIUM, SIZE_LARGE.</p> <code>SIZE_DEFAULT</code> <p>Returns:</p> Type Description <p>wx.Bitmap. If the resource cannot be loaded, ART_MISSING_IMAGE is</p> <p>returned.</p> Source code in <code>pytigon_gui/guilib/image.py</code> <pre><code>def bitmap_from_href(href, size_type=SIZE_DEFAULT):\n    \"\"\"Load a resource and convert it to a wx.Bitmap.\n\n    Args:\n        href: Resource identifier. Supported formats:\n            - ArtProvider id, e.g. 'wx.ART_COPY'\n            - Font Awesome id, e.g. 'fa://icon_name' or 'fa://bus'\n            - Local library image, e.g. 'png://image_name.png'\n            - Client-side image, e.g. 'client://image_name.png'\n            - Inline SVG data, e.g. 'data:image/svg+xml,...'\n            - Remote HTTP/HTTPS URL, e.g. 'http://www.pytigon.cloud/test.png'\n        size_type: One of SIZE_DEFAULT, SIZE_SMALL, SIZE_MEDIUM, SIZE_LARGE.\n\n    Returns:\n        wx.Bitmap. If the resource cannot be loaded, ART_MISSING_IMAGE is\n        returned.\n    \"\"\"\n    size_type2 = size_type\n    if \"?\" in href:\n        x = href.split(\"?\")\n        href2 = x[0]\n        if \"=\" in x[1]:\n            try:\n                size_type2 = int(x[1].split(\"=\")[1])\n            except (ValueError, IndexError):\n                size_type2 = 1\n    else:\n        href2 = href\n\n    if size_type2 == SIZE_SMALL:\n        icon_size = 16\n    elif size_type2 == SIZE_MEDIUM:\n        icon_size = 22\n    else:\n        icon_size = 32\n\n    if href2[:3] == \"wx.\":\n        if (art_id := _resolve_art_id(href2)) is not None:\n            bmp = wx.ArtProvider.GetBitmap(\n                art_id, wx.ART_TOOLBAR, (icon_size, icon_size)\n            )\n        else:\n            bmp = wx.ArtProvider.GetBitmap(\n                wx.ART_MISSING_IMAGE, wx.ART_TOOLBAR, (icon_size, icon_size)\n            )\n    elif href.startswith(\"client://\"):\n        image = wx.Image(\n            str(wx.GetApp().src_path)\n            + f\"/static/icons/{icon_size}x{icon_size}/\"\n            + href2[9:]\n        )\n        bmp = wx.Bitmap(image)\n    elif href.startswith(\"png://\"):\n        image = wx.Image(\n            str(wx.GetApp().src_path)\n            + f\"/static/icons/{icon_size}x{icon_size}/\"\n            + href2[6:]\n        )\n        bmp = wx.Bitmap(image)\n    elif href.startswith(\"fa://\"):\n        suffix = \"\" if \".png\" in href.lower() else \".png\"\n        try:\n            image = wx.Image(\n                str(wx.GetApp().src_path)\n                + f\"/static/fonts/fork-awesome/fonts/{icon_size}x{icon_size}/\"\n                + href2[5:].replace(\"fa-\", \"\")\n                + suffix\n            )\n            image = image.AdjustChannels(1, 1, 1, 0.55)\n            bmp = wx.Bitmap(image)\n        except Exception:\n            logger.warning(\"Cannot load image: %s\", href2)\n            bmp = wx.Bitmap()\n    elif href.startswith(\"data:image/svg+xml\"):\n        x = href.split(\",\", 1)\n        svg_code = x[1]\n        svg = SVGimage.CreateFromBytes(svg_code.encode(\"utf-8\"))\n        bmp = svg.ConvertToBitmap(width=icon_size, height=icon_size)\n    else:\n        if href.lower().startswith(\"http\"):\n            http = wx.GetApp().get_http(None)\n            response = http.get(None, str(href))\n            if response.ret_code != 404:\n                s = response.ptr()\n                stream = BytesIO(s)\n                bmp = wx.Bitmap(wx.Image(stream))\n            else:\n                bmp = wx.ArtProvider.GetBitmap(\n                    wx.ART_MISSING_IMAGE, wx.ART_TOOLBAR, (32, 32)\n                )\n        else:\n            bmp = wx.ArtProvider.GetBitmap(\n                wx.ART_MISSING_IMAGE, wx.ART_TOOLBAR, (32, 32)\n            )\n    return bmp\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.finish_video","title":"finish_video","text":"<pre><code>finish_video()\n</code></pre> Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def finish_video():\n    if _RECORD_VIDEO_OUT:\n        _RECORD_VIDEO_OUT.release()\n        _RECORD_VIDEO_STRUCT = None\n</code></pre>"},{"location":"api/guiframe_appframe/#pytigon_gui.guiframe.appframe.save_video_frame","title":"save_video_frame","text":"<pre><code>save_video_frame(win)\n</code></pre> Source code in <code>pytigon_gui/guiframe/appframe.py</code> <pre><code>def save_video_frame(win):\n    global _RECORD_VIDEO_STRUCT, _RECORD_VIDEO_OUT, _RECORD_VIDEO_MONITOR\n    if _RECORD_VIDEO_STRUCT:\n        mss = _RECORD_VIDEO_STRUCT[0]\n        cv2 = _RECORD_VIDEO_STRUCT[1]\n        numpy = _RECORD_VIDEO_STRUCT[2]\n        if not _RECORD_VIDEO_OUT:\n            pos = win.GetScreenPosition()\n            size = win.GetSize()\n\n            # fourcc = cv2.VideoWriter_fourcc(*'MJPG')\n            fourcc = cv2.VideoWriter_fourcc(*\"XVID\")\n            _RECORD_VIDEO_OUT = cv2.VideoWriter(\n                win._video, fourcc, 4, (size.width, size.height)\n            )\n            _RECORD_VIDEO_MONITOR = {\n                \"top\": pos.y,\n                \"left\": pos.x,\n                \"width\": size.width,\n                \"height\": size.height,\n            }\n\n        with mss() as sct:\n            pos = win.GetScreenPosition()\n            size = win.GetSize()\n            frame = numpy.array(sct.grab(_RECORD_VIDEO_MONITOR))\n            frame = numpy.flip(frame[:, :, :3], 2)\n            frame = cv2.cvtColor(frame, cv2.COLOR_BGR2RGB)\n            _RECORD_VIDEO_OUT.write(frame)\n</code></pre>"},{"location":"api/guiframe_baseframe/","title":"guiframe.baseframe \u2013 Base Frame","text":"<p>Abstract base frame containing shared frame logic: menu bar setup, status bar, and event handling.</p>"},{"location":"api/guiframe_baseframe/#pytigon_gui.guiframe.baseframe","title":"baseframe","text":"<p>Base frame class for Pytigon application windows.</p> <p>Provides the core SchBaseFrame from which both SchAppFrame (desktop) and SchBrowserFrame (webview) inherit.</p>"},{"location":"api/guiframe_baseframe/#pytigon_gui.guiframe.baseframe-attributes","title":"Attributes","text":""},{"location":"api/guiframe_baseframe/#pytigon_gui.guiframe.baseframe.logger","title":"logger  <code>module-attribute</code>","text":"<pre><code>logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/guiframe_baseframe/#pytigon_gui.guiframe.baseframe-classes","title":"Classes","text":""},{"location":"api/guiframe_baseframe/#pytigon_gui.guiframe.baseframe.SchBaseFrame","title":"SchBaseFrame","text":"<pre><code>SchBaseFrame(\n    parent,\n    gui_style=\"tree(toolbar,statusbar)\",\n    id=-1,\n    title=\"\",\n    pos=DefaultPosition,\n    size=DefaultSize,\n    style=DEFAULT_FRAME_STYLE | CLIP_CHILDREN | WANTS_CHARS,\n    name=\"MainWindow\",\n)\n</code></pre> <p>               Bases: <code>Frame</code></p> <p>Base main window for Pytigon applications.</p> <p>Handles plugin initialization and close event management.</p> <p>Initialize the base frame.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window (usually None for top-level).</p> required <code>gui_style</code> <p>Style string describing UI layout (e.g. \"tree(toolbar,statusbar)\").</p> <code>'tree(toolbar,statusbar)'</code> <code>id</code> <p>Window identifier.</p> <code>-1</code> <code>title</code> <p>Window title.</p> <code>''</code> <code>pos</code> <p>Window position.</p> <code>DefaultPosition</code> <code>size</code> <p>Window size.</p> <code>DefaultSize</code> <code>style</code> <p>wx.Frame style flags.</p> <code>DEFAULT_FRAME_STYLE | CLIP_CHILDREN | WANTS_CHARS</code> <code>name</code> <p>Widget name.</p> <code>'MainWindow'</code> Source code in <code>pytigon_gui/guiframe/baseframe.py</code> <pre><code>def __init__(\n    self,\n    parent,\n    gui_style=\"tree(toolbar,statusbar)\",\n    id=-1,\n    title=\"\",\n    pos=wx.DefaultPosition,\n    size=wx.DefaultSize,\n    style=wx.DEFAULT_FRAME_STYLE | wx.CLIP_CHILDREN | wx.WANTS_CHARS,\n    name=\"MainWindow\",\n):\n    \"\"\"Initialize the base frame.\n\n    Args:\n        parent: Parent window (usually None for top-level).\n        gui_style: Style string describing UI layout (e.g. \"tree(toolbar,statusbar)\").\n        id: Window identifier.\n        title: Window title.\n        pos: Window position.\n        size: Window size.\n        style: wx.Frame style flags.\n        name: Widget name.\n    \"\"\"\n    wx.Frame.__init__(self, None, wx.ID_ANY, title, pos, size, style, \"MainWindow\")\n    self.run_on_close = []\n    self.destroy_fun_tab = []\n    self.Bind(wx.EVT_CLOSE, self.on_close)\n</code></pre>"},{"location":"api/guiframe_baseframe/#pytigon_gui.guiframe.baseframe.SchBaseFrame-functions","title":"Methods:","text":""},{"location":"api/guiframe_baseframe/#pytigon_gui.guiframe.baseframe.SchBaseFrame.init_plugins","title":"init_plugins","text":"<pre><code>init_plugins()\n</code></pre> <p>Discover and initialize application plugins.</p> <p>Searches for plugins in: 1. Built-in pytigon appdata/plugins directory 2. User data plugins directory 3. Application-specific plugins directory</p> <p>Plugins in the 'auto' directory or listed in auto_plugins config are loaded automatically. Each plugin module may expose an <code>init_plugin</code> function that receives the application, frame, desktop notebook, frame manager, menu bar, toolbar interface, and accelerator table.</p> Source code in <code>pytigon_gui/guiframe/baseframe.py</code> <pre><code>def init_plugins(self):\n    \"\"\"Discover and initialize application plugins.\n\n    Searches for plugins in:\n    1. Built-in pytigon appdata/plugins directory\n    2. User data plugins directory\n    3. Application-specific plugins directory\n\n    Plugins in the 'auto' directory or listed in auto_plugins config\n    are loaded automatically. Each plugin module may expose an\n    ``init_plugin`` function that receives the application, frame,\n    desktop notebook, frame manager, menu bar, toolbar interface,\n    and accelerator table.\n    \"\"\"\n    home_dir = wx.GetApp().get_working_dir()\n    app_plugins = Path(wx.GetApp().cwd) / \"plugins\"\n    dirnames = [\n        Path(wx.GetApp().pytigon_path) / \"appdata\" / \"plugins\",\n        Path(wx.GetApp().data_path) / \"plugins\",\n        app_plugins,\n    ]\n\n    auto_plugins = []\n    try:\n        auto_plugins = (\n            wx.GetApp().config[\"Global settings\"].get(\"auto_plugins\", \"\").split(\";\")\n        )\n    except (KeyError, AttributeError):\n        pass\n\n    for dirname in dirnames:\n        if not Path(dirname).exists():\n            continue\n\n        for ff in sorted(os.listdir(dirname)):\n            dirname2 = Path(dirname) / ff\n            if not Path(dirname2).is_dir():\n                continue\n\n            files = sorted(os.listdir(dirname2))\n\n            for f in files:\n                if not (Path(dirname2) / f).is_dir():\n                    continue\n\n                mod_name = ff + \".\" + f\n                # Skip internal modules\n                if \".__\" in mod_name:\n                    break\n\n                x = mod_name.replace(\".\", \"/\")\n                if not (\n                    ff == \"auto\"\n                    or (wx.GetApp().plugins and x in wx.GetApp().plugins)\n                    or x in auto_plugins\n                ):\n                    continue\n\n                try:\n                    mod = import_plugin(mod_name)\n                except Exception:\n                    logger.exception(\"Error importing plugin: %s\", mod_name)\n                    continue\n\n                if not hasattr(mod, \"init_plugin\"):\n                    continue\n\n                try:\n                    destroy = mod.init_plugin(\n                        wx.GetApp(),\n                        self,\n                        getattr(self, \"desktop\", None),\n                        getattr(self, \"_mgr\", None),\n                        self.get_menu_bar(),\n                        getattr(self, \"toolbar_interface\", None),\n                        getattr(self, \"aTable\", None),\n                    )\n                except Exception:\n                    logger.exception(\"Error initializing plugin: %s\", mod_name)\n                    continue\n\n                if destroy is not None:\n                    self.destroy_fun_tab.append(destroy)\n</code></pre>"},{"location":"api/guiframe_baseframe/#pytigon_gui.guiframe.baseframe.SchBaseFrame.on_close","title":"on_close","text":"<pre><code>on_close(event)\n</code></pre> <p>Handle the window close event.</p> <p>Calls all registered close handlers in order, then allows the event to propagate.</p> Source code in <code>pytigon_gui/guiframe/baseframe.py</code> <pre><code>def on_close(self, event):\n    \"\"\"Handle the window close event.\n\n    Calls all registered close handlers in order, then allows\n    the event to propagate.\n    \"\"\"\n    for fun in self.run_on_close:\n        try:\n            fun(self)\n        except Exception:\n            traceback.print_exc()\n    event.Skip()\n</code></pre>"},{"location":"api/guiframe_baseframe/#pytigon_gui.guiframe.baseframe-functions","title":"Functions:","text":""},{"location":"api/guiframe_baseframe/#pytigon_gui.guiframe.baseframe.import_plugin","title":"import_plugin","text":"<pre><code>import_plugin(plugin_name, prj_name=None)\n</code></pre> <p>Import a plugin module by name, searching standard plugin paths.</p> <p>Lookup order depends on whether prj_name is given:   - With project: prj_cfg, then prj_cfg_alt.   - Without project: pytigon_cfg, then data_cfg.</p> <p>Parameters:</p> Name Type Description Default <code>plugin_name</code> <p>Dotted module name (e.g. 'plugins.myplugin').</p> required <code>prj_name</code> <p>Optional project name for project-specific plugins.</p> <code>None</code> <p>Returns:</p> Type Description <p>The imported module, or None if not found or import failed.</p> Source code in <code>pytigon_gui/guilib/tools.py</code> <pre><code>def import_plugin(plugin_name, prj_name=None):\n    \"\"\"Import a plugin module by name, searching standard plugin paths.\n\n    Lookup order depends on whether *prj_name* is given:\n      - With project: prj_cfg, then prj_cfg_alt.\n      - Without project: pytigon_cfg, then data_cfg.\n\n    Args:\n        plugin_name: Dotted module name (e.g. 'plugins.myplugin').\n        prj_name: Optional project name for project-specific plugins.\n\n    Returns:\n        The imported module, or None if not found or import failed.\n    \"\"\"\n    cfg = get_main_paths()\n    pytigon_cfg = [cfg[\"PYTIGON_PATH\"], \"appdata\", \"plugins\"]\n    data_cfg = [cfg[\"DATA_PATH\"], \"plugins\"]\n    prj_cfg = [cfg[\"PRJ_PATH\"], prj_name, \"applib\"]\n    prj_cfg_alt = [cfg[\"PRJ_PATH_ALT\"], prj_name, \"applib\"]\n\n    if prj_name:\n        folders = [prj_cfg, prj_cfg_alt]\n    else:\n        folders = [pytigon_cfg, data_cfg]\n\n    path = None\n    for folder in folders:\n        plugins_path = Path(folder[0]).joinpath(*folder[1:])\n        if prj_name:\n            plugin_path = Path(plugins_path).joinpath(*plugin_name.split(\".\")[:-1])\n        else:\n            plugin_path = Path(plugins_path).joinpath(*plugin_name.split(\".\"))\n        if Path(plugin_path).exists():\n            path = plugins_path\n            break\n\n    if not path:\n        logger.warning(\"Plugin path not found: %s\", plugin_name)\n        return None\n\n    try:\n        return importlib.import_module(plugin_name, package=None)\n    except ImportError as e:\n        logger.error(\"Failed to import plugin '%s': %s\", plugin_name, e)\n        return None\n</code></pre>"},{"location":"api/guiframe_browserframe/","title":"guiframe.browserframe \u2013 Browser Frame","text":"<p>Embedded web browser frame using CEF (Chromium Embedded Framework) or wxWebView for rendering web content within the desktop app.</p>"},{"location":"api/guiframe_browserframe/#pytigon_gui.guiframe.browserframe","title":"browserframe","text":"<p>Browser-based main frame for Pytigon.</p> <p>SchBrowserFrame embeds an HTML2 webview control as the main content area instead of the standard AUI-managed desktop. It is used when the application runs in a webview-embedded mode.</p>"},{"location":"api/guiframe_browserframe/#pytigon_gui.guiframe.browserframe-attributes","title":"Attributes","text":""},{"location":"api/guiframe_browserframe/#pytigon_gui.guiframe.browserframe._","title":"_  <code>module-attribute</code>","text":"<pre><code>_ = wx.GetTranslation\n</code></pre>"},{"location":"api/guiframe_browserframe/#pytigon_gui.guiframe.browserframe.logger","title":"logger  <code>module-attribute</code>","text":"<pre><code>logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/guiframe_browserframe/#pytigon_gui.guiframe.browserframe-classes","title":"Classes","text":""},{"location":"api/guiframe_browserframe/#pytigon_gui.guiframe.browserframe.SchBaseFrame","title":"SchBaseFrame","text":"<pre><code>SchBaseFrame(\n    parent,\n    gui_style=\"tree(toolbar,statusbar)\",\n    id=-1,\n    title=\"\",\n    pos=DefaultPosition,\n    size=DefaultSize,\n    style=DEFAULT_FRAME_STYLE | CLIP_CHILDREN | WANTS_CHARS,\n    name=\"MainWindow\",\n)\n</code></pre> <p>               Bases: <code>Frame</code></p> <p>Base main window for Pytigon applications.</p> <p>Handles plugin initialization and close event management.</p> <p>Initialize the base frame.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window (usually None for top-level).</p> required <code>gui_style</code> <p>Style string describing UI layout (e.g. \"tree(toolbar,statusbar)\").</p> <code>'tree(toolbar,statusbar)'</code> <code>id</code> <p>Window identifier.</p> <code>-1</code> <code>title</code> <p>Window title.</p> <code>''</code> <code>pos</code> <p>Window position.</p> <code>DefaultPosition</code> <code>size</code> <p>Window size.</p> <code>DefaultSize</code> <code>style</code> <p>wx.Frame style flags.</p> <code>DEFAULT_FRAME_STYLE | CLIP_CHILDREN | WANTS_CHARS</code> <code>name</code> <p>Widget name.</p> <code>'MainWindow'</code> Source code in <code>pytigon_gui/guiframe/baseframe.py</code> <pre><code>def __init__(\n    self,\n    parent,\n    gui_style=\"tree(toolbar,statusbar)\",\n    id=-1,\n    title=\"\",\n    pos=wx.DefaultPosition,\n    size=wx.DefaultSize,\n    style=wx.DEFAULT_FRAME_STYLE | wx.CLIP_CHILDREN | wx.WANTS_CHARS,\n    name=\"MainWindow\",\n):\n    \"\"\"Initialize the base frame.\n\n    Args:\n        parent: Parent window (usually None for top-level).\n        gui_style: Style string describing UI layout (e.g. \"tree(toolbar,statusbar)\").\n        id: Window identifier.\n        title: Window title.\n        pos: Window position.\n        size: Window size.\n        style: wx.Frame style flags.\n        name: Widget name.\n    \"\"\"\n    wx.Frame.__init__(self, None, wx.ID_ANY, title, pos, size, style, \"MainWindow\")\n    self.run_on_close = []\n    self.destroy_fun_tab = []\n    self.Bind(wx.EVT_CLOSE, self.on_close)\n</code></pre>"},{"location":"api/guiframe_browserframe/#pytigon_gui.guiframe.browserframe.SchBaseFrame-functions","title":"Methods:","text":""},{"location":"api/guiframe_browserframe/#pytigon_gui.guiframe.browserframe.SchBaseFrame.init_plugins","title":"init_plugins","text":"<pre><code>init_plugins()\n</code></pre> <p>Discover and initialize application plugins.</p> <p>Searches for plugins in: 1. Built-in pytigon appdata/plugins directory 2. User data plugins directory 3. Application-specific plugins directory</p> <p>Plugins in the 'auto' directory or listed in auto_plugins config are loaded automatically. Each plugin module may expose an <code>init_plugin</code> function that receives the application, frame, desktop notebook, frame manager, menu bar, toolbar interface, and accelerator table.</p> Source code in <code>pytigon_gui/guiframe/baseframe.py</code> <pre><code>def init_plugins(self):\n    \"\"\"Discover and initialize application plugins.\n\n    Searches for plugins in:\n    1. Built-in pytigon appdata/plugins directory\n    2. User data plugins directory\n    3. Application-specific plugins directory\n\n    Plugins in the 'auto' directory or listed in auto_plugins config\n    are loaded automatically. Each plugin module may expose an\n    ``init_plugin`` function that receives the application, frame,\n    desktop notebook, frame manager, menu bar, toolbar interface,\n    and accelerator table.\n    \"\"\"\n    home_dir = wx.GetApp().get_working_dir()\n    app_plugins = Path(wx.GetApp().cwd) / \"plugins\"\n    dirnames = [\n        Path(wx.GetApp().pytigon_path) / \"appdata\" / \"plugins\",\n        Path(wx.GetApp().data_path) / \"plugins\",\n        app_plugins,\n    ]\n\n    auto_plugins = []\n    try:\n        auto_plugins = (\n            wx.GetApp().config[\"Global settings\"].get(\"auto_plugins\", \"\").split(\";\")\n        )\n    except (KeyError, AttributeError):\n        pass\n\n    for dirname in dirnames:\n        if not Path(dirname).exists():\n            continue\n\n        for ff in sorted(os.listdir(dirname)):\n            dirname2 = Path(dirname) / ff\n            if not Path(dirname2).is_dir():\n                continue\n\n            files = sorted(os.listdir(dirname2))\n\n            for f in files:\n                if not (Path(dirname2) / f).is_dir():\n                    continue\n\n                mod_name = ff + \".\" + f\n                # Skip internal modules\n                if \".__\" in mod_name:\n                    break\n\n                x = mod_name.replace(\".\", \"/\")\n                if not (\n                    ff == \"auto\"\n                    or (wx.GetApp().plugins and x in wx.GetApp().plugins)\n                    or x in auto_plugins\n                ):\n                    continue\n\n                try:\n                    mod = import_plugin(mod_name)\n                except Exception:\n                    logger.exception(\"Error importing plugin: %s\", mod_name)\n                    continue\n\n                if not hasattr(mod, \"init_plugin\"):\n                    continue\n\n                try:\n                    destroy = mod.init_plugin(\n                        wx.GetApp(),\n                        self,\n                        getattr(self, \"desktop\", None),\n                        getattr(self, \"_mgr\", None),\n                        self.get_menu_bar(),\n                        getattr(self, \"toolbar_interface\", None),\n                        getattr(self, \"aTable\", None),\n                    )\n                except Exception:\n                    logger.exception(\"Error initializing plugin: %s\", mod_name)\n                    continue\n\n                if destroy is not None:\n                    self.destroy_fun_tab.append(destroy)\n</code></pre>"},{"location":"api/guiframe_browserframe/#pytigon_gui.guiframe.browserframe.SchBaseFrame.on_close","title":"on_close","text":"<pre><code>on_close(event)\n</code></pre> <p>Handle the window close event.</p> <p>Calls all registered close handlers in order, then allows the event to propagate.</p> Source code in <code>pytigon_gui/guiframe/baseframe.py</code> <pre><code>def on_close(self, event):\n    \"\"\"Handle the window close event.\n\n    Calls all registered close handlers in order, then allows\n    the event to propagate.\n    \"\"\"\n    for fun in self.run_on_close:\n        try:\n            fun(self)\n        except Exception:\n            traceback.print_exc()\n    event.Skip()\n</code></pre>"},{"location":"api/guiframe_browserframe/#pytigon_gui.guiframe.browserframe.SchBrowserFrame","title":"SchBrowserFrame","text":"<pre><code>SchBrowserFrame(\n    parent,\n    gui_style=\"tree(toolbar,statusbar)\",\n    id=-1,\n    title=\"\",\n    pos=DefaultPosition,\n    size=DefaultSize,\n    style=DEFAULT_FRAME_STYLE | CLIP_CHILDREN | WANTS_CHARS,\n    name=\"MainWindow\",\n)\n</code></pre> <p>               Bases: <code>SchBaseFrame</code></p> <p>Main window for browser-embedded Pytigon applications.</p> <p>Uses an HTML2 webview control as the primary content area, loading the initial page directly from the embedded HTTP client.</p> <p>Construct the browser frame.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window (usually None).</p> required <code>gui_style</code> <p>UI layout style string.</p> <code>'tree(toolbar,statusbar)'</code> <code>id</code> <p>Window identifier.</p> <code>-1</code> <code>title</code> <p>Window title.</p> <code>''</code> <code>pos</code> <p>Initial position.</p> <code>DefaultPosition</code> <code>size</code> <p>Initial size.</p> <code>DefaultSize</code> <code>style</code> <p>wx.Frame style flags.</p> <code>DEFAULT_FRAME_STYLE | CLIP_CHILDREN | WANTS_CHARS</code> <code>name</code> <p>Widget name.</p> <code>'MainWindow'</code> Source code in <code>pytigon_gui/guiframe/browserframe.py</code> <pre><code>def __init__(\n    self,\n    parent,\n    gui_style=\"tree(toolbar,statusbar)\",\n    id=-1,\n    title=\"\",\n    pos=wx.DefaultPosition,\n    size=wx.DefaultSize,\n    style=wx.DEFAULT_FRAME_STYLE | wx.CLIP_CHILDREN | wx.WANTS_CHARS,\n    name=\"MainWindow\",\n):\n    \"\"\"Construct the browser frame.\n\n    Args:\n        parent: Parent window (usually None).\n        gui_style: UI layout style string.\n        id: Window identifier.\n        title: Window title.\n        pos: Initial position.\n        size: Initial size.\n        style: wx.Frame style flags.\n        name: Widget name.\n    \"\"\"\n    self.gui_style = gui_style\n    self.destroy_fun_tab = []\n    self.idle_objects = []\n    self.after_init = False\n    self.desktop = None\n    self._mgr = None\n    self.toolbar_interface = None\n    self.aTable = None\n    self.ctrl = None\n\n    SchBaseFrame.__init__(\n        self, parent, id, gui_style, title, pos, size, style | wx.WANTS_CHARS, name\n    )\n    wx.GetApp().SetTopWindow(self)\n\n    self.init_plugins()\n    self.Bind(wx.EVT_IDLE, self.on_idle)\n    self.Bind(wx.EVT_SIZE, self.on_size)\n\n    env = get_environ()\n    username = env(\"USERNAME\")\n    password = env(\"PASSWORD\")\n\n    init(os.environ[\"PRJ_NAME\"], username, password, user_agent=\"webviewembeded\")\n    start_request = request(\"/\", None, user_agent=\"webviewembeded\")\n\n    self.ctrl = pytigon_gui.guictrl.ctrl.HTML2(\n        self, name=\"schbrowser\", size=self.GetClientSize()\n    )\n    self.ctrl.load_str(start_request.str(), \"http://127.0.0.5/\")\n\n    if sys.platform != \"win32\":\n        start = time.time()\n        while not self.ctrl.page_loaded:\n            if time.time() - start &gt; 10:\n                logger.warning(\"Page load timed out\")\n                break\n            wx.Yield()\n\n    size = wx.GetApp().app_size\n    wx.CallAfter(self.SetSize, (size[0], size[1]))\n    wx.CallAfter(self.Show)\n</code></pre>"},{"location":"api/guiframe_browserframe/#pytigon_gui.guiframe.browserframe.SchBrowserFrame-functions","title":"Methods:","text":""},{"location":"api/guiframe_browserframe/#pytigon_gui.guiframe.browserframe.SchBrowserFrame.get_menu_bar","title":"get_menu_bar","text":"<pre><code>get_menu_bar()\n</code></pre> <p>Return None; browser frames have no menu bar.</p> Source code in <code>pytigon_gui/guiframe/browserframe.py</code> <pre><code>def get_menu_bar(self):\n    \"\"\"Return None; browser frames have no menu bar.\"\"\"\n    return None\n</code></pre>"},{"location":"api/guiframe_browserframe/#pytigon_gui.guiframe.browserframe.SchBrowserFrame.on_idle","title":"on_idle","text":"<pre><code>on_idle(event)\n</code></pre> <p>Idle handler: process idle objects and load start pages.</p> <p>On the first idle event, any configured <code>start_pages</code> are opened via <code>_on_html</code>.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>wx.IdleEvent.</p> required Source code in <code>pytigon_gui/guiframe/browserframe.py</code> <pre><code>def on_idle(self, event):\n    \"\"\"Idle handler: process idle objects and load start pages.\n\n    On the first idle event, any configured ``start_pages`` are\n    opened via ``_on_html``.\n\n    Args:\n        event: wx.IdleEvent.\n    \"\"\"\n    for obj in self.idle_objects:\n        try:\n            obj.on_idle()\n        except Exception:\n            logger.debug(\"Error in idle object %s\", type(obj).__name__, exc_info=True)\n\n    if not self.after_init:\n        self.after_init = True\n        app = wx.GetApp()\n        if len(app.start_pages) &gt; 0:\n\n            def start_pages():\n                for page in app.start_pages:\n                    url_page = page.split(\";\")\n                    if len(url_page) == 2:\n                        self._on_html(\n                            _(url_page[0]) + \",\" + app.base_address + url_page[1]\n                        )\n\n            wx.CallAfter(start_pages)\n\n    event.Skip()\n</code></pre>"},{"location":"api/guiframe_browserframe/#pytigon_gui.guiframe.browserframe.SchBrowserFrame.on_size","title":"on_size","text":"<pre><code>on_size(event)\n</code></pre> <p>Resize the embedded webview to match the frame.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>wx.SizeEvent or None.</p> required Source code in <code>pytigon_gui/guiframe/browserframe.py</code> <pre><code>def on_size(self, event):\n    \"\"\"Resize the embedded webview to match the frame.\n\n    Args:\n        event: wx.SizeEvent or None.\n    \"\"\"\n    if self.ctrl:\n        if event:\n            self.ctrl.SetSize(event.GetSize())\n            event.Skip()\n        else:\n            self.ctrl.SetSize(self.GetSize())\n</code></pre>"},{"location":"api/guiframe_browserframe/#pytigon_gui.guiframe.browserframe.SchBrowserFrame.set_acc_key_tab","title":"set_acc_key_tab","text":"<pre><code>set_acc_key_tab(win, tab)\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>win</code> <p>Target window.</p> required <code>tab</code> <p>Accelerator table entries.</p> required Source code in <code>pytigon_gui/guiframe/browserframe.py</code> <pre><code>def set_acc_key_tab(self, win, tab):\n    \"\"\"No-op: keyboard accelerators not supported in browser mode.\n\n    Args:\n        win: Target window.\n        tab: Accelerator table entries.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/guiframe_form/","title":"guiframe.form \u2013 Form Frame","text":"<p>Dynamic form generation frame \u2013 creates wxPython forms from Django model definitions or tag-based layouts.</p>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form","title":"form","text":"<p>Form rendering panel for Pytigon applications.</p> <p>SchForm is a scrollable panel that parses HTML, renders it via a custom wxDC backend, and manages interactive widgets defined by the HTML markup.</p>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form-attributes","title":"Attributes","text":""},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form._","title":"_  <code>module-attribute</code>","text":"<pre><code>_ = wx.GetTranslation\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form._INIT_CSS_STR","title":"_INIT_CSS_STR  <code>module-attribute</code>","text":"<pre><code>_INIT_CSS_STR = None\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form._PRE_PRECESS_LIB","title":"_PRE_PRECESS_LIB  <code>module-attribute</code>","text":"<pre><code>_PRE_PRECESS_LIB = []\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.logger","title":"logger  <code>module-attribute</code>","text":"<pre><code>logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form-classes","title":"Classes","text":""},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm","title":"SchForm","text":"<pre><code>SchForm(parent, page=None, hscroll=False, vscroll=False, form_type='body')\n</code></pre> <p>               Bases: <code>ScrolledPanel</code></p> <p>Html window</p> <p>Constructor</p> <p>Parameters:</p> Name Type Description Default <code>form_type - can be</code> <p>body, header, footer, panel</p> required Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def __init__(\n    self, parent, page=None, hscroll=False, vscroll=False, form_type=\"body\"\n):\n    \"\"\"Constructor\n\n    Args:\n        parent - parent window\n        page - parent SchPage object\n        hscroll - enable horizontal scroll bar\n        vscroll - enable vertical scroll  bar\n        form_type - can be: body, header, footer, panel\n    \"\"\"\n    self._bestsize = None\n    self._scroll_xy = (0, 0)\n    self._act_scroll_xy = (0, 0)\n    self._enabled_controls = False\n    self._last_size = (-1, -1)\n    self._best_virtual_size = None\n    self._block = False\n    self._dc_buf = None\n    self._dc_buf_x = 0\n    self._dc_buf_y = 0\n    self._lock = False\n\n    self.no_vscrollbar = not vscroll\n    self.no_hscrollbar = not hscroll\n    self.address = None\n    self.script = None\n    self.parameters = None\n    self.page_source = \"&lt;html&gt;&lt;/html&gt;\"\n    self.bestsize = None\n    self.hover_obj = None\n    self.cursor_type = 0\n    self.wxdc = None\n    self.after_init = False\n    self.update_controls = False\n    self.obj_action_dict = {}\n    self.obj_id_dict = {}\n    self.last_control_with_focus = None\n    self.page = page\n    self.form_type = form_type\n    self.init_css_str = None\n    self.evt_ind = -1\n    self.closing = False\n    self.acc_tabs = {}\n    self.acc_tab = None\n    self.acc_tab_bind = False\n    self.last_clicked = None\n    self.t1 = None\n    self.websockets = []\n\n    self.hscroll = hscroll\n    self.vscroll = vscroll\n\n    ScrolledPanel.__init__(self, parent, -1, style=wx.WANTS_CHARS)\n    try:\n        self.SetBackgroundStyle(wx.BG_STYLE_ERASE)\n    except Exception:\n        self.SetBackgroundStyle(wx.BG_STYLE_CUSTOM)\n\n    self.SetupScrolling(hscroll, vscroll, rate_y=1)\n\n    self.SetAcceleratorTable(wx.AcceleratorTable(wx.GetApp().GetTopWindow().aTable))\n\n    self.Bind(wx.EVT_SIZE, self.on_size)\n    self.Bind(wx.EVT_LEFT_DOWN, self.on_left_down)\n    self.Bind(wx.EVT_LEFT_UP, self.on_left_up)\n    self.Bind(wx.EVT_RIGHT_UP, self.on_right_up)\n    self.Bind(wx.EVT_MOTION, self.on_motion)\n    self.Bind(wx.EVT_ERASE_BACKGROUND, self.on_erase_background)\n    self.Bind(wx.EVT_SET_FOCUS, self.on_focus)\n\n    self.EnableScrolling(False, False)\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm-functions","title":"Methods:","text":""},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.CanClose","title":"CanClose","text":"<pre><code>CanClose()\n</code></pre> <p>Test if form can be closed</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def CanClose(self):\n    \"\"\"Test if form can be closed\"\"\"\n    ret = True\n    if hasattr(self, \"can_close\"):\n        ret = self.can_close()\n    else:\n        widgets = list(self.GetParent().get_widgets().values())\n        for w in widgets:\n            if hasattr(w, \"CanClose\"):\n                if not w.CanClose():\n                    ret = False\n    return ret\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.any_parent_command","title":"any_parent_command","text":"<pre><code>any_parent_command(command, *args, **kwds)\n</code></pre> <p>Walk up the parent hierarchy to find and call command.</p> <p>Parameters:</p> Name Type Description Default <code>command</code> <p>Method name to search for.</p> required <code>args, kwds</code> <p>Forwarded to the found method.</p> required <p>Returns:</p> Type Description <p>Return value of the method, or None if not found.</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def any_parent_command(self, command, *args, **kwds):\n    \"\"\"Walk up the parent hierarchy to find and call *command*.\n\n    Args:\n        command: Method name to search for.\n        args, kwds: Forwarded to the found method.\n\n    Returns:\n        Return value of the method, or None if not found.\n    \"\"\"\n    parent = self.GetParent()\n    while parent is not None:\n        if hasattr(parent, command):\n            return getattr(parent, command)(*args, **kwds)\n        parent = parent.GetParent()\n    logger.warning(\"METHOD NOT FOUND: %s\", command)\n    return None\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.bind_to_ctrl","title":"bind_to_ctrl","text":"<pre><code>bind_to_ctrl(ctrl, id, fun, fun2=None)\n</code></pre> <p>bind function callback to command event from ctrl</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def bind_to_ctrl(self, ctrl, id, fun, fun2=None):\n    \"\"\"bind function callback to command event from ctrl\n\n    Args:\n        ctrl - widget\n        id - command id\n        fun - callback function command event\n        fun2 - callback function for EVT_UPDATE_UI event\n\n    \"\"\"\n    if fun2:\n        ctrl.Bind(wx.EVT_UPDATE_UI, fun2, id=id)\n    ctrl.Bind(RB.EVT_RIBBONBUTTONBAR_CLICKED, fun, id=id)\n    ctrl.Bind(RB.EVT_RIBBONTOOLBAR_CLICKED, fun, id=id)\n    ctrl.Bind(wx.EVT_MENU, fun, id=id)\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.cancel","title":"cancel","text":"<pre><code>cancel()\n</code></pre> <p>Close this form without saving its content</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def cancel(self):\n    \"\"\"Close this form without saving its content\"\"\"\n    if self.page:\n        self.page.exists = False\n    if self.t1:\n        self.t1.Stop()\n    if self.page:\n        self.page.GetParent().on_child_form_cancel()\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.draw_background","title":"draw_background","text":"<pre><code>draw_background(refresh_all=False, size=None)\n</code></pre> <p>drawn a rendered html page as a background</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def draw_background(self, refresh_all=False, size=None):\n    \"\"\"drawn a rendered html page as a background\"\"\"\n    if self._block:\n        return\n    self._block = True\n    if not self.wxdc:\n        self.Scroll(0, 0)\n    (xv, yv) = self.GetViewStart()\n    (dx, dy) = self.GetScrollPixelsPerUnit()\n    (x, y) = (xv * dx, yv * dy)\n    self._act_scroll_xy = (x, y)\n    dc = wx.ClientDC(self)\n    if self.wxdc and self._dc_buf and self._dc_buf_x == x and self._dc_buf_y == y:\n        dc.SetDeviceOrigin(-1 * x, -1 * y)\n        rect = self.GetRect()\n        dc.Blit(x, y, rect.GetWidth(), rect.GetHeight(), self._dc_buf, x, y)\n    else:\n        rect = self.GetRect()\n        if rect.GetWidth() &lt; 1 or rect.GetHeight() &lt; 1:\n            rect = wx.Rect(0, 0, 1, 1)  # width=1, height=1)\n        dc.SetDeviceOrigin(-1 * x, -1 * y)\n        bitmap = wx.Bitmap(rect.GetWidth(), rect.GetHeight())\n        dc2 = wx.MemoryDC(bitmap)\n        dc2.SetBackground(\n            wx.Brush(wx.SystemSettings.GetColour(wx.SYS_COLOUR_3DFACE))\n        )\n        dc2.Clear()\n        dc2.SetDeviceOrigin(-1 * x, -1 * y)\n        if not self.wxdc:\n            if size:\n                self.html_updatet(dc2, size)\n            else:\n                self.html_updatet(dc2, rect)\n        else:\n            self.wxdc.dc = dc2\n            self.wxdc.play()\n        dc.Blit(x, y, rect.GetWidth(), rect.GetHeight(), dc2, x, y)\n        self._dc_buf = dc2\n        self._dc_buf_x = x\n        self._dc_buf_y = y\n    self._block = False\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.enable","title":"enable","text":"<pre><code>enable(enable=True)\n</code></pre> <p>enable od disable the form</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def enable(self, enable=True):\n    \"\"\"enable od disable the form\"\"\"\n    ret = super().Enable(enable)\n    if enable:\n        wx.CallAfter(self.restore_scroll_pos)\n    return ret\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.enable_ctrls","title":"enable_ctrls","text":"<pre><code>enable_ctrls(ctrls)\n</code></pre> <p>set widgets which are enabled or disabled Widget enabled is updated when html page is refreshed</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def enable_ctrls(self, ctrls):\n    \"\"\"set widgets which are enabled or disabled\n    Widget enabled is updated when html page is refreshed\n\n    Args:\n        ctrls - list or tuple with widgets names\n    \"\"\"\n    self._enabled_controls = ctrls\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.exec_code","title":"exec_code","text":"<pre><code>exec_code(script, parm=None)\n</code></pre> <p>run code attached to html page</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def exec_code(self, script, parm=None):\n    \"\"\"run code attached to html page\"\"\"\n    if parm is None:\n        d = {\"wx\": wx, \"self\": self}\n    else:\n        d = parm\n        d[\"self\"] = self\n    try:\n        exec(script, d)\n    except Exception:\n        logger.error(\"ERROR IN SCRIPT:\\n%s\", script, exc_info=True)\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.get_gparent_page","title":"get_gparent_page","text":"<pre><code>get_gparent_page()\n</code></pre> <p>return grand parent SChPage object in hierarchy</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def get_gparent_page(self):\n    \"\"\"return grand parent SChPage object in hierarchy\"\"\"\n    page = self.get_parent_page()\n    if page:\n        return page.get_parent_page()\n    else:\n        return None\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.get_parent_form","title":"get_parent_form","text":"<pre><code>get_parent_form()\n</code></pre> <p>return parent SchFrame object in hierarchy</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def get_parent_form(self):\n    \"\"\"return parent SchFrame object in hierarchy\"\"\"\n    gpage = self.get_gparent_page()\n    if gpage:\n        return gpage.active_form\n    return None\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.get_parent_page","title":"get_parent_page","text":"<pre><code>get_parent_page()\n</code></pre> <p>return parent SChPage object in hierarchy</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def get_parent_page(self):\n    \"\"\"return parent SChPage object in hierarchy\"\"\"\n    return self.page\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.get_parm_obj","title":"get_parm_obj","text":"<pre><code>get_parm_obj()\n</code></pre> <p>Get parent object width method get_par</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def get_parm_obj(self):\n    \"\"\"Get parent object width method get_par\"\"\"\n    parent = self\n    while parent is not None:\n        if hasattr(parent, \"get_parm\"):\n            return parent\n        parent = parent.GetParent()\n    return None\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.href_clicked","title":"href_clicked","text":"<pre><code>href_clicked(ctrl, attr_dict, upload=False, fields=False)\n</code></pre> <p>Handle action connected to widget with href attribute</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def href_clicked(self, ctrl, attr_dict, upload=False, fields=False):\n    \"\"\"Handle action connected to widget with href attribute\n\n    Args:\n        ctrl - widget\n        attr_dict - dict with attributes for http request\n        upload - if true, this is action prepared to upload content to server\n        fields - list of fields which values are sended to http server\n    \"\"\"\n    self.last_clicked = ctrl\n\n    if \"href\" in attr_dict:\n        href = attr_dict[\"href\"]\n        if href and len(href) &gt; 0 and href[0] == \"#\":\n            return self.scroll_to_href(href)\n        if href:\n            href = clean_href(href)\n        if \"target\" in attr_dict:\n            target = attr_dict[\"target\"]\n        else:\n            target = \"_blank\"\n        if \"title\" in attr_dict:\n            title = attr_dict[\"title\"]\n        else:\n            title = None\n\n        if href and hasattr(self, \"filter_url\"):\n            f = self.filter_url(target, href)\n            if f != None:\n                if type(f) == str:\n                    href = clean_href(f)\n                else:\n                    return f\n\n        if href:\n            parent_form = self.get_parent_form()\n            if hasattr(parent_form, \"filter_child_url\"):\n                ret = parent_form.filter_child_url(self, href)\n                if ret:\n                    return ret\n            # parent_page = self.GetParent().get_parent_page()\n            # if hasattr(parent_page.body, \"filter_child_url\"):\n            #    ret = parent_page.body.filter_child_url(self, href)\n            #    if ret:\n            #        return ret\n\n        else:\n            if target == \"_parent\":\n                self.any_parent_command(\"on_child_form_cancel\")\n                return\n            if target == \"_refresh\":\n                self.any_parent_command(\"send_refr_obj\")\n            if target == \"refresh_frame\":\n                self.any_parent_command(\"_refresh\")\n            return\n        if href[:7] == \"http://\" or href[:7] == \"file://\":\n            adr = href\n        else:\n            if href:\n                if href[0] == \".\":\n                    elm1 = self.get_parm_obj().address.split(\"?\")[0]\n                    if not elm1[-1] == \"/\":\n                        id = elm1.rfind(\"/\")\n                        if id &gt;= 0:\n                            elm1 = elm1[: id + 1]\n                        else:\n                            elm1 = \"\"\n                    adr = elm1 + href\n                else:\n                    adr = href\n            else:\n                adr = self.get_parm_obj().address\n\n        if \"/pdf/view\" in adr:\n            wx.GetApp().GetTopWindow().show_pdf(adr)\n            return\n\n        if \"/odf/view\" in adr:\n            wx.GetApp().GetTopWindow().show_odf(adr)\n            return\n\n        parm = createparm.create_parm(adr, self.get_parm_obj(), no_encode=True)\n        if parm:\n            adr = parm[0]\n\n        if fields:\n            (typ, fields2) = fields.split(\":\")\n            adr = adr + \"|\" + fields2\n        if target == \"refresh_frame\":\n            self.any_parent_command(\"set_adr_and_param\", adr, self.get_parm_obj())\n            self.any_parent_command(\"refresh_html\")\n            return\n        parm = createparm.create_parm(adr, self.get_parm_obj(), no_encode=True)\n        post = None\n        if parm:\n            if typ in (\"POST\", \"post\"):\n                adr = parm[0]\n                post = parm[2]\n            else:\n                adr = parm[0] + parm[1] + parm[2]\n                post = None\n\n        http = wx.GetApp().get_http(ctrl)\n\n        if upload:\n            response = http.post(self, adr, post, upload=True)\n        else:\n            if post:\n                response = http.post(self, adr, post)\n            else:\n                response = http.get(self, adr)\n\n        if \"500\" in response.ret_content_type:\n            return\n\n        if \"text/plain\" in response.ret_content_type:\n            s = response.str()\n            if title:\n                edit_name = title\n            else:\n                edit_name = href\n            okno = self.new_main_page(\"^standard/editor/editor.html\", href, None)\n            okno.body.EDITOR.SetValue(s)\n            okno.body.EDITOR.GotoPos(0)\n        elif \"/json\" in response.ret_content_type:\n            x = response.json()\n            if \"action\" in x and x[\"action\"] in (\n                \"update_row_ok\",\n                \"new_row_ok\",\n                \"delete_row_ok\",\n            ):\n                if \"obj\" in x:\n                    obj = x[\"obj\"]\n                else:\n                    obj = None\n                if self.GetParent().parent_page:\n                    ret = self.GetParent().parent_page.signal(x[\"action\"], obj)\n                else:\n                    ret = self.GetParent().signal(x[\"action\"], obj)\n                if ret:\n                    self.cancel()\n                else:\n                    self.ok()\n            return\n        else:\n            if \"text/\" not in response.ret_content_type:\n                wx.GetApp().GetTopWindow()._open_page(response)\n                return\n\n            s = response.str()\n            mp = ShtmlParser()\n            mp.process(s, response.new_url)\n            if \"target\" in mp.var:\n                target = mp.var[\"target\"]\n\n            if href and hasattr(self, \"filter_http_result\"):\n                f = self.filter_http_result(target, href, mp)\n                if f != None:\n                    return f\n            if target == \"_self\":\n                update_controls = self.update_controls\n                self.update_controls = True\n                self.show_form(mp.get_body(), self.get_parm_obj())\n                self.wxdc = None\n                self.draw_background()\n                self.Refresh()\n                self.init()\n                self.update_controls = update_controls\n                if \"href\" in attr_dict:\n                    href = attr_dict[\"href\"]\n                else:\n                    href = None\n\n                if (\n                    self.GetParent().address_or_parser.__class__.__name__\n                    == \"ShtmlParser\"\n                ):\n                    self.GetParent().address_or_parser.address = response.new_url\n                else:\n                    self.GetParent().address_or_parser = response.new_url\n\n                return\n            if (\n                target.startswith(\"popup\")\n                or target.startswith(\"inline\")\n                or target\n                in (\n                    \"_blank\",\n                    \"_self\",\n                )\n            ):\n                self.GetParent().active_ctrl = ctrl\n                win = self.new_child_page(\n                    mp, is_null(mp.title, title), parameters=self.get_parm_obj()\n                )\n                if win:\n                    win.body.set_address_parm(str(response.new_url))\n                return\n            if target == \"_parent\":\n                self.new_main_page(\n                    mp,\n                    is_null(mp.title, title),\n                    parameters=self.get_parm_obj(),\n                    view_in=None,\n                )\n                return\n            if target == \"refresh_page\":\n                # self.any_parent_command(\"set_new_href\", adr)\n                self.any_parent_command(\"refresh_html\")\n\n            if target.startswith(\"_top2\"):\n                x = target[5:]\n                if x[0:1] == \"_\":\n                    self.new_main_page(\n                        mp,\n                        is_null(mp.title, title),\n                        parameters=self.get_parm_obj(),\n                        view_in=x[1:],\n                    )\n                else:\n                    self.new_main_page(\n                        mp,\n                        is_null(mp.title, title),\n                        parameters=self.get_parm_obj(),\n                        view_in=\"desktop2\",\n                    )\n                return\n            if target == \"_parent\":\n                self.any_parent_command(\"on_child_form_cancel\")\n                return\n            if target == \"_refresh\":\n                self.GetParent().refresh_html()\n                return\n            if target == \"_parent_refr\":\n                self.any_parent_command(\"on_child_form_ok\")\n                return\n            if target == \"message\":\n                dlg = wx.MessageDialog(\n                    self, s, is_null(mp.title, title), style=wx.OK\n                )\n                dlg.ShowModal()\n                return\n\n            if target == \"code\":\n                script = mp.get_body()[1]\n                app = wx.GetApp()\n                main_window = app.GetTopWindow()\n                desktop = (main_window.desktop,)\n                mgr = main_window._mgr\n                menu_bar = main_window.get_menu_bar()\n                tool_bars = main_window.get_tool_bar()\n                exec(script.elem.text.replace(\"\\r\", \"\"))\n                return\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.html_updatet","title":"html_updatet","text":"<pre><code>html_updatet(dc, size)\n</code></pre> <p>parse a html page after it was updated</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def html_updatet(self, dc, size):\n    \"\"\"parse a html page after it was updated\"\"\"\n    w = size.GetWidth()\n    h = size.GetHeight()\n    if w == 20 and h == 20:\n        return\n    if not self.wxdc:\n        if self.no_vscrollbar:\n            self.wxdc = DcDc(\n                dc, calc_only=False, width=w - 1, height=-1, record=True\n            )\n        else:\n            self.wxdc = DcDc(\n                dc,\n                calc_only=False,\n                width=w - 1 - wx.SystemSettings.GetMetric(wx.SYS_VSCROLL_X),\n                height=-1,\n                record=True,\n            )\n        p = HtmlViewerParser(\n            dc=self.wxdc, calc_only=False, init_css_str=self.get_css(), css_type=1\n        )\n        p.set_http_object(wx.GetApp().http)\n        p.set_parent_window(self)\n        if not self._lock:\n            self._lock = True\n            self.GetParent().restart_ctrl_lp()\n            p.feed(self.page_source)\n            self.GetParent().remove_old_ctrls()\n            self._lock = False\n        self.obj_action_dict = p.obj_action_dict\n        self.obj_id_dict = p.obj_id_dict\n        if not self.no_vscrollbar:\n            (w2, h2) = p.get_max_sizes()\n            # h2+=8\n            # h2-=wx.SystemSettings.GetMetric(wx.SYS_VSCROLL_X)\n            self.hscroll = True if w2 &gt; w + 3 else False\n            self.vscroll = True if h2 &gt; h + 3 else False\n            if self.no_hscrollbar:\n                self.hscroll = False\n            if (\n                (self.vscroll or self.hscroll)\n                and w &gt; 0\n                and h &gt; 0\n                and w2 &gt; 0\n                and h2 &gt; 0\n            ):\n                if self.vscroll:\n                    w2 = w2 + wx.SystemSettings.GetMetric(wx.SYS_VSCROLL_X)\n                if self.hscroll:\n                    h2 = h2 + wx.SystemSettings.GetMetric(wx.SYS_HSCROLL_Y)\n                self._best_virtual_size = (int(w2), int(h2))\n                self.EnableScrolling(self.hscroll, self.vscroll)\n                self._SetupAfter(False)\n            self.SetVirtualSize((w2, h2))\n        else:\n            self.EnableScrolling(False, False)\n            self.SetVirtualSize((w, h))\n        p.close()\n        self.update_controls = False\n        wx.CallAfter(self.SetupScrolling, self.hscroll, self.vscroll, rate_y=1)\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.init","title":"init","text":"<pre><code>init(callback=None)\n</code></pre> <p>init form</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def init(self, callback=None):\n    \"\"\"init form\"\"\"\n    if not self.after_init:\n        if hasattr(self, \"init_form\"):\n\n            def _init():\n                nonlocal self\n                self.init_form()\n                if callback:\n                    callback(self)\n\n            wx.CallAfter(_init)\n        self.after_init = True\n    else:\n        if hasattr(self, \"reinit\"):\n            self.reinit()\n        elif hasattr(self, \"init_form\"):\n            self.init_form()\n        if callback:\n            callback(self)\n    wx.CallAfter(self._build_acc_tab)\n    wx.CallAfter(self._check_scroll_bar)\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.new_child_page","title":"new_child_page","text":"<pre><code>new_child_page(address_or_parser, title='', parameters=None, callback=None)\n</code></pre> <p>Create a child page</p> <p>Parameters:</p> Name Type Description Default <code>address_or_parser</code> <p>can be: address of http page (str type) or</p> required <code></code> required <code>parameters</code> <p>dict</p> <code>None</code> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def new_child_page(\n    self, address_or_parser, title=\"\", parameters=None, callback=None\n):\n    \"\"\"Create a child page\n\n    Args:\n        address_or_parser: can be: address of http page (str type) or\n        :class:'~pytigon_lib.schparser.html_parsers.ShtmlParser'\n        title - new page title\n        parameters: dict\n    \"\"\"\n    self._scroll_xy = self.GetViewStart()\n    self.get_parm_obj().active_form = self\n    self.GetParent().active_form = self\n    if hasattr(self.GetParent(), \"last_control_with_focus\"):\n        self.last_control_with_focus = self.GetParent().last_control_with_focus\n    else:\n        self.last_control_with_focus = None\n    self.GetParent().disable_setfocus = True\n    return self.any_parent_command(\n        \"new_child_page\", address_or_parser, title, parameters, callback=callback\n    )\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.refr","title":"refr","text":"<pre><code>refr(refr_always=False)\n</code></pre> <p>Refresh html content</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def refr(self, refr_always=False):\n    # \"\"\"Refresh form\"\"\"\n    # self.refr_obj(refr_always=True)\n    # def refr_obj(self, refr_always=False):\n    \"\"\"Refresh html content\"\"\"\n\n    if refr_always or self.GetParent().IsShown():\n        if self.address:\n            parm = createparm.create_parm(self.address, self.get_parm_obj())\n            http = wx.GetApp().get_http(self)\n            if parm:\n                response = http.get(self, str(parm[0] + parm[1] + parm[2]))\n            else:\n                response = http.get(self, self.address)\n            if response.ret_code == 404:\n                return\n\n            mp = ShtmlParser()\n            mp.process(response.str(), \"local\")\n            if self.form_type == \"panel\":\n                strona = mp.get_panel()\n            elif self.form_type == \"header\":\n                strona = mp.get_header()\n            elif self.form_type == \"footer\":\n                strona = mp.get_footer()\n            else:\n                strona = mp.get_body()\n            if self.show_form(strona, self.parameters):\n                self.init()\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.refresh_time","title":"refresh_time","text":"<pre><code>refresh_time(number_of_sec)\n</code></pre> <p>When refresh time is set form automatically refresh his content every number_of_sec seconds</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def refresh_time(self, number_of_sec):\n    \"\"\"When refresh time is set form automatically refresh his content every number_of_sec seconds\"\"\"\n    self.t1 = wx.Timer(self)\n    self.t1.Start(int(number_of_sec * 1000))\n    self.Bind(wx.EVT_TIMER, self.on_timer, self.t1)\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.scroll_child_into_view","title":"scroll_child_into_view","text":"<pre><code>scroll_child_into_view(child)\n</code></pre> <p>scroll this window to position at which child window is visible</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def scroll_child_into_view(self, child):\n    \"\"\"scroll this window to position at which child window is visible\"\"\"\n    if child.GetParent() == self:\n        (sppu_x, sppu_y) = self.GetScrollPixelsPerUnit()\n        (vs_x, vs_y) = self.GetViewStart()\n        cr = child.GetRect()\n        clntsz = self.GetClientSize()\n        (new_vs_x, new_vs_y) = (-1, -1)\n        if cr.y &lt; 0 and sppu_y &gt; 0:\n            new_vs_y = vs_y + cr.y / sppu_y\n        if cr.bottom &gt; clntsz.height and sppu_y &gt; 0:\n            diff = math.ceil((1.0 * (cr.bottom - clntsz.height + 1)) / sppu_y)\n            if cr.y - diff * sppu_y &gt; 0:\n                new_vs_y = vs_y + diff\n            else:\n                new_vs_y = vs_y + cr.y / sppu_y\n        if new_vs_y != -1:\n            self.Scroll(-1, new_vs_y)\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.scroll_to_href","title":"scroll_to_href","text":"<pre><code>scroll_to_href(href)\n</code></pre> <p>scroll this window to position at which widget with a href attribute is visible</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def scroll_to_href(self, href):\n    \"\"\"scroll this window to position at which widget with a href attribute is visible\"\"\"\n    ref = href[1:]\n    if ref in self.obj_id_dict:\n        obj = self.obj_id_dict[ref]\n        rects = obj.rendered_rects\n        if len(rects) &gt; 0:\n            rect = rects[0]\n            x = rect[0]\n            y = rect[1]\n            if self.GetScrollPixelsPerUnit()[0] &gt; 0:\n                dx = x / self.GetScrollPixelsPerUnit()[0]\n            else:\n                dx = -1\n            if self.GetScrollPixelsPerUnit()[1] &gt; 0:\n                dy = y / self.GetScrollPixelsPerUnit()[1]\n            else:\n                dy = -1\n            self.Scroll(dx, dy)\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.set_acc_key_tab","title":"set_acc_key_tab","text":"<pre><code>set_acc_key_tab(win, tab)\n</code></pre> <p>Set accelerator table for window</p> <p>Parameters:</p> Name Type Description Default <code>tab - list or tuple of accelerator elements. Each emement has structure</code> <p>(flag, keycode, callback fun)</p> required Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def set_acc_key_tab(self, win, tab):\n    \"\"\"Set accelerator table for window\n\n    Args:\n        win - window which handle accelerator keys\n        tab - list or tuple of accelerator elements. Each emement has structure: (flag, keycode, callback fun)\n    \"\"\"\n    if win in self.acc_tabs:\n        self.acc_tabs[win].append(tab)\n    else:\n        self.acc_tabs[win] = [\n            tab,\n        ]\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.set_address_parm","title":"set_address_parm","text":"<pre><code>set_address_parm(address)\n</code></pre> <p>page address for refr_obj in format http://adres?zmienna1|zmienna2,zmienna</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def set_address_parm(self, address):\n    \"\"\"page address for refr_obj in format http://adres?zmienna1|zmienna2,zmienna\"\"\"\n    if address:\n        elementy = address.split(\"|\")\n        if elementy[0][-1] == \"/\":\n            address2 = address\n        else:\n            address2 = elementy[0] + \"/\"\n            if len(elementy) &gt; 1:\n                address2 = address2 + \"|\" + elementy[1]\n        self.address = address2\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.set_best_size","title":"set_best_size","text":"<pre><code>set_best_size(bestsize)\n</code></pre> <p>Set the best size for this form</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def set_best_size(self, bestsize):\n    \"\"\"Set the best size for this form\"\"\"\n    self.bestsize = bestsize\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.set_css","title":"set_css","text":"<pre><code>set_css(css_str)\n</code></pre> <p>set css file for this form</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def set_css(self, css_str):\n    \"\"\"set css file for this form\"\"\"\n    self.init_css_str = css_str\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.set_page","title":"set_page","text":"<pre><code>set_page(page_source)\n</code></pre> <p>Set html page source</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def set_page(self, page_source):\n    \"\"\"Set html page source\"\"\"\n    self.page_source = page_source\n    self.wxdc = None\n    self.draw_background()\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.SchForm.show_form","title":"show_form","text":"<pre><code>show_form(page_and_script, parameters=None)\n</code></pre> <p>Show form</p> <p>Parameters:</p> Name Type Description Default <code>page_and_script</code> <p>list or touple, 0: html page content, 1: script</p> required <code>parameters</code> <p>parameters forwarded to http server in request</p> <code>None</code> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def show_form(self, page_and_script, parameters=None):\n    \"\"\"Show form\n\n    Args:\n        page_and_script: list or touple, 0: html page content, 1: script\n        parameters: parameters forwarded to http server in request\n    \"\"\"\n    if self.page and self.page.exists == False:\n        return False\n\n    self.page_source = self.pre_process_page(page_and_script[0])\n    self.parameters = parameters\n    if not self.script:\n        self.script = str(page_and_script[1])\n        if self.script and len(self.script) &gt; 1:\n            app = wx.GetApp()\n            main_window = app.GetTopWindow()\n            desktop = (main_window.desktop,)\n            mgr = main_window._mgr\n            menu_bar = main_window.get_menu_bar()\n            script_tmp = self.script.replace(\"\\r\", \"\").split(\"\\n\")\n            script = \"\"\n            tab = -1\n            for line in script_tmp:\n                if tab == -1:\n                    if line.strip() != \"\":\n                        tab = len(line) - len(line.lstrip())\n                if tab &gt;= 0:\n                    script = script + line[tab:] + \"\\n\"\n            d = {\"wx\": wx}\n            self.exec_code(script, d)\n\n            for key in d:\n                if not key.startswith(\"__\"):\n                    if hasattr(d[key], \"__call__\"):\n                        fun = d[key]\n                        try:\n                            method = types.MethodType(d[key], self, self.__class__)\n                        except TypeError:\n                            method = types.MethodType(d[key], self)\n                        if hasattr(self, key):\n                            setattr(self, \"base_\" + key, getattr(self, key))\n                        setattr(self, key, method)\n                    else:\n                        setattr(self, key, d[key])\n    return True\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form-functions","title":"Functions:","text":""},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form._get_css","title":"_get_css","text":"<pre><code>_get_css()\n</code></pre> <p>Load and cache the combined CSS for SchForm rendering.</p> <p>Returns:</p> Type Description <p>String containing concatenated _core.icss and wx.icss.</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def _get_css():\n    \"\"\"Load and cache the combined CSS for SchForm rendering.\n\n    Returns:\n        String containing concatenated _core.icss and wx.icss.\n    \"\"\"\n    global _INIT_CSS_STR\n    if _INIT_CSS_STR is None:\n        with open(str(wx.GetApp().src_path) + \"/appdata/icss/_core.icss\", \"r\") as f:\n            _INIT_CSS_STR = f.read()\n        with open(str(wx.GetApp().src_path) + \"/appdata/icss/wx.icss\", \"r\") as f:\n            _INIT_CSS_STR += f.read()\n    return _INIT_CSS_STR\n</code></pre>"},{"location":"api/guiframe_form/#pytigon_gui.guiframe.form.install_pre_process_lib","title":"install_pre_process_lib","text":"<pre><code>install_pre_process_lib(fun)\n</code></pre> <p>Register a page pre-processing function.</p> <p>Pre-processors are called on HTML pages before rendering. Each receives <code>(form, page_string)</code> and returns a string.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <p>Callable with signature fun(form, page_str) -&gt; str.</p> required Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def install_pre_process_lib(fun):\n    \"\"\"Register a page pre-processing function.\n\n    Pre-processors are called on HTML pages before rendering.\n    Each receives ``(form, page_string)`` and returns a string.\n\n    Args:\n        fun: Callable with signature fun(form, page_str) -&gt; str.\n    \"\"\"\n    global _PRE_PRECESS_LIB\n    _PRE_PRECESS_LIB.append(fun)\n</code></pre>"},{"location":"api/guiframe_manager/","title":"guiframe.manager \u2013 Frame Manager","text":"<p>Frame lifecycle manager \u2013 tracks open frames, handles focus, and coordinates frame-to-frame communication.</p>"},{"location":"api/guiframe_manager/#pytigon_gui.guiframe.manager","title":"manager","text":"<p>Top-level AUI manager classes for Pytigon window layout management.</p> <p>Provides SChAuiBaseManager (base) and SChAuiManager, which wrap wx.lib.agw.aui to add pane activation and hit-test refinements.</p>"},{"location":"api/guiframe_manager/#pytigon_gui.guiframe.manager-attributes","title":"Attributes","text":""},{"location":"api/guiframe_manager/#pytigon_gui.guiframe.manager.logger","title":"logger  <code>module-attribute</code>","text":"<pre><code>logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/guiframe_manager/#pytigon_gui.guiframe.manager-classes","title":"Classes","text":""},{"location":"api/guiframe_manager/#pytigon_gui.guiframe.manager.SChAuiBaseManager","title":"SChAuiBaseManager","text":"<pre><code>SChAuiBaseManager(*argi, **argv)\n</code></pre> <p>               Bases: <code>AuiManager</code></p> <p>Extended AuiManager that restricts sash dragging to non-client areas.</p> <p>Overrides OnLeftDown so that drag operations are only initiated when the hit-test type is not a pane border (types 0 or 1).</p> Source code in <code>pytigon_gui/guiframe/manager.py</code> <pre><code>def __init__(self, *argi, **argv):\n    aui.framemanager.AuiManager.__init__(self, *argi, **argv)\n    self.Bind(wx.EVT_WINDOW_CREATE, self.DoUpdateEvt)\n</code></pre>"},{"location":"api/guiframe_manager/#pytigon_gui.guiframe.manager.SChAuiBaseManager-functions","title":"Methods:","text":""},{"location":"api/guiframe_manager/#pytigon_gui.guiframe.manager.SChAuiBaseManager.OnLeftDown","title":"OnLeftDown","text":"<pre><code>OnLeftDown(event)\n</code></pre> <p>Handle left mouse down, restricting drag to non-border areas.</p> <p>Only propagates the event to the parent handler if the hit-test result is a client or title-bar type.</p> Source code in <code>pytigon_gui/guiframe/manager.py</code> <pre><code>def OnLeftDown(self, event):\n    \"\"\"Handle left mouse down, restricting drag to non-border areas.\n\n    Only propagates the event to the parent handler if the hit-test\n    result is a client or title-bar type.\n    \"\"\"\n    part = self.HitTest(*event.GetPosition())\n    if part.type not in (0, 1):\n        super().OnLeftDown(event)\n</code></pre>"},{"location":"api/guiframe_manager/#pytigon_gui.guiframe.manager.SChAuiManager","title":"SChAuiManager","text":"<pre><code>SChAuiManager(*argi, **argv)\n</code></pre> <p>               Bases: <code>SChAuiBaseManager</code></p> <p>AUI manager that stores pane info on added windows via SetPanel.</p> Source code in <code>pytigon_gui/guiframe/manager.py</code> <pre><code>def __init__(self, *argi, **argv):\n    aui.AuiManager.__init__(self, *argi, **argv)\n</code></pre>"},{"location":"api/guiframe_manager/#pytigon_gui.guiframe.manager.SChAuiManager-functions","title":"Methods:","text":""},{"location":"api/guiframe_manager/#pytigon_gui.guiframe.manager.SChAuiManager.ActivatePane","title":"ActivatePane","text":"<pre><code>ActivatePane(window)\n</code></pre> <p>Activate a pane, returning None on failure instead of raising.</p> <p>Parameters:</p> Name Type Description Default <code>window</code> <p>The wx.Window whose pane should be activated.</p> required <p>Returns:</p> Type Description <p>Result of parent ActivatePane, or None if it fails.</p> Source code in <code>pytigon_gui/guiframe/manager.py</code> <pre><code>def ActivatePane(self, window):\n    \"\"\"Activate a pane, returning None on failure instead of raising.\n\n    Args:\n        window: The wx.Window whose pane should be activated.\n\n    Returns:\n        Result of parent ActivatePane, or None if it fails.\n    \"\"\"\n    try:\n        return aui.AuiManager.ActivatePane(self, window)\n    except Exception:\n        logger.debug(\"ActivatePane failed for window: %s\", window, exc_info=True)\n        return None\n</code></pre>"},{"location":"api/guiframe_manager/#pytigon_gui.guiframe.manager.SChAuiManager.AddPane","title":"AddPane","text":"<pre><code>AddPane(window, arg1, *argi, **argv)\n</code></pre> <p>Add a pane and call SetPanel on the window if supported.</p> <p>Parameters:</p> Name Type Description Default <code>window</code> <p>The wx.Window to add as a pane.</p> required <code>arg1</code> <p>Pane info or direction argument.</p> required <code>*argi</code> <p>Additional positional arguments.</p> <code>()</code> <code>**argv</code> <p>Additional keyword arguments.</p> <code>{}</code> <p>Returns:</p> Type Description <p>Result of the parent AddPane call.</p> Source code in <code>pytigon_gui/guiframe/manager.py</code> <pre><code>def AddPane(self, window, arg1, *argi, **argv):\n    \"\"\"Add a pane and call SetPanel on the window if supported.\n\n    Args:\n        window: The wx.Window to add as a pane.\n        arg1: Pane info or direction argument.\n        *argi: Additional positional arguments.\n        **argv: Additional keyword arguments.\n\n    Returns:\n        Result of the parent AddPane call.\n    \"\"\"\n    ret = aui.AuiManager.AddPane(self, window, arg1, *argi, **argv)\n    if hasattr(window, \"SetPanel\"):\n        window.SetPanel(arg1)\n    return ret\n</code></pre>"},{"location":"api/guiframe_notebook/","title":"guiframe.notebook \u2013 Notebook","text":"<p>Tabbed notebook container (wx.Notebook wrapper) with dynamic page creation, close buttons, and drag-reorder support.</p>"},{"location":"api/guiframe_notebook/#pytigon_gui.guiframe.notebook","title":"notebook","text":"<p>SchNotebook object used as a top-level container in panels: 'desktop', 'panel', 'header' and 'footer'.</p> <p>Extends aui.AuiNotebook with custom page activation, closing, splitting, and wiki-based help lookup.</p>"},{"location":"api/guiframe_notebook/#pytigon_gui.guiframe.notebook-attributes","title":"Attributes","text":""},{"location":"api/guiframe_notebook/#pytigon_gui.guiframe.notebook.logger","title":"logger  <code>module-attribute</code>","text":"<pre><code>logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/guiframe_notebook/#pytigon_gui.guiframe.notebook-classes","title":"Classes","text":""},{"location":"api/guiframe_notebook/#pytigon_gui.guiframe.notebook.SChAuiBaseManager","title":"SChAuiBaseManager","text":"<pre><code>SChAuiBaseManager(*argi, **argv)\n</code></pre> <p>               Bases: <code>AuiManager</code></p> <p>Extended AuiManager that restricts sash dragging to non-client areas.</p> <p>Overrides OnLeftDown so that drag operations are only initiated when the hit-test type is not a pane border (types 0 or 1).</p> Source code in <code>pytigon_gui/guiframe/manager.py</code> <pre><code>def __init__(self, *argi, **argv):\n    aui.framemanager.AuiManager.__init__(self, *argi, **argv)\n    self.Bind(wx.EVT_WINDOW_CREATE, self.DoUpdateEvt)\n</code></pre>"},{"location":"api/guiframe_notebook/#pytigon_gui.guiframe.notebook.SChAuiBaseManager-functions","title":"Methods:","text":""},{"location":"api/guiframe_notebook/#pytigon_gui.guiframe.notebook.SChAuiBaseManager.OnLeftDown","title":"OnLeftDown","text":"<pre><code>OnLeftDown(event)\n</code></pre> <p>Handle left mouse down, restricting drag to non-border areas.</p> <p>Only propagates the event to the parent handler if the hit-test result is a client or title-bar type.</p> Source code in <code>pytigon_gui/guiframe/manager.py</code> <pre><code>def OnLeftDown(self, event):\n    \"\"\"Handle left mouse down, restricting drag to non-border areas.\n\n    Only propagates the event to the parent handler if the hit-test\n    result is a client or title-bar type.\n    \"\"\"\n    part = self.HitTest(*event.GetPosition())\n    if part.type not in (0, 1):\n        super().OnLeftDown(event)\n</code></pre>"},{"location":"api/guiframe_notebook/#pytigon_gui.guiframe.notebook.SchNotebook","title":"SchNotebook","text":"<pre><code>SchNotebook(\n    parent,\n    pos=DefaultPosition,\n    size=DefaultSize,\n    style=BORDER_NONE | TAB_TRAVERSAL | WANTS_CHARS,\n)\n</code></pre> <p>               Bases: <code>AuiNotebook</code></p> <p>Custom AUI notebook for Pytigon applications.</p> <p>Manages tab pages within desktop, panel, header, and footer containers.  Supports page splitting, custom close behaviour, and wiki-based help on double-click of a tab.</p> <p>Construct a SchNotebook.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>pos</code> <p>Window position (default: wx.DefaultPosition).</p> <code>DefaultPosition</code> <code>size</code> <p>Window size (default: wx.DefaultSize).</p> <code>DefaultSize</code> <code>style</code> <p>Window style flags.</p> <code>BORDER_NONE | TAB_TRAVERSAL | WANTS_CHARS</code> Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def __init__(\n    self,\n    parent,\n    pos=wx.DefaultPosition,\n    size=wx.DefaultSize,\n    style=wx.BORDER_NONE | wx.TAB_TRAVERSAL | wx.WANTS_CHARS,\n):\n    \"\"\"Construct a SchNotebook.\n\n    Args:\n        parent: Parent window.\n        pos: Window position (default: wx.DefaultPosition).\n        size: Window size (default: wx.DefaultSize).\n        style: Window style flags.\n    \"\"\"\n    self._curpage = -1\n    self._tab_id_counter = AuiBaseTabCtrlId\n    self._dummy_wnd = None\n    self._hide_tabs = False\n    self._sash_dclick_unsplit = False\n    self._tab_ctrl_height = 20\n    self._requested_bmp_size = wx.Size(-1, -1)\n    self._requested_tabctrl_height = -1\n    self._textCtrl = None\n    self._tabBounds = (-1, -1)\n\n    self.panel = None\n    self.active = False\n    self.closing = False\n    self.last_active = None\n\n    # Deliberately call wx.Panel.__init__ instead of AuiNotebook's\n    # to keep control over the initialization sequence.\n    wx.Panel.__init__(self, parent, wx.ID_ANY, pos, size, style, name=\"SchNotebook\")\n    self._mgr = SChAuiBaseManager()\n\n    self._tabs = aui.AuiTabContainer(self)\n    self.InitNotebook(AUI_NB_DEFAULT_STYLE)\n\n    self.Bind(aui.EVT_AUINOTEBOOK_PAGE_CLOSE, self.on_closing)\n    self.Bind(aui.EVT_AUINOTEBOOK_TAB_DCLICK, self.on_dclick)\n    self.Bind(aui.EVT_AUINOTEBOOK_PAGE_CHANGED, self.on_changed)\n    self.Bind(aui.EVT_AUINOTEBOOK_PAGE_CHANGING, self.on_changing)\n    self.Bind(wx.EVT_NAVIGATION_KEY, self.on_navigate)\n\n    self.SetWindowStyleFlag(wx.WANTS_CHARS)\n    self.SetBackgroundColour(wx.SystemSettings.GetColour(wx.SYS_COLOUR_BACKGROUND))\n</code></pre>"},{"location":"api/guiframe_notebook/#pytigon_gui.guiframe.notebook.SchNotebook-functions","title":"Methods:","text":""},{"location":"api/guiframe_notebook/#pytigon_gui.guiframe.notebook.SchNotebook.DeletePage","title":"DeletePage","text":"<pre><code>DeletePage(sel)\n</code></pre> <p>Delete the page at index sel.</p> <p>When the last page is removed, hides the parent panel and restores focus to a visible pane.</p> Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def DeletePage(self, sel):\n    \"\"\"Delete the page at index *sel*.\n\n    When the last page is removed, hides the parent panel and\n    restores focus to a visible pane.\n    \"\"\"\n    page = self.GetPage(sel)\n    if page == self.last_active:\n        self.last_active = None\n\n    def _close():\n        self.panel.Hide()\n        top = wx.GetApp().GetTopWindow()\n        if top:\n            top._mgr.Update()\n        for pane_name in (\"desktop\", \"panel\", \"menu\", \"header\", \"footer\"):\n            pane_info = top._mgr.GetPane(pane_name) if top else None\n            if pane_info and pane_info.IsOk() and pane_info.IsShown():\n                pane_info.window.SetFocus()\n\n    if len(self._tabs._pages) == 1:\n        wx.CallAfter(_close)\n\n    return aui.AuiNotebook.DeletePage(self, sel)\n</code></pre>"},{"location":"api/guiframe_notebook/#pytigon_gui.guiframe.notebook.SchNotebook.Freeze","title":"Freeze","text":"<pre><code>Freeze()\n</code></pre> <p>Prevent visual updates (no-op for this control).</p> Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def Freeze(self):\n    \"\"\"Prevent visual updates (no-op for this control).\"\"\"\n    pass\n</code></pre>"},{"location":"api/guiframe_notebook/#pytigon_gui.guiframe.notebook.SchNotebook.GetPanel","title":"GetPanel","text":"<pre><code>GetPanel()\n</code></pre> <p>Return the owning panel reference.</p> Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def GetPanel(self):\n    \"\"\"Return the owning panel reference.\"\"\"\n    return self.panel\n</code></pre>"},{"location":"api/guiframe_notebook/#pytigon_gui.guiframe.notebook.SchNotebook.SetPanel","title":"SetPanel","text":"<pre><code>SetPanel(panel)\n</code></pre> <p>Store a reference to the owning panel.</p> <p>Parameters:</p> Name Type Description Default <code>panel</code> <p>The parent panel object.</p> required Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def SetPanel(self, panel):\n    \"\"\"Store a reference to the owning panel.\n\n    Args:\n        panel: The parent panel object.\n    \"\"\"\n    self.panel = panel\n</code></pre>"},{"location":"api/guiframe_notebook/#pytigon_gui.guiframe.notebook.SchNotebook.Thaw","title":"Thaw","text":"<pre><code>Thaw()\n</code></pre> <p>Allow visual updates (no-op for this control).</p> Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def Thaw(self):\n    \"\"\"Allow visual updates (no-op for this control).\"\"\"\n    pass\n</code></pre>"},{"location":"api/guiframe_notebook/#pytigon_gui.guiframe.notebook.SchNotebook.activate_page","title":"activate_page","text":"<pre><code>activate_page(page)\n</code></pre> <p>Activate page and deactivate the previously active one.</p> <p>Parameters:</p> Name Type Description Default <code>page</code> <p>The SchNotebookPage to activate, or None.</p> required Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def activate_page(self, page):\n    \"\"\"Activate *page* and deactivate the previously active one.\n\n    Args:\n        page: The SchNotebookPage to activate, or None.\n    \"\"\"\n    if self.last_active:\n        self.last_active.deactivate_page()\n    self.last_active = page\n    if page:\n        page.activate_page()\n</code></pre>"},{"location":"api/guiframe_notebook/#pytigon_gui.guiframe.notebook.SchNotebook.add_and_split","title":"add_and_split","text":"<pre><code>add_and_split(page, title, direction)\n</code></pre> <p>Add a page and split the notebook window.</p> <p>Parameters:</p> Name Type Description Default <code>page</code> <p>SchNotebookPage to add.</p> required <code>title</code> <p>Caption for the new tab.</p> required <code>direction</code> <p>wx.LEFT, wx.TOP, wx.RIGHT, or wx.BOTTOM.</p> required Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def add_and_split(self, page, title, direction):\n    \"\"\"Add a page and split the notebook window.\n\n    Args:\n        page: SchNotebookPage to add.\n        title: Caption for the new tab.\n        direction: wx.LEFT, wx.TOP, wx.RIGHT, or wx.BOTTOM.\n    \"\"\"\n    self.closing = False\n    if self.GetPageCount() &lt; 1:\n        return\n\n    cli_size = self.GetClientSize()\n    if self.GetPageCount() &gt; 2:\n        split_size = self.CalculateNewSplitSize()\n    else:\n        split_size = self.GetClientSize()\n        split_size.x = int(split_size.x / 2)\n        split_size.y = int(split_size.y / 2)\n\n    new_tabs = aui.TabFrame(self)\n    new_tabs.SetTabCtrlHeight(self._tab_ctrl_height)\n    self._tab_id_counter += 1\n    new_tabs._tabs = aui.AuiTabCtrl(self, self._tab_id_counter)\n    new_tabs._tabs.SetArtProvider(self._tabs.GetArtProvider().Clone())\n    new_tabs._tabs.SetAGWFlags(self._agwFlags)\n    dest_tabs = new_tabs._tabs\n\n    pane_info = (\n        framemanager.AuiPaneInfo()\n        .CaptionVisible(False)\n        .BestSize(split_size.GetWidth(), split_size.GetHeight())\n    )\n\n    if direction == wx.LEFT:\n        pane_info.Left()\n        mouse_pt = wx.Point(0, int(cli_size.y / 2))\n    elif direction == wx.RIGHT:\n        pane_info.Right()\n        mouse_pt = wx.Point(cli_size.x, int(cli_size.y / 2))\n    elif direction == wx.TOP:\n        pane_info.Top()\n        mouse_pt = wx.Point(int(cli_size.x / 2), 0)\n    elif direction == wx.BOTTOM:\n        pane_info.Bottom()\n        mouse_pt = wx.Point(int(cli_size.x / 2), cli_size.y)\n    else:\n        pane_info.Right()\n        mouse_pt = wx.Point(cli_size.x, int(cli_size.y / 2))\n\n    page_info = aui.AuiNotebookPage()\n    page_info.window = page\n    page_info.caption = title\n    page_info.active = False\n    page_info.control = None\n\n    idn = self.GetPageCount()\n    self._tabs.InsertPage(page, page_info, idn)\n    dest_tabs.AddPage(page, page_info)\n    self.SetPageTextColour(idn, wx.Colour(0, 96, 0))\n    page.Reparent(self)\n    self.DoSizing()\n    dest_tabs.Refresh()\n    self.UpdateHintWindowSize()\n\n    self._mgr.AddPane(new_tabs, pane_info, mouse_pt)\n    self._mgr.Update()\n</code></pre>"},{"location":"api/guiframe_notebook/#pytigon_gui.guiframe.notebook.SchNotebook.on_changed","title":"on_changed","text":"<pre><code>on_changed(event)\n</code></pre> <p>Handle page selection change.</p> <p>Refreshes the previously selected and newly selected pages, then activates the new page.</p> Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def on_changed(self, event):\n    \"\"\"Handle page selection change.\n\n    Refreshes the previously selected and newly selected pages,\n    then activates the new page.\n    \"\"\"\n    sel = event.GetSelection()\n    old_sel = event.GetOldSelection()\n    if old_sel &gt;= 0:\n        self.GetPage(old_sel).Refresh()\n    if sel &gt;= 0:\n        self.GetPage(sel).Refresh()\n        self.activate_page(self.GetPage(sel))\n    event.Skip()\n</code></pre>"},{"location":"api/guiframe_notebook/#pytigon_gui.guiframe.notebook.SchNotebook.on_changing","title":"on_changing","text":"<pre><code>on_changing(event)\n</code></pre> <p>Handle page selection about to change.</p> <p>When closing a tab, ensures the selection moves to a sensible neighbouring page.</p> Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def on_changing(self, event):\n    \"\"\"Handle page selection about to change.\n\n    When closing a tab, ensures the selection moves to a sensible\n    neighbouring page.\n    \"\"\"\n    if self.closing:\n        self.closing = False\n        sel = event.GetSelection()\n        oldsel = event.GetOldSelection()\n        if oldsel == -1:\n            new_page = self.GetPage(sel)\n            tab = self.FindTab(new_page)[0]\n            if tab:\n                count = tab.GetPageCount()\n                if count &gt; 0:\n                    new_sel = count - 1\n                    if new_sel &gt; sel:\n                        self.SetSelection(new_sel)\n                        event.Veto()\n                        return\n    event.Skip()\n</code></pre>"},{"location":"api/guiframe_notebook/#pytigon_gui.guiframe.notebook.SchNotebook.on_closing","title":"on_closing","text":"<pre><code>on_closing(event)\n</code></pre> <p>Handle page close request.</p> <p>Vetoes the close if the page's <code>close_no_del</code> method indicates there are still child pages to close first.</p> Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def on_closing(self, event):\n    \"\"\"Handle page close request.\n\n    Vetoes the close if the page's ``close_no_del`` method\n    indicates there are still child pages to close first.\n    \"\"\"\n    idn = event.GetSelection()\n    if idn &gt;= 0:\n        page = self.GetPage(idn)\n        self.closing = True\n        if page.close_no_del(True):\n            self.GetParent().GetParent().after_close(self)\n            event.Veto()\n</code></pre>"},{"location":"api/guiframe_notebook/#pytigon_gui.guiframe.notebook.SchNotebook.on_dclick","title":"on_dclick","text":"<pre><code>on_dclick(event)\n</code></pre> <p>Handle tab double-click.</p> <p>Opens the associated wiki help page in a new split pane.</p> Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def on_dclick(self, event):\n    \"\"\"Handle tab double-click.\n\n    Opens the associated wiki help page in a new split pane.\n    \"\"\"\n    try:\n        txt = self.GetPageText(event.GetSelection())\n        mp, adr = wx.GetApp().read_html(\n            self, \"/schwiki/help/\" + wiki_from_str(txt) + \"/view/\", None\n        )\n        if not txt.startswith(\"?\"):\n            wiki = wiki_from_str(txt)\n            wx.GetApp().GetTopWindow().new_main_page(\n                mp, \"?: \" + wiki, panel=\"desktop2\"\n            )\n    except Exception:\n        logger.debug(\"Failed to open wiki page for: %s\", txt, exc_info=True)\n</code></pre>"},{"location":"api/guiframe_notebook/#pytigon_gui.guiframe.notebook.SchNotebook.on_navigate","title":"on_navigate","text":"<pre><code>on_navigate(evt)\n</code></pre> <p>Handle navigation key events.</p> <p>Activates the current page and passes the event on.</p> Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def on_navigate(self, evt):\n    \"\"\"Handle navigation key events.\n\n    Activates the current page and passes the event on.\n    \"\"\"\n    forward = evt.GetDirection()\n    self.activate_page(self.GetCurrentPage())\n    evt.Skip()\n</code></pre>"},{"location":"api/guiframe_notebook/#pytigon_gui.guiframe.notebook.SchNotebook.set_active","title":"set_active","text":"<pre><code>set_active(active)\n</code></pre> <p>Set the active state and refresh the current page.</p> <p>Parameters:</p> Name Type Description Default <code>active</code> <p>True to activate, False to deactivate.</p> required Source code in <code>pytigon_gui/guiframe/notebook.py</code> <pre><code>def set_active(self, active):\n    \"\"\"Set the active state and refresh the current page.\n\n    Args:\n        active: True to activate, False to deactivate.\n    \"\"\"\n    self.active = active\n    if active:\n        w = self.GetCurrentPage()\n        self.activate_page(w)\n        self.GetParent().GetParent().SetActive(self, w)\n</code></pre>"},{"location":"api/guiframe_notebookpage/","title":"guiframe.notebookpage \u2013 Notebook Page","text":"<p>Individual notebook page component with integrated toolbar, form rendering, and Django communication.</p>"},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage","title":"notebookpage","text":"<p>Notebook page container for Pytigon applications.</p> <p>SchNotebookPage represents one tab within a SchNotebook. It manages multiple SchPage windows laid out inside a single tab using a custom split/stack algorithm and supports mouse-driven divider resizing.</p>"},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage-classes","title":"Classes","text":""},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage.SchNotebookPage","title":"SchNotebookPage","text":"<pre><code>SchNotebookPage(parent)\n</code></pre> <p>               Bases: <code>Window</code></p> <p>One tab in a SchNotebook, hosting one or more SchPage windows.</p> <p>Initialize the notebook page.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent SchNotebook window.</p> required Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def __init__(self, parent):\n    \"\"\"Initialize the notebook page.\n\n    Args:\n        parent: Parent SchNotebook window.\n    \"\"\"\n    wx.Window.__init__(\n        self,\n        parent,\n        style=wx.TAB_TRAVERSAL | wx.WANTS_CHARS,\n        name=\"SchNotebookPage\",\n    )\n    try:\n        self.SetBackgroundStyle(wx.BG_STYLE_ERASE)\n    except Exception:\n        self.SetBackgroundStyle(wx.BG_STYLE_CUSTOM)\n\n    self._start_pos = None\n    self._start_pos_x_y_dx_dy = None\n    self._last_x_y_dx_dy = None\n    self._best_x_y_dx_dy = None\n    self._layout_style = 0\n\n    self.child_panels = []\n    self.bestx = -1\n    self.orient = None\n    self.http = None\n    self.reverse_style = True\n\n    self.Bind(wx.EVT_SIZE, self.on_size)\n    self.Bind(wx.EVT_ERASE_BACKGROUND, self.on_erase_background)\n    self.Bind(wx.EVT_LEFT_DOWN, self.on_left_down)\n    self.Bind(wx.EVT_LEFT_UP, self.on_left_up)\n    self.Bind(wx.EVT_MOTION, self.on_move)\n    self.Bind(wx.EVT_SET_FOCUS, self.on_set_focus)\n\n    self.SetWindowStyleFlag(wx.WANTS_CHARS)\n</code></pre>"},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage.SchNotebookPage-functions","title":"Methods:","text":""},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage.SchNotebookPage.activate_page","title":"activate_page","text":"<pre><code>activate_page()\n</code></pre> <p>Activate the last child page.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def activate_page(self):\n    \"\"\"Activate the last child page.\"\"\"\n    if self.get_page_count() &gt; 0:\n        self.get_page(-1).activate_page()\n    else:\n        self.SetFocus()\n</code></pre>"},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage.SchNotebookPage.add_page","title":"add_page","text":"<pre><code>add_page(page)\n</code></pre> <p>Append a child SchPage to this notebook page.</p> <p>If the new page has <code>disable_parent</code> set and there are existing pages, the last existing page's forms are disabled.</p> <p>Parameters:</p> Name Type Description Default <code>page</code> <p>The SchPage to add.</p> required Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def add_page(self, page):\n    \"\"\"Append a child SchPage to this notebook page.\n\n    If the new page has ``disable_parent`` set and there are\n    existing pages, the last existing page's forms are disabled.\n\n    Args:\n        page: The SchPage to add.\n    \"\"\"\n    if self.get_page_count() &gt; 0:\n        if page.disable_parent:\n            self.get_page(-1).enable_forms(False)\n    self.child_panels.append(page)\n    self._layout()\n</code></pre>"},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage.SchNotebookPage.close_child_page","title":"close_child_page","text":"<pre><code>close_child_page(close_without_refresh=True)\n</code></pre> <p>Close the last child page; remove this tab if it was the only page.</p> <p>Parameters:</p> Name Type Description Default <code>close_without_refresh</code> <p>Forwarded to :meth:<code>close_no_del</code>.</p> <code>True</code> <p>Returns:</p> Type Description <p>Result of :meth:<code>close_no_del</code>.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def close_child_page(self, close_without_refresh=True):\n    \"\"\"Close the last child page; remove this tab if it was the only page.\n\n    Args:\n        close_without_refresh: Forwarded to :meth:`close_no_del`.\n\n    Returns:\n        Result of :meth:`close_no_del`.\n    \"\"\"\n    parent = self.GetParent()\n    pages = parent._tabs._pages\n    for i, page_info in enumerate(pages):\n        if page_info.window == self:\n            ret = self.close_no_del(close_without_refresh)\n            if not ret:\n                parent.DeletePage(i)\n            return ret\n    return False\n</code></pre>"},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage.SchNotebookPage.close_no_del","title":"close_no_del","text":"<pre><code>close_no_del(close_without_refresh=True)\n</code></pre> <p>Close the last child page without destroying this container.</p> <p>Parameters:</p> Name Type Description Default <code>close_without_refresh</code> <p>If True, signal 'child_canceled'; otherwise signal 'child_closed_with_ok'.</p> <code>True</code> <p>Returns:</p> Type Description <p>True if the close was handled (more pages remain or vetoed),</p> <p>False if this container itself should be closed.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def close_no_del(self, close_without_refresh=True):\n    \"\"\"Close the last child page without destroying this container.\n\n    Args:\n        close_without_refresh: If True, signal 'child_canceled';\n            otherwise signal 'child_closed_with_ok'.\n\n    Returns:\n        True if the close was handled (more pages remain or vetoed),\n        False if this container itself should be closed.\n    \"\"\"\n    count = self.get_page_count()\n    if count == 0:\n        return False\n\n    win = self.get_page(-1)\n    if not win.CanClose():\n        return True\n\n    del self.child_panels[-1]\n    if count &gt; 1:\n        self.get_page(-1).enable_forms(True)\n        self._layout()\n        self.get_page(-1).set_focus()\n        if close_without_refresh:\n            self.get_page(-1).signal(\"child_canceled\", win)\n        else:\n            self.get_page(-1).signal(\"child_closed_with_ok\", win)\n        win.Destroy()\n        return True\n\n    win.Destroy()\n    return False\n</code></pre>"},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage.SchNotebookPage.deactivate_page","title":"deactivate_page","text":"<pre><code>deactivate_page()\n</code></pre> <p>Deactivate the last child page.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def deactivate_page(self):\n    \"\"\"Deactivate the last child page.\"\"\"\n    if self.get_page_count() &gt; 0:\n        self.get_page(-1).deactivate_page()\n</code></pre>"},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage.SchNotebookPage.get_app_http","title":"get_app_http","text":"<pre><code>get_app_http()\n</code></pre> <p>Return the HTTP client associated with this notebook page.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def get_app_http(self):\n    \"\"\"Return the HTTP client associated with this notebook page.\"\"\"\n    return self.http\n</code></pre>"},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage.SchNotebookPage.get_margins","title":"get_margins","text":"<pre><code>get_margins()\n</code></pre> <p>Return the margin between child windows, in pixels.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def get_margins(self):\n    \"\"\"Return the margin between child windows, in pixels.\"\"\"\n    return 2\n</code></pre>"},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage.SchNotebookPage.get_page","title":"get_page","text":"<pre><code>get_page(nr)\n</code></pre> <p>Return a child SchPage by index.</p> <p>Parameters:</p> Name Type Description Default <code>nr</code> <p>Index of the page (supports negative indexing).</p> required <p>Returns:</p> Type Description <p>SchPage instance.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def get_page(self, nr):\n    \"\"\"Return a child SchPage by index.\n\n    Args:\n        nr: Index of the page (supports negative indexing).\n\n    Returns:\n        SchPage instance.\n    \"\"\"\n    return self.child_panels[nr]\n</code></pre>"},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage.SchNotebookPage.get_page_count","title":"get_page_count","text":"<pre><code>get_page_count()\n</code></pre> <p>Return number of child SchPage windows.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def get_page_count(self):\n    \"\"\"Return number of child SchPage windows.\"\"\"\n    return len(self.child_panels)\n</code></pre>"},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage.SchNotebookPage.get_xy","title":"get_xy","text":"<pre><code>get_xy(size=None, page=-1)\n</code></pre> <p>Calculate the divider position for two child pages.</p> <p>Parameters:</p> Name Type Description Default <code>size</code> <p>Size of the container (uses self.GetSize() if None).</p> <code>None</code> <code>page</code> <p>Index of the page to measure (default: last page).</p> <code>-1</code> <p>Returns:</p> Type Description <p>Tuple (x, y, width, height) for the divider position.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def get_xy(self, size=None, page=-1):\n    \"\"\"Calculate the divider position for two child pages.\n\n    Args:\n        size: Size of the container (uses self.GetSize() if None).\n        page: Index of the page to measure (default: last page).\n\n    Returns:\n        Tuple (x, y, width, height) for the divider position.\n    \"\"\"\n    if size is None:\n        size = self.GetSize()\n    if self._best_x_y_dx_dy:\n        return self._best_x_y_dx_dy\n\n    bestx, besty = self.get_page(page).calculate_best_size()\n    self.bestx = bestx\n\n    if bestx &gt; size.GetWidth() - 20:\n        x = size.GetWidth() / 10\n    else:\n        x = (size.GetWidth() - 2) - bestx\n    if besty &gt; size.GetHeight() - 20:\n        y = size.GetHeight() / 10\n    else:\n        y = (size.GetHeight() - 2) - besty\n\n    self._last_x_y_dx_dy = (x, y, size.GetWidth(), size.GetHeight())\n    return self._last_x_y_dx_dy\n</code></pre>"},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage.SchNotebookPage.new_child_page","title":"new_child_page","text":"<pre><code>new_child_page(address_or_parser, title='', parameters=None, callback=None)\n</code></pre> <p>Append a new SchPage as a child of this notebook page.</p> <p>Parameters:</p> Name Type Description Default <code>address_or_parser</code> <p>HTTP address (str) or ShtmlParser instance.</p> required <code>title</code> <p>Caption for the new page.</p> <code>''</code> <code>parameters</code> <p>Dictionary of request parameters.</p> <code>None</code> <code>callback</code> <p>Optional callback invoked after init.</p> <code>None</code> <p>Returns:</p> Type Description <p>The created SchPage.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def new_child_page(\n    self, address_or_parser, title=\"\", parameters=None, callback=None\n):\n    \"\"\"Append a new SchPage as a child of this notebook page.\n\n    Args:\n        address_or_parser: HTTP address (str) or ShtmlParser instance.\n        title: Caption for the new page.\n        parameters: Dictionary of request parameters.\n        callback: Optional callback invoked after init.\n\n    Returns:\n        The created SchPage.\n    \"\"\"\n    h = page.SchPage(self, address_or_parser, parameters)\n    if self.get_page_count() &gt; 0:\n        h.parent_page = self.get_page(-1)\n\n    nr = 0\n    if (header := h.header) is not None:\n        nr += 4\n    if (footer := h.footer) is not None:\n        nr += 2\n    if (panel := h.panel) is not None:\n        nr += 1\n\n    title2 = title if title else h.get_title()\n    self.add_page(h)\n\n    def init_page():\n        nonlocal h, callback\n        h.init_frame(callback)\n        h.activate_page()\n        top = wx.GetApp().GetTopWindow()\n        if top:\n            top._mgr.GetPane(\"desktop\").Show()\n        h.Update()\n\n    wx.CallAfter(init_page)\n    return h\n</code></pre>"},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage.SchNotebookPage.new_main_page","title":"new_main_page","text":"<pre><code>new_main_page(address_or_parser, title='', parameters=None, view_in=None)\n</code></pre> <p>Create a new top-level page in a specific pane.</p> <p>Delegates to the application frame's <code>new_main_page</code>.</p> <p>Parameters:</p> Name Type Description Default <code>address_or_parser</code> <p>HTTP address (str) or ShtmlParser instance.</p> required <code>title</code> <p>Caption for the new page.</p> <code>''</code> <code>parameters</code> <p>Dictionary of request parameters.</p> <code>None</code> <code>view_in</code> <p>Pane name ('desktop', 'panel', 'header', 'footer').</p> <code>None</code> <p>Returns:</p> Type Description <p>Result of the application frame's new_main_page.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def new_main_page(self, address_or_parser, title=\"\", parameters=None, view_in=None):\n    \"\"\"Create a new top-level page in a specific pane.\n\n    Delegates to the application frame's ``new_main_page``.\n\n    Args:\n        address_or_parser: HTTP address (str) or ShtmlParser instance.\n        title: Caption for the new page.\n        parameters: Dictionary of request parameters.\n        view_in: Pane name ('desktop', 'panel', 'header', 'footer').\n\n    Returns:\n        Result of the application frame's new_main_page.\n    \"\"\"\n    top = wx.GetApp().GetTopWindow()\n    if view_in is None:\n        pp = top._mgr.GetPane(self.GetParent())\n        return top.new_main_page(address_or_parser, title, parameters, pp.name)\n    return top.new_main_page(address_or_parser, title, parameters, view_in)\n</code></pre>"},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage.SchNotebookPage.on_child_form_cancel","title":"on_child_form_cancel","text":"<pre><code>on_child_form_cancel()\n</code></pre> <p>Called by child SchForm when Cancel is pushed.</p> <p>Closes the child without refreshing the parent.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def on_child_form_cancel(self):\n    \"\"\"Called by child SchForm when Cancel is pushed.\n\n    Closes the child without refreshing the parent.\n    \"\"\"\n    self.close_child_page(True)\n</code></pre>"},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage.SchNotebookPage.on_child_form_ok","title":"on_child_form_ok","text":"<pre><code>on_child_form_ok()\n</code></pre> <p>Called by child SchForm when OK is pushed.</p> <p>Closes the child and refreshes the parent form.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def on_child_form_ok(self):\n    \"\"\"Called by child SchForm when OK is pushed.\n\n    Closes the child and refreshes the parent form.\n    \"\"\"\n    self.close_child_page(False)\n</code></pre>"},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage.SchNotebookPage.on_erase_background","title":"on_erase_background","text":"<pre><code>on_erase_background(evt)\n</code></pre> <p>Draw borders between child pages and resize handles.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def on_erase_background(self, evt):\n    \"\"\"Draw borders between child pages and resize handles.\"\"\"\n    top = wx.GetApp().GetTopWindow()\n    if not top:\n        return\n\n    dc = wx.ClientDC(self)\n    dc.Clear()\n    margin = self.get_margins()\n\n    has_desktop = hasattr(top, \"desktop\")\n    tabs_count = len(top.desktop._mgr.GetAllPanes()) if has_desktop else 0\n    page_count = self.get_page_count()\n\n    show_border = (\n        (page_count &gt; 0 and tabs_count &gt; 2)\n        or page_count &gt; 1\n        or (top.count_shown_panels(count_toolbars=False) &gt; 1 and page_count &gt; 0)\n    )\n\n    if not show_border:\n        # Nothing to draw.\n        return\n\n    last_page = self.get_page(-1)\n    (dx, dy) = last_page.GetSize()\n    (x, y) = last_page.GetPosition()\n    x -= margin / 2\n    y -= margin / 2\n    dx += margin\n    dy += margin\n\n    parent = self.GetParent()\n    if parent.active and parent.GetCurrentPage() == self:\n        col = wx.SystemSettings.GetColour(wx.SYS_COLOUR_HIGHLIGHT)\n    else:\n        col = wx.SystemSettings.GetColour(wx.SYS_COLOUR_INACTIVEBORDER)\n    dc.SetPen(wx.Pen(col, margin))\n\n    # Border rectangle\n    dc.DrawLine(int(x), int(y), int(x + dx), int(y))\n    dc.DrawLine(int(x + dx), int(y), int(x + dx), int(y + dy))\n    dc.DrawLine(int(x), int(y - margin / 2), int(x), int(y + dy))\n    dc.DrawLine(int(x), int(y + dy), int(x + dx), int(y + dy))\n\n    # Resize handle lines\n    if self._layout_style &gt; 0:\n        col = wx.SystemSettings.GetColour(wx.SYS_COLOUR_3DSHADOW)\n        dc.SetPen(wx.Pen(col, 1))\n        self._draw_resize_handles(dc, x, y, dx, dy, margin)\n</code></pre>"},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage.SchNotebookPage.on_left_down","title":"on_left_down","text":"<pre><code>on_left_down(event)\n</code></pre> <p>Begin splitter drag.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def on_left_down(self, event):\n    \"\"\"Begin splitter drag.\"\"\"\n    self._start_pos = event.GetPosition()\n    self._start_pos_x_y_dx_dy = self._last_x_y_dx_dy\n    self.SetCursor(wx.Cursor(wx.CURSOR_SIZING))\n    self.CaptureMouse()\n    event.Skip()\n</code></pre>"},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage.SchNotebookPage.on_left_up","title":"on_left_up","text":"<pre><code>on_left_up(event)\n</code></pre> <p>End splitter drag.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def on_left_up(self, event):\n    \"\"\"End splitter drag.\"\"\"\n    if self._start_pos:\n        self._start_pos = None\n        self._start_pos_x_y_dx_dy = None\n        self._best_x_y_dx_dy = None\n        self.ReleaseMouse()\n        self.SetCursor(wx.Cursor(wx.CURSOR_ARROW))\n    event.Skip()\n</code></pre>"},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage.SchNotebookPage.on_move","title":"on_move","text":"<pre><code>on_move(event)\n</code></pre> <p>Handle mouse movement during splitter drag.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def on_move(self, event):\n    \"\"\"Handle mouse movement during splitter drag.\"\"\"\n    if self._start_pos and self._start_pos_x_y_dx_dy:\n        pos = event.GetPosition()\n        dx = pos[0] - self._start_pos[0]\n        dy = pos[1] - self._start_pos[1]\n        if self.reverse_style:\n            dy = -dy\n        self._best_x_y_dx_dy = (\n            self._start_pos_x_y_dx_dy[0] + dx,\n            self._start_pos_x_y_dx_dy[1] + dy,\n            self._start_pos_x_y_dx_dy[2],\n            self._start_pos_x_y_dx_dy[3],\n        )\n        self._layout()\n    event.Skip()\n</code></pre>"},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage.SchNotebookPage.on_set_focus","title":"on_set_focus","text":"<pre><code>on_set_focus(evt)\n</code></pre> <p>Delegate focus to the last child page.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def on_set_focus(self, evt):\n    \"\"\"Delegate focus to the last child page.\"\"\"\n    if self.get_page_count() &gt; 0:\n        self.get_page(-1).SetFocus()\n</code></pre>"},{"location":"api/guiframe_notebookpage/#pytigon_gui.guiframe.notebookpage.SchNotebookPage.on_size","title":"on_size","text":"<pre><code>on_size(event)\n</code></pre> <p>Re-layout children when the container is resized.</p> Source code in <code>pytigon_gui/guiframe/notebookpage.py</code> <pre><code>def on_size(self, event):\n    \"\"\"Re-layout children when the container is resized.\"\"\"\n    size = event.GetSize()\n    self.bestx = -1\n    if size:\n        wx.CallAfter(self._layout, size)\n    event.Skip()\n</code></pre>"},{"location":"api/guiframe_page/","title":"guiframe.page \u2013 Page Component","text":"<p>Base page component \u2013 the fundamental building block for all notebook pages, providing layout, data binding, and event handling.</p>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page","title":"page","text":"<p>SchPage is a container for four SchForm sub-windows: body, header, footer and panel.  Only the 'body' form is mandatory; the others are created on demand from the HTML response.</p>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page-attributes","title":"Attributes","text":""},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_BOOKMARK_ADD","title":"ID_BOOKMARK_ADD  <code>module-attribute</code>","text":"<pre><code>ID_BOOKMARK_ADD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_CLEAR_HISTORY","title":"ID_CLEAR_HISTORY  <code>module-attribute</code>","text":"<pre><code>ID_CLEAR_HISTORY = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_CLOSETAB","title":"ID_CLOSETAB  <code>module-attribute</code>","text":"<pre><code>ID_CLOSETAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_COPY","title":"ID_COPY  <code>module-attribute</code>","text":"<pre><code>ID_COPY = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_CUT","title":"ID_CUT  <code>module-attribute</code>","text":"<pre><code>ID_CUT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_DOWNLOAD_OPTIONS","title":"ID_DOWNLOAD_OPTIONS  <code>module-attribute</code>","text":"<pre><code>ID_DOWNLOAD_OPTIONS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_END","title":"ID_END  <code>module-attribute</code>","text":"<pre><code>ID_END = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_EXIT","title":"ID_EXIT  <code>module-attribute</code>","text":"<pre><code>ID_EXIT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_FIND","title":"ID_FIND  <code>module-attribute</code>","text":"<pre><code>ID_FIND = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_GENERAL_OPTIONS","title":"ID_GENERAL_OPTIONS  <code>module-attribute</code>","text":"<pre><code>ID_GENERAL_OPTIONS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_GOTODESKTOP","title":"ID_GOTODESKTOP  <code>module-attribute</code>","text":"<pre><code>ID_GOTODESKTOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_GOTOFOOTER","title":"ID_GOTOFOOTER  <code>module-attribute</code>","text":"<pre><code>ID_GOTOFOOTER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_GOTOHEADER","title":"ID_GOTOHEADER  <code>module-attribute</code>","text":"<pre><code>ID_GOTOHEADER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_GOTOPANEL","title":"ID_GOTOPANEL  <code>module-attribute</code>","text":"<pre><code>ID_GOTOPANEL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_HELP","title":"ID_HELP  <code>module-attribute</code>","text":"<pre><code>ID_HELP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_HISTORY_SHOW","title":"ID_HISTORY_SHOW  <code>module-attribute</code>","text":"<pre><code>ID_HISTORY_SHOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_LOAD","title":"ID_LOAD  <code>module-attribute</code>","text":"<pre><code>ID_LOAD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_NEXTPAGE","title":"ID_NEXTPAGE  <code>module-attribute</code>","text":"<pre><code>ID_NEXTPAGE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_NEXTTAB","title":"ID_NEXTTAB  <code>module-attribute</code>","text":"<pre><code>ID_NEXTTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_PAGE_BLOCK","title":"ID_PAGE_BLOCK  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_BLOCK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_PAGE_SOURCE","title":"ID_PAGE_SOURCE  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_SOURCE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_PAGE_VIEW","title":"ID_PAGE_VIEW  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_VIEW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_PASTE","title":"ID_PASTE  <code>module-attribute</code>","text":"<pre><code>ID_PASTE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_PREVPAGE","title":"ID_PREVPAGE  <code>module-attribute</code>","text":"<pre><code>ID_PREVPAGE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_PREVTAB","title":"ID_PREVTAB  <code>module-attribute</code>","text":"<pre><code>ID_PREVTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_PRINT","title":"ID_PRINT  <code>module-attribute</code>","text":"<pre><code>ID_PRINT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_PRINT_PREVIEW","title":"ID_PRINT_PREVIEW  <code>module-attribute</code>","text":"<pre><code>ID_PRINT_PREVIEW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_PRINT_SETTINGS","title":"ID_PRINT_SETTINGS  <code>module-attribute</code>","text":"<pre><code>ID_PRINT_SETTINGS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_REDO","title":"ID_REDO  <code>module-attribute</code>","text":"<pre><code>ID_REDO = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_REFRESHTAB","title":"ID_REFRESHTAB  <code>module-attribute</code>","text":"<pre><code>ID_REFRESHTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_REPLACE","title":"ID_REPLACE  <code>module-attribute</code>","text":"<pre><code>ID_REPLACE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_RESET","title":"ID_RESET  <code>module-attribute</code>","text":"<pre><code>ID_RESET = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_RETVALUE","title":"ID_RETVALUE  <code>module-attribute</code>","text":"<pre><code>ID_RETVALUE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_SAVE","title":"ID_SAVE  <code>module-attribute</code>","text":"<pre><code>ID_SAVE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_SAVE_AS","title":"ID_SAVE_AS  <code>module-attribute</code>","text":"<pre><code>ID_SAVE_AS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_SELALL","title":"ID_SELALL  <code>module-attribute</code>","text":"<pre><code>ID_SELALL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_SHOWDESKTOP","title":"ID_SHOWDESKTOP  <code>module-attribute</code>","text":"<pre><code>ID_SHOWDESKTOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_SHOWFOOTER","title":"ID_SHOWFOOTER  <code>module-attribute</code>","text":"<pre><code>ID_SHOWFOOTER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_SHOWHEADER","title":"ID_SHOWHEADER  <code>module-attribute</code>","text":"<pre><code>ID_SHOWHEADER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_SHOWPANEL","title":"ID_SHOWPANEL  <code>module-attribute</code>","text":"<pre><code>ID_SHOWPANEL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_SHOWSTATUSBAR","title":"ID_SHOWSTATUSBAR  <code>module-attribute</code>","text":"<pre><code>ID_SHOWSTATUSBAR = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_SHOWTOOLBAR1","title":"ID_SHOWTOOLBAR1  <code>module-attribute</code>","text":"<pre><code>ID_SHOWTOOLBAR1 = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_SHOWTOOLBAR2","title":"ID_SHOWTOOLBAR2  <code>module-attribute</code>","text":"<pre><code>ID_SHOWTOOLBAR2 = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_SHOW_BOOKMARKS","title":"ID_SHOW_BOOKMARKS  <code>module-attribute</code>","text":"<pre><code>ID_SHOW_BOOKMARKS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_SHOW_DOWNLOAD","title":"ID_SHOW_DOWNLOAD  <code>module-attribute</code>","text":"<pre><code>ID_SHOW_DOWNLOAD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_START","title":"ID_START  <code>module-attribute</code>","text":"<pre><code>ID_START = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_TASKBAR_CLOSE","title":"ID_TASKBAR_CLOSE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_CLOSE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_TASKBAR_HIDE","title":"ID_TASKBAR_HIDE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_HIDE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_TASKBAR_SHOW","title":"ID_TASKBAR_SHOW  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_SHOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_TASKBAR_TOGGLE","title":"ID_TASKBAR_TOGGLE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_TOGGLE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_UNDO","title":"ID_UNDO  <code>module-attribute</code>","text":"<pre><code>ID_UNDO = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_WEB_ADDBOOKMARK","title":"ID_WEB_ADDBOOKMARK  <code>module-attribute</code>","text":"<pre><code>ID_WEB_ADDBOOKMARK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_WEB_BACK","title":"ID_WEB_BACK  <code>module-attribute</code>","text":"<pre><code>ID_WEB_BACK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_WEB_DOWN","title":"ID_WEB_DOWN  <code>module-attribute</code>","text":"<pre><code>ID_WEB_DOWN = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_WEB_EDIT","title":"ID_WEB_EDIT  <code>module-attribute</code>","text":"<pre><code>ID_WEB_EDIT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_WEB_FORWARD","title":"ID_WEB_FORWARD  <code>module-attribute</code>","text":"<pre><code>ID_WEB_FORWARD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_WEB_NEW_WINDOW","title":"ID_WEB_NEW_WINDOW  <code>module-attribute</code>","text":"<pre><code>ID_WEB_NEW_WINDOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_WEB_REFRESH","title":"ID_WEB_REFRESH  <code>module-attribute</code>","text":"<pre><code>ID_WEB_REFRESH = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_WEB_SOURCE","title":"ID_WEB_SOURCE  <code>module-attribute</code>","text":"<pre><code>ID_WEB_SOURCE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_WEB_STOP","title":"ID_WEB_STOP  <code>module-attribute</code>","text":"<pre><code>ID_WEB_STOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_WEB_UP","title":"ID_WEB_UP  <code>module-attribute</code>","text":"<pre><code>ID_WEB_UP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.ID_ZOOM","title":"ID_ZOOM  <code>module-attribute</code>","text":"<pre><code>ID_ZOOM = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.logger","title":"logger  <code>module-attribute</code>","text":"<pre><code>logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page-classes","title":"Classes","text":""},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm","title":"SchForm","text":"<pre><code>SchForm(parent, page=None, hscroll=False, vscroll=False, form_type='body')\n</code></pre> <p>               Bases: <code>ScrolledPanel</code></p> <p>Html window</p> <p>Constructor</p> <p>Parameters:</p> Name Type Description Default <code>form_type - can be</code> <p>body, header, footer, panel</p> required Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def __init__(\n    self, parent, page=None, hscroll=False, vscroll=False, form_type=\"body\"\n):\n    \"\"\"Constructor\n\n    Args:\n        parent - parent window\n        page - parent SchPage object\n        hscroll - enable horizontal scroll bar\n        vscroll - enable vertical scroll  bar\n        form_type - can be: body, header, footer, panel\n    \"\"\"\n    self._bestsize = None\n    self._scroll_xy = (0, 0)\n    self._act_scroll_xy = (0, 0)\n    self._enabled_controls = False\n    self._last_size = (-1, -1)\n    self._best_virtual_size = None\n    self._block = False\n    self._dc_buf = None\n    self._dc_buf_x = 0\n    self._dc_buf_y = 0\n    self._lock = False\n\n    self.no_vscrollbar = not vscroll\n    self.no_hscrollbar = not hscroll\n    self.address = None\n    self.script = None\n    self.parameters = None\n    self.page_source = \"&lt;html&gt;&lt;/html&gt;\"\n    self.bestsize = None\n    self.hover_obj = None\n    self.cursor_type = 0\n    self.wxdc = None\n    self.after_init = False\n    self.update_controls = False\n    self.obj_action_dict = {}\n    self.obj_id_dict = {}\n    self.last_control_with_focus = None\n    self.page = page\n    self.form_type = form_type\n    self.init_css_str = None\n    self.evt_ind = -1\n    self.closing = False\n    self.acc_tabs = {}\n    self.acc_tab = None\n    self.acc_tab_bind = False\n    self.last_clicked = None\n    self.t1 = None\n    self.websockets = []\n\n    self.hscroll = hscroll\n    self.vscroll = vscroll\n\n    ScrolledPanel.__init__(self, parent, -1, style=wx.WANTS_CHARS)\n    try:\n        self.SetBackgroundStyle(wx.BG_STYLE_ERASE)\n    except Exception:\n        self.SetBackgroundStyle(wx.BG_STYLE_CUSTOM)\n\n    self.SetupScrolling(hscroll, vscroll, rate_y=1)\n\n    self.SetAcceleratorTable(wx.AcceleratorTable(wx.GetApp().GetTopWindow().aTable))\n\n    self.Bind(wx.EVT_SIZE, self.on_size)\n    self.Bind(wx.EVT_LEFT_DOWN, self.on_left_down)\n    self.Bind(wx.EVT_LEFT_UP, self.on_left_up)\n    self.Bind(wx.EVT_RIGHT_UP, self.on_right_up)\n    self.Bind(wx.EVT_MOTION, self.on_motion)\n    self.Bind(wx.EVT_ERASE_BACKGROUND, self.on_erase_background)\n    self.Bind(wx.EVT_SET_FOCUS, self.on_focus)\n\n    self.EnableScrolling(False, False)\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm-functions","title":"Methods:","text":""},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.CanClose","title":"CanClose","text":"<pre><code>CanClose()\n</code></pre> <p>Test if form can be closed</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def CanClose(self):\n    \"\"\"Test if form can be closed\"\"\"\n    ret = True\n    if hasattr(self, \"can_close\"):\n        ret = self.can_close()\n    else:\n        widgets = list(self.GetParent().get_widgets().values())\n        for w in widgets:\n            if hasattr(w, \"CanClose\"):\n                if not w.CanClose():\n                    ret = False\n    return ret\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.any_parent_command","title":"any_parent_command","text":"<pre><code>any_parent_command(command, *args, **kwds)\n</code></pre> <p>Walk up the parent hierarchy to find and call command.</p> <p>Parameters:</p> Name Type Description Default <code>command</code> <p>Method name to search for.</p> required <code>args, kwds</code> <p>Forwarded to the found method.</p> required <p>Returns:</p> Type Description <p>Return value of the method, or None if not found.</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def any_parent_command(self, command, *args, **kwds):\n    \"\"\"Walk up the parent hierarchy to find and call *command*.\n\n    Args:\n        command: Method name to search for.\n        args, kwds: Forwarded to the found method.\n\n    Returns:\n        Return value of the method, or None if not found.\n    \"\"\"\n    parent = self.GetParent()\n    while parent is not None:\n        if hasattr(parent, command):\n            return getattr(parent, command)(*args, **kwds)\n        parent = parent.GetParent()\n    logger.warning(\"METHOD NOT FOUND: %s\", command)\n    return None\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.bind_to_ctrl","title":"bind_to_ctrl","text":"<pre><code>bind_to_ctrl(ctrl, id, fun, fun2=None)\n</code></pre> <p>bind function callback to command event from ctrl</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def bind_to_ctrl(self, ctrl, id, fun, fun2=None):\n    \"\"\"bind function callback to command event from ctrl\n\n    Args:\n        ctrl - widget\n        id - command id\n        fun - callback function command event\n        fun2 - callback function for EVT_UPDATE_UI event\n\n    \"\"\"\n    if fun2:\n        ctrl.Bind(wx.EVT_UPDATE_UI, fun2, id=id)\n    ctrl.Bind(RB.EVT_RIBBONBUTTONBAR_CLICKED, fun, id=id)\n    ctrl.Bind(RB.EVT_RIBBONTOOLBAR_CLICKED, fun, id=id)\n    ctrl.Bind(wx.EVT_MENU, fun, id=id)\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.cancel","title":"cancel","text":"<pre><code>cancel()\n</code></pre> <p>Close this form without saving its content</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def cancel(self):\n    \"\"\"Close this form without saving its content\"\"\"\n    if self.page:\n        self.page.exists = False\n    if self.t1:\n        self.t1.Stop()\n    if self.page:\n        self.page.GetParent().on_child_form_cancel()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.draw_background","title":"draw_background","text":"<pre><code>draw_background(refresh_all=False, size=None)\n</code></pre> <p>drawn a rendered html page as a background</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def draw_background(self, refresh_all=False, size=None):\n    \"\"\"drawn a rendered html page as a background\"\"\"\n    if self._block:\n        return\n    self._block = True\n    if not self.wxdc:\n        self.Scroll(0, 0)\n    (xv, yv) = self.GetViewStart()\n    (dx, dy) = self.GetScrollPixelsPerUnit()\n    (x, y) = (xv * dx, yv * dy)\n    self._act_scroll_xy = (x, y)\n    dc = wx.ClientDC(self)\n    if self.wxdc and self._dc_buf and self._dc_buf_x == x and self._dc_buf_y == y:\n        dc.SetDeviceOrigin(-1 * x, -1 * y)\n        rect = self.GetRect()\n        dc.Blit(x, y, rect.GetWidth(), rect.GetHeight(), self._dc_buf, x, y)\n    else:\n        rect = self.GetRect()\n        if rect.GetWidth() &lt; 1 or rect.GetHeight() &lt; 1:\n            rect = wx.Rect(0, 0, 1, 1)  # width=1, height=1)\n        dc.SetDeviceOrigin(-1 * x, -1 * y)\n        bitmap = wx.Bitmap(rect.GetWidth(), rect.GetHeight())\n        dc2 = wx.MemoryDC(bitmap)\n        dc2.SetBackground(\n            wx.Brush(wx.SystemSettings.GetColour(wx.SYS_COLOUR_3DFACE))\n        )\n        dc2.Clear()\n        dc2.SetDeviceOrigin(-1 * x, -1 * y)\n        if not self.wxdc:\n            if size:\n                self.html_updatet(dc2, size)\n            else:\n                self.html_updatet(dc2, rect)\n        else:\n            self.wxdc.dc = dc2\n            self.wxdc.play()\n        dc.Blit(x, y, rect.GetWidth(), rect.GetHeight(), dc2, x, y)\n        self._dc_buf = dc2\n        self._dc_buf_x = x\n        self._dc_buf_y = y\n    self._block = False\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.enable","title":"enable","text":"<pre><code>enable(enable=True)\n</code></pre> <p>enable od disable the form</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def enable(self, enable=True):\n    \"\"\"enable od disable the form\"\"\"\n    ret = super().Enable(enable)\n    if enable:\n        wx.CallAfter(self.restore_scroll_pos)\n    return ret\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.enable_ctrls","title":"enable_ctrls","text":"<pre><code>enable_ctrls(ctrls)\n</code></pre> <p>set widgets which are enabled or disabled Widget enabled is updated when html page is refreshed</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def enable_ctrls(self, ctrls):\n    \"\"\"set widgets which are enabled or disabled\n    Widget enabled is updated when html page is refreshed\n\n    Args:\n        ctrls - list or tuple with widgets names\n    \"\"\"\n    self._enabled_controls = ctrls\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.exec_code","title":"exec_code","text":"<pre><code>exec_code(script, parm=None)\n</code></pre> <p>run code attached to html page</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def exec_code(self, script, parm=None):\n    \"\"\"run code attached to html page\"\"\"\n    if parm is None:\n        d = {\"wx\": wx, \"self\": self}\n    else:\n        d = parm\n        d[\"self\"] = self\n    try:\n        exec(script, d)\n    except Exception:\n        logger.error(\"ERROR IN SCRIPT:\\n%s\", script, exc_info=True)\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.get_gparent_page","title":"get_gparent_page","text":"<pre><code>get_gparent_page()\n</code></pre> <p>return grand parent SChPage object in hierarchy</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def get_gparent_page(self):\n    \"\"\"return grand parent SChPage object in hierarchy\"\"\"\n    page = self.get_parent_page()\n    if page:\n        return page.get_parent_page()\n    else:\n        return None\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.get_parent_form","title":"get_parent_form","text":"<pre><code>get_parent_form()\n</code></pre> <p>return parent SchFrame object in hierarchy</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def get_parent_form(self):\n    \"\"\"return parent SchFrame object in hierarchy\"\"\"\n    gpage = self.get_gparent_page()\n    if gpage:\n        return gpage.active_form\n    return None\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.get_parent_page","title":"get_parent_page","text":"<pre><code>get_parent_page()\n</code></pre> <p>return parent SChPage object in hierarchy</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def get_parent_page(self):\n    \"\"\"return parent SChPage object in hierarchy\"\"\"\n    return self.page\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.get_parm_obj","title":"get_parm_obj","text":"<pre><code>get_parm_obj()\n</code></pre> <p>Get parent object width method get_par</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def get_parm_obj(self):\n    \"\"\"Get parent object width method get_par\"\"\"\n    parent = self\n    while parent is not None:\n        if hasattr(parent, \"get_parm\"):\n            return parent\n        parent = parent.GetParent()\n    return None\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.href_clicked","title":"href_clicked","text":"<pre><code>href_clicked(ctrl, attr_dict, upload=False, fields=False)\n</code></pre> <p>Handle action connected to widget with href attribute</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def href_clicked(self, ctrl, attr_dict, upload=False, fields=False):\n    \"\"\"Handle action connected to widget with href attribute\n\n    Args:\n        ctrl - widget\n        attr_dict - dict with attributes for http request\n        upload - if true, this is action prepared to upload content to server\n        fields - list of fields which values are sended to http server\n    \"\"\"\n    self.last_clicked = ctrl\n\n    if \"href\" in attr_dict:\n        href = attr_dict[\"href\"]\n        if href and len(href) &gt; 0 and href[0] == \"#\":\n            return self.scroll_to_href(href)\n        if href:\n            href = clean_href(href)\n        if \"target\" in attr_dict:\n            target = attr_dict[\"target\"]\n        else:\n            target = \"_blank\"\n        if \"title\" in attr_dict:\n            title = attr_dict[\"title\"]\n        else:\n            title = None\n\n        if href and hasattr(self, \"filter_url\"):\n            f = self.filter_url(target, href)\n            if f != None:\n                if type(f) == str:\n                    href = clean_href(f)\n                else:\n                    return f\n\n        if href:\n            parent_form = self.get_parent_form()\n            if hasattr(parent_form, \"filter_child_url\"):\n                ret = parent_form.filter_child_url(self, href)\n                if ret:\n                    return ret\n            # parent_page = self.GetParent().get_parent_page()\n            # if hasattr(parent_page.body, \"filter_child_url\"):\n            #    ret = parent_page.body.filter_child_url(self, href)\n            #    if ret:\n            #        return ret\n\n        else:\n            if target == \"_parent\":\n                self.any_parent_command(\"on_child_form_cancel\")\n                return\n            if target == \"_refresh\":\n                self.any_parent_command(\"send_refr_obj\")\n            if target == \"refresh_frame\":\n                self.any_parent_command(\"_refresh\")\n            return\n        if href[:7] == \"http://\" or href[:7] == \"file://\":\n            adr = href\n        else:\n            if href:\n                if href[0] == \".\":\n                    elm1 = self.get_parm_obj().address.split(\"?\")[0]\n                    if not elm1[-1] == \"/\":\n                        id = elm1.rfind(\"/\")\n                        if id &gt;= 0:\n                            elm1 = elm1[: id + 1]\n                        else:\n                            elm1 = \"\"\n                    adr = elm1 + href\n                else:\n                    adr = href\n            else:\n                adr = self.get_parm_obj().address\n\n        if \"/pdf/view\" in adr:\n            wx.GetApp().GetTopWindow().show_pdf(adr)\n            return\n\n        if \"/odf/view\" in adr:\n            wx.GetApp().GetTopWindow().show_odf(adr)\n            return\n\n        parm = createparm.create_parm(adr, self.get_parm_obj(), no_encode=True)\n        if parm:\n            adr = parm[0]\n\n        if fields:\n            (typ, fields2) = fields.split(\":\")\n            adr = adr + \"|\" + fields2\n        if target == \"refresh_frame\":\n            self.any_parent_command(\"set_adr_and_param\", adr, self.get_parm_obj())\n            self.any_parent_command(\"refresh_html\")\n            return\n        parm = createparm.create_parm(adr, self.get_parm_obj(), no_encode=True)\n        post = None\n        if parm:\n            if typ in (\"POST\", \"post\"):\n                adr = parm[0]\n                post = parm[2]\n            else:\n                adr = parm[0] + parm[1] + parm[2]\n                post = None\n\n        http = wx.GetApp().get_http(ctrl)\n\n        if upload:\n            response = http.post(self, adr, post, upload=True)\n        else:\n            if post:\n                response = http.post(self, adr, post)\n            else:\n                response = http.get(self, adr)\n\n        if \"500\" in response.ret_content_type:\n            return\n\n        if \"text/plain\" in response.ret_content_type:\n            s = response.str()\n            if title:\n                edit_name = title\n            else:\n                edit_name = href\n            okno = self.new_main_page(\"^standard/editor/editor.html\", href, None)\n            okno.body.EDITOR.SetValue(s)\n            okno.body.EDITOR.GotoPos(0)\n        elif \"/json\" in response.ret_content_type:\n            x = response.json()\n            if \"action\" in x and x[\"action\"] in (\n                \"update_row_ok\",\n                \"new_row_ok\",\n                \"delete_row_ok\",\n            ):\n                if \"obj\" in x:\n                    obj = x[\"obj\"]\n                else:\n                    obj = None\n                if self.GetParent().parent_page:\n                    ret = self.GetParent().parent_page.signal(x[\"action\"], obj)\n                else:\n                    ret = self.GetParent().signal(x[\"action\"], obj)\n                if ret:\n                    self.cancel()\n                else:\n                    self.ok()\n            return\n        else:\n            if \"text/\" not in response.ret_content_type:\n                wx.GetApp().GetTopWindow()._open_page(response)\n                return\n\n            s = response.str()\n            mp = ShtmlParser()\n            mp.process(s, response.new_url)\n            if \"target\" in mp.var:\n                target = mp.var[\"target\"]\n\n            if href and hasattr(self, \"filter_http_result\"):\n                f = self.filter_http_result(target, href, mp)\n                if f != None:\n                    return f\n            if target == \"_self\":\n                update_controls = self.update_controls\n                self.update_controls = True\n                self.show_form(mp.get_body(), self.get_parm_obj())\n                self.wxdc = None\n                self.draw_background()\n                self.Refresh()\n                self.init()\n                self.update_controls = update_controls\n                if \"href\" in attr_dict:\n                    href = attr_dict[\"href\"]\n                else:\n                    href = None\n\n                if (\n                    self.GetParent().address_or_parser.__class__.__name__\n                    == \"ShtmlParser\"\n                ):\n                    self.GetParent().address_or_parser.address = response.new_url\n                else:\n                    self.GetParent().address_or_parser = response.new_url\n\n                return\n            if (\n                target.startswith(\"popup\")\n                or target.startswith(\"inline\")\n                or target\n                in (\n                    \"_blank\",\n                    \"_self\",\n                )\n            ):\n                self.GetParent().active_ctrl = ctrl\n                win = self.new_child_page(\n                    mp, is_null(mp.title, title), parameters=self.get_parm_obj()\n                )\n                if win:\n                    win.body.set_address_parm(str(response.new_url))\n                return\n            if target == \"_parent\":\n                self.new_main_page(\n                    mp,\n                    is_null(mp.title, title),\n                    parameters=self.get_parm_obj(),\n                    view_in=None,\n                )\n                return\n            if target == \"refresh_page\":\n                # self.any_parent_command(\"set_new_href\", adr)\n                self.any_parent_command(\"refresh_html\")\n\n            if target.startswith(\"_top2\"):\n                x = target[5:]\n                if x[0:1] == \"_\":\n                    self.new_main_page(\n                        mp,\n                        is_null(mp.title, title),\n                        parameters=self.get_parm_obj(),\n                        view_in=x[1:],\n                    )\n                else:\n                    self.new_main_page(\n                        mp,\n                        is_null(mp.title, title),\n                        parameters=self.get_parm_obj(),\n                        view_in=\"desktop2\",\n                    )\n                return\n            if target == \"_parent\":\n                self.any_parent_command(\"on_child_form_cancel\")\n                return\n            if target == \"_refresh\":\n                self.GetParent().refresh_html()\n                return\n            if target == \"_parent_refr\":\n                self.any_parent_command(\"on_child_form_ok\")\n                return\n            if target == \"message\":\n                dlg = wx.MessageDialog(\n                    self, s, is_null(mp.title, title), style=wx.OK\n                )\n                dlg.ShowModal()\n                return\n\n            if target == \"code\":\n                script = mp.get_body()[1]\n                app = wx.GetApp()\n                main_window = app.GetTopWindow()\n                desktop = (main_window.desktop,)\n                mgr = main_window._mgr\n                menu_bar = main_window.get_menu_bar()\n                tool_bars = main_window.get_tool_bar()\n                exec(script.elem.text.replace(\"\\r\", \"\"))\n                return\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.html_updatet","title":"html_updatet","text":"<pre><code>html_updatet(dc, size)\n</code></pre> <p>parse a html page after it was updated</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def html_updatet(self, dc, size):\n    \"\"\"parse a html page after it was updated\"\"\"\n    w = size.GetWidth()\n    h = size.GetHeight()\n    if w == 20 and h == 20:\n        return\n    if not self.wxdc:\n        if self.no_vscrollbar:\n            self.wxdc = DcDc(\n                dc, calc_only=False, width=w - 1, height=-1, record=True\n            )\n        else:\n            self.wxdc = DcDc(\n                dc,\n                calc_only=False,\n                width=w - 1 - wx.SystemSettings.GetMetric(wx.SYS_VSCROLL_X),\n                height=-1,\n                record=True,\n            )\n        p = HtmlViewerParser(\n            dc=self.wxdc, calc_only=False, init_css_str=self.get_css(), css_type=1\n        )\n        p.set_http_object(wx.GetApp().http)\n        p.set_parent_window(self)\n        if not self._lock:\n            self._lock = True\n            self.GetParent().restart_ctrl_lp()\n            p.feed(self.page_source)\n            self.GetParent().remove_old_ctrls()\n            self._lock = False\n        self.obj_action_dict = p.obj_action_dict\n        self.obj_id_dict = p.obj_id_dict\n        if not self.no_vscrollbar:\n            (w2, h2) = p.get_max_sizes()\n            # h2+=8\n            # h2-=wx.SystemSettings.GetMetric(wx.SYS_VSCROLL_X)\n            self.hscroll = True if w2 &gt; w + 3 else False\n            self.vscroll = True if h2 &gt; h + 3 else False\n            if self.no_hscrollbar:\n                self.hscroll = False\n            if (\n                (self.vscroll or self.hscroll)\n                and w &gt; 0\n                and h &gt; 0\n                and w2 &gt; 0\n                and h2 &gt; 0\n            ):\n                if self.vscroll:\n                    w2 = w2 + wx.SystemSettings.GetMetric(wx.SYS_VSCROLL_X)\n                if self.hscroll:\n                    h2 = h2 + wx.SystemSettings.GetMetric(wx.SYS_HSCROLL_Y)\n                self._best_virtual_size = (int(w2), int(h2))\n                self.EnableScrolling(self.hscroll, self.vscroll)\n                self._SetupAfter(False)\n            self.SetVirtualSize((w2, h2))\n        else:\n            self.EnableScrolling(False, False)\n            self.SetVirtualSize((w, h))\n        p.close()\n        self.update_controls = False\n        wx.CallAfter(self.SetupScrolling, self.hscroll, self.vscroll, rate_y=1)\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.init","title":"init","text":"<pre><code>init(callback=None)\n</code></pre> <p>init form</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def init(self, callback=None):\n    \"\"\"init form\"\"\"\n    if not self.after_init:\n        if hasattr(self, \"init_form\"):\n\n            def _init():\n                nonlocal self\n                self.init_form()\n                if callback:\n                    callback(self)\n\n            wx.CallAfter(_init)\n        self.after_init = True\n    else:\n        if hasattr(self, \"reinit\"):\n            self.reinit()\n        elif hasattr(self, \"init_form\"):\n            self.init_form()\n        if callback:\n            callback(self)\n    wx.CallAfter(self._build_acc_tab)\n    wx.CallAfter(self._check_scroll_bar)\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.new_child_page","title":"new_child_page","text":"<pre><code>new_child_page(address_or_parser, title='', parameters=None, callback=None)\n</code></pre> <p>Create a child page</p> <p>Parameters:</p> Name Type Description Default <code>address_or_parser</code> <p>can be: address of http page (str type) or</p> required <code></code> required <code>parameters</code> <p>dict</p> <code>None</code> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def new_child_page(\n    self, address_or_parser, title=\"\", parameters=None, callback=None\n):\n    \"\"\"Create a child page\n\n    Args:\n        address_or_parser: can be: address of http page (str type) or\n        :class:'~pytigon_lib.schparser.html_parsers.ShtmlParser'\n        title - new page title\n        parameters: dict\n    \"\"\"\n    self._scroll_xy = self.GetViewStart()\n    self.get_parm_obj().active_form = self\n    self.GetParent().active_form = self\n    if hasattr(self.GetParent(), \"last_control_with_focus\"):\n        self.last_control_with_focus = self.GetParent().last_control_with_focus\n    else:\n        self.last_control_with_focus = None\n    self.GetParent().disable_setfocus = True\n    return self.any_parent_command(\n        \"new_child_page\", address_or_parser, title, parameters, callback=callback\n    )\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.refr","title":"refr","text":"<pre><code>refr(refr_always=False)\n</code></pre> <p>Refresh html content</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def refr(self, refr_always=False):\n    # \"\"\"Refresh form\"\"\"\n    # self.refr_obj(refr_always=True)\n    # def refr_obj(self, refr_always=False):\n    \"\"\"Refresh html content\"\"\"\n\n    if refr_always or self.GetParent().IsShown():\n        if self.address:\n            parm = createparm.create_parm(self.address, self.get_parm_obj())\n            http = wx.GetApp().get_http(self)\n            if parm:\n                response = http.get(self, str(parm[0] + parm[1] + parm[2]))\n            else:\n                response = http.get(self, self.address)\n            if response.ret_code == 404:\n                return\n\n            mp = ShtmlParser()\n            mp.process(response.str(), \"local\")\n            if self.form_type == \"panel\":\n                strona = mp.get_panel()\n            elif self.form_type == \"header\":\n                strona = mp.get_header()\n            elif self.form_type == \"footer\":\n                strona = mp.get_footer()\n            else:\n                strona = mp.get_body()\n            if self.show_form(strona, self.parameters):\n                self.init()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.refresh_time","title":"refresh_time","text":"<pre><code>refresh_time(number_of_sec)\n</code></pre> <p>When refresh time is set form automatically refresh his content every number_of_sec seconds</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def refresh_time(self, number_of_sec):\n    \"\"\"When refresh time is set form automatically refresh his content every number_of_sec seconds\"\"\"\n    self.t1 = wx.Timer(self)\n    self.t1.Start(int(number_of_sec * 1000))\n    self.Bind(wx.EVT_TIMER, self.on_timer, self.t1)\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.scroll_child_into_view","title":"scroll_child_into_view","text":"<pre><code>scroll_child_into_view(child)\n</code></pre> <p>scroll this window to position at which child window is visible</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def scroll_child_into_view(self, child):\n    \"\"\"scroll this window to position at which child window is visible\"\"\"\n    if child.GetParent() == self:\n        (sppu_x, sppu_y) = self.GetScrollPixelsPerUnit()\n        (vs_x, vs_y) = self.GetViewStart()\n        cr = child.GetRect()\n        clntsz = self.GetClientSize()\n        (new_vs_x, new_vs_y) = (-1, -1)\n        if cr.y &lt; 0 and sppu_y &gt; 0:\n            new_vs_y = vs_y + cr.y / sppu_y\n        if cr.bottom &gt; clntsz.height and sppu_y &gt; 0:\n            diff = math.ceil((1.0 * (cr.bottom - clntsz.height + 1)) / sppu_y)\n            if cr.y - diff * sppu_y &gt; 0:\n                new_vs_y = vs_y + diff\n            else:\n                new_vs_y = vs_y + cr.y / sppu_y\n        if new_vs_y != -1:\n            self.Scroll(-1, new_vs_y)\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.scroll_to_href","title":"scroll_to_href","text":"<pre><code>scroll_to_href(href)\n</code></pre> <p>scroll this window to position at which widget with a href attribute is visible</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def scroll_to_href(self, href):\n    \"\"\"scroll this window to position at which widget with a href attribute is visible\"\"\"\n    ref = href[1:]\n    if ref in self.obj_id_dict:\n        obj = self.obj_id_dict[ref]\n        rects = obj.rendered_rects\n        if len(rects) &gt; 0:\n            rect = rects[0]\n            x = rect[0]\n            y = rect[1]\n            if self.GetScrollPixelsPerUnit()[0] &gt; 0:\n                dx = x / self.GetScrollPixelsPerUnit()[0]\n            else:\n                dx = -1\n            if self.GetScrollPixelsPerUnit()[1] &gt; 0:\n                dy = y / self.GetScrollPixelsPerUnit()[1]\n            else:\n                dy = -1\n            self.Scroll(dx, dy)\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.set_acc_key_tab","title":"set_acc_key_tab","text":"<pre><code>set_acc_key_tab(win, tab)\n</code></pre> <p>Set accelerator table for window</p> <p>Parameters:</p> Name Type Description Default <code>tab - list or tuple of accelerator elements. Each emement has structure</code> <p>(flag, keycode, callback fun)</p> required Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def set_acc_key_tab(self, win, tab):\n    \"\"\"Set accelerator table for window\n\n    Args:\n        win - window which handle accelerator keys\n        tab - list or tuple of accelerator elements. Each emement has structure: (flag, keycode, callback fun)\n    \"\"\"\n    if win in self.acc_tabs:\n        self.acc_tabs[win].append(tab)\n    else:\n        self.acc_tabs[win] = [\n            tab,\n        ]\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.set_address_parm","title":"set_address_parm","text":"<pre><code>set_address_parm(address)\n</code></pre> <p>page address for refr_obj in format http://adres?zmienna1|zmienna2,zmienna</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def set_address_parm(self, address):\n    \"\"\"page address for refr_obj in format http://adres?zmienna1|zmienna2,zmienna\"\"\"\n    if address:\n        elementy = address.split(\"|\")\n        if elementy[0][-1] == \"/\":\n            address2 = address\n        else:\n            address2 = elementy[0] + \"/\"\n            if len(elementy) &gt; 1:\n                address2 = address2 + \"|\" + elementy[1]\n        self.address = address2\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.set_best_size","title":"set_best_size","text":"<pre><code>set_best_size(bestsize)\n</code></pre> <p>Set the best size for this form</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def set_best_size(self, bestsize):\n    \"\"\"Set the best size for this form\"\"\"\n    self.bestsize = bestsize\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.set_css","title":"set_css","text":"<pre><code>set_css(css_str)\n</code></pre> <p>set css file for this form</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def set_css(self, css_str):\n    \"\"\"set css file for this form\"\"\"\n    self.init_css_str = css_str\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.set_page","title":"set_page","text":"<pre><code>set_page(page_source)\n</code></pre> <p>Set html page source</p> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def set_page(self, page_source):\n    \"\"\"Set html page source\"\"\"\n    self.page_source = page_source\n    self.wxdc = None\n    self.draw_background()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchForm.show_form","title":"show_form","text":"<pre><code>show_form(page_and_script, parameters=None)\n</code></pre> <p>Show form</p> <p>Parameters:</p> Name Type Description Default <code>page_and_script</code> <p>list or touple, 0: html page content, 1: script</p> required <code>parameters</code> <p>parameters forwarded to http server in request</p> <code>None</code> Source code in <code>pytigon_gui/guiframe/form.py</code> <pre><code>def show_form(self, page_and_script, parameters=None):\n    \"\"\"Show form\n\n    Args:\n        page_and_script: list or touple, 0: html page content, 1: script\n        parameters: parameters forwarded to http server in request\n    \"\"\"\n    if self.page and self.page.exists == False:\n        return False\n\n    self.page_source = self.pre_process_page(page_and_script[0])\n    self.parameters = parameters\n    if not self.script:\n        self.script = str(page_and_script[1])\n        if self.script and len(self.script) &gt; 1:\n            app = wx.GetApp()\n            main_window = app.GetTopWindow()\n            desktop = (main_window.desktop,)\n            mgr = main_window._mgr\n            menu_bar = main_window.get_menu_bar()\n            script_tmp = self.script.replace(\"\\r\", \"\").split(\"\\n\")\n            script = \"\"\n            tab = -1\n            for line in script_tmp:\n                if tab == -1:\n                    if line.strip() != \"\":\n                        tab = len(line) - len(line.lstrip())\n                if tab &gt;= 0:\n                    script = script + line[tab:] + \"\\n\"\n            d = {\"wx\": wx}\n            self.exec_code(script, d)\n\n            for key in d:\n                if not key.startswith(\"__\"):\n                    if hasattr(d[key], \"__call__\"):\n                        fun = d[key]\n                        try:\n                            method = types.MethodType(d[key], self, self.__class__)\n                        except TypeError:\n                            method = types.MethodType(d[key], self)\n                        if hasattr(self, key):\n                            setattr(self, \"base_\" + key, getattr(self, key))\n                        setattr(self, key, method)\n                    else:\n                        setattr(self, key, d[key])\n    return True\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage","title":"SchPage","text":"<pre><code>SchPage(parent, address_or_parser, parameters, pos=(0, 0), size=DefaultSize)\n</code></pre> <p>               Bases: <code>Window</code>, <code>Signal</code></p> <p>Container that manages up to four SchForm instances.</p> <p>Reads an HTML response from the server and distributes its sections (header, body, footer, panel) into corresponding sash-layout sub-windows.</p> <p>Construct a SchPage.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent SchNotebookPage.</p> required <code>address_or_parser</code> <p>HTTP address (str) or ShtmlParser instance.</p> required <code>parameters</code> <p>Dictionary of request parameters.</p> required <code>pos</code> <p>Initial position.</p> <code>(0, 0)</code> <code>size</code> <p>Initial size.</p> <code>DefaultSize</code> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def __init__(\n    self, parent, address_or_parser, parameters, pos=(0, 0), size=wx.DefaultSize\n):\n    \"\"\"Construct a SchPage.\n\n    Args:\n        parent: Parent SchNotebookPage.\n        address_or_parser: HTTP address (str) or ShtmlParser instance.\n        parameters: Dictionary of request parameters.\n        pos: Initial position.\n        size: Initial size.\n    \"\"\"\n    self._active = True\n    self._ctrl_dict_old = {}\n    self._ctrl_dict = {}\n    self._last_size = None\n    self._disable_setfocus = 0\n    self._signal_handlers = []\n\n    self.address_or_parser = address_or_parser\n    self.parameters = parameters\n    self.default_button = None\n    self.parent_page = None\n    self.last_control_with_focus = None\n    self.vertical_position = None\n\n    self.header = None\n    self.panel = None\n    self.body = None\n    self.footer = None\n    self.active_form = None\n    self.active_ctrl = None\n\n    Signal.__init__(self)\n    wx.Window.__init__(\n        self, parent, -1, pos, size, style=wx.WANTS_CHARS, name=\"SchPage\"\n    )\n\n    self.exists = True\n\n    mp = self._read_html(address_or_parser, parameters)\n    if not mp:\n        logger.error(\"ERROR READ HTML: %s %s\", address_or_parser, parameters)\n\n    self.address = mp.address\n\n    header = mp.get_header()\n    body = mp.get_body()\n    footer = mp.get_footer()\n    panel = mp.get_panel()\n    config = mp.var\n\n    vscroll = True\n    hscroll = True\n\n    if \"no_vscrollbar\" in config:\n        vscroll = False\n    if \"no_hscrollbar\" in config:\n        hscroll = False\n    if \"vertical_position\" in config:\n        self.set_vertical_position(config[\"vertical_position\"])\n\n    self.title = mp.title\n\n    if \"disable_parent\" in config:\n        self.disable_parent = config[\"disable_parent\"] != \"0\"\n    else:\n        self.disable_parent = True\n\n    winids = []\n\n    if header[0]:\n        topwin = wx.adv.SashLayoutWindow(\n            self,\n            -1,\n            wx.DefaultPosition,\n            (100, 5),\n            wx.adv.SW_3D,\n            style=wx.WANTS_CHARS,\n        )\n        topwin.SetOrientation(wx.adv.LAYOUT_HORIZONTAL)\n        topwin.SetAlignment(wx.adv.LAYOUT_TOP)\n        topwin.SetSashVisible(wx.adv.SASH_BOTTOM, True)\n        self.header = SchForm(topwin, page=self, form_type=\"header\")\n        self.header.show_form(header)\n        dy = self.header.calculate_best_size()[1]\n        topwin.SetDefaultSize((1000, dy))\n        self._top_window = topwin\n        winids.append(topwin.GetId())\n    else:\n        self._top_window = None\n\n    if footer[0]:\n        bottomwin = wx.adv.SashLayoutWindow(\n            self, -1, wx.DefaultPosition, (1000, 5), wx.adv.SW_3D\n        )\n        bottomwin.SetOrientation(wx.adv.LAYOUT_HORIZONTAL)\n        bottomwin.SetAlignment(wx.adv.LAYOUT_BOTTOM)\n        bottomwin.SetSashVisible(wx.adv.SASH_TOP, True)\n        self.footer = SchForm(bottomwin, page=self, form_type=\"footer\")\n        self.footer.show_form(footer)\n        bottomwin.SetDefaultSize((1000, self.footer.calculate_best_size()[1]))\n        self._bottom_window = bottomwin\n        winids.append(bottomwin.GetId())\n    else:\n        self._bottom_window = None\n\n    if panel[0]:\n        leftwin = wx.adv.SashLayoutWindow(\n            self, -1, wx.DefaultPosition, (5, 100), wx.adv.SW_3D\n        )\n        leftwin.SetOrientation(wx.adv.LAYOUT_VERTICAL)\n        leftwin.SetAlignment(wx.adv.LAYOUT_LEFT)\n        leftwin.SetSashVisible(wx.adv.SASH_RIGHT, True)\n        self.panel = SchForm(leftwin, page=self, form_type=\"panel\")\n        self.panel.show_form(panel)\n        xy = self.panel.calculate_best_size()\n        leftwin.SetDefaultSize(xy)\n        self._left_window = leftwin\n        winids.append(leftwin.GetId())\n    else:\n        self._left_window = None\n\n    self.body = SchForm(self, self, hscroll, vscroll)\n    self.body.set_htm_type(\"body\")\n    attrs = mp.get_body_attrs()\n    if \"width\" in attrs and \"height\" in attrs:\n        w = attrs[\"width\"]\n        h = attrs[\"height\"]\n        self.body.bestsize = (int(w) * 4 / 3, int(h) * 4 / 3)\n    self.body.show_form(body, parameters)\n    self.body.set_address_parm(self.address)\n    if \"refresh\" in mp.var:\n        self.body.refresh_time(int(mp.var[\"refresh\"]))\n\n    if winids:\n        self.Bind(\n            wx.adv.EVT_SASH_DRAGGED_RANGE,\n            self.on_sash_drag,\n            id=min(winids),\n            id2=max(winids),\n        )\n\n    self.Bind(wx.EVT_SIZE, self.on_size)\n    self.body.Bind(wx.EVT_CHILD_FOCUS, self.on_child_focus)\n    self.Bind(wx.EVT_SET_FOCUS, self.on_set_focus)\n\n    self.register_signal(self, \"child_closed_with_ok\")\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage-functions","title":"Methods:","text":""},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.CanClose","title":"CanClose","text":"<pre><code>CanClose()\n</code></pre> <p>Check whether all child forms can be closed.</p> <p>If all forms agree, their <code>_on_close</code> methods are called and signal handlers are disconnected.</p> <p>Returns:</p> Type Description <p>True if all forms can close.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def CanClose(self):\n    \"\"\"Check whether all child forms can be closed.\n\n    If all forms agree, their ``_on_close`` methods are called\n    and signal handlers are disconnected.\n\n    Returns:\n        True if all forms can close.\n    \"\"\"\n    for form in (self.body, self.header, self.footer, self.panel):\n        if form and not form.CanClose():\n            return False\n\n    if self.header:\n        self.header._on_close()\n    if self.body:\n        self.body._on_close()\n    if self.footer:\n        self.footer._on_close()\n    if self.panel:\n        self.panel._on_close()\n\n    for handler in self._signal_handlers:\n        dispatcher.disconnect(handler[0], handler[1], sender=dispatcher.Any)\n\n    return True\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.activate_page","title":"activate_page","text":"<pre><code>activate_page()\n</code></pre> <p>Activate this page and focus the body.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def activate_page(self):\n    \"\"\"Activate this page and focus the body.\"\"\"\n    self._active = True\n    self.body.SetFocus()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.append_ctrl","title":"append_ctrl","text":"<pre><code>append_ctrl(obj)\n</code></pre> <p>Register a widget with its unique name.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <p>The widget to register.</p> required Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def append_ctrl(self, obj):\n    \"\"\"Register a widget with its unique name.\n\n    Args:\n        obj: The widget to register.\n    \"\"\"\n    self._ctrl_dict[obj.get_unique_name()] = obj\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.calculate_best_size","title":"calculate_best_size","text":"<pre><code>calculate_best_size()\n</code></pre> <p>Compute the best size based on all child forms.</p> <p>Returns:</p> Type Description <p>Tuple (width, height).</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def calculate_best_size(self):\n    \"\"\"Compute the best size based on all child forms.\n\n    Returns:\n        Tuple (width, height).\n    \"\"\"\n    if self._last_size:\n        dx = self._last_size.GetWidth()\n        dy = self._last_size.GetHeight()\n        self._last_size = None\n        return (dx, dy)\n\n    dx = 0\n    dy = 0\n\n    if self.header:\n        x, y = self.header.calculate_best_size()\n        dy += y\n    if self.body:\n        x, y = self.body.calculate_best_size()\n        dx += x\n        dy += y\n    if self.footer:\n        x, y = self.footer.calculate_best_size()\n        dy += y\n    if self.panel:\n        x, y = self.panel.calculate_best_size()\n        dx += x\n        if y &gt; dy:\n            dy = y\n    return (dx, dy)\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.change_notebook_page_title","title":"change_notebook_page_title","text":"<pre><code>change_notebook_page_title(new_title)\n</code></pre> <p>Update the tab caption that contains this SchPage.</p> <p>Parameters:</p> Name Type Description Default <code>new_title</code> <p>New tab text.</p> required Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def change_notebook_page_title(self, new_title):\n    \"\"\"Update the tab caption that contains this SchPage.\n\n    Args:\n        new_title: New tab text.\n    \"\"\"\n    p = self.GetParent()\n    tab = p.GetParent()\n    sel = tab.GetPageIndex(p)\n    tab.SetPageText(sel, new_title)\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.child_closed_with_ok","title":"child_closed_with_ok","text":"<pre><code>child_closed_with_ok(win=None)\n</code></pre> <p>Refresh HTML when a child window closes with OK.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def child_closed_with_ok(self, win=None):\n    \"\"\"Refresh HTML when a child window closes with OK.\"\"\"\n    wx.CallAfter(self.refresh_html)\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Initiate cancellation of this page.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def close(self):\n    \"\"\"Initiate cancellation of this page.\"\"\"\n    self.GetParent().on_child_form_cancel()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.deactivate_page","title":"deactivate_page","text":"<pre><code>deactivate_page()\n</code></pre> <p>Deactivate this page.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def deactivate_page(self):\n    \"\"\"Deactivate this page.\"\"\"\n    self._active = False\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.enable_forms","title":"enable_forms","text":"<pre><code>enable_forms(enable)\n</code></pre> <p>Enable or disable all managed forms.</p> <p>Parameters:</p> Name Type Description Default <code>enable</code> <p>If True, enable; if False, disable.</p> required Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def enable_forms(self, enable):\n    \"\"\"Enable or disable all managed forms.\n\n    Args:\n        enable: If True, enable; if False, disable.\n    \"\"\"\n    if not enable:\n        self._last_size = self.GetSize()\n\n    if enable or self.disable_parent:\n        for form in (self.header, self.body, self.footer, self.panel):\n            if form is not None:\n                form.enable(enable)\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.get_item","title":"get_item","text":"<pre><code>get_item(ctrl_name)\n</code></pre> <p>Look up a control by name across all child forms.</p> <p>Parameters:</p> Name Type Description Default <code>ctrl_name</code> <p>Name of the control.</p> required <p>Returns:</p> Type Description <p>The control widget, or None.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def get_item(self, ctrl_name):\n    \"\"\"Look up a control by name across all child forms.\n\n    Args:\n        ctrl_name: Name of the control.\n\n    Returns:\n        The control widget, or None.\n    \"\"\"\n    if ctrl_name in self._ctrl_dict:\n        return self._ctrl_dict[ctrl_name]\n    for form in (self.body, self.header, self.panel, self.footer):\n        if form is not None and hasattr(form, ctrl_name):\n            return getattr(form, ctrl_name)\n    return None\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.get_last_control_with_focus","title":"get_last_control_with_focus","text":"<pre><code>get_last_control_with_focus()\n</code></pre> <p>Return the last control that had focus.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def get_last_control_with_focus(self):\n    \"\"\"Return the last control that had focus.\"\"\"\n    return self.last_control_with_focus\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.get_parent_page","title":"get_parent_page","text":"<pre><code>get_parent_page()\n</code></pre> <p>Return the parent SchPage, if any.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def get_parent_page(self):\n    \"\"\"Return the parent SchPage, if any.\"\"\"\n    return self.parent_page\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.get_parm","title":"get_parm","text":"<pre><code>get_parm(param)\n</code></pre> <p>Get the value of a named control.</p> <p>Parameters:</p> Name Type Description Default <code>param</code> <p>Control name.</p> required <p>Returns:</p> Type Description <p>The control's value, or None if not found.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def get_parm(self, param):\n    \"\"\"Get the value of a named control.\n\n    Args:\n        param: Control name.\n\n    Returns:\n        The control's value, or None if not found.\n    \"\"\"\n    ctrl = self._ctrl_dict.get(param) or self.get_item(param)\n    if ctrl:\n        return ctrl.GetValue()\n    return None\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.get_title","title":"get_title","text":"<pre><code>get_title()\n</code></pre> <p>Return the page title.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def get_title(self):\n    \"\"\"Return the page title.\"\"\"\n    return self.title\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.get_widgets","title":"get_widgets","text":"<pre><code>get_widgets()\n</code></pre> <p>Return the current widget dictionary.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def get_widgets(self):\n    \"\"\"Return the current widget dictionary.\"\"\"\n    return self._ctrl_dict\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.has_parm","title":"has_parm","text":"<pre><code>has_parm(param)\n</code></pre> <p>Check if a named control exists.</p> <p>Parameters:</p> Name Type Description Default <code>param</code> <p>Control name.</p> required <p>Returns:</p> Type Description <p>True if the control is found.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def has_parm(self, param):\n    \"\"\"Check if a named control exists.\n\n    Args:\n        param: Control name.\n\n    Returns:\n        True if the control is found.\n    \"\"\"\n    return param in self._ctrl_dict or self.item_exist(param)\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.init_frame","title":"init_frame","text":"<pre><code>init_frame(callback=None)\n</code></pre> <p>Initialize header, footer, panel, and body forms.</p> <p>Parameters:</p> Name Type Description Default <code>callback</code> <p>Optional callable invoked after body init.</p> <code>None</code> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def init_frame(self, callback=None):\n    \"\"\"Initialize header, footer, panel, and body forms.\n\n    Args:\n        callback: Optional callable invoked after body init.\n    \"\"\"\n    if self.header:\n        self.header.init()\n    if self.footer:\n        self.footer.init()\n    if self.panel:\n        self.panel.init()\n\n    self.body.init(callback)\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.is_active","title":"is_active","text":"<pre><code>is_active()\n</code></pre> <p>Return whether this page is active.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def is_active(self):\n    \"\"\"Return whether this page is active.\"\"\"\n    return self._active\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.item_exist","title":"item_exist","text":"<pre><code>item_exist(ctrl_name)\n</code></pre> <p>Check whether a named control exists in any child form.</p> <p>Parameters:</p> Name Type Description Default <code>ctrl_name</code> <p>Name of the control.</p> required <p>Returns:</p> Type Description <p>True if found.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def item_exist(self, ctrl_name):\n    \"\"\"Check whether a named control exists in any child form.\n\n    Args:\n        ctrl_name: Name of the control.\n\n    Returns:\n        True if found.\n    \"\"\"\n    if ctrl_name in self._ctrl_dict:\n        return True\n    for form in (self.body, self.header, self.panel, self.footer):\n        if form is not None and hasattr(form, ctrl_name):\n            return True\n    return False\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.on_char_hook","title":"on_char_hook","text":"<pre><code>on_char_hook(event)\n</code></pre> <p>Handle character input: Escape cancels the child form.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def on_char_hook(self, event):\n    \"\"\"Handle character input: Escape cancels the child form.\"\"\"\n    if event.KeyCode == wx.WXK_ESCAPE:\n        parent = self.GetParent()\n        if hasattr(parent, \"on_child_form_cancel\"):\n            parent.on_child_form_cancel()\n        else:\n            event.Skip()\n    else:\n        event.Skip()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.on_child_focus","title":"on_child_focus","text":"<pre><code>on_child_focus(evt)\n</code></pre> <p>Track which widget last received focus.</p> <p>Ensures the owning HtmlPanel tab is selected and calls KillFocus on the previously focused widget.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def on_child_focus(self, evt):\n    \"\"\"Track which widget last received focus.\n\n    Ensures the owning HtmlPanel tab is selected and calls\n    KillFocus on the previously focused widget.\n    \"\"\"\n    if self._disable_setfocus:\n        evt.Skip()\n        return\n\n    new_win = evt.GetWindow()\n    parent = new_win.GetParent()\n    while parent is not None:\n        if parent.__class__.__name__ == \"HtmlPanel\":\n            parent.SelectTab()\n            break\n        parent = parent.GetParent()\n\n    while new_win is not None:\n        if (\n            new_win.GetParent()\n            and new_win.GetParent().GetWindowStyleFlag() &amp; wx.TAB_TRAVERSAL != 0\n        ):\n            break\n        new_win = new_win.GetParent()\n\n    if new_win is not self.last_control_with_focus:\n        if self.last_control_with_focus and hasattr(\n            self.last_control_with_focus, \"KillFocus\"\n        ):\n            self._disable_setfocus = True\n            self.last_control_with_focus.KillFocus()\n            self._disable_setfocus = False\n        self.last_control_with_focus = new_win\n\n    evt.Skip()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.on_key_down","title":"on_key_down","text":"<pre><code>on_key_down(event)\n</code></pre> <p>Handle key down: Enter triggers the default button.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def on_key_down(self, event):\n    \"\"\"Handle key down: Enter triggers the default button.\"\"\"\n    if event.KeyCode == wx.WXK_RETURN and self.default_button:\n        self.default_button.OnClick(event)\n    else:\n        event.Skip()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.on_sash_drag","title":"on_sash_drag","text":"<pre><code>on_sash_drag(event)\n</code></pre> <p>Handle sash drag events for header, footer, and panel.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def on_sash_drag(self, event):\n    \"\"\"Handle sash drag events for header, footer, and panel.\"\"\"\n    if event.GetDragStatus() == wx.SASH_STATUS_OUT_OF_RANGE:\n        return\n    eobj = event.GetEventObject()\n    if eobj is self._top_window:\n        self._top_window.SetDefaultSize((1000, event.GetDragRect().height))\n    elif eobj is self._left_window:\n        self._left_window.SetDefaultSize((event.GetDragRect().width, 1000))\n    elif eobj is self._bottom_window:\n        self._bottom_window.SetDefaultSize((1000, event.GetDragRect().height))\n    LayoutAlgorithm().LayoutWindow(self, self.body)\n    self.body.Refresh()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.on_set_focus","title":"on_set_focus","text":"<pre><code>on_set_focus(evt)\n</code></pre> <p>Delegate focus to the body form.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def on_set_focus(self, evt):\n    \"\"\"Delegate focus to the body form.\"\"\"\n    if self.body:\n        self.body.SetFocus()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.on_size","title":"on_size","text":"<pre><code>on_size(event)\n</code></pre> <p>Handle size changes, re-laying out the sash windows.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def on_size(self, event):\n    \"\"\"Handle size changes, re-laying out the sash windows.\"\"\"\n    LayoutAlgorithm().LayoutWindow(self, self.body)\n    self.body.Refresh()\n    if event:\n        event.Skip()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.pop_ctrl","title":"pop_ctrl","text":"<pre><code>pop_ctrl(name)\n</code></pre> <p>Retrieve and remove a widget from the previous lifecycle.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <p>Unique widget name.</p> required <p>Returns:</p> Type Description <p>The widget or None if not found.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def pop_ctrl(self, name):\n    \"\"\"Retrieve and remove a widget from the previous lifecycle.\n\n    Args:\n        name: Unique widget name.\n\n    Returns:\n        The widget or None if not found.\n    \"\"\"\n    return self._ctrl_dict_old.pop(name, None)\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.refresh_html","title":"refresh_html","text":"<pre><code>refresh_html(method=0)\n</code></pre> <p>Reload page content from the server.</p> <p>Parameters:</p> Name Type Description Default <code>method</code> <p>Refresh method (reserved for future use).</p> <code>0</code> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def refresh_html(self, method=0):\n    \"\"\"Reload page content from the server.\n\n    Args:\n        method: Refresh method (reserved for future use).\n    \"\"\"\n    return self._refresh_html(method)\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.reg_application_signal_handler","title":"reg_application_signal_handler","text":"<pre><code>reg_application_signal_handler(fun, signal)\n</code></pre> <p>Register a callback for a PyDispatcher signal.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <p>Callback function.</p> required <code>signal</code> <p>Signal name.</p> required Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def reg_application_signal_handler(self, fun, signal):\n    \"\"\"Register a callback for a PyDispatcher signal.\n\n    Args:\n        fun: Callback function.\n        signal: Signal name.\n    \"\"\"\n    for existing in self._signal_handlers:\n        if existing[1] == signal:\n            break\n    dispatcher.connect(fun, signal, sender=dispatcher.Any)\n    self._signal_handlers.append((fun, signal))\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.remove_old_ctrls","title":"remove_old_ctrls","text":"<pre><code>remove_old_ctrls()\n</code></pre> <p>Destroy all widgets from the previous lifecycle.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def remove_old_ctrls(self):\n    \"\"\"Destroy all widgets from the previous lifecycle.\"\"\"\n    for name, win in list(self._ctrl_dict_old.items()):\n        win.Destroy()\n    self._ctrl_dict_old = {}\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.restart_ctrl_lp","title":"restart_ctrl_lp","text":"<pre><code>restart_ctrl_lp()\n</code></pre> <p>Begin a new control lifecycle, preserving the old map.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def restart_ctrl_lp(self):\n    \"\"\"Begin a new control lifecycle, preserving the old map.\"\"\"\n    self._ctrl_dict_old = self._ctrl_dict\n    self._ctrl_dict = {}\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.set_adr_and_param","title":"set_adr_and_param","text":"<pre><code>set_adr_and_param(address_or_parser, param)\n</code></pre> <p>Change the source address and request parameters.</p> <p>Parameters:</p> Name Type Description Default <code>address_or_parser</code> <p>New address or parser.</p> required <code>param</code> <p>New parameters dict.</p> required Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def set_adr_and_param(self, address_or_parser, param):\n    \"\"\"Change the source address and request parameters.\n\n    Args:\n        address_or_parser: New address or parser.\n        param: New parameters dict.\n    \"\"\"\n    self.address_or_parser = address_or_parser\n    self.parameters = param\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.set_default_button","title":"set_default_button","text":"<pre><code>set_default_button(button)\n</code></pre> <p>Set the default button activated by Enter.</p> <p>Parameters:</p> Name Type Description Default <code>button</code> <p>The wx.Button-like control.</p> required Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def set_default_button(self, button):\n    \"\"\"Set the default button activated by Enter.\n\n    Args:\n        button: The wx.Button-like control.\n    \"\"\"\n    self.default_button = button\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.set_focus","title":"set_focus","text":"<pre><code>set_focus()\n</code></pre> <p>Restore focus to the last focused control or this window.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def set_focus(self):\n    \"\"\"Restore focus to the last focused control or this window.\"\"\"\n    if self.last_control_with_focus:\n        self.last_control_with_focus.SetFocus()\n    else:\n        super().SetFocus()\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.set_new_href","title":"set_new_href","text":"<pre><code>set_new_href(href)\n</code></pre> <p>Change the source address.</p> <p>Parameters:</p> Name Type Description Default <code>href</code> <p>New address string.</p> required Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def set_new_href(self, href):\n    \"\"\"Change the source address.\n\n    Args:\n        href: New address string.\n    \"\"\"\n    self.address_or_parser = href\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.set_page","title":"set_page","text":"<pre><code>set_page(page_source)\n</code></pre> <p>Replace the body form's HTML content.</p> <p>Parameters:</p> Name Type Description Default <code>page_source</code> <p>New HTML string.</p> required Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def set_page(self, page_source):\n    \"\"\"Replace the body form's HTML content.\n\n    Args:\n        page_source: New HTML string.\n    \"\"\"\n    self.body.set_page(page_source)\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.set_vertical_position","title":"set_vertical_position","text":"<pre><code>set_vertical_position(position)\n</code></pre> <p>Set the placement preference for child forms.</p> <p>Parameters:</p> Name Type Description Default <code>position</code> <p>'top', 'bottom', None, or 'default'.</p> required Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def set_vertical_position(self, position):\n    \"\"\"Set the placement preference for child forms.\n\n    Args:\n        position: 'top', 'bottom', None, or 'default'.\n    \"\"\"\n    self.vertical_position = position\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.test_ctrl","title":"test_ctrl","text":"<pre><code>test_ctrl(name)\n</code></pre> <p>Check if a widget exists in the current lifecycle.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <p>Unique widget name.</p> required <p>Returns:</p> Type Description <p>True if the widget exists.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def test_ctrl(self, name):\n    \"\"\"Check if a widget exists in the current lifecycle.\n\n    Args:\n        name: Unique widget name.\n\n    Returns:\n        True if the widget exists.\n    \"\"\"\n    return name in self._ctrl_dict\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.SchPage.unreg_application_signal_handler","title":"unreg_application_signal_handler","text":"<pre><code>unreg_application_signal_handler(signal)\n</code></pre> <p>Unregister a previously registered signal handler.</p> <p>Parameters:</p> Name Type Description Default <code>signal</code> <p>Signal name.</p> required Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def unreg_application_signal_handler(self, signal):\n    \"\"\"Unregister a previously registered signal handler.\n\n    Args:\n        signal: Signal name.\n    \"\"\"\n    i = 0\n    found = None\n    for pos in self._signal_handlers:\n        if pos[1] == signal:\n            found = pos\n            break\n        i += 1\n    if found:\n        dispatcher.disconnect(found[0], found[1], sender=dispatcher.Any)\n        del self._signal_handlers[i]\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.Signal","title":"Signal","text":"<pre><code>Signal()\n</code></pre> <p>Simple signal/slot mechanism for communication between windows.</p> <p>Registered objects must have a method matching the signal name. That method must accept the arguments passed to :meth:<code>signal</code>.</p> <p>Usage::</p> <pre><code>sig = Signal()\nsig.register_signal(my_window, \"on_data_ready\")\nsig.signal(\"on_data_ready\", data=some_data)\nsig.unregister_signal(my_window, \"on_data_ready\")\n</code></pre> <p>Initialize an empty signal registry.</p> Source code in <code>pytigon_gui/guilib/signal.py</code> <pre><code>def __init__(self):\n    \"\"\"Initialize an empty signal registry.\"\"\"\n    self._signals = {}\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.Signal-functions","title":"Methods:","text":""},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.Signal.register_signal","title":"register_signal","text":"<pre><code>register_signal(obj, signal_name)\n</code></pre> <p>Register an object to receive a signal.</p> <p>The object must have a method with the name signal_name that accepts the arguments passed to :meth:<code>signal</code>.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <p>Object to receive signals.</p> required <code>signal_name</code> <p>Name of the signal (also the method name on obj).</p> required Source code in <code>pytigon_gui/guilib/signal.py</code> <pre><code>def register_signal(self, obj, signal_name):\n    \"\"\"Register an object to receive a signal.\n\n    The object must have a method with the name *signal_name* that\n    accepts the arguments passed to :meth:`signal`.\n\n    Args:\n        obj: Object to receive signals.\n        signal_name: Name of the signal (also the method name on obj).\n    \"\"\"\n    if signal_name not in self._signals:\n        self._signals[signal_name] = []\n    if obj not in self._signals[signal_name]:\n        self._signals[signal_name].append(obj)\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.Signal.signal","title":"signal","text":"<pre><code>signal(signal_name, *argi, **argv)\n</code></pre> <p>Send a signal to all registered listeners.</p> <p>Parameters:</p> Name Type Description Default <code>signal_name</code> <p>Name of the signal/method to call.</p> required <code>*argi</code> <p>Positional arguments forwarded to the handler.</p> <code>()</code> <code>**argv</code> <p>Keyword arguments forwarded to the handler.</p> <code>{}</code> <p>Returns:</p> Type Description <p>List of return values from handlers (excluding None values).</p> Source code in <code>pytigon_gui/guilib/signal.py</code> <pre><code>def signal(self, signal_name, *argi, **argv):\n    \"\"\"Send a signal to all registered listeners.\n\n    Args:\n        signal_name: Name of the signal/method to call.\n        *argi: Positional arguments forwarded to the handler.\n        **argv: Keyword arguments forwarded to the handler.\n\n    Returns:\n        List of return values from handlers (excluding None values).\n    \"\"\"\n    ret = []\n    if signal_name in self._signals:\n        for obj in self._signals[signal_name]:\n            if (x := getattr(obj, signal_name)(*argi, **argv)) is not None:\n                ret.append(x)\n    return ret\n</code></pre>"},{"location":"api/guiframe_page/#pytigon_gui.guiframe.page.Signal.unregister_signal","title":"unregister_signal","text":"<pre><code>unregister_signal(obj, signal_name)\n</code></pre> <p>Unregister an object from receiving a signal.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <p>Object to stop receiving signals.</p> required <code>signal_name</code> <p>Name of the signal.</p> required <p>Raises:</p> Type Description <code>KeyError</code> <p>If signal_name is not registered.</p> Source code in <code>pytigon_gui/guilib/signal.py</code> <pre><code>def unregister_signal(self, obj, signal_name):\n    \"\"\"Unregister an object from receiving a signal.\n\n    Args:\n        obj: Object to stop receiving signals.\n        signal_name: Name of the signal.\n\n    Raises:\n        KeyError: If *signal_name* is not registered.\n    \"\"\"\n    if signal_name not in self._signals:\n        return\n    signal_list = self._signals[signal_name]\n    if obj in signal_list:\n        idx = signal_list.index(obj)\n        del signal_list[idx]\n</code></pre>"},{"location":"api/guilib/","title":"guilib \u2013 GUI Library","text":"<p>Shared utility library: event system, HTTP error handling, image loading, login dialogs, signal dispatching, threading, and WebSocket.</p>"},{"location":"api/guilib/#pytigon_gui.guilib","title":"guilib","text":"<p>Utility libraries for the Pytigon GUI framework.</p> <p>Provides image handling, event IDs, HTTP error processing, login dialog, Pytigon installation utilities, signal dispatch, thread management, tools, and websocket client support.</p>"},{"location":"api/guilib_events/","title":"guilib.events \u2013 Event System","text":"<p>Custom event types and event handling infrastructure for inter-component communication.</p>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events","title":"events","text":"<p>Module containing all user-defined wx event IDs.</p> <p>Provides a centralized registry of wx.Window.NewControlId() identifiers used throughout the application for menu commands, toolbar actions, and custom events.</p>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events-attributes","title":"Attributes","text":""},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_BOOKMARK_ADD","title":"ID_BOOKMARK_ADD  <code>module-attribute</code>","text":"<pre><code>ID_BOOKMARK_ADD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_CLEAR_HISTORY","title":"ID_CLEAR_HISTORY  <code>module-attribute</code>","text":"<pre><code>ID_CLEAR_HISTORY = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_CLOSETAB","title":"ID_CLOSETAB  <code>module-attribute</code>","text":"<pre><code>ID_CLOSETAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_COPY","title":"ID_COPY  <code>module-attribute</code>","text":"<pre><code>ID_COPY = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_CUT","title":"ID_CUT  <code>module-attribute</code>","text":"<pre><code>ID_CUT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_DOWNLOAD_OPTIONS","title":"ID_DOWNLOAD_OPTIONS  <code>module-attribute</code>","text":"<pre><code>ID_DOWNLOAD_OPTIONS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_END","title":"ID_END  <code>module-attribute</code>","text":"<pre><code>ID_END = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_EXIT","title":"ID_EXIT  <code>module-attribute</code>","text":"<pre><code>ID_EXIT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_FIND","title":"ID_FIND  <code>module-attribute</code>","text":"<pre><code>ID_FIND = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_GENERAL_OPTIONS","title":"ID_GENERAL_OPTIONS  <code>module-attribute</code>","text":"<pre><code>ID_GENERAL_OPTIONS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_GOTODESKTOP","title":"ID_GOTODESKTOP  <code>module-attribute</code>","text":"<pre><code>ID_GOTODESKTOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_GOTOFOOTER","title":"ID_GOTOFOOTER  <code>module-attribute</code>","text":"<pre><code>ID_GOTOFOOTER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_GOTOHEADER","title":"ID_GOTOHEADER  <code>module-attribute</code>","text":"<pre><code>ID_GOTOHEADER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_GOTOPANEL","title":"ID_GOTOPANEL  <code>module-attribute</code>","text":"<pre><code>ID_GOTOPANEL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_HELP","title":"ID_HELP  <code>module-attribute</code>","text":"<pre><code>ID_HELP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_HISTORY_SHOW","title":"ID_HISTORY_SHOW  <code>module-attribute</code>","text":"<pre><code>ID_HISTORY_SHOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_LOAD","title":"ID_LOAD  <code>module-attribute</code>","text":"<pre><code>ID_LOAD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_NEXTPAGE","title":"ID_NEXTPAGE  <code>module-attribute</code>","text":"<pre><code>ID_NEXTPAGE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_NEXTTAB","title":"ID_NEXTTAB  <code>module-attribute</code>","text":"<pre><code>ID_NEXTTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_PAGE_BLOCK","title":"ID_PAGE_BLOCK  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_BLOCK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_PAGE_SOURCE","title":"ID_PAGE_SOURCE  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_SOURCE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_PAGE_VIEW","title":"ID_PAGE_VIEW  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_VIEW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_PASTE","title":"ID_PASTE  <code>module-attribute</code>","text":"<pre><code>ID_PASTE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_PREVPAGE","title":"ID_PREVPAGE  <code>module-attribute</code>","text":"<pre><code>ID_PREVPAGE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_PREVTAB","title":"ID_PREVTAB  <code>module-attribute</code>","text":"<pre><code>ID_PREVTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_PRINT","title":"ID_PRINT  <code>module-attribute</code>","text":"<pre><code>ID_PRINT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_PRINT_PREVIEW","title":"ID_PRINT_PREVIEW  <code>module-attribute</code>","text":"<pre><code>ID_PRINT_PREVIEW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_PRINT_SETTINGS","title":"ID_PRINT_SETTINGS  <code>module-attribute</code>","text":"<pre><code>ID_PRINT_SETTINGS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_REDO","title":"ID_REDO  <code>module-attribute</code>","text":"<pre><code>ID_REDO = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_REFRESHTAB","title":"ID_REFRESHTAB  <code>module-attribute</code>","text":"<pre><code>ID_REFRESHTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_REPLACE","title":"ID_REPLACE  <code>module-attribute</code>","text":"<pre><code>ID_REPLACE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_RESET","title":"ID_RESET  <code>module-attribute</code>","text":"<pre><code>ID_RESET = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_RETVALUE","title":"ID_RETVALUE  <code>module-attribute</code>","text":"<pre><code>ID_RETVALUE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_SAVE","title":"ID_SAVE  <code>module-attribute</code>","text":"<pre><code>ID_SAVE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_SAVE_AS","title":"ID_SAVE_AS  <code>module-attribute</code>","text":"<pre><code>ID_SAVE_AS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_SELALL","title":"ID_SELALL  <code>module-attribute</code>","text":"<pre><code>ID_SELALL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_SHOWDESKTOP","title":"ID_SHOWDESKTOP  <code>module-attribute</code>","text":"<pre><code>ID_SHOWDESKTOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_SHOWFOOTER","title":"ID_SHOWFOOTER  <code>module-attribute</code>","text":"<pre><code>ID_SHOWFOOTER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_SHOWHEADER","title":"ID_SHOWHEADER  <code>module-attribute</code>","text":"<pre><code>ID_SHOWHEADER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_SHOWPANEL","title":"ID_SHOWPANEL  <code>module-attribute</code>","text":"<pre><code>ID_SHOWPANEL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_SHOWSTATUSBAR","title":"ID_SHOWSTATUSBAR  <code>module-attribute</code>","text":"<pre><code>ID_SHOWSTATUSBAR = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_SHOWTOOLBAR1","title":"ID_SHOWTOOLBAR1  <code>module-attribute</code>","text":"<pre><code>ID_SHOWTOOLBAR1 = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_SHOWTOOLBAR2","title":"ID_SHOWTOOLBAR2  <code>module-attribute</code>","text":"<pre><code>ID_SHOWTOOLBAR2 = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_SHOW_BOOKMARKS","title":"ID_SHOW_BOOKMARKS  <code>module-attribute</code>","text":"<pre><code>ID_SHOW_BOOKMARKS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_SHOW_DOWNLOAD","title":"ID_SHOW_DOWNLOAD  <code>module-attribute</code>","text":"<pre><code>ID_SHOW_DOWNLOAD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_START","title":"ID_START  <code>module-attribute</code>","text":"<pre><code>ID_START = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_TASKBAR_CLOSE","title":"ID_TASKBAR_CLOSE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_CLOSE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_TASKBAR_HIDE","title":"ID_TASKBAR_HIDE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_HIDE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_TASKBAR_SHOW","title":"ID_TASKBAR_SHOW  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_SHOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_TASKBAR_TOGGLE","title":"ID_TASKBAR_TOGGLE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_TOGGLE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_UNDO","title":"ID_UNDO  <code>module-attribute</code>","text":"<pre><code>ID_UNDO = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_WEB_ADDBOOKMARK","title":"ID_WEB_ADDBOOKMARK  <code>module-attribute</code>","text":"<pre><code>ID_WEB_ADDBOOKMARK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_WEB_BACK","title":"ID_WEB_BACK  <code>module-attribute</code>","text":"<pre><code>ID_WEB_BACK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_WEB_DOWN","title":"ID_WEB_DOWN  <code>module-attribute</code>","text":"<pre><code>ID_WEB_DOWN = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_WEB_EDIT","title":"ID_WEB_EDIT  <code>module-attribute</code>","text":"<pre><code>ID_WEB_EDIT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_WEB_FORWARD","title":"ID_WEB_FORWARD  <code>module-attribute</code>","text":"<pre><code>ID_WEB_FORWARD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_WEB_NEW_WINDOW","title":"ID_WEB_NEW_WINDOW  <code>module-attribute</code>","text":"<pre><code>ID_WEB_NEW_WINDOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_WEB_REFRESH","title":"ID_WEB_REFRESH  <code>module-attribute</code>","text":"<pre><code>ID_WEB_REFRESH = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_WEB_SOURCE","title":"ID_WEB_SOURCE  <code>module-attribute</code>","text":"<pre><code>ID_WEB_SOURCE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_WEB_STOP","title":"ID_WEB_STOP  <code>module-attribute</code>","text":"<pre><code>ID_WEB_STOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_WEB_UP","title":"ID_WEB_UP  <code>module-attribute</code>","text":"<pre><code>ID_WEB_UP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_events/#pytigon_gui.guilib.events.ID_ZOOM","title":"ID_ZOOM  <code>module-attribute</code>","text":"<pre><code>ID_ZOOM = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/guilib_httperror/","title":"guilib.httperror \u2013 HTTP Error Handling","text":"<p>HTTP error display and recovery: shows user-friendly error dialogs for connection failures and server errors.</p>"},{"location":"api/guilib_httperror/#pytigon_gui.guilib.httperror","title":"httperror","text":"<p>HTTP error dialog module.</p> <p>Provides a dialog for displaying HTTP error responses to the user, with options to continue or break the application.</p>"},{"location":"api/guilib_httperror/#pytigon_gui.guilib.httperror-attributes","title":"Attributes","text":""},{"location":"api/guilib_httperror/#pytigon_gui.guilib.httperror._","title":"_  <code>module-attribute</code>","text":"<pre><code>_ = wx.GetTranslation\n</code></pre>"},{"location":"api/guilib_httperror/#pytigon_gui.guilib.httperror.logger","title":"logger  <code>module-attribute</code>","text":"<pre><code>logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/guilib_httperror/#pytigon_gui.guilib.httperror-classes","title":"Classes","text":""},{"location":"api/guilib_httperror/#pytigon_gui.guilib.httperror.HttpErrorDialog","title":"HttpErrorDialog","text":"<pre><code>HttpErrorDialog(\n    parent,\n    title,\n    text,\n    size=DefaultSize,\n    pos=DefaultPosition,\n    style=DEFAULT_DIALOG_STYLE,\n    use_metal=False,\n)\n</code></pre> <p>               Bases: <code>Dialog</code></p> <p>Dialog that displays an HTML error page returned by an HTTP server.</p> <p>Provides Continue and Break buttons. Choosing Break terminates the application.</p> <p>Initialize the error dialog.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>title</code> <p>Dialog title.</p> required <code>text</code> <p>HTML content to display.</p> required <code>size</code> <p>Dialog size (default: wx.DefaultSize).</p> <code>DefaultSize</code> <code>pos</code> <p>Dialog position (default: wx.DefaultPosition).</p> <code>DefaultPosition</code> <code>style</code> <p>Dialog style flags.</p> <code>DEFAULT_DIALOG_STYLE</code> <code>use_metal</code> <p>Use metal look on macOS.</p> <code>False</code> Source code in <code>pytigon_gui/guilib/httperror.py</code> <pre><code>def __init__(\n    self,\n    parent,\n    title,\n    text,\n    size=wx.DefaultSize,\n    pos=wx.DefaultPosition,\n    style=wx.DEFAULT_DIALOG_STYLE,\n    use_metal=False,\n):\n    \"\"\"Initialize the error dialog.\n\n    Args:\n        parent: Parent window.\n        title: Dialog title.\n        text: HTML content to display.\n        size: Dialog size (default: wx.DefaultSize).\n        pos: Dialog position (default: wx.DefaultPosition).\n        style: Dialog style flags.\n        use_metal: Use metal look on macOS.\n    \"\"\"\n    try:\n        pre = wx.PreDialog()\n        pre.SetExtraStyle(wx.DIALOG_EX_CONTEXTHELP)\n        pre.Create(parent, wx.ID_ANY, title, pos, size, style)\n        self.PostCreate(pre)\n    except Exception:\n        wx.Dialog.__init__(self, parent, wx.ID_ANY, title, pos, size, style)\n\n    if \"wxMac\" in wx.PlatformInfo and use_metal:\n        self.SetExtraStyle(wx.DIALOG_EX_METAL)\n\n    sizer = wx.BoxSizer(wx.VERTICAL)\n\n    self.label = pytigon_gui.guictrl.ctrl.HTML2(\n        self, size=(1024, 768), name=\"webbrowser\"\n    )\n\n    try:\n        sizer.Add(self.label.wb, 0, wx.ALIGN_CENTRE | wx.ALL, 5)\n    except AttributeError:\n        sizer.Add(self.label, 0, wx.ALIGN_CENTRE | wx.ALL, 5)\n\n    btnsizer = wx.StdDialogButtonSizer()\n    btn = wx.Button(self, wx.ID_OK, _(\"Continue\"))\n    btn.SetDefault()\n    btnsizer.AddButton(btn)\n    btn = wx.Button(self, wx.ID_CANCEL, _(\"Break\"))\n    btn.SetHelpText(_(\"The Break button breaks the application\"))\n    btnsizer.AddButton(btn)\n    btnsizer.Realize()\n    sizer.Add(btnsizer, 0, wx.ALL, 5)\n\n    self.SetSizer(sizer)\n    sizer.Fit(self)\n\n    wx.CallAfter(self.label.load_str, text)\n</code></pre>"},{"location":"api/guilib_httperror/#pytigon_gui.guilib.httperror.HttpErrorDialog-functions","title":"Methods:","text":""},{"location":"api/guilib_httperror/#pytigon_gui.guilib.httperror.HttpErrorDialog.set_acc_key_tab","title":"set_acc_key_tab","text":"<pre><code>set_acc_key_tab(ctrl, tab)\n</code></pre> <p>Placeholder for keyboard accelerator setup (not used).</p> Source code in <code>pytigon_gui/guilib/httperror.py</code> <pre><code>def set_acc_key_tab(self, ctrl, tab):\n    \"\"\"Placeholder for keyboard accelerator setup (not used).\"\"\"\n    pass\n</code></pre>"},{"location":"api/guilib_httperror/#pytigon_gui.guilib.httperror-functions","title":"Functions:","text":""},{"location":"api/guilib_httperror/#pytigon_gui.guilib.httperror._http_error","title":"_http_error","text":"<pre><code>_http_error(parent, content)\n</code></pre> <p>Show a modal dialog with error content returned by an HTTP server.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>content</code> <p>HTML page returned by HTTP server (str or bytes).</p> required Source code in <code>pytigon_gui/guilib/httperror.py</code> <pre><code>def _http_error(parent, content):\n    \"\"\"Show a modal dialog with error content returned by an HTTP server.\n\n    Args:\n        parent: Parent window.\n        content: HTML page returned by HTTP server (str or bytes).\n    \"\"\"\n    app = wx.GetApp()\n    if app.lock:\n        return\n\n    app.lock = True\n    val = None\n\n    try:\n        if parent and hasattr(parent, \"Invalidate\"):\n            parent.Invalidate()\n\n        if isinstance(content, str):\n            c = content\n        else:\n            c = content.decode(\"utf-8\")\n\n        dlg = HttpErrorDialog(\n            app.GetTopWindow(), _(\"Error message\"), c, size=(800, 600)\n        )\n        dlg.CenterOnScreen()\n        val = dlg.ShowModal()\n    except Exception as e:\n        logger.error(\"Error displaying HTTP error dialog: %s\", e)\n    finally:\n        app.lock = False\n\n    if val == wx.ID_CANCEL:\n        sys.exit()\n</code></pre>"},{"location":"api/guilib_httperror/#pytigon_gui.guilib.httperror.http_error","title":"http_error","text":"<pre><code>http_error(parent, content)\n</code></pre> <p>Schedule an HTTP error dialog to be shown in the main GUI thread.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>content</code> <p>HTML error page content.</p> required Source code in <code>pytigon_gui/guilib/httperror.py</code> <pre><code>def http_error(parent, content):\n    \"\"\"Schedule an HTTP error dialog to be shown in the main GUI thread.\n\n    Args:\n        parent: Parent window.\n        content: HTML error page content.\n    \"\"\"\n    wx.CallAfter(_http_error, parent, content)\n</code></pre>"},{"location":"api/guilib_image/","title":"guilib.image \u2013 Image Management","text":"<p>Image loading, caching, scaling, and format conversion using Pillow with wxPython bitmap integration.</p>"},{"location":"api/guilib_image/#pytigon_gui.guilib.image","title":"image","text":"<p>Module contains helper classes and functions for image operations.</p> <p>Provides conversion utilities between wx.Bitmap, wx.Image, and PIL images, as well as functions to load bitmaps from ArtProviders, local files, fonts, and remote HTTP sources.</p>"},{"location":"api/guilib_image/#pytigon_gui.guilib.image-attributes","title":"Attributes","text":""},{"location":"api/guilib_image/#pytigon_gui.guilib.image.SIZE_DEFAULT","title":"SIZE_DEFAULT  <code>module-attribute</code>","text":"<pre><code>SIZE_DEFAULT = -1\n</code></pre>"},{"location":"api/guilib_image/#pytigon_gui.guilib.image.SIZE_LARGE","title":"SIZE_LARGE  <code>module-attribute</code>","text":"<pre><code>SIZE_LARGE = 2\n</code></pre>"},{"location":"api/guilib_image/#pytigon_gui.guilib.image.SIZE_MEDIUM","title":"SIZE_MEDIUM  <code>module-attribute</code>","text":"<pre><code>SIZE_MEDIUM = 1\n</code></pre>"},{"location":"api/guilib_image/#pytigon_gui.guilib.image.SIZE_SMALL","title":"SIZE_SMALL  <code>module-attribute</code>","text":"<pre><code>SIZE_SMALL = 0\n</code></pre>"},{"location":"api/guilib_image/#pytigon_gui.guilib.image._","title":"_  <code>module-attribute</code>","text":"<pre><code>_ = wx.GetTranslation\n</code></pre>"},{"location":"api/guilib_image/#pytigon_gui.guilib.image._WX_ART_MAP","title":"_WX_ART_MAP  <code>module-attribute</code>","text":"<pre><code>_WX_ART_MAP = {\n    \"wx.ART_ADD_BOOKMARK\": wx.ART_ADD_BOOKMARK,\n    \"wx.ART_COPY\": wx.ART_COPY,\n    \"wx.ART_CUT\": wx.ART_CUT,\n    \"wx.ART_DELETE\": wx.ART_DELETE,\n    \"wx.ART_FILE_OPEN\": wx.ART_FILE_OPEN,\n    \"wx.ART_FILE_SAVE\": wx.ART_FILE_SAVE,\n    \"wx.ART_FILE_SAVE_AS\": wx.ART_FILE_SAVE_AS,\n    \"wx.ART_FIND\": wx.ART_FIND,\n    \"wx.ART_FIND_AND_REPLACE\": wx.ART_FIND_AND_REPLACE,\n    \"wx.ART_FRAME_ICON\": wx.ART_FRAME_ICON,\n    \"wx.ART_GO_BACK\": wx.ART_GO_BACK,\n    \"wx.ART_GO_DOWN\": wx.ART_GO_DOWN,\n    \"wx.ART_GO_FORWARD\": wx.ART_GO_FORWARD,\n    \"wx.ART_GO_HOME\": wx.ART_GO_HOME,\n    \"wx.ART_GO_TO_PARENT\": wx.ART_GO_TO_PARENT,\n    \"wx.ART_GO_UP\": wx.ART_GO_UP,\n    \"wx.ART_HELP\": wx.ART_HELP,\n    \"wx.ART_MISSING_IMAGE\": wx.ART_MISSING_IMAGE,\n    \"wx.ART_NEW\": wx.ART_NEW,\n    \"wx.ART_PASTE\": wx.ART_PASTE,\n    \"wx.ART_PRINT\": wx.ART_PRINT,\n    \"wx.ART_QUIT\": wx.ART_QUIT,\n    \"wx.ART_REDO\": wx.ART_REDO,\n    \"wx.ART_TIP\": wx.ART_TIP,\n    \"wx.ART_UNDO\": wx.ART_UNDO,\n}\n</code></pre>"},{"location":"api/guilib_image/#pytigon_gui.guilib.image.logger","title":"logger  <code>module-attribute</code>","text":"<pre><code>logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/guilib_image/#pytigon_gui.guilib.image-classes","title":"Classes","text":""},{"location":"api/guilib_image/#pytigon_gui.guilib.image.ArtProviderFromIcon","title":"ArtProviderFromIcon","text":"<pre><code>ArtProviderFromIcon()\n</code></pre> <p>               Bases: <code>ArtProvider</code></p> <p>Extended ArtProvider that loads bitmaps from a local icon library.</p> <p>Reads icon identifiers from static/icons/ids.txt and caches bitmaps at 16px, 22px, and 32px sizes.</p> Source code in <code>pytigon_gui/guilib/image.py</code> <pre><code>def __init__(self):\n    wx.ArtProvider.__init__(self)\n    self.tab_16 = {}\n    self.tab_22 = {}\n    self.tab_32 = {}\n\n    ids_path = str(wx.GetApp().src_path) + \"/static/icons/ids.txt\"\n    try:\n        with open(ids_path) as ids:\n            for line in ids:\n                line = line.strip()\n                if not line:\n                    continue\n                l = line.split(\",\")\n                if len(l) &gt; 1:\n                    image_path = l[1]\n                    art_key = \"wx.ART_\" + l[0]\n                    if (art_id := _resolve_art_id(art_key)) is not None:\n                        self.tab_16[art_id] = [image_path, None]\n                        self.tab_22[art_id] = [image_path, None]\n                        self.tab_32[art_id] = [image_path, None]\n    except FileNotFoundError:\n        logger.warning(\"Icon id file not found: %s\", ids_path)\n    except Exception:\n        logger.exception(\"Error loading icon id file: %s\", ids_path)\n</code></pre>"},{"location":"api/guilib_image/#pytigon_gui.guilib.image.ArtProviderFromIcon-functions","title":"Methods:","text":""},{"location":"api/guilib_image/#pytigon_gui.guilib.image.ArtProviderFromIcon.CreateBitmap","title":"CreateBitmap","text":"<pre><code>CreateBitmap(artid2, client, size)\n</code></pre> <p>Create and cache a bitmap for the given art id and size.</p> <p>Parameters:</p> Name Type Description Default <code>artid2</code> <p>Art id (string).</p> required <code>client</code> <p>Client identifier (unused).</p> required <code>size</code> <p>Requested size in pixels.</p> required <p>Returns:</p> Type Description <p>wx.Bitmap or wx.NullBitmap.</p> Source code in <code>pytigon_gui/guilib/image.py</code> <pre><code>def CreateBitmap(self, artid2, client, size):\n    \"\"\"Create and cache a bitmap for the given art id and size.\n\n    Args:\n        artid2: Art id (string).\n        client: Client identifier (unused).\n        size: Requested size in pixels.\n\n    Returns:\n        wx.Bitmap or wx.NullBitmap.\n    \"\"\"\n    artid = artid2.encode(\"utf-8\")\n    if size not in (16, 22, 32):\n        size = 32\n    path = None\n    cached_table = None\n\n    if size == 16:\n        cached_table = self.tab_16\n        if artid in self.tab_16:\n            if self.tab_16[artid][1]:\n                return self.tab_16[artid][1]\n            path = \"png://\" + self.tab_16[artid][0] + \"?size=0\"\n    elif size == 22:\n        cached_table = self.tab_22\n        if artid in self.tab_22:\n            if self.tab_22[artid][1]:\n                return self.tab_22[artid][1]\n            path = \"png://\" + self.tab_22[artid][0] + \"?size=1\"\n    else:\n        cached_table = self.tab_32\n        if artid in self.tab_32:\n            if self.tab_32[artid][1]:\n                return self.tab_32[artid][1]\n            path = \"png://\" + self.tab_32[artid][0] + \"?size=2\"\n\n    if path is None:\n        return wx.NullBitmap\n\n    try:\n        bitmap = bitmap_from_href(path)\n    except Exception:\n        bitmap = None\n\n    if bitmap and cached_table and artid in cached_table:\n        cached_table[artid][1] = bitmap\n        return bitmap\n    return wx.NullBitmap\n</code></pre>"},{"location":"api/guilib_image/#pytigon_gui.guilib.image.SchImage","title":"SchImage","text":"<pre><code>SchImage(address, one_image_size=(20, 20))\n</code></pre> <p>Extract individual bitmaps from a sprite sheet image.</p> <p>All partial bitmaps should be the same size. Between bitmaps there should be a gap of 1 pixel width.</p> <p>Usage::</p> <pre><code>images = SchImage(\"http://www.page.com\", (32, 32))\nimage1 = images[0]\nimage2 = images[2]\n</code></pre> <p>Initialize and load the sprite sheet.</p> <p>Parameters:</p> Name Type Description Default <code>address</code> <p>URL of the sprite sheet image.</p> required <code>one_image_size</code> <p>(width, height) of each sub-image.</p> <code>(20, 20)</code> Source code in <code>pytigon_gui/guilib/image.py</code> <pre><code>def __init__(self, address, one_image_size=(20, 20)):\n    \"\"\"Initialize and load the sprite sheet.\n\n    Args:\n        address: URL of the sprite sheet image.\n        one_image_size: (width, height) of each sub-image.\n    \"\"\"\n    self.one_image_size = one_image_size\n    try:\n        http = wx.GetApp().http\n        response = http.get(self, address)\n        if response.ret_code != 200:\n            logger.warning(\"Cannot load image from: %s\", address)\n            self.bmp = wx.ArtProvider.GetBitmap(\n                wx.ART_MISSING_IMAGE, wx.ART_TOOLBAR, (32, 32)\n            )\n        else:\n            data = response.ptr()\n            stream = io.BytesIO(data)\n            self.bmp = wx.Bitmap(wx.Image(stream))\n    except Exception:\n        logger.exception(\"Exception while loading image from: %s\", address)\n        self.bmp = wx.ArtProvider.GetBitmap(\n            wx.ART_MISSING_IMAGE, wx.ART_TOOLBAR, (32, 32)\n        )\n</code></pre>"},{"location":"api/guilib_image/#pytigon_gui.guilib.image.SchImage-functions","title":"Methods:","text":""},{"location":"api/guilib_image/#pytigon_gui.guilib.image-functions","title":"Functions:","text":""},{"location":"api/guilib_image/#pytigon_gui.guilib.image._resolve_art_id","title":"_resolve_art_id","text":"<pre><code>_resolve_art_id(art_id_str)\n</code></pre> <p>Safely resolve a wx.ART_* string to its wxPython constant.</p> <p>Parameters:</p> Name Type Description Default <code>art_id_str</code> <p>String like 'wx.ART_COPY'.</p> required <p>Returns:</p> Type Description <p>wxPython art id constant, or None if not found.</p> Source code in <code>pytigon_gui/guilib/image.py</code> <pre><code>def _resolve_art_id(art_id_str):\n    \"\"\"Safely resolve a wx.ART_* string to its wxPython constant.\n\n    Args:\n        art_id_str: String like 'wx.ART_COPY'.\n\n    Returns:\n        wxPython art id constant, or None if not found.\n    \"\"\"\n    if art_id_str in _WX_ART_MAP:\n        return _WX_ART_MAP[art_id_str]\n    try:\n        return getattr(wx, art_id_str.split(\".\")[1], None)\n    except (IndexError, AttributeError):\n        return None\n</code></pre>"},{"location":"api/guilib_image/#pytigon_gui.guilib.image.bitmap_from_art_id","title":"bitmap_from_art_id","text":"<pre><code>bitmap_from_art_id(art_id, size)\n</code></pre> <p>Get bitmap from ArtProvider.</p> <p>Parameters:</p> Name Type Description Default <code>art_id</code> <p>Bitmap id in ArtProvider.</p> required <code>size</code> <p>wx.Size or (width, height) tuple.</p> required <p>Returns:</p> Type Description <p>wx.Bitmap.</p> Source code in <code>pytigon_gui/guilib/image.py</code> <pre><code>def bitmap_from_art_id(art_id, size):\n    \"\"\"Get bitmap from ArtProvider.\n\n    Args:\n        art_id: Bitmap id in ArtProvider.\n        size: wx.Size or (width, height) tuple.\n\n    Returns:\n        wx.Bitmap.\n    \"\"\"\n    return wx.ArtProvider.GetBitmap(art_id, wx.ART_OTHER, size)\n</code></pre>"},{"location":"api/guilib_image/#pytigon_gui.guilib.image.bitmap_from_href","title":"bitmap_from_href","text":"<pre><code>bitmap_from_href(href, size_type=SIZE_DEFAULT)\n</code></pre> <p>Load a resource and convert it to a wx.Bitmap.</p> <p>Parameters:</p> Name Type Description Default <code>href</code> <p>Resource identifier. Supported formats: - ArtProvider id, e.g. 'wx.ART_COPY' - Font Awesome id, e.g. 'fa://icon_name' or 'fa://bus' - Local library image, e.g. 'png://image_name.png' - Client-side image, e.g. 'client://image_name.png' - Inline SVG data, e.g. 'data:image/svg+xml,...' - Remote HTTP/HTTPS URL, e.g. 'http://www.pytigon.cloud/test.png'</p> required <code>size_type</code> <p>One of SIZE_DEFAULT, SIZE_SMALL, SIZE_MEDIUM, SIZE_LARGE.</p> <code>SIZE_DEFAULT</code> <p>Returns:</p> Type Description <p>wx.Bitmap. If the resource cannot be loaded, ART_MISSING_IMAGE is</p> <p>returned.</p> Source code in <code>pytigon_gui/guilib/image.py</code> <pre><code>def bitmap_from_href(href, size_type=SIZE_DEFAULT):\n    \"\"\"Load a resource and convert it to a wx.Bitmap.\n\n    Args:\n        href: Resource identifier. Supported formats:\n            - ArtProvider id, e.g. 'wx.ART_COPY'\n            - Font Awesome id, e.g. 'fa://icon_name' or 'fa://bus'\n            - Local library image, e.g. 'png://image_name.png'\n            - Client-side image, e.g. 'client://image_name.png'\n            - Inline SVG data, e.g. 'data:image/svg+xml,...'\n            - Remote HTTP/HTTPS URL, e.g. 'http://www.pytigon.cloud/test.png'\n        size_type: One of SIZE_DEFAULT, SIZE_SMALL, SIZE_MEDIUM, SIZE_LARGE.\n\n    Returns:\n        wx.Bitmap. If the resource cannot be loaded, ART_MISSING_IMAGE is\n        returned.\n    \"\"\"\n    size_type2 = size_type\n    if \"?\" in href:\n        x = href.split(\"?\")\n        href2 = x[0]\n        if \"=\" in x[1]:\n            try:\n                size_type2 = int(x[1].split(\"=\")[1])\n            except (ValueError, IndexError):\n                size_type2 = 1\n    else:\n        href2 = href\n\n    if size_type2 == SIZE_SMALL:\n        icon_size = 16\n    elif size_type2 == SIZE_MEDIUM:\n        icon_size = 22\n    else:\n        icon_size = 32\n\n    if href2[:3] == \"wx.\":\n        if (art_id := _resolve_art_id(href2)) is not None:\n            bmp = wx.ArtProvider.GetBitmap(\n                art_id, wx.ART_TOOLBAR, (icon_size, icon_size)\n            )\n        else:\n            bmp = wx.ArtProvider.GetBitmap(\n                wx.ART_MISSING_IMAGE, wx.ART_TOOLBAR, (icon_size, icon_size)\n            )\n    elif href.startswith(\"client://\"):\n        image = wx.Image(\n            str(wx.GetApp().src_path)\n            + f\"/static/icons/{icon_size}x{icon_size}/\"\n            + href2[9:]\n        )\n        bmp = wx.Bitmap(image)\n    elif href.startswith(\"png://\"):\n        image = wx.Image(\n            str(wx.GetApp().src_path)\n            + f\"/static/icons/{icon_size}x{icon_size}/\"\n            + href2[6:]\n        )\n        bmp = wx.Bitmap(image)\n    elif href.startswith(\"fa://\"):\n        suffix = \"\" if \".png\" in href.lower() else \".png\"\n        try:\n            image = wx.Image(\n                str(wx.GetApp().src_path)\n                + f\"/static/fonts/fork-awesome/fonts/{icon_size}x{icon_size}/\"\n                + href2[5:].replace(\"fa-\", \"\")\n                + suffix\n            )\n            image = image.AdjustChannels(1, 1, 1, 0.55)\n            bmp = wx.Bitmap(image)\n        except Exception:\n            logger.warning(\"Cannot load image: %s\", href2)\n            bmp = wx.Bitmap()\n    elif href.startswith(\"data:image/svg+xml\"):\n        x = href.split(\",\", 1)\n        svg_code = x[1]\n        svg = SVGimage.CreateFromBytes(svg_code.encode(\"utf-8\"))\n        bmp = svg.ConvertToBitmap(width=icon_size, height=icon_size)\n    else:\n        if href.lower().startswith(\"http\"):\n            http = wx.GetApp().get_http(None)\n            response = http.get(None, str(href))\n            if response.ret_code != 404:\n                s = response.ptr()\n                stream = BytesIO(s)\n                bmp = wx.Bitmap(wx.Image(stream))\n            else:\n                bmp = wx.ArtProvider.GetBitmap(\n                    wx.ART_MISSING_IMAGE, wx.ART_TOOLBAR, (32, 32)\n                )\n        else:\n            bmp = wx.ArtProvider.GetBitmap(\n                wx.ART_MISSING_IMAGE, wx.ART_TOOLBAR, (32, 32)\n            )\n    return bmp\n</code></pre>"},{"location":"api/guilib_image/#pytigon_gui.guilib.image.bitmap_to_image","title":"bitmap_to_image","text":"<pre><code>bitmap_to_image(bitmap)\n</code></pre> <p>Convert wx.Bitmap object to wx.Image object.</p> Source code in <code>pytigon_gui/guilib/image.py</code> <pre><code>def bitmap_to_image(bitmap):\n    \"\"\"Convert wx.Bitmap object to wx.Image object.\"\"\"\n    return wx.ImageFromBitmap(bitmap)\n</code></pre>"},{"location":"api/guilib_image/#pytigon_gui.guilib.image.bitmap_to_pil","title":"bitmap_to_pil","text":"<pre><code>bitmap_to_pil(bitmap)\n</code></pre> <p>Convert wx.Bitmap object to PIL Image.</p> Source code in <code>pytigon_gui/guilib/image.py</code> <pre><code>def bitmap_to_pil(bitmap):\n    \"\"\"Convert wx.Bitmap object to PIL Image.\"\"\"\n    return image_to_pil(bitmap_to_image(bitmap))\n</code></pre>"},{"location":"api/guilib_image/#pytigon_gui.guilib.image.bitmaps_from_art_id","title":"bitmaps_from_art_id","text":"<pre><code>bitmaps_from_art_id(art_id, size)\n</code></pre> <p>Get a tuple of two bitmaps from ArtProvider.</p> <p>The first is the standard bitmap, the second is the same bitmap converted to grayscale to emulate a disabled state.</p> <p>Parameters:</p> Name Type Description Default <code>art_id</code> <p>Bitmap id in ArtProvider.</p> required <code>size</code> <p>wx.Size or (width, height) tuple.</p> required <p>Returns:</p> Type Description <p>(bitmap, bitmap_disabled) tuple.</p> Source code in <code>pytigon_gui/guilib/image.py</code> <pre><code>def bitmaps_from_art_id(art_id, size):\n    \"\"\"Get a tuple of two bitmaps from ArtProvider.\n\n    The first is the standard bitmap, the second is the same bitmap\n    converted to grayscale to emulate a disabled state.\n\n    Args:\n        art_id: Bitmap id in ArtProvider.\n        size: wx.Size or (width, height) tuple.\n\n    Returns:\n        (bitmap, bitmap_disabled) tuple.\n    \"\"\"\n    bitmap = wx.ArtProvider.GetBitmap(art_id, wx.ART_OTHER, size)\n    img = bitmap.ConvertToImage()\n    bitmap_disabled = wx.Bitmap(img.ConvertToGreyscale().AdjustChannels(1, 1, 1, 0.3))\n    return (bitmap, bitmap_disabled)\n</code></pre>"},{"location":"api/guilib_image/#pytigon_gui.guilib.image.image_to_bitmap","title":"image_to_bitmap","text":"<pre><code>image_to_bitmap(image)\n</code></pre> <p>Convert wx.Image to wx.Bitmap.</p> Source code in <code>pytigon_gui/guilib/image.py</code> <pre><code>def image_to_bitmap(image):\n    \"\"\"Convert wx.Image to wx.Bitmap.\"\"\"\n    return image.ConvertToBitmap()\n</code></pre>"},{"location":"api/guilib_image/#pytigon_gui.guilib.image.image_to_pil","title":"image_to_pil","text":"<pre><code>image_to_pil(image)\n</code></pre> <p>Convert wx.Image to PIL Image.</p> Source code in <code>pytigon_gui/guilib/image.py</code> <pre><code>def image_to_pil(image):\n    \"\"\"Convert wx.Image to PIL Image.\"\"\"\n    pil = Image.new(\"RGB\", (image.GetWidth(), image.GetHeight()))\n    pil.frombytes(image.GetData())\n    return pil\n</code></pre>"},{"location":"api/guilib_image/#pytigon_gui.guilib.image.pil_to_bitmap","title":"pil_to_bitmap","text":"<pre><code>pil_to_bitmap(pil)\n</code></pre> <p>Convert PIL Image to wx.Bitmap.</p> Source code in <code>pytigon_gui/guilib/image.py</code> <pre><code>def pil_to_bitmap(pil):\n    \"\"\"Convert PIL Image to wx.Bitmap.\"\"\"\n    return image_to_bitmap(pil_to_image(pil))\n</code></pre>"},{"location":"api/guilib_image/#pytigon_gui.guilib.image.pil_to_image","title":"pil_to_image","text":"<pre><code>pil_to_image(pil, alpha=True)\n</code></pre> <p>Convert PIL Image to wx.Image.</p> <p>Parameters:</p> Name Type Description Default <code>pil</code> <p>PIL Image object.</p> required <code>alpha</code> <p>If True, preserve alpha channel.</p> <code>True</code> <p>Returns:</p> Type Description <p>wx.Image object.</p> Source code in <code>pytigon_gui/guilib/image.py</code> <pre><code>def pil_to_image(pil, alpha=True):\n    \"\"\"Convert PIL Image to wx.Image.\n\n    Args:\n        pil: PIL Image object.\n        alpha: If True, preserve alpha channel.\n\n    Returns:\n        wx.Image object.\n    \"\"\"\n    if alpha:\n        image = wx.Image(*pil.size)\n        image.SetData(pil.convert(\"RGB\").tobytes())\n        image.SetAlphaBuffer(pil.convert(\"RGBA\").tobytes()[3::4])\n    else:\n        image = wx.Image(pil.size[0], pil.size[1])\n        new_image = pil.convert(\"RGB\")\n        data = new_image.tobytes()\n        image.SetData(data)\n    return image\n</code></pre>"},{"location":"api/guilib_logindialog/","title":"guilib.logindialog \u2013 Login Dialog","text":"<p>Authentication dialog with username/password fields, session token storage, and Django backend integration.</p>"},{"location":"api/guilib_logindialog/#pytigon_gui.guilib.logindialog","title":"logindialog","text":"<p>Login dialog module.</p> <p>Provides a simple username/password dialog for application authentication.</p>"},{"location":"api/guilib_logindialog/#pytigon_gui.guilib.logindialog-attributes","title":"Attributes","text":""},{"location":"api/guilib_logindialog/#pytigon_gui.guilib.logindialog._","title":"_  <code>module-attribute</code>","text":"<pre><code>_ = wx.GetTranslation\n</code></pre>"},{"location":"api/guilib_logindialog/#pytigon_gui.guilib.logindialog-classes","title":"Classes","text":""},{"location":"api/guilib_logindialog/#pytigon_gui.guilib.logindialog.LoginDialog","title":"LoginDialog","text":"<pre><code>LoginDialog(\n    parent,\n    id_,\n    title,\n    size=DefaultSize,\n    pos=DefaultPosition,\n    style=DEFAULT_DIALOG_STYLE,\n    use_metal=False,\n    username=None,\n)\n</code></pre> <p>               Bases: <code>Dialog</code></p> <p>Modal dialog for user login with username and password fields.</p> <p>Usage::</p> <pre><code>dlg = LoginDialog(parent, wx.ID_ANY, \"Login\")\nif dlg.ShowModal() == wx.ID_OK:\n    username = dlg.text1.GetValue()\n    password = dlg.text2.GetValue()\ndlg.Destroy()\n</code></pre> <p>Initialize the login dialog.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>id_</code> <p>Window identifier.</p> required <code>title</code> <p>Dialog title.</p> required <code>size</code> <p>Dialog size (default: wx.DefaultSize).</p> <code>DefaultSize</code> <code>pos</code> <p>Dialog position (default: wx.DefaultPosition).</p> <code>DefaultPosition</code> <code>style</code> <p>Dialog style flags.</p> <code>DEFAULT_DIALOG_STYLE</code> <code>use_metal</code> <p>Use metal look on macOS.</p> <code>False</code> <code>username</code> <p>Default username to pre-fill (optional).</p> <code>None</code> Source code in <code>pytigon_gui/guilib/logindialog.py</code> <pre><code>def __init__(\n    self,\n    parent,\n    id_,\n    title,\n    size=wx.DefaultSize,\n    pos=wx.DefaultPosition,\n    style=wx.DEFAULT_DIALOG_STYLE,\n    use_metal=False,\n    username=None,\n):\n    \"\"\"Initialize the login dialog.\n\n    Args:\n        parent: Parent window.\n        id_: Window identifier.\n        title: Dialog title.\n        size: Dialog size (default: wx.DefaultSize).\n        pos: Dialog position (default: wx.DefaultPosition).\n        style: Dialog style flags.\n        use_metal: Use metal look on macOS.\n        username: Default username to pre-fill (optional).\n    \"\"\"\n    wx.Dialog.__init__(self, parent, id_, title, pos, size, style)\n    self.CenterOnParent()\n\n    if \"wxMac\" in wx.PlatformInfo and use_metal:\n        self.SetExtraStyle(wx.DIALOG_EX_METAL)\n\n    sizer = wx.GridBagSizer(5, 2)\n\n    self.message = wx.StaticText(self, -1, \"\")\n    font = wx.Font(\n        10, wx.FONTFAMILY_DEFAULT, wx.FONTSTYLE_NORMAL, wx.FONTWEIGHT_BOLD\n    )\n    self.message.SetFont(font)\n\n    label1 = wx.StaticText(self, -1, _(\"User name:\"))\n    self.text1 = wx.TextCtrl(self, -1, \"\", size=(180, -1))\n    if username:\n        self.text1.SetValue(username)\n\n    label2 = wx.StaticText(self, -1, _(\"Password:\"))\n    self.text2 = wx.TextCtrl(self, -1, \"\", size=(180, -1), style=wx.TE_PASSWORD)\n\n    btnsizer = wx.StdDialogButtonSizer()\n    btn = wx.Button(self, wx.ID_OK)\n    btn.SetDefault()\n    btnsizer.AddButton(btn)\n    btn = wx.Button(self, wx.ID_CANCEL)\n    btnsizer.AddButton(btn)\n    btnsizer.Realize()\n\n    sizer.Add(self.message, (0, 0))\n    sizer.Add(5, 5, (1, 0))\n    sizer.Add(label1, (1, 1))\n    sizer.Add(self.text1, (1, 2))\n    sizer.Add(label2, (2, 1))\n    sizer.Add(self.text2, (2, 2))\n    sizer.Add(btnsizer, (3, 2), (1, 1))\n    sizer.Add(5, 5, (4, 4))\n\n    self.SetSizer(sizer)\n    sizer.Fit(self)\n\n    if username:\n        self.text2.SetFocus()\n</code></pre>"},{"location":"api/guilib_logindialog/#pytigon_gui.guilib.logindialog.LoginDialog-functions","title":"Methods:","text":""},{"location":"api/guilib_pytigon_install/","title":"guilib.pytigon_install \u2013 Installer GUI","text":"<p>Graphical installer for pytigon applications \u2013 downloads, extracts, and configures project archives (.ptig files).</p>"},{"location":"api/guilib_pytigon_install/#pytigon_gui.guilib.pytigon_install","title":"pytigon_install","text":"<p>Pytigon application installation wizard module.</p> <p>Provides a wxWizard-based installer for .ptig archive files.</p>"},{"location":"api/guilib_pytigon_install/#pytigon_gui.guilib.pytigon_install-attributes","title":"Attributes","text":""},{"location":"api/guilib_pytigon_install/#pytigon_gui.guilib.pytigon_install._","title":"_  <code>module-attribute</code>","text":"<pre><code>_ = wx.GetTranslation\n</code></pre>"},{"location":"api/guilib_pytigon_install/#pytigon_gui.guilib.pytigon_install.logger","title":"logger  <code>module-attribute</code>","text":"<pre><code>logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/guilib_pytigon_install/#pytigon_gui.guilib.pytigon_install-classes","title":"Classes","text":""},{"location":"api/guilib_pytigon_install/#pytigon_gui.guilib.pytigon_install.InstallWizard","title":"InstallWizard","text":"<pre><code>InstallWizard(file_name)\n</code></pre> <p>               Bases: <code>Wizard</code></p> <p>Installation wizard for .ptig application archives.</p> <p>Initialize the wizard.</p> <p>Parameters:</p> Name Type Description Default <code>file_name</code> <p>Path to the .ptig archive file.</p> required Source code in <code>pytigon_gui/guilib/pytigon_install.py</code> <pre><code>def __init__(self, file_name):\n    \"\"\"Initialize the wizard.\n\n    Args:\n        file_name: Path to the .ptig archive file.\n    \"\"\"\n    Wizard.__init__(self, None, -1, \"Install app\")\n    self.Bind(EVT_WIZARD_PAGE_CHANGING, self.on_wiz_page_changing)\n    self.ptig = Ptig(file_name)\n\n    page1 = TitledPage(self, 1, _(\"Program description\"))\n    self.page1 = page1\n    page2 = TitledPage(self, 2, _(\"License\"))\n    page3 = TitledPage(self, 3, _(\"Run\"))\n\n    r_txt = wx.TextCtrl(\n        page1, -1, size=wx.Size(600, 300), style=wx.TE_MULTILINE | wx.TE_READONLY\n    )\n    r_txt.SetValue(self.ptig.get_readme())\n    page1.sizer.Add(r_txt)\n    self.FitToPage(page1)\n\n    l_txt = wx.TextCtrl(\n        page2,\n        -1,\n        self.ptig.get_license(),\n        size=wx.Size(600, 300),\n        style=wx.TE_MULTILINE | wx.TE_READONLY,\n    )\n    page2.sizer.Add(l_txt)\n    page2.sizer.AddSpacer(5)\n    self.licence = wx.CheckBox(page2, -1, _(\"Accept\"))\n    page2.sizer.Add(self.licence)\n\n    page3.sizer.Add(\n        wx.StaticText(page3, -1, _(\"Run the installed program or cancel\"))\n    )\n\n    WizardPageSimple.Chain(page1, page2)\n    WizardPageSimple.Chain(page2, page3)\n    self.GetPageAreaSizer().Add(page1)\n</code></pre>"},{"location":"api/guilib_pytigon_install/#pytigon_gui.guilib.pytigon_install.InstallWizard-functions","title":"Methods:","text":""},{"location":"api/guilib_pytigon_install/#pytigon_gui.guilib.pytigon_install.InstallWizard.install","title":"install","text":"<pre><code>install()\n</code></pre> <p>Extract the .ptig archive and create desktop shortcut.</p> <p>Returns:</p> Type Description <p>True always (installation errors are logged).</p> Source code in <code>pytigon_gui/guilib/pytigon_install.py</code> <pre><code>def install(self):\n    \"\"\"Extract the .ptig archive and create desktop shortcut.\n\n    Returns:\n        True always (installation errors are logged).\n    \"\"\"\n    try:\n        self.ptig.extract_ptig()\n    except Exception:\n        logger.exception(\"Failed to extract ptig archive\")\n        return False\n\n    ini_file = Path(self.ptig.extract_to) / \"install.ini\"\n    shortcut_created = False\n\n    if Path(ini_file).exists():\n        try:\n            config = configparser.ConfigParser()\n            config.read(ini_file)\n            if \"INSTALL\" in config.sections():\n                install = config[\"INSTALL\"]\n                title = install.get(\"Title\", self.ptig.prj_name)\n                parameters = install.get(\"Parameters\", \"\")\n                create_desktop_shortcut(self.ptig.prj_name, title, parameters)\n                shortcut_created = True\n        except Exception:\n            logger.exception(\"Error reading install.ini\")\n\n    if not shortcut_created:\n        create_desktop_shortcut(self.ptig.prj_name, self.ptig.prj_name)\n\n    return True\n</code></pre>"},{"location":"api/guilib_pytigon_install/#pytigon_gui.guilib.pytigon_install.InstallWizard.on_wiz_page_changing","title":"on_wiz_page_changing","text":"<pre><code>on_wiz_page_changing(event)\n</code></pre> <p>Handle page changing event - validate license acceptance.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>wxWizardEvent.</p> required Source code in <code>pytigon_gui/guilib/pytigon_install.py</code> <pre><code>def on_wiz_page_changing(self, event):\n    \"\"\"Handle page changing event - validate license acceptance.\n\n    Args:\n        event: wxWizardEvent.\n    \"\"\"\n    if event.GetPage().lp == 2 and event.GetDirection():\n        if not self.licence.GetValue():\n            wx.MessageBox(\n                _(\"You must accept the license or cancel the installation!\"),\n                _(\"Installation\"),\n            )\n            event.Veto()\n            return\n        else:\n            if not self.install():\n                wx.MessageBox(_(\"Installation error!\"), _(\"Installation\"))\n                event.Veto()\n                return\n    if event.GetPage().lp == 3 and not event.GetDirection():\n        wx.MessageBox(\n            _(\"Program installed, use the uninstaller!\"), _(\"Installation\")\n        )\n        event.Veto()\n        return\n    event.Skip()\n</code></pre>"},{"location":"api/guilib_pytigon_install/#pytigon_gui.guilib.pytigon_install.InstallWizard.run","title":"run","text":"<pre><code>run()\n</code></pre> <p>Run the wizard and return whether installation completed.</p> <p>Returns:</p> Type Description <p>True if the wizard finished successfully, False otherwise.</p> Source code in <code>pytigon_gui/guilib/pytigon_install.py</code> <pre><code>def run(self):\n    \"\"\"Run the wizard and return whether installation completed.\n\n    Returns:\n        True if the wizard finished successfully, False otherwise.\n    \"\"\"\n    ret = self.RunWizard(self.page1)\n    wx.CallAfter(self.Destroy)\n    return ret\n</code></pre>"},{"location":"api/guilib_pytigon_install/#pytigon_gui.guilib.pytigon_install.TitledPage","title":"TitledPage","text":"<pre><code>TitledPage(parent, lp, title)\n</code></pre> <p>               Bases: <code>WizardPageSimple</code></p> <p>A wizard page with a pre-formatted title.</p> <p>Initialize the titled page.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent wizard.</p> required <code>lp</code> <p>Logical page number.</p> required <code>title</code> <p>Page title.</p> required Source code in <code>pytigon_gui/guilib/pytigon_install.py</code> <pre><code>def __init__(self, parent, lp, title):\n    \"\"\"Initialize the titled page.\n\n    Args:\n        parent: Parent wizard.\n        lp: Logical page number.\n        title: Page title.\n    \"\"\"\n    WizardPageSimple.__init__(self, parent)\n    self.sizer = make_page_title(self, title)\n    self.lp = lp\n</code></pre>"},{"location":"api/guilib_pytigon_install/#pytigon_gui.guilib.pytigon_install.TitledPage-functions","title":"Methods:","text":""},{"location":"api/guilib_pytigon_install/#pytigon_gui.guilib.pytigon_install-functions","title":"Functions:","text":""},{"location":"api/guilib_pytigon_install/#pytigon_gui.guilib.pytigon_install.create_desktop_shortcut","title":"create_desktop_shortcut","text":"<pre><code>create_desktop_shortcut(app_name, title=None, parameters='')\n</code></pre> <p>Create a desktop shortcut for launching a Pytigon application.</p> <p>Parameters:</p> Name Type Description Default <code>app_name</code> <p>Application/project name.</p> required <code>title</code> <p>Shortcut display name (defaults to app_name).</p> <code>None</code> <code>parameters</code> <p>Additional command-line parameters.</p> <code>''</code> Source code in <code>pytigon_gui/guilib/tools.py</code> <pre><code>def create_desktop_shortcut(app_name, title=None, parameters=\"\"):\n    \"\"\"Create a desktop shortcut for launching a Pytigon application.\n\n    Args:\n        app_name: Application/project name.\n        title: Shortcut display name (defaults to app_name).\n        parameters: Additional command-line parameters.\n    \"\"\"\n    pytigon_init_path = Path(pytigon.__file__).resolve()\n    ico_path = pytigon_init_path.replace(\"__init__.py\", \"pytigon.ico\")\n    ptig_path = pytigon_init_path.replace(\"__init__.py\", \"ptig.py\")\n\n    if \"python\" in sys.executable and make_shortcut:\n        make_shortcut(\n            ptig_path + \" \" + app_name,\n            name=title if title else app_name,\n            icon=ico_path,\n        )\n</code></pre>"},{"location":"api/guilib_pytigon_install/#pytigon_gui.guilib.pytigon_install.install","title":"install","text":"<pre><code>install(ptig_path)\n</code></pre> <p>Launch the installation wizard for a .ptig file.</p> <p>Parameters:</p> Name Type Description Default <code>ptig_path</code> <p>Path to the .ptig archive.</p> required <p>Returns:</p> Type Description <p>True if installation completed, False otherwise.</p> Source code in <code>pytigon_gui/guilib/pytigon_install.py</code> <pre><code>def install(ptig_path):\n    \"\"\"Launch the installation wizard for a .ptig file.\n\n    Args:\n        ptig_path: Path to the .ptig archive.\n\n    Returns:\n        True if installation completed, False otherwise.\n    \"\"\"\n    wizard = InstallWizard(ptig_path)\n    return wizard.run()\n</code></pre>"},{"location":"api/guilib_pytigon_install/#pytigon_gui.guilib.pytigon_install.make_page_title","title":"make_page_title","text":"<pre><code>make_page_title(wiz_pg, title)\n</code></pre> <p>Create a standard title block for a wizard page.</p> <p>Parameters:</p> Name Type Description Default <code>wiz_pg</code> <p>Wizard page instance.</p> required <code>title</code> <p>Title text to display.</p> required <p>Returns:</p> Type Description <p>wx.BoxSizer containing the title layout.</p> Source code in <code>pytigon_gui/guilib/pytigon_install.py</code> <pre><code>def make_page_title(wiz_pg, title):\n    \"\"\"Create a standard title block for a wizard page.\n\n    Args:\n        wiz_pg: Wizard page instance.\n        title: Title text to display.\n\n    Returns:\n        wx.BoxSizer containing the title layout.\n    \"\"\"\n    sizer = wx.BoxSizer(wx.VERTICAL)\n    wiz_pg.SetSizer(sizer)\n    title_ctrl = wx.StaticText(wiz_pg, -1, title)\n    title_ctrl.SetFont(wx.Font(18, wx.SWISS, wx.NORMAL, wx.BOLD))\n    sizer.Add(title_ctrl, 0, wx.ALIGN_CENTRE | wx.ALL, 5)\n    sizer.Add(wx.StaticLine(wiz_pg, -1), 0, wx.EXPAND | wx.ALL, 5)\n    return sizer\n</code></pre>"},{"location":"api/guilib_signal/","title":"guilib.signal \u2013 Signal System","text":"<p>Signal/slot pattern implementation for decoupled component communication within the GUI application.</p>"},{"location":"api/guilib_signal/#pytigon_gui.guilib.signal","title":"signal","text":"<p>Signal dispatch module.</p> <p>Provides a simple publish-subscribe mechanism for inter-window communication. Objects register to receive named signals, and signal senders can broadcast to all registered listeners.</p>"},{"location":"api/guilib_signal/#pytigon_gui.guilib.signal-classes","title":"Classes","text":""},{"location":"api/guilib_signal/#pytigon_gui.guilib.signal.Signal","title":"Signal","text":"<pre><code>Signal()\n</code></pre> <p>Simple signal/slot mechanism for communication between windows.</p> <p>Registered objects must have a method matching the signal name. That method must accept the arguments passed to :meth:<code>signal</code>.</p> <p>Usage::</p> <pre><code>sig = Signal()\nsig.register_signal(my_window, \"on_data_ready\")\nsig.signal(\"on_data_ready\", data=some_data)\nsig.unregister_signal(my_window, \"on_data_ready\")\n</code></pre> <p>Initialize an empty signal registry.</p> Source code in <code>pytigon_gui/guilib/signal.py</code> <pre><code>def __init__(self):\n    \"\"\"Initialize an empty signal registry.\"\"\"\n    self._signals = {}\n</code></pre>"},{"location":"api/guilib_signal/#pytigon_gui.guilib.signal.Signal-functions","title":"Methods:","text":""},{"location":"api/guilib_signal/#pytigon_gui.guilib.signal.Signal.register_signal","title":"register_signal","text":"<pre><code>register_signal(obj, signal_name)\n</code></pre> <p>Register an object to receive a signal.</p> <p>The object must have a method with the name signal_name that accepts the arguments passed to :meth:<code>signal</code>.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <p>Object to receive signals.</p> required <code>signal_name</code> <p>Name of the signal (also the method name on obj).</p> required Source code in <code>pytigon_gui/guilib/signal.py</code> <pre><code>def register_signal(self, obj, signal_name):\n    \"\"\"Register an object to receive a signal.\n\n    The object must have a method with the name *signal_name* that\n    accepts the arguments passed to :meth:`signal`.\n\n    Args:\n        obj: Object to receive signals.\n        signal_name: Name of the signal (also the method name on obj).\n    \"\"\"\n    if signal_name not in self._signals:\n        self._signals[signal_name] = []\n    if obj not in self._signals[signal_name]:\n        self._signals[signal_name].append(obj)\n</code></pre>"},{"location":"api/guilib_signal/#pytigon_gui.guilib.signal.Signal.signal","title":"signal","text":"<pre><code>signal(signal_name, *argi, **argv)\n</code></pre> <p>Send a signal to all registered listeners.</p> <p>Parameters:</p> Name Type Description Default <code>signal_name</code> <p>Name of the signal/method to call.</p> required <code>*argi</code> <p>Positional arguments forwarded to the handler.</p> <code>()</code> <code>**argv</code> <p>Keyword arguments forwarded to the handler.</p> <code>{}</code> <p>Returns:</p> Type Description <p>List of return values from handlers (excluding None values).</p> Source code in <code>pytigon_gui/guilib/signal.py</code> <pre><code>def signal(self, signal_name, *argi, **argv):\n    \"\"\"Send a signal to all registered listeners.\n\n    Args:\n        signal_name: Name of the signal/method to call.\n        *argi: Positional arguments forwarded to the handler.\n        **argv: Keyword arguments forwarded to the handler.\n\n    Returns:\n        List of return values from handlers (excluding None values).\n    \"\"\"\n    ret = []\n    if signal_name in self._signals:\n        for obj in self._signals[signal_name]:\n            if (x := getattr(obj, signal_name)(*argi, **argv)) is not None:\n                ret.append(x)\n    return ret\n</code></pre>"},{"location":"api/guilib_signal/#pytigon_gui.guilib.signal.Signal.unregister_signal","title":"unregister_signal","text":"<pre><code>unregister_signal(obj, signal_name)\n</code></pre> <p>Unregister an object from receiving a signal.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <p>Object to stop receiving signals.</p> required <code>signal_name</code> <p>Name of the signal.</p> required <p>Raises:</p> Type Description <code>KeyError</code> <p>If signal_name is not registered.</p> Source code in <code>pytigon_gui/guilib/signal.py</code> <pre><code>def unregister_signal(self, obj, signal_name):\n    \"\"\"Unregister an object from receiving a signal.\n\n    Args:\n        obj: Object to stop receiving signals.\n        signal_name: Name of the signal.\n\n    Raises:\n        KeyError: If *signal_name* is not registered.\n    \"\"\"\n    if signal_name not in self._signals:\n        return\n    signal_list = self._signals[signal_name]\n    if obj in signal_list:\n        idx = signal_list.index(obj)\n        del signal_list[idx]\n</code></pre>"},{"location":"api/guilib_threadwindow/","title":"guilib.threadwindow \u2013 Threaded Window","text":"<p>Thread-safe window operations \u2013 runs wxPython GUI updates from background threads using wx.CallAfter.</p>"},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow","title":"threadwindow","text":"<p>Thread status window module.</p> <p>Provides a GUI panel displayed in the status bar area that shows progress and status information for background threads. Includes a manager that handles layout and periodic updates.</p>"},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow-attributes","title":"Attributes","text":""},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow.EVT_THREAD_INFO","title":"EVT_THREAD_INFO  <code>module-attribute</code>","text":"<pre><code>EVT_THREAD_INFO = wx.PyEventBinder(schEVT_THREAD_INFO, 1)\n</code></pre>"},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow.logger","title":"logger  <code>module-attribute</code>","text":"<pre><code>logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow.schEVT_THREAD_INFO","title":"schEVT_THREAD_INFO  <code>module-attribute</code>","text":"<pre><code>schEVT_THREAD_INFO = wx.NewEventType()\n</code></pre>"},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow-classes","title":"Classes","text":""},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow.SchThreadManager","title":"SchThreadManager","text":"<pre><code>SchThreadManager(app, statusbar)\n</code></pre> <p>Manages multiple SchThreadWindow instances in a status bar.</p> <p>Handles layout, positioning, and periodic polling of thread windows.</p> <p>Initialize the thread manager.</p> <p>Parameters:</p> Name Type Description Default <code>app</code> <p>The wx application.</p> required <code>statusbar</code> <p>The status bar to place thread windows in.</p> required Source code in <code>pytigon_gui/guilib/threadwindow.py</code> <pre><code>def __init__(self, app, statusbar):\n    \"\"\"Initialize the thread manager.\n\n    Args:\n        app: The wx application.\n        statusbar: The status bar to place thread windows in.\n    \"\"\"\n    self.app = app\n    self.statusbar = statusbar\n    self.sizeChanged = True\n    self.windows = []\n    statusbar.Bind(wx.EVT_SIZE, self.on_size)\n    statusbar.Bind(wx.EVT_IDLE, self.on_idle)\n</code></pre>"},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow.SchThreadManager-functions","title":"Methods:","text":""},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow.SchThreadManager.append","title":"append","text":"<pre><code>append(thread_address)\n</code></pre> <p>Add a new thread window for the given thread address.</p> <p>Parameters:</p> Name Type Description Default <code>thread_address</code> <p>Identifier for the background thread.</p> required Source code in <code>pytigon_gui/guilib/threadwindow.py</code> <pre><code>def append(self, thread_address):\n    \"\"\"Add a new thread window for the given thread address.\n\n    Args:\n        thread_address: Identifier for the background thread.\n    \"\"\"\n    self.windows.append(SchThreadWindow(self, thread_address, self.statusbar))\n    self.sizeChanged = True\n</code></pre>"},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow.SchThreadManager.on_idle","title":"on_idle","text":"<pre><code>on_idle(evt)\n</code></pre> <p>Handle idle event - reposition if size changed.</p> Source code in <code>pytigon_gui/guilib/threadwindow.py</code> <pre><code>def on_idle(self, evt):\n    \"\"\"Handle idle event - reposition if size changed.\"\"\"\n    if self.sizeChanged:\n        self.reposition()\n        self.sizeChanged = False\n</code></pre>"},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow.SchThreadManager.on_size","title":"on_size","text":"<pre><code>on_size(evt)\n</code></pre> <p>Handle status bar resize event.</p> Source code in <code>pytigon_gui/guilib/threadwindow.py</code> <pre><code>def on_size(self, evt):\n    \"\"\"Handle status bar resize event.\"\"\"\n    self.reposition()\n    self.sizeChanged = True\n</code></pre>"},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow.SchThreadManager.reposition","title":"reposition","text":"<pre><code>reposition()\n</code></pre> <p>Reposition and resize all thread windows in the status bar.</p> Source code in <code>pytigon_gui/guilib/threadwindow.py</code> <pre><code>def reposition(self):\n    \"\"\"Reposition and resize all thread windows in the status bar.\"\"\"\n    count = len(self.windows)\n    if count == 0:\n        return\n\n    rect = self.statusbar.GetFieldRect(1)\n    offset = 0\n    for win in self.windows:\n        win.SetPosition((int(rect.x + offset + 2), int(rect.y + 2)))\n        win.SetSize(\n            (\n                max(0, int(rect.width / count - 4)),\n                max(0, int(rect.height - 3)),\n            )\n        )\n        offset += rect.width / count\n</code></pre>"},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow.SchThreadManager.timer","title":"timer","text":"<pre><code>timer()\n</code></pre> <p>Periodic update for all thread windows.</p> <p>Removes closed windows and polls open ones. Uses list slicing to safely iterate while modifying the list.</p> Source code in <code>pytigon_gui/guilib/threadwindow.py</code> <pre><code>def timer(self):\n    \"\"\"Periodic update for all thread windows.\n\n    Removes closed windows and polls open ones. Uses list slicing\n    to safely iterate while modifying the list.\n    \"\"\"\n    if not self.windows:\n        return\n\n    # Iterate over a copy to safely remove while iterating\n    for win in self.windows[:]:\n        if win.is_closed():\n            win.Destroy()\n            self.windows.remove(win)\n            self.sizeChanged = True\n        else:\n            win.timer()\n</code></pre>"},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow.SchThreadWindow","title":"SchThreadWindow","text":"<pre><code>SchThreadWindow(manager, thread_name, parent)\n</code></pre> <p>               Bases: <code>Panel</code></p> <p>Status bar panel showing progress and info for a background thread.</p> <p>Displays a progress gauge, HTML description, and expand/kill buttons.</p> <p>Initialize the thread window.</p> <p>Parameters:</p> Name Type Description Default <code>manager</code> <p>The SchThreadManager that owns this window.</p> required <code>thread_name</code> <p>Identifier for the background thread.</p> required <code>parent</code> <p>Parent window (typically the status bar).</p> required Source code in <code>pytigon_gui/guilib/threadwindow.py</code> <pre><code>def __init__(self, manager, thread_name, parent):\n    \"\"\"Initialize the thread window.\n\n    Args:\n        manager: The SchThreadManager that owns this window.\n        thread_name: Identifier for the background thread.\n        parent: Parent window (typically the status bar).\n    \"\"\"\n    wx.Panel.__init__(self, parent)\n    self.thread_name = thread_name\n    self.manager = manager\n    self.closed = False\n\n    self.SetBackgroundColour(wx.NamedColour(\"#eec\"))\n\n    self.gauge = wx.Gauge(self, range=100, size=(100, 1))\n    self.gauge.SetValue(0)\n\n    self.html = wx.html.HtmlWindow(\n        self, size=(50, 50), style=wx.html.HW_SCROLLBAR_NEVER\n    )\n    self.html.SetBorders(0)\n    self.html.SetPage(\"&lt;body bgcolor='#eec'&gt;&lt;/body&gt;\")\n\n    bmp = wx.ArtProvider.GetBitmap(wx.ART_GO_TO_PARENT, wx.ART_TOOLBAR, (16, 16))\n    bmp = bmp.ConvertToImage().Rescale(16, 8).ConvertToBitmap()\n    bmp2 = wx.ArtProvider.GetBitmap(wx.ART_QUIT, wx.ART_TOOLBAR, (16, 16))\n    bmp2 = bmp2.ConvertToImage().Rescale(16, 8).ConvertToBitmap()\n\n    button1 = wx.BitmapButton(self, -1, bmp)\n    button2 = wx.BitmapButton(self, -1, bmp2)\n    self.Bind(wx.EVT_BUTTON, self.on_expand, button1)\n    self.Bind(wx.EVT_BUTTON, self.on_kill, button2)\n\n    box = wx.BoxSizer(wx.HORIZONTAL)\n    box.Add(self.html, 1, wx.EXPAND)\n    box.Add(self.gauge, 0, wx.EXPAND)\n    box.Add(button1, 0, wx.EXPAND)\n    box.Add(button2, 0, wx.EXPAND)\n    self.SetSizer(box)\n    box.Fit(self)\n</code></pre>"},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow.SchThreadWindow-functions","title":"Methods:","text":""},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow.SchThreadWindow.is_closed","title":"is_closed","text":"<pre><code>is_closed()\n</code></pre> <p>Check if the associated thread has completed.</p> <p>Returns:</p> Type Description <p>True if the thread is closed.</p> Source code in <code>pytigon_gui/guilib/threadwindow.py</code> <pre><code>def is_closed(self):\n    \"\"\"Check if the associated thread has completed.\n\n    Returns:\n        True if the thread is closed.\n    \"\"\"\n    return self.closed\n</code></pre>"},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow.SchThreadWindow.on_expand","title":"on_expand","text":"<pre><code>on_expand(event)\n</code></pre> <p>Open a detailed view of the thread.</p> Source code in <code>pytigon_gui/guilib/threadwindow.py</code> <pre><code>def on_expand(self, event):\n    \"\"\"Open a detailed view of the thread.\"\"\"\n    address = \"http://local.net/schsys/thread_long_info/\" + self.thread_name\n    wx.GetApp().GetTopWindow().new_main_page(address, \"aplikacja\")\n</code></pre>"},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow.SchThreadWindow.on_kill","title":"on_kill","text":"<pre><code>on_kill(event)\n</code></pre> <p>Request termination of the background thread.</p> Source code in <code>pytigon_gui/guilib/threadwindow.py</code> <pre><code>def on_kill(self, event):\n    \"\"\"Request termination of the background thread.\"\"\"\n    try:\n        http = wx.GetApp().http\n        http.get(self, \"http://local.net/schsys/thread_kill/\" + self.thread_name)\n    except Exception:\n        logger.exception(\"Failed to kill thread %s\", self.thread_name)\n</code></pre>"},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow.SchThreadWindow.timer","title":"timer","text":"<pre><code>timer()\n</code></pre> <p>Periodic update - polls server for thread progress and status.</p> Source code in <code>pytigon_gui/guilib/threadwindow.py</code> <pre><code>def timer(self):\n    \"\"\"Periodic update - polls server for thread progress and status.\"\"\"\n    try:\n        http = wx.GetApp().http\n        response = http.get(\n            self,\n            \"http://local.net/schsys/thread_short_info/\" + self.thread_name,\n        )\n        info_json = response.str()\n        info = schjson.loads(info_json)\n    except Exception:\n        logger.exception(\"Failed to poll thread info for %s\", self.thread_name)\n        return\n\n    if isinstance(info, str) and info == \"$$$\":\n        self.closed = True\n        evt = ThreadEvent(schEVT_THREAD_INFO, -1)\n        evt.set_info(self.thread_name)\n        wx.GetApp().GetTopWindow().GetEventHandler().ProcessEvent(evt)\n    elif info:\n        if \"progress\" in info:\n            try:\n                self.gauge.SetValue(int(info[\"progress\"]))\n            except (ValueError, TypeError):\n                pass\n        if \"description\" in info:\n            self.html.SetPage(\n                \"&lt;body bgcolor='#eec'&gt;\" + info[\"description\"] + \"&lt;/body&gt;\"\n            )\n</code></pre>"},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow.ThreadEvent","title":"ThreadEvent","text":"<pre><code>ThreadEvent(evt_type, id_)\n</code></pre> <p>               Bases: <code>PyCommandEvent</code></p> <p>Custom event carrying thread status information.</p> <p>Initialize the event.</p> <p>Parameters:</p> Name Type Description Default <code>evt_type</code> <p>Event type identifier.</p> required <code>id_</code> <p>Event source ID.</p> required Source code in <code>pytigon_gui/guilib/threadwindow.py</code> <pre><code>def __init__(self, evt_type, id_):\n    \"\"\"Initialize the event.\n\n    Args:\n        evt_type: Event type identifier.\n        id_: Event source ID.\n    \"\"\"\n    wx.PyCommandEvent.__init__(self, evt_type, id_)\n    self.info = None\n</code></pre>"},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow.ThreadEvent-functions","title":"Methods:","text":""},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow.ThreadEvent.get_info","title":"get_info","text":"<pre><code>get_info()\n</code></pre> <p>Get the event payload.</p> <p>Returns:</p> Type Description <p>The stored information.</p> Source code in <code>pytigon_gui/guilib/threadwindow.py</code> <pre><code>def get_info(self):\n    \"\"\"Get the event payload.\n\n    Returns:\n        The stored information.\n    \"\"\"\n    return self.info\n</code></pre>"},{"location":"api/guilib_threadwindow/#pytigon_gui.guilib.threadwindow.ThreadEvent.set_info","title":"set_info","text":"<pre><code>set_info(val)\n</code></pre> <p>Set the event payload.</p> <p>Parameters:</p> Name Type Description Default <code>val</code> <p>Information to attach to the event.</p> required Source code in <code>pytigon_gui/guilib/threadwindow.py</code> <pre><code>def set_info(self, val):\n    \"\"\"Set the event payload.\n\n    Args:\n        val: Information to attach to the event.\n    \"\"\"\n    self.info = val\n</code></pre>"},{"location":"api/guilib_tools/","title":"guilib.tools \u2013 GUI Tools","text":"<p>General-purpose GUI utilities: screen metrics, clipboard operations, file dialogs, and platform-specific helpers.</p>"},{"location":"api/guilib_tools/#pytigon_gui.guilib.tools","title":"tools","text":"<p>Utility functions for the Pytigon GUI library.</p> <p>Includes color manipulation helpers, desktop shortcut creation, focus tracking, and plugin import functionality.</p>"},{"location":"api/guilib_tools/#pytigon_gui.guilib.tools-attributes","title":"Attributes","text":""},{"location":"api/guilib_tools/#pytigon_gui.guilib.tools.LAST_FOCUS_CTRL_IN_FORM","title":"LAST_FOCUS_CTRL_IN_FORM  <code>module-attribute</code>","text":"<pre><code>LAST_FOCUS_CTRL_IN_FORM = None\n</code></pre>"},{"location":"api/guilib_tools/#pytigon_gui.guilib.tools.SIZE_DEFAULT","title":"SIZE_DEFAULT  <code>module-attribute</code>","text":"<pre><code>SIZE_DEFAULT = -1\n</code></pre>"},{"location":"api/guilib_tools/#pytigon_gui.guilib.tools.SIZE_LARGE","title":"SIZE_LARGE  <code>module-attribute</code>","text":"<pre><code>SIZE_LARGE = 2\n</code></pre>"},{"location":"api/guilib_tools/#pytigon_gui.guilib.tools.SIZE_MEDIUM","title":"SIZE_MEDIUM  <code>module-attribute</code>","text":"<pre><code>SIZE_MEDIUM = 1\n</code></pre>"},{"location":"api/guilib_tools/#pytigon_gui.guilib.tools.SIZE_SMALL","title":"SIZE_SMALL  <code>module-attribute</code>","text":"<pre><code>SIZE_SMALL = 0\n</code></pre>"},{"location":"api/guilib_tools/#pytigon_gui.guilib.tools._","title":"_  <code>module-attribute</code>","text":"<pre><code>_ = wx.GetTranslation\n</code></pre>"},{"location":"api/guilib_tools/#pytigon_gui.guilib.tools.logger","title":"logger  <code>module-attribute</code>","text":"<pre><code>logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/guilib_tools/#pytigon_gui.guilib.tools-functions","title":"Functions:","text":""},{"location":"api/guilib_tools/#pytigon_gui.guilib.tools.colour_to_html","title":"colour_to_html","text":"<pre><code>colour_to_html(colour)\n</code></pre> <p>Convert a wx.Colour to an HTML hex string (e.g. '#AABBCC').</p> <p>Parameters:</p> Name Type Description Default <code>colour</code> <p>wx.Colour instance.</p> required <p>Returns:</p> Type Description <p>HTML color string.</p> Source code in <code>pytigon_gui/guilib/tools.py</code> <pre><code>def colour_to_html(colour):\n    \"\"\"Convert a wx.Colour to an HTML hex string (e.g. '#AABBCC').\n\n    Args:\n        colour: wx.Colour instance.\n\n    Returns:\n        HTML color string.\n    \"\"\"\n    return wx.Colour(colour.Red(), colour.Green(), colour.Blue()).GetAsString(\n        wx.C2S_HTML_SYNTAX\n    )\n</code></pre>"},{"location":"api/guilib_tools/#pytigon_gui.guilib.tools.create_desktop_shortcut","title":"create_desktop_shortcut","text":"<pre><code>create_desktop_shortcut(app_name, title=None, parameters='')\n</code></pre> <p>Create a desktop shortcut for launching a Pytigon application.</p> <p>Parameters:</p> Name Type Description Default <code>app_name</code> <p>Application/project name.</p> required <code>title</code> <p>Shortcut display name (defaults to app_name).</p> <code>None</code> <code>parameters</code> <p>Additional command-line parameters.</p> <code>''</code> Source code in <code>pytigon_gui/guilib/tools.py</code> <pre><code>def create_desktop_shortcut(app_name, title=None, parameters=\"\"):\n    \"\"\"Create a desktop shortcut for launching a Pytigon application.\n\n    Args:\n        app_name: Application/project name.\n        title: Shortcut display name (defaults to app_name).\n        parameters: Additional command-line parameters.\n    \"\"\"\n    pytigon_init_path = Path(pytigon.__file__).resolve()\n    ico_path = pytigon_init_path.replace(\"__init__.py\", \"pytigon.ico\")\n    ptig_path = pytigon_init_path.replace(\"__init__.py\", \"ptig.py\")\n\n    if \"python\" in sys.executable and make_shortcut:\n        make_shortcut(\n            ptig_path + \" \" + app_name,\n            name=title if title else app_name,\n            icon=ico_path,\n        )\n</code></pre>"},{"location":"api/guilib_tools/#pytigon_gui.guilib.tools.find_focus_in_form","title":"find_focus_in_form","text":"<pre><code>find_focus_in_form()\n</code></pre> <p>Find the currently focused control that belongs to a SchForm.</p> <p>Walks up the window hierarchy from the current focus to locate a SchForm parent. Caches the last known focused control and invalidates it if the parent form is closing.</p> <p>Returns:</p> Type Description <p>The focused wx.Window inside a SchForm, or None.</p> Source code in <code>pytigon_gui/guilib/tools.py</code> <pre><code>def find_focus_in_form():\n    \"\"\"Find the currently focused control that belongs to a SchForm.\n\n    Walks up the window hierarchy from the current focus to locate a\n    SchForm parent. Caches the last known focused control and invalidates\n    it if the parent form is closing.\n\n    Returns:\n        The focused wx.Window inside a SchForm, or None.\n    \"\"\"\n    global LAST_FOCUS_CTRL_IN_FORM\n\n    win_focus = wx.Window.FindFocus()\n    win = win_focus\n    while win:\n        if win.__class__.__name__ == \"SchForm\":\n            LAST_FOCUS_CTRL_IN_FORM = win_focus\n            return win_focus\n        win = win.GetParent()\n\n    if LAST_FOCUS_CTRL_IN_FORM and (\n        not hasattr(LAST_FOCUS_CTRL_IN_FORM, \"parent\")\n        or not LAST_FOCUS_CTRL_IN_FORM.parent\n        or (\n            hasattr(LAST_FOCUS_CTRL_IN_FORM.parent, \"closing\")\n            and LAST_FOCUS_CTRL_IN_FORM.parent.closing\n        )\n    ):\n        LAST_FOCUS_CTRL_IN_FORM = None\n        return None\n\n    return LAST_FOCUS_CTRL_IN_FORM\n</code></pre>"},{"location":"api/guilib_tools/#pytigon_gui.guilib.tools.get_colour","title":"get_colour","text":"<pre><code>get_colour(wx_id, proc=None)\n</code></pre> <p>Get a system color as an HTML string, optionally blended.</p> <p>Parameters:</p> Name Type Description Default <code>wx_id</code> <p>wx.SYS_COLOUR_* constant.</p> required <code>proc</code> <p>Optional blend factor for norm_colour.</p> <code>None</code> <p>Returns:</p> Type Description <p>HTML color string.</p> Source code in <code>pytigon_gui/guilib/tools.py</code> <pre><code>def get_colour(wx_id, proc=None):\n    \"\"\"Get a system color as an HTML string, optionally blended.\n\n    Args:\n        wx_id: wx.SYS_COLOUR_* constant.\n        proc: Optional blend factor for norm_colour.\n\n    Returns:\n        HTML color string.\n    \"\"\"\n    c1 = wx.SystemSettings.GetColour(wx_id)\n    if not proc:\n        return colour_to_html(c1)\n    x = norm_colour(c1.Red(), c1.Green(), c1.Blue(), proc)\n    return colour_to_html(wx.Colour(x[0], x[1], x[2]))\n</code></pre>"},{"location":"api/guilib_tools/#pytigon_gui.guilib.tools.import_plugin","title":"import_plugin","text":"<pre><code>import_plugin(plugin_name, prj_name=None)\n</code></pre> <p>Import a plugin module by name, searching standard plugin paths.</p> <p>Lookup order depends on whether prj_name is given:   - With project: prj_cfg, then prj_cfg_alt.   - Without project: pytigon_cfg, then data_cfg.</p> <p>Parameters:</p> Name Type Description Default <code>plugin_name</code> <p>Dotted module name (e.g. 'plugins.myplugin').</p> required <code>prj_name</code> <p>Optional project name for project-specific plugins.</p> <code>None</code> <p>Returns:</p> Type Description <p>The imported module, or None if not found or import failed.</p> Source code in <code>pytigon_gui/guilib/tools.py</code> <pre><code>def import_plugin(plugin_name, prj_name=None):\n    \"\"\"Import a plugin module by name, searching standard plugin paths.\n\n    Lookup order depends on whether *prj_name* is given:\n      - With project: prj_cfg, then prj_cfg_alt.\n      - Without project: pytigon_cfg, then data_cfg.\n\n    Args:\n        plugin_name: Dotted module name (e.g. 'plugins.myplugin').\n        prj_name: Optional project name for project-specific plugins.\n\n    Returns:\n        The imported module, or None if not found or import failed.\n    \"\"\"\n    cfg = get_main_paths()\n    pytigon_cfg = [cfg[\"PYTIGON_PATH\"], \"appdata\", \"plugins\"]\n    data_cfg = [cfg[\"DATA_PATH\"], \"plugins\"]\n    prj_cfg = [cfg[\"PRJ_PATH\"], prj_name, \"applib\"]\n    prj_cfg_alt = [cfg[\"PRJ_PATH_ALT\"], prj_name, \"applib\"]\n\n    if prj_name:\n        folders = [prj_cfg, prj_cfg_alt]\n    else:\n        folders = [pytigon_cfg, data_cfg]\n\n    path = None\n    for folder in folders:\n        plugins_path = Path(folder[0]).joinpath(*folder[1:])\n        if prj_name:\n            plugin_path = Path(plugins_path).joinpath(*plugin_name.split(\".\")[:-1])\n        else:\n            plugin_path = Path(plugins_path).joinpath(*plugin_name.split(\".\"))\n        if Path(plugin_path).exists():\n            path = plugins_path\n            break\n\n    if not path:\n        logger.warning(\"Plugin path not found: %s\", plugin_name)\n        return None\n\n    try:\n        return importlib.import_module(plugin_name, package=None)\n    except ImportError as e:\n        logger.error(\"Failed to import plugin '%s': %s\", plugin_name, e)\n        return None\n</code></pre>"},{"location":"api/guilib_tools/#pytigon_gui.guilib.tools.norm_colour","title":"norm_colour","text":"<pre><code>norm_colour(r, g, b, proc)\n</code></pre> <p>Interpolate an RGB color toward white or black.</p> <p>When proc &gt; 1, blends toward white (lightening). When proc &lt; 1, blends toward black (darkening).</p> <p>Parameters:</p> Name Type Description Default <code>r</code> <p>Red channel (0-255).</p> required <code>g</code> <p>Green channel (0-255).</p> required <code>b</code> <p>Blue channel (0-255).</p> required <code>proc</code> <p>Blend factor. 1.0 = no change, &gt;1 = lighter, &lt;1 = darker.</p> required <p>Returns:</p> Type Description <p>[r, g, b] list with each channel clamped to 255.</p> Source code in <code>pytigon_gui/guilib/tools.py</code> <pre><code>def norm_colour(r, g, b, proc):\n    \"\"\"Interpolate an RGB color toward white or black.\n\n    When *proc* &gt; 1, blends toward white (lightening).\n    When *proc* &lt; 1, blends toward black (darkening).\n\n    Args:\n        r: Red channel (0-255).\n        g: Green channel (0-255).\n        b: Blue channel (0-255).\n        proc: Blend factor. 1.0 = no change, &gt;1 = lighter, &lt;1 = darker.\n\n    Returns:\n        [r, g, b] list with each channel clamped to 255.\n    \"\"\"\n    if proc &gt; 1:\n        y = (255, 255, 255)\n        dx = 2 - proc\n    else:\n        y = (0, 0, 0)\n        dx = proc\n    dy = 1 - dx\n    ret = [\n        int(r * dx + y[0] * dy),\n        int(g * dx + y[1] * dy),\n        int(b * dx + y[2] * dy),\n    ]\n    for i in range(3):\n        if ret[i] &gt; 255:\n            ret[i] = 255\n    return ret\n</code></pre>"},{"location":"api/guilib_tools/#pytigon_gui.guilib.tools.norm_colour2","title":"norm_colour2","text":"<pre><code>norm_colour2(c)\n</code></pre> <p>Clamp a single color channel value to [0, 255].</p> <p>Parameters:</p> Name Type Description Default <code>c</code> <p>Color channel value.</p> required <p>Returns:</p> Type Description <p>int between 0 and 255.</p> Source code in <code>pytigon_gui/guilib/tools.py</code> <pre><code>def norm_colour2(c):\n    \"\"\"Clamp a single color channel value to [0, 255].\n\n    Args:\n        c: Color channel value.\n\n    Returns:\n        int between 0 and 255.\n    \"\"\"\n    x = int(c)\n    return 255 if x &gt; 255 else x\n</code></pre>"},{"location":"api/guilib_tools/#pytigon_gui.guilib.tools.standard_tab_colour","title":"standard_tab_colour","text":"<pre><code>standard_tab_colour()\n</code></pre> <p>Return a tuple of (name, html_color) pairs for tab styling.</p> <p>These colors are derived from the system 3D face color at various blend levels, plus highlight, shadow, and info background colors.</p> <p>Returns:</p> Type Description <p>Tuple of (str, str) pairs mapping CSS variable names to HTML colors.</p> Source code in <code>pytigon_gui/guilib/tools.py</code> <pre><code>def standard_tab_colour():\n    \"\"\"Return a tuple of (name, html_color) pairs for tab styling.\n\n    These colors are derived from the system 3D face color at various\n    blend levels, plus highlight, shadow, and info background colors.\n\n    Returns:\n        Tuple of (str, str) pairs mapping CSS variable names to HTML colors.\n    \"\"\"\n    return (\n        (\"color_body_0_2\", get_colour(wx.SYS_COLOUR_3DFACE, 0.2)),\n        (\"color_body_0_5\", get_colour(wx.SYS_COLOUR_3DFACE, 0.5)),\n        (\"color_body_0_7\", get_colour(wx.SYS_COLOUR_3DFACE, 0.7)),\n        (\"color_body_0_9\", get_colour(wx.SYS_COLOUR_3DFACE, 0.9)),\n        (\"color_body\", get_colour(wx.SYS_COLOUR_3DFACE)),\n        (\"color_body_1_1\", get_colour(wx.SYS_COLOUR_3DFACE, 1.1)),\n        (\"color_body_1_3\", get_colour(wx.SYS_COLOUR_3DFACE, 1.3)),\n        (\"color_body_1_5\", get_colour(wx.SYS_COLOUR_3DFACE, 1.5)),\n        (\"color_body_1_8\", get_colour(wx.SYS_COLOUR_3DFACE, 1.8)),\n        (\"color_higlight\", get_colour(wx.SYS_COLOUR_3DHIGHLIGHT)),\n        (\"color_shadow\", get_colour(wx.SYS_COLOUR_3DSHADOW)),\n        (\"color_background_0_5\", get_colour(wx.SYS_COLOUR_3DFACE, 0.5)),\n        (\"color_background_0_8\", get_colour(wx.SYS_COLOUR_3DFACE, 0.8)),\n        (\"color_background_0_9\", get_colour(wx.SYS_COLOUR_3DFACE, 0.9)),\n        (\"color_background\", get_colour(wx.SYS_COLOUR_3DFACE)),\n        (\"color_background_1_1\", get_colour(wx.SYS_COLOUR_3DFACE, 1.1)),\n        (\"color_background_1_2\", get_colour(wx.SYS_COLOUR_3DFACE, 1.2)),\n        (\"color_background_1_5\", get_colour(wx.SYS_COLOUR_3DFACE, 1.5)),\n        (\"color_info\", get_colour(wx.SYS_COLOUR_INFOBK, 1.5)),\n    )\n</code></pre>"},{"location":"api/guilib_websocket/","title":"guilib.websocket \u2013 WebSocket Client","text":"<p>Django Channels WebSocket bridge \u2013 real-time bidirectional communication between the wxPython desktop and Django backend.</p>"},{"location":"api/guilib_websocket/#pytigon_gui.guilib.websocket","title":"websocket","text":"<p>WebSocket client protocol module.</p> <p>Provides a WebSocket client protocol implementation based on autobahn.twisted.websocket for use in Pytigon applications.</p>"},{"location":"api/guilib_websocket/#pytigon_gui.guilib.websocket-attributes","title":"Attributes","text":""},{"location":"api/guilib_websocket/#pytigon_gui.guilib.websocket.logger","title":"logger  <code>module-attribute</code>","text":"<pre><code>logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/guilib_websocket/#pytigon_gui.guilib.websocket-classes","title":"Classes","text":""},{"location":"api/guilib_websocket/#pytigon_gui.guilib.websocket.MyClientProtocol","title":"MyClientProtocol","text":"<p>               Bases: <code>WebSocketClientProtocol</code></p> <p>WebSocket client protocol with automatic keep-alive messaging.</p> <p>On connection open, starts sending a message every second. Handles text and binary messages and logs connection lifecycle events.</p> <p>Usage::</p> <pre><code>from autobahn.twisted.websocket import WebSocketClientFactory\nfrom twisted.internet import reactor\n\nfactory = WebSocketClientFactory(\"ws://localhost:9000\")\nfactory.protocol = MyClientProtocol\nreactor.connectTCP(\"localhost\", 9000, factory)\nreactor.run()\n</code></pre>"},{"location":"api/guilib_websocket/#pytigon_gui.guilib.websocket.MyClientProtocol-functions","title":"Methods:","text":""},{"location":"api/guilib_websocket/#pytigon_gui.guilib.websocket.MyClientProtocol.onClose","title":"onClose","text":"<pre><code>onClose(wasClean, code, reason)\n</code></pre> <p>Called when the WebSocket connection is closed.</p> <p>Parameters:</p> Name Type Description Default <code>wasClean</code> <p>True if the close was clean.</p> required <code>code</code> <p>Close status code.</p> required <code>reason</code> <p>Close reason string.</p> required Source code in <code>pytigon_gui/guilib/websocket.py</code> <pre><code>def onClose(self, wasClean, code, reason):\n    \"\"\"Called when the WebSocket connection is closed.\n\n    Args:\n        wasClean: True if the close was clean.\n        code: Close status code.\n        reason: Close reason string.\n    \"\"\"\n    logger.info(\"WebSocket connection closed: %s\", reason)\n</code></pre>"},{"location":"api/guilib_websocket/#pytigon_gui.guilib.websocket.MyClientProtocol.onConnect","title":"onConnect","text":"<pre><code>onConnect(response)\n</code></pre> <p>Called when the client connects to the server.</p> <p>Parameters:</p> Name Type Description Default <code>response</code> <p>Connection response object.</p> required Source code in <code>pytigon_gui/guilib/websocket.py</code> <pre><code>def onConnect(self, response):\n    \"\"\"Called when the client connects to the server.\n\n    Args:\n        response: Connection response object.\n    \"\"\"\n    logger.info(\"Server connected: %s\", response.peer)\n</code></pre>"},{"location":"api/guilib_websocket/#pytigon_gui.guilib.websocket.MyClientProtocol.onMessage","title":"onMessage","text":"<pre><code>onMessage(payload, isBinary)\n</code></pre> <p>Called when a message is received from the server.</p> <p>Parameters:</p> Name Type Description Default <code>payload</code> <p>Message data (bytes).</p> required <code>isBinary</code> <p>True if the message is binary, False if text.</p> required Source code in <code>pytigon_gui/guilib/websocket.py</code> <pre><code>def onMessage(self, payload, isBinary):\n    \"\"\"Called when a message is received from the server.\n\n    Args:\n        payload: Message data (bytes).\n        isBinary: True if the message is binary, False if text.\n    \"\"\"\n    if isBinary:\n        logger.info(\"Binary message received: %d bytes\", len(payload))\n    else:\n        logger.info(\"Text message received: %s\", payload.decode(\"utf8\"))\n</code></pre>"},{"location":"api/guilib_websocket/#pytigon_gui.guilib.websocket.MyClientProtocol.onOpen","title":"onOpen","text":"<pre><code>onOpen()\n</code></pre> <p>Called when the WebSocket connection is fully open.</p> <p>Starts a periodic message send loop.</p> Source code in <code>pytigon_gui/guilib/websocket.py</code> <pre><code>def onOpen(self):\n    \"\"\"Called when the WebSocket connection is fully open.\n\n    Starts a periodic message send loop.\n    \"\"\"\n\n    def send_hello():\n        \"\"\"Send a text and binary keep-alive message.\"\"\"\n        self.sendMessage(\"Hello, world!\".encode(\"utf8\"))\n        self.sendMessage(b\"\\x00\\x01\\x03\\x04\", isBinary=True)\n        self.factory.reactor.callLater(1, send_hello)\n\n    logger.info(\"WebSocket connection open.\")\n    send_hello()\n</code></pre>"},{"location":"api/pytigon_gui/","title":"pytigon_gui \u2013 Root Package","text":"<p>Package init and metadata for the Pytigon GUI library.</p>"},{"location":"api/pytigon_gui/#pytigon_gui","title":"pytigon_gui","text":"<p>Pytigon GUI - wxPython and Django application framework.</p> <p>This package provides the graphical user interface for the Pytigon framework, including the main application class, GUI automation tools, and widget libraries for building cross-platform desktop applications with embedded Django web server capabilities.</p> Subpackages"},{"location":"api/pytigon_gui/#pytigon_gui-attributes","title":"Attributes","text":""},{"location":"api/pytigon_gui/#pytigon_gui.__version__","title":"__version__  <code>module-attribute</code>","text":"<pre><code>__version__ = '0.260707'\n</code></pre>"},{"location":"api/pytigon_main/","title":"pytigon \u2013 Main Application","text":"<p>Main wxPython application entry point. Handles app initialization, frame creation, Django embedding, toolbar setup, and the main event loop.</p>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon","title":"pytigon","text":"<p>This is the main Pytigon client module. Function :func:<code>~pytigon_gui.pytigon.main</code> create SchApp object, which extends wxPython wx.App. Function :func:<code>~pytigon_gui.pytigon.main</code> process pytigon command line arguments. Module supports:</p> <ul> <li> <p>instalation of pytigon applications,</p> </li> <li> <p>Remote Procedure Calling protocol</p> </li> <li> <p>login to server process</p> </li> </ul>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon-attributes","title":"Attributes","text":""},{"location":"api/pytigon_main/#pytigon_gui.pytigon.App","title":"App  <code>module-attribute</code>","text":"<pre><code>App = InspectableApp\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.CWD_PATH","title":"CWD_PATH  <code>module-attribute</code>","text":"<pre><code>CWD_PATH = Path.cwd().parent\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.DATA_PATH","title":"DATA_PATH  <code>module-attribute</code>","text":"<pre><code>DATA_PATH = PATHS['DATA_PATH']\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.PATHS","title":"PATHS  <code>module-attribute</code>","text":"<pre><code>PATHS = get_main_paths(os.environ['PRJ_NAME'])\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.PRJ_NAME","title":"PRJ_NAME  <code>module-attribute</code>","text":"<pre><code>PRJ_NAME = os.environ['PRJ_NAME']\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.PYTIGON_PATH","title":"PYTIGON_PATH  <code>module-attribute</code>","text":"<pre><code>PYTIGON_PATH = PATHS['PYTIGON_PATH']\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.ROOT_PATH","title":"ROOT_PATH  <code>module-attribute</code>","text":"<pre><code>ROOT_PATH = str(SRC_PATH)\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SRC_PATH","title":"SRC_PATH  <code>module-attribute</code>","text":"<pre><code>SRC_PATH = Path(pytigon.__file__).parent\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon._","title":"_  <code>module-attribute</code>","text":"<pre><code>_ = wx.GetTranslation\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon._APP_SIZE","title":"_APP_SIZE  <code>module-attribute</code>","text":"<pre><code>_APP_SIZE = (1024, 768)\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon._BASE_APP","title":"_BASE_APP  <code>module-attribute</code>","text":"<pre><code>_BASE_APP = xmlrpc.XMLRPC\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon._DEBUG","title":"_DEBUG  <code>module-attribute</code>","text":"<pre><code>_DEBUG = False\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon._INSPECTION","title":"_INSPECTION  <code>module-attribute</code>","text":"<pre><code>_INSPECTION = False\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon._PARAM","title":"_PARAM  <code>module-attribute</code>","text":"<pre><code>_PARAM = process_argv(sys.argv[1:])\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon._RPC","title":"_RPC  <code>module-attribute</code>","text":"<pre><code>_RPC = False\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon._TRACE","title":"_TRACE  <code>module-attribute</code>","text":"<pre><code>_TRACE = False\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon._VIDEO","title":"_VIDEO  <code>module-attribute</code>","text":"<pre><code>_VIDEO = False\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon._VIDEO_NAME","title":"_VIDEO_NAME  <code>module-attribute</code>","text":"<pre><code>_VIDEO_NAME = ''\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon._WEBSOCKET","title":"_WEBSOCKET  <code>module-attribute</code>","text":"<pre><code>_WEBSOCKET = None\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.logger","title":"logger  <code>module-attribute</code>","text":"<pre><code>logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.myappid","title":"myappid  <code>module-attribute</code>","text":"<pre><code>myappid = 'slawomir_cholaj.pytigon.main.01'\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon-classes","title":"Classes","text":""},{"location":"api/pytigon_main/#pytigon_gui.pytigon.InspectableApp","title":"InspectableApp","text":"<p>               Bases: <code>SchAsyncApp</code>, <code>InspectionMixin</code></p>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.LoginDialog","title":"LoginDialog","text":"<pre><code>LoginDialog(\n    parent,\n    id_,\n    title,\n    size=DefaultSize,\n    pos=DefaultPosition,\n    style=DEFAULT_DIALOG_STYLE,\n    use_metal=False,\n    username=None,\n)\n</code></pre> <p>               Bases: <code>Dialog</code></p> <p>Modal dialog for user login with username and password fields.</p> <p>Usage::</p> <pre><code>dlg = LoginDialog(parent, wx.ID_ANY, \"Login\")\nif dlg.ShowModal() == wx.ID_OK:\n    username = dlg.text1.GetValue()\n    password = dlg.text2.GetValue()\ndlg.Destroy()\n</code></pre> <p>Initialize the login dialog.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>id_</code> <p>Window identifier.</p> required <code>title</code> <p>Dialog title.</p> required <code>size</code> <p>Dialog size (default: wx.DefaultSize).</p> <code>DefaultSize</code> <code>pos</code> <p>Dialog position (default: wx.DefaultPosition).</p> <code>DefaultPosition</code> <code>style</code> <p>Dialog style flags.</p> <code>DEFAULT_DIALOG_STYLE</code> <code>use_metal</code> <p>Use metal look on macOS.</p> <code>False</code> <code>username</code> <p>Default username to pre-fill (optional).</p> <code>None</code> Source code in <code>pytigon_gui/guilib/logindialog.py</code> <pre><code>def __init__(\n    self,\n    parent,\n    id_,\n    title,\n    size=wx.DefaultSize,\n    pos=wx.DefaultPosition,\n    style=wx.DEFAULT_DIALOG_STYLE,\n    use_metal=False,\n    username=None,\n):\n    \"\"\"Initialize the login dialog.\n\n    Args:\n        parent: Parent window.\n        id_: Window identifier.\n        title: Dialog title.\n        size: Dialog size (default: wx.DefaultSize).\n        pos: Dialog position (default: wx.DefaultPosition).\n        style: Dialog style flags.\n        use_metal: Use metal look on macOS.\n        username: Default username to pre-fill (optional).\n    \"\"\"\n    wx.Dialog.__init__(self, parent, id_, title, pos, size, style)\n    self.CenterOnParent()\n\n    if \"wxMac\" in wx.PlatformInfo and use_metal:\n        self.SetExtraStyle(wx.DIALOG_EX_METAL)\n\n    sizer = wx.GridBagSizer(5, 2)\n\n    self.message = wx.StaticText(self, -1, \"\")\n    font = wx.Font(\n        10, wx.FONTFAMILY_DEFAULT, wx.FONTSTYLE_NORMAL, wx.FONTWEIGHT_BOLD\n    )\n    self.message.SetFont(font)\n\n    label1 = wx.StaticText(self, -1, _(\"User name:\"))\n    self.text1 = wx.TextCtrl(self, -1, \"\", size=(180, -1))\n    if username:\n        self.text1.SetValue(username)\n\n    label2 = wx.StaticText(self, -1, _(\"Password:\"))\n    self.text2 = wx.TextCtrl(self, -1, \"\", size=(180, -1), style=wx.TE_PASSWORD)\n\n    btnsizer = wx.StdDialogButtonSizer()\n    btn = wx.Button(self, wx.ID_OK)\n    btn.SetDefault()\n    btnsizer.AddButton(btn)\n    btn = wx.Button(self, wx.ID_CANCEL)\n    btnsizer.AddButton(btn)\n    btnsizer.Realize()\n\n    sizer.Add(self.message, (0, 0))\n    sizer.Add(5, 5, (1, 0))\n    sizer.Add(label1, (1, 1))\n    sizer.Add(self.text1, (1, 2))\n    sizer.Add(label2, (2, 1))\n    sizer.Add(self.text2, (2, 2))\n    sizer.Add(btnsizer, (3, 2), (1, 1))\n    sizer.Add(5, 5, (4, 4))\n\n    self.SetSizer(sizer)\n    sizer.Fit(self)\n\n    if username:\n        self.text2.SetFocus()\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.LoginDialog-functions","title":"Methods:","text":""},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchApp","title":"SchApp","text":"<pre><code>SchApp()\n</code></pre> <p>               Bases: <code>App</code>, <code>_BASE_APP</code></p> <p>It is a subclass of wxPython wx.App. Depending on the command line parameters it is also subclass of xmlrpc.XMLRPC and wx.lib.mixins.inspection.InspectableApp.</p> <p>Construct an application.</p> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def __init__(self):\n    \"\"\"Construct an application.\"\"\"\n    global _PARAM\n    App.__init__(self)\n    if _RPC:\n        xmlrpc.XMLRPC.__init__(self)\n\n    if (\n        not \"no_splash\" in _PARAM\n        and not \"nogui\" in _PARAM\n        and not \"server_only\" in _PARAM\n    ):\n        img = wx.svg.SVGimage.CreateFromFile(str(SRC_PATH / \"pytigon.svg\"))\n        bitmap = img.ConvertToBitmap(\n            scale=2, width=int(img.width * 2), height=int(img.height * 2)\n        )\n        splash = wx.adv.SplashScreen(\n            bitmap,\n            wx.adv.SPLASH_CENTRE_ON_SCREEN | wx.adv.SPLASH_TIMEOUT,\n            1000,\n            None,\n            -1,\n            wx.DefaultPosition,\n            wx.DefaultSize,\n            wx.BORDER_SIMPLE | wx.STAY_ON_TOP,\n        )\n        splash.Update()\n        wx.Yield()\n\n    config_name = SRC_PATH / \"pytigon.ini\"\n    self.config = configparser.ConfigParser()\n    self.config.read(config_name)\n\n    self.app_size = _APP_SIZE\n\n    self.locale = None\n    self.ext_app = []\n    self.ext_app_http = {}\n    self.lock = None\n\n    self.base_address = None\n\n    self.src_path = SRC_PATH\n    self.root_path = ROOT_PATH\n    self.cwd_path = CWD_PATH\n    self.data_path = DATA_PATH\n    self.pytigon_path = PYTIGON_PATH\n\n    self.http = None\n    self.images = None\n    self.mp = None\n\n    self.server = None\n    self.cwd = None\n    self.inst_dir = None\n    self.start_pages = []\n\n    self.csrf_token = None\n    self.title = None\n    self.plugins = None\n    self.extern_data = {}\n\n    self.thread_manager = None\n    self.task_manager = None\n\n    self.menu_always = False\n    self.authorized = False\n    self.rpc = None\n\n    self.websockets = {}\n    self.websockets_callbacks = {}\n\n    self.gui_style = (\n        \"app.gui_style = tree(toolbar(file(exit,open),clipboard, statusbar))\"\n    )\n\n    self.COLOUR_HIGHLIGHT = colour_to_html(\n        wx.SystemSettings.GetColour(wx.SYS_COLOUR_HIGHLIGHT)\n    )\n    self.COLOUR_BACKGROUND = colour_to_html(\n        wx.SystemSettings.GetColour(wx.SYS_COLOUR_3DFACE)\n    )\n    self.COLOUR_SHADOW = colour_to_html(\n        wx.SystemSettings.GetColour(wx.SYS_COLOUR_3DSHADOW)\n    )\n    self.COLOUR_DKSHADOW = colour_to_html(\n        wx.SystemSettings.GetColour(wx.SYS_COLOUR_3DDKSHADOW)\n    )\n    self.COLOUR_ACTIVECATPION = colour_to_html(\n        wx.SystemSettings.GetColour(wx.SYS_COLOUR_ACTIVECAPTION)\n    )\n    self.COLOUR_INFOBK = colour_to_html(\n        wx.SystemSettings.GetColour(wx.SYS_COLOUR_INFOBK)\n    )\n\n    self.ctrl_process = {}\n\n    class callback:\n        def on_websocket_message(msg):\n            # pass\n            logger.debug(\"CALLBACK: %s\", msg)\n\n    self.add_websoket_callback(\"/schbuilder/clock/channel/\", callback)\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchApp-functions","title":"Methods:","text":""},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchApp.add_websoket_callback","title":"add_websoket_callback","text":"<pre><code>add_websoket_callback(websocket_id, callback)\n</code></pre> <p>Register a callback for websocket events on a given websocket ID.</p> <p>Parameters:</p> Name Type Description Default <code>websocket_id</code> <p>the websocket endpoint identifier.</p> required <code>callback</code> <p>object with websocket event handler methods.</p> required Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def add_websoket_callback(self, websocket_id, callback):\n    \"\"\"Register a callback for websocket events on a given websocket ID.\n\n    Args:\n        websocket_id: the websocket endpoint identifier.\n        callback: object with websocket event handler methods.\n    \"\"\"\n    if websocket_id in self.websockets_callbacks:\n        self.websockets_callbacks[websocket_id].append(callback)\n    else:\n        self.websockets_callbacks[websocket_id] = [callback]\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchApp.get_http","title":"get_http","text":"<pre><code>get_http(win)\n</code></pre> <p>Get :class:<code>~pytigon_lib.schhttptools.httpclient.HttpClient</code> object</p> <p>With returned object you can directly make requests to http server.</p> <p>Returns:</p> Type Description Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def get_http(self, win):\n    \"\"\"Get :class:`~pytigon_lib.schhttptools.httpclient.HttpClient` object\n\n    With returned object you can directly make requests to http server.\n\n    Args:\n        win - wx.Window derived object\n\n    Returns:\n        :class:`~pytigon_lib.schhttptools.httpclient.HttpClient` object\n    \"\"\"\n    winparent = win\n    while winparent:\n        if hasattr(winparent, \"get_app_http\"):\n            return winparent.get_app_http()\n        winparent = winparent.GetParent()\n    return self.http\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchApp.get_locale_object","title":"get_locale_object","text":"<pre><code>get_locale_object()\n</code></pre> <p>Get locale object</p> <p>Returns:</p> Type Description <p>application wx.Locale object</p> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def get_locale_object(self):\n    \"\"\"Get locale object\n\n    Returns:\n        application wx.Locale object\n    \"\"\"\n    return self.locale\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchApp.get_tab","title":"get_tab","text":"<pre><code>get_tab(nr)\n</code></pre> <p>Return setup tab</p> <p>When pytigon client connect to server it receive html page with few configuration tables. This function return rendered table.</p> <p>Parameters:</p> Name Type Description Default <code>nr</code> <p>table number</p> required <p>Returns:</p> Type Description <p>table - :class:'~pytigon_lib.schparser.html_parsers.SimpleTabParser' object</p> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def get_tab(self, nr):\n    \"\"\"Return setup tab\n\n    When pytigon client connect to server it receive html page with few configuration tables.\n    This function return rendered table.\n\n    Args:\n        nr: table number\n\n    Returns:\n        table - :class:'~pytigon_lib.schparser.html_parsers.SimpleTabParser' object\n\n    \"\"\"\n    if self.mp:\n        return self.mp.tables[nr]\n    else:\n        return None\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchApp.get_working_dir","title":"get_working_dir","text":"<pre><code>get_working_dir()\n</code></pre> <p>Return pytigon working directory - ~/pytigon_data/.</p> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def get_working_dir(self):\n    \"\"\"Return pytigon working directory - ~/pytigon_data/.\"\"\"\n    return Path.home() / \"pytigon_data/\"\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchApp.on_exit","title":"on_exit","text":"<pre><code>on_exit()\n</code></pre> <p>Clean up resources when the application exits.</p> <p>Terminates the task manager if it is running.</p> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def on_exit(self):\n    \"\"\"Clean up resources when the application exits.\n\n    Terminates the task manager if it is running.\n    \"\"\"\n    if self.task_manager:\n        with contextlib.suppress(Exception):\n            self.task_manager.terminate()\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchApp.on_websocket_callback","title":"on_websocket_callback","text":"<pre><code>on_websocket_callback(client, event_name, argv)\n</code></pre> <p>Dispatch a websocket event to all registered callbacks.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <p>the websocket client instance.</p> required <code>event_name</code> <p>name of the event (e.g. 'on_websocket_message').</p> required <code>argv</code> <p>event arguments dictionary.</p> required Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def on_websocket_callback(self, client, event_name, argv):\n    \"\"\"Dispatch a websocket event to all registered callbacks.\n\n    Args:\n        client: the websocket client instance.\n        event_name: name of the event (e.g. 'on_websocket_message').\n        argv: event arguments dictionary.\n    \"\"\"\n    if client.websocket_id in self.websockets_callbacks:\n        for callback in self.websockets_callbacks[client.websocket_id]:\n            if hasattr(callback, event_name):\n                if \"channel\" in argv:\n                    if hasattr(callback, \"accept_channel\"):\n                        if not getattr(callback, \"accept_channel\")(argv[\"channel\"]):\n                            continue\n                try:\n                    getattr(callback, event_name)(**argv)\n                except Exception as e:\n                    logger.error(\n                        \"Websocket callback error for %s: %s\", event_name, e\n                    )\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchApp.on_websocket_connect","title":"on_websocket_connect","text":"<pre><code>on_websocket_connect(client, websocket_id, response)\n</code></pre> <p>Handle websocket connect event.</p> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def on_websocket_connect(self, client, websocket_id, response):\n    \"\"\"Handle websocket connect event.\"\"\"\n    return self.on_websocket_callback(\n        client, \"on_websocket_connect\", {\"response\": response}\n    )\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchApp.on_websocket_message","title":"on_websocket_message","text":"<pre><code>on_websocket_message(client, websocket_id, msg)\n</code></pre> <p>Handle websocket message event.</p> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def on_websocket_message(self, client, websocket_id, msg):\n    \"\"\"Handle websocket message event.\"\"\"\n    return self.on_websocket_callback(client, \"on_websocket_message\", msg)\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchApp.on_websocket_open","title":"on_websocket_open","text":"<pre><code>on_websocket_open(client, websocket_id)\n</code></pre> <p>Handle websocket open event.</p> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def on_websocket_open(self, client, websocket_id):\n    \"\"\"Handle websocket open event.\"\"\"\n    return self.on_websocket_callback(client, \"on_websocket_open\", {})\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchApp.read_html","title":"read_html","text":"<pre><code>read_html(win, address_or_parser, parameters)\n</code></pre> <p>Prepare request to http server and read result</p> <p>Parameters:</p> Name Type Description Default <code>win</code> <p>wx.Window derived object</p> required <code>address_or_parser</code> <p>can be: address of read_htmlhttp page (str type) or</p> required <code></code> required <code>parameters</code> <p>dict</p> required <p>Returns :class:'~pytigon_lib.schparser.html_parsers.ShtmlParser' object</p> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def read_html(self, win, address_or_parser, parameters):\n    \"\"\"Prepare request to http server and read result\n\n    Args:\n        win: wx.Window derived object\n        address_or_parser: can be: address of read_htmlhttp page (str type) or\n        :class:'~pytigon_lib.schparser.html_parsers.ShtmlParser'\n        parameters: dict\n\n    Returns :class:'~pytigon_lib.schparser.html_parsers.ShtmlParser' object\n    \"\"\"\n    if isinstance(address_or_parser, str):\n        http = self.get_http(win)\n\n        if parameters and isinstance(parameters, dict):\n            adr = address_or_parser\n            response = http.post(self, adr, parm=parameters)\n        else:\n            if parameters:\n                adrtmp = createparm.create_parm(address_or_parser, parameters)\n                if adrtmp:\n                    adr = adrtmp[0] + adrtmp[1] + adrtmp[2]\n                else:\n                    adr = address_or_parser\n            else:\n                adr = address_or_parser\n            response = http.get(win, adr)\n        if response is None or response.ret_code == 404:\n            raise ValueError(f\"HTTP request failed with 404 for address: {adr}\")\n        ptr = response.str()\n        mp = ShtmlParser()\n        mp.process(ptr, address_or_parser)\n        mp.address = adr\n    elif isinstance(address_or_parser, HttpResponse):\n        adr = address_or_parser.url\n        ptr = address_or_parser.str()\n        mp = ShtmlParser()\n        mp.process(ptr, adr)\n        mp.address = adr\n    else:\n        adr = None\n        mp = address_or_parser if address_or_parser else ShtmlParser()\n        if not address_or_parser:\n            mp.process(\"&lt;html&gt;&lt;body&gt;&lt;/body&gt;&lt;/html&gt;\")\n            mp.address = None\n    return (mp, adr)\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchApp.register_ctrl_process_fun","title":"register_ctrl_process_fun","text":"<pre><code>register_ctrl_process_fun(tag, fun)\n</code></pre> <p>Register function, which is called when widget connected to the specified tag is created.</p> <p>Parameters:</p> Name Type Description Default <code>tag</code> <p>it must be the name of class in pytigon_gui/guictrl/schctrl typed in lowercase, preceded by \"ctrl\".</p> required <code>For example</code> <p>ctrlgrid, ctrltable, ctrllistbox are valid tag names.</p> required <code>fun</code> <p>a callback function with is called with one parameter - widget defined in pytigon_gui/guictrl/schctrl instance.</p> required <p>Returns:</p> Type Description <p>None</p> Example Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def register_ctrl_process_fun(self, tag, fun):\n    \"\"\"Register function, which is called when widget connected to the specified tag is created.\n\n    Args:\n        tag: it must be the name of class in pytigon_gui/guictrl/schctrl typed in lowercase, preceded by \"ctrl\".\n        For example: ctrlgrid, ctrltable, ctrllistbox are valid tag names.\n\n        fun: a callback function with is called with one parameter - widget defined in pytigon_gui/guictrl/schctrl instance.\n\n    Returns:\n        None\n\n    Example:\n        see: appdata/plugins/standard/keymap/__init__.py\n    \"\"\"\n\n    if tag in self.ctrl_process:\n        self.ctrl_process[tag].append(fun)\n    else:\n        self.ctrl_process[tag] = [fun]\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchApp.remove_websocket_callback","title":"remove_websocket_callback","text":"<pre><code>remove_websocket_callback(websocket_id, callback)\n</code></pre> <p>Unregister a previously registered websocket callback.</p> <p>Parameters:</p> Name Type Description Default <code>websocket_id</code> <p>the websocket endpoint identifier.</p> required <code>callback</code> <p>the callback object to remove.</p> required Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def remove_websocket_callback(self, websocket_id, callback):\n    \"\"\"Unregister a previously registered websocket callback.\n\n    Args:\n        websocket_id: the websocket endpoint identifier.\n        callback: the callback object to remove.\n    \"\"\"\n    if websocket_id in self.websockets_callbacks:\n        if callback in self.websockets_callbacks[websocket_id]:\n            self.websockets_callbacks[websocket_id].remove(callback)\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchApp.run_script","title":"run_script","text":"<pre><code>run_script(app_name, script_path)\n</code></pre> <p>Run a script file and send its content to the server.</p> <p>Parameters:</p> Name Type Description Default <code>app_name</code> <p>name of the application.</p> required <code>script_path</code> <p>path to the script file to execute.</p> required Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def run_script(self, app_name, script_path):\n    \"\"\"Run a script file and send its content to the server.\n\n    Args:\n        app_name: name of the application.\n        script_path: path to the script file to execute.\n    \"\"\"\n    try:\n        with open(script_path, \"rb\") as s:\n            top_window = self.GetTopWindow()\n            if top_window and hasattr(top_window, \"new_main_page\"):\n                wx.CallAfter(\n                    top_window.new_main_page,\n                    \"/\" + app_name + \"/run_script/\",\n                    \"Run script\",\n                    {\"script\": s.read()},\n                )\n    except OSError as e:\n        logger.error(\"Error reading script %s: %s\", script_path, e)\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchApp.websocket_send","title":"websocket_send  <code>async</code>","text":"<pre><code>websocket_send(websocket_id, msg)\n</code></pre> <p>Send a message through a websocket connection.</p> <p>Parameters:</p> Name Type Description Default <code>websocket_id</code> <p>the websocket endpoint identifier.</p> required <code>msg</code> <p>message to send (will be JSON-serialized by the transport).</p> required Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>async def websocket_send(self, websocket_id, msg):\n    \"\"\"Send a message through a websocket connection.\n\n    Args:\n        websocket_id: the websocket endpoint identifier.\n        msg: message to send (will be JSON-serialized by the transport).\n    \"\"\"\n    if websocket_id in self.websockets:\n        obj = self.websockets[websocket_id].send_message(msg)\n        if obj:\n            await obj\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchApp.xmlrpc_stop","title":"xmlrpc_stop","text":"<pre><code>xmlrpc_stop()\n</code></pre> <p>Closes the wx application.</p> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def xmlrpc_stop(self):\n    \"\"\"Closes the wx application.\"\"\"\n    top_window = self.GetTopWindow()\n    if top_window:\n        top_window.Close()\n    return \"Shutdown initiated\"\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchApp.xmlrpc_title","title":"xmlrpc_title","text":"<pre><code>xmlrpc_title(title)\n</code></pre> <p>Set the main window title.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>new title of application top window</p> required <p>Returns:</p> Type Description <p>The title that was set, or None if no top window exists.</p> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def xmlrpc_title(self, title):\n    \"\"\"Set the main window title.\n\n    Args:\n        title: new title of application top window\n\n    Returns:\n        The title that was set, or None if no top window exists.\n    \"\"\"\n    top_window = self.GetTopWindow()\n    if top_window:\n        top_window.SetTitle(title)\n    return title\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchAsyncApp","title":"SchAsyncApp","text":"<p>               Bases: <code>WxAsyncApp</code></p>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchThreadManager","title":"SchThreadManager","text":"<pre><code>SchThreadManager(app, statusbar)\n</code></pre> <p>Manages multiple SchThreadWindow instances in a status bar.</p> <p>Handles layout, positioning, and periodic polling of thread windows.</p> <p>Initialize the thread manager.</p> <p>Parameters:</p> Name Type Description Default <code>app</code> <p>The wx application.</p> required <code>statusbar</code> <p>The status bar to place thread windows in.</p> required Source code in <code>pytigon_gui/guilib/threadwindow.py</code> <pre><code>def __init__(self, app, statusbar):\n    \"\"\"Initialize the thread manager.\n\n    Args:\n        app: The wx application.\n        statusbar: The status bar to place thread windows in.\n    \"\"\"\n    self.app = app\n    self.statusbar = statusbar\n    self.sizeChanged = True\n    self.windows = []\n    statusbar.Bind(wx.EVT_SIZE, self.on_size)\n    statusbar.Bind(wx.EVT_IDLE, self.on_idle)\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchThreadManager-functions","title":"Methods:","text":""},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchThreadManager.append","title":"append","text":"<pre><code>append(thread_address)\n</code></pre> <p>Add a new thread window for the given thread address.</p> <p>Parameters:</p> Name Type Description Default <code>thread_address</code> <p>Identifier for the background thread.</p> required Source code in <code>pytigon_gui/guilib/threadwindow.py</code> <pre><code>def append(self, thread_address):\n    \"\"\"Add a new thread window for the given thread address.\n\n    Args:\n        thread_address: Identifier for the background thread.\n    \"\"\"\n    self.windows.append(SchThreadWindow(self, thread_address, self.statusbar))\n    self.sizeChanged = True\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchThreadManager.on_idle","title":"on_idle","text":"<pre><code>on_idle(evt)\n</code></pre> <p>Handle idle event - reposition if size changed.</p> Source code in <code>pytigon_gui/guilib/threadwindow.py</code> <pre><code>def on_idle(self, evt):\n    \"\"\"Handle idle event - reposition if size changed.\"\"\"\n    if self.sizeChanged:\n        self.reposition()\n        self.sizeChanged = False\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchThreadManager.on_size","title":"on_size","text":"<pre><code>on_size(evt)\n</code></pre> <p>Handle status bar resize event.</p> Source code in <code>pytigon_gui/guilib/threadwindow.py</code> <pre><code>def on_size(self, evt):\n    \"\"\"Handle status bar resize event.\"\"\"\n    self.reposition()\n    self.sizeChanged = True\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchThreadManager.reposition","title":"reposition","text":"<pre><code>reposition()\n</code></pre> <p>Reposition and resize all thread windows in the status bar.</p> Source code in <code>pytigon_gui/guilib/threadwindow.py</code> <pre><code>def reposition(self):\n    \"\"\"Reposition and resize all thread windows in the status bar.\"\"\"\n    count = len(self.windows)\n    if count == 0:\n        return\n\n    rect = self.statusbar.GetFieldRect(1)\n    offset = 0\n    for win in self.windows:\n        win.SetPosition((int(rect.x + offset + 2), int(rect.y + 2)))\n        win.SetSize(\n            (\n                max(0, int(rect.width / count - 4)),\n                max(0, int(rect.height - 3)),\n            )\n        )\n        offset += rect.width / count\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.SchThreadManager.timer","title":"timer","text":"<pre><code>timer()\n</code></pre> <p>Periodic update for all thread windows.</p> <p>Removes closed windows and polls open ones. Uses list slicing to safely iterate while modifying the list.</p> Source code in <code>pytigon_gui/guilib/threadwindow.py</code> <pre><code>def timer(self):\n    \"\"\"Periodic update for all thread windows.\n\n    Removes closed windows and polls open ones. Uses list slicing\n    to safely iterate while modifying the list.\n    \"\"\"\n    if not self.windows:\n        return\n\n    # Iterate over a copy to safely remove while iterating\n    for win in self.windows[:]:\n        if win.is_closed():\n            win.Destroy()\n            self.windows.remove(win)\n            self.sizeChanged = True\n        else:\n            win.timer()\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon._base","title":"_base","text":""},{"location":"api/pytigon_main/#pytigon_gui.pytigon-functions","title":"Functions:","text":""},{"location":"api/pytigon_main/#pytigon_gui.pytigon._do_login_flow","title":"_do_login_flow","text":"<pre><code>_do_login_flow(app_name, address)\n</code></pre> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def _do_login_flow(app_name, address):\n    tab = app.get_tab(0)\n\n    autologin = True\n    if \"server_only\" in _PARAM:\n        app.gui_style = \"app.gui_style = tray(file(exit,open))\"\n        app.authorized = True\n        reinit = False\n    else:\n        reinit = True\n        for row in tab:\n            if row[0].data == \"autologin\":\n                autologin = row[1].data == \"1\"\n            elif row[0].data == \"gui_style\":\n                app.gui_style = row[1].data\n            elif row[0].data == \"csrf_token\":\n                app.csrf_token = row[1].data\n            elif \"start_page\" in row[0].data:\n                app.start_pages.extend(\n                    [x for x in row[1].data.split(\";\") if x and x != \"None\"]\n                )\n            elif row[0].data == \"title\":\n                app.title = row[1].data\n            elif row[0].data == \"plugins\":\n                if row[1].data and row[1].data != \"\":\n                    app.plugins = row[1].data.split(\";\")\n\n    app._install_plugins()\n\n    ready_to_run = True\n\n    if not app.authorized and (\n        (autologin and \"username\" not in _PARAM)\n        or (\"username\" in _PARAM and \"password\" in _PARAM)\n    ):\n        if \"username\" in _PARAM:\n            username2 = _PARAM[\"username\"]\n            password2 = _PARAM[\"password\"]\n        else:\n            env = get_environ(ROOT_PATH)\n            username2 = env(\"USERNAME\")\n            password2 = env(\"PASSWORD\")\n\n        ready_to_run = False\n        response = app.http.post(\n            app,\n            \"/\" + app_name + \"/schsys/do_login/?from_pytigon=1\"\n            if app_name\n            else \"/schsys/do_login/?from_pytigon\",\n            {\n                \"username\": username2,\n                \"password\": password2,\n                \"next\": address + \"/\" + app_name + \"/schsys/ok/\"\n                if app_name\n                else address + \"/schsys/ok/\",\n                \"client_param\": app._get_parm_for_server(),\n            },\n        )\n        ret_str = response.str()\n        if \"$$RETURN_OK\" in ret_str:\n            app.authorized = True\n            ready_to_run = True\n        else:\n            logger.error(\"Login error: %s\", ret_str)\n    if not app.authorized and \"username\" in _PARAM:\n        ready_to_run = False\n        href = \"/\" + app_name + \"/\" if app_name else \"/\"\n        if login(href, auth_type=None, username=_PARAM[\"username\"]):\n            app.authorized = True\n            ready_to_run = True\n    if reinit:\n        app._re_init(address, app_name)\n    return ready_to_run\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon._main_init","title":"_main_init","text":"<pre><code>_main_init()\n</code></pre> <p>Initialize the Pytigon application: parse args, set up Django, connect to server.</p> <p>Returns:</p> Type Description <p>Tuple of (ready_to_run: bool, nogui: bool), or (None, None) on failure.</p> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def _main_init():\n    \"\"\"Initialize the Pytigon application: parse args, set up Django, connect to server.\n\n    Returns:\n        Tuple of (ready_to_run: bool, nogui: bool), or (None, None) on failure.\n    \"\"\"\n    global CWD_PATH, _PARAM, app\n\n    args, apps, address, app_title, app_name, extern_prj = _setup_app_params()\n\n    os.environ[\"DJANGO_SETTINGS_MODULE\"] = \"settings_app\"\n    result = _process_args(args, address, app_name, extern_prj)\n    if result[0] is None:\n        return (None, None)\n    address, app_name, extern_prj = result\n\n    settings, cwd, inst_dir = _setup_django(apps)\n\n    address, server = _setup_server(address)\n\n    _start_task_queue()\n\n    settings.BASE_URL = \"http://\" + address\n    settings.URL_ROOT_FOLDER = \"\"\n    if \"server_only\" not in _PARAM:\n        init_ret = app._init2(address, app_name)\n        if init_ret != 200:\n            return (False, False)\n\n    app.server = server\n    app.cwd = cwd\n    app.inst_dir = inst_dir\n\n    app.title = app_title\n    ready_to_run = _do_login_flow(app_name, address)\n\n    return (ready_to_run, \"nogui\" in _PARAM)\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon._main_run","title":"_main_run","text":"<pre><code>_main_run()\n</code></pre> <p>Create the main application frame and start the wx event loop.</p> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def _main_run():\n    \"\"\"Create the main application frame and start the wx event loop.\"\"\"\n    app = wx.GetApp()\n    app.locale = wx.Locale(wx.LANGUAGE_DEFAULT)\n    app.locale.AddCatalogLookupPathPrefix(str(SRC_PATH / \"pytigon_gui/locale\"))\n\n    app.locale.AddCatalog(\"wx\")\n    app.locale.AddCatalog(\"pytigon\")\n\n    if app.embeded_browser:\n        frame = browserframe.SchBrowserFrame(\n            None,\n            app.gui_style,\n            wx.ID_ANY,\n            app.title,\n            wx.DefaultPosition,\n            wx.Size(_APP_SIZE[0], _APP_SIZE[1]),\n        )\n    else:\n        frame = appframe.SchAppFrame(\n            app.gui_style,\n            app.title,\n            wx.DefaultPosition,\n            wx.Size(_APP_SIZE[0], _APP_SIZE[1]),\n            video_name=_VIDEO_NAME,\n        )\n\n    frame.CenterOnScreen()\n\n    wx.Log.SetActiveTarget(wx.LogStderr())\n\n    destroy_fun_tab = frame.destroy_fun_tab\n    httpclient.set_http_error_func(http_error)\n\n    def idle_fun():\n        wx.GetApp().web_ctrl.OnIdle(None)\n\n    httpclient.set_http_idle_func(idle_fun)\n\n    if _RPC:\n        reactor.listenTCP(app.rpc, server.Site(app))\n\n    if _WEBSOCKET and app.base_address:\n        if \";\" in _WEBSOCKET:\n            websockets = _WEBSOCKET.split(\";\")\n        else:\n            websockets = [_WEBSOCKET]\n\n        local = (\n            True\n            if app.base_address and app.base_address.startswith(\"http://127.0.0.2\")\n            else False\n        )\n\n        for websocket_id in websockets:\n            create_websocket_client(app, websocket_id, local)\n\n    if \"import\" in _PARAM:\n        x = __import__(_PARAM[\"import\"])\n\n        def s():\n            nonlocal frame, x\n            x(frame)\n\n        wx.CallAfter(s)\n\n    if hasattr(wx, \"pseudoimport\"):\n        x = getattr(wx, \"pseudoimport\")\n\n        def s():\n            nonlocal frame, x\n            x(frame)\n\n        wx.CallAfter(s)\n\n    if \"channels\" in _PARAM or \"rpc\" in _PARAM or \"websocket\" in _PARAM:\n        asyncio.run(app.MainLoop())\n    else:\n        app.MainLoop()\n\n    if app.server:\n        app.server.stop()\n    del app\n    for pos in destroy_fun_tab:\n        pos()\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon._process_args","title":"_process_args","text":"<pre><code>_process_args(args, address, app_name, extern_prj)\n</code></pre> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def _process_args(args, address, app_name, extern_prj):\n    global CWD_PATH\n\n    if len(args) &gt; 0:\n        if \".ptig\" in args[0].lower():\n            prg_name = args[0].replace(\"\\\\\", \"/\").split(\"/\")[-1]\n            x = prg_name.split(\".\")\n            if len(x) == 2 or (len(x) &gt; 2 and x[-2].lower() == \"inst\"):\n                prg_name2 = x[0]\n                path = Path(PATHS[\"PRJ_PATH_ALT\"]) / \"_schremote\"\n                sys.path.append(str(path))\n                if not pytigon_install.install(args[0]):\n                    return None, None, None\n                CWD_PATH = Path(PATHS[\"PRJ_PATH\"]) / prg_name2\n                return None, None, None\n            else:\n                if len(x) &gt; 3:\n                    prg_name2 = x[0]\n                    app_name2 = x[-2]\n                    prj = x[-3]\n                    CWD_PATH = Path(PATHS[\"PRJ_PATH\"]) / prj.strip()\n                    if not (Path(CWD_PATH) / \"settings_app.py\").exists():\n                        logger.error(\n                            _(\"Application pack: '%s' does not exists\"), prj.strip()\n                        )\n                        return None, None, None\n                    wx.CallAfter(app.run_script, app_name2, args[0])\n                else:\n                    logger.error(_(\"Name of script: '%s' is not valid\"), prg_name)\n                    return None, None, None\n        else:\n            arg = args[0].strip()\n            if arg == \"embeded\" or \".\" in arg or \"/\" in arg:\n                if arg != \"embeded\":\n                    CWD_PATH = Path(PATHS[\"PRJ_PATH_ALT\"]) / \"_schremote\"\n\n                tmp = arg.replace(\"//\", \"$$$\")\n                if \"/\" in arg:\n                    x = tmp.split(\"/\", 1)\n                    address = x[0].replace(\"$$$\", \"//\")\n                    if len(x) &gt; 1:\n                        app_name = x[1]\n                        if app_name.endswith(\"/\"):\n                            app_name = app_name[:-1]\n                else:\n                    address = arg\n\n                extern_prj = True\n            else:\n                CWD_PATH = Path(PATHS[\"PRJ_PATH\"]) / arg\n                if not (Path(CWD_PATH) / \"settings_app.py\").exists():\n                    logger.error(_(\"Application pack: '%s' does not exists\"), arg)\n                    return None, None, None\n\n    return address, app_name, extern_prj\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon._setup_app_params","title":"_setup_app_params","text":"<pre><code>_setup_app_params()\n</code></pre> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def _setup_app_params():\n    global CWD_PATH, _PARAM, app\n\n    args = _PARAM[\"args\"]\n    apps = []\n    address = \"http://127.0.0.2\"\n    app_title = _(\"Pytigon\")\n    app_name = \"\"\n\n    app = SchApp()\n    if \"menu_always\" in _PARAM:\n        app.menu_always = True\n    if \"rpc\" in _PARAM:\n        app.rpc = int(_PARAM[\"rpc\"])\n    if \"param\" in _PARAM:\n        app.param = _PARAM[\"param\"]\n    if \"embeded_browser\" in _PARAM:\n        app.embeded_browser = True\n    else:\n        app.embeded_browser = False\n    if \"extern_prj\" in _PARAM:\n        extern_prj = True\n    else:\n        extern_prj = False\n    if \"address\" in _PARAM:\n        address = _PARAM[\"address\"]\n\n    return args, apps, address, app_title, app_name, extern_prj\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon._setup_django","title":"_setup_django","text":"<pre><code>_setup_django(apps)\n</code></pre> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def _setup_django(apps):\n    sys.path.insert(0, str(CWD_PATH))\n    httpclient.init_embeded_django()\n\n    if not (\"channels\" in _PARAM or \"rpc\" in _PARAM):\n        wx.Yield()\n    import settings_app\n\n    os.environ[\"DJANGO_SETTINGS_MODULE\"] = \"settings_app\"\n    from django.conf import settings\n\n    if wx.Font.CanUsePrivateFont():\n        fonts_path = Path(settings.STATIC_ROOT) / \"fonts\"\n        font_names = [\n            \"DejaVuSansCondensed.ttf\",\n            \"DejaVuSansCondensed-Bold.ttf\",\n            \"DejaVuSansCondensed-Oblique.ttf\",\n            \"DejaVuSansCondensed-BoldOblique.ttf\",\n            \"DejaVuSerifCondensed.ttf\",\n            \"DejaVuSerifCondensed-Bold.ttf\",\n            \"DejaVuSerifCondensed-Italic.ttf\",\n            \"DejaVuSerifCondensed-BoldItalic.ttf\",\n            \"DejaVuSansMono.ttf\",\n            \"DejaVuSansMono-Bold.ttf\",\n            \"DejaVuSansMono-Oblique.ttf\",\n            \"DejaVuSansMono-BoldOblique.ttf\",\n        ]\n        for font_name in font_names:\n            wx.Font.AddPrivateFont(str(Path(fonts_path) / font_name))\n\n    if \"sync\" in _PARAM:\n        from django.core.management.commands.migrate import Command as migrate_command\n\n        migrate = migrate_command()\n        with contextlib.suppress(SystemExit):\n            migrate.run_from_argv([\"manage.py\", \"migrate\"])\n    if \"loaddb\" in _PARAM:\n        from django.core.management.commands.loaddata import Command as load_command\n\n        load = load_command()\n        with contextlib.suppress(SystemExit):\n            load.run_from_argv([\"manage.py\", \"loaddata\"])\n\n    cwd = str(CWD_PATH)\n    inst_dir = str(SRC_PATH)\n    if inst_dir == \"\":\n        inst_dir = cwd\n\n    settings.TEMPLATES[0][\"DIRS\"].insert(0, inst_dir + \"/appdata/plugins\")\n    settings.TEMPLATES[0][\"DIRS\"].insert(0, cwd + \"/appdata/plugins\")\n    settings.TEMPLATES[0][\"DIRS\"].insert(0, inst_dir + \"/../templates\")\n    settings.TEMPLATES[0][\"DIRS\"].insert(0, cwd + \"/templates\")\n\n    for a in apps:\n        settings.INSTALLED_APPS.append(a)\n\n    return settings, cwd, inst_dir\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon._setup_server","title":"_setup_server","text":"<pre><code>_setup_server(address)\n</code></pre> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def _setup_server(address):\n    port = 0\n    if \"server_only\" in _PARAM:\n        port = 8000\n        if \":\" in address:\n            l = address.split(\":\")\n            if len(l) == 3:\n                port = int(l[2])\n        address = \"embeded\"\n    if address == \"embeded\":\n        import socket\n        from pytigon_lib.schdjangoext.server import run_server\n\n        if \"server_only\" in _PARAM:\n            address = \"127.0.0.1\"\n            if \"listen\" in _PARAM:\n                if \":\" in _PARAM[\"listen\"]:\n                    address, port = _PARAM[\"listen\"].split(\":\")\n                    port = int(port)\n                else:\n                    address = _PARAM[\"listen\"]\n                    port = 8000\n        else:\n            address = \"127.0.0.3\"\n        if port == 0:\n            port = 8000\n        test = True\n        s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\n        while test:\n            try:\n                s.bind((address, port))\n                s.close()\n                s = None\n                test = False\n            except OSError:\n                port += 1\n\n        if \"extra\" in _PARAM:\n            server = run_server(address, port, prod=False, params=_PARAM[\"extra\"])\n        else:\n            server = run_server(address, port, prod=False)\n        address = \"http://\" + address + \":\" + str(port)\n    else:\n        server = None\n\n    return address, server\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon._start_task_queue","title":"_start_task_queue","text":"<pre><code>_start_task_queue()\n</code></pre> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def _start_task_queue():\n    if \"embeded_taskqueue\" in _PARAM:\n        from multiprocessing import Process\n        from django_q.management.commands.qcluster import Command as qcluster_command\n\n        qcluster = qcluster_command()\n        app.task_manager = Process(\n            target=qcluster.run_from_argv, args=([\"manage.py\", \"qcluster\"],)\n        )\n        app.task_manager.start()\n        logger.info(\"Task manager started\")\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.colour_to_html","title":"colour_to_html","text":"<pre><code>colour_to_html(colour)\n</code></pre> <p>Convert a wx.Colour to an HTML hex string (e.g. '#AABBCC').</p> <p>Parameters:</p> Name Type Description Default <code>colour</code> <p>wx.Colour instance.</p> required <p>Returns:</p> Type Description <p>HTML color string.</p> Source code in <code>pytigon_gui/guilib/tools.py</code> <pre><code>def colour_to_html(colour):\n    \"\"\"Convert a wx.Colour to an HTML hex string (e.g. '#AABBCC').\n\n    Args:\n        colour: wx.Colour instance.\n\n    Returns:\n        HTML color string.\n    \"\"\"\n    return wx.Colour(colour.Red(), colour.Green(), colour.Blue()).GetAsString(\n        wx.C2S_HTML_SYNTAX\n    )\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.http_error","title":"http_error","text":"<pre><code>http_error(parent, content)\n</code></pre> <p>Schedule an HTTP error dialog to be shown in the main GUI thread.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window.</p> required <code>content</code> <p>HTML error page content.</p> required Source code in <code>pytigon_gui/guilib/httperror.py</code> <pre><code>def http_error(parent, content):\n    \"\"\"Schedule an HTTP error dialog to be shown in the main GUI thread.\n\n    Args:\n        parent: Parent window.\n        content: HTML error page content.\n    \"\"\"\n    wx.CallAfter(_http_error, parent, content)\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.login","title":"login","text":"<pre><code>login(base_href, auth_type=None, username=None)\n</code></pre> <p>Show the login dialog and attempt authentication against the server.</p> <p>Parameters:</p> Name Type Description Default <code>base_href</code> <p>base URL for authentication requests.</p> required <code>auth_type</code> <p>authentication type ('basic' for HTTP basic auth, None for form-based).</p> <code>None</code> <code>username</code> <p>pre-filled username (optional).</p> <code>None</code> <p>Returns:</p> Type Description <p>True if login succeeded, False otherwise.</p> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def login(base_href, auth_type=None, username=None):\n    \"\"\"Show the login dialog and attempt authentication against the server.\n\n    Args:\n        base_href: base URL for authentication requests.\n        auth_type: authentication type ('basic' for HTTP basic auth, None for form-based).\n        username: pre-filled username (optional).\n\n    Returns:\n        True if login succeeded, False otherwise.\n    \"\"\"\n    dlg = LoginDialog(None, 101, _(\"Pytigon - login\"), username=username)\n\n    while dlg.ShowModal() == wx.ID_OK:\n        try:\n            username = dlg.text1.GetValue()\n            password = dlg.text2.GetValue()\n\n            parm = {\n                \"username\": username,\n                \"password\": password,\n                \"next\": \"/schsys/ok/\",\n                \"client_param\": wx.GetApp()._get_parm_for_server(),\n            }\n            if auth_type is None:\n                ret = wx.GetApp().http.post(\n                    wx.GetApp(),\n                    \"/schsys/do_login/?from_pytigon=1\",\n                    parm,\n                    credentials=(username, password),\n                )\n\n                if ret is None:\n                    dlg.message.SetLabel(_(\"Connection error!\"))\n                    continue\n\n                ret_str = ret.str()\n\n                if \"$$RETURN_OK\" in ret_str:\n                    dlg.Destroy()\n                    return True\n                else:\n                    if \"id_password\" not in ret_str:\n                        dlg.Destroy()\n                        return False\n                    else:\n                        dlg.message.SetLabel(_(\"Failed login attempt!\"))\n            else:\n                result = wx.GetApp().http.get(\n                    wx.GetApp(), base_href, credentials=(username, password)\n                )\n                if result is not None and result.ret_code == 200:\n                    dlg.Destroy()\n                    return True\n                else:\n                    ret_code = result.ret_code if result else \"N/A\"\n                    dlg.message.SetLabel(\n                        _(f\"Failed login attempt! http error: {ret_code}\")\n                    )\n        except Exception as e:\n            dlg.message.SetLabel(_(f\"Login error: {str(e)}\"))\n    dlg.Destroy()\n    return False\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.main","title":"main","text":"<pre><code>main()\n</code></pre> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def main():\n    ready_to_run, nogui = _main_init()\n    if ready_to_run:\n        if nogui:\n            while True:\n                time.sleep(100)\n        else:\n            _main_run()\n    os._exit(0)\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.process_adv_argv","title":"process_adv_argv","text":"<pre><code>process_adv_argv()\n</code></pre> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def process_adv_argv():\n    global _PARAM\n    if not (\"args\" in _PARAM and len(_PARAM[\"args\"]) &gt; 0):\n        _app = wx.App()\n\n        choices = [\n            ff\n            for ff in os.listdir(PATHS[\"PRJ_PATH\"])\n            if not ff.startswith(\"_\")\n            and (Path(PATHS[\"PRJ_PATH\"]) / ff).is_dir()\n            and (Path(PATHS[\"PRJ_PATH\"]) / ff / \"settings_app.py\").exists()\n        ]\n        dlg = wx.SingleChoiceDialog(\n            None,\n            _(\"select the application to run\"),\n            _(\"Pytigon\"),\n            choices,\n            wx.CHOICEDLG_STYLE,\n        )\n        if dlg.ShowModal() == wx.ID_OK:\n            arg = dlg.GetStringSelection()\n            dlg.Destroy()\n\n        else:\n            dlg.Destroy()\n            sys.exit(0)\n\n        _app.MainLoop()\n        _app = None\n\n    else:\n        arg = _PARAM[\"args\"][0].strip()\n    if not (arg == \"embeded\" or \".\" in arg or \"/\" in arg):\n        CWD_PATH = Path(PATHS[\"PRJ_PATH\"]) / arg\n        if not (Path(CWD_PATH) / \"settings_app.py\").exists():\n            logger.error(_(\"Application pack: '%s' does not exists\"), arg)\n            sys.exit(0)\n        else:\n            sys.path.insert(0, str(CWD_PATH))\n\n            import settings_app\n\n            if hasattr(settings_app, \"GUI_COMMAND_LINE\"):\n                x = settings_app.GUI_COMMAND_LINE.split(\" \")\n                param = process_argv(x)\n                for key, value in param.items():\n                    if key not in _PARAM:\n                        _PARAM[key] = value\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.process_argv","title":"process_argv","text":"<pre><code>process_argv(argv)\n</code></pre> <p>Run pytigon application: pytigon [option]... arg</p> command line arguments <p>options:     -h, --help - show help</p> <pre><code>-b --embededbrowser - run embeded browseer window\n-s --embededserver - run in background embeded http server\n--listen=&lt;address&gt;:&lt;port&gt; - set address and port for embeded serwer\n--menu_always - show menu event then configuration says otherwise\n--no_splash - do not show splash window\n--no_gui - run program without gui\n--server_only - run only http server\n--channels - start with channels library\n--websocket_id=relative address - embed websocket client, address can be mulitipart,\n               parts separated by a semicolon\n--no_gui - run program without gui\n\n--migrate - run django command: manage.py migrate\n--loaddb - run django command: manage.py loaddb\n\n--rpc=&lt;port&gt; - set tcp port of rpc\n--video - record video session\n-p &lt;parameters&gt;, --param=&lt;parameters&gt; - parametres of request to http server\n\n-d --debug - debug mode\n\n--inspection - turn on wxPython inspection\n--trace - show trace of python calls\n</code></pre> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def process_argv(argv):\n    \"\"\"Run pytigon application: pytigon [option]... arg\n\n    command line arguments:\n        arg: project name, schdevtools for example\n             or address of http server, http://www.pytigon.cloud for example\n             or pytigon script name, test.schdevtools.schsimplescripts.ptig for example\n             or pytigon installation file name\n             or django management command in format: manage_[[package name]], manage_schdevtools runserver for example\n             or python script, test.py for example\n\n        options:\n            -h, --help - show help\n\n            -b --embededbrowser - run embeded browseer window\n            -s --embededserver - run in background embeded http server\n            --listen=&lt;address&gt;:&lt;port&gt; - set address and port for embeded serwer\n            --menu_always - show menu event then configuration says otherwise\n            --no_splash - do not show splash window\n            --no_gui - run program without gui\n            --server_only - run only http server\n            --channels - start with channels library\n            --websocket_id=relative address - embed websocket client, address can be mulitipart,\n                           parts separated by a semicolon\n            --no_gui - run program without gui\n\n            --migrate - run django command: manage.py migrate\n            --loaddb - run django command: manage.py loaddb\n\n            --rpc=&lt;port&gt; - set tcp port of rpc\n            --video - record video session\n            -p &lt;parameters&gt;, --param=&lt;parameters&gt; - parametres of request to http server\n\n            -d --debug - debug mode\n\n            --inspection - turn on wxPython inspection\n            --trace - show trace of python calls\n    \"\"\"\n    try:\n        (opts, args) = getopt.gnu_getopt(\n            argv,\n            \"h:dmpbsu:p:\",\n            [\n                \"help\",\n                \"username=\",\n                \"password=\",\n                \"embededbrowser\",\n                \"embededserver\",\n                \"embededtaskqueue\",\n                \"websocket_id=\",\n                \"channels\",\n                \"migrate\",\n                \"loaddb\",\n                \"server_only\",\n                \"debug\",\n                \"rpc=\",\n                \"param=\",\n                \"inspection\",\n                \"trace\",\n                \"video=\",\n                \"no_gui\",\n                \"no_splash\",\n                \"menu_always\",\n                \"listen=\",\n                \"import=\",\n                \"extra=\",\n            ],\n        )\n    except getopt.GetoptError:\n        usage()\n        return None\n\n    ret = {\"args\": args}\n\n    for opt, arg in opts:\n        if opt in (\"-h\", \"--help\"):\n            usage()\n            return None\n        elif opt == \"--migrate\":\n            ret[\"sync\"] = True\n        elif opt == \"--loaddb\":\n            ret[\"loaddb\"] = True\n        elif opt == \"--server_only\":\n            ret[\"server_only\"] = True\n        elif opt in (\"-u\", \"--username\"):\n            ret[\"username\"] = arg\n        elif opt in (\"-p\", \"--password\"):\n            ret[\"password\"] = arg\n        elif opt in (\"--listen\",):\n            ret[\"listen\"] = arg\n        elif opt in (\"--extra\",):\n            ret[\"extra\"] = arg\n        elif opt in (\"--import\",):\n            ret[\"import\"] = arg\n        elif opt in (\"-b\", \"--embededbrowser\"):\n            ret[\"embeded_browser\"] = True\n        elif opt in (\"-s\", \"--embededserver\"):\n            ret[\"address\"] = \"embeded\"\n            ret[\"extern_prj\"] = True\n        elif opt in (\"--embededtaskqueue\",):\n            ret[\"embeded_taskqueue\"] = True\n        elif opt == \"--no_gui\":\n            ret[\"nogui\"] = True\n        elif opt in (\"--menu_always\",):\n            ret[\"menu_always\"] = True\n        elif opt in (\"--rpc\"):\n            ret[\"rpc\"] = int(arg)\n        elif opt in (\"-p\", \"--param\"):\n            ret[\"param\"] = arg\n        elif opt in (\"--channels\"):\n            ret[\"channels\"] = True\n        elif opt in (\"--websocket_id\",):\n            ret[\"websocket\"] = arg\n        elif opt in (\"--no_splash\"):\n            ret[\"no_splash\"] = True\n        elif opt in (\"-d\", \"--debug\"):\n            global _DEBUG\n            _DEBUG = True\n        elif opt in (\"--inspection\",):\n            global _INSPECTION\n            _INSPECTION = True\n        elif opt in (\"--trace\",):\n            global _TRACE\n            _TRACE = True\n        elif opt in (\"--video\",):\n            global _VIDEO, _APP_SIZE, _VIDEO_NAME\n            _APP_SIZE = (1280, 720)\n            _VIDEO = True\n            _VIDEO_NAME = arg\n            if not _VIDEO_NAME:\n                _VIDEO_NAME = \"video.avi\"\n    return ret\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.standard_tab_colour","title":"standard_tab_colour","text":"<pre><code>standard_tab_colour()\n</code></pre> <p>Return a tuple of (name, html_color) pairs for tab styling.</p> <p>These colors are derived from the system 3D face color at various blend levels, plus highlight, shadow, and info background colors.</p> <p>Returns:</p> Type Description <p>Tuple of (str, str) pairs mapping CSS variable names to HTML colors.</p> Source code in <code>pytigon_gui/guilib/tools.py</code> <pre><code>def standard_tab_colour():\n    \"\"\"Return a tuple of (name, html_color) pairs for tab styling.\n\n    These colors are derived from the system 3D face color at various\n    blend levels, plus highlight, shadow, and info background colors.\n\n    Returns:\n        Tuple of (str, str) pairs mapping CSS variable names to HTML colors.\n    \"\"\"\n    return (\n        (\"color_body_0_2\", get_colour(wx.SYS_COLOUR_3DFACE, 0.2)),\n        (\"color_body_0_5\", get_colour(wx.SYS_COLOUR_3DFACE, 0.5)),\n        (\"color_body_0_7\", get_colour(wx.SYS_COLOUR_3DFACE, 0.7)),\n        (\"color_body_0_9\", get_colour(wx.SYS_COLOUR_3DFACE, 0.9)),\n        (\"color_body\", get_colour(wx.SYS_COLOUR_3DFACE)),\n        (\"color_body_1_1\", get_colour(wx.SYS_COLOUR_3DFACE, 1.1)),\n        (\"color_body_1_3\", get_colour(wx.SYS_COLOUR_3DFACE, 1.3)),\n        (\"color_body_1_5\", get_colour(wx.SYS_COLOUR_3DFACE, 1.5)),\n        (\"color_body_1_8\", get_colour(wx.SYS_COLOUR_3DFACE, 1.8)),\n        (\"color_higlight\", get_colour(wx.SYS_COLOUR_3DHIGHLIGHT)),\n        (\"color_shadow\", get_colour(wx.SYS_COLOUR_3DSHADOW)),\n        (\"color_background_0_5\", get_colour(wx.SYS_COLOUR_3DFACE, 0.5)),\n        (\"color_background_0_8\", get_colour(wx.SYS_COLOUR_3DFACE, 0.8)),\n        (\"color_background_0_9\", get_colour(wx.SYS_COLOUR_3DFACE, 0.9)),\n        (\"color_background\", get_colour(wx.SYS_COLOUR_3DFACE)),\n        (\"color_background_1_1\", get_colour(wx.SYS_COLOUR_3DFACE, 1.1)),\n        (\"color_background_1_2\", get_colour(wx.SYS_COLOUR_3DFACE, 1.2)),\n        (\"color_background_1_5\", get_colour(wx.SYS_COLOUR_3DFACE, 1.5)),\n        (\"color_info\", get_colour(wx.SYS_COLOUR_INFOBK, 1.5)),\n    )\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.trace_calls","title":"trace_calls","text":"<pre><code>trace_calls(frame, event, arg)\n</code></pre> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def trace_calls(frame, event, arg):\n    if event != \"call\":\n        return\n    co = frame.f_code\n    func_name = co.co_name\n    if func_name == \"write\":\n        # Ignore write() calls from print statements\n        return\n    for pos in (\"process_window_event\", \"idle\", \"timer\", \"update_ui\"):\n        if pos in func_name:\n            return\n    func_line_no = frame.f_lineno\n    func_filename = co.co_filename\n    if not \"wx/core\" in func_filename:\n        return\n    caller = frame.f_back\n    caller_line_no = caller.f_lineno\n    caller_filename = caller.f_code.co_filename\n    logger.debug(\n        \"Call to %s on line %s of %s from line %s of %s\",\n        func_name,\n        func_line_no,\n        func_filename,\n        caller_line_no,\n        caller_filename,\n    )\n    return\n</code></pre>"},{"location":"api/pytigon_main/#pytigon_gui.pytigon.usage","title":"usage","text":"<pre><code>usage()\n</code></pre> Source code in <code>pytigon_gui/pytigon.py</code> <pre><code>def usage():\n    sys.stdout.write(str(process_argv.__doc__) + \"\\n\")\n</code></pre>"},{"location":"api/toolbar/","title":"toolbar \u2013 Toolbar Package","text":"<p>Toolbar system providing modern ribbon-style, standard classic, tree-based, and generic toolbars with button management.</p>"},{"location":"api/toolbar/#pytigon_gui.toolbar","title":"toolbar","text":"<p>Toolbar package.</p> Provides various toolbar implementations <ul> <li>basetoolbar: Abstract base classes (ToolbarBar, ToolbarPage, etc.)</li> <li>standardtoolbar: Classic wx.ToolBar-based toolbar</li> <li>generictoolbar: AUI dockable toolbar</li> <li>moderntoolbar: Ribbon-style toolbar</li> <li>treetoolbar: Vertical tree navigation toolbar</li> <li>menubar: Standard wx.MenuBar-based menu</li> <li>standardtoolbarbuttons: Standard button creation and event handling</li> </ul>"},{"location":"api/toolbar_basetoolbar/","title":"toolbar.basetoolbar \u2013 Base Toolbar","text":"<p>Abstract base class for all toolbars providing common infrastructure: button registration, layout calculation, and event routing.</p>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar","title":"basetoolbar","text":"<p>Base classes for toolbars and menu bar.</p> Class hierarchy <p>ToolbarBar     ToolbarPage         ToolbarPanel             ToolbarButton             ToolbarButton             ...         ToolbarPanel         ...         BaseHtmlPanel     ...</p> <p>One ToolbarBar can contain many ToolbarPage objects, one ToolbarPage can contain many ToolbarPanel objects, etc.</p> Concrete toolbars and menus based on these abstract classes <ul> <li>MenuToolbarBar</li> <li>GenericToolbarBar</li> <li>StandardToolbarBar</li> <li>ModernToolbarBar</li> <li>TreeToolbarBar</li> </ul>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar-attributes","title":"Attributes","text":""},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_BOOKMARK_ADD","title":"ID_BOOKMARK_ADD  <code>module-attribute</code>","text":"<pre><code>ID_BOOKMARK_ADD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_CLEAR_HISTORY","title":"ID_CLEAR_HISTORY  <code>module-attribute</code>","text":"<pre><code>ID_CLEAR_HISTORY = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_CLOSETAB","title":"ID_CLOSETAB  <code>module-attribute</code>","text":"<pre><code>ID_CLOSETAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_COPY","title":"ID_COPY  <code>module-attribute</code>","text":"<pre><code>ID_COPY = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_CUT","title":"ID_CUT  <code>module-attribute</code>","text":"<pre><code>ID_CUT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_DOWNLOAD_OPTIONS","title":"ID_DOWNLOAD_OPTIONS  <code>module-attribute</code>","text":"<pre><code>ID_DOWNLOAD_OPTIONS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_END","title":"ID_END  <code>module-attribute</code>","text":"<pre><code>ID_END = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_EXIT","title":"ID_EXIT  <code>module-attribute</code>","text":"<pre><code>ID_EXIT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_FIND","title":"ID_FIND  <code>module-attribute</code>","text":"<pre><code>ID_FIND = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_GENERAL_OPTIONS","title":"ID_GENERAL_OPTIONS  <code>module-attribute</code>","text":"<pre><code>ID_GENERAL_OPTIONS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_GOTODESKTOP","title":"ID_GOTODESKTOP  <code>module-attribute</code>","text":"<pre><code>ID_GOTODESKTOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_GOTOFOOTER","title":"ID_GOTOFOOTER  <code>module-attribute</code>","text":"<pre><code>ID_GOTOFOOTER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_GOTOHEADER","title":"ID_GOTOHEADER  <code>module-attribute</code>","text":"<pre><code>ID_GOTOHEADER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_GOTOPANEL","title":"ID_GOTOPANEL  <code>module-attribute</code>","text":"<pre><code>ID_GOTOPANEL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_HELP","title":"ID_HELP  <code>module-attribute</code>","text":"<pre><code>ID_HELP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_HISTORY_SHOW","title":"ID_HISTORY_SHOW  <code>module-attribute</code>","text":"<pre><code>ID_HISTORY_SHOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_LOAD","title":"ID_LOAD  <code>module-attribute</code>","text":"<pre><code>ID_LOAD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_NEXTPAGE","title":"ID_NEXTPAGE  <code>module-attribute</code>","text":"<pre><code>ID_NEXTPAGE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_NEXTTAB","title":"ID_NEXTTAB  <code>module-attribute</code>","text":"<pre><code>ID_NEXTTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_PAGE_BLOCK","title":"ID_PAGE_BLOCK  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_BLOCK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_PAGE_SOURCE","title":"ID_PAGE_SOURCE  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_SOURCE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_PAGE_VIEW","title":"ID_PAGE_VIEW  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_VIEW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_PASTE","title":"ID_PASTE  <code>module-attribute</code>","text":"<pre><code>ID_PASTE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_PREVPAGE","title":"ID_PREVPAGE  <code>module-attribute</code>","text":"<pre><code>ID_PREVPAGE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_PREVTAB","title":"ID_PREVTAB  <code>module-attribute</code>","text":"<pre><code>ID_PREVTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_PRINT","title":"ID_PRINT  <code>module-attribute</code>","text":"<pre><code>ID_PRINT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_PRINT_PREVIEW","title":"ID_PRINT_PREVIEW  <code>module-attribute</code>","text":"<pre><code>ID_PRINT_PREVIEW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_PRINT_SETTINGS","title":"ID_PRINT_SETTINGS  <code>module-attribute</code>","text":"<pre><code>ID_PRINT_SETTINGS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_REDO","title":"ID_REDO  <code>module-attribute</code>","text":"<pre><code>ID_REDO = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_REFRESHTAB","title":"ID_REFRESHTAB  <code>module-attribute</code>","text":"<pre><code>ID_REFRESHTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_REPLACE","title":"ID_REPLACE  <code>module-attribute</code>","text":"<pre><code>ID_REPLACE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_RESET","title":"ID_RESET  <code>module-attribute</code>","text":"<pre><code>ID_RESET = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_RETVALUE","title":"ID_RETVALUE  <code>module-attribute</code>","text":"<pre><code>ID_RETVALUE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_SAVE","title":"ID_SAVE  <code>module-attribute</code>","text":"<pre><code>ID_SAVE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_SAVE_AS","title":"ID_SAVE_AS  <code>module-attribute</code>","text":"<pre><code>ID_SAVE_AS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_SELALL","title":"ID_SELALL  <code>module-attribute</code>","text":"<pre><code>ID_SELALL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_SHOWDESKTOP","title":"ID_SHOWDESKTOP  <code>module-attribute</code>","text":"<pre><code>ID_SHOWDESKTOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_SHOWFOOTER","title":"ID_SHOWFOOTER  <code>module-attribute</code>","text":"<pre><code>ID_SHOWFOOTER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_SHOWHEADER","title":"ID_SHOWHEADER  <code>module-attribute</code>","text":"<pre><code>ID_SHOWHEADER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_SHOWPANEL","title":"ID_SHOWPANEL  <code>module-attribute</code>","text":"<pre><code>ID_SHOWPANEL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_SHOWSTATUSBAR","title":"ID_SHOWSTATUSBAR  <code>module-attribute</code>","text":"<pre><code>ID_SHOWSTATUSBAR = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_SHOWTOOLBAR1","title":"ID_SHOWTOOLBAR1  <code>module-attribute</code>","text":"<pre><code>ID_SHOWTOOLBAR1 = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_SHOWTOOLBAR2","title":"ID_SHOWTOOLBAR2  <code>module-attribute</code>","text":"<pre><code>ID_SHOWTOOLBAR2 = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_SHOW_BOOKMARKS","title":"ID_SHOW_BOOKMARKS  <code>module-attribute</code>","text":"<pre><code>ID_SHOW_BOOKMARKS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_SHOW_DOWNLOAD","title":"ID_SHOW_DOWNLOAD  <code>module-attribute</code>","text":"<pre><code>ID_SHOW_DOWNLOAD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_START","title":"ID_START  <code>module-attribute</code>","text":"<pre><code>ID_START = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_TASKBAR_CLOSE","title":"ID_TASKBAR_CLOSE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_CLOSE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_TASKBAR_HIDE","title":"ID_TASKBAR_HIDE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_HIDE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_TASKBAR_SHOW","title":"ID_TASKBAR_SHOW  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_SHOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_TASKBAR_TOGGLE","title":"ID_TASKBAR_TOGGLE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_TOGGLE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_UNDO","title":"ID_UNDO  <code>module-attribute</code>","text":"<pre><code>ID_UNDO = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_WEB_ADDBOOKMARK","title":"ID_WEB_ADDBOOKMARK  <code>module-attribute</code>","text":"<pre><code>ID_WEB_ADDBOOKMARK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_WEB_BACK","title":"ID_WEB_BACK  <code>module-attribute</code>","text":"<pre><code>ID_WEB_BACK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_WEB_DOWN","title":"ID_WEB_DOWN  <code>module-attribute</code>","text":"<pre><code>ID_WEB_DOWN = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_WEB_EDIT","title":"ID_WEB_EDIT  <code>module-attribute</code>","text":"<pre><code>ID_WEB_EDIT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_WEB_FORWARD","title":"ID_WEB_FORWARD  <code>module-attribute</code>","text":"<pre><code>ID_WEB_FORWARD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_WEB_NEW_WINDOW","title":"ID_WEB_NEW_WINDOW  <code>module-attribute</code>","text":"<pre><code>ID_WEB_NEW_WINDOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_WEB_REFRESH","title":"ID_WEB_REFRESH  <code>module-attribute</code>","text":"<pre><code>ID_WEB_REFRESH = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_WEB_SOURCE","title":"ID_WEB_SOURCE  <code>module-attribute</code>","text":"<pre><code>ID_WEB_SOURCE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_WEB_STOP","title":"ID_WEB_STOP  <code>module-attribute</code>","text":"<pre><code>ID_WEB_STOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_WEB_UP","title":"ID_WEB_UP  <code>module-attribute</code>","text":"<pre><code>ID_WEB_UP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ID_ZOOM","title":"ID_ZOOM  <code>module-attribute</code>","text":"<pre><code>ID_ZOOM = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar._","title":"_  <code>module-attribute</code>","text":"<pre><code>_ = wx.GetTranslation\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar-classes","title":"Classes","text":""},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.BaseHtmlPanel","title":"BaseHtmlPanel","text":"<pre><code>BaseHtmlPanel(page, real_panel)\n</code></pre> <p>Base panel that can host an HTML-rendered page within a toolbar.</p> <p>This is used by toolbar types that support embedding web content (e.g. ModernToolbarBar, TreeToolbarBar).</p> <p>Initialize the HTML panel wrapper.</p> <p>Parameters:</p> Name Type Description Default <code>page</code> <p>Parent ToolbarPage object.</p> required <code>real_panel</code> <p>The actual wx.Panel that will contain the HTML page.</p> required Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, page, real_panel):\n    \"\"\"Initialize the HTML panel wrapper.\n\n    Args:\n        page: Parent ToolbarPage object.\n        real_panel: The actual wx.Panel that will contain the HTML page.\n    \"\"\"\n    self.page = page\n    self.real_panel = real_panel\n    self.html_page = None\n    self.sizer = wx.BoxSizer(wx.VERTICAL)\n    self.real_panel.SetSizer(self.sizer)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.BaseHtmlPanel-functions","title":"Methods:","text":""},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.BaseHtmlPanel.get_height","title":"get_height","text":"<pre><code>get_height()\n</code></pre> <p>Return the preferred height of the panel.</p> <p>Override in subclasses to provide actual dimensions.</p> <p>Returns:</p> Name Type Description <code>int</code> <p>Height in pixels (0 by default).</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def get_height(self):\n    \"\"\"Return the preferred height of the panel.\n\n    Override in subclasses to provide actual dimensions.\n\n    Returns:\n        int: Height in pixels (0 by default).\n    \"\"\"\n    return 0\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.BaseHtmlPanel.get_width","title":"get_width","text":"<pre><code>get_width()\n</code></pre> <p>Return the preferred width of the panel.</p> <p>Override in subclasses to provide actual dimensions.</p> <p>Returns:</p> Name Type Description <code>int</code> <p>Width in pixels (0 by default).</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def get_width(self):\n    \"\"\"Return the preferred width of the panel.\n\n    Override in subclasses to provide actual dimensions.\n\n    Returns:\n        int: Width in pixels (0 by default).\n    \"\"\"\n    return 0\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.BaseHtmlPanel.get_window","title":"get_window","text":"<pre><code>get_window()\n</code></pre> <p>Return the underlying wx.Window for this panel.</p> <p>Returns:</p> Type Description <p>wx.Window: The real panel window.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def get_window(self):\n    \"\"\"Return the underlying wx.Window for this panel.\n\n    Returns:\n        wx.Window: The real panel window.\n    \"\"\"\n    return self.real_panel\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.BaseHtmlPanel.set_page","title":"set_page","text":"<pre><code>set_page(html_page)\n</code></pre> <p>Replace the current HTML page with a new one.</p> <p>If a page already exists, it is replaced in the sizer and destroyed. Otherwise the new page is added to the sizer.</p> <p>Parameters:</p> Name Type Description Default <code>html_page</code> <p>The new SchPage (or wx.Window) to display.</p> required Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def set_page(self, html_page):\n    \"\"\"Replace the current HTML page with a new one.\n\n    If a page already exists, it is replaced in the sizer and destroyed.\n    Otherwise the new page is added to the sizer.\n\n    Args:\n        html_page: The new SchPage (or wx.Window) to display.\n    \"\"\"\n    if self.html_page:\n        self.sizer.Replace(self.html_page, html_page)\n        self.html_page.Destroy()\n    else:\n        self.sizer.Add(html_page, 0, wx.LEFT | wx.TOP | wx.EXPAND | wx.RIGHT, 2)\n\n    self.html_page = html_page\n\n    self.sizer.Fit(self.real_panel)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage","title":"SchPage","text":"<pre><code>SchPage(parent, address_or_parser, parameters, pos=(0, 0), size=DefaultSize)\n</code></pre> <p>               Bases: <code>Window</code>, <code>Signal</code></p> <p>Container that manages up to four SchForm instances.</p> <p>Reads an HTML response from the server and distributes its sections (header, body, footer, panel) into corresponding sash-layout sub-windows.</p> <p>Construct a SchPage.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent SchNotebookPage.</p> required <code>address_or_parser</code> <p>HTTP address (str) or ShtmlParser instance.</p> required <code>parameters</code> <p>Dictionary of request parameters.</p> required <code>pos</code> <p>Initial position.</p> <code>(0, 0)</code> <code>size</code> <p>Initial size.</p> <code>DefaultSize</code> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def __init__(\n    self, parent, address_or_parser, parameters, pos=(0, 0), size=wx.DefaultSize\n):\n    \"\"\"Construct a SchPage.\n\n    Args:\n        parent: Parent SchNotebookPage.\n        address_or_parser: HTTP address (str) or ShtmlParser instance.\n        parameters: Dictionary of request parameters.\n        pos: Initial position.\n        size: Initial size.\n    \"\"\"\n    self._active = True\n    self._ctrl_dict_old = {}\n    self._ctrl_dict = {}\n    self._last_size = None\n    self._disable_setfocus = 0\n    self._signal_handlers = []\n\n    self.address_or_parser = address_or_parser\n    self.parameters = parameters\n    self.default_button = None\n    self.parent_page = None\n    self.last_control_with_focus = None\n    self.vertical_position = None\n\n    self.header = None\n    self.panel = None\n    self.body = None\n    self.footer = None\n    self.active_form = None\n    self.active_ctrl = None\n\n    Signal.__init__(self)\n    wx.Window.__init__(\n        self, parent, -1, pos, size, style=wx.WANTS_CHARS, name=\"SchPage\"\n    )\n\n    self.exists = True\n\n    mp = self._read_html(address_or_parser, parameters)\n    if not mp:\n        logger.error(\"ERROR READ HTML: %s %s\", address_or_parser, parameters)\n\n    self.address = mp.address\n\n    header = mp.get_header()\n    body = mp.get_body()\n    footer = mp.get_footer()\n    panel = mp.get_panel()\n    config = mp.var\n\n    vscroll = True\n    hscroll = True\n\n    if \"no_vscrollbar\" in config:\n        vscroll = False\n    if \"no_hscrollbar\" in config:\n        hscroll = False\n    if \"vertical_position\" in config:\n        self.set_vertical_position(config[\"vertical_position\"])\n\n    self.title = mp.title\n\n    if \"disable_parent\" in config:\n        self.disable_parent = config[\"disable_parent\"] != \"0\"\n    else:\n        self.disable_parent = True\n\n    winids = []\n\n    if header[0]:\n        topwin = wx.adv.SashLayoutWindow(\n            self,\n            -1,\n            wx.DefaultPosition,\n            (100, 5),\n            wx.adv.SW_3D,\n            style=wx.WANTS_CHARS,\n        )\n        topwin.SetOrientation(wx.adv.LAYOUT_HORIZONTAL)\n        topwin.SetAlignment(wx.adv.LAYOUT_TOP)\n        topwin.SetSashVisible(wx.adv.SASH_BOTTOM, True)\n        self.header = SchForm(topwin, page=self, form_type=\"header\")\n        self.header.show_form(header)\n        dy = self.header.calculate_best_size()[1]\n        topwin.SetDefaultSize((1000, dy))\n        self._top_window = topwin\n        winids.append(topwin.GetId())\n    else:\n        self._top_window = None\n\n    if footer[0]:\n        bottomwin = wx.adv.SashLayoutWindow(\n            self, -1, wx.DefaultPosition, (1000, 5), wx.adv.SW_3D\n        )\n        bottomwin.SetOrientation(wx.adv.LAYOUT_HORIZONTAL)\n        bottomwin.SetAlignment(wx.adv.LAYOUT_BOTTOM)\n        bottomwin.SetSashVisible(wx.adv.SASH_TOP, True)\n        self.footer = SchForm(bottomwin, page=self, form_type=\"footer\")\n        self.footer.show_form(footer)\n        bottomwin.SetDefaultSize((1000, self.footer.calculate_best_size()[1]))\n        self._bottom_window = bottomwin\n        winids.append(bottomwin.GetId())\n    else:\n        self._bottom_window = None\n\n    if panel[0]:\n        leftwin = wx.adv.SashLayoutWindow(\n            self, -1, wx.DefaultPosition, (5, 100), wx.adv.SW_3D\n        )\n        leftwin.SetOrientation(wx.adv.LAYOUT_VERTICAL)\n        leftwin.SetAlignment(wx.adv.LAYOUT_LEFT)\n        leftwin.SetSashVisible(wx.adv.SASH_RIGHT, True)\n        self.panel = SchForm(leftwin, page=self, form_type=\"panel\")\n        self.panel.show_form(panel)\n        xy = self.panel.calculate_best_size()\n        leftwin.SetDefaultSize(xy)\n        self._left_window = leftwin\n        winids.append(leftwin.GetId())\n    else:\n        self._left_window = None\n\n    self.body = SchForm(self, self, hscroll, vscroll)\n    self.body.set_htm_type(\"body\")\n    attrs = mp.get_body_attrs()\n    if \"width\" in attrs and \"height\" in attrs:\n        w = attrs[\"width\"]\n        h = attrs[\"height\"]\n        self.body.bestsize = (int(w) * 4 / 3, int(h) * 4 / 3)\n    self.body.show_form(body, parameters)\n    self.body.set_address_parm(self.address)\n    if \"refresh\" in mp.var:\n        self.body.refresh_time(int(mp.var[\"refresh\"]))\n\n    if winids:\n        self.Bind(\n            wx.adv.EVT_SASH_DRAGGED_RANGE,\n            self.on_sash_drag,\n            id=min(winids),\n            id2=max(winids),\n        )\n\n    self.Bind(wx.EVT_SIZE, self.on_size)\n    self.body.Bind(wx.EVT_CHILD_FOCUS, self.on_child_focus)\n    self.Bind(wx.EVT_SET_FOCUS, self.on_set_focus)\n\n    self.register_signal(self, \"child_closed_with_ok\")\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage-functions","title":"Methods:","text":""},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.CanClose","title":"CanClose","text":"<pre><code>CanClose()\n</code></pre> <p>Check whether all child forms can be closed.</p> <p>If all forms agree, their <code>_on_close</code> methods are called and signal handlers are disconnected.</p> <p>Returns:</p> Type Description <p>True if all forms can close.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def CanClose(self):\n    \"\"\"Check whether all child forms can be closed.\n\n    If all forms agree, their ``_on_close`` methods are called\n    and signal handlers are disconnected.\n\n    Returns:\n        True if all forms can close.\n    \"\"\"\n    for form in (self.body, self.header, self.footer, self.panel):\n        if form and not form.CanClose():\n            return False\n\n    if self.header:\n        self.header._on_close()\n    if self.body:\n        self.body._on_close()\n    if self.footer:\n        self.footer._on_close()\n    if self.panel:\n        self.panel._on_close()\n\n    for handler in self._signal_handlers:\n        dispatcher.disconnect(handler[0], handler[1], sender=dispatcher.Any)\n\n    return True\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.activate_page","title":"activate_page","text":"<pre><code>activate_page()\n</code></pre> <p>Activate this page and focus the body.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def activate_page(self):\n    \"\"\"Activate this page and focus the body.\"\"\"\n    self._active = True\n    self.body.SetFocus()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.append_ctrl","title":"append_ctrl","text":"<pre><code>append_ctrl(obj)\n</code></pre> <p>Register a widget with its unique name.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <p>The widget to register.</p> required Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def append_ctrl(self, obj):\n    \"\"\"Register a widget with its unique name.\n\n    Args:\n        obj: The widget to register.\n    \"\"\"\n    self._ctrl_dict[obj.get_unique_name()] = obj\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.calculate_best_size","title":"calculate_best_size","text":"<pre><code>calculate_best_size()\n</code></pre> <p>Compute the best size based on all child forms.</p> <p>Returns:</p> Type Description <p>Tuple (width, height).</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def calculate_best_size(self):\n    \"\"\"Compute the best size based on all child forms.\n\n    Returns:\n        Tuple (width, height).\n    \"\"\"\n    if self._last_size:\n        dx = self._last_size.GetWidth()\n        dy = self._last_size.GetHeight()\n        self._last_size = None\n        return (dx, dy)\n\n    dx = 0\n    dy = 0\n\n    if self.header:\n        x, y = self.header.calculate_best_size()\n        dy += y\n    if self.body:\n        x, y = self.body.calculate_best_size()\n        dx += x\n        dy += y\n    if self.footer:\n        x, y = self.footer.calculate_best_size()\n        dy += y\n    if self.panel:\n        x, y = self.panel.calculate_best_size()\n        dx += x\n        if y &gt; dy:\n            dy = y\n    return (dx, dy)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.change_notebook_page_title","title":"change_notebook_page_title","text":"<pre><code>change_notebook_page_title(new_title)\n</code></pre> <p>Update the tab caption that contains this SchPage.</p> <p>Parameters:</p> Name Type Description Default <code>new_title</code> <p>New tab text.</p> required Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def change_notebook_page_title(self, new_title):\n    \"\"\"Update the tab caption that contains this SchPage.\n\n    Args:\n        new_title: New tab text.\n    \"\"\"\n    p = self.GetParent()\n    tab = p.GetParent()\n    sel = tab.GetPageIndex(p)\n    tab.SetPageText(sel, new_title)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.child_closed_with_ok","title":"child_closed_with_ok","text":"<pre><code>child_closed_with_ok(win=None)\n</code></pre> <p>Refresh HTML when a child window closes with OK.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def child_closed_with_ok(self, win=None):\n    \"\"\"Refresh HTML when a child window closes with OK.\"\"\"\n    wx.CallAfter(self.refresh_html)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Initiate cancellation of this page.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def close(self):\n    \"\"\"Initiate cancellation of this page.\"\"\"\n    self.GetParent().on_child_form_cancel()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.deactivate_page","title":"deactivate_page","text":"<pre><code>deactivate_page()\n</code></pre> <p>Deactivate this page.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def deactivate_page(self):\n    \"\"\"Deactivate this page.\"\"\"\n    self._active = False\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.enable_forms","title":"enable_forms","text":"<pre><code>enable_forms(enable)\n</code></pre> <p>Enable or disable all managed forms.</p> <p>Parameters:</p> Name Type Description Default <code>enable</code> <p>If True, enable; if False, disable.</p> required Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def enable_forms(self, enable):\n    \"\"\"Enable or disable all managed forms.\n\n    Args:\n        enable: If True, enable; if False, disable.\n    \"\"\"\n    if not enable:\n        self._last_size = self.GetSize()\n\n    if enable or self.disable_parent:\n        for form in (self.header, self.body, self.footer, self.panel):\n            if form is not None:\n                form.enable(enable)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.get_item","title":"get_item","text":"<pre><code>get_item(ctrl_name)\n</code></pre> <p>Look up a control by name across all child forms.</p> <p>Parameters:</p> Name Type Description Default <code>ctrl_name</code> <p>Name of the control.</p> required <p>Returns:</p> Type Description <p>The control widget, or None.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def get_item(self, ctrl_name):\n    \"\"\"Look up a control by name across all child forms.\n\n    Args:\n        ctrl_name: Name of the control.\n\n    Returns:\n        The control widget, or None.\n    \"\"\"\n    if ctrl_name in self._ctrl_dict:\n        return self._ctrl_dict[ctrl_name]\n    for form in (self.body, self.header, self.panel, self.footer):\n        if form is not None and hasattr(form, ctrl_name):\n            return getattr(form, ctrl_name)\n    return None\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.get_last_control_with_focus","title":"get_last_control_with_focus","text":"<pre><code>get_last_control_with_focus()\n</code></pre> <p>Return the last control that had focus.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def get_last_control_with_focus(self):\n    \"\"\"Return the last control that had focus.\"\"\"\n    return self.last_control_with_focus\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.get_parent_page","title":"get_parent_page","text":"<pre><code>get_parent_page()\n</code></pre> <p>Return the parent SchPage, if any.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def get_parent_page(self):\n    \"\"\"Return the parent SchPage, if any.\"\"\"\n    return self.parent_page\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.get_parm","title":"get_parm","text":"<pre><code>get_parm(param)\n</code></pre> <p>Get the value of a named control.</p> <p>Parameters:</p> Name Type Description Default <code>param</code> <p>Control name.</p> required <p>Returns:</p> Type Description <p>The control's value, or None if not found.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def get_parm(self, param):\n    \"\"\"Get the value of a named control.\n\n    Args:\n        param: Control name.\n\n    Returns:\n        The control's value, or None if not found.\n    \"\"\"\n    ctrl = self._ctrl_dict.get(param) or self.get_item(param)\n    if ctrl:\n        return ctrl.GetValue()\n    return None\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.get_title","title":"get_title","text":"<pre><code>get_title()\n</code></pre> <p>Return the page title.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def get_title(self):\n    \"\"\"Return the page title.\"\"\"\n    return self.title\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.get_widgets","title":"get_widgets","text":"<pre><code>get_widgets()\n</code></pre> <p>Return the current widget dictionary.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def get_widgets(self):\n    \"\"\"Return the current widget dictionary.\"\"\"\n    return self._ctrl_dict\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.has_parm","title":"has_parm","text":"<pre><code>has_parm(param)\n</code></pre> <p>Check if a named control exists.</p> <p>Parameters:</p> Name Type Description Default <code>param</code> <p>Control name.</p> required <p>Returns:</p> Type Description <p>True if the control is found.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def has_parm(self, param):\n    \"\"\"Check if a named control exists.\n\n    Args:\n        param: Control name.\n\n    Returns:\n        True if the control is found.\n    \"\"\"\n    return param in self._ctrl_dict or self.item_exist(param)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.init_frame","title":"init_frame","text":"<pre><code>init_frame(callback=None)\n</code></pre> <p>Initialize header, footer, panel, and body forms.</p> <p>Parameters:</p> Name Type Description Default <code>callback</code> <p>Optional callable invoked after body init.</p> <code>None</code> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def init_frame(self, callback=None):\n    \"\"\"Initialize header, footer, panel, and body forms.\n\n    Args:\n        callback: Optional callable invoked after body init.\n    \"\"\"\n    if self.header:\n        self.header.init()\n    if self.footer:\n        self.footer.init()\n    if self.panel:\n        self.panel.init()\n\n    self.body.init(callback)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.is_active","title":"is_active","text":"<pre><code>is_active()\n</code></pre> <p>Return whether this page is active.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def is_active(self):\n    \"\"\"Return whether this page is active.\"\"\"\n    return self._active\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.item_exist","title":"item_exist","text":"<pre><code>item_exist(ctrl_name)\n</code></pre> <p>Check whether a named control exists in any child form.</p> <p>Parameters:</p> Name Type Description Default <code>ctrl_name</code> <p>Name of the control.</p> required <p>Returns:</p> Type Description <p>True if found.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def item_exist(self, ctrl_name):\n    \"\"\"Check whether a named control exists in any child form.\n\n    Args:\n        ctrl_name: Name of the control.\n\n    Returns:\n        True if found.\n    \"\"\"\n    if ctrl_name in self._ctrl_dict:\n        return True\n    for form in (self.body, self.header, self.panel, self.footer):\n        if form is not None and hasattr(form, ctrl_name):\n            return True\n    return False\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.on_char_hook","title":"on_char_hook","text":"<pre><code>on_char_hook(event)\n</code></pre> <p>Handle character input: Escape cancels the child form.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def on_char_hook(self, event):\n    \"\"\"Handle character input: Escape cancels the child form.\"\"\"\n    if event.KeyCode == wx.WXK_ESCAPE:\n        parent = self.GetParent()\n        if hasattr(parent, \"on_child_form_cancel\"):\n            parent.on_child_form_cancel()\n        else:\n            event.Skip()\n    else:\n        event.Skip()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.on_child_focus","title":"on_child_focus","text":"<pre><code>on_child_focus(evt)\n</code></pre> <p>Track which widget last received focus.</p> <p>Ensures the owning HtmlPanel tab is selected and calls KillFocus on the previously focused widget.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def on_child_focus(self, evt):\n    \"\"\"Track which widget last received focus.\n\n    Ensures the owning HtmlPanel tab is selected and calls\n    KillFocus on the previously focused widget.\n    \"\"\"\n    if self._disable_setfocus:\n        evt.Skip()\n        return\n\n    new_win = evt.GetWindow()\n    parent = new_win.GetParent()\n    while parent is not None:\n        if parent.__class__.__name__ == \"HtmlPanel\":\n            parent.SelectTab()\n            break\n        parent = parent.GetParent()\n\n    while new_win is not None:\n        if (\n            new_win.GetParent()\n            and new_win.GetParent().GetWindowStyleFlag() &amp; wx.TAB_TRAVERSAL != 0\n        ):\n            break\n        new_win = new_win.GetParent()\n\n    if new_win is not self.last_control_with_focus:\n        if self.last_control_with_focus and hasattr(\n            self.last_control_with_focus, \"KillFocus\"\n        ):\n            self._disable_setfocus = True\n            self.last_control_with_focus.KillFocus()\n            self._disable_setfocus = False\n        self.last_control_with_focus = new_win\n\n    evt.Skip()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.on_key_down","title":"on_key_down","text":"<pre><code>on_key_down(event)\n</code></pre> <p>Handle key down: Enter triggers the default button.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def on_key_down(self, event):\n    \"\"\"Handle key down: Enter triggers the default button.\"\"\"\n    if event.KeyCode == wx.WXK_RETURN and self.default_button:\n        self.default_button.OnClick(event)\n    else:\n        event.Skip()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.on_sash_drag","title":"on_sash_drag","text":"<pre><code>on_sash_drag(event)\n</code></pre> <p>Handle sash drag events for header, footer, and panel.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def on_sash_drag(self, event):\n    \"\"\"Handle sash drag events for header, footer, and panel.\"\"\"\n    if event.GetDragStatus() == wx.SASH_STATUS_OUT_OF_RANGE:\n        return\n    eobj = event.GetEventObject()\n    if eobj is self._top_window:\n        self._top_window.SetDefaultSize((1000, event.GetDragRect().height))\n    elif eobj is self._left_window:\n        self._left_window.SetDefaultSize((event.GetDragRect().width, 1000))\n    elif eobj is self._bottom_window:\n        self._bottom_window.SetDefaultSize((1000, event.GetDragRect().height))\n    LayoutAlgorithm().LayoutWindow(self, self.body)\n    self.body.Refresh()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.on_set_focus","title":"on_set_focus","text":"<pre><code>on_set_focus(evt)\n</code></pre> <p>Delegate focus to the body form.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def on_set_focus(self, evt):\n    \"\"\"Delegate focus to the body form.\"\"\"\n    if self.body:\n        self.body.SetFocus()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.on_size","title":"on_size","text":"<pre><code>on_size(event)\n</code></pre> <p>Handle size changes, re-laying out the sash windows.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def on_size(self, event):\n    \"\"\"Handle size changes, re-laying out the sash windows.\"\"\"\n    LayoutAlgorithm().LayoutWindow(self, self.body)\n    self.body.Refresh()\n    if event:\n        event.Skip()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.pop_ctrl","title":"pop_ctrl","text":"<pre><code>pop_ctrl(name)\n</code></pre> <p>Retrieve and remove a widget from the previous lifecycle.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <p>Unique widget name.</p> required <p>Returns:</p> Type Description <p>The widget or None if not found.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def pop_ctrl(self, name):\n    \"\"\"Retrieve and remove a widget from the previous lifecycle.\n\n    Args:\n        name: Unique widget name.\n\n    Returns:\n        The widget or None if not found.\n    \"\"\"\n    return self._ctrl_dict_old.pop(name, None)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.refresh_html","title":"refresh_html","text":"<pre><code>refresh_html(method=0)\n</code></pre> <p>Reload page content from the server.</p> <p>Parameters:</p> Name Type Description Default <code>method</code> <p>Refresh method (reserved for future use).</p> <code>0</code> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def refresh_html(self, method=0):\n    \"\"\"Reload page content from the server.\n\n    Args:\n        method: Refresh method (reserved for future use).\n    \"\"\"\n    return self._refresh_html(method)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.reg_application_signal_handler","title":"reg_application_signal_handler","text":"<pre><code>reg_application_signal_handler(fun, signal)\n</code></pre> <p>Register a callback for a PyDispatcher signal.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <p>Callback function.</p> required <code>signal</code> <p>Signal name.</p> required Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def reg_application_signal_handler(self, fun, signal):\n    \"\"\"Register a callback for a PyDispatcher signal.\n\n    Args:\n        fun: Callback function.\n        signal: Signal name.\n    \"\"\"\n    for existing in self._signal_handlers:\n        if existing[1] == signal:\n            break\n    dispatcher.connect(fun, signal, sender=dispatcher.Any)\n    self._signal_handlers.append((fun, signal))\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.remove_old_ctrls","title":"remove_old_ctrls","text":"<pre><code>remove_old_ctrls()\n</code></pre> <p>Destroy all widgets from the previous lifecycle.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def remove_old_ctrls(self):\n    \"\"\"Destroy all widgets from the previous lifecycle.\"\"\"\n    for name, win in list(self._ctrl_dict_old.items()):\n        win.Destroy()\n    self._ctrl_dict_old = {}\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.restart_ctrl_lp","title":"restart_ctrl_lp","text":"<pre><code>restart_ctrl_lp()\n</code></pre> <p>Begin a new control lifecycle, preserving the old map.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def restart_ctrl_lp(self):\n    \"\"\"Begin a new control lifecycle, preserving the old map.\"\"\"\n    self._ctrl_dict_old = self._ctrl_dict\n    self._ctrl_dict = {}\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.set_adr_and_param","title":"set_adr_and_param","text":"<pre><code>set_adr_and_param(address_or_parser, param)\n</code></pre> <p>Change the source address and request parameters.</p> <p>Parameters:</p> Name Type Description Default <code>address_or_parser</code> <p>New address or parser.</p> required <code>param</code> <p>New parameters dict.</p> required Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def set_adr_and_param(self, address_or_parser, param):\n    \"\"\"Change the source address and request parameters.\n\n    Args:\n        address_or_parser: New address or parser.\n        param: New parameters dict.\n    \"\"\"\n    self.address_or_parser = address_or_parser\n    self.parameters = param\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.set_default_button","title":"set_default_button","text":"<pre><code>set_default_button(button)\n</code></pre> <p>Set the default button activated by Enter.</p> <p>Parameters:</p> Name Type Description Default <code>button</code> <p>The wx.Button-like control.</p> required Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def set_default_button(self, button):\n    \"\"\"Set the default button activated by Enter.\n\n    Args:\n        button: The wx.Button-like control.\n    \"\"\"\n    self.default_button = button\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.set_focus","title":"set_focus","text":"<pre><code>set_focus()\n</code></pre> <p>Restore focus to the last focused control or this window.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def set_focus(self):\n    \"\"\"Restore focus to the last focused control or this window.\"\"\"\n    if self.last_control_with_focus:\n        self.last_control_with_focus.SetFocus()\n    else:\n        super().SetFocus()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.set_new_href","title":"set_new_href","text":"<pre><code>set_new_href(href)\n</code></pre> <p>Change the source address.</p> <p>Parameters:</p> Name Type Description Default <code>href</code> <p>New address string.</p> required Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def set_new_href(self, href):\n    \"\"\"Change the source address.\n\n    Args:\n        href: New address string.\n    \"\"\"\n    self.address_or_parser = href\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.set_page","title":"set_page","text":"<pre><code>set_page(page_source)\n</code></pre> <p>Replace the body form's HTML content.</p> <p>Parameters:</p> Name Type Description Default <code>page_source</code> <p>New HTML string.</p> required Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def set_page(self, page_source):\n    \"\"\"Replace the body form's HTML content.\n\n    Args:\n        page_source: New HTML string.\n    \"\"\"\n    self.body.set_page(page_source)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.set_vertical_position","title":"set_vertical_position","text":"<pre><code>set_vertical_position(position)\n</code></pre> <p>Set the placement preference for child forms.</p> <p>Parameters:</p> Name Type Description Default <code>position</code> <p>'top', 'bottom', None, or 'default'.</p> required Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def set_vertical_position(self, position):\n    \"\"\"Set the placement preference for child forms.\n\n    Args:\n        position: 'top', 'bottom', None, or 'default'.\n    \"\"\"\n    self.vertical_position = position\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.test_ctrl","title":"test_ctrl","text":"<pre><code>test_ctrl(name)\n</code></pre> <p>Check if a widget exists in the current lifecycle.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <p>Unique widget name.</p> required <p>Returns:</p> Type Description <p>True if the widget exists.</p> Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def test_ctrl(self, name):\n    \"\"\"Check if a widget exists in the current lifecycle.\n\n    Args:\n        name: Unique widget name.\n\n    Returns:\n        True if the widget exists.\n    \"\"\"\n    return name in self._ctrl_dict\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.SchPage.unreg_application_signal_handler","title":"unreg_application_signal_handler","text":"<pre><code>unreg_application_signal_handler(signal)\n</code></pre> <p>Unregister a previously registered signal handler.</p> <p>Parameters:</p> Name Type Description Default <code>signal</code> <p>Signal name.</p> required Source code in <code>pytigon_gui/guiframe/page.py</code> <pre><code>def unreg_application_signal_handler(self, signal):\n    \"\"\"Unregister a previously registered signal handler.\n\n    Args:\n        signal: Signal name.\n    \"\"\"\n    i = 0\n    found = None\n    for pos in self._signal_handlers:\n        if pos[1] == signal:\n            found = pos\n            break\n        i += 1\n    if found:\n        dispatcher.disconnect(found[0], found[1], sender=dispatcher.Any)\n        del self._signal_handlers[i]\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.StandardButtons","title":"StandardButtons","text":"<pre><code>StandardButtons(toolbar_interface, gui_style)\n</code></pre> <p>Creates and manages standard toolbar buttons and their event handlers.</p> <p>Reads the gui_style string to determine which panels (File, Clipboard, Operations, Browser, Address) should be created and configures event bindings for each.</p> <p>Initialize standard buttons.</p> <p>Parameters:</p> Name Type Description Default <code>toolbar_interface</code> <p>The toolbar bar instance (ToolbarBar subclass).</p> required <code>gui_style</code> <p>String describing which features to enable.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def __init__(self, toolbar_interface, gui_style):\n    \"\"\"Initialize standard buttons.\n\n    Args:\n        toolbar_interface: The toolbar bar instance (ToolbarBar subclass).\n        gui_style: String describing which features to enable.\n    \"\"\"\n    self.toolbar_interface = toolbar_interface\n    self.gui_style = gui_style\n    self.ti = self.toolbar_interface\n    self.tbs = self.ti.toolbars\n    self._object_list = {}\n    self.webobject = None\n    self.file = \"file\" in self.gui_style\n    self.clipboard = \"clipboard\" in self.gui_style\n    self.operations = \"operations\" in self.gui_style\n    self.browse = \"browse\" in self.gui_style\n    self.nav = \"nav\" in self.gui_style\n    self.address_bar = \"address_bar\" in self.gui_style\n    self.bg = colour_to_html(wx.SystemSettings.GetColour(wx.SYS_COLOUR_3DFACE))\n    self.bg_info = colour_to_html(wx.SystemSettings.GetColour(wx.SYS_COLOUR_INFOBK))\n    self.bg_h = colour_to_html(wx.SystemSettings.GetColour(wx.SYS_COLOUR_HIGHLIGHT))\n    self.progress = 0\n    self.info = \"\"\n    self.address_txt = \"\"\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.StandardButtons-functions","title":"Methods:","text":""},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.StandardButtons.address_refr","title":"address_refr","text":"<pre><code>address_refr(panel)\n</code></pre> <p>Refresh the address bar based on the active panel's web status.</p> <p>Parameters:</p> Name Type Description Default <code>panel</code> <p>The active panel, or None.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def address_refr(self, panel):\n    \"\"\"Refresh the address bar based on the active panel's web status.\n\n    Args:\n        panel: The active panel, or None.\n    \"\"\"\n    if self.address_bar:\n        if panel is not None and hasattr(panel.body, \"WEB\"):\n            ctrl = panel.body.WEB\n            if hasattr(ctrl, \"GetStatus\"):\n                status = ctrl.GetStatus()\n                if status[\"progress\"] &gt;= 0:\n                    self._info_proc(\n                        status[\"txt\"].replace(\"INFO:\", \"\").replace(\"ERROR:\", \"\"),\n                        status[\"progress\"],\n                    )\n                else:\n                    if status[\"txt\"] and status[\"txt\"] != \"\":\n                        if \"ERROR:\" in status[\"txt\"]:\n                            self._info_alarm(status[\"txt\"].replace(\"ERROR:\", \"\"))\n                        else:\n                            self._info_txt(status[\"txt\"].replace(\"INFO:\", \"\"))\n                    else:\n                        self._info_clear()\n                if self.address_txt != status[\"address\"]:\n                    if status[\"address\"]:\n                        self.address.SetValue(status[\"address\"])\n                        self.address_txt = status[\"address\"]\n                    else:\n                        self.address.SetValue(\"\")\n                        self.address_txt = \"\"\n        else:\n            if self.address_txt != \"\":\n                self.address.SetValue(\"\")\n                self.address_txt = \"\"\n            self._info_clear()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.StandardButtons.clipboard_refr","title":"clipboard_refr","text":"<pre><code>clipboard_refr(editctrl)\n</code></pre> <p>Refresh clipboard buttons based on the focused control's capabilities.</p> <p>Parameters:</p> Name Type Description Default <code>editctrl</code> <p>The currently focused edit control, or None.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def clipboard_refr(self, editctrl):\n    \"\"\"Refresh clipboard buttons based on the focused control's capabilities.\n\n    Args:\n        editctrl: The currently focused edit control, or None.\n    \"\"\"\n    if self.clipboard:\n        tab_status = []\n        if editctrl:\n            tab_status.append(\n                (\n                    hasattr(editctrl, \"CanCopy\") and editctrl.CanCopy(),\n                    \"clipboard\",\n                    \"copy\",\n                )\n            )\n            tab_status.append(\n                (\n                    hasattr(editctrl, \"CanCut\") and editctrl.CanCut(),\n                    \"clipboard\",\n                    \"cut\",\n                )\n            )\n            tab_status.append(\n                (\n                    hasattr(editctrl, \"CanPaste\") and editctrl.CanPaste(),\n                    \"clipboard\",\n                    \"paste\",\n                )\n            )\n        else:\n            tab_status.append((False, \"clipboard\", \"copy\"))\n            tab_status.append((False, \"clipboard\", \"cut\"))\n            tab_status.append((False, \"clipboard\", \"paste\"))\n        return self._status(tab_status)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.StandardButtons.create_address_panel","title":"create_address_panel","text":"<pre><code>create_address_panel(toolbar_page)\n</code></pre> <p>Create the Address bar panel with auto-complete and web menu.</p> <p>Parameters:</p> Name Type Description Default <code>toolbar_page</code> <p>The toolbar page (unused).</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def create_address_panel(self, toolbar_page):\n    \"\"\"Create the Address bar panel with auto-complete and web menu.\n\n    Args:\n        toolbar_page: The toolbar page (unused).\n    \"\"\"\n    if self.address_bar:\n        bar = self.ti.create_panel_in_main_page(_(\"Address\"), TYPE_PANELBAR)\n        if hasattr(bar, \"panel\"):\n            width = 350\n            panel = bar.panel\n            self.address = SchAutoComplete(panel, size=wx.Size(width, -1))\n            self.address.set_callback_fun(self.open_page)\n            self.tbs[\"browser\"][\"address\"] = self.address\n            self.webmenu = FM.FlatMenuBar(panel, spacer=5)\n            self.webmenu.GetRendererManager().SetTheme(FM.Style2007)\n            self.tbs[\"browser\"][\"webmenu\"] = self.webmenu\n            webmenu_source = self.web_menu()\n            self.web_info = wx.html.HtmlWindow(\n                panel, style=wx.html.HW_SCROLLBAR_NEVER, size=(width, 25)\n            )\n            self.web_info.SetBorders(0)\n            self._info_clear()\n            self.make_menu(self.webmenu, webmenu_source)\n            controls = (self.webmenu, self.web_info, self.address)\n            bar.AddPanel(controls)\n        else:\n            self.tbs[\"browser\"][\"address\"] = EmptyControl()\n            self.web_info = None\n    else:\n        self.tbs[\"browser\"][\"address\"] = EmptyControl()\n        self.web_info = None\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.StandardButtons.create_browse_panel","title":"create_browse_panel","text":"<pre><code>create_browse_panel(toolbar_page)\n</code></pre> <p>Create the Browser panel with navigation buttons.</p> <p>Parameters:</p> Name Type Description Default <code>toolbar_page</code> <p>The toolbar page (unused).</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def create_browse_panel(self, toolbar_page):\n    \"\"\"Create the Browser panel with navigation buttons.\n\n    Args:\n        toolbar_page: The toolbar page (unused).\n    \"\"\"\n    if self.browse or self.nav:\n        self.tbs[\"browser\"] = {}\n        bar = self.ti.create_panel_in_main_page(_(\"Browser\"), TYPE_BUTTONBAR)\n        bar.Refr = self.web_refr\n        self.webobject = None\n        self.tbs[\"browser\"][\"bar\"] = bar\n        self.tbs[\"browser\"][\"back\"] = bar.add_simple_tool(\n            ID_WEB_BACK,\n            _(\"Back\"),\n            bitmaps_from_art_id(wx.ART_GO_BACK, wx.Size(32, 32)),\n        )\n        self.tbs[\"browser\"][\"forward\"] = bar.add_simple_tool(\n            ID_WEB_FORWARD,\n            _(\"Forward\"),\n            bitmaps_from_art_id(wx.ART_GO_FORWARD, wx.Size(32, 32)),\n        )\n        self.make_handlers(ID_WEB_BACK, \"WebBack\", \"CanWebBack\", propagate=True)\n        self.make_handlers(\n            ID_WEB_FORWARD, \"WebForward\", \"CanWebForward\", propagate=True\n        )\n\n        if self.browse:\n            self.tbs[\"browser\"][\"stop\"] = bar.add_simple_tool(\n                ID_WEB_STOP,\n                _(\"Stop\"),\n                bitmaps_from_art_id(wx.ART_CROSS_MARK, wx.Size(32, 32)),\n            )\n            self.tbs[\"browser\"][\"refresh\"] = bar.add_simple_tool(\n                ID_WEB_REFRESH,\n                _(\"Refresh\"),\n                bitmaps_from_art_id(wx.ART_GO_TO_PARENT, wx.Size(32, 32)),\n            )\n            self.tbs[\"browser\"][\"addbookmark\"] = bar.add_simple_tool(\n                ID_WEB_ADDBOOKMARK,\n                _(\"Add bookmark\"),\n                bitmaps_from_art_id(wx.ART_ADD_BOOKMARK, wx.Size(32, 32)),\n            )\n            self.tbs[\"browser\"][\"newpage\"] = bar.add_simple_tool(\n                ID_WEB_NEW_WINDOW,\n                _(\"New page\"),\n                bitmaps_from_art_id(wx.ART_NEW, wx.Size(32, 32)),\n            )\n\n            self.make_handlers(ID_WEB_STOP, \"WebStop\", \"CanWebStop\", propagate=True)\n            self.make_handlers(\n                ID_WEB_REFRESH, \"WebRefresh\", \"CanWebRefresh\", propagate=True\n            )\n            self.make_handlers(\n                ID_WEB_ADDBOOKMARK,\n                \"WebAddBookmark\",\n                \"CanWebAddBookmark\",\n                propagate=True,\n            )\n            self.make_handlers(ID_WEB_NEW_WINDOW, \"WebNewWindow\", propagate=False)\n    else:\n        self.tbs[\"browser\"] = {}\n        self.tbs[\"browser\"][\"bar\"] = EmptyBar()\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.StandardButtons.create_edit_panel","title":"create_edit_panel","text":"<pre><code>create_edit_panel(toolbar_page)\n</code></pre> <p>Create the Clipboard panel with Copy, Cut, and Paste buttons.</p> <p>Parameters:</p> Name Type Description Default <code>toolbar_page</code> <p>The toolbar page (unused).</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def create_edit_panel(self, toolbar_page):\n    \"\"\"Create the Clipboard panel with Copy, Cut, and Paste buttons.\n\n    Args:\n        toolbar_page: The toolbar page (unused).\n    \"\"\"\n    if self.clipboard:\n        self.tbs[\"clipboard\"] = {}\n        bar = self.ti.create_panel_in_main_page(_(\"Clipboard\"), TYPE_BUTTONBAR)\n        bar.Refr = self.clipboard_refr\n        self.tbs[\"clipboard\"][\"bar\"] = bar\n        self.tbs[\"clipboard\"][\"copy\"] = bar.add_simple_tool(\n            wx.ID_COPY, _(\"Copy\"), bitmaps_from_art_id(wx.ART_COPY, wx.Size(32, 32))\n        )\n        self.tbs[\"clipboard\"][\"cut\"] = bar.add_simple_tool(\n            wx.ID_CUT, _(\"Cut\"), bitmaps_from_art_id(wx.ART_CUT, wx.Size(32, 32))\n        )\n        self.tbs[\"clipboard\"][\"paste\"] = bar.add_simple_tool(\n            wx.ID_PASTE,\n            _(\"Paste\"),\n            bitmaps_from_art_id(wx.ART_PASTE, wx.Size(32, 32)),\n        )\n\n        self.make_handlers(wx.ID_COPY, \"Copy\", \"CanCopy\")\n        self.make_handlers(wx.ID_CUT, \"Cut\", \"CanCut\")\n        self.make_handlers(wx.ID_PASTE, \"Paste\", \"CanPaste\")\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.StandardButtons.create_file_panel","title":"create_file_panel","text":"<pre><code>create_file_panel(toolbar_page)\n</code></pre> <p>Create the File panel with Open, Save, Print, and Exit buttons.</p> <p>Parameters:</p> Name Type Description Default <code>toolbar_page</code> <p>The toolbar page to add panels to (unused; buttons are added to the main page).</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def create_file_panel(self, toolbar_page):\n    \"\"\"Create the File panel with Open, Save, Print, and Exit buttons.\n\n    Args:\n        toolbar_page: The toolbar page to add panels to (unused; buttons\n            are added to the main page).\n    \"\"\"\n    if self.file:\n        bar = self.ti.create_panel_in_main_page(_(\"File\"), TYPE_TOOLBAR)\n        self.tbs[\"file\"] = {}\n        self.tbs[\"file\"][\"bar\"] = bar\n        if \"exit\" in self.gui_style:\n            self.tbs[\"file\"][\"exit\"] = bar.add_simple_tool(\n                wx.ID_EXIT,\n                _(\"Exit\"),\n                bitmaps_from_art_id(wx.ART_QUIT, wx.Size(32, 32)),\n            )\n        test = False\n        if \"open\" in self.gui_style:\n            bar.add_separator()\n            self.tbs[\"file\"][\"open\"] = bar.add_simple_tool(\n                wx.ID_OPEN,\n                _(\"Open\"),\n                bitmaps_from_art_id(wx.ART_FILE_OPEN, wx.Size(32, 32)),\n            )\n            test = True\n        if \"save\" in self.gui_style:\n            if not test:\n                bar.add_separator()\n            self.tbs[\"file\"][\"save\"] = bar.add_simple_tool(\n                wx.ID_SAVE,\n                _(\"Save\"),\n                bitmaps_from_art_id(wx.ART_FILE_SAVE, wx.Size(32, 32)),\n            )\n            self.make_handlers(wx.ID_SAVE, \"Save\", \"CanSave\", propagate=True)\n            if \"save_as\" in self.gui_style:\n                self.tbs[\"file\"][\"save_as\"] = bar.add_simple_tool(\n                    wx.ID_SAVEAS,\n                    _(\"Save as\"),\n                    bitmaps_from_art_id(wx.ART_FILE_SAVE_AS, wx.Size(32, 32)),\n                )\n                self.make_handlers(\n                    wx.ID_SAVEAS, \"SaveAs\", \"CanSaveAs\", propagate=True\n                )\n            test = True\n        if \"print\" in self.gui_style:\n            if test:\n                bar.add_separator()\n                self.tbs[\"file\"][\"print\"] = bar.add_hybrid_tool(\n                    ID_PRINT,\n                    _(\"Print\"),\n                    bitmaps_from_art_id(wx.ART_PRINT, wx.Size(32, 32)),\n                )\n                bar.add_separator()\n            else:\n                self.tbs[\"file\"][\"print\"] = bar.add_hybrid_tool(\n                    ID_PRINT,\n                    _(\"Print\"),\n                    bitmaps_from_art_id(wx.ART_PRINT, wx.Size(32, 32)),\n                )\n            self.make_handlers(ID_PRINT, \"Print\", \"CanPrint\", propagate=True)\n            self.ti.bind_dropdown(self.on_print, ID_PRINT)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.StandardButtons.create_operations_panel","title":"create_operations_panel","text":"<pre><code>create_operations_panel(toolbar_page)\n</code></pre> <p>Create the Operations panel with Undo, Redo, Find, etc.</p> <p>Parameters:</p> Name Type Description Default <code>toolbar_page</code> <p>The toolbar page (unused).</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def create_operations_panel(self, toolbar_page):\n    \"\"\"Create the Operations panel with Undo, Redo, Find, etc.\n\n    Args:\n        toolbar_page: The toolbar page (unused).\n    \"\"\"\n    if self.operations:\n        self.tbs[\"operations\"] = {}\n        bar = self.ti.create_panel_in_main_page(_(\"Operations\"), TYPE_TOOLBAR)\n        self.tbs[\"operations\"][\"bar\"] = bar\n        self.tbs[\"operations\"][\"undo\"] = bar.add_simple_tool(\n            wx.ID_UNDO, _(\"Undo\"), bitmaps_from_art_id(wx.ART_UNDO, wx.Size(32, 32))\n        )\n        self.tbs[\"operations\"][\"redo\"] = bar.add_simple_tool(\n            wx.ID_REDO, _(\"Redo\"), bitmaps_from_art_id(wx.ART_REDO, wx.Size(32, 32))\n        )\n        self.tbs[\"operations\"][\"find\"] = bar.add_hybrid_tool(\n            ID_FIND, _(\"Find\"), bitmaps_from_art_id(wx.ART_FIND, wx.Size(32, 32))\n        )\n        bar.add_separator()\n        self.tbs[\"operations\"][\"filemanager\"] = bar.add_simple_tool(\n            wx.ID_ANY,\n            _(\"File manager\"),\n            bitmaps_from_art_id(wx.ART_FOLDER_OPEN, wx.Size(32, 32)),\n        )\n        self.tbs[\"operations\"][\"editor\"] = bar.add_simple_tool(\n            wx.ID_ANY,\n            _(\"Editor\"),\n            bitmaps_from_art_id(wx.ART_NORMAL_FILE, wx.Size(32, 32)),\n        )\n        self.ti.bind_dropdown(self.on_find, ID_FIND)\n\n        self.make_handlers(wx.ID_UNDO, \"Undo\", \"CanUndo\")\n        self.make_handlers(wx.ID_REDO, \"Redo\", \"CanRedo\")\n        self.make_handlers(ID_FIND, \"Find\", \"CanFind\")\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.StandardButtons.load_page","title":"load_page","text":"<pre><code>load_page(address)\n</code></pre> <p>Load a URL in the browser panel if one is active.</p> <p>Parameters:</p> Name Type Description Default <code>address</code> <p>URL to load.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def load_page(self, address):\n    \"\"\"Load a URL in the browser panel if one is active.\n\n    Args:\n        address: URL to load.\n    \"\"\"\n    if \"browser\" in self._object_list and self._object_list[\"browser\"]:\n        self._object_list[\"browser\"].Go(address)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.StandardButtons.make_handler","title":"make_handler","text":"<pre><code>make_handler(fun_name, event_id, propagate=False)\n</code></pre> <p>Create a command handler that delegates to the focused control.</p> <p>Walks up the focus chain calling the named method on the first control that has it.</p> <p>Parameters:</p> Name Type Description Default <code>fun_name</code> <p>Name of the method to call (e.g. 'Copy').</p> required <code>event_id</code> <p>wx event ID to bind to.</p> required <code>propagate</code> <p>If True, walk up the parent chain.</p> <code>False</code> Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def make_handler(self, fun_name, event_id, propagate=False):\n    \"\"\"Create a command handler that delegates to the focused control.\n\n    Walks up the focus chain calling the named method on the first\n    control that has it.\n\n    Args:\n        fun_name: Name of the method to call (e.g. 'Copy').\n        event_id: wx event ID to bind to.\n        propagate: If True, walk up the parent chain.\n    \"\"\"\n\n    def on_command(event):\n        win = find_focus_in_form()\n        while win:\n            if hasattr(win, fun_name):\n                getattr(win, fun_name)()\n            if propagate:\n                win = win.GetParent()\n            else:\n                break\n\n    self.toolbar_interface.bind(on_command, id=event_id)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.StandardButtons.make_handlers","title":"make_handlers","text":"<pre><code>make_handlers(event_id, fun_name, fun_name2=None, propagate=False)\n</code></pre> <p>Create both command and UI update handlers for a button.</p> <p>Parameters:</p> Name Type Description Default <code>event_id</code> <p>wx event ID to bind to.</p> required <code>fun_name</code> <p>Name of the command method (e.g. 'Copy').</p> required <code>fun_name2</code> <p>Name of the UI update method (e.g. 'CanCopy'). If None, the button is always enabled.</p> <code>None</code> <code>propagate</code> <p>If True, walk up the parent chain.</p> <code>False</code> Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def make_handlers(self, event_id, fun_name, fun_name2=None, propagate=False):\n    \"\"\"Create both command and UI update handlers for a button.\n\n    Args:\n        event_id: wx event ID to bind to.\n        fun_name: Name of the command method (e.g. 'Copy').\n        fun_name2: Name of the UI update method (e.g. 'CanCopy').\n            If None, the button is always enabled.\n        propagate: If True, walk up the parent chain.\n    \"\"\"\n    self.make_handler(fun_name, event_id, propagate)\n    if fun_name2:\n        self.make_ui_handler(fun_name2, event_id, propagate)\n    else:\n\n        def on_update_ui(event):\n            event.Enable(True)\n\n        self.toolbar_interface.parent.Bind(\n            wx.EVT_UPDATE_UI, on_update_ui, id=event_id\n        )\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.StandardButtons.make_menu","title":"make_menu","text":"<pre><code>make_menu(menubar, menulist)\n</code></pre> <p>Build a flat menu bar from a nested menu definition.</p> <p>Parameters:</p> Name Type Description Default <code>menubar</code> <p>The FlatMenuBar to populate.</p> required <code>menulist</code> <p>List of (title, ((label, id), ...)) tuples.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def make_menu(self, menubar, menulist):\n    \"\"\"Build a flat menu bar from a nested menu definition.\n\n    Args:\n        menubar: The FlatMenuBar to populate.\n        menulist: List of (title, ((label, id), ...)) tuples.\n    \"\"\"\n    for item in menulist:\n        menu = FM.FlatMenu()\n        for pos in item[1]:\n            menuitem = FM.FlatMenuItem(menu, pos[1], pos[0], pos[0], wx.ITEM_NORMAL)\n            menu.AppendItem(menuitem)\n        menubar.Append(menu, item[0])\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.StandardButtons.make_ui_handler","title":"make_ui_handler","text":"<pre><code>make_ui_handler(fun_name, event_id, propagate=False)\n</code></pre> <p>Create a UI update handler that enables/disables based on focus.</p> <p>Walks up the focus chain calling the named method; enables the event if any focused control returns True.</p> <p>Parameters:</p> Name Type Description Default <code>fun_name</code> <p>Name of the method to call (e.g. 'CanCopy').</p> required <code>event_id</code> <p>wx event ID to bind to.</p> required <code>propagate</code> <p>If True, walk up the parent chain.</p> <code>False</code> Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def make_ui_handler(self, fun_name, event_id, propagate=False):\n    \"\"\"Create a UI update handler that enables/disables based on focus.\n\n    Walks up the focus chain calling the named method; enables\n    the event if any focused control returns True.\n\n    Args:\n        fun_name: Name of the method to call (e.g. 'CanCopy').\n        event_id: wx event ID to bind to.\n        propagate: If True, walk up the parent chain.\n    \"\"\"\n\n    def on_update_ui(event):\n        win = find_focus_in_form()\n        while win:\n            if hasattr(win, fun_name):\n                try:\n                    if getattr(win, fun_name)():\n                        event.Enable(True)\n                        return\n                except wx._core.wxAssertionError:\n                    pass\n            if propagate:\n                win = win.GetParent()\n            else:\n                break\n        event.Enable(False)\n\n    self.toolbar_interface.parent.Bind(wx.EVT_UPDATE_UI, on_update_ui, id=event_id)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.StandardButtons.memke_menu","title":"memke_menu","text":"<pre><code>memke_menu(menubar, menulist)\n</code></pre> <p>Backward compatibility alias for make_menu.</p> <p>Parameters:</p> Name Type Description Default <code>menubar</code> <p>The FlatMenuBar to populate.</p> required <code>menulist</code> <p>List of (title, ((label, id), ...)) tuples.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def memke_menu(self, menubar, menulist):\n    \"\"\"Backward compatibility alias for make_menu.\n\n    Args:\n        menubar: The FlatMenuBar to populate.\n        menulist: List of (title, ((label, id), ...)) tuples.\n    \"\"\"\n    self.make_menu(menubar, menulist)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.StandardButtons.on_find","title":"on_find","text":"<pre><code>on_find(event)\n</code></pre> <p>Show the find/replace dropdown menu.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>The dropdown event.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def on_find(self, event):\n    \"\"\"Show the find/replace dropdown menu.\n\n    Args:\n        event: The dropdown event.\n    \"\"\"\n    menu = FM.FlatMenu()\n    menu.Append(wx.ID_ANY, _(\"Find\"), _(\"Find\"), wx.ITEM_NORMAL)\n    menu.Append(wx.ID_ANY, _(\"Replace\"), _(\"Replace\"), wx.ITEM_NORMAL)\n    event.PopupMenu(menu)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.StandardButtons.on_print","title":"on_print","text":"<pre><code>on_print(event)\n</code></pre> <p>Show the print dropdown menu.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>The dropdown event.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def on_print(self, event):\n    \"\"\"Show the print dropdown menu.\n\n    Args:\n        event: The dropdown event.\n    \"\"\"\n    menu = FM.FlatMenu()\n    menu.Append(wx.ID_ANY, _(\"Print\"), _(\"Print\"), wx.ITEM_NORMAL)\n    menu.Append(wx.ID_ANY, _(\"Print preview\"), _(\"Print preview\"), wx.ITEM_NORMAL)\n    menu.Append(\n        wx.ID_ANY, _(\"Printer settings\"), _(\"Printer settings\"), wx.ITEM_NORMAL\n    )\n    self.ti.popup_menu(event, menu)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.StandardButtons.open_page","title":"open_page","text":"<pre><code>open_page(page)\n</code></pre> <p>Open a URL in a new web view page.</p> <p>Parameters:</p> Name Type Description Default <code>page</code> <p>URL to open.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>True on success.</p> Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def open_page(self, page):\n    \"\"\"Open a URL in a new web view page.\n\n    Args:\n        page: URL to open.\n\n    Returns:\n        bool: True on success.\n    \"\"\"\n    name = self._title_from_address(page)\n    okno = (\n        wx.GetApp()\n        .GetTopWindow()\n        .new_main_page(\"^standard/webview/widget_web.html\", name)\n    )\n    okno.body.WEB.Go(page)\n    return True\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.StandardButtons.operations_refr","title":"operations_refr","text":"<pre><code>operations_refr(editctrl)\n</code></pre> <p>Refresh operations buttons based on the focused control's capabilities.</p> <p>Parameters:</p> Name Type Description Default <code>editctrl</code> <p>The currently focused edit control, or None.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def operations_refr(self, editctrl):\n    \"\"\"Refresh operations buttons based on the focused control's capabilities.\n\n    Args:\n        editctrl: The currently focused edit control, or None.\n    \"\"\"\n    if self.operations:\n        tab_status = []\n        if editctrl:\n            tab_status.append(\n                (\n                    hasattr(editctrl, \"CanRedo\") and editctrl.CanRedo(),\n                    \"operations\",\n                    \"redo\",\n                )\n            )\n            tab_status.append(\n                (\n                    hasattr(editctrl, \"CanUndo\") and editctrl.CanUndo(),\n                    \"operations\",\n                    \"undo\",\n                )\n            )\n        else:\n            tab_status.append((False, \"operations\", \"redo\"))\n            tab_status.append((False, \"operations\", \"undo\"))\n        return self._status(tab_status)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.StandardButtons.realize","title":"realize","text":"<pre><code>realize()\n</code></pre> <p>No-op realization method for compatibility.</p> Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def realize(self):\n    \"\"\"No-op realization method for compatibility.\"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.StandardButtons.web_menu","title":"web_menu","text":"<pre><code>web_menu()\n</code></pre> <p>Return the web menu structure definition.</p> <p>Returns:</p> Name Type Description <code>tuple</code> <p>Nested menu definition for the web toolbar menu.</p> Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def web_menu(self):\n    \"\"\"Return the web menu structure definition.\n\n    Returns:\n        tuple: Nested menu definition for the web toolbar menu.\n    \"\"\"\n    menu = (\n        (\n            _(\"View\"),\n            (\n                (_(\"Zoom\"), ID_ZOOM),\n                (_(\"Block elements\"), ID_PAGE_BLOCK),\n                (_(\"View elements\"), ID_PAGE_VIEW),\n                (_(\"View page source\"), ID_WEB_SOURCE),\n                (_(\"Edit page\"), ID_WEB_EDIT),\n            ),\n        ),\n        (\n            _(\"History\"),\n            (\n                (_(\"Show history panel\"), ID_HISTORY_SHOW),\n                (_(\"Clear history\"), ID_CLEAR_HISTORY),\n            ),\n        ),\n        (\n            _(\"Bookmarks\"),\n            (\n                (_(\"Add to bookmarks\"), ID_BOOKMARK_ADD),\n                (_(\"Show bookmarks panel\"), ID_SHOW_BOOKMARKS),\n            ),\n        ),\n        (\n            _(\"Downloads\"),\n            (\n                (_(\"Show download panel\"), ID_SHOW_DOWNLOAD),\n                (_(\"Options\"), ID_DOWNLOAD_OPTIONS),\n            ),\n        ),\n        (\n            _(\"Tools\"),\n            (\n                (_(\"Show download panel\"), ID_SHOW_DOWNLOAD),\n                (_(\"Options\"), ID_DOWNLOAD_OPTIONS),\n            ),\n        ),\n        (\n            _(\"Options\"),\n            (\n                (_(\"General\"), ID_GENERAL_OPTIONS),\n                (_(\"Black list\"), ID_DOWNLOAD_OPTIONS),\n            ),\n        ),\n    )\n    return menu\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.StandardButtons.web_refr","title":"web_refr","text":"<pre><code>web_refr(webobject)\n</code></pre> <p>Refresh browser navigation buttons based on web view state.</p> <p>Parameters:</p> Name Type Description Default <code>webobject</code> <p>The web view control, or None.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def web_refr(self, webobject):\n    \"\"\"Refresh browser navigation buttons based on web view state.\n\n    Args:\n        webobject: The web view control, or None.\n    \"\"\"\n    if self.browse or self.nav:\n        tab_status = []\n        if webobject:\n            tab_status.append((webobject.CanGoBack(), \"browser\", \"back\"))\n            tab_status.append((webobject.CanGoForward(), \"browser\", \"forward\"))\n            if self.browse:\n                tab_status.append((True, \"browser\", \"stop\"))\n                tab_status.append((True, \"browser\", \"refresh\"))\n                tab_status.append((True, \"browser\", \"addbookmark\"))\n                tab_status.append((True, \"browser\", \"newpage\"))\n        else:\n            tab_status.append((False, \"browser\", \"back\"))\n            tab_status.append((False, \"browser\", \"forward\"))\n            if self.browse:\n                tab_status.append((False, \"browser\", \"stop\"))\n                tab_status.append((False, \"browser\", \"refresh\"))\n                tab_status.append((False, \"browser\", \"addbookmark\"))\n                tab_status.append((False, \"browser\", \"newpage\"))\n        return self._status(tab_status)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarBar","title":"ToolbarBar","text":"<pre><code>ToolbarBar(parent, gui_style)\n</code></pre> <p>Base class for all menus and toolbars connected to the top frame window.</p> <p>Manages a collection of ToolbarPage objects and provides common operations like binding events and creating standard buttons.</p> <p>Initialize the toolbar bar.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window - the top wx.Frame derived object.</p> required <code>gui_style</code> <p>String description of the GUI interface. Based on this string, standard toolbar elements are created.</p> required Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, parent, gui_style):\n    \"\"\"Initialize the toolbar bar.\n\n    Args:\n        parent: Parent window - the top wx.Frame derived object.\n        gui_style: String description of the GUI interface. Based on\n            this string, standard toolbar elements are created.\n    \"\"\"\n    self.parent = parent\n    self.main_page = None\n    self.gui_style = gui_style\n\n    self.toolbars = {}\n    self.pages = []\n    self.user_panels = {}\n\n    self.standard_buttons = StandardButtons(self, gui_style)\n    self.standard_buttons.create_file_panel(self.main_page)\n    self.standard_buttons.create_edit_panel(self.main_page)\n    self.standard_buttons.create_operations_panel(self.main_page)\n    self.standard_buttons.create_browse_panel(self.main_page)\n    self.standard_buttons.create_address_panel(self.main_page)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarBar-functions","title":"Methods:","text":""},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarBar.append","title":"append","text":"<pre><code>append(title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>Append a page to the toolbar, or return an existing one.</p> <p>If a page with the same name and kind already exists, it is returned instead of creating a duplicate.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Page title.</p> required <code>kind</code> <p>Type of page; one of ToolbarPage.TYPE_PAGE_* constants.</p> <code>TYPE_PAGE_NORMAL</code> <p>Returns:</p> Name Type Description <code>ToolbarPage</code> <p>The existing or newly created page.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def append(self, title, kind=ToolbarPage.TYPE_PAGE_NORMAL):\n    \"\"\"Append a page to the toolbar, or return an existing one.\n\n    If a page with the same name and kind already exists, it is\n    returned instead of creating a duplicate.\n\n    Args:\n        title: Page title.\n        kind: Type of page; one of ToolbarPage.TYPE_PAGE_* constants.\n\n    Returns:\n        ToolbarPage: The existing or newly created page.\n    \"\"\"\n    for page in self.pages:\n        if page.name == title and page.kind == kind:\n            return page\n    p = self.create_page(title, kind)\n    if not self.main_page:\n        self.main_page = p\n    self.pages.append(p)\n    return p\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarBar.bind","title":"bind","text":"<pre><code>bind(fun, id=ID_ANY, e=None)\n</code></pre> <p>Bind an event handler to the toolbar's parent window.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <p>Handler function.</p> required <code>id</code> <p>Identifier of the event.</p> <code>ID_ANY</code> <code>e</code> <p>Event type; defaults to wx.EVT_MENU if not specified.</p> <code>None</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def bind(self, fun, id=wx.ID_ANY, e=None):\n    \"\"\"Bind an event handler to the toolbar's parent window.\n\n    Args:\n        fun: Handler function.\n        id: Identifier of the event.\n        e: Event type; defaults to wx.EVT_MENU if not specified.\n    \"\"\"\n    if e:\n        self.parent.Bind(e, fun, id=id)\n    else:\n        self.parent.Bind(wx.EVT_MENU, fun, id=id)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarBar.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Close/destroy the toolbar.</p> <p>Override in subclasses to perform cleanup.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def close(self):\n    \"\"\"Close/destroy the toolbar.\n\n    Override in subclasses to perform cleanup.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarBar.create","title":"create","text":"<pre><code>create()\n</code></pre> <p>Create/realize the toolbar.</p> <p>Override in subclasses to perform actual widget creation.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create(self):\n    \"\"\"Create/realize the toolbar.\n\n    Override in subclasses to perform actual widget creation.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarBar.create_html_win","title":"create_html_win","text":"<pre><code>create_html_win(toolbar_page, address_or_parser, parameters, callback=None)\n</code></pre> <p>Create an HTML page embedded in the toolbar.</p> <p>Not all toolbar types support this feature.</p> <p>Parameters:</p> Name Type Description Default <code>toolbar_page</code> <p>Name of the toolbar page to host the HTML. Use '__' to separate page and panel names, e.g. 'PageName__PanelName'.</p> required <code>address_or_parser</code> <p>Address of the HTTP page or a parser.</p> required <code>parameters</code> <p>Parameters for the HTTP request.</p> required <code>callback</code> <p>Optional callback invoked after initialization.</p> <code>None</code> <p>Returns:</p> Type Description <p>SchPage or None: The created page, or None on failure.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_html_win(\n    self, toolbar_page, address_or_parser, parameters, callback=None\n):\n    \"\"\"Create an HTML page embedded in the toolbar.\n\n    Not all toolbar types support this feature.\n\n    Args:\n        toolbar_page: Name of the toolbar page to host the HTML.\n            Use '__' to separate page and panel names,\n            e.g. 'PageName__PanelName'.\n        address_or_parser: Address of the HTTP page or a parser.\n        parameters: Parameters for the HTTP request.\n        callback: Optional callback invoked after initialization.\n\n    Returns:\n        SchPage or None: The created page, or None on failure.\n    \"\"\"\n    page = None\n    if not toolbar_page:\n        u_name = \"main tools\"\n        page_name = _(\"main tools\")\n        panel_name = \"main tools\"\n    else:\n        u_name = toolbar_page\n        names = toolbar_page.split(\"__\")\n        if len(names) &gt; 1:\n            page_name = names[0].replace(\"_\", \" \")\n            panel_name = names[1].replace(\"_\", \" \")\n        else:\n            page_name = toolbar_page.replace(\"_\", \" \")\n            panel_name = page_name\n\n    if u_name in self.user_panels:\n        panel = self.user_panels[u_name]\n    else:\n        page = self._find_page_by_title(page_name)\n        if page is None:\n            page = self.append(page_name)\n        panel = page.create_html_panel(panel_name)\n\n    if panel:\n        dx = panel.get_width() + 3\n        dy = panel.get_height() + 5\n\n        page2 = SchPage(\n            panel.get_window(),\n            address_or_parser,\n            parameters,\n            size=wx.Size(dx, dy),\n            pos=wx.Point(2, 2),\n        )\n        best = page2.body.calculate_best_size()\n        page2.SetSize(wx.Size(int(best[0]), int(best[1])))\n        panel.set_page(page2)\n        self.user_panels[u_name] = panel\n        page2.body.toolbar_interface = self\n        page2.body.toolbar_interface_page = page\n\n        def init_page(callback):\n            page2.init_frame()\n            page2.activate_page()\n            page2.Update()\n\n        wx.CallAfter(init_page, callback)\n\n        return page2\n\n    return None\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarBar.create_page","title":"create_page","text":"<pre><code>create_page(title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>Create and return a new ToolbarPage.</p> <p>Override in derived classes to return a subclass of ToolbarPage.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Page title.</p> required <code>kind</code> <p>Type of page; one of ToolbarPage.TYPE_PAGE_* constants.</p> <code>TYPE_PAGE_NORMAL</code> <p>Returns:</p> Name Type Description <code>ToolbarPage</code> <p>The newly created page.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_page(self, title, kind=ToolbarPage.TYPE_PAGE_NORMAL):\n    \"\"\"Create and return a new ToolbarPage.\n\n    Override in derived classes to return a subclass of ToolbarPage.\n\n    Args:\n        title: Page title.\n        kind: Type of page; one of ToolbarPage.TYPE_PAGE_* constants.\n\n    Returns:\n        ToolbarPage: The newly created page.\n    \"\"\"\n    return ToolbarPage(self, title, kind)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarBar.create_panel_in_main_page","title":"create_panel_in_main_page","text":"<pre><code>create_panel_in_main_page(title, kind)\n</code></pre> <p>Create a panel in the main page.</p> <p>If no main page exists yet, one is created automatically.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>New panel's title.</p> required <code>kind</code> <p>Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.</p> required <p>Returns:</p> Name Type Description <code>ToolbarPanel</code> <p>The newly created panel.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_panel_in_main_page(self, title, kind):\n    \"\"\"Create a panel in the main page.\n\n    If no main page exists yet, one is created automatically.\n\n    Args:\n        title: New panel's title.\n        kind: Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.\n\n    Returns:\n        ToolbarPanel: The newly created panel.\n    \"\"\"\n    if not self.main_page:\n        self.append(\"main tools\")\n    return self.main_page.create_panel(title, kind)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarBar.new_child_page","title":"new_child_page","text":"<pre><code>new_child_page(address_or_parser, title='', param=None, callback=None)\n</code></pre> <p>Create a new child page.</p> <p>For toolbar, the child page is transferred to the desktop window.</p> <p>Parameters:</p> Name Type Description Default <code>address_or_parser</code> <p>Address of the HTTP page or a parser.</p> required <code>title</code> <p>Page title.</p> <code>''</code> <code>param</code> <p>Optional parameters.</p> <code>None</code> <code>callback</code> <p>Optional callback.</p> <code>None</code> <p>Returns:</p> Type Description <p>The result of new_main_page on the top window.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def new_child_page(self, address_or_parser, title=\"\", param=None, callback=None):\n    \"\"\"Create a new child page.\n\n    For toolbar, the child page is transferred to the desktop window.\n\n    Args:\n        address_or_parser: Address of the HTTP page or a parser.\n        title: Page title.\n        param: Optional parameters.\n        callback: Optional callback.\n\n    Returns:\n        The result of new_main_page on the top window.\n    \"\"\"\n    return wx.GetApp().GetTopWindow().new_main_page(address_or_parser, title, param)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarBar.remove_page","title":"remove_page","text":"<pre><code>remove_page(title)\n</code></pre> <p>Remove the page with the specified title from the toolbar.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Title of the page to remove.</p> required Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def remove_page(self, title):\n    \"\"\"Remove the page with the specified title from the toolbar.\n\n    Args:\n        title: Title of the page to remove.\n    \"\"\"\n    # Remove from pages list\n    for page in self.pages:\n        if page.title == title:\n            self.pages.remove(page)\n            break\n\n    # If the main page was removed, pick a new one\n    if self.main_page and self.main_page.title == title:\n        self.main_page = self.pages[0] if self.pages else None\n\n    # Remove associated user panels (iterate over a static list of keys\n    # to avoid \"dictionary changed size during iteration\" errors)\n    for key in list(self.user_panels.keys()):\n        panel = self.user_panels[key]\n        if panel.page.title == title:\n            del self.user_panels[key]\n            return\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarBar.un_bind","title":"un_bind","text":"<pre><code>un_bind(id=ID_ANY, e=None)\n</code></pre> <p>Unbind an event handler from the toolbar's parent window.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>Identifier of the event.</p> <code>ID_ANY</code> <code>e</code> <p>Event type; defaults to wx.EVT_MENU if not specified.</p> <code>None</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def un_bind(self, id=wx.ID_ANY, e=None):\n    \"\"\"Unbind an event handler from the toolbar's parent window.\n\n    Args:\n        id: Identifier of the event.\n        e: Event type; defaults to wx.EVT_MENU if not specified.\n    \"\"\"\n    if e:\n        self.parent.Unbind(e, id=id)\n    else:\n        self.parent.Unbind(wx.EVT_MENU, id=id)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarButton","title":"ToolbarButton","text":"<pre><code>ToolbarButton(\n    parent_panel, id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE\n)\n</code></pre> <p>Represents a single button (or separator) on a toolbar panel.</p> <p>This is an abstract data model; the actual wx widget is created by the concrete toolbar implementation.</p> Button type constants <p>TYPE_SIMPLE = 0     - Simple push button. TYPE_DROPDOWN = 1   - Button with a dropdown menu. TYPE_HYBRID = 2     - Hybrid button (part button, part dropdown). TYPE_TOOGLE = 3     - Toggle button. TYPE_PANEL = 4      - Embedded panel. TYPE_SEPARATOR = 5  - Visual separator.</p> <p>Create a new toolbar button.</p> <p>Parameters:</p> Name Type Description Default <code>parent_panel</code> <p>Parent ToolbarPanel object.</p> required <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title (display text).</p> required <code>bitmap</code> <p>Button bitmap for the normal state.</p> <code>None</code> <code>bitmap_disabled</code> <p>Button bitmap for the disabled state. Defaults to wx.NullBitmap when bitmap is provided.</p> <code>None</code> <code>kind</code> <p>Type of button; one of the TYPE_* class constants.</p> <code>TYPE_SIMPLE</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(\n    self,\n    parent_panel,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=TYPE_SIMPLE,\n):\n    \"\"\"Create a new toolbar button.\n\n    Args:\n        parent_panel: Parent ToolbarPanel object.\n        id: An integer by which the tool may be identified.\n        title: Button title (display text).\n        bitmap: Button bitmap for the normal state.\n        bitmap_disabled: Button bitmap for the disabled state.\n            Defaults to wx.NullBitmap when bitmap is provided.\n        kind: Type of button; one of the TYPE_* class constants.\n    \"\"\"\n    self.parent_panel = parent_panel\n    self.id = id\n    self.title = title\n    self.bitmap = bitmap\n    if bitmap_disabled is None and bitmap is not None:\n        self.bitmap_disabled = wx.NullBitmap\n    else:\n        self.bitmap_disabled = bitmap_disabled\n    self.kind = kind\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarButton-functions","title":"Methods:","text":""},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarPage","title":"ToolbarPage","text":"<pre><code>ToolbarPage(parent_bar, title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>A page (tab) within a toolbar bar, containing one or more panels.</p> Page type constants <p>TYPE_PAGE_NORMAL = 0  - Standard page.</p> <p>Create a new toolbar page.</p> <p>Parameters:</p> Name Type Description Default <code>parent_bar</code> <p>Parent ToolbarBar object.</p> required <code>title</code> <p>Page title (will be translated via wx.GetTranslation).</p> required <code>kind</code> <p>Type of page; one of the TYPE_PAGE_* class constants.</p> <code>TYPE_PAGE_NORMAL</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, parent_bar, title, kind=TYPE_PAGE_NORMAL):\n    \"\"\"Create a new toolbar page.\n\n    Args:\n        parent_bar: Parent ToolbarBar object.\n        title: Page title (will be translated via wx.GetTranslation).\n        kind: Type of page; one of the TYPE_PAGE_* class constants.\n    \"\"\"\n    self.parent_bar = parent_bar\n    self.name = title\n    self.title = _(title)\n    self.kind = kind\n    self.panels = []\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarPage-functions","title":"Methods:","text":""},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarPage.append","title":"append","text":"<pre><code>append(title, kind=TYPE_PANEL_BUTTONBAR)\n</code></pre> <p>Create a new panel and append it to this page.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Panel title.</p> required <code>kind</code> <p>Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.</p> <code>TYPE_PANEL_BUTTONBAR</code> <p>Returns:</p> Name Type Description <code>ToolbarPanel</code> <p>The newly created panel.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def append(self, title, kind=ToolbarPanel.TYPE_PANEL_BUTTONBAR):\n    \"\"\"Create a new panel and append it to this page.\n\n    Args:\n        title: Panel title.\n        kind: Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.\n\n    Returns:\n        ToolbarPanel: The newly created panel.\n    \"\"\"\n    p = self.create_panel(title, kind)\n    self.panels.append(p)\n    return p\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarPage.create_html_panel","title":"create_html_panel","text":"<pre><code>create_html_panel(title)\n</code></pre> <p>Create an HTML-capable panel within this page.</p> <p>Override in derived classes to return a BaseHtmlPanel subclass.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Panel title.</p> required <p>Returns:</p> Type Description <p>BaseHtmlPanel or None: The panel, or None if not supported.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_html_panel(self, title):\n    \"\"\"Create an HTML-capable panel within this page.\n\n    Override in derived classes to return a BaseHtmlPanel subclass.\n\n    Args:\n        title: Panel title.\n\n    Returns:\n        BaseHtmlPanel or None: The panel, or None if not supported.\n    \"\"\"\n    return None\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarPage.create_panel","title":"create_panel","text":"<pre><code>create_panel(title, kind)\n</code></pre> <p>Create and return a new ToolbarPanel for this page.</p> <p>Override in derived classes to return a subclass of ToolbarPanel.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Panel title.</p> required <code>kind</code> <p>Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.</p> required <p>Returns:</p> Name Type Description <code>ToolbarPanel</code> <p>The newly created panel.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_panel(self, title, kind):\n    \"\"\"Create and return a new ToolbarPanel for this page.\n\n    Override in derived classes to return a subclass of ToolbarPanel.\n\n    Args:\n        title: Panel title.\n        kind: Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.\n\n    Returns:\n        ToolbarPanel: The newly created panel.\n    \"\"\"\n    return ToolbarPanel(self, title, kind=ToolbarPanel.TYPE_PANEL_BUTTONBAR)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarPanel","title":"ToolbarPanel","text":"<pre><code>ToolbarPanel(parent_page, title, kind=TYPE_PANEL_BUTTONBAR)\n</code></pre> <p>A panel within a toolbar page that holds a group of buttons.</p> Panel type constants <p>TYPE_PANEL_BUTTONBAR = 0  - Button bar layout. TYPE_PANEL_TOOLBAR = 1    - Toolbar layout. TYPE_PANEL_PANELBAR = 2   - Custom panel layout.</p> <p>Create a new toolbar panel.</p> <p>Parameters:</p> Name Type Description Default <code>parent_page</code> <p>Parent ToolbarPage object.</p> required <code>title</code> <p>Panel title.</p> required <code>kind</code> <p>Type of panel; one of the TYPE_PANEL_* class constants.</p> <code>TYPE_PANEL_BUTTONBAR</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, parent_page, title, kind=TYPE_PANEL_BUTTONBAR):\n    \"\"\"Create a new toolbar panel.\n\n    Args:\n        parent_page: Parent ToolbarPage object.\n        title: Panel title.\n        kind: Type of panel; one of the TYPE_PANEL_* class constants.\n    \"\"\"\n    self.parent_page = parent_page\n    self.title = title\n    self.kind = kind\n    self.buttons = []\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarPanel-functions","title":"Methods:","text":""},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarPanel.add_dropdown_tool","title":"add_dropdown_tool","text":"<pre><code>add_dropdown_tool(id, title, bitmaps)\n</code></pre> <p>Append a dropdown button to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_dropdown_tool(self, id, title, bitmaps):\n    \"\"\"Append a dropdown button to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_DROPDOWN)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarPanel.add_hybrid_tool","title":"add_hybrid_tool","text":"<pre><code>add_hybrid_tool(id, title, bitmaps)\n</code></pre> <p>Append a hybrid button (button + dropdown) to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_hybrid_tool(self, id, title, bitmaps):\n    \"\"\"Append a hybrid button (button + dropdown) to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_HYBRID)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarPanel.add_separator","title":"add_separator","text":"<pre><code>add_separator()\n</code></pre> <p>Append a visual separator to the panel.</p> <p>The default implementation does nothing; override in subclasses.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_separator(self):\n    \"\"\"Append a visual separator to the panel.\n\n    The default implementation does nothing; override in subclasses.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarPanel.add_simple_tool","title":"add_simple_tool","text":"<pre><code>add_simple_tool(id, title, bitmaps)\n</code></pre> <p>Append a simple push button to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_simple_tool(self, id, title, bitmaps):\n    \"\"\"Append a simple push button to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_SIMPLE)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarPanel.add_toogle_tool","title":"add_toogle_tool","text":"<pre><code>add_toogle_tool(id, title, bitmaps)\n</code></pre> <p>Append a toggle button to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_toogle_tool(self, id, title, bitmaps):\n    \"\"\"Append a toggle button to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_TOOGLE)\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarPanel.append","title":"append","text":"<pre><code>append(id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE)\n</code></pre> <p>Create a button and append it to this panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmap</code> <p>Button bitmap for the normal state.</p> <code>None</code> <code>bitmap_disabled</code> <p>Button bitmap for the disabled state.</p> <code>None</code> <code>kind</code> <p>Type of button; one of ToolbarButton.TYPE_* constants.</p> <code>TYPE_SIMPLE</code> <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created and appended button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def append(\n    self,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=ToolbarButton.TYPE_SIMPLE,\n):\n    \"\"\"Create a button and append it to this panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmap: Button bitmap for the normal state.\n        bitmap_disabled: Button bitmap for the disabled state.\n        kind: Type of button; one of ToolbarButton.TYPE_* constants.\n\n    Returns:\n        ToolbarButton: The newly created and appended button.\n    \"\"\"\n    button = self.create_button(id, title, bitmap, bitmap_disabled, kind)\n    self.buttons.append(button)\n    return button\n</code></pre>"},{"location":"api/toolbar_basetoolbar/#pytigon_gui.toolbar.basetoolbar.ToolbarPanel.create_button","title":"create_button","text":"<pre><code>create_button(id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE)\n</code></pre> <p>Create and return a new ToolbarButton.</p> <p>Override in derived classes to return a subclass of ToolbarButton.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmap</code> <p>Button bitmap for the normal state.</p> <code>None</code> <code>bitmap_disabled</code> <p>Button bitmap for the disabled state.</p> <code>None</code> <code>kind</code> <p>Type of button; one of ToolbarButton.TYPE_* constants.</p> <code>TYPE_SIMPLE</code> <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_button(\n    self,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=ToolbarButton.TYPE_SIMPLE,\n):\n    \"\"\"Create and return a new ToolbarButton.\n\n    Override in derived classes to return a subclass of ToolbarButton.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmap: Button bitmap for the normal state.\n        bitmap_disabled: Button bitmap for the disabled state.\n        kind: Type of button; one of ToolbarButton.TYPE_* constants.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    return ToolbarButton(self, id, title, bitmap, bitmap_disabled, kind)\n</code></pre>"},{"location":"api/toolbar_generictoolbar/","title":"toolbar.generictoolbar \u2013 Generic Toolbar","text":"<p>Generic configurable toolbar built from JSON or dictionary definitions, suitable for dynamic toolbars.</p>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar","title":"generictoolbar","text":"<p>Generic (AUI) toolbar implementation.</p> <p>Provides a toolbar based on wx.lib.agw.aui that supports dockable toolbar panes with standard button types.</p>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar-attributes","title":"Attributes","text":""},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_BOOKMARK_ADD","title":"ID_BOOKMARK_ADD  <code>module-attribute</code>","text":"<pre><code>ID_BOOKMARK_ADD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_CLEAR_HISTORY","title":"ID_CLEAR_HISTORY  <code>module-attribute</code>","text":"<pre><code>ID_CLEAR_HISTORY = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_CLOSETAB","title":"ID_CLOSETAB  <code>module-attribute</code>","text":"<pre><code>ID_CLOSETAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_COPY","title":"ID_COPY  <code>module-attribute</code>","text":"<pre><code>ID_COPY = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_CUT","title":"ID_CUT  <code>module-attribute</code>","text":"<pre><code>ID_CUT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_DOWNLOAD_OPTIONS","title":"ID_DOWNLOAD_OPTIONS  <code>module-attribute</code>","text":"<pre><code>ID_DOWNLOAD_OPTIONS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_END","title":"ID_END  <code>module-attribute</code>","text":"<pre><code>ID_END = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_EXIT","title":"ID_EXIT  <code>module-attribute</code>","text":"<pre><code>ID_EXIT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_FIND","title":"ID_FIND  <code>module-attribute</code>","text":"<pre><code>ID_FIND = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_GENERAL_OPTIONS","title":"ID_GENERAL_OPTIONS  <code>module-attribute</code>","text":"<pre><code>ID_GENERAL_OPTIONS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_GOTODESKTOP","title":"ID_GOTODESKTOP  <code>module-attribute</code>","text":"<pre><code>ID_GOTODESKTOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_GOTOFOOTER","title":"ID_GOTOFOOTER  <code>module-attribute</code>","text":"<pre><code>ID_GOTOFOOTER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_GOTOHEADER","title":"ID_GOTOHEADER  <code>module-attribute</code>","text":"<pre><code>ID_GOTOHEADER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_GOTOPANEL","title":"ID_GOTOPANEL  <code>module-attribute</code>","text":"<pre><code>ID_GOTOPANEL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_HELP","title":"ID_HELP  <code>module-attribute</code>","text":"<pre><code>ID_HELP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_HISTORY_SHOW","title":"ID_HISTORY_SHOW  <code>module-attribute</code>","text":"<pre><code>ID_HISTORY_SHOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_LOAD","title":"ID_LOAD  <code>module-attribute</code>","text":"<pre><code>ID_LOAD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_NEXTPAGE","title":"ID_NEXTPAGE  <code>module-attribute</code>","text":"<pre><code>ID_NEXTPAGE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_NEXTTAB","title":"ID_NEXTTAB  <code>module-attribute</code>","text":"<pre><code>ID_NEXTTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_PAGE_BLOCK","title":"ID_PAGE_BLOCK  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_BLOCK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_PAGE_SOURCE","title":"ID_PAGE_SOURCE  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_SOURCE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_PAGE_VIEW","title":"ID_PAGE_VIEW  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_VIEW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_PASTE","title":"ID_PASTE  <code>module-attribute</code>","text":"<pre><code>ID_PASTE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_PREVPAGE","title":"ID_PREVPAGE  <code>module-attribute</code>","text":"<pre><code>ID_PREVPAGE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_PREVTAB","title":"ID_PREVTAB  <code>module-attribute</code>","text":"<pre><code>ID_PREVTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_PRINT","title":"ID_PRINT  <code>module-attribute</code>","text":"<pre><code>ID_PRINT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_PRINT_PREVIEW","title":"ID_PRINT_PREVIEW  <code>module-attribute</code>","text":"<pre><code>ID_PRINT_PREVIEW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_PRINT_SETTINGS","title":"ID_PRINT_SETTINGS  <code>module-attribute</code>","text":"<pre><code>ID_PRINT_SETTINGS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_REDO","title":"ID_REDO  <code>module-attribute</code>","text":"<pre><code>ID_REDO = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_REFRESHTAB","title":"ID_REFRESHTAB  <code>module-attribute</code>","text":"<pre><code>ID_REFRESHTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_REPLACE","title":"ID_REPLACE  <code>module-attribute</code>","text":"<pre><code>ID_REPLACE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_RESET","title":"ID_RESET  <code>module-attribute</code>","text":"<pre><code>ID_RESET = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_RETVALUE","title":"ID_RETVALUE  <code>module-attribute</code>","text":"<pre><code>ID_RETVALUE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_SAVE","title":"ID_SAVE  <code>module-attribute</code>","text":"<pre><code>ID_SAVE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_SAVE_AS","title":"ID_SAVE_AS  <code>module-attribute</code>","text":"<pre><code>ID_SAVE_AS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_SELALL","title":"ID_SELALL  <code>module-attribute</code>","text":"<pre><code>ID_SELALL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_SHOWDESKTOP","title":"ID_SHOWDESKTOP  <code>module-attribute</code>","text":"<pre><code>ID_SHOWDESKTOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_SHOWFOOTER","title":"ID_SHOWFOOTER  <code>module-attribute</code>","text":"<pre><code>ID_SHOWFOOTER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_SHOWHEADER","title":"ID_SHOWHEADER  <code>module-attribute</code>","text":"<pre><code>ID_SHOWHEADER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_SHOWPANEL","title":"ID_SHOWPANEL  <code>module-attribute</code>","text":"<pre><code>ID_SHOWPANEL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_SHOWSTATUSBAR","title":"ID_SHOWSTATUSBAR  <code>module-attribute</code>","text":"<pre><code>ID_SHOWSTATUSBAR = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_SHOWTOOLBAR1","title":"ID_SHOWTOOLBAR1  <code>module-attribute</code>","text":"<pre><code>ID_SHOWTOOLBAR1 = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_SHOWTOOLBAR2","title":"ID_SHOWTOOLBAR2  <code>module-attribute</code>","text":"<pre><code>ID_SHOWTOOLBAR2 = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_SHOW_BOOKMARKS","title":"ID_SHOW_BOOKMARKS  <code>module-attribute</code>","text":"<pre><code>ID_SHOW_BOOKMARKS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_SHOW_DOWNLOAD","title":"ID_SHOW_DOWNLOAD  <code>module-attribute</code>","text":"<pre><code>ID_SHOW_DOWNLOAD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_START","title":"ID_START  <code>module-attribute</code>","text":"<pre><code>ID_START = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_TASKBAR_CLOSE","title":"ID_TASKBAR_CLOSE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_CLOSE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_TASKBAR_HIDE","title":"ID_TASKBAR_HIDE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_HIDE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_TASKBAR_SHOW","title":"ID_TASKBAR_SHOW  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_SHOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_TASKBAR_TOGGLE","title":"ID_TASKBAR_TOGGLE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_TOGGLE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_UNDO","title":"ID_UNDO  <code>module-attribute</code>","text":"<pre><code>ID_UNDO = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_WEB_ADDBOOKMARK","title":"ID_WEB_ADDBOOKMARK  <code>module-attribute</code>","text":"<pre><code>ID_WEB_ADDBOOKMARK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_WEB_BACK","title":"ID_WEB_BACK  <code>module-attribute</code>","text":"<pre><code>ID_WEB_BACK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_WEB_DOWN","title":"ID_WEB_DOWN  <code>module-attribute</code>","text":"<pre><code>ID_WEB_DOWN = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_WEB_EDIT","title":"ID_WEB_EDIT  <code>module-attribute</code>","text":"<pre><code>ID_WEB_EDIT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_WEB_FORWARD","title":"ID_WEB_FORWARD  <code>module-attribute</code>","text":"<pre><code>ID_WEB_FORWARD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_WEB_NEW_WINDOW","title":"ID_WEB_NEW_WINDOW  <code>module-attribute</code>","text":"<pre><code>ID_WEB_NEW_WINDOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_WEB_REFRESH","title":"ID_WEB_REFRESH  <code>module-attribute</code>","text":"<pre><code>ID_WEB_REFRESH = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_WEB_SOURCE","title":"ID_WEB_SOURCE  <code>module-attribute</code>","text":"<pre><code>ID_WEB_SOURCE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_WEB_STOP","title":"ID_WEB_STOP  <code>module-attribute</code>","text":"<pre><code>ID_WEB_STOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_WEB_UP","title":"ID_WEB_UP  <code>module-attribute</code>","text":"<pre><code>ID_WEB_UP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ID_ZOOM","title":"ID_ZOOM  <code>module-attribute</code>","text":"<pre><code>ID_ZOOM = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar._","title":"_  <code>module-attribute</code>","text":"<pre><code>_ = wx.GetTranslation\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar-classes","title":"Classes","text":""},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.GenericToolbarBar","title":"GenericToolbarBar","text":"<pre><code>GenericToolbarBar(parent, gui_style)\n</code></pre> <p>               Bases: <code>ToolbarBar</code></p> <p>The top-level AUI toolbar bar managing multiple AUI toolbar pages.</p> Source code in <code>pytigon_gui/toolbar/generictoolbar.py</code> <pre><code>def __init__(self, parent, gui_style):\n    super().__init__(parent, gui_style)\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.GenericToolbarBar-functions","title":"Methods:","text":""},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.GenericToolbarBar.bind","title":"bind","text":"<pre><code>bind(fun, id=ID_ANY, e=None)\n</code></pre> <p>Bind an event handler to the toolbar's parent window.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <p>Handler function.</p> required <code>id</code> <p>Event identifier.</p> <code>ID_ANY</code> <code>e</code> <p>Event type; defaults to wx.EVT_MENU.</p> <code>None</code> Source code in <code>pytigon_gui/toolbar/generictoolbar.py</code> <pre><code>def bind(self, fun, id=wx.ID_ANY, e=None):\n    \"\"\"Bind an event handler to the toolbar's parent window.\n\n    Args:\n        fun: Handler function.\n        id: Event identifier.\n        e: Event type; defaults to wx.EVT_MENU.\n    \"\"\"\n    if e:\n        self.parent.Bind(e, fun, id=id)\n    else:\n        self.parent.Bind(wx.EVT_MENU, fun, id=id)\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.GenericToolbarBar.bind_dropdown","title":"bind_dropdown","text":"<pre><code>bind_dropdown(fun, id)\n</code></pre> <p>Bind a dropdown menu event handler.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <p>Handler function.</p> required <code>id</code> <p>Event identifier.</p> required Source code in <code>pytigon_gui/toolbar/generictoolbar.py</code> <pre><code>def bind_dropdown(self, fun, id):\n    \"\"\"Bind a dropdown menu event handler.\n\n    Args:\n        fun: Handler function.\n        id: Event identifier.\n    \"\"\"\n    self.parent.Bind(wx.EVT_MENU, fun, id=id)\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.GenericToolbarBar.bind_ui","title":"bind_ui","text":"<pre><code>bind_ui(fun, id=ID_ANY)\n</code></pre> <p>Bind a UI update event handler.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <p>Handler function.</p> required <code>id</code> <p>Event identifier.</p> <code>ID_ANY</code> Source code in <code>pytigon_gui/toolbar/generictoolbar.py</code> <pre><code>def bind_ui(self, fun, id=wx.ID_ANY):\n    \"\"\"Bind a UI update event handler.\n\n    Args:\n        fun: Handler function.\n        id: Event identifier.\n    \"\"\"\n    self.parent.Bind(wx.EVT_UPDATE_UI, fun, id=id)\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.GenericToolbarBar.create","title":"create","text":"<pre><code>create()\n</code></pre> <p>Realize all toolbar pages and set their best sizes.</p> Source code in <code>pytigon_gui/toolbar/generictoolbar.py</code> <pre><code>def create(self):\n    \"\"\"Realize all toolbar pages and set their best sizes.\"\"\"\n    for bar in self.pages:\n        bar.Realize()\n        bar.SetSize(bar.GetBestSize())\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.GenericToolbarBar.create_page","title":"create_page","text":"<pre><code>create_page(title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>Create a new AUI toolbar page.</p> <p>Returns:</p> Name Type Description <code>GenericToolbarPage</code> <p>The newly created page.</p> Source code in <code>pytigon_gui/toolbar/generictoolbar.py</code> <pre><code>def create_page(self, title, kind=ToolbarPage.TYPE_PAGE_NORMAL):\n    \"\"\"Create a new AUI toolbar page.\n\n    Returns:\n        GenericToolbarPage: The newly created page.\n    \"\"\"\n    return GenericToolbarPage(self, title, kind)\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.GenericToolbarBar.un_bind","title":"un_bind","text":"<pre><code>un_bind(id, e=None)\n</code></pre> <p>Unbind an event handler from the toolbar's parent window.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>Event identifier.</p> required <code>e</code> <p>Event type; defaults to wx.EVT_MENU.</p> <code>None</code> Source code in <code>pytigon_gui/toolbar/generictoolbar.py</code> <pre><code>def un_bind(self, id, e=None):\n    \"\"\"Unbind an event handler from the toolbar's parent window.\n\n    Args:\n        id: Event identifier.\n        e: Event type; defaults to wx.EVT_MENU.\n    \"\"\"\n    if e:\n        self.parent.Unbind(e, id=id)\n    else:\n        self.parent.Unbind(wx.EVT_MENU, id=id)\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.GenericToolbarBar.update","title":"update","text":"<pre><code>update()\n</code></pre> <p>Update the toolbar layout (no-op for generic toolbar).</p> Source code in <code>pytigon_gui/toolbar/generictoolbar.py</code> <pre><code>def update(self):\n    \"\"\"Update the toolbar layout (no-op for generic toolbar).\"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.GenericToolbarButton","title":"GenericToolbarButton","text":"<pre><code>GenericToolbarButton(\n    parent_panel, id, title, bitmap, bitmap_disabled=None, kind=TYPE_SIMPLE\n)\n</code></pre> <p>               Bases: <code>ToolbarButton</code></p> <p>AUI-specific toolbar button (thin wrapper around ToolbarButton).</p> Source code in <code>pytigon_gui/toolbar/generictoolbar.py</code> <pre><code>def __init__(\n    self,\n    parent_panel,\n    id,\n    title,\n    bitmap,\n    bitmap_disabled=None,\n    kind=ToolbarButton.TYPE_SIMPLE,\n):\n    ToolbarButton.__init__(\n        self, parent_panel, id, title, bitmap, bitmap_disabled, kind\n    )\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.GenericToolbarPage","title":"GenericToolbarPage","text":"<pre><code>GenericToolbarPage(parent_bar, title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>               Bases: <code>ToolbarPage</code>, <code>AuiToolBar</code></p> <p>A toolbar page that wraps an AUI AuiToolBar pane.</p> Source code in <code>pytigon_gui/toolbar/generictoolbar.py</code> <pre><code>def __init__(self, parent_bar, title, kind=ToolbarPage.TYPE_PAGE_NORMAL):\n    ToolbarPage.__init__(self, parent_bar, title, kind)\n    agwStyle = aui.AUI_TB_DEFAULT_STYLE | aui.AUI_TB_HORZ_LAYOUT\n    aui.AuiToolBar.__init__(\n        self,\n        parent_bar.parent,\n        -1,\n        wx.DefaultPosition,\n        wx.DefaultSize,\n        agwStyle=agwStyle,\n    )\n\n    self.SetToolBitmapSize(wx.Size(24, 24))\n    if wx.Platform != \"__WXMSW__\":\n        self.SetArtProvider(SchAuiToolBarArt())\n    nr = len(parent_bar.pages) + 1\n\n    pinfo = (\n        parent_bar.parent._create_pane_info(\"tb\" + str(nr), \"Toolbar \" + str(nr))\n        .ToolbarPane()\n        .Top()\n        .LeftDockable(False)\n        .RightDockable(False)\n        .Row(1)\n    )\n    parent_bar.parent._mgr.AddPane(self, pinfo)\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.GenericToolbarPage-functions","title":"Methods:","text":""},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.GenericToolbarPage.create_panel","title":"create_panel","text":"<pre><code>create_panel(title, kind=TYPE_PANEL_TOOLBAR)\n</code></pre> <p>Create a panel within this AUI toolbar page.</p> <p>Returns:</p> Name Type Description <code>GenericToolbarPanel</code> <p>The newly created panel.</p> Source code in <code>pytigon_gui/toolbar/generictoolbar.py</code> <pre><code>def create_panel(self, title, kind=ToolbarPanel.TYPE_PANEL_TOOLBAR):\n    \"\"\"Create a panel within this AUI toolbar page.\n\n    Returns:\n        GenericToolbarPanel: The newly created panel.\n    \"\"\"\n    return GenericToolbarPanel(self, title, kind)\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.GenericToolbarPanel","title":"GenericToolbarPanel","text":"<pre><code>GenericToolbarPanel(parent_page, title, kind=TYPE_PANEL_TOOLBAR)\n</code></pre> <p>               Bases: <code>ToolbarPanel</code></p> <p>A panel within an AUI toolbar page that holds a group of buttons.</p> Source code in <code>pytigon_gui/toolbar/generictoolbar.py</code> <pre><code>def __init__(self, parent_page, title, kind=ToolbarPanel.TYPE_PANEL_TOOLBAR):\n    ToolbarPanel.__init__(self, parent_page, title, kind)\n\n    if len(parent_page.panels) &gt; 0:\n        parent_page.AddStretchSpacer()\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.GenericToolbarPanel-functions","title":"Methods:","text":""},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.GenericToolbarPanel.add_separator","title":"add_separator","text":"<pre><code>add_separator()\n</code></pre> <p>Append a visual separator to this panel.</p> <p>Returns:</p> Name Type Description <code>GenericToolbarButton</code> <p>The separator button object.</p> Source code in <code>pytigon_gui/toolbar/generictoolbar.py</code> <pre><code>def add_separator(self):\n    \"\"\"Append a visual separator to this panel.\n\n    Returns:\n        GenericToolbarButton: The separator button object.\n    \"\"\"\n    b = GenericToolbarButton(\n        self, 0, \"\", None, None, kind=ToolbarButton.TYPE_SEPARATOR\n    )\n    self._append(b)\n    return b\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.GenericToolbarPanel.create_button","title":"create_button","text":"<pre><code>create_button(id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE)\n</code></pre> <p>Create and append a button to this panel.</p> <p>Returns:</p> Name Type Description <code>GenericToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/generictoolbar.py</code> <pre><code>def create_button(\n    self,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=ToolbarButton.TYPE_SIMPLE,\n):\n    \"\"\"Create and append a button to this panel.\n\n    Returns:\n        GenericToolbarButton: The newly created button.\n    \"\"\"\n    b = GenericToolbarButton(self, id, title, bitmap, bitmap_disabled, kind)\n    self._append(b)\n    return b\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.SchAuiToolBarArt","title":"SchAuiToolBarArt","text":"<pre><code>SchAuiToolBarArt()\n</code></pre> <p>               Bases: <code>AuiDefaultToolBarArt</code></p> <p>Custom AUI toolbar art provider that draws a flat background.</p> <p>Uses the system background colour and draws a subtle reflex line at the bottom (horizontal) or right edge (vertical).</p> Source code in <code>pytigon_gui/toolbar/generictoolbar.py</code> <pre><code>def __init__(self):\n    super().__init__()\n    self._base_colour = wx.SystemSettings.GetColour(wx.SYS_COLOUR_BACKGROUND)\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.SchAuiToolBarArt-functions","title":"Methods:","text":""},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.SchAuiToolBarArt.draw_background","title":"draw_background","text":"<pre><code>draw_background(dc, wnd, _rect, horizontal=True)\n</code></pre> <p>Draw the toolbar background with a reflex border line.</p> <p>Parameters:</p> Name Type Description Default <code>dc</code> <p>wx.DC to draw on.</p> required <code>wnd</code> <p>The toolbar window.</p> required <code>_rect</code> <p>Rectangle to fill.</p> required <code>horizontal</code> <p>True for horizontal toolbar, False for vertical.</p> <code>True</code> Source code in <code>pytigon_gui/toolbar/generictoolbar.py</code> <pre><code>def draw_background(self, dc, wnd, _rect, horizontal=True):\n    \"\"\"Draw the toolbar background with a reflex border line.\n\n    Args:\n        dc: wx.DC to draw on.\n        wnd: The toolbar window.\n        _rect: Rectangle to fill.\n        horizontal: True for horizontal toolbar, False for vertical.\n    \"\"\"\n    rect = wx.Rect(*_rect)\n    start_colour = self._base_colour\n    end_colour = self._base_colour\n    reflex_colour = aui.StepColour(self._base_colour, 95)\n    dc.GradientFillLinear(\n        rect, start_colour, end_colour, (horizontal and [wx.SOUTH] or [wx.EAST])[0]\n    )\n    left = rect.GetLeft()\n    right = rect.GetRight()\n    top = rect.GetTop()\n    bottom = rect.GetBottom()\n    dc.SetPen(wx.Pen(reflex_colour))\n    if horizontal:\n        dc.DrawLine(left, bottom, right + 1, bottom)\n    else:\n        dc.DrawLine(right, top, right, bottom + 1)\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarBar","title":"ToolbarBar","text":"<pre><code>ToolbarBar(parent, gui_style)\n</code></pre> <p>Base class for all menus and toolbars connected to the top frame window.</p> <p>Manages a collection of ToolbarPage objects and provides common operations like binding events and creating standard buttons.</p> <p>Initialize the toolbar bar.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window - the top wx.Frame derived object.</p> required <code>gui_style</code> <p>String description of the GUI interface. Based on this string, standard toolbar elements are created.</p> required Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, parent, gui_style):\n    \"\"\"Initialize the toolbar bar.\n\n    Args:\n        parent: Parent window - the top wx.Frame derived object.\n        gui_style: String description of the GUI interface. Based on\n            this string, standard toolbar elements are created.\n    \"\"\"\n    self.parent = parent\n    self.main_page = None\n    self.gui_style = gui_style\n\n    self.toolbars = {}\n    self.pages = []\n    self.user_panels = {}\n\n    self.standard_buttons = StandardButtons(self, gui_style)\n    self.standard_buttons.create_file_panel(self.main_page)\n    self.standard_buttons.create_edit_panel(self.main_page)\n    self.standard_buttons.create_operations_panel(self.main_page)\n    self.standard_buttons.create_browse_panel(self.main_page)\n    self.standard_buttons.create_address_panel(self.main_page)\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarBar-functions","title":"Methods:","text":""},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarBar.append","title":"append","text":"<pre><code>append(title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>Append a page to the toolbar, or return an existing one.</p> <p>If a page with the same name and kind already exists, it is returned instead of creating a duplicate.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Page title.</p> required <code>kind</code> <p>Type of page; one of ToolbarPage.TYPE_PAGE_* constants.</p> <code>TYPE_PAGE_NORMAL</code> <p>Returns:</p> Name Type Description <code>ToolbarPage</code> <p>The existing or newly created page.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def append(self, title, kind=ToolbarPage.TYPE_PAGE_NORMAL):\n    \"\"\"Append a page to the toolbar, or return an existing one.\n\n    If a page with the same name and kind already exists, it is\n    returned instead of creating a duplicate.\n\n    Args:\n        title: Page title.\n        kind: Type of page; one of ToolbarPage.TYPE_PAGE_* constants.\n\n    Returns:\n        ToolbarPage: The existing or newly created page.\n    \"\"\"\n    for page in self.pages:\n        if page.name == title and page.kind == kind:\n            return page\n    p = self.create_page(title, kind)\n    if not self.main_page:\n        self.main_page = p\n    self.pages.append(p)\n    return p\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarBar.bind","title":"bind","text":"<pre><code>bind(fun, id=ID_ANY, e=None)\n</code></pre> <p>Bind an event handler to the toolbar's parent window.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <p>Handler function.</p> required <code>id</code> <p>Identifier of the event.</p> <code>ID_ANY</code> <code>e</code> <p>Event type; defaults to wx.EVT_MENU if not specified.</p> <code>None</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def bind(self, fun, id=wx.ID_ANY, e=None):\n    \"\"\"Bind an event handler to the toolbar's parent window.\n\n    Args:\n        fun: Handler function.\n        id: Identifier of the event.\n        e: Event type; defaults to wx.EVT_MENU if not specified.\n    \"\"\"\n    if e:\n        self.parent.Bind(e, fun, id=id)\n    else:\n        self.parent.Bind(wx.EVT_MENU, fun, id=id)\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarBar.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Close/destroy the toolbar.</p> <p>Override in subclasses to perform cleanup.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def close(self):\n    \"\"\"Close/destroy the toolbar.\n\n    Override in subclasses to perform cleanup.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarBar.create","title":"create","text":"<pre><code>create()\n</code></pre> <p>Create/realize the toolbar.</p> <p>Override in subclasses to perform actual widget creation.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create(self):\n    \"\"\"Create/realize the toolbar.\n\n    Override in subclasses to perform actual widget creation.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarBar.create_html_win","title":"create_html_win","text":"<pre><code>create_html_win(toolbar_page, address_or_parser, parameters, callback=None)\n</code></pre> <p>Create an HTML page embedded in the toolbar.</p> <p>Not all toolbar types support this feature.</p> <p>Parameters:</p> Name Type Description Default <code>toolbar_page</code> <p>Name of the toolbar page to host the HTML. Use '__' to separate page and panel names, e.g. 'PageName__PanelName'.</p> required <code>address_or_parser</code> <p>Address of the HTTP page or a parser.</p> required <code>parameters</code> <p>Parameters for the HTTP request.</p> required <code>callback</code> <p>Optional callback invoked after initialization.</p> <code>None</code> <p>Returns:</p> Type Description <p>SchPage or None: The created page, or None on failure.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_html_win(\n    self, toolbar_page, address_or_parser, parameters, callback=None\n):\n    \"\"\"Create an HTML page embedded in the toolbar.\n\n    Not all toolbar types support this feature.\n\n    Args:\n        toolbar_page: Name of the toolbar page to host the HTML.\n            Use '__' to separate page and panel names,\n            e.g. 'PageName__PanelName'.\n        address_or_parser: Address of the HTTP page or a parser.\n        parameters: Parameters for the HTTP request.\n        callback: Optional callback invoked after initialization.\n\n    Returns:\n        SchPage or None: The created page, or None on failure.\n    \"\"\"\n    page = None\n    if not toolbar_page:\n        u_name = \"main tools\"\n        page_name = _(\"main tools\")\n        panel_name = \"main tools\"\n    else:\n        u_name = toolbar_page\n        names = toolbar_page.split(\"__\")\n        if len(names) &gt; 1:\n            page_name = names[0].replace(\"_\", \" \")\n            panel_name = names[1].replace(\"_\", \" \")\n        else:\n            page_name = toolbar_page.replace(\"_\", \" \")\n            panel_name = page_name\n\n    if u_name in self.user_panels:\n        panel = self.user_panels[u_name]\n    else:\n        page = self._find_page_by_title(page_name)\n        if page is None:\n            page = self.append(page_name)\n        panel = page.create_html_panel(panel_name)\n\n    if panel:\n        dx = panel.get_width() + 3\n        dy = panel.get_height() + 5\n\n        page2 = SchPage(\n            panel.get_window(),\n            address_or_parser,\n            parameters,\n            size=wx.Size(dx, dy),\n            pos=wx.Point(2, 2),\n        )\n        best = page2.body.calculate_best_size()\n        page2.SetSize(wx.Size(int(best[0]), int(best[1])))\n        panel.set_page(page2)\n        self.user_panels[u_name] = panel\n        page2.body.toolbar_interface = self\n        page2.body.toolbar_interface_page = page\n\n        def init_page(callback):\n            page2.init_frame()\n            page2.activate_page()\n            page2.Update()\n\n        wx.CallAfter(init_page, callback)\n\n        return page2\n\n    return None\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarBar.create_page","title":"create_page","text":"<pre><code>create_page(title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>Create and return a new ToolbarPage.</p> <p>Override in derived classes to return a subclass of ToolbarPage.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Page title.</p> required <code>kind</code> <p>Type of page; one of ToolbarPage.TYPE_PAGE_* constants.</p> <code>TYPE_PAGE_NORMAL</code> <p>Returns:</p> Name Type Description <code>ToolbarPage</code> <p>The newly created page.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_page(self, title, kind=ToolbarPage.TYPE_PAGE_NORMAL):\n    \"\"\"Create and return a new ToolbarPage.\n\n    Override in derived classes to return a subclass of ToolbarPage.\n\n    Args:\n        title: Page title.\n        kind: Type of page; one of ToolbarPage.TYPE_PAGE_* constants.\n\n    Returns:\n        ToolbarPage: The newly created page.\n    \"\"\"\n    return ToolbarPage(self, title, kind)\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarBar.create_panel_in_main_page","title":"create_panel_in_main_page","text":"<pre><code>create_panel_in_main_page(title, kind)\n</code></pre> <p>Create a panel in the main page.</p> <p>If no main page exists yet, one is created automatically.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>New panel's title.</p> required <code>kind</code> <p>Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.</p> required <p>Returns:</p> Name Type Description <code>ToolbarPanel</code> <p>The newly created panel.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_panel_in_main_page(self, title, kind):\n    \"\"\"Create a panel in the main page.\n\n    If no main page exists yet, one is created automatically.\n\n    Args:\n        title: New panel's title.\n        kind: Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.\n\n    Returns:\n        ToolbarPanel: The newly created panel.\n    \"\"\"\n    if not self.main_page:\n        self.append(\"main tools\")\n    return self.main_page.create_panel(title, kind)\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarBar.new_child_page","title":"new_child_page","text":"<pre><code>new_child_page(address_or_parser, title='', param=None, callback=None)\n</code></pre> <p>Create a new child page.</p> <p>For toolbar, the child page is transferred to the desktop window.</p> <p>Parameters:</p> Name Type Description Default <code>address_or_parser</code> <p>Address of the HTTP page or a parser.</p> required <code>title</code> <p>Page title.</p> <code>''</code> <code>param</code> <p>Optional parameters.</p> <code>None</code> <code>callback</code> <p>Optional callback.</p> <code>None</code> <p>Returns:</p> Type Description <p>The result of new_main_page on the top window.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def new_child_page(self, address_or_parser, title=\"\", param=None, callback=None):\n    \"\"\"Create a new child page.\n\n    For toolbar, the child page is transferred to the desktop window.\n\n    Args:\n        address_or_parser: Address of the HTTP page or a parser.\n        title: Page title.\n        param: Optional parameters.\n        callback: Optional callback.\n\n    Returns:\n        The result of new_main_page on the top window.\n    \"\"\"\n    return wx.GetApp().GetTopWindow().new_main_page(address_or_parser, title, param)\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarBar.remove_page","title":"remove_page","text":"<pre><code>remove_page(title)\n</code></pre> <p>Remove the page with the specified title from the toolbar.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Title of the page to remove.</p> required Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def remove_page(self, title):\n    \"\"\"Remove the page with the specified title from the toolbar.\n\n    Args:\n        title: Title of the page to remove.\n    \"\"\"\n    # Remove from pages list\n    for page in self.pages:\n        if page.title == title:\n            self.pages.remove(page)\n            break\n\n    # If the main page was removed, pick a new one\n    if self.main_page and self.main_page.title == title:\n        self.main_page = self.pages[0] if self.pages else None\n\n    # Remove associated user panels (iterate over a static list of keys\n    # to avoid \"dictionary changed size during iteration\" errors)\n    for key in list(self.user_panels.keys()):\n        panel = self.user_panels[key]\n        if panel.page.title == title:\n            del self.user_panels[key]\n            return\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarBar.un_bind","title":"un_bind","text":"<pre><code>un_bind(id=ID_ANY, e=None)\n</code></pre> <p>Unbind an event handler from the toolbar's parent window.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>Identifier of the event.</p> <code>ID_ANY</code> <code>e</code> <p>Event type; defaults to wx.EVT_MENU if not specified.</p> <code>None</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def un_bind(self, id=wx.ID_ANY, e=None):\n    \"\"\"Unbind an event handler from the toolbar's parent window.\n\n    Args:\n        id: Identifier of the event.\n        e: Event type; defaults to wx.EVT_MENU if not specified.\n    \"\"\"\n    if e:\n        self.parent.Unbind(e, id=id)\n    else:\n        self.parent.Unbind(wx.EVT_MENU, id=id)\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarButton","title":"ToolbarButton","text":"<pre><code>ToolbarButton(\n    parent_panel, id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE\n)\n</code></pre> <p>Represents a single button (or separator) on a toolbar panel.</p> <p>This is an abstract data model; the actual wx widget is created by the concrete toolbar implementation.</p> Button type constants <p>TYPE_SIMPLE = 0     - Simple push button. TYPE_DROPDOWN = 1   - Button with a dropdown menu. TYPE_HYBRID = 2     - Hybrid button (part button, part dropdown). TYPE_TOOGLE = 3     - Toggle button. TYPE_PANEL = 4      - Embedded panel. TYPE_SEPARATOR = 5  - Visual separator.</p> <p>Create a new toolbar button.</p> <p>Parameters:</p> Name Type Description Default <code>parent_panel</code> <p>Parent ToolbarPanel object.</p> required <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title (display text).</p> required <code>bitmap</code> <p>Button bitmap for the normal state.</p> <code>None</code> <code>bitmap_disabled</code> <p>Button bitmap for the disabled state. Defaults to wx.NullBitmap when bitmap is provided.</p> <code>None</code> <code>kind</code> <p>Type of button; one of the TYPE_* class constants.</p> <code>TYPE_SIMPLE</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(\n    self,\n    parent_panel,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=TYPE_SIMPLE,\n):\n    \"\"\"Create a new toolbar button.\n\n    Args:\n        parent_panel: Parent ToolbarPanel object.\n        id: An integer by which the tool may be identified.\n        title: Button title (display text).\n        bitmap: Button bitmap for the normal state.\n        bitmap_disabled: Button bitmap for the disabled state.\n            Defaults to wx.NullBitmap when bitmap is provided.\n        kind: Type of button; one of the TYPE_* class constants.\n    \"\"\"\n    self.parent_panel = parent_panel\n    self.id = id\n    self.title = title\n    self.bitmap = bitmap\n    if bitmap_disabled is None and bitmap is not None:\n        self.bitmap_disabled = wx.NullBitmap\n    else:\n        self.bitmap_disabled = bitmap_disabled\n    self.kind = kind\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarButton-functions","title":"Methods:","text":""},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarPage","title":"ToolbarPage","text":"<pre><code>ToolbarPage(parent_bar, title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>A page (tab) within a toolbar bar, containing one or more panels.</p> Page type constants <p>TYPE_PAGE_NORMAL = 0  - Standard page.</p> <p>Create a new toolbar page.</p> <p>Parameters:</p> Name Type Description Default <code>parent_bar</code> <p>Parent ToolbarBar object.</p> required <code>title</code> <p>Page title (will be translated via wx.GetTranslation).</p> required <code>kind</code> <p>Type of page; one of the TYPE_PAGE_* class constants.</p> <code>TYPE_PAGE_NORMAL</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, parent_bar, title, kind=TYPE_PAGE_NORMAL):\n    \"\"\"Create a new toolbar page.\n\n    Args:\n        parent_bar: Parent ToolbarBar object.\n        title: Page title (will be translated via wx.GetTranslation).\n        kind: Type of page; one of the TYPE_PAGE_* class constants.\n    \"\"\"\n    self.parent_bar = parent_bar\n    self.name = title\n    self.title = _(title)\n    self.kind = kind\n    self.panels = []\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarPage-functions","title":"Methods:","text":""},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarPage.append","title":"append","text":"<pre><code>append(title, kind=TYPE_PANEL_BUTTONBAR)\n</code></pre> <p>Create a new panel and append it to this page.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Panel title.</p> required <code>kind</code> <p>Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.</p> <code>TYPE_PANEL_BUTTONBAR</code> <p>Returns:</p> Name Type Description <code>ToolbarPanel</code> <p>The newly created panel.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def append(self, title, kind=ToolbarPanel.TYPE_PANEL_BUTTONBAR):\n    \"\"\"Create a new panel and append it to this page.\n\n    Args:\n        title: Panel title.\n        kind: Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.\n\n    Returns:\n        ToolbarPanel: The newly created panel.\n    \"\"\"\n    p = self.create_panel(title, kind)\n    self.panels.append(p)\n    return p\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarPage.create_html_panel","title":"create_html_panel","text":"<pre><code>create_html_panel(title)\n</code></pre> <p>Create an HTML-capable panel within this page.</p> <p>Override in derived classes to return a BaseHtmlPanel subclass.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Panel title.</p> required <p>Returns:</p> Type Description <p>BaseHtmlPanel or None: The panel, or None if not supported.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_html_panel(self, title):\n    \"\"\"Create an HTML-capable panel within this page.\n\n    Override in derived classes to return a BaseHtmlPanel subclass.\n\n    Args:\n        title: Panel title.\n\n    Returns:\n        BaseHtmlPanel or None: The panel, or None if not supported.\n    \"\"\"\n    return None\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarPage.create_panel","title":"create_panel","text":"<pre><code>create_panel(title, kind)\n</code></pre> <p>Create and return a new ToolbarPanel for this page.</p> <p>Override in derived classes to return a subclass of ToolbarPanel.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Panel title.</p> required <code>kind</code> <p>Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.</p> required <p>Returns:</p> Name Type Description <code>ToolbarPanel</code> <p>The newly created panel.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_panel(self, title, kind):\n    \"\"\"Create and return a new ToolbarPanel for this page.\n\n    Override in derived classes to return a subclass of ToolbarPanel.\n\n    Args:\n        title: Panel title.\n        kind: Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.\n\n    Returns:\n        ToolbarPanel: The newly created panel.\n    \"\"\"\n    return ToolbarPanel(self, title, kind=ToolbarPanel.TYPE_PANEL_BUTTONBAR)\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarPanel","title":"ToolbarPanel","text":"<pre><code>ToolbarPanel(parent_page, title, kind=TYPE_PANEL_BUTTONBAR)\n</code></pre> <p>A panel within a toolbar page that holds a group of buttons.</p> Panel type constants <p>TYPE_PANEL_BUTTONBAR = 0  - Button bar layout. TYPE_PANEL_TOOLBAR = 1    - Toolbar layout. TYPE_PANEL_PANELBAR = 2   - Custom panel layout.</p> <p>Create a new toolbar panel.</p> <p>Parameters:</p> Name Type Description Default <code>parent_page</code> <p>Parent ToolbarPage object.</p> required <code>title</code> <p>Panel title.</p> required <code>kind</code> <p>Type of panel; one of the TYPE_PANEL_* class constants.</p> <code>TYPE_PANEL_BUTTONBAR</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, parent_page, title, kind=TYPE_PANEL_BUTTONBAR):\n    \"\"\"Create a new toolbar panel.\n\n    Args:\n        parent_page: Parent ToolbarPage object.\n        title: Panel title.\n        kind: Type of panel; one of the TYPE_PANEL_* class constants.\n    \"\"\"\n    self.parent_page = parent_page\n    self.title = title\n    self.kind = kind\n    self.buttons = []\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarPanel-functions","title":"Methods:","text":""},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarPanel.add_dropdown_tool","title":"add_dropdown_tool","text":"<pre><code>add_dropdown_tool(id, title, bitmaps)\n</code></pre> <p>Append a dropdown button to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_dropdown_tool(self, id, title, bitmaps):\n    \"\"\"Append a dropdown button to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_DROPDOWN)\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarPanel.add_hybrid_tool","title":"add_hybrid_tool","text":"<pre><code>add_hybrid_tool(id, title, bitmaps)\n</code></pre> <p>Append a hybrid button (button + dropdown) to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_hybrid_tool(self, id, title, bitmaps):\n    \"\"\"Append a hybrid button (button + dropdown) to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_HYBRID)\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarPanel.add_separator","title":"add_separator","text":"<pre><code>add_separator()\n</code></pre> <p>Append a visual separator to the panel.</p> <p>The default implementation does nothing; override in subclasses.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_separator(self):\n    \"\"\"Append a visual separator to the panel.\n\n    The default implementation does nothing; override in subclasses.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarPanel.add_simple_tool","title":"add_simple_tool","text":"<pre><code>add_simple_tool(id, title, bitmaps)\n</code></pre> <p>Append a simple push button to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_simple_tool(self, id, title, bitmaps):\n    \"\"\"Append a simple push button to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_SIMPLE)\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarPanel.add_toogle_tool","title":"add_toogle_tool","text":"<pre><code>add_toogle_tool(id, title, bitmaps)\n</code></pre> <p>Append a toggle button to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_toogle_tool(self, id, title, bitmaps):\n    \"\"\"Append a toggle button to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_TOOGLE)\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarPanel.append","title":"append","text":"<pre><code>append(id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE)\n</code></pre> <p>Create a button and append it to this panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmap</code> <p>Button bitmap for the normal state.</p> <code>None</code> <code>bitmap_disabled</code> <p>Button bitmap for the disabled state.</p> <code>None</code> <code>kind</code> <p>Type of button; one of ToolbarButton.TYPE_* constants.</p> <code>TYPE_SIMPLE</code> <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created and appended button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def append(\n    self,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=ToolbarButton.TYPE_SIMPLE,\n):\n    \"\"\"Create a button and append it to this panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmap: Button bitmap for the normal state.\n        bitmap_disabled: Button bitmap for the disabled state.\n        kind: Type of button; one of ToolbarButton.TYPE_* constants.\n\n    Returns:\n        ToolbarButton: The newly created and appended button.\n    \"\"\"\n    button = self.create_button(id, title, bitmap, bitmap_disabled, kind)\n    self.buttons.append(button)\n    return button\n</code></pre>"},{"location":"api/toolbar_generictoolbar/#pytigon_gui.toolbar.generictoolbar.ToolbarPanel.create_button","title":"create_button","text":"<pre><code>create_button(id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE)\n</code></pre> <p>Create and return a new ToolbarButton.</p> <p>Override in derived classes to return a subclass of ToolbarButton.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmap</code> <p>Button bitmap for the normal state.</p> <code>None</code> <code>bitmap_disabled</code> <p>Button bitmap for the disabled state.</p> <code>None</code> <code>kind</code> <p>Type of button; one of ToolbarButton.TYPE_* constants.</p> <code>TYPE_SIMPLE</code> <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_button(\n    self,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=ToolbarButton.TYPE_SIMPLE,\n):\n    \"\"\"Create and return a new ToolbarButton.\n\n    Override in derived classes to return a subclass of ToolbarButton.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmap: Button bitmap for the normal state.\n        bitmap_disabled: Button bitmap for the disabled state.\n        kind: Type of button; one of ToolbarButton.TYPE_* constants.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    return ToolbarButton(self, id, title, bitmap, bitmap_disabled, kind)\n</code></pre>"},{"location":"api/toolbar_menubar/","title":"toolbar.menubar \u2013 Menu Bar","text":"<p>Application menu bar with standard File, Edit, View, and Help menus plus project-specific menu extensions.</p>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar","title":"menubar","text":"<p>Menu bar toolbar implementation.</p> <p>Provides a standard wx.MenuBar-based toolbar using the abstract ToolbarBar/ToolbarPage/ToolbarPanel hierarchy.</p>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar-attributes","title":"Attributes","text":""},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_BOOKMARK_ADD","title":"ID_BOOKMARK_ADD  <code>module-attribute</code>","text":"<pre><code>ID_BOOKMARK_ADD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_CLEAR_HISTORY","title":"ID_CLEAR_HISTORY  <code>module-attribute</code>","text":"<pre><code>ID_CLEAR_HISTORY = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_CLOSETAB","title":"ID_CLOSETAB  <code>module-attribute</code>","text":"<pre><code>ID_CLOSETAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_COPY","title":"ID_COPY  <code>module-attribute</code>","text":"<pre><code>ID_COPY = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_CUT","title":"ID_CUT  <code>module-attribute</code>","text":"<pre><code>ID_CUT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_DOWNLOAD_OPTIONS","title":"ID_DOWNLOAD_OPTIONS  <code>module-attribute</code>","text":"<pre><code>ID_DOWNLOAD_OPTIONS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_END","title":"ID_END  <code>module-attribute</code>","text":"<pre><code>ID_END = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_EXIT","title":"ID_EXIT  <code>module-attribute</code>","text":"<pre><code>ID_EXIT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_FIND","title":"ID_FIND  <code>module-attribute</code>","text":"<pre><code>ID_FIND = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_GENERAL_OPTIONS","title":"ID_GENERAL_OPTIONS  <code>module-attribute</code>","text":"<pre><code>ID_GENERAL_OPTIONS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_GOTODESKTOP","title":"ID_GOTODESKTOP  <code>module-attribute</code>","text":"<pre><code>ID_GOTODESKTOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_GOTOFOOTER","title":"ID_GOTOFOOTER  <code>module-attribute</code>","text":"<pre><code>ID_GOTOFOOTER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_GOTOHEADER","title":"ID_GOTOHEADER  <code>module-attribute</code>","text":"<pre><code>ID_GOTOHEADER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_GOTOPANEL","title":"ID_GOTOPANEL  <code>module-attribute</code>","text":"<pre><code>ID_GOTOPANEL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_HELP","title":"ID_HELP  <code>module-attribute</code>","text":"<pre><code>ID_HELP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_HISTORY_SHOW","title":"ID_HISTORY_SHOW  <code>module-attribute</code>","text":"<pre><code>ID_HISTORY_SHOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_LOAD","title":"ID_LOAD  <code>module-attribute</code>","text":"<pre><code>ID_LOAD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_NEXTPAGE","title":"ID_NEXTPAGE  <code>module-attribute</code>","text":"<pre><code>ID_NEXTPAGE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_NEXTTAB","title":"ID_NEXTTAB  <code>module-attribute</code>","text":"<pre><code>ID_NEXTTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_PAGE_BLOCK","title":"ID_PAGE_BLOCK  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_BLOCK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_PAGE_SOURCE","title":"ID_PAGE_SOURCE  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_SOURCE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_PAGE_VIEW","title":"ID_PAGE_VIEW  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_VIEW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_PASTE","title":"ID_PASTE  <code>module-attribute</code>","text":"<pre><code>ID_PASTE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_PREVPAGE","title":"ID_PREVPAGE  <code>module-attribute</code>","text":"<pre><code>ID_PREVPAGE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_PREVTAB","title":"ID_PREVTAB  <code>module-attribute</code>","text":"<pre><code>ID_PREVTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_PRINT","title":"ID_PRINT  <code>module-attribute</code>","text":"<pre><code>ID_PRINT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_PRINT_PREVIEW","title":"ID_PRINT_PREVIEW  <code>module-attribute</code>","text":"<pre><code>ID_PRINT_PREVIEW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_PRINT_SETTINGS","title":"ID_PRINT_SETTINGS  <code>module-attribute</code>","text":"<pre><code>ID_PRINT_SETTINGS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_REDO","title":"ID_REDO  <code>module-attribute</code>","text":"<pre><code>ID_REDO = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_REFRESHTAB","title":"ID_REFRESHTAB  <code>module-attribute</code>","text":"<pre><code>ID_REFRESHTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_REPLACE","title":"ID_REPLACE  <code>module-attribute</code>","text":"<pre><code>ID_REPLACE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_RESET","title":"ID_RESET  <code>module-attribute</code>","text":"<pre><code>ID_RESET = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_RETVALUE","title":"ID_RETVALUE  <code>module-attribute</code>","text":"<pre><code>ID_RETVALUE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_SAVE","title":"ID_SAVE  <code>module-attribute</code>","text":"<pre><code>ID_SAVE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_SAVE_AS","title":"ID_SAVE_AS  <code>module-attribute</code>","text":"<pre><code>ID_SAVE_AS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_SELALL","title":"ID_SELALL  <code>module-attribute</code>","text":"<pre><code>ID_SELALL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_SHOWDESKTOP","title":"ID_SHOWDESKTOP  <code>module-attribute</code>","text":"<pre><code>ID_SHOWDESKTOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_SHOWFOOTER","title":"ID_SHOWFOOTER  <code>module-attribute</code>","text":"<pre><code>ID_SHOWFOOTER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_SHOWHEADER","title":"ID_SHOWHEADER  <code>module-attribute</code>","text":"<pre><code>ID_SHOWHEADER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_SHOWPANEL","title":"ID_SHOWPANEL  <code>module-attribute</code>","text":"<pre><code>ID_SHOWPANEL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_SHOWSTATUSBAR","title":"ID_SHOWSTATUSBAR  <code>module-attribute</code>","text":"<pre><code>ID_SHOWSTATUSBAR = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_SHOWTOOLBAR1","title":"ID_SHOWTOOLBAR1  <code>module-attribute</code>","text":"<pre><code>ID_SHOWTOOLBAR1 = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_SHOWTOOLBAR2","title":"ID_SHOWTOOLBAR2  <code>module-attribute</code>","text":"<pre><code>ID_SHOWTOOLBAR2 = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_SHOW_BOOKMARKS","title":"ID_SHOW_BOOKMARKS  <code>module-attribute</code>","text":"<pre><code>ID_SHOW_BOOKMARKS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_SHOW_DOWNLOAD","title":"ID_SHOW_DOWNLOAD  <code>module-attribute</code>","text":"<pre><code>ID_SHOW_DOWNLOAD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_START","title":"ID_START  <code>module-attribute</code>","text":"<pre><code>ID_START = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_TASKBAR_CLOSE","title":"ID_TASKBAR_CLOSE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_CLOSE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_TASKBAR_HIDE","title":"ID_TASKBAR_HIDE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_HIDE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_TASKBAR_SHOW","title":"ID_TASKBAR_SHOW  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_SHOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_TASKBAR_TOGGLE","title":"ID_TASKBAR_TOGGLE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_TOGGLE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_UNDO","title":"ID_UNDO  <code>module-attribute</code>","text":"<pre><code>ID_UNDO = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_WEB_ADDBOOKMARK","title":"ID_WEB_ADDBOOKMARK  <code>module-attribute</code>","text":"<pre><code>ID_WEB_ADDBOOKMARK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_WEB_BACK","title":"ID_WEB_BACK  <code>module-attribute</code>","text":"<pre><code>ID_WEB_BACK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_WEB_DOWN","title":"ID_WEB_DOWN  <code>module-attribute</code>","text":"<pre><code>ID_WEB_DOWN = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_WEB_EDIT","title":"ID_WEB_EDIT  <code>module-attribute</code>","text":"<pre><code>ID_WEB_EDIT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_WEB_FORWARD","title":"ID_WEB_FORWARD  <code>module-attribute</code>","text":"<pre><code>ID_WEB_FORWARD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_WEB_NEW_WINDOW","title":"ID_WEB_NEW_WINDOW  <code>module-attribute</code>","text":"<pre><code>ID_WEB_NEW_WINDOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_WEB_REFRESH","title":"ID_WEB_REFRESH  <code>module-attribute</code>","text":"<pre><code>ID_WEB_REFRESH = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_WEB_SOURCE","title":"ID_WEB_SOURCE  <code>module-attribute</code>","text":"<pre><code>ID_WEB_SOURCE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_WEB_STOP","title":"ID_WEB_STOP  <code>module-attribute</code>","text":"<pre><code>ID_WEB_STOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_WEB_UP","title":"ID_WEB_UP  <code>module-attribute</code>","text":"<pre><code>ID_WEB_UP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ID_ZOOM","title":"ID_ZOOM  <code>module-attribute</code>","text":"<pre><code>ID_ZOOM = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar-classes","title":"Classes","text":""},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.MenuToolbarBar","title":"MenuToolbarBar","text":"<pre><code>MenuToolbarBar(parent, gui_style)\n</code></pre> <p>               Bases: <code>ToolbarBar</code>, <code>MenuBar</code></p> <p>The top-level menu bar, combining ToolbarBar and wx.MenuBar.</p> <p>Initialize the menu bar.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent wx.Frame.</p> required <code>gui_style</code> <p>GUI style string for standard button creation.</p> required Source code in <code>pytigon_gui/toolbar/menubar.py</code> <pre><code>def __init__(self, parent, gui_style):\n    \"\"\"Initialize the menu bar.\n\n    Args:\n        parent: Parent wx.Frame.\n        gui_style: GUI style string for standard button creation.\n    \"\"\"\n    wx.MenuBar.__init__(self)\n    ToolbarBar.__init__(self, parent, gui_style)\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.MenuToolbarBar-functions","title":"Methods:","text":""},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.MenuToolbarBar.create_page","title":"create_page","text":"<pre><code>create_page(title, kind)\n</code></pre> <p>Create a new top-level menu page.</p> <p>Returns:</p> Name Type Description <code>MenuToolbarPage</code> <p>The newly created page.</p> Source code in <code>pytigon_gui/toolbar/menubar.py</code> <pre><code>def create_page(self, title, kind):\n    \"\"\"Create a new top-level menu page.\n\n    Returns:\n        MenuToolbarPage: The newly created page.\n    \"\"\"\n    return MenuToolbarPage(self, title, kind)\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.MenuToolbarBar.update_bar","title":"update_bar","text":"<pre><code>update_bar(obj)\n</code></pre> <p>Apply this menu bar to the given frame/window.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <p>The wx.Frame to attach the menu bar to.</p> required Source code in <code>pytigon_gui/toolbar/menubar.py</code> <pre><code>def update_bar(self, obj):\n    \"\"\"Apply this menu bar to the given frame/window.\n\n    Args:\n        obj: The wx.Frame to attach the menu bar to.\n    \"\"\"\n    obj.SetMenuBar(self)\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.MenuToolbarButton","title":"MenuToolbarButton","text":"<pre><code>MenuToolbarButton(\n    parent_panel, id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE\n)\n</code></pre> <p>               Bases: <code>ToolbarButton</code>, <code>MenuItem</code></p> <p>A toolbar button that wraps a wx.MenuItem.</p> <p>Initialize the menu item button.</p> <p>Parameters:</p> Name Type Description Default <code>parent_panel</code> <p>Parent wx.Menu (acting as the panel).</p> required <code>id</code> <p>Menu item identifier.</p> required <code>title</code> <p>Menu item text.</p> required <code>bitmap</code> <p>Optional bitmap for the menu item.</p> <code>None</code> <code>bitmap_disabled</code> <p>Ignored for menu items.</p> <code>None</code> <code>kind</code> <p>Button type (ignored for menu items).</p> <code>TYPE_SIMPLE</code> Source code in <code>pytigon_gui/toolbar/menubar.py</code> <pre><code>def __init__(\n    self,\n    parent_panel,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=ToolbarButton.TYPE_SIMPLE,\n):\n    \"\"\"Initialize the menu item button.\n\n    Args:\n        parent_panel: Parent wx.Menu (acting as the panel).\n        id: Menu item identifier.\n        title: Menu item text.\n        bitmap: Optional bitmap for the menu item.\n        bitmap_disabled: Ignored for menu items.\n        kind: Button type (ignored for menu items).\n    \"\"\"\n    wx.MenuItem.__init__(self, parentMenu=parent_panel, id=id, text=title)\n    if bitmap is not None and bitmap.IsOk():\n        self.SetBitmap(bitmap)\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.MenuToolbarButton-functions","title":"Methods:","text":""},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.MenuToolbarPage","title":"MenuToolbarPage","text":"<pre><code>MenuToolbarPage(parent_bar, title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>               Bases: <code>ToolbarPage</code></p> <p>A toolbar page for the menu bar (essentially a top-level menu).</p> <p>Initialize the menu page.</p> <p>Parameters:</p> Name Type Description Default <code>parent_bar</code> <p>Parent MenuToolbarBar.</p> required <code>title</code> <p>Menu title.</p> required <code>kind</code> <p>Page type (defaults to TYPE_PAGE_NORMAL).</p> <code>TYPE_PAGE_NORMAL</code> Source code in <code>pytigon_gui/toolbar/menubar.py</code> <pre><code>def __init__(self, parent_bar, title, kind=ToolbarPage.TYPE_PAGE_NORMAL):\n    \"\"\"Initialize the menu page.\n\n    Args:\n        parent_bar: Parent MenuToolbarBar.\n        title: Menu title.\n        kind: Page type (defaults to TYPE_PAGE_NORMAL).\n    \"\"\"\n    ToolbarPage.__init__(self, parent_bar, title, kind)\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.MenuToolbarPage-functions","title":"Methods:","text":""},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.MenuToolbarPage.create_panel","title":"create_panel","text":"<pre><code>create_panel(title, kind=TYPE_PANEL_TOOLBAR)\n</code></pre> <p>Create a submenu panel within this menu page.</p> <p>Returns:</p> Name Type Description <code>MenuToolbarPanel</code> <p>The newly created menu panel.</p> Source code in <code>pytigon_gui/toolbar/menubar.py</code> <pre><code>def create_panel(self, title, kind=ToolbarPanel.TYPE_PANEL_TOOLBAR):\n    \"\"\"Create a submenu panel within this menu page.\n\n    Returns:\n        MenuToolbarPanel: The newly created menu panel.\n    \"\"\"\n    return MenuToolbarPanel(self, title, kind)\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.MenuToolbarPanel","title":"MenuToolbarPanel","text":"<pre><code>MenuToolbarPanel(parent_page, title, kind=TYPE_PANEL_TOOLBAR)\n</code></pre> <p>               Bases: <code>ToolbarPanel</code>, <code>Menu</code></p> <p>A toolbar panel that wraps a wx.Menu (dropdown or submenu).</p> <p>Initialize the menu panel.</p> <p>Parameters:</p> Name Type Description Default <code>parent_page</code> <p>Parent MenuToolbarPage.</p> required <code>title</code> <p>Menu title.</p> required <code>kind</code> <p>Panel type (defaults to TYPE_PANEL_TOOLBAR).</p> <code>TYPE_PANEL_TOOLBAR</code> Source code in <code>pytigon_gui/toolbar/menubar.py</code> <pre><code>def __init__(self, parent_page, title, kind=ToolbarPanel.TYPE_PANEL_TOOLBAR):\n    \"\"\"Initialize the menu panel.\n\n    Args:\n        parent_page: Parent MenuToolbarPage.\n        title: Menu title.\n        kind: Panel type (defaults to TYPE_PANEL_TOOLBAR).\n    \"\"\"\n    wx.Menu.__init__(self)\n    ToolbarPanel.__init__(self, parent_page, title, kind)\n    self.parent_page.parent_bar.Append(self, title)\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.MenuToolbarPanel-functions","title":"Methods:","text":""},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.MenuToolbarPanel.create_button","title":"create_button","text":"<pre><code>create_button(id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE)\n</code></pre> <p>Create and append a menu item button to this menu.</p> <p>Returns:</p> Name Type Description <code>MenuToolbarButton</code> <p>The newly created menu item.</p> Source code in <code>pytigon_gui/toolbar/menubar.py</code> <pre><code>def create_button(\n    self,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=ToolbarButton.TYPE_SIMPLE,\n):\n    \"\"\"Create and append a menu item button to this menu.\n\n    Returns:\n        MenuToolbarButton: The newly created menu item.\n    \"\"\"\n    b = MenuToolbarButton(self, id, title, bitmap, bitmap_disabled, kind)\n    self.Append(b)\n    return b\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarBar","title":"ToolbarBar","text":"<pre><code>ToolbarBar(parent, gui_style)\n</code></pre> <p>Base class for all menus and toolbars connected to the top frame window.</p> <p>Manages a collection of ToolbarPage objects and provides common operations like binding events and creating standard buttons.</p> <p>Initialize the toolbar bar.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window - the top wx.Frame derived object.</p> required <code>gui_style</code> <p>String description of the GUI interface. Based on this string, standard toolbar elements are created.</p> required Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, parent, gui_style):\n    \"\"\"Initialize the toolbar bar.\n\n    Args:\n        parent: Parent window - the top wx.Frame derived object.\n        gui_style: String description of the GUI interface. Based on\n            this string, standard toolbar elements are created.\n    \"\"\"\n    self.parent = parent\n    self.main_page = None\n    self.gui_style = gui_style\n\n    self.toolbars = {}\n    self.pages = []\n    self.user_panels = {}\n\n    self.standard_buttons = StandardButtons(self, gui_style)\n    self.standard_buttons.create_file_panel(self.main_page)\n    self.standard_buttons.create_edit_panel(self.main_page)\n    self.standard_buttons.create_operations_panel(self.main_page)\n    self.standard_buttons.create_browse_panel(self.main_page)\n    self.standard_buttons.create_address_panel(self.main_page)\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarBar-functions","title":"Methods:","text":""},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarBar.append","title":"append","text":"<pre><code>append(title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>Append a page to the toolbar, or return an existing one.</p> <p>If a page with the same name and kind already exists, it is returned instead of creating a duplicate.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Page title.</p> required <code>kind</code> <p>Type of page; one of ToolbarPage.TYPE_PAGE_* constants.</p> <code>TYPE_PAGE_NORMAL</code> <p>Returns:</p> Name Type Description <code>ToolbarPage</code> <p>The existing or newly created page.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def append(self, title, kind=ToolbarPage.TYPE_PAGE_NORMAL):\n    \"\"\"Append a page to the toolbar, or return an existing one.\n\n    If a page with the same name and kind already exists, it is\n    returned instead of creating a duplicate.\n\n    Args:\n        title: Page title.\n        kind: Type of page; one of ToolbarPage.TYPE_PAGE_* constants.\n\n    Returns:\n        ToolbarPage: The existing or newly created page.\n    \"\"\"\n    for page in self.pages:\n        if page.name == title and page.kind == kind:\n            return page\n    p = self.create_page(title, kind)\n    if not self.main_page:\n        self.main_page = p\n    self.pages.append(p)\n    return p\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarBar.bind","title":"bind","text":"<pre><code>bind(fun, id=ID_ANY, e=None)\n</code></pre> <p>Bind an event handler to the toolbar's parent window.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <p>Handler function.</p> required <code>id</code> <p>Identifier of the event.</p> <code>ID_ANY</code> <code>e</code> <p>Event type; defaults to wx.EVT_MENU if not specified.</p> <code>None</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def bind(self, fun, id=wx.ID_ANY, e=None):\n    \"\"\"Bind an event handler to the toolbar's parent window.\n\n    Args:\n        fun: Handler function.\n        id: Identifier of the event.\n        e: Event type; defaults to wx.EVT_MENU if not specified.\n    \"\"\"\n    if e:\n        self.parent.Bind(e, fun, id=id)\n    else:\n        self.parent.Bind(wx.EVT_MENU, fun, id=id)\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarBar.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Close/destroy the toolbar.</p> <p>Override in subclasses to perform cleanup.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def close(self):\n    \"\"\"Close/destroy the toolbar.\n\n    Override in subclasses to perform cleanup.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarBar.create","title":"create","text":"<pre><code>create()\n</code></pre> <p>Create/realize the toolbar.</p> <p>Override in subclasses to perform actual widget creation.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create(self):\n    \"\"\"Create/realize the toolbar.\n\n    Override in subclasses to perform actual widget creation.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarBar.create_html_win","title":"create_html_win","text":"<pre><code>create_html_win(toolbar_page, address_or_parser, parameters, callback=None)\n</code></pre> <p>Create an HTML page embedded in the toolbar.</p> <p>Not all toolbar types support this feature.</p> <p>Parameters:</p> Name Type Description Default <code>toolbar_page</code> <p>Name of the toolbar page to host the HTML. Use '__' to separate page and panel names, e.g. 'PageName__PanelName'.</p> required <code>address_or_parser</code> <p>Address of the HTTP page or a parser.</p> required <code>parameters</code> <p>Parameters for the HTTP request.</p> required <code>callback</code> <p>Optional callback invoked after initialization.</p> <code>None</code> <p>Returns:</p> Type Description <p>SchPage or None: The created page, or None on failure.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_html_win(\n    self, toolbar_page, address_or_parser, parameters, callback=None\n):\n    \"\"\"Create an HTML page embedded in the toolbar.\n\n    Not all toolbar types support this feature.\n\n    Args:\n        toolbar_page: Name of the toolbar page to host the HTML.\n            Use '__' to separate page and panel names,\n            e.g. 'PageName__PanelName'.\n        address_or_parser: Address of the HTTP page or a parser.\n        parameters: Parameters for the HTTP request.\n        callback: Optional callback invoked after initialization.\n\n    Returns:\n        SchPage or None: The created page, or None on failure.\n    \"\"\"\n    page = None\n    if not toolbar_page:\n        u_name = \"main tools\"\n        page_name = _(\"main tools\")\n        panel_name = \"main tools\"\n    else:\n        u_name = toolbar_page\n        names = toolbar_page.split(\"__\")\n        if len(names) &gt; 1:\n            page_name = names[0].replace(\"_\", \" \")\n            panel_name = names[1].replace(\"_\", \" \")\n        else:\n            page_name = toolbar_page.replace(\"_\", \" \")\n            panel_name = page_name\n\n    if u_name in self.user_panels:\n        panel = self.user_panels[u_name]\n    else:\n        page = self._find_page_by_title(page_name)\n        if page is None:\n            page = self.append(page_name)\n        panel = page.create_html_panel(panel_name)\n\n    if panel:\n        dx = panel.get_width() + 3\n        dy = panel.get_height() + 5\n\n        page2 = SchPage(\n            panel.get_window(),\n            address_or_parser,\n            parameters,\n            size=wx.Size(dx, dy),\n            pos=wx.Point(2, 2),\n        )\n        best = page2.body.calculate_best_size()\n        page2.SetSize(wx.Size(int(best[0]), int(best[1])))\n        panel.set_page(page2)\n        self.user_panels[u_name] = panel\n        page2.body.toolbar_interface = self\n        page2.body.toolbar_interface_page = page\n\n        def init_page(callback):\n            page2.init_frame()\n            page2.activate_page()\n            page2.Update()\n\n        wx.CallAfter(init_page, callback)\n\n        return page2\n\n    return None\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarBar.create_page","title":"create_page","text":"<pre><code>create_page(title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>Create and return a new ToolbarPage.</p> <p>Override in derived classes to return a subclass of ToolbarPage.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Page title.</p> required <code>kind</code> <p>Type of page; one of ToolbarPage.TYPE_PAGE_* constants.</p> <code>TYPE_PAGE_NORMAL</code> <p>Returns:</p> Name Type Description <code>ToolbarPage</code> <p>The newly created page.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_page(self, title, kind=ToolbarPage.TYPE_PAGE_NORMAL):\n    \"\"\"Create and return a new ToolbarPage.\n\n    Override in derived classes to return a subclass of ToolbarPage.\n\n    Args:\n        title: Page title.\n        kind: Type of page; one of ToolbarPage.TYPE_PAGE_* constants.\n\n    Returns:\n        ToolbarPage: The newly created page.\n    \"\"\"\n    return ToolbarPage(self, title, kind)\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarBar.create_panel_in_main_page","title":"create_panel_in_main_page","text":"<pre><code>create_panel_in_main_page(title, kind)\n</code></pre> <p>Create a panel in the main page.</p> <p>If no main page exists yet, one is created automatically.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>New panel's title.</p> required <code>kind</code> <p>Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.</p> required <p>Returns:</p> Name Type Description <code>ToolbarPanel</code> <p>The newly created panel.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_panel_in_main_page(self, title, kind):\n    \"\"\"Create a panel in the main page.\n\n    If no main page exists yet, one is created automatically.\n\n    Args:\n        title: New panel's title.\n        kind: Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.\n\n    Returns:\n        ToolbarPanel: The newly created panel.\n    \"\"\"\n    if not self.main_page:\n        self.append(\"main tools\")\n    return self.main_page.create_panel(title, kind)\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarBar.new_child_page","title":"new_child_page","text":"<pre><code>new_child_page(address_or_parser, title='', param=None, callback=None)\n</code></pre> <p>Create a new child page.</p> <p>For toolbar, the child page is transferred to the desktop window.</p> <p>Parameters:</p> Name Type Description Default <code>address_or_parser</code> <p>Address of the HTTP page or a parser.</p> required <code>title</code> <p>Page title.</p> <code>''</code> <code>param</code> <p>Optional parameters.</p> <code>None</code> <code>callback</code> <p>Optional callback.</p> <code>None</code> <p>Returns:</p> Type Description <p>The result of new_main_page on the top window.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def new_child_page(self, address_or_parser, title=\"\", param=None, callback=None):\n    \"\"\"Create a new child page.\n\n    For toolbar, the child page is transferred to the desktop window.\n\n    Args:\n        address_or_parser: Address of the HTTP page or a parser.\n        title: Page title.\n        param: Optional parameters.\n        callback: Optional callback.\n\n    Returns:\n        The result of new_main_page on the top window.\n    \"\"\"\n    return wx.GetApp().GetTopWindow().new_main_page(address_or_parser, title, param)\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarBar.remove_page","title":"remove_page","text":"<pre><code>remove_page(title)\n</code></pre> <p>Remove the page with the specified title from the toolbar.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Title of the page to remove.</p> required Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def remove_page(self, title):\n    \"\"\"Remove the page with the specified title from the toolbar.\n\n    Args:\n        title: Title of the page to remove.\n    \"\"\"\n    # Remove from pages list\n    for page in self.pages:\n        if page.title == title:\n            self.pages.remove(page)\n            break\n\n    # If the main page was removed, pick a new one\n    if self.main_page and self.main_page.title == title:\n        self.main_page = self.pages[0] if self.pages else None\n\n    # Remove associated user panels (iterate over a static list of keys\n    # to avoid \"dictionary changed size during iteration\" errors)\n    for key in list(self.user_panels.keys()):\n        panel = self.user_panels[key]\n        if panel.page.title == title:\n            del self.user_panels[key]\n            return\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarBar.un_bind","title":"un_bind","text":"<pre><code>un_bind(id=ID_ANY, e=None)\n</code></pre> <p>Unbind an event handler from the toolbar's parent window.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>Identifier of the event.</p> <code>ID_ANY</code> <code>e</code> <p>Event type; defaults to wx.EVT_MENU if not specified.</p> <code>None</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def un_bind(self, id=wx.ID_ANY, e=None):\n    \"\"\"Unbind an event handler from the toolbar's parent window.\n\n    Args:\n        id: Identifier of the event.\n        e: Event type; defaults to wx.EVT_MENU if not specified.\n    \"\"\"\n    if e:\n        self.parent.Unbind(e, id=id)\n    else:\n        self.parent.Unbind(wx.EVT_MENU, id=id)\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarButton","title":"ToolbarButton","text":"<pre><code>ToolbarButton(\n    parent_panel, id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE\n)\n</code></pre> <p>Represents a single button (or separator) on a toolbar panel.</p> <p>This is an abstract data model; the actual wx widget is created by the concrete toolbar implementation.</p> Button type constants <p>TYPE_SIMPLE = 0     - Simple push button. TYPE_DROPDOWN = 1   - Button with a dropdown menu. TYPE_HYBRID = 2     - Hybrid button (part button, part dropdown). TYPE_TOOGLE = 3     - Toggle button. TYPE_PANEL = 4      - Embedded panel. TYPE_SEPARATOR = 5  - Visual separator.</p> <p>Create a new toolbar button.</p> <p>Parameters:</p> Name Type Description Default <code>parent_panel</code> <p>Parent ToolbarPanel object.</p> required <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title (display text).</p> required <code>bitmap</code> <p>Button bitmap for the normal state.</p> <code>None</code> <code>bitmap_disabled</code> <p>Button bitmap for the disabled state. Defaults to wx.NullBitmap when bitmap is provided.</p> <code>None</code> <code>kind</code> <p>Type of button; one of the TYPE_* class constants.</p> <code>TYPE_SIMPLE</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(\n    self,\n    parent_panel,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=TYPE_SIMPLE,\n):\n    \"\"\"Create a new toolbar button.\n\n    Args:\n        parent_panel: Parent ToolbarPanel object.\n        id: An integer by which the tool may be identified.\n        title: Button title (display text).\n        bitmap: Button bitmap for the normal state.\n        bitmap_disabled: Button bitmap for the disabled state.\n            Defaults to wx.NullBitmap when bitmap is provided.\n        kind: Type of button; one of the TYPE_* class constants.\n    \"\"\"\n    self.parent_panel = parent_panel\n    self.id = id\n    self.title = title\n    self.bitmap = bitmap\n    if bitmap_disabled is None and bitmap is not None:\n        self.bitmap_disabled = wx.NullBitmap\n    else:\n        self.bitmap_disabled = bitmap_disabled\n    self.kind = kind\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarButton-functions","title":"Methods:","text":""},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarPage","title":"ToolbarPage","text":"<pre><code>ToolbarPage(parent_bar, title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>A page (tab) within a toolbar bar, containing one or more panels.</p> Page type constants <p>TYPE_PAGE_NORMAL = 0  - Standard page.</p> <p>Create a new toolbar page.</p> <p>Parameters:</p> Name Type Description Default <code>parent_bar</code> <p>Parent ToolbarBar object.</p> required <code>title</code> <p>Page title (will be translated via wx.GetTranslation).</p> required <code>kind</code> <p>Type of page; one of the TYPE_PAGE_* class constants.</p> <code>TYPE_PAGE_NORMAL</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, parent_bar, title, kind=TYPE_PAGE_NORMAL):\n    \"\"\"Create a new toolbar page.\n\n    Args:\n        parent_bar: Parent ToolbarBar object.\n        title: Page title (will be translated via wx.GetTranslation).\n        kind: Type of page; one of the TYPE_PAGE_* class constants.\n    \"\"\"\n    self.parent_bar = parent_bar\n    self.name = title\n    self.title = _(title)\n    self.kind = kind\n    self.panels = []\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarPage-functions","title":"Methods:","text":""},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarPage.append","title":"append","text":"<pre><code>append(title, kind=TYPE_PANEL_BUTTONBAR)\n</code></pre> <p>Create a new panel and append it to this page.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Panel title.</p> required <code>kind</code> <p>Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.</p> <code>TYPE_PANEL_BUTTONBAR</code> <p>Returns:</p> Name Type Description <code>ToolbarPanel</code> <p>The newly created panel.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def append(self, title, kind=ToolbarPanel.TYPE_PANEL_BUTTONBAR):\n    \"\"\"Create a new panel and append it to this page.\n\n    Args:\n        title: Panel title.\n        kind: Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.\n\n    Returns:\n        ToolbarPanel: The newly created panel.\n    \"\"\"\n    p = self.create_panel(title, kind)\n    self.panels.append(p)\n    return p\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarPage.create_html_panel","title":"create_html_panel","text":"<pre><code>create_html_panel(title)\n</code></pre> <p>Create an HTML-capable panel within this page.</p> <p>Override in derived classes to return a BaseHtmlPanel subclass.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Panel title.</p> required <p>Returns:</p> Type Description <p>BaseHtmlPanel or None: The panel, or None if not supported.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_html_panel(self, title):\n    \"\"\"Create an HTML-capable panel within this page.\n\n    Override in derived classes to return a BaseHtmlPanel subclass.\n\n    Args:\n        title: Panel title.\n\n    Returns:\n        BaseHtmlPanel or None: The panel, or None if not supported.\n    \"\"\"\n    return None\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarPage.create_panel","title":"create_panel","text":"<pre><code>create_panel(title, kind)\n</code></pre> <p>Create and return a new ToolbarPanel for this page.</p> <p>Override in derived classes to return a subclass of ToolbarPanel.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Panel title.</p> required <code>kind</code> <p>Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.</p> required <p>Returns:</p> Name Type Description <code>ToolbarPanel</code> <p>The newly created panel.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_panel(self, title, kind):\n    \"\"\"Create and return a new ToolbarPanel for this page.\n\n    Override in derived classes to return a subclass of ToolbarPanel.\n\n    Args:\n        title: Panel title.\n        kind: Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.\n\n    Returns:\n        ToolbarPanel: The newly created panel.\n    \"\"\"\n    return ToolbarPanel(self, title, kind=ToolbarPanel.TYPE_PANEL_BUTTONBAR)\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarPanel","title":"ToolbarPanel","text":"<pre><code>ToolbarPanel(parent_page, title, kind=TYPE_PANEL_BUTTONBAR)\n</code></pre> <p>A panel within a toolbar page that holds a group of buttons.</p> Panel type constants <p>TYPE_PANEL_BUTTONBAR = 0  - Button bar layout. TYPE_PANEL_TOOLBAR = 1    - Toolbar layout. TYPE_PANEL_PANELBAR = 2   - Custom panel layout.</p> <p>Create a new toolbar panel.</p> <p>Parameters:</p> Name Type Description Default <code>parent_page</code> <p>Parent ToolbarPage object.</p> required <code>title</code> <p>Panel title.</p> required <code>kind</code> <p>Type of panel; one of the TYPE_PANEL_* class constants.</p> <code>TYPE_PANEL_BUTTONBAR</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, parent_page, title, kind=TYPE_PANEL_BUTTONBAR):\n    \"\"\"Create a new toolbar panel.\n\n    Args:\n        parent_page: Parent ToolbarPage object.\n        title: Panel title.\n        kind: Type of panel; one of the TYPE_PANEL_* class constants.\n    \"\"\"\n    self.parent_page = parent_page\n    self.title = title\n    self.kind = kind\n    self.buttons = []\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarPanel-functions","title":"Methods:","text":""},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarPanel.add_dropdown_tool","title":"add_dropdown_tool","text":"<pre><code>add_dropdown_tool(id, title, bitmaps)\n</code></pre> <p>Append a dropdown button to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_dropdown_tool(self, id, title, bitmaps):\n    \"\"\"Append a dropdown button to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_DROPDOWN)\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarPanel.add_hybrid_tool","title":"add_hybrid_tool","text":"<pre><code>add_hybrid_tool(id, title, bitmaps)\n</code></pre> <p>Append a hybrid button (button + dropdown) to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_hybrid_tool(self, id, title, bitmaps):\n    \"\"\"Append a hybrid button (button + dropdown) to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_HYBRID)\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarPanel.add_separator","title":"add_separator","text":"<pre><code>add_separator()\n</code></pre> <p>Append a visual separator to the panel.</p> <p>The default implementation does nothing; override in subclasses.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_separator(self):\n    \"\"\"Append a visual separator to the panel.\n\n    The default implementation does nothing; override in subclasses.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarPanel.add_simple_tool","title":"add_simple_tool","text":"<pre><code>add_simple_tool(id, title, bitmaps)\n</code></pre> <p>Append a simple push button to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_simple_tool(self, id, title, bitmaps):\n    \"\"\"Append a simple push button to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_SIMPLE)\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarPanel.add_toogle_tool","title":"add_toogle_tool","text":"<pre><code>add_toogle_tool(id, title, bitmaps)\n</code></pre> <p>Append a toggle button to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_toogle_tool(self, id, title, bitmaps):\n    \"\"\"Append a toggle button to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_TOOGLE)\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarPanel.append","title":"append","text":"<pre><code>append(id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE)\n</code></pre> <p>Create a button and append it to this panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmap</code> <p>Button bitmap for the normal state.</p> <code>None</code> <code>bitmap_disabled</code> <p>Button bitmap for the disabled state.</p> <code>None</code> <code>kind</code> <p>Type of button; one of ToolbarButton.TYPE_* constants.</p> <code>TYPE_SIMPLE</code> <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created and appended button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def append(\n    self,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=ToolbarButton.TYPE_SIMPLE,\n):\n    \"\"\"Create a button and append it to this panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmap: Button bitmap for the normal state.\n        bitmap_disabled: Button bitmap for the disabled state.\n        kind: Type of button; one of ToolbarButton.TYPE_* constants.\n\n    Returns:\n        ToolbarButton: The newly created and appended button.\n    \"\"\"\n    button = self.create_button(id, title, bitmap, bitmap_disabled, kind)\n    self.buttons.append(button)\n    return button\n</code></pre>"},{"location":"api/toolbar_menubar/#pytigon_gui.toolbar.menubar.ToolbarPanel.create_button","title":"create_button","text":"<pre><code>create_button(id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE)\n</code></pre> <p>Create and return a new ToolbarButton.</p> <p>Override in derived classes to return a subclass of ToolbarButton.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmap</code> <p>Button bitmap for the normal state.</p> <code>None</code> <code>bitmap_disabled</code> <p>Button bitmap for the disabled state.</p> <code>None</code> <code>kind</code> <p>Type of button; one of ToolbarButton.TYPE_* constants.</p> <code>TYPE_SIMPLE</code> <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_button(\n    self,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=ToolbarButton.TYPE_SIMPLE,\n):\n    \"\"\"Create and return a new ToolbarButton.\n\n    Override in derived classes to return a subclass of ToolbarButton.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmap: Button bitmap for the normal state.\n        bitmap_disabled: Button bitmap for the disabled state.\n        kind: Type of button; one of ToolbarButton.TYPE_* constants.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    return ToolbarButton(self, id, title, bitmap, bitmap_disabled, kind)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/","title":"toolbar.moderntoolbar \u2013 Modern Toolbar","text":"<p>Ribbon-style modern toolbar with tab groups, large/small icon modes, and collapsible sections.</p>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar","title":"moderntoolbar","text":"<p>Modern (Ribbon) toolbar implementation.</p> <p>Provides a ribbon-style toolbar based on wx.lib.agw.ribbon.</p>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar-attributes","title":"Attributes","text":""},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_BOOKMARK_ADD","title":"ID_BOOKMARK_ADD  <code>module-attribute</code>","text":"<pre><code>ID_BOOKMARK_ADD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_CLEAR_HISTORY","title":"ID_CLEAR_HISTORY  <code>module-attribute</code>","text":"<pre><code>ID_CLEAR_HISTORY = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_CLOSETAB","title":"ID_CLOSETAB  <code>module-attribute</code>","text":"<pre><code>ID_CLOSETAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_COPY","title":"ID_COPY  <code>module-attribute</code>","text":"<pre><code>ID_COPY = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_CUT","title":"ID_CUT  <code>module-attribute</code>","text":"<pre><code>ID_CUT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_DOWNLOAD_OPTIONS","title":"ID_DOWNLOAD_OPTIONS  <code>module-attribute</code>","text":"<pre><code>ID_DOWNLOAD_OPTIONS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_END","title":"ID_END  <code>module-attribute</code>","text":"<pre><code>ID_END = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_EXIT","title":"ID_EXIT  <code>module-attribute</code>","text":"<pre><code>ID_EXIT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_FIND","title":"ID_FIND  <code>module-attribute</code>","text":"<pre><code>ID_FIND = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_GENERAL_OPTIONS","title":"ID_GENERAL_OPTIONS  <code>module-attribute</code>","text":"<pre><code>ID_GENERAL_OPTIONS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_GOTODESKTOP","title":"ID_GOTODESKTOP  <code>module-attribute</code>","text":"<pre><code>ID_GOTODESKTOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_GOTOFOOTER","title":"ID_GOTOFOOTER  <code>module-attribute</code>","text":"<pre><code>ID_GOTOFOOTER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_GOTOHEADER","title":"ID_GOTOHEADER  <code>module-attribute</code>","text":"<pre><code>ID_GOTOHEADER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_GOTOPANEL","title":"ID_GOTOPANEL  <code>module-attribute</code>","text":"<pre><code>ID_GOTOPANEL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_HELP","title":"ID_HELP  <code>module-attribute</code>","text":"<pre><code>ID_HELP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_HISTORY_SHOW","title":"ID_HISTORY_SHOW  <code>module-attribute</code>","text":"<pre><code>ID_HISTORY_SHOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_LOAD","title":"ID_LOAD  <code>module-attribute</code>","text":"<pre><code>ID_LOAD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_NEXTPAGE","title":"ID_NEXTPAGE  <code>module-attribute</code>","text":"<pre><code>ID_NEXTPAGE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_NEXTTAB","title":"ID_NEXTTAB  <code>module-attribute</code>","text":"<pre><code>ID_NEXTTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_PAGE_BLOCK","title":"ID_PAGE_BLOCK  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_BLOCK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_PAGE_SOURCE","title":"ID_PAGE_SOURCE  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_SOURCE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_PAGE_VIEW","title":"ID_PAGE_VIEW  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_VIEW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_PASTE","title":"ID_PASTE  <code>module-attribute</code>","text":"<pre><code>ID_PASTE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_PREVPAGE","title":"ID_PREVPAGE  <code>module-attribute</code>","text":"<pre><code>ID_PREVPAGE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_PREVTAB","title":"ID_PREVTAB  <code>module-attribute</code>","text":"<pre><code>ID_PREVTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_PRINT","title":"ID_PRINT  <code>module-attribute</code>","text":"<pre><code>ID_PRINT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_PRINT_PREVIEW","title":"ID_PRINT_PREVIEW  <code>module-attribute</code>","text":"<pre><code>ID_PRINT_PREVIEW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_PRINT_SETTINGS","title":"ID_PRINT_SETTINGS  <code>module-attribute</code>","text":"<pre><code>ID_PRINT_SETTINGS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_REDO","title":"ID_REDO  <code>module-attribute</code>","text":"<pre><code>ID_REDO = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_REFRESHTAB","title":"ID_REFRESHTAB  <code>module-attribute</code>","text":"<pre><code>ID_REFRESHTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_REPLACE","title":"ID_REPLACE  <code>module-attribute</code>","text":"<pre><code>ID_REPLACE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_RESET","title":"ID_RESET  <code>module-attribute</code>","text":"<pre><code>ID_RESET = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_RETVALUE","title":"ID_RETVALUE  <code>module-attribute</code>","text":"<pre><code>ID_RETVALUE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_SAVE","title":"ID_SAVE  <code>module-attribute</code>","text":"<pre><code>ID_SAVE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_SAVE_AS","title":"ID_SAVE_AS  <code>module-attribute</code>","text":"<pre><code>ID_SAVE_AS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_SELALL","title":"ID_SELALL  <code>module-attribute</code>","text":"<pre><code>ID_SELALL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_SHOWDESKTOP","title":"ID_SHOWDESKTOP  <code>module-attribute</code>","text":"<pre><code>ID_SHOWDESKTOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_SHOWFOOTER","title":"ID_SHOWFOOTER  <code>module-attribute</code>","text":"<pre><code>ID_SHOWFOOTER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_SHOWHEADER","title":"ID_SHOWHEADER  <code>module-attribute</code>","text":"<pre><code>ID_SHOWHEADER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_SHOWPANEL","title":"ID_SHOWPANEL  <code>module-attribute</code>","text":"<pre><code>ID_SHOWPANEL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_SHOWSTATUSBAR","title":"ID_SHOWSTATUSBAR  <code>module-attribute</code>","text":"<pre><code>ID_SHOWSTATUSBAR = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_SHOWTOOLBAR1","title":"ID_SHOWTOOLBAR1  <code>module-attribute</code>","text":"<pre><code>ID_SHOWTOOLBAR1 = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_SHOWTOOLBAR2","title":"ID_SHOWTOOLBAR2  <code>module-attribute</code>","text":"<pre><code>ID_SHOWTOOLBAR2 = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_SHOW_BOOKMARKS","title":"ID_SHOW_BOOKMARKS  <code>module-attribute</code>","text":"<pre><code>ID_SHOW_BOOKMARKS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_SHOW_DOWNLOAD","title":"ID_SHOW_DOWNLOAD  <code>module-attribute</code>","text":"<pre><code>ID_SHOW_DOWNLOAD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_START","title":"ID_START  <code>module-attribute</code>","text":"<pre><code>ID_START = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_TASKBAR_CLOSE","title":"ID_TASKBAR_CLOSE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_CLOSE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_TASKBAR_HIDE","title":"ID_TASKBAR_HIDE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_HIDE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_TASKBAR_SHOW","title":"ID_TASKBAR_SHOW  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_SHOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_TASKBAR_TOGGLE","title":"ID_TASKBAR_TOGGLE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_TOGGLE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_UNDO","title":"ID_UNDO  <code>module-attribute</code>","text":"<pre><code>ID_UNDO = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_WEB_ADDBOOKMARK","title":"ID_WEB_ADDBOOKMARK  <code>module-attribute</code>","text":"<pre><code>ID_WEB_ADDBOOKMARK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_WEB_BACK","title":"ID_WEB_BACK  <code>module-attribute</code>","text":"<pre><code>ID_WEB_BACK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_WEB_DOWN","title":"ID_WEB_DOWN  <code>module-attribute</code>","text":"<pre><code>ID_WEB_DOWN = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_WEB_EDIT","title":"ID_WEB_EDIT  <code>module-attribute</code>","text":"<pre><code>ID_WEB_EDIT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_WEB_FORWARD","title":"ID_WEB_FORWARD  <code>module-attribute</code>","text":"<pre><code>ID_WEB_FORWARD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_WEB_NEW_WINDOW","title":"ID_WEB_NEW_WINDOW  <code>module-attribute</code>","text":"<pre><code>ID_WEB_NEW_WINDOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_WEB_REFRESH","title":"ID_WEB_REFRESH  <code>module-attribute</code>","text":"<pre><code>ID_WEB_REFRESH = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_WEB_SOURCE","title":"ID_WEB_SOURCE  <code>module-attribute</code>","text":"<pre><code>ID_WEB_SOURCE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_WEB_STOP","title":"ID_WEB_STOP  <code>module-attribute</code>","text":"<pre><code>ID_WEB_STOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_WEB_UP","title":"ID_WEB_UP  <code>module-attribute</code>","text":"<pre><code>ID_WEB_UP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ID_ZOOM","title":"ID_ZOOM  <code>module-attribute</code>","text":"<pre><code>ID_ZOOM = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.MSW_STYLE","title":"MSW_STYLE  <code>module-attribute</code>","text":"<pre><code>MSW_STYLE = True\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ORG_LIKE_PRIMARY","title":"ORG_LIKE_PRIMARY  <code>module-attribute</code>","text":"<pre><code>ORG_LIKE_PRIMARY = None\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar._","title":"_  <code>module-attribute</code>","text":"<pre><code>_ = wx.GetTranslation\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar-classes","title":"Classes","text":""},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.BaseHtmlPanel","title":"BaseHtmlPanel","text":"<pre><code>BaseHtmlPanel(page, real_panel)\n</code></pre> <p>Base panel that can host an HTML-rendered page within a toolbar.</p> <p>This is used by toolbar types that support embedding web content (e.g. ModernToolbarBar, TreeToolbarBar).</p> <p>Initialize the HTML panel wrapper.</p> <p>Parameters:</p> Name Type Description Default <code>page</code> <p>Parent ToolbarPage object.</p> required <code>real_panel</code> <p>The actual wx.Panel that will contain the HTML page.</p> required Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, page, real_panel):\n    \"\"\"Initialize the HTML panel wrapper.\n\n    Args:\n        page: Parent ToolbarPage object.\n        real_panel: The actual wx.Panel that will contain the HTML page.\n    \"\"\"\n    self.page = page\n    self.real_panel = real_panel\n    self.html_page = None\n    self.sizer = wx.BoxSizer(wx.VERTICAL)\n    self.real_panel.SetSizer(self.sizer)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.BaseHtmlPanel-functions","title":"Methods:","text":""},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.BaseHtmlPanel.get_height","title":"get_height","text":"<pre><code>get_height()\n</code></pre> <p>Return the preferred height of the panel.</p> <p>Override in subclasses to provide actual dimensions.</p> <p>Returns:</p> Name Type Description <code>int</code> <p>Height in pixels (0 by default).</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def get_height(self):\n    \"\"\"Return the preferred height of the panel.\n\n    Override in subclasses to provide actual dimensions.\n\n    Returns:\n        int: Height in pixels (0 by default).\n    \"\"\"\n    return 0\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.BaseHtmlPanel.get_width","title":"get_width","text":"<pre><code>get_width()\n</code></pre> <p>Return the preferred width of the panel.</p> <p>Override in subclasses to provide actual dimensions.</p> <p>Returns:</p> Name Type Description <code>int</code> <p>Width in pixels (0 by default).</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def get_width(self):\n    \"\"\"Return the preferred width of the panel.\n\n    Override in subclasses to provide actual dimensions.\n\n    Returns:\n        int: Width in pixels (0 by default).\n    \"\"\"\n    return 0\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.BaseHtmlPanel.get_window","title":"get_window","text":"<pre><code>get_window()\n</code></pre> <p>Return the underlying wx.Window for this panel.</p> <p>Returns:</p> Type Description <p>wx.Window: The real panel window.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def get_window(self):\n    \"\"\"Return the underlying wx.Window for this panel.\n\n    Returns:\n        wx.Window: The real panel window.\n    \"\"\"\n    return self.real_panel\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.BaseHtmlPanel.set_page","title":"set_page","text":"<pre><code>set_page(html_page)\n</code></pre> <p>Replace the current HTML page with a new one.</p> <p>If a page already exists, it is replaced in the sizer and destroyed. Otherwise the new page is added to the sizer.</p> <p>Parameters:</p> Name Type Description Default <code>html_page</code> <p>The new SchPage (or wx.Window) to display.</p> required Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def set_page(self, html_page):\n    \"\"\"Replace the current HTML page with a new one.\n\n    If a page already exists, it is replaced in the sizer and destroyed.\n    Otherwise the new page is added to the sizer.\n\n    Args:\n        html_page: The new SchPage (or wx.Window) to display.\n    \"\"\"\n    if self.html_page:\n        self.sizer.Replace(self.html_page, html_page)\n        self.html_page.Destroy()\n    else:\n        self.sizer.Add(html_page, 0, wx.LEFT | wx.TOP | wx.EXPAND | wx.RIGHT, 2)\n\n    self.html_page = html_page\n\n    self.sizer.Fit(self.real_panel)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernHtmlPanel","title":"ModernHtmlPanel","text":"<pre><code>ModernHtmlPanel(page, real_panel)\n</code></pre> <p>               Bases: <code>BaseHtmlPanel</code></p> <p>HTML panel wrapper for the modern ribbon toolbar.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, page, real_panel):\n    \"\"\"Initialize the HTML panel wrapper.\n\n    Args:\n        page: Parent ToolbarPage object.\n        real_panel: The actual wx.Panel that will contain the HTML page.\n    \"\"\"\n    self.page = page\n    self.real_panel = real_panel\n    self.html_page = None\n    self.sizer = wx.BoxSizer(wx.VERTICAL)\n    self.real_panel.SetSizer(self.sizer)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernHtmlPanel-functions","title":"Methods:","text":""},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernHtmlPanel.get_height","title":"get_height","text":"<pre><code>get_height()\n</code></pre> <p>Return panel height from the parent ribbon bar.</p> Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def get_height(self):\n    \"\"\"Return panel height from the parent ribbon bar.\"\"\"\n    return self.page.parent_bar.get_bar_height()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernHtmlPanel.get_width","title":"get_width","text":"<pre><code>get_width()\n</code></pre> <p>Return panel width from the parent ribbon bar.</p> Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def get_width(self):\n    \"\"\"Return panel width from the parent ribbon bar.\"\"\"\n    return self.page.parent_bar.get_bar_width()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernHtmlPanel.set_page","title":"set_page","text":"<pre><code>set_page(html_page)\n</code></pre> <p>Set the HTML page and update the ribbon bar layout.</p> <p>Parameters:</p> Name Type Description Default <code>html_page</code> <p>The SchPage to display.</p> required Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def set_page(self, html_page):\n    \"\"\"Set the HTML page and update the ribbon bar layout.\n\n    Args:\n        html_page: The SchPage to display.\n    \"\"\"\n    super().set_page(html_page)\n    self.page.parent_bar.update()\n    self.page.parent_bar.SetActivePage(self.page)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarBar","title":"ModernToolbarBar","text":"<pre><code>ModernToolbarBar(parent, gui_style)\n</code></pre> <p>               Bases: <code>ToolbarBar</code>, <code>RibbonBar</code></p> <p>The top-level ribbon toolbar bar.</p> <p>Initialize the ribbon bar.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent wx.Frame.</p> required <code>gui_style</code> <p>GUI style string.</p> required Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def __init__(self, parent, gui_style):\n    \"\"\"Initialize the ribbon bar.\n\n    Args:\n        parent: Parent wx.Frame.\n        gui_style: GUI style string.\n    \"\"\"\n    RB.RibbonBar.__init__(self, parent, wx.ID_ANY)\n    super().__init__(parent, gui_style)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarBar-functions","title":"Methods:","text":""},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarBar.RecalculateTabSizes","title":"RecalculateTabSizes","text":"<pre><code>RecalculateTabSizes()\n</code></pre> <p>Recalculates the :class:<code>RibbonBar</code> tab sizes.</p> Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def RecalculateTabSizes(self):\n    \"\"\"Recalculates the :class:`RibbonBar` tab sizes.\"\"\"\n\n    numtabs = len(self._pages)\n\n    if numtabs == 0:\n        return\n\n    width = (\n        self.GetSize().GetWidth() - self._tab_margin_left - self._tab_margin_right\n    )\n    tabsep = self._art.GetMetric(RIBBON_ART_TAB_SEPARATION_SIZE)\n    x = self._tab_margin_left\n    y = 0\n\n    if width &gt;= self._tabs_total_width_ideal:\n        # Simple case: everything at ideal width\n        for info in self._pages:\n            info.rect.x = x\n            info.rect.y = y\n            info.rect.width = info.ideal_width\n            info.rect.height = self._tab_height\n            x += info.rect.width + tabsep\n\n        self._tab_scroll_buttons_shown = False\n        self._tab_scroll_left_button_rect.SetWidth(0)\n        self._tab_scroll_right_button_rect.SetWidth(0)\n\n    elif width &lt; self._tabs_total_width_minimum:\n        # Simple case: everything minimum with scrollbar\n        for info in self._pages:\n            info.rect.x = x\n            info.rect.y = y\n            info.rect.width = info.minimum_width\n            info.rect.height = self._tab_height\n            x += info.rect.width + tabsep\n\n        if not self._tab_scroll_buttons_shown:\n            self._tab_scroll_left_button_state = RIBBON_SCROLL_BTN_NORMAL\n            self._tab_scroll_right_button_state = RIBBON_SCROLL_BTN_NORMAL\n            self._tab_scroll_buttons_shown = True\n\n        temp_dc = wx.ClientDC(self)\n        self._tab_scroll_left_button_rect.SetWidth(\n            self._art.GetScrollButtonMinimumSize(\n                temp_dc,\n                self,\n                RIBBON_SCROLL_BTN_LEFT\n                | RIBBON_SCROLL_BTN_NORMAL\n                | RIBBON_SCROLL_BTN_FOR_TABS,\n            ).GetWidth()\n        )\n        self._tab_scroll_left_button_rect.SetHeight(self._tab_height)\n        self._tab_scroll_left_button_rect.SetX(self._tab_margin_left)\n        self._tab_scroll_left_button_rect.SetY(0)\n        self._tab_scroll_right_button_rect.SetWidth(\n            self._art.GetScrollButtonMinimumSize(\n                temp_dc,\n                self,\n                RIBBON_SCROLL_BTN_RIGHT\n                | RIBBON_SCROLL_BTN_NORMAL\n                | RIBBON_SCROLL_BTN_FOR_TABS,\n            ).GetWidth()\n        )\n        self._tab_scroll_right_button_rect.SetHeight(self._tab_height)\n        self._tab_scroll_right_button_rect.SetX(\n            self.GetClientSize().GetWidth()\n            - self._tab_margin_right\n            - self._tab_scroll_right_button_rect.GetWidth()\n        )\n        self._tab_scroll_right_button_rect.SetY(0)\n\n        if self._tab_scroll_amount == 0:\n            self._tab_scroll_left_button_rect.SetWidth(0)\n\n        elif self._tab_scroll_amount + width &gt;= self._tabs_total_width_minimum:\n            self._tab_scroll_amount = self._tabs_total_width_minimum - width\n            self._tab_scroll_right_button_rect.SetX(\n                self._tab_scroll_right_button_rect.GetX()\n                + self._tab_scroll_right_button_rect.GetWidth()\n            )\n            self._tab_scroll_right_button_rect.SetWidth(0)\n\n        for info in self._pages:\n            info.rect.x -= self._tab_scroll_amount\n\n    else:\n        self._tab_scroll_buttons_shown = False\n        self._tab_scroll_left_button_rect.SetWidth(0)\n        self._tab_scroll_right_button_rect.SetWidth(0)\n        # Complex case: everything sized such that: minimum &lt;= width &lt; ideal\n        #\n        #   Strategy:\n        #     1) Uniformly reduce all tab widths from ideal to small_must_have_separator_width\n        #     2) Reduce the largest tab by 1 pixel, repeating until all tabs are same width (or at minimum)\n        #     3) Uniformly reduce all tabs down to their minimum width\n        #\n        smallest_tab_width = 10000\n        total_small_width = tabsep * (numtabs - 1)\n\n        for info in self._pages:\n            if info.small_must_have_separator_width &lt; smallest_tab_width:\n                smallest_tab_width = info.small_must_have_separator_width\n\n            total_small_width += info.small_must_have_separator_width\n\n        if width &gt;= total_small_width:\n            # Do (1)\n            total_delta = self._tabs_total_width_ideal - total_small_width\n            total_small_width -= tabsep * (numtabs - 1)\n            width -= tabsep * (numtabs - 1)\n            for info in self._pages:\n                delta = info.ideal_width - info.small_must_have_separator_width\n                info.rect.x = x\n                info.rect.y = y\n                info.rect.width = (\n                    info.small_must_have_separator_width\n                    + delta * (width - total_small_width) // total_delta\n                )\n                info.rect.height = self._tab_height\n\n                x += info.rect.width + tabsep\n                total_delta -= delta\n                total_small_width -= info.small_must_have_separator_width\n                width -= info.rect.width\n\n        else:\n            total_small_width = tabsep * (numtabs - 1)\n            for info in self._pages:\n                if info.minimum_width &lt; smallest_tab_width:\n                    total_small_width += smallest_tab_width\n                else:\n                    total_small_width += info.minimum_width\n\n            if width &gt;= total_small_width:\n                # Do (2)\n                sorted_pages = []\n                for info in self._pages:\n                    # Sneaky obj array trickery to not copy the tab descriptors\n                    sorted_pages.append(info)\n\n                sorted_pages.sort(\n                    key=cmp_to_key(self.OrderPageTabInfoBySmallWidthAsc)\n                )\n                width -= tabsep * (numtabs - 1)\n\n                for i, info in enumerate(self._pages):\n                    if (\n                        info.small_must_have_separator_width * (numtabs - i)\n                        &lt;= width\n                    ):\n                        info.rect.width = info.small_must_have_separator_width\n                    else:\n                        info.rect.width = width // (numtabs - i)\n\n                    width -= info.rect.width\n\n                for i, info in enumerate(self._pages):\n                    info.rect.x = x\n                    info.rect.y = y\n                    info.rect.height = self._tab_height\n                    x += info.rect.width + tabsep\n                    sorted_pages.pop(numtabs - (i + 1))\n\n            else:\n                # Do (3)\n                total_small_width = (smallest_tab_width + tabsep) * numtabs - tabsep\n                total_delta = total_small_width - self._tabs_total_width_minimum\n                total_small_width = self._tabs_total_width_minimum - tabsep * (\n                    numtabs - 1\n                )\n                width -= tabsep * (numtabs - 1)\n\n                for info in self._pages:\n                    delta = smallest_tab_width - info.minimum_width\n                    info.rect.x = x\n                    info.rect.y = y\n                    info.rect.width = (\n                        info.minimum_width\n                        + delta * (width - total_small_width) // total_delta\n                    )\n                    info.rect.height = self._tab_height\n\n                    x += info.rect.width + tabsep\n                    total_delta -= delta\n                    total_small_width -= info.minimum_width\n                    width -= info.rect.width\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarBar.bind","title":"bind","text":"<pre><code>bind(fun, id=ID_ANY, e=None)\n</code></pre> <p>Bind a click event handler to the ribbon bar.</p> <p>Binds to both ribbon button bar and ribbon toolbar click events.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <p>Handler function.</p> required <code>id</code> <p>Event identifier.</p> <code>ID_ANY</code> <code>e</code> <p>Optional specific event type.</p> <code>None</code> Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def bind(self, fun, id=wx.ID_ANY, e=None):\n    \"\"\"Bind a click event handler to the ribbon bar.\n\n    Binds to both ribbon button bar and ribbon toolbar click events.\n\n    Args:\n        fun: Handler function.\n        id: Event identifier.\n        e: Optional specific event type.\n    \"\"\"\n    if e:\n        self.Bind(e, fun, id=id)\n    else:\n        self.Bind(RB.EVT_RIBBONBUTTONBAR_CLICKED, fun, id=id)\n        self.Bind(RB.EVT_RIBBONTOOLBAR_CLICKED, fun, id=id)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarBar.bind_dropdown","title":"bind_dropdown","text":"<pre><code>bind_dropdown(fun, id)\n</code></pre> <p>Bind a dropdown click event handler to the ribbon bar.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <p>Handler function.</p> required <code>id</code> <p>Event identifier.</p> required Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def bind_dropdown(self, fun, id):\n    \"\"\"Bind a dropdown click event handler to the ribbon bar.\n\n    Args:\n        fun: Handler function.\n        id: Event identifier.\n    \"\"\"\n    self.Bind(RB.EVT_RIBBONTOOLBAR_DROPDOWN_CLICKED, fun, id=id)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarBar.bind_ui","title":"bind_ui","text":"<pre><code>bind_ui(fun, id=ID_ANY)\n</code></pre> <p>Bind a UI update event handler to the ribbon bar.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <p>Handler function.</p> required <code>id</code> <p>Event identifier.</p> <code>ID_ANY</code> Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def bind_ui(self, fun, id=wx.ID_ANY):\n    \"\"\"Bind a UI update event handler to the ribbon bar.\n\n    Args:\n        fun: Handler function.\n        id: Event identifier.\n    \"\"\"\n    self.Bind(wx.EVT_UPDATE_UI, fun, id=id)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarBar.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Close/destroy the ribbon bar.</p> Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def close(self):\n    \"\"\"Close/destroy the ribbon bar.\"\"\"\n    self.Close()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarBar.create","title":"create","text":"<pre><code>create()\n</code></pre> <p>Realize the ribbon bar and its children.</p> Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def create(self):\n    \"\"\"Realize the ribbon bar and its children.\"\"\"\n    self.Realize()\n\n    def _realize():\n        for child in self.Children:\n            child.Realize()\n\n    wx.CallAfter(_realize)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarBar.create_page","title":"create_page","text":"<pre><code>create_page(title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>Create a new ribbon page.</p> <p>Returns:</p> Name Type Description <code>ModernToolbarPage</code> <p>The newly created page.</p> Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def create_page(self, title, kind=ToolbarPage.TYPE_PAGE_NORMAL):\n    \"\"\"Create a new ribbon page.\n\n    Returns:\n        ModernToolbarPage: The newly created page.\n    \"\"\"\n    return ModernToolbarPage(self, title, kind)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarBar.get_bar_height","title":"get_bar_height","text":"<pre><code>get_bar_height()\n</code></pre> <p>Return the preferred height of the ribbon bar.</p> <p>Returns:</p> Name Type Description <code>int</code> <p>Height in pixels.</p> Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def get_bar_height(self):\n    \"\"\"Return the preferred height of the ribbon bar.\n\n    Returns:\n        int: Height in pixels.\n    \"\"\"\n    s = (48, 48)\n    ret = self.GetArtProvider().GetButtonBarButtonSize(\n        self,\n        self,\n        art.RIBBON_BUTTON_NORMAL,\n        art.RIBBON_BUTTONBAR_BUTTON_LARGE,\n        \"TXT\",\n        s,\n        s,\n    )\n    size_ret = ret[1]\n    return size_ret.GetHeight()\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarBar.get_bar_width","title":"get_bar_width","text":"<pre><code>get_bar_width()\n</code></pre> <p>Return the preferred width of the ribbon bar.</p> <p>Returns:</p> Name Type Description <code>int</code> <p>Width in pixels (always 2000 for full width).</p> Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def get_bar_width(self):\n    \"\"\"Return the preferred width of the ribbon bar.\n\n    Returns:\n        int: Width in pixels (always 2000 for full width).\n    \"\"\"\n    return 2000\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarBar.remove_page","title":"remove_page","text":"<pre><code>remove_page(title)\n</code></pre> <p>Remove a page from the ribbon bar.</p> <p>Switches to the first page before removal, then updates layout.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Title of the page to remove.</p> required Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def remove_page(self, title):\n    \"\"\"Remove a page from the ribbon bar.\n\n    Switches to the first page before removal, then updates layout.\n\n    Args:\n        title: Title of the page to remove.\n    \"\"\"\n    self.SetActivePage(0)\n    for page_info in self._pages:\n        if page_info.page.title == title:\n            self._pages.remove(page_info)\n    self.Update()\n    super().remove_page(title)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarBar.un_bind","title":"un_bind","text":"<pre><code>un_bind(id, e=None)\n</code></pre> <p>Unbind an event handler from the ribbon bar.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>Event identifier.</p> required <code>e</code> <p>Optional specific event type.</p> <code>None</code> Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def un_bind(self, id, e=None):\n    \"\"\"Unbind an event handler from the ribbon bar.\n\n    Args:\n        id: Event identifier.\n        e: Optional specific event type.\n    \"\"\"\n    if e:\n        self.Unbind(e, id=id)\n    else:\n        self.Unbind(RB.EVT_RIBBONBUTTONBAR_CLICKED, id=id)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarBar.update","title":"update","text":"<pre><code>update()\n</code></pre> <p>Force a layout refresh by briefly resizing the bar.</p> Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def update(self):\n    \"\"\"Force a layout refresh by briefly resizing the bar.\"\"\"\n    size = self.GetSize()\n    width = size.GetWidth()\n    if width &gt; 0:\n        self.SetSize(wx.Size(width - 1, size.GetHeight()))\n    self.SetSize(wx.Size(width, size.GetHeight()))\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarButton","title":"ModernToolbarButton","text":"<pre><code>ModernToolbarButton(\n    parent_panel, id, title, bitmap, bitmap_disabled=None, kind=TYPE_SIMPLE\n)\n</code></pre> <p>               Bases: <code>ToolbarButton</code></p> <p>Ribbon-specific toolbar button (thin wrapper around ToolbarButton).</p> Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def __init__(\n    self,\n    parent_panel,\n    id,\n    title,\n    bitmap,\n    bitmap_disabled=None,\n    kind=ToolbarButton.TYPE_SIMPLE,\n):\n    super().__init__(parent_panel, id, title, bitmap, bitmap_disabled, kind)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarPage","title":"ModernToolbarPage","text":"<pre><code>ModernToolbarPage(parent_bar, title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>               Bases: <code>ToolbarPage</code>, <code>RibbonPage</code></p> <p>A ribbon page (tab) within the ribbon bar.</p> <p>Initialize the ribbon page.</p> <p>Parameters:</p> Name Type Description Default <code>parent_bar</code> <p>Parent ModernToolbarBar (a RibbonBar).</p> required <code>title</code> <p>Page title.</p> required <code>kind</code> <p>Page type.</p> <code>TYPE_PAGE_NORMAL</code> Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def __init__(self, parent_bar, title, kind=ToolbarPage.TYPE_PAGE_NORMAL):\n    \"\"\"Initialize the ribbon page.\n\n    Args:\n        parent_bar: Parent ModernToolbarBar (a RibbonBar).\n        title: Page title.\n        kind: Page type.\n    \"\"\"\n    ToolbarPage.__init__(self, parent_bar, title, kind)\n    RB.RibbonPage.__init__(self, parent_bar, wx.ID_ANY, self.title)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarPage-functions","title":"Methods:","text":""},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarPage.create_html_panel","title":"create_html_panel","text":"<pre><code>create_html_panel(title)\n</code></pre> <p>Create an HTML-capable panel within this page.</p> <p>Returns:</p> Name Type Description <code>ModernHtmlPanel</code> <p>The HTML panel wrapper.</p> Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def create_html_panel(self, title):\n    \"\"\"Create an HTML-capable panel within this page.\n\n    Returns:\n        ModernHtmlPanel: The HTML panel wrapper.\n    \"\"\"\n    p = self.create_panel(title)\n    return ModernHtmlPanel(self, p)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarPage.create_panel","title":"create_panel","text":"<pre><code>create_panel(title, kind=TYPE_PANEL_TOOLBAR)\n</code></pre> <p>Create a ribbon panel within this page.</p> <p>Returns:</p> Name Type Description <code>ModernToolbarPanel</code> <p>The newly created panel.</p> Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def create_panel(self, title, kind=ToolbarPanel.TYPE_PANEL_TOOLBAR):\n    \"\"\"Create a ribbon panel within this page.\n\n    Returns:\n        ModernToolbarPanel: The newly created panel.\n    \"\"\"\n    return ModernToolbarPanel(self, title, kind)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarPanel","title":"ModernToolbarPanel","text":"<pre><code>ModernToolbarPanel(parent_page, title, kind=TYPE_PANEL_TOOLBAR)\n</code></pre> <p>               Bases: <code>ToolbarPanel</code>, <code>RibbonPanel</code></p> <p>A ribbon panel that holds a RibbonToolBar or RibbonButtonBar.</p> <p>Initialize the ribbon panel.</p> <p>Parameters:</p> Name Type Description Default <code>parent_page</code> <p>Parent ModernToolbarPage (a RibbonPage).</p> required <code>title</code> <p>Panel title.</p> required <code>kind</code> <p>Panel type; determines whether to use a toolbar or button bar layout.</p> <code>TYPE_PANEL_TOOLBAR</code> Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def __init__(self, parent_page, title, kind=ToolbarPanel.TYPE_PANEL_TOOLBAR):\n    \"\"\"Initialize the ribbon panel.\n\n    Args:\n        parent_page: Parent ModernToolbarPage (a RibbonPage).\n        title: Panel title.\n        kind: Panel type; determines whether to use a toolbar\n            or button bar layout.\n    \"\"\"\n    RB.RibbonPanel.__init__(\n        self,\n        parent_page,\n        wx.ID_ANY,\n        title,\n        wx.NullBitmap,\n        wx.DefaultPosition,\n        wx.DefaultSize,\n        RB.RIBBON_PANEL_NO_AUTO_MINIMISE,\n    )\n    ToolbarPanel.__init__(self, parent_page, title, kind)\n\n    if self.kind == ToolbarPanel.TYPE_PANEL_TOOLBAR:\n        self.toolbar = RB.RibbonToolBar(self)\n    elif self.kind == ToolbarPanel.TYPE_PANEL_BUTTONBAR:\n        self.toolbar = RB.RibbonButtonBar(self)\n\n    self.lock = False\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarPanel-functions","title":"Methods:","text":""},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarPanel.Append","title":"Append","text":"<pre><code>Append(b)\n</code></pre> <p>Add a button to the underlying ribbon toolbar/button bar.</p> <p>Parameters:</p> Name Type Description Default <code>b</code> <p>A ModernToolbarButton to add.</p> required Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def Append(self, b):\n    \"\"\"Add a button to the underlying ribbon toolbar/button bar.\n\n    Args:\n        b: A ModernToolbarButton to add.\n    \"\"\"\n    if self.kind == ToolbarPanel.TYPE_PANEL_TOOLBAR:\n        if b.kind == ToolbarButton.TYPE_SIMPLE:\n            self.toolbar.AddSimpleTool(b.id, b.bitmap, b.title)\n        elif b.kind == ToolbarButton.TYPE_DROPDOWN:\n            self.toolbar.AddDropdownTool(b.id, b.bitmap, b.title)\n        elif b.kind == ToolbarButton.TYPE_HYBRID:\n            self.toolbar.AddHybridTool(b.id, b.bitmap, b.title)\n        elif b.kind == ToolbarButton.TYPE_TOOGLE:\n            self.toolbar.AddToggleTool(b.id, b.bitmap, b.title)\n        elif b.kind == ToolbarButton.TYPE_PANEL:\n            pass\n        elif b.kind == ToolbarButton.TYPE_SEPARATOR:\n            self.toolbar.AddSeparator()\n    elif self.kind == ToolbarPanel.TYPE_PANEL_BUTTONBAR:\n        if b.kind == ToolbarButton.TYPE_SIMPLE:\n            self.toolbar.AddSimpleButton(b.id, b.title, b.bitmap, \"\")\n        elif b.kind == ToolbarButton.TYPE_DROPDOWN:\n            self.toolbar.AddDropdownButton(b.id, b.title, b.bitmap, \"\")\n        elif b.kind == ToolbarButton.TYPE_HYBRID:\n            self.toolbar.AddHybridButton(b.id, b.title, b.bitmap, \"\")\n        elif b.kind == ToolbarButton.TYPE_TOOGLE:\n            self.toolbar.AddToggleButton(b.id, b.title, b.bitmap, \"\")\n        elif b.kind == ToolbarButton.TYPE_PANEL:\n            pass\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarPanel.OnInternalIdle","title":"OnInternalIdle","text":"<pre><code>OnInternalIdle()\n</code></pre> <p>Handle idle event to update the toolbar UI state.</p> <p>Uses a lock to prevent re-entrant calls.</p> Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def OnInternalIdle(self):\n    \"\"\"Handle idle event to update the toolbar UI state.\n\n    Uses a lock to prevent re-entrant calls.\n    \"\"\"\n    if not self.lock:\n        self.lock = True\n        if self.toolbar:\n            self.toolbar.UpdateWindowUI(wx.UPDATE_UI_FROMIDLE)\n        self.lock = False\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarPanel.add_separator","title":"add_separator","text":"<pre><code>add_separator()\n</code></pre> <p>Append a visual separator to this ribbon panel.</p> <p>Returns:</p> Name Type Description <code>ModernToolbarButton</code> <p>The separator button object.</p> Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def add_separator(self):\n    \"\"\"Append a visual separator to this ribbon panel.\n\n    Returns:\n        ModernToolbarButton: The separator button object.\n    \"\"\"\n    b = ModernToolbarButton(\n        self, 0, \"\", None, None, kind=ToolbarButton.TYPE_SEPARATOR\n    )\n    self.Append(b)\n    return b\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ModernToolbarPanel.create_button","title":"create_button","text":"<pre><code>create_button(id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE)\n</code></pre> <p>Create and append a button to this ribbon panel.</p> <p>Returns:</p> Name Type Description <code>ModernToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def create_button(\n    self,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=ToolbarButton.TYPE_SIMPLE,\n):\n    \"\"\"Create and append a button to this ribbon panel.\n\n    Returns:\n        ModernToolbarButton: The newly created button.\n    \"\"\"\n    b = ModernToolbarButton(self, id, title, bitmap, bitmap_disabled, kind)\n    self.Append(b)\n    return b\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarBar","title":"ToolbarBar","text":"<pre><code>ToolbarBar(parent, gui_style)\n</code></pre> <p>Base class for all menus and toolbars connected to the top frame window.</p> <p>Manages a collection of ToolbarPage objects and provides common operations like binding events and creating standard buttons.</p> <p>Initialize the toolbar bar.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window - the top wx.Frame derived object.</p> required <code>gui_style</code> <p>String description of the GUI interface. Based on this string, standard toolbar elements are created.</p> required Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, parent, gui_style):\n    \"\"\"Initialize the toolbar bar.\n\n    Args:\n        parent: Parent window - the top wx.Frame derived object.\n        gui_style: String description of the GUI interface. Based on\n            this string, standard toolbar elements are created.\n    \"\"\"\n    self.parent = parent\n    self.main_page = None\n    self.gui_style = gui_style\n\n    self.toolbars = {}\n    self.pages = []\n    self.user_panels = {}\n\n    self.standard_buttons = StandardButtons(self, gui_style)\n    self.standard_buttons.create_file_panel(self.main_page)\n    self.standard_buttons.create_edit_panel(self.main_page)\n    self.standard_buttons.create_operations_panel(self.main_page)\n    self.standard_buttons.create_browse_panel(self.main_page)\n    self.standard_buttons.create_address_panel(self.main_page)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarBar-functions","title":"Methods:","text":""},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarBar.append","title":"append","text":"<pre><code>append(title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>Append a page to the toolbar, or return an existing one.</p> <p>If a page with the same name and kind already exists, it is returned instead of creating a duplicate.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Page title.</p> required <code>kind</code> <p>Type of page; one of ToolbarPage.TYPE_PAGE_* constants.</p> <code>TYPE_PAGE_NORMAL</code> <p>Returns:</p> Name Type Description <code>ToolbarPage</code> <p>The existing or newly created page.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def append(self, title, kind=ToolbarPage.TYPE_PAGE_NORMAL):\n    \"\"\"Append a page to the toolbar, or return an existing one.\n\n    If a page with the same name and kind already exists, it is\n    returned instead of creating a duplicate.\n\n    Args:\n        title: Page title.\n        kind: Type of page; one of ToolbarPage.TYPE_PAGE_* constants.\n\n    Returns:\n        ToolbarPage: The existing or newly created page.\n    \"\"\"\n    for page in self.pages:\n        if page.name == title and page.kind == kind:\n            return page\n    p = self.create_page(title, kind)\n    if not self.main_page:\n        self.main_page = p\n    self.pages.append(p)\n    return p\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarBar.bind","title":"bind","text":"<pre><code>bind(fun, id=ID_ANY, e=None)\n</code></pre> <p>Bind an event handler to the toolbar's parent window.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <p>Handler function.</p> required <code>id</code> <p>Identifier of the event.</p> <code>ID_ANY</code> <code>e</code> <p>Event type; defaults to wx.EVT_MENU if not specified.</p> <code>None</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def bind(self, fun, id=wx.ID_ANY, e=None):\n    \"\"\"Bind an event handler to the toolbar's parent window.\n\n    Args:\n        fun: Handler function.\n        id: Identifier of the event.\n        e: Event type; defaults to wx.EVT_MENU if not specified.\n    \"\"\"\n    if e:\n        self.parent.Bind(e, fun, id=id)\n    else:\n        self.parent.Bind(wx.EVT_MENU, fun, id=id)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarBar.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Close/destroy the toolbar.</p> <p>Override in subclasses to perform cleanup.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def close(self):\n    \"\"\"Close/destroy the toolbar.\n\n    Override in subclasses to perform cleanup.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarBar.create","title":"create","text":"<pre><code>create()\n</code></pre> <p>Create/realize the toolbar.</p> <p>Override in subclasses to perform actual widget creation.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create(self):\n    \"\"\"Create/realize the toolbar.\n\n    Override in subclasses to perform actual widget creation.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarBar.create_html_win","title":"create_html_win","text":"<pre><code>create_html_win(toolbar_page, address_or_parser, parameters, callback=None)\n</code></pre> <p>Create an HTML page embedded in the toolbar.</p> <p>Not all toolbar types support this feature.</p> <p>Parameters:</p> Name Type Description Default <code>toolbar_page</code> <p>Name of the toolbar page to host the HTML. Use '__' to separate page and panel names, e.g. 'PageName__PanelName'.</p> required <code>address_or_parser</code> <p>Address of the HTTP page or a parser.</p> required <code>parameters</code> <p>Parameters for the HTTP request.</p> required <code>callback</code> <p>Optional callback invoked after initialization.</p> <code>None</code> <p>Returns:</p> Type Description <p>SchPage or None: The created page, or None on failure.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_html_win(\n    self, toolbar_page, address_or_parser, parameters, callback=None\n):\n    \"\"\"Create an HTML page embedded in the toolbar.\n\n    Not all toolbar types support this feature.\n\n    Args:\n        toolbar_page: Name of the toolbar page to host the HTML.\n            Use '__' to separate page and panel names,\n            e.g. 'PageName__PanelName'.\n        address_or_parser: Address of the HTTP page or a parser.\n        parameters: Parameters for the HTTP request.\n        callback: Optional callback invoked after initialization.\n\n    Returns:\n        SchPage or None: The created page, or None on failure.\n    \"\"\"\n    page = None\n    if not toolbar_page:\n        u_name = \"main tools\"\n        page_name = _(\"main tools\")\n        panel_name = \"main tools\"\n    else:\n        u_name = toolbar_page\n        names = toolbar_page.split(\"__\")\n        if len(names) &gt; 1:\n            page_name = names[0].replace(\"_\", \" \")\n            panel_name = names[1].replace(\"_\", \" \")\n        else:\n            page_name = toolbar_page.replace(\"_\", \" \")\n            panel_name = page_name\n\n    if u_name in self.user_panels:\n        panel = self.user_panels[u_name]\n    else:\n        page = self._find_page_by_title(page_name)\n        if page is None:\n            page = self.append(page_name)\n        panel = page.create_html_panel(panel_name)\n\n    if panel:\n        dx = panel.get_width() + 3\n        dy = panel.get_height() + 5\n\n        page2 = SchPage(\n            panel.get_window(),\n            address_or_parser,\n            parameters,\n            size=wx.Size(dx, dy),\n            pos=wx.Point(2, 2),\n        )\n        best = page2.body.calculate_best_size()\n        page2.SetSize(wx.Size(int(best[0]), int(best[1])))\n        panel.set_page(page2)\n        self.user_panels[u_name] = panel\n        page2.body.toolbar_interface = self\n        page2.body.toolbar_interface_page = page\n\n        def init_page(callback):\n            page2.init_frame()\n            page2.activate_page()\n            page2.Update()\n\n        wx.CallAfter(init_page, callback)\n\n        return page2\n\n    return None\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarBar.create_page","title":"create_page","text":"<pre><code>create_page(title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>Create and return a new ToolbarPage.</p> <p>Override in derived classes to return a subclass of ToolbarPage.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Page title.</p> required <code>kind</code> <p>Type of page; one of ToolbarPage.TYPE_PAGE_* constants.</p> <code>TYPE_PAGE_NORMAL</code> <p>Returns:</p> Name Type Description <code>ToolbarPage</code> <p>The newly created page.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_page(self, title, kind=ToolbarPage.TYPE_PAGE_NORMAL):\n    \"\"\"Create and return a new ToolbarPage.\n\n    Override in derived classes to return a subclass of ToolbarPage.\n\n    Args:\n        title: Page title.\n        kind: Type of page; one of ToolbarPage.TYPE_PAGE_* constants.\n\n    Returns:\n        ToolbarPage: The newly created page.\n    \"\"\"\n    return ToolbarPage(self, title, kind)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarBar.create_panel_in_main_page","title":"create_panel_in_main_page","text":"<pre><code>create_panel_in_main_page(title, kind)\n</code></pre> <p>Create a panel in the main page.</p> <p>If no main page exists yet, one is created automatically.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>New panel's title.</p> required <code>kind</code> <p>Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.</p> required <p>Returns:</p> Name Type Description <code>ToolbarPanel</code> <p>The newly created panel.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_panel_in_main_page(self, title, kind):\n    \"\"\"Create a panel in the main page.\n\n    If no main page exists yet, one is created automatically.\n\n    Args:\n        title: New panel's title.\n        kind: Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.\n\n    Returns:\n        ToolbarPanel: The newly created panel.\n    \"\"\"\n    if not self.main_page:\n        self.append(\"main tools\")\n    return self.main_page.create_panel(title, kind)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarBar.new_child_page","title":"new_child_page","text":"<pre><code>new_child_page(address_or_parser, title='', param=None, callback=None)\n</code></pre> <p>Create a new child page.</p> <p>For toolbar, the child page is transferred to the desktop window.</p> <p>Parameters:</p> Name Type Description Default <code>address_or_parser</code> <p>Address of the HTTP page or a parser.</p> required <code>title</code> <p>Page title.</p> <code>''</code> <code>param</code> <p>Optional parameters.</p> <code>None</code> <code>callback</code> <p>Optional callback.</p> <code>None</code> <p>Returns:</p> Type Description <p>The result of new_main_page on the top window.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def new_child_page(self, address_or_parser, title=\"\", param=None, callback=None):\n    \"\"\"Create a new child page.\n\n    For toolbar, the child page is transferred to the desktop window.\n\n    Args:\n        address_or_parser: Address of the HTTP page or a parser.\n        title: Page title.\n        param: Optional parameters.\n        callback: Optional callback.\n\n    Returns:\n        The result of new_main_page on the top window.\n    \"\"\"\n    return wx.GetApp().GetTopWindow().new_main_page(address_or_parser, title, param)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarBar.remove_page","title":"remove_page","text":"<pre><code>remove_page(title)\n</code></pre> <p>Remove the page with the specified title from the toolbar.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Title of the page to remove.</p> required Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def remove_page(self, title):\n    \"\"\"Remove the page with the specified title from the toolbar.\n\n    Args:\n        title: Title of the page to remove.\n    \"\"\"\n    # Remove from pages list\n    for page in self.pages:\n        if page.title == title:\n            self.pages.remove(page)\n            break\n\n    # If the main page was removed, pick a new one\n    if self.main_page and self.main_page.title == title:\n        self.main_page = self.pages[0] if self.pages else None\n\n    # Remove associated user panels (iterate over a static list of keys\n    # to avoid \"dictionary changed size during iteration\" errors)\n    for key in list(self.user_panels.keys()):\n        panel = self.user_panels[key]\n        if panel.page.title == title:\n            del self.user_panels[key]\n            return\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarBar.un_bind","title":"un_bind","text":"<pre><code>un_bind(id=ID_ANY, e=None)\n</code></pre> <p>Unbind an event handler from the toolbar's parent window.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>Identifier of the event.</p> <code>ID_ANY</code> <code>e</code> <p>Event type; defaults to wx.EVT_MENU if not specified.</p> <code>None</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def un_bind(self, id=wx.ID_ANY, e=None):\n    \"\"\"Unbind an event handler from the toolbar's parent window.\n\n    Args:\n        id: Identifier of the event.\n        e: Event type; defaults to wx.EVT_MENU if not specified.\n    \"\"\"\n    if e:\n        self.parent.Unbind(e, id=id)\n    else:\n        self.parent.Unbind(wx.EVT_MENU, id=id)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarButton","title":"ToolbarButton","text":"<pre><code>ToolbarButton(\n    parent_panel, id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE\n)\n</code></pre> <p>Represents a single button (or separator) on a toolbar panel.</p> <p>This is an abstract data model; the actual wx widget is created by the concrete toolbar implementation.</p> Button type constants <p>TYPE_SIMPLE = 0     - Simple push button. TYPE_DROPDOWN = 1   - Button with a dropdown menu. TYPE_HYBRID = 2     - Hybrid button (part button, part dropdown). TYPE_TOOGLE = 3     - Toggle button. TYPE_PANEL = 4      - Embedded panel. TYPE_SEPARATOR = 5  - Visual separator.</p> <p>Create a new toolbar button.</p> <p>Parameters:</p> Name Type Description Default <code>parent_panel</code> <p>Parent ToolbarPanel object.</p> required <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title (display text).</p> required <code>bitmap</code> <p>Button bitmap for the normal state.</p> <code>None</code> <code>bitmap_disabled</code> <p>Button bitmap for the disabled state. Defaults to wx.NullBitmap when bitmap is provided.</p> <code>None</code> <code>kind</code> <p>Type of button; one of the TYPE_* class constants.</p> <code>TYPE_SIMPLE</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(\n    self,\n    parent_panel,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=TYPE_SIMPLE,\n):\n    \"\"\"Create a new toolbar button.\n\n    Args:\n        parent_panel: Parent ToolbarPanel object.\n        id: An integer by which the tool may be identified.\n        title: Button title (display text).\n        bitmap: Button bitmap for the normal state.\n        bitmap_disabled: Button bitmap for the disabled state.\n            Defaults to wx.NullBitmap when bitmap is provided.\n        kind: Type of button; one of the TYPE_* class constants.\n    \"\"\"\n    self.parent_panel = parent_panel\n    self.id = id\n    self.title = title\n    self.bitmap = bitmap\n    if bitmap_disabled is None and bitmap is not None:\n        self.bitmap_disabled = wx.NullBitmap\n    else:\n        self.bitmap_disabled = bitmap_disabled\n    self.kind = kind\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarButton-functions","title":"Methods:","text":""},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarPage","title":"ToolbarPage","text":"<pre><code>ToolbarPage(parent_bar, title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>A page (tab) within a toolbar bar, containing one or more panels.</p> Page type constants <p>TYPE_PAGE_NORMAL = 0  - Standard page.</p> <p>Create a new toolbar page.</p> <p>Parameters:</p> Name Type Description Default <code>parent_bar</code> <p>Parent ToolbarBar object.</p> required <code>title</code> <p>Page title (will be translated via wx.GetTranslation).</p> required <code>kind</code> <p>Type of page; one of the TYPE_PAGE_* class constants.</p> <code>TYPE_PAGE_NORMAL</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, parent_bar, title, kind=TYPE_PAGE_NORMAL):\n    \"\"\"Create a new toolbar page.\n\n    Args:\n        parent_bar: Parent ToolbarBar object.\n        title: Page title (will be translated via wx.GetTranslation).\n        kind: Type of page; one of the TYPE_PAGE_* class constants.\n    \"\"\"\n    self.parent_bar = parent_bar\n    self.name = title\n    self.title = _(title)\n    self.kind = kind\n    self.panels = []\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarPage-functions","title":"Methods:","text":""},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarPage.append","title":"append","text":"<pre><code>append(title, kind=TYPE_PANEL_BUTTONBAR)\n</code></pre> <p>Create a new panel and append it to this page.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Panel title.</p> required <code>kind</code> <p>Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.</p> <code>TYPE_PANEL_BUTTONBAR</code> <p>Returns:</p> Name Type Description <code>ToolbarPanel</code> <p>The newly created panel.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def append(self, title, kind=ToolbarPanel.TYPE_PANEL_BUTTONBAR):\n    \"\"\"Create a new panel and append it to this page.\n\n    Args:\n        title: Panel title.\n        kind: Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.\n\n    Returns:\n        ToolbarPanel: The newly created panel.\n    \"\"\"\n    p = self.create_panel(title, kind)\n    self.panels.append(p)\n    return p\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarPage.create_html_panel","title":"create_html_panel","text":"<pre><code>create_html_panel(title)\n</code></pre> <p>Create an HTML-capable panel within this page.</p> <p>Override in derived classes to return a BaseHtmlPanel subclass.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Panel title.</p> required <p>Returns:</p> Type Description <p>BaseHtmlPanel or None: The panel, or None if not supported.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_html_panel(self, title):\n    \"\"\"Create an HTML-capable panel within this page.\n\n    Override in derived classes to return a BaseHtmlPanel subclass.\n\n    Args:\n        title: Panel title.\n\n    Returns:\n        BaseHtmlPanel or None: The panel, or None if not supported.\n    \"\"\"\n    return None\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarPage.create_panel","title":"create_panel","text":"<pre><code>create_panel(title, kind)\n</code></pre> <p>Create and return a new ToolbarPanel for this page.</p> <p>Override in derived classes to return a subclass of ToolbarPanel.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Panel title.</p> required <code>kind</code> <p>Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.</p> required <p>Returns:</p> Name Type Description <code>ToolbarPanel</code> <p>The newly created panel.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_panel(self, title, kind):\n    \"\"\"Create and return a new ToolbarPanel for this page.\n\n    Override in derived classes to return a subclass of ToolbarPanel.\n\n    Args:\n        title: Panel title.\n        kind: Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.\n\n    Returns:\n        ToolbarPanel: The newly created panel.\n    \"\"\"\n    return ToolbarPanel(self, title, kind=ToolbarPanel.TYPE_PANEL_BUTTONBAR)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarPanel","title":"ToolbarPanel","text":"<pre><code>ToolbarPanel(parent_page, title, kind=TYPE_PANEL_BUTTONBAR)\n</code></pre> <p>A panel within a toolbar page that holds a group of buttons.</p> Panel type constants <p>TYPE_PANEL_BUTTONBAR = 0  - Button bar layout. TYPE_PANEL_TOOLBAR = 1    - Toolbar layout. TYPE_PANEL_PANELBAR = 2   - Custom panel layout.</p> <p>Create a new toolbar panel.</p> <p>Parameters:</p> Name Type Description Default <code>parent_page</code> <p>Parent ToolbarPage object.</p> required <code>title</code> <p>Panel title.</p> required <code>kind</code> <p>Type of panel; one of the TYPE_PANEL_* class constants.</p> <code>TYPE_PANEL_BUTTONBAR</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, parent_page, title, kind=TYPE_PANEL_BUTTONBAR):\n    \"\"\"Create a new toolbar panel.\n\n    Args:\n        parent_page: Parent ToolbarPage object.\n        title: Panel title.\n        kind: Type of panel; one of the TYPE_PANEL_* class constants.\n    \"\"\"\n    self.parent_page = parent_page\n    self.title = title\n    self.kind = kind\n    self.buttons = []\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarPanel-functions","title":"Methods:","text":""},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarPanel.add_dropdown_tool","title":"add_dropdown_tool","text":"<pre><code>add_dropdown_tool(id, title, bitmaps)\n</code></pre> <p>Append a dropdown button to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_dropdown_tool(self, id, title, bitmaps):\n    \"\"\"Append a dropdown button to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_DROPDOWN)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarPanel.add_hybrid_tool","title":"add_hybrid_tool","text":"<pre><code>add_hybrid_tool(id, title, bitmaps)\n</code></pre> <p>Append a hybrid button (button + dropdown) to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_hybrid_tool(self, id, title, bitmaps):\n    \"\"\"Append a hybrid button (button + dropdown) to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_HYBRID)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarPanel.add_separator","title":"add_separator","text":"<pre><code>add_separator()\n</code></pre> <p>Append a visual separator to the panel.</p> <p>The default implementation does nothing; override in subclasses.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_separator(self):\n    \"\"\"Append a visual separator to the panel.\n\n    The default implementation does nothing; override in subclasses.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarPanel.add_simple_tool","title":"add_simple_tool","text":"<pre><code>add_simple_tool(id, title, bitmaps)\n</code></pre> <p>Append a simple push button to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_simple_tool(self, id, title, bitmaps):\n    \"\"\"Append a simple push button to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_SIMPLE)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarPanel.add_toogle_tool","title":"add_toogle_tool","text":"<pre><code>add_toogle_tool(id, title, bitmaps)\n</code></pre> <p>Append a toggle button to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_toogle_tool(self, id, title, bitmaps):\n    \"\"\"Append a toggle button to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_TOOGLE)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarPanel.append","title":"append","text":"<pre><code>append(id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE)\n</code></pre> <p>Create a button and append it to this panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmap</code> <p>Button bitmap for the normal state.</p> <code>None</code> <code>bitmap_disabled</code> <p>Button bitmap for the disabled state.</p> <code>None</code> <code>kind</code> <p>Type of button; one of ToolbarButton.TYPE_* constants.</p> <code>TYPE_SIMPLE</code> <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created and appended button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def append(\n    self,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=ToolbarButton.TYPE_SIMPLE,\n):\n    \"\"\"Create a button and append it to this panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmap: Button bitmap for the normal state.\n        bitmap_disabled: Button bitmap for the disabled state.\n        kind: Type of button; one of ToolbarButton.TYPE_* constants.\n\n    Returns:\n        ToolbarButton: The newly created and appended button.\n    \"\"\"\n    button = self.create_button(id, title, bitmap, bitmap_disabled, kind)\n    self.buttons.append(button)\n    return button\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.ToolbarPanel.create_button","title":"create_button","text":"<pre><code>create_button(id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE)\n</code></pre> <p>Create and return a new ToolbarButton.</p> <p>Override in derived classes to return a subclass of ToolbarButton.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmap</code> <p>Button bitmap for the normal state.</p> <code>None</code> <code>bitmap_disabled</code> <p>Button bitmap for the disabled state.</p> <code>None</code> <code>kind</code> <p>Type of button; one of ToolbarButton.TYPE_* constants.</p> <code>TYPE_SIMPLE</code> <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_button(\n    self,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=ToolbarButton.TYPE_SIMPLE,\n):\n    \"\"\"Create and return a new ToolbarButton.\n\n    Override in derived classes to return a subclass of ToolbarButton.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmap: Button bitmap for the normal state.\n        bitmap_disabled: Button bitmap for the disabled state.\n        kind: Type of button; one of ToolbarButton.TYPE_* constants.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    return ToolbarButton(self, id, title, bitmap, bitmap_disabled, kind)\n</code></pre>"},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar-functions","title":"Functions:","text":""},{"location":"api/toolbar_moderntoolbar/#pytigon_gui.toolbar.moderntoolbar.like_primary","title":"like_primary","text":"<pre><code>like_primary(primary_hsl, h, s, l, x=None)\n</code></pre> <p>Custom colour blending function for MSW ribbon style.</p> <p>Blends a primary colour with the system 3D face colour to produce a muted, system-integrated look.</p> <p>Parameters:</p> Name Type Description Default <code>primary_hsl</code> <p>Original LikePrimary function reference.</p> required <code>h</code> <p>Hue component.</p> required <code>s</code> <p>Saturation component.</p> required <code>l</code> <p>Lightness component.</p> required <code>x</code> <p>Optional extra parameter passed to the original function.</p> <code>None</code> <p>Returns:</p> Type Description <p>wx.Colour: The blended colour.</p> Source code in <code>pytigon_gui/toolbar/moderntoolbar.py</code> <pre><code>def like_primary(primary_hsl, h, s, l, x=None):\n    \"\"\"Custom colour blending function for MSW ribbon style.\n\n    Blends a primary colour with the system 3D face colour to produce\n    a muted, system-integrated look.\n\n    Args:\n        primary_hsl: Original LikePrimary function reference.\n        h: Hue component.\n        s: Saturation component.\n        l: Lightness component.\n        x: Optional extra parameter passed to the original function.\n\n    Returns:\n        wx.Colour: The blended colour.\n    \"\"\"\n    if x is not None:\n        c1 = ORG_LIKE_PRIMARY(primary_hsl, h, 0, l * 1.5, x)\n    else:\n        c1 = ORG_LIKE_PRIMARY(primary_hsl, h, 0, l * 1.5)\n    c2 = wx.SystemSettings.GetColour(wx.SYS_COLOUR_3DFACE)\n    r2 = c2.Red()\n    g2 = c2.Green()\n    b2 = c2.Blue()\n    if r2 == 0:\n        r2 = 1\n    if g2 == 0:\n        g2 = 1\n    if b2 == 0:\n        b2 = 1\n    m = ((1.0 * c1.Red()) / r2 + (1.0 * c1.Green()) / g2 + (1.0 * c1.Blue()) / b2) / 3\n    x1 = min(int(c2.Red() * m), 255)\n    x2 = min(int(c2.Green() * m), 255)\n    x3 = min(int(c2.Blue() * m), 255)\n    return wx.Colour(x1, x2, x3)\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/","title":"toolbar.standardtoolbar \u2013 Standard Toolbar","text":"<p>Classic horizontal toolbar with icon+text buttons, separators, and dropdown menus.</p>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar","title":"standardtoolbar","text":"<p>Standard (classic) toolbar implementation.</p> <p>Provides a simple wx.ToolBar-based toolbar.</p>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar-attributes","title":"Attributes","text":""},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_BOOKMARK_ADD","title":"ID_BOOKMARK_ADD  <code>module-attribute</code>","text":"<pre><code>ID_BOOKMARK_ADD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_CLEAR_HISTORY","title":"ID_CLEAR_HISTORY  <code>module-attribute</code>","text":"<pre><code>ID_CLEAR_HISTORY = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_CLOSETAB","title":"ID_CLOSETAB  <code>module-attribute</code>","text":"<pre><code>ID_CLOSETAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_COPY","title":"ID_COPY  <code>module-attribute</code>","text":"<pre><code>ID_COPY = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_CUT","title":"ID_CUT  <code>module-attribute</code>","text":"<pre><code>ID_CUT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_DOWNLOAD_OPTIONS","title":"ID_DOWNLOAD_OPTIONS  <code>module-attribute</code>","text":"<pre><code>ID_DOWNLOAD_OPTIONS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_END","title":"ID_END  <code>module-attribute</code>","text":"<pre><code>ID_END = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_EXIT","title":"ID_EXIT  <code>module-attribute</code>","text":"<pre><code>ID_EXIT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_FIND","title":"ID_FIND  <code>module-attribute</code>","text":"<pre><code>ID_FIND = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_GENERAL_OPTIONS","title":"ID_GENERAL_OPTIONS  <code>module-attribute</code>","text":"<pre><code>ID_GENERAL_OPTIONS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_GOTODESKTOP","title":"ID_GOTODESKTOP  <code>module-attribute</code>","text":"<pre><code>ID_GOTODESKTOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_GOTOFOOTER","title":"ID_GOTOFOOTER  <code>module-attribute</code>","text":"<pre><code>ID_GOTOFOOTER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_GOTOHEADER","title":"ID_GOTOHEADER  <code>module-attribute</code>","text":"<pre><code>ID_GOTOHEADER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_GOTOPANEL","title":"ID_GOTOPANEL  <code>module-attribute</code>","text":"<pre><code>ID_GOTOPANEL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_HELP","title":"ID_HELP  <code>module-attribute</code>","text":"<pre><code>ID_HELP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_HISTORY_SHOW","title":"ID_HISTORY_SHOW  <code>module-attribute</code>","text":"<pre><code>ID_HISTORY_SHOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_LOAD","title":"ID_LOAD  <code>module-attribute</code>","text":"<pre><code>ID_LOAD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_NEXTPAGE","title":"ID_NEXTPAGE  <code>module-attribute</code>","text":"<pre><code>ID_NEXTPAGE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_NEXTTAB","title":"ID_NEXTTAB  <code>module-attribute</code>","text":"<pre><code>ID_NEXTTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_PAGE_BLOCK","title":"ID_PAGE_BLOCK  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_BLOCK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_PAGE_SOURCE","title":"ID_PAGE_SOURCE  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_SOURCE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_PAGE_VIEW","title":"ID_PAGE_VIEW  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_VIEW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_PASTE","title":"ID_PASTE  <code>module-attribute</code>","text":"<pre><code>ID_PASTE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_PREVPAGE","title":"ID_PREVPAGE  <code>module-attribute</code>","text":"<pre><code>ID_PREVPAGE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_PREVTAB","title":"ID_PREVTAB  <code>module-attribute</code>","text":"<pre><code>ID_PREVTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_PRINT","title":"ID_PRINT  <code>module-attribute</code>","text":"<pre><code>ID_PRINT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_PRINT_PREVIEW","title":"ID_PRINT_PREVIEW  <code>module-attribute</code>","text":"<pre><code>ID_PRINT_PREVIEW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_PRINT_SETTINGS","title":"ID_PRINT_SETTINGS  <code>module-attribute</code>","text":"<pre><code>ID_PRINT_SETTINGS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_REDO","title":"ID_REDO  <code>module-attribute</code>","text":"<pre><code>ID_REDO = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_REFRESHTAB","title":"ID_REFRESHTAB  <code>module-attribute</code>","text":"<pre><code>ID_REFRESHTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_REPLACE","title":"ID_REPLACE  <code>module-attribute</code>","text":"<pre><code>ID_REPLACE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_RESET","title":"ID_RESET  <code>module-attribute</code>","text":"<pre><code>ID_RESET = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_RETVALUE","title":"ID_RETVALUE  <code>module-attribute</code>","text":"<pre><code>ID_RETVALUE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_SAVE","title":"ID_SAVE  <code>module-attribute</code>","text":"<pre><code>ID_SAVE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_SAVE_AS","title":"ID_SAVE_AS  <code>module-attribute</code>","text":"<pre><code>ID_SAVE_AS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_SELALL","title":"ID_SELALL  <code>module-attribute</code>","text":"<pre><code>ID_SELALL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_SHOWDESKTOP","title":"ID_SHOWDESKTOP  <code>module-attribute</code>","text":"<pre><code>ID_SHOWDESKTOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_SHOWFOOTER","title":"ID_SHOWFOOTER  <code>module-attribute</code>","text":"<pre><code>ID_SHOWFOOTER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_SHOWHEADER","title":"ID_SHOWHEADER  <code>module-attribute</code>","text":"<pre><code>ID_SHOWHEADER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_SHOWPANEL","title":"ID_SHOWPANEL  <code>module-attribute</code>","text":"<pre><code>ID_SHOWPANEL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_SHOWSTATUSBAR","title":"ID_SHOWSTATUSBAR  <code>module-attribute</code>","text":"<pre><code>ID_SHOWSTATUSBAR = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_SHOWTOOLBAR1","title":"ID_SHOWTOOLBAR1  <code>module-attribute</code>","text":"<pre><code>ID_SHOWTOOLBAR1 = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_SHOWTOOLBAR2","title":"ID_SHOWTOOLBAR2  <code>module-attribute</code>","text":"<pre><code>ID_SHOWTOOLBAR2 = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_SHOW_BOOKMARKS","title":"ID_SHOW_BOOKMARKS  <code>module-attribute</code>","text":"<pre><code>ID_SHOW_BOOKMARKS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_SHOW_DOWNLOAD","title":"ID_SHOW_DOWNLOAD  <code>module-attribute</code>","text":"<pre><code>ID_SHOW_DOWNLOAD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_START","title":"ID_START  <code>module-attribute</code>","text":"<pre><code>ID_START = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_TASKBAR_CLOSE","title":"ID_TASKBAR_CLOSE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_CLOSE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_TASKBAR_HIDE","title":"ID_TASKBAR_HIDE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_HIDE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_TASKBAR_SHOW","title":"ID_TASKBAR_SHOW  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_SHOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_TASKBAR_TOGGLE","title":"ID_TASKBAR_TOGGLE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_TOGGLE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_UNDO","title":"ID_UNDO  <code>module-attribute</code>","text":"<pre><code>ID_UNDO = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_WEB_ADDBOOKMARK","title":"ID_WEB_ADDBOOKMARK  <code>module-attribute</code>","text":"<pre><code>ID_WEB_ADDBOOKMARK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_WEB_BACK","title":"ID_WEB_BACK  <code>module-attribute</code>","text":"<pre><code>ID_WEB_BACK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_WEB_DOWN","title":"ID_WEB_DOWN  <code>module-attribute</code>","text":"<pre><code>ID_WEB_DOWN = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_WEB_EDIT","title":"ID_WEB_EDIT  <code>module-attribute</code>","text":"<pre><code>ID_WEB_EDIT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_WEB_FORWARD","title":"ID_WEB_FORWARD  <code>module-attribute</code>","text":"<pre><code>ID_WEB_FORWARD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_WEB_NEW_WINDOW","title":"ID_WEB_NEW_WINDOW  <code>module-attribute</code>","text":"<pre><code>ID_WEB_NEW_WINDOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_WEB_REFRESH","title":"ID_WEB_REFRESH  <code>module-attribute</code>","text":"<pre><code>ID_WEB_REFRESH = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_WEB_SOURCE","title":"ID_WEB_SOURCE  <code>module-attribute</code>","text":"<pre><code>ID_WEB_SOURCE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_WEB_STOP","title":"ID_WEB_STOP  <code>module-attribute</code>","text":"<pre><code>ID_WEB_STOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_WEB_UP","title":"ID_WEB_UP  <code>module-attribute</code>","text":"<pre><code>ID_WEB_UP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ID_ZOOM","title":"ID_ZOOM  <code>module-attribute</code>","text":"<pre><code>ID_ZOOM = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar._","title":"_  <code>module-attribute</code>","text":"<pre><code>_ = wx.GetTranslation\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar-classes","title":"Classes","text":""},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.StandardToolbarBar","title":"StandardToolbarBar","text":"<pre><code>StandardToolbarBar(parent, gui_style)\n</code></pre> <p>               Bases: <code>ToolbarBar</code></p> <p>The top-level standard toolbar bar wrapping a wx.ToolBar.</p> <p>Initialize the standard toolbar.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent wx.Frame.</p> required <code>gui_style</code> <p>GUI style string.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbar.py</code> <pre><code>def __init__(self, parent, gui_style):\n    \"\"\"Initialize the standard toolbar.\n\n    Args:\n        parent: Parent wx.Frame.\n        gui_style: GUI style string.\n    \"\"\"\n    self.standard_tool_bar = parent.CreateToolBar()\n    super().__init__(parent, gui_style)\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.StandardToolbarBar-functions","title":"Methods:","text":""},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.StandardToolbarBar.bind","title":"bind","text":"<pre><code>bind(fun, id=ID_ANY, e=None)\n</code></pre> <p>Bind an event handler to the parent window.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <p>Handler function.</p> required <code>id</code> <p>Event identifier.</p> <code>ID_ANY</code> <code>e</code> <p>Event type; defaults to wx.EVT_MENU.</p> <code>None</code> Source code in <code>pytigon_gui/toolbar/standardtoolbar.py</code> <pre><code>def bind(self, fun, id=wx.ID_ANY, e=None):\n    \"\"\"Bind an event handler to the parent window.\n\n    Args:\n        fun: Handler function.\n        id: Event identifier.\n        e: Event type; defaults to wx.EVT_MENU.\n    \"\"\"\n    if e:\n        self.parent.Bind(e, fun, id=id)\n    else:\n        self.parent.Bind(wx.EVT_MENU, fun, id=id)\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.StandardToolbarBar.bind_dropdown","title":"bind_dropdown","text":"<pre><code>bind_dropdown(fun, id)\n</code></pre> <p>Bind a dropdown menu event handler.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <p>Handler function.</p> required <code>id</code> <p>Event identifier.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbar.py</code> <pre><code>def bind_dropdown(self, fun, id):\n    \"\"\"Bind a dropdown menu event handler.\n\n    Args:\n        fun: Handler function.\n        id: Event identifier.\n    \"\"\"\n    self.parent.Bind(wx.EVT_MENU, fun, id=id)\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.StandardToolbarBar.bind_ui","title":"bind_ui","text":"<pre><code>bind_ui(fun, id=ID_ANY)\n</code></pre> <p>Bind a UI update event handler.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <p>Handler function.</p> required <code>id</code> <p>Event identifier.</p> <code>ID_ANY</code> Source code in <code>pytigon_gui/toolbar/standardtoolbar.py</code> <pre><code>def bind_ui(self, fun, id=wx.ID_ANY):\n    \"\"\"Bind a UI update event handler.\n\n    Args:\n        fun: Handler function.\n        id: Event identifier.\n    \"\"\"\n    self.parent.Bind(wx.EVT_UPDATE_UI, fun, id=id)\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.StandardToolbarBar.create_page","title":"create_page","text":"<pre><code>create_page(title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>Create a new standard toolbar page.</p> <p>Returns:</p> Name Type Description <code>StandardToolbarPage</code> <p>The newly created page.</p> Source code in <code>pytigon_gui/toolbar/standardtoolbar.py</code> <pre><code>def create_page(self, title, kind=ToolbarPage.TYPE_PAGE_NORMAL):\n    \"\"\"Create a new standard toolbar page.\n\n    Returns:\n        StandardToolbarPage: The newly created page.\n    \"\"\"\n    return StandardToolbarPage(self, title, kind)\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.StandardToolbarBar.un_bind","title":"un_bind","text":"<pre><code>un_bind(id, e=None)\n</code></pre> <p>Unbind an event handler from the parent window.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>Event identifier.</p> required <code>e</code> <p>Event type; defaults to wx.EVT_MENU.</p> <code>None</code> Source code in <code>pytigon_gui/toolbar/standardtoolbar.py</code> <pre><code>def un_bind(self, id, e=None):\n    \"\"\"Unbind an event handler from the parent window.\n\n    Args:\n        id: Event identifier.\n        e: Event type; defaults to wx.EVT_MENU.\n    \"\"\"\n    if e:\n        self.parent.Unbind(e, id=id)\n    else:\n        self.parent.Unbind(wx.EVT_MENU, id=id)\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.StandardToolbarButton","title":"StandardToolbarButton","text":"<pre><code>StandardToolbarButton(\n    parent_panel, id, title, bitmap, bitmap_disabled=None, kind=TYPE_SIMPLE\n)\n</code></pre> <p>               Bases: <code>ToolbarButton</code></p> <p>Standard toolbar button (thin wrapper around ToolbarButton).</p> Source code in <code>pytigon_gui/toolbar/standardtoolbar.py</code> <pre><code>def __init__(\n    self,\n    parent_panel,\n    id,\n    title,\n    bitmap,\n    bitmap_disabled=None,\n    kind=ToolbarButton.TYPE_SIMPLE,\n):\n    super().__init__(parent_panel, id, title, bitmap, bitmap_disabled, kind)\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.StandardToolbarPage","title":"StandardToolbarPage","text":"<pre><code>StandardToolbarPage(parent_bar, title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>               Bases: <code>ToolbarPage</code></p> <p>A toolbar page in the standard toolbar.</p> <p>Initialize the page, adding a separator if not the first.</p> <p>Parameters:</p> Name Type Description Default <code>parent_bar</code> <p>Parent StandardToolbarBar.</p> required <code>title</code> <p>Page title.</p> required <code>kind</code> <p>Page type.</p> <code>TYPE_PAGE_NORMAL</code> Source code in <code>pytigon_gui/toolbar/standardtoolbar.py</code> <pre><code>def __init__(self, parent_bar, title, kind=ToolbarPage.TYPE_PAGE_NORMAL):\n    \"\"\"Initialize the page, adding a separator if not the first.\n\n    Args:\n        parent_bar: Parent StandardToolbarBar.\n        title: Page title.\n        kind: Page type.\n    \"\"\"\n    ToolbarPage.__init__(self, parent_bar, title, kind)\n    if len(parent_bar.pages) &gt; 0:\n        parent_bar.standard_tool_bar.AddSeparator()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.StandardToolbarPage-functions","title":"Methods:","text":""},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.StandardToolbarPage.create_panel","title":"create_panel","text":"<pre><code>create_panel(title, kind=TYPE_PANEL_TOOLBAR)\n</code></pre> <p>Create a panel within this page.</p> <p>Returns:</p> Name Type Description <code>StandardToolbarPanel</code> <p>The newly created panel.</p> Source code in <code>pytigon_gui/toolbar/standardtoolbar.py</code> <pre><code>def create_panel(self, title, kind=ToolbarPanel.TYPE_PANEL_TOOLBAR):\n    \"\"\"Create a panel within this page.\n\n    Returns:\n        StandardToolbarPanel: The newly created panel.\n    \"\"\"\n    return StandardToolbarPanel(self, title, kind)\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.StandardToolbarPanel","title":"StandardToolbarPanel","text":"<pre><code>StandardToolbarPanel(parent_page, title, kind=TYPE_PANEL_TOOLBAR)\n</code></pre> <p>               Bases: <code>ToolbarPanel</code></p> <p>A panel within a standard toolbar page that holds a group of buttons.</p> <p>Initialize the panel, adding a separator if not the first.</p> <p>Parameters:</p> Name Type Description Default <code>parent_page</code> <p>Parent StandardToolbarPage.</p> required <code>title</code> <p>Panel title.</p> required <code>kind</code> <p>Panel type.</p> <code>TYPE_PANEL_TOOLBAR</code> Source code in <code>pytigon_gui/toolbar/standardtoolbar.py</code> <pre><code>def __init__(self, parent_page, title, kind=ToolbarPanel.TYPE_PANEL_TOOLBAR):\n    \"\"\"Initialize the panel, adding a separator if not the first.\n\n    Args:\n        parent_page: Parent StandardToolbarPage.\n        title: Panel title.\n        kind: Panel type.\n    \"\"\"\n    ToolbarPanel.__init__(self, parent_page, title, kind)\n    if len(parent_page.panels) &gt; 0:\n        parent_page.parent_bar.standard_tool_bar.AddSeparator()\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.StandardToolbarPanel-functions","title":"Methods:","text":""},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.StandardToolbarPanel.add_separator","title":"add_separator","text":"<pre><code>add_separator()\n</code></pre> <p>Append a visual separator to this panel.</p> <p>Returns:</p> Name Type Description <code>StandardToolbarButton</code> <p>The separator button object.</p> Source code in <code>pytigon_gui/toolbar/standardtoolbar.py</code> <pre><code>def add_separator(self):\n    \"\"\"Append a visual separator to this panel.\n\n    Returns:\n        StandardToolbarButton: The separator button object.\n    \"\"\"\n    b = StandardToolbarButton(\n        self, 0, \"\", None, None, kind=ToolbarButton.TYPE_SEPARATOR\n    )\n    self._append(b)\n    return b\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.StandardToolbarPanel.create_button","title":"create_button","text":"<pre><code>create_button(id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE)\n</code></pre> <p>Create and append a button to this panel.</p> <p>Returns:</p> Name Type Description <code>StandardToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/standardtoolbar.py</code> <pre><code>def create_button(\n    self,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=ToolbarButton.TYPE_SIMPLE,\n):\n    \"\"\"Create and append a button to this panel.\n\n    Returns:\n        StandardToolbarButton: The newly created button.\n    \"\"\"\n    b = StandardToolbarButton(self, id, title, bitmap, bitmap_disabled, kind)\n    self._append(b)\n    return b\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarBar","title":"ToolbarBar","text":"<pre><code>ToolbarBar(parent, gui_style)\n</code></pre> <p>Base class for all menus and toolbars connected to the top frame window.</p> <p>Manages a collection of ToolbarPage objects and provides common operations like binding events and creating standard buttons.</p> <p>Initialize the toolbar bar.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window - the top wx.Frame derived object.</p> required <code>gui_style</code> <p>String description of the GUI interface. Based on this string, standard toolbar elements are created.</p> required Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, parent, gui_style):\n    \"\"\"Initialize the toolbar bar.\n\n    Args:\n        parent: Parent window - the top wx.Frame derived object.\n        gui_style: String description of the GUI interface. Based on\n            this string, standard toolbar elements are created.\n    \"\"\"\n    self.parent = parent\n    self.main_page = None\n    self.gui_style = gui_style\n\n    self.toolbars = {}\n    self.pages = []\n    self.user_panels = {}\n\n    self.standard_buttons = StandardButtons(self, gui_style)\n    self.standard_buttons.create_file_panel(self.main_page)\n    self.standard_buttons.create_edit_panel(self.main_page)\n    self.standard_buttons.create_operations_panel(self.main_page)\n    self.standard_buttons.create_browse_panel(self.main_page)\n    self.standard_buttons.create_address_panel(self.main_page)\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarBar-functions","title":"Methods:","text":""},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarBar.append","title":"append","text":"<pre><code>append(title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>Append a page to the toolbar, or return an existing one.</p> <p>If a page with the same name and kind already exists, it is returned instead of creating a duplicate.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Page title.</p> required <code>kind</code> <p>Type of page; one of ToolbarPage.TYPE_PAGE_* constants.</p> <code>TYPE_PAGE_NORMAL</code> <p>Returns:</p> Name Type Description <code>ToolbarPage</code> <p>The existing or newly created page.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def append(self, title, kind=ToolbarPage.TYPE_PAGE_NORMAL):\n    \"\"\"Append a page to the toolbar, or return an existing one.\n\n    If a page with the same name and kind already exists, it is\n    returned instead of creating a duplicate.\n\n    Args:\n        title: Page title.\n        kind: Type of page; one of ToolbarPage.TYPE_PAGE_* constants.\n\n    Returns:\n        ToolbarPage: The existing or newly created page.\n    \"\"\"\n    for page in self.pages:\n        if page.name == title and page.kind == kind:\n            return page\n    p = self.create_page(title, kind)\n    if not self.main_page:\n        self.main_page = p\n    self.pages.append(p)\n    return p\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarBar.bind","title":"bind","text":"<pre><code>bind(fun, id=ID_ANY, e=None)\n</code></pre> <p>Bind an event handler to the toolbar's parent window.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <p>Handler function.</p> required <code>id</code> <p>Identifier of the event.</p> <code>ID_ANY</code> <code>e</code> <p>Event type; defaults to wx.EVT_MENU if not specified.</p> <code>None</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def bind(self, fun, id=wx.ID_ANY, e=None):\n    \"\"\"Bind an event handler to the toolbar's parent window.\n\n    Args:\n        fun: Handler function.\n        id: Identifier of the event.\n        e: Event type; defaults to wx.EVT_MENU if not specified.\n    \"\"\"\n    if e:\n        self.parent.Bind(e, fun, id=id)\n    else:\n        self.parent.Bind(wx.EVT_MENU, fun, id=id)\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarBar.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Close/destroy the toolbar.</p> <p>Override in subclasses to perform cleanup.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def close(self):\n    \"\"\"Close/destroy the toolbar.\n\n    Override in subclasses to perform cleanup.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarBar.create","title":"create","text":"<pre><code>create()\n</code></pre> <p>Create/realize the toolbar.</p> <p>Override in subclasses to perform actual widget creation.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create(self):\n    \"\"\"Create/realize the toolbar.\n\n    Override in subclasses to perform actual widget creation.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarBar.create_html_win","title":"create_html_win","text":"<pre><code>create_html_win(toolbar_page, address_or_parser, parameters, callback=None)\n</code></pre> <p>Create an HTML page embedded in the toolbar.</p> <p>Not all toolbar types support this feature.</p> <p>Parameters:</p> Name Type Description Default <code>toolbar_page</code> <p>Name of the toolbar page to host the HTML. Use '__' to separate page and panel names, e.g. 'PageName__PanelName'.</p> required <code>address_or_parser</code> <p>Address of the HTTP page or a parser.</p> required <code>parameters</code> <p>Parameters for the HTTP request.</p> required <code>callback</code> <p>Optional callback invoked after initialization.</p> <code>None</code> <p>Returns:</p> Type Description <p>SchPage or None: The created page, or None on failure.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_html_win(\n    self, toolbar_page, address_or_parser, parameters, callback=None\n):\n    \"\"\"Create an HTML page embedded in the toolbar.\n\n    Not all toolbar types support this feature.\n\n    Args:\n        toolbar_page: Name of the toolbar page to host the HTML.\n            Use '__' to separate page and panel names,\n            e.g. 'PageName__PanelName'.\n        address_or_parser: Address of the HTTP page or a parser.\n        parameters: Parameters for the HTTP request.\n        callback: Optional callback invoked after initialization.\n\n    Returns:\n        SchPage or None: The created page, or None on failure.\n    \"\"\"\n    page = None\n    if not toolbar_page:\n        u_name = \"main tools\"\n        page_name = _(\"main tools\")\n        panel_name = \"main tools\"\n    else:\n        u_name = toolbar_page\n        names = toolbar_page.split(\"__\")\n        if len(names) &gt; 1:\n            page_name = names[0].replace(\"_\", \" \")\n            panel_name = names[1].replace(\"_\", \" \")\n        else:\n            page_name = toolbar_page.replace(\"_\", \" \")\n            panel_name = page_name\n\n    if u_name in self.user_panels:\n        panel = self.user_panels[u_name]\n    else:\n        page = self._find_page_by_title(page_name)\n        if page is None:\n            page = self.append(page_name)\n        panel = page.create_html_panel(panel_name)\n\n    if panel:\n        dx = panel.get_width() + 3\n        dy = panel.get_height() + 5\n\n        page2 = SchPage(\n            panel.get_window(),\n            address_or_parser,\n            parameters,\n            size=wx.Size(dx, dy),\n            pos=wx.Point(2, 2),\n        )\n        best = page2.body.calculate_best_size()\n        page2.SetSize(wx.Size(int(best[0]), int(best[1])))\n        panel.set_page(page2)\n        self.user_panels[u_name] = panel\n        page2.body.toolbar_interface = self\n        page2.body.toolbar_interface_page = page\n\n        def init_page(callback):\n            page2.init_frame()\n            page2.activate_page()\n            page2.Update()\n\n        wx.CallAfter(init_page, callback)\n\n        return page2\n\n    return None\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarBar.create_page","title":"create_page","text":"<pre><code>create_page(title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>Create and return a new ToolbarPage.</p> <p>Override in derived classes to return a subclass of ToolbarPage.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Page title.</p> required <code>kind</code> <p>Type of page; one of ToolbarPage.TYPE_PAGE_* constants.</p> <code>TYPE_PAGE_NORMAL</code> <p>Returns:</p> Name Type Description <code>ToolbarPage</code> <p>The newly created page.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_page(self, title, kind=ToolbarPage.TYPE_PAGE_NORMAL):\n    \"\"\"Create and return a new ToolbarPage.\n\n    Override in derived classes to return a subclass of ToolbarPage.\n\n    Args:\n        title: Page title.\n        kind: Type of page; one of ToolbarPage.TYPE_PAGE_* constants.\n\n    Returns:\n        ToolbarPage: The newly created page.\n    \"\"\"\n    return ToolbarPage(self, title, kind)\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarBar.create_panel_in_main_page","title":"create_panel_in_main_page","text":"<pre><code>create_panel_in_main_page(title, kind)\n</code></pre> <p>Create a panel in the main page.</p> <p>If no main page exists yet, one is created automatically.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>New panel's title.</p> required <code>kind</code> <p>Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.</p> required <p>Returns:</p> Name Type Description <code>ToolbarPanel</code> <p>The newly created panel.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_panel_in_main_page(self, title, kind):\n    \"\"\"Create a panel in the main page.\n\n    If no main page exists yet, one is created automatically.\n\n    Args:\n        title: New panel's title.\n        kind: Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.\n\n    Returns:\n        ToolbarPanel: The newly created panel.\n    \"\"\"\n    if not self.main_page:\n        self.append(\"main tools\")\n    return self.main_page.create_panel(title, kind)\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarBar.new_child_page","title":"new_child_page","text":"<pre><code>new_child_page(address_or_parser, title='', param=None, callback=None)\n</code></pre> <p>Create a new child page.</p> <p>For toolbar, the child page is transferred to the desktop window.</p> <p>Parameters:</p> Name Type Description Default <code>address_or_parser</code> <p>Address of the HTTP page or a parser.</p> required <code>title</code> <p>Page title.</p> <code>''</code> <code>param</code> <p>Optional parameters.</p> <code>None</code> <code>callback</code> <p>Optional callback.</p> <code>None</code> <p>Returns:</p> Type Description <p>The result of new_main_page on the top window.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def new_child_page(self, address_or_parser, title=\"\", param=None, callback=None):\n    \"\"\"Create a new child page.\n\n    For toolbar, the child page is transferred to the desktop window.\n\n    Args:\n        address_or_parser: Address of the HTTP page or a parser.\n        title: Page title.\n        param: Optional parameters.\n        callback: Optional callback.\n\n    Returns:\n        The result of new_main_page on the top window.\n    \"\"\"\n    return wx.GetApp().GetTopWindow().new_main_page(address_or_parser, title, param)\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarBar.remove_page","title":"remove_page","text":"<pre><code>remove_page(title)\n</code></pre> <p>Remove the page with the specified title from the toolbar.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Title of the page to remove.</p> required Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def remove_page(self, title):\n    \"\"\"Remove the page with the specified title from the toolbar.\n\n    Args:\n        title: Title of the page to remove.\n    \"\"\"\n    # Remove from pages list\n    for page in self.pages:\n        if page.title == title:\n            self.pages.remove(page)\n            break\n\n    # If the main page was removed, pick a new one\n    if self.main_page and self.main_page.title == title:\n        self.main_page = self.pages[0] if self.pages else None\n\n    # Remove associated user panels (iterate over a static list of keys\n    # to avoid \"dictionary changed size during iteration\" errors)\n    for key in list(self.user_panels.keys()):\n        panel = self.user_panels[key]\n        if panel.page.title == title:\n            del self.user_panels[key]\n            return\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarBar.un_bind","title":"un_bind","text":"<pre><code>un_bind(id=ID_ANY, e=None)\n</code></pre> <p>Unbind an event handler from the toolbar's parent window.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>Identifier of the event.</p> <code>ID_ANY</code> <code>e</code> <p>Event type; defaults to wx.EVT_MENU if not specified.</p> <code>None</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def un_bind(self, id=wx.ID_ANY, e=None):\n    \"\"\"Unbind an event handler from the toolbar's parent window.\n\n    Args:\n        id: Identifier of the event.\n        e: Event type; defaults to wx.EVT_MENU if not specified.\n    \"\"\"\n    if e:\n        self.parent.Unbind(e, id=id)\n    else:\n        self.parent.Unbind(wx.EVT_MENU, id=id)\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarButton","title":"ToolbarButton","text":"<pre><code>ToolbarButton(\n    parent_panel, id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE\n)\n</code></pre> <p>Represents a single button (or separator) on a toolbar panel.</p> <p>This is an abstract data model; the actual wx widget is created by the concrete toolbar implementation.</p> Button type constants <p>TYPE_SIMPLE = 0     - Simple push button. TYPE_DROPDOWN = 1   - Button with a dropdown menu. TYPE_HYBRID = 2     - Hybrid button (part button, part dropdown). TYPE_TOOGLE = 3     - Toggle button. TYPE_PANEL = 4      - Embedded panel. TYPE_SEPARATOR = 5  - Visual separator.</p> <p>Create a new toolbar button.</p> <p>Parameters:</p> Name Type Description Default <code>parent_panel</code> <p>Parent ToolbarPanel object.</p> required <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title (display text).</p> required <code>bitmap</code> <p>Button bitmap for the normal state.</p> <code>None</code> <code>bitmap_disabled</code> <p>Button bitmap for the disabled state. Defaults to wx.NullBitmap when bitmap is provided.</p> <code>None</code> <code>kind</code> <p>Type of button; one of the TYPE_* class constants.</p> <code>TYPE_SIMPLE</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(\n    self,\n    parent_panel,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=TYPE_SIMPLE,\n):\n    \"\"\"Create a new toolbar button.\n\n    Args:\n        parent_panel: Parent ToolbarPanel object.\n        id: An integer by which the tool may be identified.\n        title: Button title (display text).\n        bitmap: Button bitmap for the normal state.\n        bitmap_disabled: Button bitmap for the disabled state.\n            Defaults to wx.NullBitmap when bitmap is provided.\n        kind: Type of button; one of the TYPE_* class constants.\n    \"\"\"\n    self.parent_panel = parent_panel\n    self.id = id\n    self.title = title\n    self.bitmap = bitmap\n    if bitmap_disabled is None and bitmap is not None:\n        self.bitmap_disabled = wx.NullBitmap\n    else:\n        self.bitmap_disabled = bitmap_disabled\n    self.kind = kind\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarButton-functions","title":"Methods:","text":""},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarPage","title":"ToolbarPage","text":"<pre><code>ToolbarPage(parent_bar, title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>A page (tab) within a toolbar bar, containing one or more panels.</p> Page type constants <p>TYPE_PAGE_NORMAL = 0  - Standard page.</p> <p>Create a new toolbar page.</p> <p>Parameters:</p> Name Type Description Default <code>parent_bar</code> <p>Parent ToolbarBar object.</p> required <code>title</code> <p>Page title (will be translated via wx.GetTranslation).</p> required <code>kind</code> <p>Type of page; one of the TYPE_PAGE_* class constants.</p> <code>TYPE_PAGE_NORMAL</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, parent_bar, title, kind=TYPE_PAGE_NORMAL):\n    \"\"\"Create a new toolbar page.\n\n    Args:\n        parent_bar: Parent ToolbarBar object.\n        title: Page title (will be translated via wx.GetTranslation).\n        kind: Type of page; one of the TYPE_PAGE_* class constants.\n    \"\"\"\n    self.parent_bar = parent_bar\n    self.name = title\n    self.title = _(title)\n    self.kind = kind\n    self.panels = []\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarPage-functions","title":"Methods:","text":""},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarPage.append","title":"append","text":"<pre><code>append(title, kind=TYPE_PANEL_BUTTONBAR)\n</code></pre> <p>Create a new panel and append it to this page.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Panel title.</p> required <code>kind</code> <p>Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.</p> <code>TYPE_PANEL_BUTTONBAR</code> <p>Returns:</p> Name Type Description <code>ToolbarPanel</code> <p>The newly created panel.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def append(self, title, kind=ToolbarPanel.TYPE_PANEL_BUTTONBAR):\n    \"\"\"Create a new panel and append it to this page.\n\n    Args:\n        title: Panel title.\n        kind: Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.\n\n    Returns:\n        ToolbarPanel: The newly created panel.\n    \"\"\"\n    p = self.create_panel(title, kind)\n    self.panels.append(p)\n    return p\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarPage.create_html_panel","title":"create_html_panel","text":"<pre><code>create_html_panel(title)\n</code></pre> <p>Create an HTML-capable panel within this page.</p> <p>Override in derived classes to return a BaseHtmlPanel subclass.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Panel title.</p> required <p>Returns:</p> Type Description <p>BaseHtmlPanel or None: The panel, or None if not supported.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_html_panel(self, title):\n    \"\"\"Create an HTML-capable panel within this page.\n\n    Override in derived classes to return a BaseHtmlPanel subclass.\n\n    Args:\n        title: Panel title.\n\n    Returns:\n        BaseHtmlPanel or None: The panel, or None if not supported.\n    \"\"\"\n    return None\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarPage.create_panel","title":"create_panel","text":"<pre><code>create_panel(title, kind)\n</code></pre> <p>Create and return a new ToolbarPanel for this page.</p> <p>Override in derived classes to return a subclass of ToolbarPanel.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Panel title.</p> required <code>kind</code> <p>Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.</p> required <p>Returns:</p> Name Type Description <code>ToolbarPanel</code> <p>The newly created panel.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_panel(self, title, kind):\n    \"\"\"Create and return a new ToolbarPanel for this page.\n\n    Override in derived classes to return a subclass of ToolbarPanel.\n\n    Args:\n        title: Panel title.\n        kind: Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.\n\n    Returns:\n        ToolbarPanel: The newly created panel.\n    \"\"\"\n    return ToolbarPanel(self, title, kind=ToolbarPanel.TYPE_PANEL_BUTTONBAR)\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarPanel","title":"ToolbarPanel","text":"<pre><code>ToolbarPanel(parent_page, title, kind=TYPE_PANEL_BUTTONBAR)\n</code></pre> <p>A panel within a toolbar page that holds a group of buttons.</p> Panel type constants <p>TYPE_PANEL_BUTTONBAR = 0  - Button bar layout. TYPE_PANEL_TOOLBAR = 1    - Toolbar layout. TYPE_PANEL_PANELBAR = 2   - Custom panel layout.</p> <p>Create a new toolbar panel.</p> <p>Parameters:</p> Name Type Description Default <code>parent_page</code> <p>Parent ToolbarPage object.</p> required <code>title</code> <p>Panel title.</p> required <code>kind</code> <p>Type of panel; one of the TYPE_PANEL_* class constants.</p> <code>TYPE_PANEL_BUTTONBAR</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, parent_page, title, kind=TYPE_PANEL_BUTTONBAR):\n    \"\"\"Create a new toolbar panel.\n\n    Args:\n        parent_page: Parent ToolbarPage object.\n        title: Panel title.\n        kind: Type of panel; one of the TYPE_PANEL_* class constants.\n    \"\"\"\n    self.parent_page = parent_page\n    self.title = title\n    self.kind = kind\n    self.buttons = []\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarPanel-functions","title":"Methods:","text":""},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarPanel.add_dropdown_tool","title":"add_dropdown_tool","text":"<pre><code>add_dropdown_tool(id, title, bitmaps)\n</code></pre> <p>Append a dropdown button to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_dropdown_tool(self, id, title, bitmaps):\n    \"\"\"Append a dropdown button to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_DROPDOWN)\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarPanel.add_hybrid_tool","title":"add_hybrid_tool","text":"<pre><code>add_hybrid_tool(id, title, bitmaps)\n</code></pre> <p>Append a hybrid button (button + dropdown) to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_hybrid_tool(self, id, title, bitmaps):\n    \"\"\"Append a hybrid button (button + dropdown) to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_HYBRID)\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarPanel.add_separator","title":"add_separator","text":"<pre><code>add_separator()\n</code></pre> <p>Append a visual separator to the panel.</p> <p>The default implementation does nothing; override in subclasses.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_separator(self):\n    \"\"\"Append a visual separator to the panel.\n\n    The default implementation does nothing; override in subclasses.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarPanel.add_simple_tool","title":"add_simple_tool","text":"<pre><code>add_simple_tool(id, title, bitmaps)\n</code></pre> <p>Append a simple push button to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_simple_tool(self, id, title, bitmaps):\n    \"\"\"Append a simple push button to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_SIMPLE)\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarPanel.add_toogle_tool","title":"add_toogle_tool","text":"<pre><code>add_toogle_tool(id, title, bitmaps)\n</code></pre> <p>Append a toggle button to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_toogle_tool(self, id, title, bitmaps):\n    \"\"\"Append a toggle button to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_TOOGLE)\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarPanel.append","title":"append","text":"<pre><code>append(id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE)\n</code></pre> <p>Create a button and append it to this panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmap</code> <p>Button bitmap for the normal state.</p> <code>None</code> <code>bitmap_disabled</code> <p>Button bitmap for the disabled state.</p> <code>None</code> <code>kind</code> <p>Type of button; one of ToolbarButton.TYPE_* constants.</p> <code>TYPE_SIMPLE</code> <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created and appended button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def append(\n    self,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=ToolbarButton.TYPE_SIMPLE,\n):\n    \"\"\"Create a button and append it to this panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmap: Button bitmap for the normal state.\n        bitmap_disabled: Button bitmap for the disabled state.\n        kind: Type of button; one of ToolbarButton.TYPE_* constants.\n\n    Returns:\n        ToolbarButton: The newly created and appended button.\n    \"\"\"\n    button = self.create_button(id, title, bitmap, bitmap_disabled, kind)\n    self.buttons.append(button)\n    return button\n</code></pre>"},{"location":"api/toolbar_standardtoolbar/#pytigon_gui.toolbar.standardtoolbar.ToolbarPanel.create_button","title":"create_button","text":"<pre><code>create_button(id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE)\n</code></pre> <p>Create and return a new ToolbarButton.</p> <p>Override in derived classes to return a subclass of ToolbarButton.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmap</code> <p>Button bitmap for the normal state.</p> <code>None</code> <code>bitmap_disabled</code> <p>Button bitmap for the disabled state.</p> <code>None</code> <code>kind</code> <p>Type of button; one of ToolbarButton.TYPE_* constants.</p> <code>TYPE_SIMPLE</code> <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_button(\n    self,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=ToolbarButton.TYPE_SIMPLE,\n):\n    \"\"\"Create and return a new ToolbarButton.\n\n    Override in derived classes to return a subclass of ToolbarButton.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmap: Button bitmap for the normal state.\n        bitmap_disabled: Button bitmap for the disabled state.\n        kind: Type of button; one of ToolbarButton.TYPE_* constants.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    return ToolbarButton(self, id, title, bitmap, bitmap_disabled, kind)\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/","title":"toolbar.standardtoolbarbuttons \u2013 Standard Toolbar Buttons","text":"<p>Pre-built button definitions for common operations: new, open, save, print, undo, redo, cut, copy, paste, find, and navigation.</p>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons","title":"standardtoolbarbuttons","text":"<p>Standard toolbar button creation and event handling.</p> <p>Defines the StandardButtons class that creates File, Edit, Operations, Browse, and Address panels with their associated event handlers. Also includes the SchAutoComplete widget for the address bar.</p>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons-attributes","title":"Attributes","text":""},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ADDRESS_HISTORY","title":"ADDRESS_HISTORY  <code>module-attribute</code>","text":"<pre><code>ADDRESS_HISTORY = [\n    \"www.allegro.pl\",\n    \"www.onet.pl\",\n    \"www.google.pl\",\n    \"www.sourceforge.org\",\n    \"www.freshmeat.net\",\n    \"127.0.0.2\",\n    \"127.0.0.1:8000\",\n    \"books.evc-cit.info/index.html\",\n    \"www.un.org/Depts/Cartographic/map/profile/poland.pdf\",\n]\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.EmpytControl","title":"EmpytControl  <code>module-attribute</code>","text":"<pre><code>EmpytControl = EmptyControl\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_BOOKMARK_ADD","title":"ID_BOOKMARK_ADD  <code>module-attribute</code>","text":"<pre><code>ID_BOOKMARK_ADD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_CLEAR_HISTORY","title":"ID_CLEAR_HISTORY  <code>module-attribute</code>","text":"<pre><code>ID_CLEAR_HISTORY = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_CLOSETAB","title":"ID_CLOSETAB  <code>module-attribute</code>","text":"<pre><code>ID_CLOSETAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_COPY","title":"ID_COPY  <code>module-attribute</code>","text":"<pre><code>ID_COPY = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_CUT","title":"ID_CUT  <code>module-attribute</code>","text":"<pre><code>ID_CUT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_DOWNLOAD_OPTIONS","title":"ID_DOWNLOAD_OPTIONS  <code>module-attribute</code>","text":"<pre><code>ID_DOWNLOAD_OPTIONS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_END","title":"ID_END  <code>module-attribute</code>","text":"<pre><code>ID_END = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_EXIT","title":"ID_EXIT  <code>module-attribute</code>","text":"<pre><code>ID_EXIT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_FIND","title":"ID_FIND  <code>module-attribute</code>","text":"<pre><code>ID_FIND = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_GENERAL_OPTIONS","title":"ID_GENERAL_OPTIONS  <code>module-attribute</code>","text":"<pre><code>ID_GENERAL_OPTIONS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_GOTODESKTOP","title":"ID_GOTODESKTOP  <code>module-attribute</code>","text":"<pre><code>ID_GOTODESKTOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_GOTOFOOTER","title":"ID_GOTOFOOTER  <code>module-attribute</code>","text":"<pre><code>ID_GOTOFOOTER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_GOTOHEADER","title":"ID_GOTOHEADER  <code>module-attribute</code>","text":"<pre><code>ID_GOTOHEADER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_GOTOPANEL","title":"ID_GOTOPANEL  <code>module-attribute</code>","text":"<pre><code>ID_GOTOPANEL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_HELP","title":"ID_HELP  <code>module-attribute</code>","text":"<pre><code>ID_HELP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_HISTORY_SHOW","title":"ID_HISTORY_SHOW  <code>module-attribute</code>","text":"<pre><code>ID_HISTORY_SHOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_LOAD","title":"ID_LOAD  <code>module-attribute</code>","text":"<pre><code>ID_LOAD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_NEXTPAGE","title":"ID_NEXTPAGE  <code>module-attribute</code>","text":"<pre><code>ID_NEXTPAGE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_NEXTTAB","title":"ID_NEXTTAB  <code>module-attribute</code>","text":"<pre><code>ID_NEXTTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_PAGE_BLOCK","title":"ID_PAGE_BLOCK  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_BLOCK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_PAGE_SOURCE","title":"ID_PAGE_SOURCE  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_SOURCE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_PAGE_VIEW","title":"ID_PAGE_VIEW  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_VIEW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_PASTE","title":"ID_PASTE  <code>module-attribute</code>","text":"<pre><code>ID_PASTE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_PREVPAGE","title":"ID_PREVPAGE  <code>module-attribute</code>","text":"<pre><code>ID_PREVPAGE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_PREVTAB","title":"ID_PREVTAB  <code>module-attribute</code>","text":"<pre><code>ID_PREVTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_PRINT","title":"ID_PRINT  <code>module-attribute</code>","text":"<pre><code>ID_PRINT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_PRINT_PREVIEW","title":"ID_PRINT_PREVIEW  <code>module-attribute</code>","text":"<pre><code>ID_PRINT_PREVIEW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_PRINT_SETTINGS","title":"ID_PRINT_SETTINGS  <code>module-attribute</code>","text":"<pre><code>ID_PRINT_SETTINGS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_REDO","title":"ID_REDO  <code>module-attribute</code>","text":"<pre><code>ID_REDO = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_REFRESHTAB","title":"ID_REFRESHTAB  <code>module-attribute</code>","text":"<pre><code>ID_REFRESHTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_REPLACE","title":"ID_REPLACE  <code>module-attribute</code>","text":"<pre><code>ID_REPLACE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_RESET","title":"ID_RESET  <code>module-attribute</code>","text":"<pre><code>ID_RESET = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_RETVALUE","title":"ID_RETVALUE  <code>module-attribute</code>","text":"<pre><code>ID_RETVALUE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_SAVE","title":"ID_SAVE  <code>module-attribute</code>","text":"<pre><code>ID_SAVE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_SAVE_AS","title":"ID_SAVE_AS  <code>module-attribute</code>","text":"<pre><code>ID_SAVE_AS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_SELALL","title":"ID_SELALL  <code>module-attribute</code>","text":"<pre><code>ID_SELALL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_SHOWDESKTOP","title":"ID_SHOWDESKTOP  <code>module-attribute</code>","text":"<pre><code>ID_SHOWDESKTOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_SHOWFOOTER","title":"ID_SHOWFOOTER  <code>module-attribute</code>","text":"<pre><code>ID_SHOWFOOTER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_SHOWHEADER","title":"ID_SHOWHEADER  <code>module-attribute</code>","text":"<pre><code>ID_SHOWHEADER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_SHOWPANEL","title":"ID_SHOWPANEL  <code>module-attribute</code>","text":"<pre><code>ID_SHOWPANEL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_SHOWSTATUSBAR","title":"ID_SHOWSTATUSBAR  <code>module-attribute</code>","text":"<pre><code>ID_SHOWSTATUSBAR = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_SHOWTOOLBAR1","title":"ID_SHOWTOOLBAR1  <code>module-attribute</code>","text":"<pre><code>ID_SHOWTOOLBAR1 = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_SHOWTOOLBAR2","title":"ID_SHOWTOOLBAR2  <code>module-attribute</code>","text":"<pre><code>ID_SHOWTOOLBAR2 = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_SHOW_BOOKMARKS","title":"ID_SHOW_BOOKMARKS  <code>module-attribute</code>","text":"<pre><code>ID_SHOW_BOOKMARKS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_SHOW_DOWNLOAD","title":"ID_SHOW_DOWNLOAD  <code>module-attribute</code>","text":"<pre><code>ID_SHOW_DOWNLOAD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_START","title":"ID_START  <code>module-attribute</code>","text":"<pre><code>ID_START = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_TASKBAR_CLOSE","title":"ID_TASKBAR_CLOSE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_CLOSE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_TASKBAR_HIDE","title":"ID_TASKBAR_HIDE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_HIDE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_TASKBAR_SHOW","title":"ID_TASKBAR_SHOW  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_SHOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_TASKBAR_TOGGLE","title":"ID_TASKBAR_TOGGLE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_TOGGLE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_UNDO","title":"ID_UNDO  <code>module-attribute</code>","text":"<pre><code>ID_UNDO = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_WEB_ADDBOOKMARK","title":"ID_WEB_ADDBOOKMARK  <code>module-attribute</code>","text":"<pre><code>ID_WEB_ADDBOOKMARK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_WEB_BACK","title":"ID_WEB_BACK  <code>module-attribute</code>","text":"<pre><code>ID_WEB_BACK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_WEB_DOWN","title":"ID_WEB_DOWN  <code>module-attribute</code>","text":"<pre><code>ID_WEB_DOWN = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_WEB_EDIT","title":"ID_WEB_EDIT  <code>module-attribute</code>","text":"<pre><code>ID_WEB_EDIT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_WEB_FORWARD","title":"ID_WEB_FORWARD  <code>module-attribute</code>","text":"<pre><code>ID_WEB_FORWARD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_WEB_NEW_WINDOW","title":"ID_WEB_NEW_WINDOW  <code>module-attribute</code>","text":"<pre><code>ID_WEB_NEW_WINDOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_WEB_REFRESH","title":"ID_WEB_REFRESH  <code>module-attribute</code>","text":"<pre><code>ID_WEB_REFRESH = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_WEB_SOURCE","title":"ID_WEB_SOURCE  <code>module-attribute</code>","text":"<pre><code>ID_WEB_SOURCE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_WEB_STOP","title":"ID_WEB_STOP  <code>module-attribute</code>","text":"<pre><code>ID_WEB_STOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_WEB_UP","title":"ID_WEB_UP  <code>module-attribute</code>","text":"<pre><code>ID_WEB_UP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.ID_ZOOM","title":"ID_ZOOM  <code>module-attribute</code>","text":"<pre><code>ID_ZOOM = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.MSW_STYLE","title":"MSW_STYLE  <code>module-attribute</code>","text":"<pre><code>MSW_STYLE = True\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.TYPE_BUTTONBAR","title":"TYPE_BUTTONBAR  <code>module-attribute</code>","text":"<pre><code>TYPE_BUTTONBAR = 1\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.TYPE_PANELBAR","title":"TYPE_PANELBAR  <code>module-attribute</code>","text":"<pre><code>TYPE_PANELBAR = 2\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.TYPE_TOOLBAR","title":"TYPE_TOOLBAR  <code>module-attribute</code>","text":"<pre><code>TYPE_TOOLBAR = 0\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons._","title":"_  <code>module-attribute</code>","text":"<pre><code>_ = wx.GetTranslation\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons-classes","title":"Classes","text":""},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.EmptyBar","title":"EmptyBar","text":"<pre><code>EmptyBar()\n</code></pre> <p>A stub toolbar bar used when a browser panel is not enabled.</p> <p>Provides a no-op refr method for compatibility.</p> Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def __init__(self):\n    pass\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.EmptyBar-functions","title":"Methods:","text":""},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.EmptyBar.refr","title":"refr","text":"<pre><code>refr()\n</code></pre> <p>No-op refresh method.</p> Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def refr(self):\n    \"\"\"No-op refresh method.\"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.EmptyControl","title":"EmptyControl","text":"<pre><code>EmptyControl()\n</code></pre> <p>A stub control used when no real address bar is available.</p> <p>Provides SetValue/GetValue with no-op behavior.</p> Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def __init__(self):\n    self.value = None\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.EmptyControl-functions","title":"Methods:","text":""},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.EmptyControl.GetValue","title":"GetValue","text":"<pre><code>GetValue()\n</code></pre> <p>Return the stored value.</p> <p>Returns:</p> Type Description <p>The stored value or None.</p> Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def GetValue(self):\n    \"\"\"Return the stored value.\n\n    Returns:\n        The stored value or None.\n    \"\"\"\n    return self.value\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.EmptyControl.SetValue","title":"SetValue","text":"<pre><code>SetValue(value)\n</code></pre> <p>Store a value (no-op visually).</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <p>Value to store.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def SetValue(self, value):\n    \"\"\"Store a value (no-op visually).\n\n    Args:\n        value: Value to store.\n    \"\"\"\n    self.value = value\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.SchAutoComplete","title":"SchAutoComplete","text":"<pre><code>SchAutoComplete(*argi, **argv)\n</code></pre> <p>               Bases: <code>TextCtrlAutoComplete</code></p> <p>Address bar auto-complete control with dynamic web history.</p> <p>Extends TextCtrlAutoComplete to provide URL auto-completion from a WebHistory instance.</p> <p>Initialize the auto-complete control.</p> <p>Parameters:</p> Name Type Description Default <code>*argi</code> <p>Positional arguments for TextCtrlAutoComplete.</p> <code>()</code> <code>**argv</code> <p>Keyword arguments; 'choices' will be overridden with a WebHistory instance if provided.</p> <code>{}</code> Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def __init__(self, *argi, **argv):\n    \"\"\"Initialize the auto-complete control.\n\n    Args:\n        *argi: Positional arguments for TextCtrlAutoComplete.\n        **argv: Keyword arguments; 'choices' will be overridden\n            with a WebHistory instance if provided.\n    \"\"\"\n    self.dynamic_choices = WebHistory()\n    argv[\"choices\"] = self.dynamic_choices\n    super().__init__(*argi, **argv)\n    self.SetEntryCallback(self.set_dynamic_choices)\n    self.SetMatchFunction(self.match)\n    self.Bind(wx.EVT_KEY_DOWN, self.on_return_key_down, self)\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.SchAutoComplete-functions","title":"Methods:","text":""},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.SchAutoComplete.match","title":"match","text":"<pre><code>match(text, choice)\n</code></pre> <p>Determine if a choice matches the entered text.</p> <p>Matches against the raw choice, the choice without 'http://', and the choice without 'www.'.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <p>User-entered text (lowercase).</p> required <code>choice</code> <p>A history entry to match against.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>True if the choice matches.</p> Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def match(self, text, choice):\n    \"\"\"Determine if a choice matches the entered text.\n\n    Matches against the raw choice, the choice without 'http://',\n    and the choice without 'www.'.\n\n    Args:\n        text: User-entered text (lowercase).\n        choice: A history entry to match against.\n\n    Returns:\n        bool: True if the choice matches.\n    \"\"\"\n    t = text.lower()\n    c = choice.lower()\n    if c.startswith(t):\n        return True\n    if c.startswith(\"http://\"):\n        c = c[7:]\n    if c.startswith(t):\n        return True\n    if c.startswith(\"www.\"):\n        c = c[4:]\n    return c.startswith(t)\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.SchAutoComplete.on_return_key_down","title":"on_return_key_down","text":"<pre><code>on_return_key_down(event)\n</code></pre> <p>Handle Return key: trigger callback if dropdown is hidden.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>wx.KeyEvent.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def on_return_key_down(self, event):\n    \"\"\"Handle Return key: trigger callback if dropdown is hidden.\n\n    Args:\n        event: wx.KeyEvent.\n    \"\"\"\n    if not self.dropdown.IsShown() and event.GetKeyCode() == wx.WXK_RETURN:\n        self.fun(self.GetValue())\n    else:\n        event.Skip()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.SchAutoComplete.set_callback_fun","title":"set_callback_fun","text":"<pre><code>set_callback_fun(fun)\n</code></pre> <p>Set the callback invoked when a URL is selected or entered.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <p>Callable that accepts a URL string.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def set_callback_fun(self, fun):\n    \"\"\"Set the callback invoked when a URL is selected or entered.\n\n    Args:\n        fun: Callable that accepts a URL string.\n    \"\"\"\n    self.fun = fun\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.SchAutoComplete.set_dynamic_choices","title":"set_dynamic_choices","text":"<pre><code>set_dynamic_choices()\n</code></pre> <p>Filter choices based on the current text input.</p> Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def set_dynamic_choices(self):\n    \"\"\"Filter choices based on the current text input.\"\"\"\n    ctrl = self\n    text = ctrl.GetValue().lower()\n    current_choices = ctrl.GetChoices()\n    choices = [\n        choice for choice in self.dynamic_choices if self.match(text, choice)\n    ]\n    if choices != current_choices:\n        ctrl.SetChoices(choices)\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.StandardButtons","title":"StandardButtons","text":"<pre><code>StandardButtons(toolbar_interface, gui_style)\n</code></pre> <p>Creates and manages standard toolbar buttons and their event handlers.</p> <p>Reads the gui_style string to determine which panels (File, Clipboard, Operations, Browser, Address) should be created and configures event bindings for each.</p> <p>Initialize standard buttons.</p> <p>Parameters:</p> Name Type Description Default <code>toolbar_interface</code> <p>The toolbar bar instance (ToolbarBar subclass).</p> required <code>gui_style</code> <p>String describing which features to enable.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def __init__(self, toolbar_interface, gui_style):\n    \"\"\"Initialize standard buttons.\n\n    Args:\n        toolbar_interface: The toolbar bar instance (ToolbarBar subclass).\n        gui_style: String describing which features to enable.\n    \"\"\"\n    self.toolbar_interface = toolbar_interface\n    self.gui_style = gui_style\n    self.ti = self.toolbar_interface\n    self.tbs = self.ti.toolbars\n    self._object_list = {}\n    self.webobject = None\n    self.file = \"file\" in self.gui_style\n    self.clipboard = \"clipboard\" in self.gui_style\n    self.operations = \"operations\" in self.gui_style\n    self.browse = \"browse\" in self.gui_style\n    self.nav = \"nav\" in self.gui_style\n    self.address_bar = \"address_bar\" in self.gui_style\n    self.bg = colour_to_html(wx.SystemSettings.GetColour(wx.SYS_COLOUR_3DFACE))\n    self.bg_info = colour_to_html(wx.SystemSettings.GetColour(wx.SYS_COLOUR_INFOBK))\n    self.bg_h = colour_to_html(wx.SystemSettings.GetColour(wx.SYS_COLOUR_HIGHLIGHT))\n    self.progress = 0\n    self.info = \"\"\n    self.address_txt = \"\"\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.StandardButtons-functions","title":"Methods:","text":""},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.StandardButtons.address_refr","title":"address_refr","text":"<pre><code>address_refr(panel)\n</code></pre> <p>Refresh the address bar based on the active panel's web status.</p> <p>Parameters:</p> Name Type Description Default <code>panel</code> <p>The active panel, or None.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def address_refr(self, panel):\n    \"\"\"Refresh the address bar based on the active panel's web status.\n\n    Args:\n        panel: The active panel, or None.\n    \"\"\"\n    if self.address_bar:\n        if panel is not None and hasattr(panel.body, \"WEB\"):\n            ctrl = panel.body.WEB\n            if hasattr(ctrl, \"GetStatus\"):\n                status = ctrl.GetStatus()\n                if status[\"progress\"] &gt;= 0:\n                    self._info_proc(\n                        status[\"txt\"].replace(\"INFO:\", \"\").replace(\"ERROR:\", \"\"),\n                        status[\"progress\"],\n                    )\n                else:\n                    if status[\"txt\"] and status[\"txt\"] != \"\":\n                        if \"ERROR:\" in status[\"txt\"]:\n                            self._info_alarm(status[\"txt\"].replace(\"ERROR:\", \"\"))\n                        else:\n                            self._info_txt(status[\"txt\"].replace(\"INFO:\", \"\"))\n                    else:\n                        self._info_clear()\n                if self.address_txt != status[\"address\"]:\n                    if status[\"address\"]:\n                        self.address.SetValue(status[\"address\"])\n                        self.address_txt = status[\"address\"]\n                    else:\n                        self.address.SetValue(\"\")\n                        self.address_txt = \"\"\n        else:\n            if self.address_txt != \"\":\n                self.address.SetValue(\"\")\n                self.address_txt = \"\"\n            self._info_clear()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.StandardButtons.clipboard_refr","title":"clipboard_refr","text":"<pre><code>clipboard_refr(editctrl)\n</code></pre> <p>Refresh clipboard buttons based on the focused control's capabilities.</p> <p>Parameters:</p> Name Type Description Default <code>editctrl</code> <p>The currently focused edit control, or None.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def clipboard_refr(self, editctrl):\n    \"\"\"Refresh clipboard buttons based on the focused control's capabilities.\n\n    Args:\n        editctrl: The currently focused edit control, or None.\n    \"\"\"\n    if self.clipboard:\n        tab_status = []\n        if editctrl:\n            tab_status.append(\n                (\n                    hasattr(editctrl, \"CanCopy\") and editctrl.CanCopy(),\n                    \"clipboard\",\n                    \"copy\",\n                )\n            )\n            tab_status.append(\n                (\n                    hasattr(editctrl, \"CanCut\") and editctrl.CanCut(),\n                    \"clipboard\",\n                    \"cut\",\n                )\n            )\n            tab_status.append(\n                (\n                    hasattr(editctrl, \"CanPaste\") and editctrl.CanPaste(),\n                    \"clipboard\",\n                    \"paste\",\n                )\n            )\n        else:\n            tab_status.append((False, \"clipboard\", \"copy\"))\n            tab_status.append((False, \"clipboard\", \"cut\"))\n            tab_status.append((False, \"clipboard\", \"paste\"))\n        return self._status(tab_status)\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.StandardButtons.create_address_panel","title":"create_address_panel","text":"<pre><code>create_address_panel(toolbar_page)\n</code></pre> <p>Create the Address bar panel with auto-complete and web menu.</p> <p>Parameters:</p> Name Type Description Default <code>toolbar_page</code> <p>The toolbar page (unused).</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def create_address_panel(self, toolbar_page):\n    \"\"\"Create the Address bar panel with auto-complete and web menu.\n\n    Args:\n        toolbar_page: The toolbar page (unused).\n    \"\"\"\n    if self.address_bar:\n        bar = self.ti.create_panel_in_main_page(_(\"Address\"), TYPE_PANELBAR)\n        if hasattr(bar, \"panel\"):\n            width = 350\n            panel = bar.panel\n            self.address = SchAutoComplete(panel, size=wx.Size(width, -1))\n            self.address.set_callback_fun(self.open_page)\n            self.tbs[\"browser\"][\"address\"] = self.address\n            self.webmenu = FM.FlatMenuBar(panel, spacer=5)\n            self.webmenu.GetRendererManager().SetTheme(FM.Style2007)\n            self.tbs[\"browser\"][\"webmenu\"] = self.webmenu\n            webmenu_source = self.web_menu()\n            self.web_info = wx.html.HtmlWindow(\n                panel, style=wx.html.HW_SCROLLBAR_NEVER, size=(width, 25)\n            )\n            self.web_info.SetBorders(0)\n            self._info_clear()\n            self.make_menu(self.webmenu, webmenu_source)\n            controls = (self.webmenu, self.web_info, self.address)\n            bar.AddPanel(controls)\n        else:\n            self.tbs[\"browser\"][\"address\"] = EmptyControl()\n            self.web_info = None\n    else:\n        self.tbs[\"browser\"][\"address\"] = EmptyControl()\n        self.web_info = None\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.StandardButtons.create_browse_panel","title":"create_browse_panel","text":"<pre><code>create_browse_panel(toolbar_page)\n</code></pre> <p>Create the Browser panel with navigation buttons.</p> <p>Parameters:</p> Name Type Description Default <code>toolbar_page</code> <p>The toolbar page (unused).</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def create_browse_panel(self, toolbar_page):\n    \"\"\"Create the Browser panel with navigation buttons.\n\n    Args:\n        toolbar_page: The toolbar page (unused).\n    \"\"\"\n    if self.browse or self.nav:\n        self.tbs[\"browser\"] = {}\n        bar = self.ti.create_panel_in_main_page(_(\"Browser\"), TYPE_BUTTONBAR)\n        bar.Refr = self.web_refr\n        self.webobject = None\n        self.tbs[\"browser\"][\"bar\"] = bar\n        self.tbs[\"browser\"][\"back\"] = bar.add_simple_tool(\n            ID_WEB_BACK,\n            _(\"Back\"),\n            bitmaps_from_art_id(wx.ART_GO_BACK, wx.Size(32, 32)),\n        )\n        self.tbs[\"browser\"][\"forward\"] = bar.add_simple_tool(\n            ID_WEB_FORWARD,\n            _(\"Forward\"),\n            bitmaps_from_art_id(wx.ART_GO_FORWARD, wx.Size(32, 32)),\n        )\n        self.make_handlers(ID_WEB_BACK, \"WebBack\", \"CanWebBack\", propagate=True)\n        self.make_handlers(\n            ID_WEB_FORWARD, \"WebForward\", \"CanWebForward\", propagate=True\n        )\n\n        if self.browse:\n            self.tbs[\"browser\"][\"stop\"] = bar.add_simple_tool(\n                ID_WEB_STOP,\n                _(\"Stop\"),\n                bitmaps_from_art_id(wx.ART_CROSS_MARK, wx.Size(32, 32)),\n            )\n            self.tbs[\"browser\"][\"refresh\"] = bar.add_simple_tool(\n                ID_WEB_REFRESH,\n                _(\"Refresh\"),\n                bitmaps_from_art_id(wx.ART_GO_TO_PARENT, wx.Size(32, 32)),\n            )\n            self.tbs[\"browser\"][\"addbookmark\"] = bar.add_simple_tool(\n                ID_WEB_ADDBOOKMARK,\n                _(\"Add bookmark\"),\n                bitmaps_from_art_id(wx.ART_ADD_BOOKMARK, wx.Size(32, 32)),\n            )\n            self.tbs[\"browser\"][\"newpage\"] = bar.add_simple_tool(\n                ID_WEB_NEW_WINDOW,\n                _(\"New page\"),\n                bitmaps_from_art_id(wx.ART_NEW, wx.Size(32, 32)),\n            )\n\n            self.make_handlers(ID_WEB_STOP, \"WebStop\", \"CanWebStop\", propagate=True)\n            self.make_handlers(\n                ID_WEB_REFRESH, \"WebRefresh\", \"CanWebRefresh\", propagate=True\n            )\n            self.make_handlers(\n                ID_WEB_ADDBOOKMARK,\n                \"WebAddBookmark\",\n                \"CanWebAddBookmark\",\n                propagate=True,\n            )\n            self.make_handlers(ID_WEB_NEW_WINDOW, \"WebNewWindow\", propagate=False)\n    else:\n        self.tbs[\"browser\"] = {}\n        self.tbs[\"browser\"][\"bar\"] = EmptyBar()\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.StandardButtons.create_edit_panel","title":"create_edit_panel","text":"<pre><code>create_edit_panel(toolbar_page)\n</code></pre> <p>Create the Clipboard panel with Copy, Cut, and Paste buttons.</p> <p>Parameters:</p> Name Type Description Default <code>toolbar_page</code> <p>The toolbar page (unused).</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def create_edit_panel(self, toolbar_page):\n    \"\"\"Create the Clipboard panel with Copy, Cut, and Paste buttons.\n\n    Args:\n        toolbar_page: The toolbar page (unused).\n    \"\"\"\n    if self.clipboard:\n        self.tbs[\"clipboard\"] = {}\n        bar = self.ti.create_panel_in_main_page(_(\"Clipboard\"), TYPE_BUTTONBAR)\n        bar.Refr = self.clipboard_refr\n        self.tbs[\"clipboard\"][\"bar\"] = bar\n        self.tbs[\"clipboard\"][\"copy\"] = bar.add_simple_tool(\n            wx.ID_COPY, _(\"Copy\"), bitmaps_from_art_id(wx.ART_COPY, wx.Size(32, 32))\n        )\n        self.tbs[\"clipboard\"][\"cut\"] = bar.add_simple_tool(\n            wx.ID_CUT, _(\"Cut\"), bitmaps_from_art_id(wx.ART_CUT, wx.Size(32, 32))\n        )\n        self.tbs[\"clipboard\"][\"paste\"] = bar.add_simple_tool(\n            wx.ID_PASTE,\n            _(\"Paste\"),\n            bitmaps_from_art_id(wx.ART_PASTE, wx.Size(32, 32)),\n        )\n\n        self.make_handlers(wx.ID_COPY, \"Copy\", \"CanCopy\")\n        self.make_handlers(wx.ID_CUT, \"Cut\", \"CanCut\")\n        self.make_handlers(wx.ID_PASTE, \"Paste\", \"CanPaste\")\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.StandardButtons.create_file_panel","title":"create_file_panel","text":"<pre><code>create_file_panel(toolbar_page)\n</code></pre> <p>Create the File panel with Open, Save, Print, and Exit buttons.</p> <p>Parameters:</p> Name Type Description Default <code>toolbar_page</code> <p>The toolbar page to add panels to (unused; buttons are added to the main page).</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def create_file_panel(self, toolbar_page):\n    \"\"\"Create the File panel with Open, Save, Print, and Exit buttons.\n\n    Args:\n        toolbar_page: The toolbar page to add panels to (unused; buttons\n            are added to the main page).\n    \"\"\"\n    if self.file:\n        bar = self.ti.create_panel_in_main_page(_(\"File\"), TYPE_TOOLBAR)\n        self.tbs[\"file\"] = {}\n        self.tbs[\"file\"][\"bar\"] = bar\n        if \"exit\" in self.gui_style:\n            self.tbs[\"file\"][\"exit\"] = bar.add_simple_tool(\n                wx.ID_EXIT,\n                _(\"Exit\"),\n                bitmaps_from_art_id(wx.ART_QUIT, wx.Size(32, 32)),\n            )\n        test = False\n        if \"open\" in self.gui_style:\n            bar.add_separator()\n            self.tbs[\"file\"][\"open\"] = bar.add_simple_tool(\n                wx.ID_OPEN,\n                _(\"Open\"),\n                bitmaps_from_art_id(wx.ART_FILE_OPEN, wx.Size(32, 32)),\n            )\n            test = True\n        if \"save\" in self.gui_style:\n            if not test:\n                bar.add_separator()\n            self.tbs[\"file\"][\"save\"] = bar.add_simple_tool(\n                wx.ID_SAVE,\n                _(\"Save\"),\n                bitmaps_from_art_id(wx.ART_FILE_SAVE, wx.Size(32, 32)),\n            )\n            self.make_handlers(wx.ID_SAVE, \"Save\", \"CanSave\", propagate=True)\n            if \"save_as\" in self.gui_style:\n                self.tbs[\"file\"][\"save_as\"] = bar.add_simple_tool(\n                    wx.ID_SAVEAS,\n                    _(\"Save as\"),\n                    bitmaps_from_art_id(wx.ART_FILE_SAVE_AS, wx.Size(32, 32)),\n                )\n                self.make_handlers(\n                    wx.ID_SAVEAS, \"SaveAs\", \"CanSaveAs\", propagate=True\n                )\n            test = True\n        if \"print\" in self.gui_style:\n            if test:\n                bar.add_separator()\n                self.tbs[\"file\"][\"print\"] = bar.add_hybrid_tool(\n                    ID_PRINT,\n                    _(\"Print\"),\n                    bitmaps_from_art_id(wx.ART_PRINT, wx.Size(32, 32)),\n                )\n                bar.add_separator()\n            else:\n                self.tbs[\"file\"][\"print\"] = bar.add_hybrid_tool(\n                    ID_PRINT,\n                    _(\"Print\"),\n                    bitmaps_from_art_id(wx.ART_PRINT, wx.Size(32, 32)),\n                )\n            self.make_handlers(ID_PRINT, \"Print\", \"CanPrint\", propagate=True)\n            self.ti.bind_dropdown(self.on_print, ID_PRINT)\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.StandardButtons.create_operations_panel","title":"create_operations_panel","text":"<pre><code>create_operations_panel(toolbar_page)\n</code></pre> <p>Create the Operations panel with Undo, Redo, Find, etc.</p> <p>Parameters:</p> Name Type Description Default <code>toolbar_page</code> <p>The toolbar page (unused).</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def create_operations_panel(self, toolbar_page):\n    \"\"\"Create the Operations panel with Undo, Redo, Find, etc.\n\n    Args:\n        toolbar_page: The toolbar page (unused).\n    \"\"\"\n    if self.operations:\n        self.tbs[\"operations\"] = {}\n        bar = self.ti.create_panel_in_main_page(_(\"Operations\"), TYPE_TOOLBAR)\n        self.tbs[\"operations\"][\"bar\"] = bar\n        self.tbs[\"operations\"][\"undo\"] = bar.add_simple_tool(\n            wx.ID_UNDO, _(\"Undo\"), bitmaps_from_art_id(wx.ART_UNDO, wx.Size(32, 32))\n        )\n        self.tbs[\"operations\"][\"redo\"] = bar.add_simple_tool(\n            wx.ID_REDO, _(\"Redo\"), bitmaps_from_art_id(wx.ART_REDO, wx.Size(32, 32))\n        )\n        self.tbs[\"operations\"][\"find\"] = bar.add_hybrid_tool(\n            ID_FIND, _(\"Find\"), bitmaps_from_art_id(wx.ART_FIND, wx.Size(32, 32))\n        )\n        bar.add_separator()\n        self.tbs[\"operations\"][\"filemanager\"] = bar.add_simple_tool(\n            wx.ID_ANY,\n            _(\"File manager\"),\n            bitmaps_from_art_id(wx.ART_FOLDER_OPEN, wx.Size(32, 32)),\n        )\n        self.tbs[\"operations\"][\"editor\"] = bar.add_simple_tool(\n            wx.ID_ANY,\n            _(\"Editor\"),\n            bitmaps_from_art_id(wx.ART_NORMAL_FILE, wx.Size(32, 32)),\n        )\n        self.ti.bind_dropdown(self.on_find, ID_FIND)\n\n        self.make_handlers(wx.ID_UNDO, \"Undo\", \"CanUndo\")\n        self.make_handlers(wx.ID_REDO, \"Redo\", \"CanRedo\")\n        self.make_handlers(ID_FIND, \"Find\", \"CanFind\")\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.StandardButtons.load_page","title":"load_page","text":"<pre><code>load_page(address)\n</code></pre> <p>Load a URL in the browser panel if one is active.</p> <p>Parameters:</p> Name Type Description Default <code>address</code> <p>URL to load.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def load_page(self, address):\n    \"\"\"Load a URL in the browser panel if one is active.\n\n    Args:\n        address: URL to load.\n    \"\"\"\n    if \"browser\" in self._object_list and self._object_list[\"browser\"]:\n        self._object_list[\"browser\"].Go(address)\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.StandardButtons.make_handler","title":"make_handler","text":"<pre><code>make_handler(fun_name, event_id, propagate=False)\n</code></pre> <p>Create a command handler that delegates to the focused control.</p> <p>Walks up the focus chain calling the named method on the first control that has it.</p> <p>Parameters:</p> Name Type Description Default <code>fun_name</code> <p>Name of the method to call (e.g. 'Copy').</p> required <code>event_id</code> <p>wx event ID to bind to.</p> required <code>propagate</code> <p>If True, walk up the parent chain.</p> <code>False</code> Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def make_handler(self, fun_name, event_id, propagate=False):\n    \"\"\"Create a command handler that delegates to the focused control.\n\n    Walks up the focus chain calling the named method on the first\n    control that has it.\n\n    Args:\n        fun_name: Name of the method to call (e.g. 'Copy').\n        event_id: wx event ID to bind to.\n        propagate: If True, walk up the parent chain.\n    \"\"\"\n\n    def on_command(event):\n        win = find_focus_in_form()\n        while win:\n            if hasattr(win, fun_name):\n                getattr(win, fun_name)()\n            if propagate:\n                win = win.GetParent()\n            else:\n                break\n\n    self.toolbar_interface.bind(on_command, id=event_id)\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.StandardButtons.make_handlers","title":"make_handlers","text":"<pre><code>make_handlers(event_id, fun_name, fun_name2=None, propagate=False)\n</code></pre> <p>Create both command and UI update handlers for a button.</p> <p>Parameters:</p> Name Type Description Default <code>event_id</code> <p>wx event ID to bind to.</p> required <code>fun_name</code> <p>Name of the command method (e.g. 'Copy').</p> required <code>fun_name2</code> <p>Name of the UI update method (e.g. 'CanCopy'). If None, the button is always enabled.</p> <code>None</code> <code>propagate</code> <p>If True, walk up the parent chain.</p> <code>False</code> Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def make_handlers(self, event_id, fun_name, fun_name2=None, propagate=False):\n    \"\"\"Create both command and UI update handlers for a button.\n\n    Args:\n        event_id: wx event ID to bind to.\n        fun_name: Name of the command method (e.g. 'Copy').\n        fun_name2: Name of the UI update method (e.g. 'CanCopy').\n            If None, the button is always enabled.\n        propagate: If True, walk up the parent chain.\n    \"\"\"\n    self.make_handler(fun_name, event_id, propagate)\n    if fun_name2:\n        self.make_ui_handler(fun_name2, event_id, propagate)\n    else:\n\n        def on_update_ui(event):\n            event.Enable(True)\n\n        self.toolbar_interface.parent.Bind(\n            wx.EVT_UPDATE_UI, on_update_ui, id=event_id\n        )\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.StandardButtons.make_menu","title":"make_menu","text":"<pre><code>make_menu(menubar, menulist)\n</code></pre> <p>Build a flat menu bar from a nested menu definition.</p> <p>Parameters:</p> Name Type Description Default <code>menubar</code> <p>The FlatMenuBar to populate.</p> required <code>menulist</code> <p>List of (title, ((label, id), ...)) tuples.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def make_menu(self, menubar, menulist):\n    \"\"\"Build a flat menu bar from a nested menu definition.\n\n    Args:\n        menubar: The FlatMenuBar to populate.\n        menulist: List of (title, ((label, id), ...)) tuples.\n    \"\"\"\n    for item in menulist:\n        menu = FM.FlatMenu()\n        for pos in item[1]:\n            menuitem = FM.FlatMenuItem(menu, pos[1], pos[0], pos[0], wx.ITEM_NORMAL)\n            menu.AppendItem(menuitem)\n        menubar.Append(menu, item[0])\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.StandardButtons.make_ui_handler","title":"make_ui_handler","text":"<pre><code>make_ui_handler(fun_name, event_id, propagate=False)\n</code></pre> <p>Create a UI update handler that enables/disables based on focus.</p> <p>Walks up the focus chain calling the named method; enables the event if any focused control returns True.</p> <p>Parameters:</p> Name Type Description Default <code>fun_name</code> <p>Name of the method to call (e.g. 'CanCopy').</p> required <code>event_id</code> <p>wx event ID to bind to.</p> required <code>propagate</code> <p>If True, walk up the parent chain.</p> <code>False</code> Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def make_ui_handler(self, fun_name, event_id, propagate=False):\n    \"\"\"Create a UI update handler that enables/disables based on focus.\n\n    Walks up the focus chain calling the named method; enables\n    the event if any focused control returns True.\n\n    Args:\n        fun_name: Name of the method to call (e.g. 'CanCopy').\n        event_id: wx event ID to bind to.\n        propagate: If True, walk up the parent chain.\n    \"\"\"\n\n    def on_update_ui(event):\n        win = find_focus_in_form()\n        while win:\n            if hasattr(win, fun_name):\n                try:\n                    if getattr(win, fun_name)():\n                        event.Enable(True)\n                        return\n                except wx._core.wxAssertionError:\n                    pass\n            if propagate:\n                win = win.GetParent()\n            else:\n                break\n        event.Enable(False)\n\n    self.toolbar_interface.parent.Bind(wx.EVT_UPDATE_UI, on_update_ui, id=event_id)\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.StandardButtons.memke_menu","title":"memke_menu","text":"<pre><code>memke_menu(menubar, menulist)\n</code></pre> <p>Backward compatibility alias for make_menu.</p> <p>Parameters:</p> Name Type Description Default <code>menubar</code> <p>The FlatMenuBar to populate.</p> required <code>menulist</code> <p>List of (title, ((label, id), ...)) tuples.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def memke_menu(self, menubar, menulist):\n    \"\"\"Backward compatibility alias for make_menu.\n\n    Args:\n        menubar: The FlatMenuBar to populate.\n        menulist: List of (title, ((label, id), ...)) tuples.\n    \"\"\"\n    self.make_menu(menubar, menulist)\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.StandardButtons.on_find","title":"on_find","text":"<pre><code>on_find(event)\n</code></pre> <p>Show the find/replace dropdown menu.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>The dropdown event.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def on_find(self, event):\n    \"\"\"Show the find/replace dropdown menu.\n\n    Args:\n        event: The dropdown event.\n    \"\"\"\n    menu = FM.FlatMenu()\n    menu.Append(wx.ID_ANY, _(\"Find\"), _(\"Find\"), wx.ITEM_NORMAL)\n    menu.Append(wx.ID_ANY, _(\"Replace\"), _(\"Replace\"), wx.ITEM_NORMAL)\n    event.PopupMenu(menu)\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.StandardButtons.on_print","title":"on_print","text":"<pre><code>on_print(event)\n</code></pre> <p>Show the print dropdown menu.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>The dropdown event.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def on_print(self, event):\n    \"\"\"Show the print dropdown menu.\n\n    Args:\n        event: The dropdown event.\n    \"\"\"\n    menu = FM.FlatMenu()\n    menu.Append(wx.ID_ANY, _(\"Print\"), _(\"Print\"), wx.ITEM_NORMAL)\n    menu.Append(wx.ID_ANY, _(\"Print preview\"), _(\"Print preview\"), wx.ITEM_NORMAL)\n    menu.Append(\n        wx.ID_ANY, _(\"Printer settings\"), _(\"Printer settings\"), wx.ITEM_NORMAL\n    )\n    self.ti.popup_menu(event, menu)\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.StandardButtons.open_page","title":"open_page","text":"<pre><code>open_page(page)\n</code></pre> <p>Open a URL in a new web view page.</p> <p>Parameters:</p> Name Type Description Default <code>page</code> <p>URL to open.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>True on success.</p> Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def open_page(self, page):\n    \"\"\"Open a URL in a new web view page.\n\n    Args:\n        page: URL to open.\n\n    Returns:\n        bool: True on success.\n    \"\"\"\n    name = self._title_from_address(page)\n    okno = (\n        wx.GetApp()\n        .GetTopWindow()\n        .new_main_page(\"^standard/webview/widget_web.html\", name)\n    )\n    okno.body.WEB.Go(page)\n    return True\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.StandardButtons.operations_refr","title":"operations_refr","text":"<pre><code>operations_refr(editctrl)\n</code></pre> <p>Refresh operations buttons based on the focused control's capabilities.</p> <p>Parameters:</p> Name Type Description Default <code>editctrl</code> <p>The currently focused edit control, or None.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def operations_refr(self, editctrl):\n    \"\"\"Refresh operations buttons based on the focused control's capabilities.\n\n    Args:\n        editctrl: The currently focused edit control, or None.\n    \"\"\"\n    if self.operations:\n        tab_status = []\n        if editctrl:\n            tab_status.append(\n                (\n                    hasattr(editctrl, \"CanRedo\") and editctrl.CanRedo(),\n                    \"operations\",\n                    \"redo\",\n                )\n            )\n            tab_status.append(\n                (\n                    hasattr(editctrl, \"CanUndo\") and editctrl.CanUndo(),\n                    \"operations\",\n                    \"undo\",\n                )\n            )\n        else:\n            tab_status.append((False, \"operations\", \"redo\"))\n            tab_status.append((False, \"operations\", \"undo\"))\n        return self._status(tab_status)\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.StandardButtons.realize","title":"realize","text":"<pre><code>realize()\n</code></pre> <p>No-op realization method for compatibility.</p> Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def realize(self):\n    \"\"\"No-op realization method for compatibility.\"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.StandardButtons.web_menu","title":"web_menu","text":"<pre><code>web_menu()\n</code></pre> <p>Return the web menu structure definition.</p> <p>Returns:</p> Name Type Description <code>tuple</code> <p>Nested menu definition for the web toolbar menu.</p> Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def web_menu(self):\n    \"\"\"Return the web menu structure definition.\n\n    Returns:\n        tuple: Nested menu definition for the web toolbar menu.\n    \"\"\"\n    menu = (\n        (\n            _(\"View\"),\n            (\n                (_(\"Zoom\"), ID_ZOOM),\n                (_(\"Block elements\"), ID_PAGE_BLOCK),\n                (_(\"View elements\"), ID_PAGE_VIEW),\n                (_(\"View page source\"), ID_WEB_SOURCE),\n                (_(\"Edit page\"), ID_WEB_EDIT),\n            ),\n        ),\n        (\n            _(\"History\"),\n            (\n                (_(\"Show history panel\"), ID_HISTORY_SHOW),\n                (_(\"Clear history\"), ID_CLEAR_HISTORY),\n            ),\n        ),\n        (\n            _(\"Bookmarks\"),\n            (\n                (_(\"Add to bookmarks\"), ID_BOOKMARK_ADD),\n                (_(\"Show bookmarks panel\"), ID_SHOW_BOOKMARKS),\n            ),\n        ),\n        (\n            _(\"Downloads\"),\n            (\n                (_(\"Show download panel\"), ID_SHOW_DOWNLOAD),\n                (_(\"Options\"), ID_DOWNLOAD_OPTIONS),\n            ),\n        ),\n        (\n            _(\"Tools\"),\n            (\n                (_(\"Show download panel\"), ID_SHOW_DOWNLOAD),\n                (_(\"Options\"), ID_DOWNLOAD_OPTIONS),\n            ),\n        ),\n        (\n            _(\"Options\"),\n            (\n                (_(\"General\"), ID_GENERAL_OPTIONS),\n                (_(\"Black list\"), ID_DOWNLOAD_OPTIONS),\n            ),\n        ),\n    )\n    return menu\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.StandardButtons.web_refr","title":"web_refr","text":"<pre><code>web_refr(webobject)\n</code></pre> <p>Refresh browser navigation buttons based on web view state.</p> <p>Parameters:</p> Name Type Description Default <code>webobject</code> <p>The web view control, or None.</p> required Source code in <code>pytigon_gui/toolbar/standardtoolbarbuttons.py</code> <pre><code>def web_refr(self, webobject):\n    \"\"\"Refresh browser navigation buttons based on web view state.\n\n    Args:\n        webobject: The web view control, or None.\n    \"\"\"\n    if self.browse or self.nav:\n        tab_status = []\n        if webobject:\n            tab_status.append((webobject.CanGoBack(), \"browser\", \"back\"))\n            tab_status.append((webobject.CanGoForward(), \"browser\", \"forward\"))\n            if self.browse:\n                tab_status.append((True, \"browser\", \"stop\"))\n                tab_status.append((True, \"browser\", \"refresh\"))\n                tab_status.append((True, \"browser\", \"addbookmark\"))\n                tab_status.append((True, \"browser\", \"newpage\"))\n        else:\n            tab_status.append((False, \"browser\", \"back\"))\n            tab_status.append((False, \"browser\", \"forward\"))\n            if self.browse:\n                tab_status.append((False, \"browser\", \"stop\"))\n                tab_status.append((False, \"browser\", \"refresh\"))\n                tab_status.append((False, \"browser\", \"addbookmark\"))\n                tab_status.append((False, \"browser\", \"newpage\"))\n        return self._status(tab_status)\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.WebHistory","title":"WebHistory","text":"<p>A simple list-like object providing address bar history.</p> <p>Wraps the ADDRESS_HISTORY list and exposes a limited subset.</p>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons-functions","title":"Functions:","text":""},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.bitmaps_from_art_id","title":"bitmaps_from_art_id","text":"<pre><code>bitmaps_from_art_id(art_id, size)\n</code></pre> <p>Get a tuple of two bitmaps from ArtProvider.</p> <p>The first is the standard bitmap, the second is the same bitmap converted to grayscale to emulate a disabled state.</p> <p>Parameters:</p> Name Type Description Default <code>art_id</code> <p>Bitmap id in ArtProvider.</p> required <code>size</code> <p>wx.Size or (width, height) tuple.</p> required <p>Returns:</p> Type Description <p>(bitmap, bitmap_disabled) tuple.</p> Source code in <code>pytigon_gui/guilib/image.py</code> <pre><code>def bitmaps_from_art_id(art_id, size):\n    \"\"\"Get a tuple of two bitmaps from ArtProvider.\n\n    The first is the standard bitmap, the second is the same bitmap\n    converted to grayscale to emulate a disabled state.\n\n    Args:\n        art_id: Bitmap id in ArtProvider.\n        size: wx.Size or (width, height) tuple.\n\n    Returns:\n        (bitmap, bitmap_disabled) tuple.\n    \"\"\"\n    bitmap = wx.ArtProvider.GetBitmap(art_id, wx.ART_OTHER, size)\n    img = bitmap.ConvertToImage()\n    bitmap_disabled = wx.Bitmap(img.ConvertToGreyscale().AdjustChannels(1, 1, 1, 0.3))\n    return (bitmap, bitmap_disabled)\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.colour_to_html","title":"colour_to_html","text":"<pre><code>colour_to_html(colour)\n</code></pre> <p>Convert a wx.Colour to an HTML hex string (e.g. '#AABBCC').</p> <p>Parameters:</p> Name Type Description Default <code>colour</code> <p>wx.Colour instance.</p> required <p>Returns:</p> Type Description <p>HTML color string.</p> Source code in <code>pytigon_gui/guilib/tools.py</code> <pre><code>def colour_to_html(colour):\n    \"\"\"Convert a wx.Colour to an HTML hex string (e.g. '#AABBCC').\n\n    Args:\n        colour: wx.Colour instance.\n\n    Returns:\n        HTML color string.\n    \"\"\"\n    return wx.Colour(colour.Red(), colour.Green(), colour.Blue()).GetAsString(\n        wx.C2S_HTML_SYNTAX\n    )\n</code></pre>"},{"location":"api/toolbar_standardtoolbarbuttons/#pytigon_gui.toolbar.standardtoolbarbuttons.find_focus_in_form","title":"find_focus_in_form","text":"<pre><code>find_focus_in_form()\n</code></pre> <p>Find the currently focused control that belongs to a SchForm.</p> <p>Walks up the window hierarchy from the current focus to locate a SchForm parent. Caches the last known focused control and invalidates it if the parent form is closing.</p> <p>Returns:</p> Type Description <p>The focused wx.Window inside a SchForm, or None.</p> Source code in <code>pytigon_gui/guilib/tools.py</code> <pre><code>def find_focus_in_form():\n    \"\"\"Find the currently focused control that belongs to a SchForm.\n\n    Walks up the window hierarchy from the current focus to locate a\n    SchForm parent. Caches the last known focused control and invalidates\n    it if the parent form is closing.\n\n    Returns:\n        The focused wx.Window inside a SchForm, or None.\n    \"\"\"\n    global LAST_FOCUS_CTRL_IN_FORM\n\n    win_focus = wx.Window.FindFocus()\n    win = win_focus\n    while win:\n        if win.__class__.__name__ == \"SchForm\":\n            LAST_FOCUS_CTRL_IN_FORM = win_focus\n            return win_focus\n        win = win.GetParent()\n\n    if LAST_FOCUS_CTRL_IN_FORM and (\n        not hasattr(LAST_FOCUS_CTRL_IN_FORM, \"parent\")\n        or not LAST_FOCUS_CTRL_IN_FORM.parent\n        or (\n            hasattr(LAST_FOCUS_CTRL_IN_FORM.parent, \"closing\")\n            and LAST_FOCUS_CTRL_IN_FORM.parent.closing\n        )\n    ):\n        LAST_FOCUS_CTRL_IN_FORM = None\n        return None\n\n    return LAST_FOCUS_CTRL_IN_FORM\n</code></pre>"},{"location":"api/toolbar_treetoolbar/","title":"toolbar.treetoolbar \u2013 Tree Toolbar","text":"<p>Tree-view based toolbar for hierarchical command navigation with expandable/collapsible command groups.</p>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar","title":"treetoolbar","text":"<p>Tree-based toolbar implementation.</p> <p>Provides a vertical tree navigation toolbar using CustomTreeCtrl from wx.lib.agw.</p>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar-attributes","title":"Attributes","text":""},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_BOOKMARK_ADD","title":"ID_BOOKMARK_ADD  <code>module-attribute</code>","text":"<pre><code>ID_BOOKMARK_ADD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_CLEAR_HISTORY","title":"ID_CLEAR_HISTORY  <code>module-attribute</code>","text":"<pre><code>ID_CLEAR_HISTORY = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_CLOSETAB","title":"ID_CLOSETAB  <code>module-attribute</code>","text":"<pre><code>ID_CLOSETAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_COPY","title":"ID_COPY  <code>module-attribute</code>","text":"<pre><code>ID_COPY = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_CUT","title":"ID_CUT  <code>module-attribute</code>","text":"<pre><code>ID_CUT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_DOWNLOAD_OPTIONS","title":"ID_DOWNLOAD_OPTIONS  <code>module-attribute</code>","text":"<pre><code>ID_DOWNLOAD_OPTIONS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_END","title":"ID_END  <code>module-attribute</code>","text":"<pre><code>ID_END = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_EXIT","title":"ID_EXIT  <code>module-attribute</code>","text":"<pre><code>ID_EXIT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_FIND","title":"ID_FIND  <code>module-attribute</code>","text":"<pre><code>ID_FIND = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_GENERAL_OPTIONS","title":"ID_GENERAL_OPTIONS  <code>module-attribute</code>","text":"<pre><code>ID_GENERAL_OPTIONS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_GOTODESKTOP","title":"ID_GOTODESKTOP  <code>module-attribute</code>","text":"<pre><code>ID_GOTODESKTOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_GOTOFOOTER","title":"ID_GOTOFOOTER  <code>module-attribute</code>","text":"<pre><code>ID_GOTOFOOTER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_GOTOHEADER","title":"ID_GOTOHEADER  <code>module-attribute</code>","text":"<pre><code>ID_GOTOHEADER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_GOTOPANEL","title":"ID_GOTOPANEL  <code>module-attribute</code>","text":"<pre><code>ID_GOTOPANEL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_HELP","title":"ID_HELP  <code>module-attribute</code>","text":"<pre><code>ID_HELP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_HISTORY_SHOW","title":"ID_HISTORY_SHOW  <code>module-attribute</code>","text":"<pre><code>ID_HISTORY_SHOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_LOAD","title":"ID_LOAD  <code>module-attribute</code>","text":"<pre><code>ID_LOAD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_NEXTPAGE","title":"ID_NEXTPAGE  <code>module-attribute</code>","text":"<pre><code>ID_NEXTPAGE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_NEXTTAB","title":"ID_NEXTTAB  <code>module-attribute</code>","text":"<pre><code>ID_NEXTTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_PAGE_BLOCK","title":"ID_PAGE_BLOCK  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_BLOCK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_PAGE_SOURCE","title":"ID_PAGE_SOURCE  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_SOURCE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_PAGE_VIEW","title":"ID_PAGE_VIEW  <code>module-attribute</code>","text":"<pre><code>ID_PAGE_VIEW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_PASTE","title":"ID_PASTE  <code>module-attribute</code>","text":"<pre><code>ID_PASTE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_PREVPAGE","title":"ID_PREVPAGE  <code>module-attribute</code>","text":"<pre><code>ID_PREVPAGE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_PREVTAB","title":"ID_PREVTAB  <code>module-attribute</code>","text":"<pre><code>ID_PREVTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_PRINT","title":"ID_PRINT  <code>module-attribute</code>","text":"<pre><code>ID_PRINT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_PRINT_PREVIEW","title":"ID_PRINT_PREVIEW  <code>module-attribute</code>","text":"<pre><code>ID_PRINT_PREVIEW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_PRINT_SETTINGS","title":"ID_PRINT_SETTINGS  <code>module-attribute</code>","text":"<pre><code>ID_PRINT_SETTINGS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_REDO","title":"ID_REDO  <code>module-attribute</code>","text":"<pre><code>ID_REDO = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_REFRESHTAB","title":"ID_REFRESHTAB  <code>module-attribute</code>","text":"<pre><code>ID_REFRESHTAB = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_REPLACE","title":"ID_REPLACE  <code>module-attribute</code>","text":"<pre><code>ID_REPLACE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_RESET","title":"ID_RESET  <code>module-attribute</code>","text":"<pre><code>ID_RESET = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_RETVALUE","title":"ID_RETVALUE  <code>module-attribute</code>","text":"<pre><code>ID_RETVALUE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_SAVE","title":"ID_SAVE  <code>module-attribute</code>","text":"<pre><code>ID_SAVE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_SAVE_AS","title":"ID_SAVE_AS  <code>module-attribute</code>","text":"<pre><code>ID_SAVE_AS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_SELALL","title":"ID_SELALL  <code>module-attribute</code>","text":"<pre><code>ID_SELALL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_SHOWDESKTOP","title":"ID_SHOWDESKTOP  <code>module-attribute</code>","text":"<pre><code>ID_SHOWDESKTOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_SHOWFOOTER","title":"ID_SHOWFOOTER  <code>module-attribute</code>","text":"<pre><code>ID_SHOWFOOTER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_SHOWHEADER","title":"ID_SHOWHEADER  <code>module-attribute</code>","text":"<pre><code>ID_SHOWHEADER = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_SHOWPANEL","title":"ID_SHOWPANEL  <code>module-attribute</code>","text":"<pre><code>ID_SHOWPANEL = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_SHOWSTATUSBAR","title":"ID_SHOWSTATUSBAR  <code>module-attribute</code>","text":"<pre><code>ID_SHOWSTATUSBAR = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_SHOWTOOLBAR1","title":"ID_SHOWTOOLBAR1  <code>module-attribute</code>","text":"<pre><code>ID_SHOWTOOLBAR1 = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_SHOWTOOLBAR2","title":"ID_SHOWTOOLBAR2  <code>module-attribute</code>","text":"<pre><code>ID_SHOWTOOLBAR2 = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_SHOW_BOOKMARKS","title":"ID_SHOW_BOOKMARKS  <code>module-attribute</code>","text":"<pre><code>ID_SHOW_BOOKMARKS = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_SHOW_DOWNLOAD","title":"ID_SHOW_DOWNLOAD  <code>module-attribute</code>","text":"<pre><code>ID_SHOW_DOWNLOAD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_START","title":"ID_START  <code>module-attribute</code>","text":"<pre><code>ID_START = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_TASKBAR_CLOSE","title":"ID_TASKBAR_CLOSE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_CLOSE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_TASKBAR_HIDE","title":"ID_TASKBAR_HIDE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_HIDE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_TASKBAR_SHOW","title":"ID_TASKBAR_SHOW  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_SHOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_TASKBAR_TOGGLE","title":"ID_TASKBAR_TOGGLE  <code>module-attribute</code>","text":"<pre><code>ID_TASKBAR_TOGGLE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_UNDO","title":"ID_UNDO  <code>module-attribute</code>","text":"<pre><code>ID_UNDO = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_WEB_ADDBOOKMARK","title":"ID_WEB_ADDBOOKMARK  <code>module-attribute</code>","text":"<pre><code>ID_WEB_ADDBOOKMARK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_WEB_BACK","title":"ID_WEB_BACK  <code>module-attribute</code>","text":"<pre><code>ID_WEB_BACK = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_WEB_DOWN","title":"ID_WEB_DOWN  <code>module-attribute</code>","text":"<pre><code>ID_WEB_DOWN = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_WEB_EDIT","title":"ID_WEB_EDIT  <code>module-attribute</code>","text":"<pre><code>ID_WEB_EDIT = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_WEB_FORWARD","title":"ID_WEB_FORWARD  <code>module-attribute</code>","text":"<pre><code>ID_WEB_FORWARD = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_WEB_NEW_WINDOW","title":"ID_WEB_NEW_WINDOW  <code>module-attribute</code>","text":"<pre><code>ID_WEB_NEW_WINDOW = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_WEB_REFRESH","title":"ID_WEB_REFRESH  <code>module-attribute</code>","text":"<pre><code>ID_WEB_REFRESH = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_WEB_SOURCE","title":"ID_WEB_SOURCE  <code>module-attribute</code>","text":"<pre><code>ID_WEB_SOURCE = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_WEB_STOP","title":"ID_WEB_STOP  <code>module-attribute</code>","text":"<pre><code>ID_WEB_STOP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_WEB_UP","title":"ID_WEB_UP  <code>module-attribute</code>","text":"<pre><code>ID_WEB_UP = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ID_ZOOM","title":"ID_ZOOM  <code>module-attribute</code>","text":"<pre><code>ID_ZOOM = wx.Window.NewControlId()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar._","title":"_  <code>module-attribute</code>","text":"<pre><code>_ = wx.GetTranslation\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar-classes","title":"Classes","text":""},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.BaseHtmlPanel","title":"BaseHtmlPanel","text":"<pre><code>BaseHtmlPanel(page, real_panel)\n</code></pre> <p>Base panel that can host an HTML-rendered page within a toolbar.</p> <p>This is used by toolbar types that support embedding web content (e.g. ModernToolbarBar, TreeToolbarBar).</p> <p>Initialize the HTML panel wrapper.</p> <p>Parameters:</p> Name Type Description Default <code>page</code> <p>Parent ToolbarPage object.</p> required <code>real_panel</code> <p>The actual wx.Panel that will contain the HTML page.</p> required Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, page, real_panel):\n    \"\"\"Initialize the HTML panel wrapper.\n\n    Args:\n        page: Parent ToolbarPage object.\n        real_panel: The actual wx.Panel that will contain the HTML page.\n    \"\"\"\n    self.page = page\n    self.real_panel = real_panel\n    self.html_page = None\n    self.sizer = wx.BoxSizer(wx.VERTICAL)\n    self.real_panel.SetSizer(self.sizer)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.BaseHtmlPanel-functions","title":"Methods:","text":""},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.BaseHtmlPanel.get_height","title":"get_height","text":"<pre><code>get_height()\n</code></pre> <p>Return the preferred height of the panel.</p> <p>Override in subclasses to provide actual dimensions.</p> <p>Returns:</p> Name Type Description <code>int</code> <p>Height in pixels (0 by default).</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def get_height(self):\n    \"\"\"Return the preferred height of the panel.\n\n    Override in subclasses to provide actual dimensions.\n\n    Returns:\n        int: Height in pixels (0 by default).\n    \"\"\"\n    return 0\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.BaseHtmlPanel.get_width","title":"get_width","text":"<pre><code>get_width()\n</code></pre> <p>Return the preferred width of the panel.</p> <p>Override in subclasses to provide actual dimensions.</p> <p>Returns:</p> Name Type Description <code>int</code> <p>Width in pixels (0 by default).</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def get_width(self):\n    \"\"\"Return the preferred width of the panel.\n\n    Override in subclasses to provide actual dimensions.\n\n    Returns:\n        int: Width in pixels (0 by default).\n    \"\"\"\n    return 0\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.BaseHtmlPanel.get_window","title":"get_window","text":"<pre><code>get_window()\n</code></pre> <p>Return the underlying wx.Window for this panel.</p> <p>Returns:</p> Type Description <p>wx.Window: The real panel window.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def get_window(self):\n    \"\"\"Return the underlying wx.Window for this panel.\n\n    Returns:\n        wx.Window: The real panel window.\n    \"\"\"\n    return self.real_panel\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.BaseHtmlPanel.set_page","title":"set_page","text":"<pre><code>set_page(html_page)\n</code></pre> <p>Replace the current HTML page with a new one.</p> <p>If a page already exists, it is replaced in the sizer and destroyed. Otherwise the new page is added to the sizer.</p> <p>Parameters:</p> Name Type Description Default <code>html_page</code> <p>The new SchPage (or wx.Window) to display.</p> required Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def set_page(self, html_page):\n    \"\"\"Replace the current HTML page with a new one.\n\n    If a page already exists, it is replaced in the sizer and destroyed.\n    Otherwise the new page is added to the sizer.\n\n    Args:\n        html_page: The new SchPage (or wx.Window) to display.\n    \"\"\"\n    if self.html_page:\n        self.sizer.Replace(self.html_page, html_page)\n        self.html_page.Destroy()\n    else:\n        self.sizer.Add(html_page, 0, wx.LEFT | wx.TOP | wx.EXPAND | wx.RIGHT, 2)\n\n    self.html_page = html_page\n\n    self.sizer.Fit(self.real_panel)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.SchBaseCtrl","title":"SchBaseCtrl","text":"<pre><code>SchBaseCtrl(parent, kwds)\n</code></pre> <p>Base mixin class for all SchForm wxPython widgets.</p> <p>Provides: - Attribute initialization from HTML tag keyword arguments - Parent form/page navigation - Server data loading for tdata (table data) and ldata (list data) - Common keyboard handling (Escape, Tab) - Widget lifecycle: after_create signal, unique name management</p> <p>Initialize base control attributes.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent wxPython window.</p> required <code>kwds</code> <p>Keyword argument dict parsed from HTML tag attributes.</p> required Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def __init__(self, parent, kwds):\n    \"\"\"Initialize base control attributes.\n\n    Args:\n        parent: Parent wxPython window.\n        kwds: Keyword argument dict parsed from HTML tag attributes.\n    \"\"\"\n    self.unique_name = None\n    self.init_base(kwds, parent)\n    self.accept_focus = True\n    self.acc_tab_bind = False\n    form = self.get_parent_form()\n    if form:\n        form.signal_from_child(self, \"__init__\")\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.SchBaseCtrl-functions","title":"Methods:","text":""},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.SchBaseCtrl.CanAcceptFocus","title":"CanAcceptFocus","text":"<pre><code>CanAcceptFocus()\n</code></pre> <p>Return True if the control can accept keyboard focus.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def CanAcceptFocus(self):\n    \"\"\"Return True if the control can accept keyboard focus.\"\"\"\n    if self.IsShown() and self.IsEnabled():\n        return self.accept_focus\n    return False\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.SchBaseCtrl.CanAcceptFocusFromKeyboard","title":"CanAcceptFocusFromKeyboard","text":"<pre><code>CanAcceptFocusFromKeyboard()\n</code></pre> <p>Return True if the control accepts focus from keyboard navigation.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def CanAcceptFocusFromKeyboard(self):\n    \"\"\"Return True if the control accepts focus from keyboard navigation.\"\"\"\n    return self.CanAcceptFocus()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.SchBaseCtrl.after_create","title":"after_create","text":"<pre><code>after_create()\n</code></pre> <p>Called immediately after the widget is fully constructed.</p> <p>Binds keyboard events, executes the 'onload' script (if any), calls the ext_init hook if present, and signals the parent form that this child is ready.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def after_create(self):\n    \"\"\"Called immediately after the widget is fully constructed.\n\n    Binds keyboard events, executes the 'onload' script (if any),\n    calls the __ext_init__ hook if present, and signals the parent\n    form that this child is ready.\n    \"\"\"\n    self.Bind(wx.EVT_KEY_DOWN, self.on_key_down_base)\n    if self.onload:\n        d = {\"wx\": wx, \"self\": self}\n        try:\n            exec(self.onload, d)\n        except Exception:\n            logger.exception(\"Error executing onload script for %s\", self.GetName())\n\n    if hasattr(self, \"__ext_init__\"):\n        self.__ext_init__()\n\n    self.get_parent_form().signal_from_child(self, \"after_create\")\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.SchBaseCtrl.get_ldata","title":"get_ldata","text":"<pre><code>get_ldata()\n</code></pre> <p>Return hierarchical list data, loading from server if necessary.</p> <p>Returns:</p> Type Description <p>List data (nested tree structure), or None if unavailable.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_ldata(self):\n    \"\"\"Return hierarchical list data, loading from server if necessary.\n\n    Returns:\n        List data (nested tree structure), or None if unavailable.\n    \"\"\"\n    if self.ldata is not None:\n        return self.ldata\n    if self.ldatabuf is not None:\n        return self.ldatabuf\n    if self.src:\n        self.refresh_ldata()\n    return self.ldatabuf\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.SchBaseCtrl.get_parent_form","title":"get_parent_form","text":"<pre><code>get_parent_form()\n</code></pre> <p>Walk up the widget hierarchy to find the parent SchForm.</p> <p>Returns:</p> Type Description <p>The SchForm ancestor, or None if not found.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_parent_form(self):\n    \"\"\"Walk up the widget hierarchy to find the parent SchForm.\n\n    Returns:\n        The SchForm ancestor, or None if not found.\n    \"\"\"\n    parent = self.parent\n    while parent is not None:\n        if type(parent).__name__ == \"SchForm\":\n            return parent\n        parent = parent.GetParent()\n    return None\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.SchBaseCtrl.get_parent_page","title":"get_parent_page","text":"<pre><code>get_parent_page()\n</code></pre> <p>Return the parent page that contains this control's form.</p> <p>Returns:</p> Type Description <p>The parent SchPage, or None if not found.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_parent_page(self):\n    \"\"\"Return the parent page that contains this control's form.\n\n    Returns:\n        The parent SchPage, or None if not found.\n    \"\"\"\n    form = self.get_parent_form()\n    if form:\n        return form.get_parent_page()\n    return None\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.SchBaseCtrl.get_tdata","title":"get_tdata","text":"<pre><code>get_tdata()\n</code></pre> <p>Return table data, loading from server if necessary.</p> <p>Returns:</p> Type Description <p>Table data (list of rows), or None if unavailable.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_tdata(self):\n    \"\"\"Return table data, loading from server if necessary.\n\n    Returns:\n        Table data (list of rows), or None if unavailable.\n    \"\"\"\n    if self.tdata is not None:\n        return self.tdata\n    if self.tdatabuf is not None:\n        return self.tdatabuf\n    if self.src:\n        self.refresh_tdata()\n    return self.tdatabuf\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.SchBaseCtrl.get_unique_name","title":"get_unique_name","text":"<pre><code>get_unique_name()\n</code></pre> <p>Return the unique name identifier.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def get_unique_name(self):\n    \"\"\"Return the unique name identifier.\"\"\"\n    return self.unique_name\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.SchBaseCtrl.init_base","title":"init_base","text":"<pre><code>init_base(kwds, parent=None)\n</code></pre> <p>Initialize all control attributes from kwds.</p> <p>Extracts well-known attributes (href, label, id, target, value, valuetype, defaultvalue, length, maxlength, readonly, hidden, src, onload, tdata, ldata, data, param, style) from kwds and assigns them as instance attributes.</p> <p>Parameters:</p> Name Type Description Default <code>kwds</code> <p>Keyword argument dict parsed from HTML tag attributes.</p> required <code>parent</code> <p>Optional parent window reference.</p> <code>None</code> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def init_base(self, kwds, parent=None):\n    \"\"\"Initialize all control attributes from kwds.\n\n    Extracts well-known attributes (href, label, id, target, value,\n    valuetype, defaultvalue, length, maxlength, readonly, hidden,\n    src, onload, tdata, ldata, data, param, style) from kwds\n    and assigns them as instance attributes.\n\n    Args:\n        kwds: Keyword argument dict parsed from HTML tag attributes.\n        parent: Optional parent window reference.\n    \"\"\"\n    if parent:\n        self.parent = parent\n\n    self.ldatabuf = None\n\n    self.tag = None\n    if \"param\" in kwds and \"tag\" in kwds[\"param\"]:\n        self.tag = kwds[\"param\"][\"tag\"]\n\n    self.href = _extract_kwarg(kwds, \"href\", None)\n    if self.href is None:\n        self.href = \"\"\n\n    self.label = _extract_kwarg(kwds, \"label\", None)\n    self.nr_id = _extract_kwarg(kwds, \"id\", None)\n    self.target = _extract_kwarg(kwds, \"target\", \"_blank\")\n    self.value = _extract_kwarg(kwds, \"value\", None)\n    self.valuetype = _extract_kwarg(kwds, \"valuetype\", \"data\")\n    self.defaultvalue = _extract_kwarg(kwds, \"defaultvalue\", None)\n    self.length = int(_extract_kwarg(kwds, \"length\", 0))\n    self.maxlength = int(_extract_kwarg(kwds, \"maxlength\", 0))\n    self.readonly = _extract_kwarg(kwds, \"readonly\", False)\n    self.hidden = True if _extract_kwarg(kwds, \"hidden\", False) else False\n    self.src = _extract_kwarg(kwds, \"src\", None)\n    self.onload = _extract_kwarg(kwds, \"onload\", None)\n\n    # tdata / tdatabuf: table data (already parsed) or lazily loaded\n    self.tdata = _extract_kwarg(kwds, \"tdata\", None)\n    self.tdatabuf = None\n    if self.tdata is not None:\n        self.tdatabuf = None\n\n    # ldata: hierarchical list data (already parsed) or lazily loaded\n    self.ldata = _extract_kwarg(kwds, \"ldata\", None)\n\n    # data: URL-encoded string data\n    raw_data = _extract_kwarg(kwds, \"data\", None)\n    self.data = unquote(raw_data) if raw_data is not None else None\n\n    self.param = _extract_kwarg(kwds, \"param\", None)\n    self.style = _extract_kwarg(kwds, \"style\", None)\n\n    # Invoke any registered ctrl_process hooks for this tag type\n    ctrl_process = wx.GetApp().ctrl_process\n    if self.tag in ctrl_process:\n        for fun in ctrl_process[self.tag]:\n            fun(self)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.SchBaseCtrl.is_ctrl_block","title":"is_ctrl_block","text":"<pre><code>is_ctrl_block()\n</code></pre> <p>Return True if this control is a block-level element.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def is_ctrl_block(self):\n    \"\"\"Return True if this control is a block-level element.\"\"\"\n    return self.GetParent().is_ctrl_block(self)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.SchBaseCtrl.load_data_from_server","title":"load_data_from_server","text":"<pre><code>load_data_from_server(path)\n</code></pre> <p>Load raw binary data from a server URL.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <p>URL path to fetch.</p> required <p>Returns:</p> Type Description <p>Raw response data (bytes).</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def load_data_from_server(self, path):\n    \"\"\"Load raw binary data from a server URL.\n\n    Args:\n        path: URL path to fetch.\n\n    Returns:\n        Raw response data (bytes).\n    \"\"\"\n    http = wx.GetApp().get_http(self.parent)\n    response = http.get(self, str(path))\n    return response.ptr()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.SchBaseCtrl.load_string_from_server","title":"load_string_from_server","text":"<pre><code>load_string_from_server(path)\n</code></pre> <p>Load a text response from a server URL.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <p>URL path to fetch.</p> required <p>Returns:</p> Type Description <p>Response body as a string.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def load_string_from_server(self, path):\n    \"\"\"Load a text response from a server URL.\n\n    Args:\n        path: URL path to fetch.\n\n    Returns:\n        Response body as a string.\n    \"\"\"\n    http = wx.GetApp().get_http(self.parent)\n    response = http.get(self, str(path))\n    return response.str()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.SchBaseCtrl.on_key_down_base","title":"on_key_down_base","text":"<pre><code>on_key_down_base(event)\n</code></pre> <p>Default key-down handler for Escape and Tab navigation.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>wx.KeyEvent.</p> required Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def on_key_down_base(self, event):\n    \"\"\"Default key-down handler for Escape and Tab navigation.\n\n    Args:\n        event: wx.KeyEvent.\n    \"\"\"\n    if event.GetKeyCode() == wx.WXK_ESCAPE:\n        wx.CallAfter(self.GetParent().cancel)\n    elif event.GetKeyCode() == wx.WXK_TAB:\n        if event.ShiftDown():\n            self.GetParent().Navigate(self, True)\n        else:\n            self.GetParent().Navigate(self, False)\n    else:\n        event.Skip()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.SchBaseCtrl.refresh_ldata","title":"refresh_ldata","text":"<pre><code>refresh_ldata()\n</code></pre> <p>Refresh hierarchical list data from the server.</p> <p>Loads from self.src and parses into self.ldatabuf using TreeParser.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def refresh_ldata(self):\n    \"\"\"Refresh hierarchical list data from the server.\n\n    Loads from self.src and parses into self.ldatabuf using TreeParser.\n    \"\"\"\n    if self.ldata is None and self.src:\n        lista = self.load_data_from_server(self.src)\n        mp = TreeParser()\n        mp.feed(lista)\n        mp.close()\n        self.ldatabuf = mp.tree_parent[0][1]\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.SchBaseCtrl.refresh_tdata","title":"refresh_tdata","text":"<pre><code>refresh_tdata(html_src=None)\n</code></pre> <p>Refresh table data from the server or provided HTML source.</p> <p>Parameters:</p> Name Type Description Default <code>html_src</code> <p>Optional HTML string containing table data. If None, loads from self.src.</p> <code>None</code> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def refresh_tdata(self, html_src=None):\n    \"\"\"Refresh table data from the server or provided HTML source.\n\n    Args:\n        html_src: Optional HTML string containing table data.\n            If None, loads from self.src.\n    \"\"\"\n    if self.tdata is None and self.src:\n        if html_src:\n            tables = html_src\n        else:\n            tables = self.load_data_from_server(self.src).decode(\"utf-8\")\n        self.tdatabuf = tdata_from_html(tables, wx.GetApp().http)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.SchBaseCtrl.set_acc_key_tab","title":"set_acc_key_tab","text":"<pre><code>set_acc_key_tab(tab)\n</code></pre> <p>Register an accelerator table for this widget.</p> <p>Parameters:</p> Name Type Description Default <code>tab</code> <p>List or tuple of accelerator elements. Each element has structure: (flag, keycode, callback function).</p> required <p>Returns:</p> Type Description <p>Result from the parent's set_acc_key_tab method.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def set_acc_key_tab(self, tab):\n    \"\"\"Register an accelerator table for this widget.\n\n    Args:\n        tab: List or tuple of accelerator elements. Each element has\n            structure: (flag, keycode, callback function).\n\n    Returns:\n        Result from the parent's set_acc_key_tab method.\n    \"\"\"\n    return self.GetParent().set_acc_key_tab(self, tab)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.SchBaseCtrl.set_unique_name","title":"set_unique_name","text":"<pre><code>set_unique_name(name)\n</code></pre> <p>Assign a unique name identifier to this control.</p> Source code in <code>pytigon_gui/guictrl/basectrl.py</code> <pre><code>def set_unique_name(self, name):\n    \"\"\"Assign a unique name identifier to this control.\"\"\"\n    self.unique_name = name\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarBar","title":"ToolbarBar","text":"<pre><code>ToolbarBar(parent, gui_style)\n</code></pre> <p>Base class for all menus and toolbars connected to the top frame window.</p> <p>Manages a collection of ToolbarPage objects and provides common operations like binding events and creating standard buttons.</p> <p>Initialize the toolbar bar.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent window - the top wx.Frame derived object.</p> required <code>gui_style</code> <p>String description of the GUI interface. Based on this string, standard toolbar elements are created.</p> required Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, parent, gui_style):\n    \"\"\"Initialize the toolbar bar.\n\n    Args:\n        parent: Parent window - the top wx.Frame derived object.\n        gui_style: String description of the GUI interface. Based on\n            this string, standard toolbar elements are created.\n    \"\"\"\n    self.parent = parent\n    self.main_page = None\n    self.gui_style = gui_style\n\n    self.toolbars = {}\n    self.pages = []\n    self.user_panels = {}\n\n    self.standard_buttons = StandardButtons(self, gui_style)\n    self.standard_buttons.create_file_panel(self.main_page)\n    self.standard_buttons.create_edit_panel(self.main_page)\n    self.standard_buttons.create_operations_panel(self.main_page)\n    self.standard_buttons.create_browse_panel(self.main_page)\n    self.standard_buttons.create_address_panel(self.main_page)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarBar-functions","title":"Methods:","text":""},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarBar.append","title":"append","text":"<pre><code>append(title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>Append a page to the toolbar, or return an existing one.</p> <p>If a page with the same name and kind already exists, it is returned instead of creating a duplicate.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Page title.</p> required <code>kind</code> <p>Type of page; one of ToolbarPage.TYPE_PAGE_* constants.</p> <code>TYPE_PAGE_NORMAL</code> <p>Returns:</p> Name Type Description <code>ToolbarPage</code> <p>The existing or newly created page.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def append(self, title, kind=ToolbarPage.TYPE_PAGE_NORMAL):\n    \"\"\"Append a page to the toolbar, or return an existing one.\n\n    If a page with the same name and kind already exists, it is\n    returned instead of creating a duplicate.\n\n    Args:\n        title: Page title.\n        kind: Type of page; one of ToolbarPage.TYPE_PAGE_* constants.\n\n    Returns:\n        ToolbarPage: The existing or newly created page.\n    \"\"\"\n    for page in self.pages:\n        if page.name == title and page.kind == kind:\n            return page\n    p = self.create_page(title, kind)\n    if not self.main_page:\n        self.main_page = p\n    self.pages.append(p)\n    return p\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarBar.bind","title":"bind","text":"<pre><code>bind(fun, id=ID_ANY, e=None)\n</code></pre> <p>Bind an event handler to the toolbar's parent window.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <p>Handler function.</p> required <code>id</code> <p>Identifier of the event.</p> <code>ID_ANY</code> <code>e</code> <p>Event type; defaults to wx.EVT_MENU if not specified.</p> <code>None</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def bind(self, fun, id=wx.ID_ANY, e=None):\n    \"\"\"Bind an event handler to the toolbar's parent window.\n\n    Args:\n        fun: Handler function.\n        id: Identifier of the event.\n        e: Event type; defaults to wx.EVT_MENU if not specified.\n    \"\"\"\n    if e:\n        self.parent.Bind(e, fun, id=id)\n    else:\n        self.parent.Bind(wx.EVT_MENU, fun, id=id)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarBar.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Close/destroy the toolbar.</p> <p>Override in subclasses to perform cleanup.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def close(self):\n    \"\"\"Close/destroy the toolbar.\n\n    Override in subclasses to perform cleanup.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarBar.create","title":"create","text":"<pre><code>create()\n</code></pre> <p>Create/realize the toolbar.</p> <p>Override in subclasses to perform actual widget creation.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create(self):\n    \"\"\"Create/realize the toolbar.\n\n    Override in subclasses to perform actual widget creation.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarBar.create_html_win","title":"create_html_win","text":"<pre><code>create_html_win(toolbar_page, address_or_parser, parameters, callback=None)\n</code></pre> <p>Create an HTML page embedded in the toolbar.</p> <p>Not all toolbar types support this feature.</p> <p>Parameters:</p> Name Type Description Default <code>toolbar_page</code> <p>Name of the toolbar page to host the HTML. Use '__' to separate page and panel names, e.g. 'PageName__PanelName'.</p> required <code>address_or_parser</code> <p>Address of the HTTP page or a parser.</p> required <code>parameters</code> <p>Parameters for the HTTP request.</p> required <code>callback</code> <p>Optional callback invoked after initialization.</p> <code>None</code> <p>Returns:</p> Type Description <p>SchPage or None: The created page, or None on failure.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_html_win(\n    self, toolbar_page, address_or_parser, parameters, callback=None\n):\n    \"\"\"Create an HTML page embedded in the toolbar.\n\n    Not all toolbar types support this feature.\n\n    Args:\n        toolbar_page: Name of the toolbar page to host the HTML.\n            Use '__' to separate page and panel names,\n            e.g. 'PageName__PanelName'.\n        address_or_parser: Address of the HTTP page or a parser.\n        parameters: Parameters for the HTTP request.\n        callback: Optional callback invoked after initialization.\n\n    Returns:\n        SchPage or None: The created page, or None on failure.\n    \"\"\"\n    page = None\n    if not toolbar_page:\n        u_name = \"main tools\"\n        page_name = _(\"main tools\")\n        panel_name = \"main tools\"\n    else:\n        u_name = toolbar_page\n        names = toolbar_page.split(\"__\")\n        if len(names) &gt; 1:\n            page_name = names[0].replace(\"_\", \" \")\n            panel_name = names[1].replace(\"_\", \" \")\n        else:\n            page_name = toolbar_page.replace(\"_\", \" \")\n            panel_name = page_name\n\n    if u_name in self.user_panels:\n        panel = self.user_panels[u_name]\n    else:\n        page = self._find_page_by_title(page_name)\n        if page is None:\n            page = self.append(page_name)\n        panel = page.create_html_panel(panel_name)\n\n    if panel:\n        dx = panel.get_width() + 3\n        dy = panel.get_height() + 5\n\n        page2 = SchPage(\n            panel.get_window(),\n            address_or_parser,\n            parameters,\n            size=wx.Size(dx, dy),\n            pos=wx.Point(2, 2),\n        )\n        best = page2.body.calculate_best_size()\n        page2.SetSize(wx.Size(int(best[0]), int(best[1])))\n        panel.set_page(page2)\n        self.user_panels[u_name] = panel\n        page2.body.toolbar_interface = self\n        page2.body.toolbar_interface_page = page\n\n        def init_page(callback):\n            page2.init_frame()\n            page2.activate_page()\n            page2.Update()\n\n        wx.CallAfter(init_page, callback)\n\n        return page2\n\n    return None\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarBar.create_page","title":"create_page","text":"<pre><code>create_page(title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>Create and return a new ToolbarPage.</p> <p>Override in derived classes to return a subclass of ToolbarPage.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Page title.</p> required <code>kind</code> <p>Type of page; one of ToolbarPage.TYPE_PAGE_* constants.</p> <code>TYPE_PAGE_NORMAL</code> <p>Returns:</p> Name Type Description <code>ToolbarPage</code> <p>The newly created page.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_page(self, title, kind=ToolbarPage.TYPE_PAGE_NORMAL):\n    \"\"\"Create and return a new ToolbarPage.\n\n    Override in derived classes to return a subclass of ToolbarPage.\n\n    Args:\n        title: Page title.\n        kind: Type of page; one of ToolbarPage.TYPE_PAGE_* constants.\n\n    Returns:\n        ToolbarPage: The newly created page.\n    \"\"\"\n    return ToolbarPage(self, title, kind)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarBar.create_panel_in_main_page","title":"create_panel_in_main_page","text":"<pre><code>create_panel_in_main_page(title, kind)\n</code></pre> <p>Create a panel in the main page.</p> <p>If no main page exists yet, one is created automatically.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>New panel's title.</p> required <code>kind</code> <p>Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.</p> required <p>Returns:</p> Name Type Description <code>ToolbarPanel</code> <p>The newly created panel.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_panel_in_main_page(self, title, kind):\n    \"\"\"Create a panel in the main page.\n\n    If no main page exists yet, one is created automatically.\n\n    Args:\n        title: New panel's title.\n        kind: Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.\n\n    Returns:\n        ToolbarPanel: The newly created panel.\n    \"\"\"\n    if not self.main_page:\n        self.append(\"main tools\")\n    return self.main_page.create_panel(title, kind)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarBar.new_child_page","title":"new_child_page","text":"<pre><code>new_child_page(address_or_parser, title='', param=None, callback=None)\n</code></pre> <p>Create a new child page.</p> <p>For toolbar, the child page is transferred to the desktop window.</p> <p>Parameters:</p> Name Type Description Default <code>address_or_parser</code> <p>Address of the HTTP page or a parser.</p> required <code>title</code> <p>Page title.</p> <code>''</code> <code>param</code> <p>Optional parameters.</p> <code>None</code> <code>callback</code> <p>Optional callback.</p> <code>None</code> <p>Returns:</p> Type Description <p>The result of new_main_page on the top window.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def new_child_page(self, address_or_parser, title=\"\", param=None, callback=None):\n    \"\"\"Create a new child page.\n\n    For toolbar, the child page is transferred to the desktop window.\n\n    Args:\n        address_or_parser: Address of the HTTP page or a parser.\n        title: Page title.\n        param: Optional parameters.\n        callback: Optional callback.\n\n    Returns:\n        The result of new_main_page on the top window.\n    \"\"\"\n    return wx.GetApp().GetTopWindow().new_main_page(address_or_parser, title, param)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarBar.remove_page","title":"remove_page","text":"<pre><code>remove_page(title)\n</code></pre> <p>Remove the page with the specified title from the toolbar.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Title of the page to remove.</p> required Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def remove_page(self, title):\n    \"\"\"Remove the page with the specified title from the toolbar.\n\n    Args:\n        title: Title of the page to remove.\n    \"\"\"\n    # Remove from pages list\n    for page in self.pages:\n        if page.title == title:\n            self.pages.remove(page)\n            break\n\n    # If the main page was removed, pick a new one\n    if self.main_page and self.main_page.title == title:\n        self.main_page = self.pages[0] if self.pages else None\n\n    # Remove associated user panels (iterate over a static list of keys\n    # to avoid \"dictionary changed size during iteration\" errors)\n    for key in list(self.user_panels.keys()):\n        panel = self.user_panels[key]\n        if panel.page.title == title:\n            del self.user_panels[key]\n            return\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarBar.un_bind","title":"un_bind","text":"<pre><code>un_bind(id=ID_ANY, e=None)\n</code></pre> <p>Unbind an event handler from the toolbar's parent window.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>Identifier of the event.</p> <code>ID_ANY</code> <code>e</code> <p>Event type; defaults to wx.EVT_MENU if not specified.</p> <code>None</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def un_bind(self, id=wx.ID_ANY, e=None):\n    \"\"\"Unbind an event handler from the toolbar's parent window.\n\n    Args:\n        id: Identifier of the event.\n        e: Event type; defaults to wx.EVT_MENU if not specified.\n    \"\"\"\n    if e:\n        self.parent.Unbind(e, id=id)\n    else:\n        self.parent.Unbind(wx.EVT_MENU, id=id)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarButton","title":"ToolbarButton","text":"<pre><code>ToolbarButton(\n    parent_panel, id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE\n)\n</code></pre> <p>Represents a single button (or separator) on a toolbar panel.</p> <p>This is an abstract data model; the actual wx widget is created by the concrete toolbar implementation.</p> Button type constants <p>TYPE_SIMPLE = 0     - Simple push button. TYPE_DROPDOWN = 1   - Button with a dropdown menu. TYPE_HYBRID = 2     - Hybrid button (part button, part dropdown). TYPE_TOOGLE = 3     - Toggle button. TYPE_PANEL = 4      - Embedded panel. TYPE_SEPARATOR = 5  - Visual separator.</p> <p>Create a new toolbar button.</p> <p>Parameters:</p> Name Type Description Default <code>parent_panel</code> <p>Parent ToolbarPanel object.</p> required <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title (display text).</p> required <code>bitmap</code> <p>Button bitmap for the normal state.</p> <code>None</code> <code>bitmap_disabled</code> <p>Button bitmap for the disabled state. Defaults to wx.NullBitmap when bitmap is provided.</p> <code>None</code> <code>kind</code> <p>Type of button; one of the TYPE_* class constants.</p> <code>TYPE_SIMPLE</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(\n    self,\n    parent_panel,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=TYPE_SIMPLE,\n):\n    \"\"\"Create a new toolbar button.\n\n    Args:\n        parent_panel: Parent ToolbarPanel object.\n        id: An integer by which the tool may be identified.\n        title: Button title (display text).\n        bitmap: Button bitmap for the normal state.\n        bitmap_disabled: Button bitmap for the disabled state.\n            Defaults to wx.NullBitmap when bitmap is provided.\n        kind: Type of button; one of the TYPE_* class constants.\n    \"\"\"\n    self.parent_panel = parent_panel\n    self.id = id\n    self.title = title\n    self.bitmap = bitmap\n    if bitmap_disabled is None and bitmap is not None:\n        self.bitmap_disabled = wx.NullBitmap\n    else:\n        self.bitmap_disabled = bitmap_disabled\n    self.kind = kind\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarButton-functions","title":"Methods:","text":""},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarPage","title":"ToolbarPage","text":"<pre><code>ToolbarPage(parent_bar, title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>A page (tab) within a toolbar bar, containing one or more panels.</p> Page type constants <p>TYPE_PAGE_NORMAL = 0  - Standard page.</p> <p>Create a new toolbar page.</p> <p>Parameters:</p> Name Type Description Default <code>parent_bar</code> <p>Parent ToolbarBar object.</p> required <code>title</code> <p>Page title (will be translated via wx.GetTranslation).</p> required <code>kind</code> <p>Type of page; one of the TYPE_PAGE_* class constants.</p> <code>TYPE_PAGE_NORMAL</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, parent_bar, title, kind=TYPE_PAGE_NORMAL):\n    \"\"\"Create a new toolbar page.\n\n    Args:\n        parent_bar: Parent ToolbarBar object.\n        title: Page title (will be translated via wx.GetTranslation).\n        kind: Type of page; one of the TYPE_PAGE_* class constants.\n    \"\"\"\n    self.parent_bar = parent_bar\n    self.name = title\n    self.title = _(title)\n    self.kind = kind\n    self.panels = []\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarPage-functions","title":"Methods:","text":""},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarPage.append","title":"append","text":"<pre><code>append(title, kind=TYPE_PANEL_BUTTONBAR)\n</code></pre> <p>Create a new panel and append it to this page.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Panel title.</p> required <code>kind</code> <p>Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.</p> <code>TYPE_PANEL_BUTTONBAR</code> <p>Returns:</p> Name Type Description <code>ToolbarPanel</code> <p>The newly created panel.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def append(self, title, kind=ToolbarPanel.TYPE_PANEL_BUTTONBAR):\n    \"\"\"Create a new panel and append it to this page.\n\n    Args:\n        title: Panel title.\n        kind: Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.\n\n    Returns:\n        ToolbarPanel: The newly created panel.\n    \"\"\"\n    p = self.create_panel(title, kind)\n    self.panels.append(p)\n    return p\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarPage.create_html_panel","title":"create_html_panel","text":"<pre><code>create_html_panel(title)\n</code></pre> <p>Create an HTML-capable panel within this page.</p> <p>Override in derived classes to return a BaseHtmlPanel subclass.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Panel title.</p> required <p>Returns:</p> Type Description <p>BaseHtmlPanel or None: The panel, or None if not supported.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_html_panel(self, title):\n    \"\"\"Create an HTML-capable panel within this page.\n\n    Override in derived classes to return a BaseHtmlPanel subclass.\n\n    Args:\n        title: Panel title.\n\n    Returns:\n        BaseHtmlPanel or None: The panel, or None if not supported.\n    \"\"\"\n    return None\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarPage.create_panel","title":"create_panel","text":"<pre><code>create_panel(title, kind)\n</code></pre> <p>Create and return a new ToolbarPanel for this page.</p> <p>Override in derived classes to return a subclass of ToolbarPanel.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Panel title.</p> required <code>kind</code> <p>Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.</p> required <p>Returns:</p> Name Type Description <code>ToolbarPanel</code> <p>The newly created panel.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_panel(self, title, kind):\n    \"\"\"Create and return a new ToolbarPanel for this page.\n\n    Override in derived classes to return a subclass of ToolbarPanel.\n\n    Args:\n        title: Panel title.\n        kind: Type of panel; one of ToolbarPanel.TYPE_PANEL_* constants.\n\n    Returns:\n        ToolbarPanel: The newly created panel.\n    \"\"\"\n    return ToolbarPanel(self, title, kind=ToolbarPanel.TYPE_PANEL_BUTTONBAR)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarPanel","title":"ToolbarPanel","text":"<pre><code>ToolbarPanel(parent_page, title, kind=TYPE_PANEL_BUTTONBAR)\n</code></pre> <p>A panel within a toolbar page that holds a group of buttons.</p> Panel type constants <p>TYPE_PANEL_BUTTONBAR = 0  - Button bar layout. TYPE_PANEL_TOOLBAR = 1    - Toolbar layout. TYPE_PANEL_PANELBAR = 2   - Custom panel layout.</p> <p>Create a new toolbar panel.</p> <p>Parameters:</p> Name Type Description Default <code>parent_page</code> <p>Parent ToolbarPage object.</p> required <code>title</code> <p>Panel title.</p> required <code>kind</code> <p>Type of panel; one of the TYPE_PANEL_* class constants.</p> <code>TYPE_PANEL_BUTTONBAR</code> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, parent_page, title, kind=TYPE_PANEL_BUTTONBAR):\n    \"\"\"Create a new toolbar panel.\n\n    Args:\n        parent_page: Parent ToolbarPage object.\n        title: Panel title.\n        kind: Type of panel; one of the TYPE_PANEL_* class constants.\n    \"\"\"\n    self.parent_page = parent_page\n    self.title = title\n    self.kind = kind\n    self.buttons = []\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarPanel-functions","title":"Methods:","text":""},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarPanel.add_dropdown_tool","title":"add_dropdown_tool","text":"<pre><code>add_dropdown_tool(id, title, bitmaps)\n</code></pre> <p>Append a dropdown button to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_dropdown_tool(self, id, title, bitmaps):\n    \"\"\"Append a dropdown button to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_DROPDOWN)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarPanel.add_hybrid_tool","title":"add_hybrid_tool","text":"<pre><code>add_hybrid_tool(id, title, bitmaps)\n</code></pre> <p>Append a hybrid button (button + dropdown) to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_hybrid_tool(self, id, title, bitmaps):\n    \"\"\"Append a hybrid button (button + dropdown) to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_HYBRID)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarPanel.add_separator","title":"add_separator","text":"<pre><code>add_separator()\n</code></pre> <p>Append a visual separator to the panel.</p> <p>The default implementation does nothing; override in subclasses.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_separator(self):\n    \"\"\"Append a visual separator to the panel.\n\n    The default implementation does nothing; override in subclasses.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarPanel.add_simple_tool","title":"add_simple_tool","text":"<pre><code>add_simple_tool(id, title, bitmaps)\n</code></pre> <p>Append a simple push button to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_simple_tool(self, id, title, bitmaps):\n    \"\"\"Append a simple push button to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_SIMPLE)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarPanel.add_toogle_tool","title":"add_toogle_tool","text":"<pre><code>add_toogle_tool(id, title, bitmaps)\n</code></pre> <p>Append a toggle button to the panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmaps</code> <p>List of bitmaps; can contain 0, 1, or 2 bitmaps. First bitmap is for the normal state, second for disabled.</p> required <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def add_toogle_tool(self, id, title, bitmaps):\n    \"\"\"Append a toggle button to the panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmaps: List of bitmaps; can contain 0, 1, or 2 bitmaps.\n            First bitmap is for the normal state, second for disabled.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    b = self._transform_bitmaps_parm(bitmaps)\n    return self.append(id, title, b[0], b[1], kind=ToolbarButton.TYPE_TOOGLE)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarPanel.append","title":"append","text":"<pre><code>append(id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE)\n</code></pre> <p>Create a button and append it to this panel.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmap</code> <p>Button bitmap for the normal state.</p> <code>None</code> <code>bitmap_disabled</code> <p>Button bitmap for the disabled state.</p> <code>None</code> <code>kind</code> <p>Type of button; one of ToolbarButton.TYPE_* constants.</p> <code>TYPE_SIMPLE</code> <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created and appended button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def append(\n    self,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=ToolbarButton.TYPE_SIMPLE,\n):\n    \"\"\"Create a button and append it to this panel.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmap: Button bitmap for the normal state.\n        bitmap_disabled: Button bitmap for the disabled state.\n        kind: Type of button; one of ToolbarButton.TYPE_* constants.\n\n    Returns:\n        ToolbarButton: The newly created and appended button.\n    \"\"\"\n    button = self.create_button(id, title, bitmap, bitmap_disabled, kind)\n    self.buttons.append(button)\n    return button\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.ToolbarPanel.create_button","title":"create_button","text":"<pre><code>create_button(id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE)\n</code></pre> <p>Create and return a new ToolbarButton.</p> <p>Override in derived classes to return a subclass of ToolbarButton.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>An integer by which the tool may be identified.</p> required <code>title</code> <p>Button title.</p> required <code>bitmap</code> <p>Button bitmap for the normal state.</p> <code>None</code> <code>bitmap_disabled</code> <p>Button bitmap for the disabled state.</p> <code>None</code> <code>kind</code> <p>Type of button; one of ToolbarButton.TYPE_* constants.</p> <code>TYPE_SIMPLE</code> <p>Returns:</p> Name Type Description <code>ToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def create_button(\n    self,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=ToolbarButton.TYPE_SIMPLE,\n):\n    \"\"\"Create and return a new ToolbarButton.\n\n    Override in derived classes to return a subclass of ToolbarButton.\n\n    Args:\n        id: An integer by which the tool may be identified.\n        title: Button title.\n        bitmap: Button bitmap for the normal state.\n        bitmap_disabled: Button bitmap for the disabled state.\n        kind: Type of button; one of ToolbarButton.TYPE_* constants.\n\n    Returns:\n        ToolbarButton: The newly created button.\n    \"\"\"\n    return ToolbarButton(self, id, title, bitmap, bitmap_disabled, kind)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeHtmlPanel","title":"TreeHtmlPanel","text":"<pre><code>TreeHtmlPanel(page, real_panel)\n</code></pre> <p>               Bases: <code>BaseHtmlPanel</code></p> <p>HTML panel wrapper for the tree toolbar.</p> Source code in <code>pytigon_gui/toolbar/basetoolbar.py</code> <pre><code>def __init__(self, page, real_panel):\n    \"\"\"Initialize the HTML panel wrapper.\n\n    Args:\n        page: Parent ToolbarPage object.\n        real_panel: The actual wx.Panel that will contain the HTML page.\n    \"\"\"\n    self.page = page\n    self.real_panel = real_panel\n    self.html_page = None\n    self.sizer = wx.BoxSizer(wx.VERTICAL)\n    self.real_panel.SetSizer(self.sizer)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeHtmlPanel-functions","title":"Methods:","text":""},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeHtmlPanel.get_height","title":"get_height","text":"<pre><code>get_height()\n</code></pre> <p>Return panel height from the parent tree bar.</p> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def get_height(self):\n    \"\"\"Return panel height from the parent tree bar.\"\"\"\n    return self.page.parent_bar.get_bar_height()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeHtmlPanel.get_width","title":"get_width","text":"<pre><code>get_width()\n</code></pre> <p>Return panel width from the parent tree bar.</p> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def get_width(self):\n    \"\"\"Return panel width from the parent tree bar.\"\"\"\n    return self.page.parent_bar.get_bar_width()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeHtmlPanel.set_page","title":"set_page","text":"<pre><code>set_page(html_page)\n</code></pre> <p>Set the HTML page within this panel.</p> <p>Parameters:</p> Name Type Description Default <code>html_page</code> <p>The SchPage to display.</p> required Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def set_page(self, html_page):\n    \"\"\"Set the HTML page within this panel.\n\n    Args:\n        html_page: The SchPage to display.\n    \"\"\"\n    super().set_page(html_page)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar","title":"TreeToolbarBar","text":"<pre><code>TreeToolbarBar(parent, gui_style)\n</code></pre> <p>               Bases: <code>ToolbarBar</code>, <code>CustomTreeCtrl</code></p> <p>The top-level tree toolbar bar, displayed as a vertical navigation tree.</p> <p>Initialize the tree toolbar bar.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <p>Parent wx.Frame.</p> required <code>gui_style</code> <p>GUI style string.</p> required Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def __init__(self, parent, gui_style):\n    \"\"\"Initialize the tree toolbar bar.\n\n    Args:\n        parent: Parent wx.Frame.\n        gui_style: GUI style string.\n    \"\"\"\n    agwStyle = (\n        CT.TR_HIDE_ROOT\n        | CT.TR_HAS_BUTTONS\n        | CT.TR_HAS_VARIABLE_ROW_HEIGHT\n        | CT.TR_NO_LINES\n    )\n    CT.CustomTreeCtrl.__init__(self, parent, agwStyle=agwStyle)\n\n    self._indent = 8\n    self.tree_main_page = self.AddRoot(\"The Root Item\")\n    self.images = wx.ImageList(32, 32)\n    self.image_id = 0\n    self.show_titles = True\n\n    # Calculate a blended background colour from the system menu colour\n    c2 = wx.SystemSettings.GetColour(wx.SYS_COLOUR_MENU)\n    m = 1.0\n    x1 = int((int(c2.Red() * m) + 255) / 2)\n    x2 = int((int(c2.Green() * m) + 255) / 2)\n    x3 = int((int(c2.Blue() * m) + 255) / 2)\n    c = wx.Colour(x1, x2, x3)\n    self.bg = c\n    self.SetBackgroundColour(c)\n    c3 = wx.Colour(0, 0, 0)\n    self._hypertextnewcolour = c3\n    self._hypertextvisitedcolour = c3\n\n    super().__init__(parent, gui_style)\n    self.EnableSelectionVista(True)\n\n    self.Bind(wx.EVT_CLOSE, self.on_close)\n    self.Bind(CT.EVT_TREE_ITEM_HYPERLINK, self.on_hyper_link)\n\n    wx.GetApp().GetTopWindow().idle_objects.append(self)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar-functions","title":"Methods:","text":""},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar.CanAcceptFocus","title":"CanAcceptFocus","text":"<pre><code>CanAcceptFocus()\n</code></pre> <p>Return True to indicate this control can accept focus.</p> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def CanAcceptFocus(self):\n    \"\"\"Return True to indicate this control can accept focus.\"\"\"\n    return True\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar.CanAcceptFocusFromKeyboard","title":"CanAcceptFocusFromKeyboard","text":"<pre><code>CanAcceptFocusFromKeyboard()\n</code></pre> <p>Return True to indicate this control accepts keyboard focus.</p> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def CanAcceptFocusFromKeyboard(self):\n    \"\"\"Return True to indicate this control accepts keyboard focus.\"\"\"\n    return self.CanAcceptFocus()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar.append_to_tree","title":"append_to_tree","text":"<pre><code>append_to_tree(parent_elem, title, bitmap=None, ct_type=0)\n</code></pre> <p>Append a child item to the tree.</p> <p>Parameters:</p> Name Type Description Default <code>parent_elem</code> <p>Parent tree item.</p> required <code>title</code> <p>Display title.</p> required <code>bitmap</code> <p>Optional wx.Bitmap; will be rescaled to 32x32 if smaller.</p> <code>None</code> <code>ct_type</code> <p>Custom tree item type (0 = normal, 1 = toggle).</p> <code>0</code> <p>Returns:</p> Type Description <p>The new tree item.</p> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def append_to_tree(self, parent_elem, title, bitmap=None, ct_type=0):\n    \"\"\"Append a child item to the tree.\n\n    Args:\n        parent_elem: Parent tree item.\n        title: Display title.\n        bitmap: Optional wx.Bitmap; will be rescaled to 32x32 if smaller.\n        ct_type: Custom tree item type (0 = normal, 1 = toggle).\n\n    Returns:\n        The new tree item.\n    \"\"\"\n    if bitmap is not None and bitmap.IsOk():\n        if bitmap.GetWidth() &lt; 32 or bitmap.GetHeight() &lt; 32:\n            b = wx.BitmapFromImage(bitmap.ConvertToImage().Rescale(32, 32))\n        else:\n            b = bitmap\n        self.images.Add(b)\n        item = self.AppendItem(\n            parent_elem,\n            title if self.show_titles else \"\",\n            image=self.image_id,\n            ct_type=ct_type,\n        )\n        self.image_id += 1\n    else:\n        item = self.AppendItem(\n            parent_elem, title if self.show_titles else \"\", ct_type=ct_type\n        )\n    return item\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar.bind","title":"bind","text":"<pre><code>bind(fun, id=ID_ANY, e=None)\n</code></pre> <p>Bind an event handler to the parent window.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <p>Handler function.</p> required <code>id</code> <p>Event identifier.</p> <code>ID_ANY</code> <code>e</code> <p>Event type; defaults to wx.EVT_MENU.</p> <code>None</code> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def bind(self, fun, id=wx.ID_ANY, e=None):\n    \"\"\"Bind an event handler to the parent window.\n\n    Args:\n        fun: Handler function.\n        id: Event identifier.\n        e: Event type; defaults to wx.EVT_MENU.\n    \"\"\"\n    if e:\n        self.parent.Bind(e, fun, id=id)\n    else:\n        self.parent.Bind(wx.EVT_MENU, fun, id=id)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar.bind_dropdown","title":"bind_dropdown","text":"<pre><code>bind_dropdown(fun, id)\n</code></pre> <p>Bind a dropdown menu event handler to the parent window.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <p>Handler function.</p> required <code>id</code> <p>Event identifier.</p> required Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def bind_dropdown(self, fun, id):\n    \"\"\"Bind a dropdown menu event handler to the parent window.\n\n    Args:\n        fun: Handler function.\n        id: Event identifier.\n    \"\"\"\n    self.parent.Bind(wx.EVT_MENU, fun, id=id)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar.bind_ui","title":"bind_ui","text":"<pre><code>bind_ui(fun, id=ID_ANY)\n</code></pre> <p>Bind a UI update event handler to the parent window.</p> <p>Parameters:</p> Name Type Description Default <code>fun</code> <p>Handler function.</p> required <code>id</code> <p>Event identifier.</p> <code>ID_ANY</code> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def bind_ui(self, fun, id=wx.ID_ANY):\n    \"\"\"Bind a UI update event handler to the parent window.\n\n    Args:\n        fun: Handler function.\n        id: Event identifier.\n    \"\"\"\n    self.parent.Bind(wx.EVT_UPDATE_UI, fun, id=id)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Close/destroy the tree toolbar.</p> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def close(self):\n    \"\"\"Close/destroy the tree toolbar.\"\"\"\n    self.Close()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar.create","title":"create","text":"<pre><code>create()\n</code></pre> <p>Create/realize the tree toolbar.</p> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def create(self):\n    \"\"\"Create/realize the tree toolbar.\"\"\"\n    self.AssignImageList(self.images)\n    self.ExpandAll()\n    self.SetSize((300, 400))\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar.create_page","title":"create_page","text":"<pre><code>create_page(title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>Create a new tree toolbar page.</p> <p>Returns:</p> Name Type Description <code>TreeToolbarPage</code> <p>The newly created page.</p> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def create_page(self, title, kind=ToolbarPage.TYPE_PAGE_NORMAL):\n    \"\"\"Create a new tree toolbar page.\n\n    Returns:\n        TreeToolbarPage: The newly created page.\n    \"\"\"\n    return TreeToolbarPage(self, title, kind)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar.get_bar_height","title":"get_bar_height","text":"<pre><code>get_bar_height()\n</code></pre> <p>Return the preferred height of the tree bar.</p> <p>Returns:</p> Name Type Description <code>int</code> <p>Height in pixels.</p> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def get_bar_height(self):\n    \"\"\"Return the preferred height of the tree bar.\n\n    Returns:\n        int: Height in pixels.\n    \"\"\"\n    return 600\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar.get_bar_width","title":"get_bar_width","text":"<pre><code>get_bar_width()\n</code></pre> <p>Return the preferred width of the tree bar.</p> <p>Returns:</p> Name Type Description <code>int</code> <p>Width in pixels.</p> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def get_bar_width(self):\n    \"\"\"Return the preferred width of the tree bar.\n\n    Returns:\n        int: Width in pixels.\n    \"\"\"\n    return 150\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar.get_max_width","title":"get_max_width","text":"<pre><code>get_max_width(respect_expansion_state=True)\n</code></pre> <p>Return the maximum width of the tree.</p> <p>Parameters:</p> Name Type Description Default <code>respect_expansion_state</code> <p>If True, consider expanded items.</p> <code>True</code> <p>Returns:</p> Name Type Description <code>int</code> <p>Maximum width in pixels.</p> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def get_max_width(self, respect_expansion_state=True):\n    \"\"\"Return the maximum width of the tree.\n\n    Args:\n        respect_expansion_state: If True, consider expanded items.\n\n    Returns:\n        int: Maximum width in pixels.\n    \"\"\"\n    return 400\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar.on_close","title":"on_close","text":"<pre><code>on_close(event)\n</code></pre> <p>Handle the close event by removing from idle objects.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>wx.CloseEvent.</p> required Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def on_close(self, event):\n    \"\"\"Handle the close event by removing from idle objects.\n\n    Args:\n        event: wx.CloseEvent.\n    \"\"\"\n    if self in wx.GetApp().GetTopWindow().idle_objects:\n        del wx.GetApp().GetTopWindow().idle_objects[self]\n    event.Skip()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar.on_hyper_link","title":"on_hyper_link","text":"<pre><code>on_hyper_link(event)\n</code></pre> <p>Handle a hyperlink click on a tree item.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>Tree item hyperlink event.</p> required Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def on_hyper_link(self, event):\n    \"\"\"Handle a hyperlink click on a tree item.\n\n    Args:\n        event: Tree item hyperlink event.\n    \"\"\"\n    item = event.GetItem()\n    if item:\n        wx.CallAfter(self.send_menu_event, item)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar.on_idle","title":"on_idle","text":"<pre><code>on_idle()\n</code></pre> <p>Handle idle event: update enabled/disabled state of tree items.</p> <p>Iterates all tree items and dispatches wx.UpdateUIEvent to determine whether each item should be enabled or disabled.</p> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def on_idle(self):\n    \"\"\"Handle idle event: update enabled/disabled state of tree items.\n\n    Iterates all tree items and dispatches wx.UpdateUIEvent to determine\n    whether each item should be enabled or disabled.\n    \"\"\"\n    refresh = False\n    for item in _tree_list(self, self.tree_main_page):\n        item_id = self.GetPyData(item)\n        if item_id:\n            if (item_id &gt;= ID_START and item_id &lt; ID_END) or (\n                item_id &gt;= wx.ID_LOWEST and item_id &lt; wx.ID_HIGHEST\n            ):\n                event = wx.UpdateUIEvent(item_id)\n                event.Enable(False)\n                event.SetEventObject(self.GetParent())\n\n                if self.ProcessWindowEvent(event):\n                    if item.IsEnabled() != event.GetEnabled():\n                        refresh = True\n                    item.Enable(event.GetEnabled())\n                else:\n                    enable = False\n                    win = wx.Window.FindFocus()\n                    if win and isinstance(win, SchBaseCtrl):\n                        if win.ProcessEvent(event):\n                            if event.GetEnabled():\n                                enable = True\n                    if enable:\n                        if not item.IsEnabled():\n                            refresh = True\n                        item.Enable(True)\n                    else:\n                        if item.IsEnabled():\n                            refresh = True\n                        item.Enable(False)\n    if refresh:\n        self.Refresh()\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar.realize","title":"realize","text":"<pre><code>realize()\n</code></pre> <p>Realize the tree: assign images, expand, set size.</p> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def realize(self):\n    \"\"\"Realize the tree: assign images, expand, set size.\"\"\"\n    self.AssignImageList(self.images)\n    self.ExpandAll()\n    self.SetSize((300, 400))\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar.remove_page","title":"remove_page","text":"<pre><code>remove_page(title)\n</code></pre> <p>Remove a page from the tree toolbar.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Title of the page to remove.</p> required Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def remove_page(self, title):\n    \"\"\"Remove a page from the tree toolbar.\n\n    Args:\n        title: Title of the page to remove.\n    \"\"\"\n    for p in self.pages:\n        if p.title == title:\n            self.Delete(p.tree_item)\n            break\n    super().remove_page(title)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar.send_menu_event","title":"send_menu_event","text":"<pre><code>send_menu_event(item)\n</code></pre> <p>Send a menu event for the given tree item.</p> <p>Tries to process the event on this tree first, then falls back to the active SchBaseCtrl control.</p> <p>Parameters:</p> Name Type Description Default <code>item</code> <p>The tree item that was clicked.</p> required Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def send_menu_event(self, item):\n    \"\"\"Send a menu event for the given tree item.\n\n    Tries to process the event on this tree first, then falls back\n    to the active SchBaseCtrl control.\n\n    Args:\n        item: The tree item that was clicked.\n    \"\"\"\n    e = wx.CommandEvent(wx.EVT_MENU.typeId, self.GetPyData(item))\n    ret = self.ProcessEvent(e)\n    if not ret:\n        win = wx.GetApp().GetTopWindow().get_active_ctrl()\n        if win and isinstance(win, SchBaseCtrl):\n            win.ProcessEvent(e)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar.set_active_page","title":"set_active_page","text":"<pre><code>set_active_page(page)\n</code></pre> <p>Set the active page (no-op for tree toolbar).</p> <p>Parameters:</p> Name Type Description Default <code>page</code> <p>The page to activate.</p> required Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def set_active_page(self, page):\n    \"\"\"Set the active page (no-op for tree toolbar).\n\n    Args:\n        page: The page to activate.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar.un_bind","title":"un_bind","text":"<pre><code>un_bind(id, e=None)\n</code></pre> <p>Unbind an event handler from the parent window.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <p>Event identifier.</p> required <code>e</code> <p>Event type; defaults to wx.EVT_MENU.</p> <code>None</code> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def un_bind(self, id, e=None):\n    \"\"\"Unbind an event handler from the parent window.\n\n    Args:\n        id: Event identifier.\n        e: Event type; defaults to wx.EVT_MENU.\n    \"\"\"\n    if e:\n        self.parent.Unbind(e, id=id)\n    else:\n        self.parent.Unbind(wx.EVT_MENU, id=id)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarBar.update","title":"update","text":"<pre><code>update()\n</code></pre> <p>Force a layout refresh by briefly resizing the bar.</p> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def update(self):\n    \"\"\"Force a layout refresh by briefly resizing the bar.\"\"\"\n    size = self.GetSize()\n    width = size.GetWidth()\n    if width &gt; 0:\n        self.SetSize(wx.Size(width - 1, size.GetHeight()))\n    self.SetSize(wx.Size(width, size.GetHeight()))\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarButton","title":"TreeToolbarButton","text":"<pre><code>TreeToolbarButton(\n    parent_panel, id, title, bitmap, bitmap_disabled=None, kind=TYPE_SIMPLE\n)\n</code></pre> <p>               Bases: <code>ToolbarButton</code></p> <p>Tree-specific toolbar button (thin wrapper around ToolbarButton).</p> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def __init__(\n    self,\n    parent_panel,\n    id,\n    title,\n    bitmap,\n    bitmap_disabled=None,\n    kind=ToolbarButton.TYPE_SIMPLE,\n):\n    ToolbarButton.__init__(\n        self, parent_panel, id, title, bitmap, bitmap_disabled, kind\n    )\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarPage","title":"TreeToolbarPage","text":"<pre><code>TreeToolbarPage(parent_bar, title, kind=TYPE_PAGE_NORMAL)\n</code></pre> <p>               Bases: <code>ToolbarPage</code></p> <p>A toolbar page (top-level tree node) in the tree toolbar.</p> <p>Initialize the tree page.</p> <p>Parameters:</p> Name Type Description Default <code>parent_bar</code> <p>Parent TreeToolbarBar.</p> required <code>title</code> <p>Page title.</p> required <code>kind</code> <p>Page type.</p> <code>TYPE_PAGE_NORMAL</code> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def __init__(self, parent_bar, title, kind=ToolbarPage.TYPE_PAGE_NORMAL):\n    \"\"\"Initialize the tree page.\n\n    Args:\n        parent_bar: Parent TreeToolbarBar.\n        title: Page title.\n        kind: Page type.\n    \"\"\"\n    ToolbarPage.__init__(self, parent_bar, title, kind)\n    self.tree_item = parent_bar.append_to_tree(parent_bar.tree_main_page, title)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarPage-functions","title":"Methods:","text":""},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarPage.create_html_panel","title":"create_html_panel","text":"<pre><code>create_html_panel(title)\n</code></pre> <p>Create an HTML-capable panel within this tree page.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <p>Panel title.</p> required <p>Returns:</p> Name Type Description <code>TreeHtmlPanel</code> <p>The HTML panel wrapper.</p> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def create_html_panel(self, title):\n    \"\"\"Create an HTML-capable panel within this tree page.\n\n    Args:\n        title: Panel title.\n\n    Returns:\n        TreeHtmlPanel: The HTML panel wrapper.\n    \"\"\"\n    p = wx.Panel(self.parent_bar, size=wx.Size(200, 200))\n    self.parent_bar.AppendItem(self.tree_item, title, 0, p)\n    self.parent_bar.Expand(self.tree_item)\n    return TreeHtmlPanel(self, p)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarPage.create_panel","title":"create_panel","text":"<pre><code>create_panel(title, kind=TYPE_PANEL_TOOLBAR)\n</code></pre> <p>Create a panel within this tree page.</p> <p>Returns:</p> Name Type Description <code>TreeToolbarPanel</code> <p>The newly created panel.</p> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def create_panel(self, title, kind=ToolbarPanel.TYPE_PANEL_TOOLBAR):\n    \"\"\"Create a panel within this tree page.\n\n    Returns:\n        TreeToolbarPanel: The newly created panel.\n    \"\"\"\n    return TreeToolbarPanel(self, title, kind)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarPanel","title":"TreeToolbarPanel","text":"<pre><code>TreeToolbarPanel(parent_page, title, kind=TYPE_PANEL_TOOLBAR)\n</code></pre> <p>               Bases: <code>ToolbarPanel</code></p> <p>A panel within the tree toolbar that holds a group of tree items.</p> <p>Initialize the tree panel as a child of a tree item.</p> <p>Parameters:</p> Name Type Description Default <code>parent_page</code> <p>Parent TreeToolbarPage.</p> required <code>title</code> <p>Panel title.</p> required <code>kind</code> <p>Panel type.</p> <code>TYPE_PANEL_TOOLBAR</code> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def __init__(self, parent_page, title, kind=ToolbarPanel.TYPE_PANEL_TOOLBAR):\n    \"\"\"Initialize the tree panel as a child of a tree item.\n\n    Args:\n        parent_page: Parent TreeToolbarPage.\n        title: Panel title.\n        kind: Panel type.\n    \"\"\"\n    ToolbarPanel.__init__(self, parent_page, title, kind)\n    bar = self.parent_page.parent_bar\n    self.tree_item = bar.append_to_tree(parent_page.tree_item, title)\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarPanel-functions","title":"Methods:","text":""},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarPanel.add_separator","title":"add_separator","text":"<pre><code>add_separator()\n</code></pre> <p>Append a visual separator to this tree panel.</p> <p>Returns:</p> Name Type Description <code>TreeToolbarButton</code> <p>The separator button object.</p> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def add_separator(self):\n    \"\"\"Append a visual separator to this tree panel.\n\n    Returns:\n        TreeToolbarButton: The separator button object.\n    \"\"\"\n    b = TreeToolbarButton(\n        self, 0, \"\", None, None, kind=ToolbarButton.TYPE_SEPARATOR\n    )\n    self._append(b)\n    return b\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar.TreeToolbarPanel.create_button","title":"create_button","text":"<pre><code>create_button(id, title, bitmap=None, bitmap_disabled=None, kind=TYPE_SIMPLE)\n</code></pre> <p>Create and append a button to this tree panel.</p> <p>Returns:</p> Name Type Description <code>TreeToolbarButton</code> <p>The newly created button.</p> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def create_button(\n    self,\n    id,\n    title,\n    bitmap=None,\n    bitmap_disabled=None,\n    kind=ToolbarButton.TYPE_SIMPLE,\n):\n    \"\"\"Create and append a button to this tree panel.\n\n    Returns:\n        TreeToolbarButton: The newly created button.\n    \"\"\"\n    b = TreeToolbarButton(self, id, title, bitmap, bitmap_disabled, kind)\n    self._append(b)\n    return b\n</code></pre>"},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar-functions","title":"Functions:","text":""},{"location":"api/toolbar_treetoolbar/#pytigon_gui.toolbar.treetoolbar._tree_list","title":"_tree_list","text":"<pre><code>_tree_list(tree, parent_item, result_list=None)\n</code></pre> <p>Recursively collect all child items of a tree node into a flat list.</p> <p>Parameters:</p> Name Type Description Default <code>tree</code> <p>The CustomTreeCtrl instance.</p> required <code>parent_item</code> <p>The parent tree item to start from.</p> required <code>result_list</code> <p>Optional existing list to append to. If None, a new list is created.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>list</code> <p>Flat list of all child tree items.</p> Source code in <code>pytigon_gui/toolbar/treetoolbar.py</code> <pre><code>def _tree_list(tree, parent_item, result_list=None):\n    \"\"\"Recursively collect all child items of a tree node into a flat list.\n\n    Args:\n        tree: The CustomTreeCtrl instance.\n        parent_item: The parent tree item to start from.\n        result_list: Optional existing list to append to. If None, a new\n            list is created.\n\n    Returns:\n        list: Flat list of all child tree items.\n    \"\"\"\n    if result_list is None:\n        result_list = []\n\n    item, cookie = tree.GetFirstChild(parent_item)\n    while item and item.IsOk():\n        result_list.append(item)\n        if tree.ItemHasChildren(item):\n            _tree_list(tree, item, result_list)\n        item, cookie = tree.GetNextChild(parent_item, cookie)\n\n    return result_list\n</code></pre>"},{"location":"api/wxauto/","title":"wxauto \u2013 wxPython Automation","text":"<p>wxPython automation and helper utilities for window management, control lookup, and UI testing.</p>"},{"location":"api/wxauto/#pytigon_gui.wxauto","title":"wxauto","text":"<p>Pytigon GUI automation module using pyautogui and wxPython.</p> <p>This module provides a lightweight scripting engine for automating wxPython GUI interactions. It is primarily used for:   - Automated UI testing (simulating user clicks, keyboard input, navigation).   - Recording and replaying user sessions (generating SRT subtitle files     synchronized with video recordings).   - Scripting complex UI workflows from plain-text <code>.txt</code> files or Python modules.</p>"},{"location":"api/wxauto/#pytigon_gui.wxauto--architecture","title":"Architecture","text":"<p>The module uses a command-overloading pattern based on Python's <code>&lt;&lt;</code> operator (left-shift / <code>__lshift__</code>). Each line of an automation script is dispatched to a handler:</p> <ul> <li>WxAuto is the core controller. It owns the pyautogui interface,     tracks timing for subtitle generation, and interprets script commands.</li> <li>ControlProxy wraps a named wx.Window. Writing <code>control &lt;&lt; \"value\"</code>     moves the mouse to the control, focuses it, and sets its value.</li> <li>ImgProxy wraps an image file path. Writing <code>img_png &lt;&lt; \"\"</code>     locates the image on screen and clicks at its center.</li> </ul>"},{"location":"api/wxauto/#pytigon_gui.wxauto--script-syntax","title":"Script Syntax","text":"<p>Automation scripts are plain text files (or multi-line strings) where each line is a command. The format is intentionally simple:</p> <p><code>.</code> (dot)       Sleep for <code>len(line) * time_unit</code> seconds. Multiple dots mean longer       pauses. Example: <code>....</code> sleeps for 4 * 0.5 = 2 seconds.</p> <p><code>^method_name</code> or <code>^method_name:argument</code>       Call a method on the WxAuto instance. The method name is taken from       everything after <code>^</code> up to the optional <code>:</code>. If a <code>:</code> is present,       the text after it is passed as a single string argument.       Example: <code>^auto_click:my_button</code> calls <code>auto_click(\"my_button\")</code>.</p> <p><code>&lt;&lt;key1 key2 ^key3 ...</code>       Simulate raw keyboard input using pyautogui. Each token is processed:         - <code>^X</code> \u2192 release key X (keyUp).         - <code>_X</code> \u2192 press and hold key X (keyDown).         - <code>-</code> followed by more dashes \u2192 sleep (each dash after the first           adds <code>time_unit / 10</code> seconds).         - Anything else \u2192 press and release the key.       Example: <code>&lt;&lt;enter</code> presses Enter. <code>&lt;&lt;_ctrl c ^ctrl</code> does Ctrl+C.</p> <p><code># comment</code>       Ignored line (comment).</p> <p><code>@filename</code>       Load commands from an external file. The file content can use Django       template syntax if <code>argv</code> context is provided.</p> <p><code>control_name &lt;&lt; value</code>       Dispatch to a ControlProxy or ImgProxy. If <code>control_name</code> ends with       <code>_png</code>, an ImgProxy is used (image-based click). Otherwise a       ControlProxy is used (window-name-based interaction).</p> <p>Any other text       Treated as a subtitle entry and written to the SRT file via       <code>WxAuto.__lshift__</code>.</p>"},{"location":"api/wxauto/#pytigon_gui.wxauto--integration","title":"Integration","text":"<p>The module hooks into the Pytigon application lifecycle through <code>wx.pseudoimport</code>. When <code>wxauto.py</code> is run directly, it sets <code>wx.pseudoimport = autoit</code> and launches the main Pytigon application with <code>--video</code> and <code>--rpc</code> flags. The application frame calls <code>wx.pseudoimport(frame)</code> after initialization, which triggers the automation coroutine.</p>"},{"location":"api/wxauto/#pytigon_gui.wxauto--dependencies","title":"Dependencies","text":"<ul> <li>pyautogui: cross-platform GUI automation (mouse, keyboard, screen).</li> <li>wxPython: window lookup via <code>wx.Window.FindWindowByName</code>.</li> <li>Django template engine: optional, for templated script files.</li> <li>asyncio: all automation actions are async coroutines.</li> </ul>"},{"location":"api/wxauto/#pytigon_gui.wxauto-attributes","title":"Attributes","text":""},{"location":"api/wxauto/#pytigon_gui.wxauto.SCRIPT","title":"SCRIPT  <code>module-attribute</code>","text":"<pre><code>SCRIPT = sys.argv[-1]\n</code></pre>"},{"location":"api/wxauto/#pytigon_gui.wxauto.logger","title":"logger  <code>module-attribute</code>","text":"<pre><code>logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/wxauto/#pytigon_gui.wxauto-classes","title":"Classes","text":""},{"location":"api/wxauto/#pytigon_gui.wxauto.ControlProxy","title":"ControlProxy","text":"<pre><code>ControlProxy(wx_auto, control_name)\n</code></pre> <p>Proxy for interacting with a named wxPython control.</p> <p>In automation scripts, writing::</p> <pre><code>button_name &lt;&lt; \"click\"\n</code></pre> <p>causes the mouse to move to the window named <code>button_name</code> and perform an action. The action depends on the value:</p> <ul> <li><code>\"click\"</code> \u2192 left mouse click at the control's center.</li> <li><code>\"focus\"</code> \u2192 only set keyboard focus, no click, no text.</li> <li>Any other string \u2192 set the control's value via <code>SetValue()</code>.</li> </ul> <p>This proxy is created automatically by :meth:<code>WxAuto.__getattr__</code> when an unknown attribute name is accessed.</p> <p>Initialize the control proxy.</p> <p>Parameters:</p> Name Type Description Default <code>wx_auto</code> <code>WxAuto</code> <p>The parent WxAuto controller instance.</p> required <code>control_name</code> <code>str</code> <p>Internal name of the wx.Window to target (set via <code>wx.Window.SetName</code>).</p> required Source code in <code>pytigon_gui/wxauto.py</code> <pre><code>def __init__(self, wx_auto, control_name):\n    \"\"\"Initialize the control proxy.\n\n    Args:\n        wx_auto (WxAuto):\n            The parent WxAuto controller instance.\n        control_name (str):\n            Internal name of the wx.Window to target (set via\n            ``wx.Window.SetName``).\n    \"\"\"\n    #: WxAuto: Reference to the parent automation controller.\n    self.wx_auto = wx_auto\n    #: str: Name of the target wx.Window.\n    self.control_name = control_name\n</code></pre>"},{"location":"api/wxauto/#pytigon_gui.wxauto.ControlProxy-functions","title":"Methods:","text":""},{"location":"api/wxauto/#pytigon_gui.wxauto.ImgProxy","title":"ImgProxy","text":"<pre><code>ImgProxy(wx_auto, img_name)\n</code></pre> <p>Proxy for interacting with a UI element identified by an image file.</p> <p>In automation scripts, writing::</p> <pre><code>icon_png &lt;&lt; \"\"\n</code></pre> <p>causes pyautogui to search the screen for the image file <code>icon.png</code> and click at its center.</p> <p>The image search is constrained to the application window region (see :meth:<code>WxAuto.get_region</code>) for better performance.</p> <p>This proxy is created automatically by :meth:<code>WxAuto.__getattr__</code> when the attribute name ends with <code>_png</code>.</p> <p>Initialize the image proxy.</p> <p>Parameters:</p> Name Type Description Default <code>wx_auto</code> <code>WxAuto</code> <p>The parent WxAuto controller instance.</p> required <code>img_name</code> <code>str</code> <p>Path to the PNG image file to locate on screen.</p> required Source code in <code>pytigon_gui/wxauto.py</code> <pre><code>def __init__(self, wx_auto, img_name):\n    \"\"\"Initialize the image proxy.\n\n    Args:\n        wx_auto (WxAuto):\n            The parent WxAuto controller instance.\n        img_name (str):\n            Path to the PNG image file to locate on screen.\n    \"\"\"\n    #: WxAuto: Reference to the parent automation controller.\n    self.wx_auto = wx_auto\n    #: str: Path to the image file for screen matching.\n    self.img_name = img_name\n    #: None: Reserved for future use (cached image data).\n    self.img = None\n</code></pre>"},{"location":"api/wxauto/#pytigon_gui.wxauto.ImgProxy-functions","title":"Methods:","text":""},{"location":"api/wxauto/#pytigon_gui.wxauto.WxAuto","title":"WxAuto","text":"<pre><code>WxAuto(script_name, pos, size)\n</code></pre> <p>Automation controller for wxPython applications.</p> <p>This is the central class that drives GUI automation. It wraps pyautogui for low-level mouse/keyboard control and wxPython for window discovery. It also manages subtitle (SRT) file generation for screen recordings.</p> <p>The controller is designed to be driven by a script (either a plain text file or a Python module) that issues commands line by line.</p> <p>Attributes:</p> Name Type Description <code>script_name</code> <code>str</code> <p>Base name used for the output subtitle file (<code>&lt;name&gt;.srt</code>).</p> <code>pos</code> <code>Point or None</code> <p>Screen position of the application window. Used to constrain image-based lookups to the application region. May be None if the window is not yet shown.</p> <code>size</code> <code>Size or None</code> <p>Dimensions of the application window.</p> <code>subtitle_id</code> <code>int</code> <p>Sequential counter for SRT subtitle entries. Starts at 1.</p> <code>start_time</code> <code>datetime</code> <p>Timestamp captured when the WxAuto instance is created. Used as the zero-point for subtitle timing.</p> <code>time_unit</code> <code>float</code> <p>Base time multiplier in seconds. Each <code>.</code> in a script line adds <code>time_unit</code> seconds of delay. Default 0.5.</p> <code>sys_time_unit</code> <code>float</code> <p>Delay inserted between system-level operations (focus changes, mouse movements) to ensure the UI has time to react. Default 0.5.</p> <code>pyautogui</code> <code>module</code> <p>Reference to the pyautogui module for mouse/keyboard control.</p> <p>Initialize the automation controller.</p> <p>Parameters:</p> Name Type Description Default <code>script_name</code> <code>str</code> <p>Base name used for the output subtitle file and for referencing the script in error messages.</p> required <code>pos</code> <code>Point or None</code> <p>Screen position of the application window. Pass None if the window position is not yet known.</p> required <code>size</code> <code>Size or None</code> <p>Dimensions of the application window. Pass None if unknown.</p> required <p>Raises:</p> Type Description <code>ImportError</code> <p>If pyautogui is not installed. Install with: <code>pip install pyautogui</code>.</p> Source code in <code>pytigon_gui/wxauto.py</code> <pre><code>def __init__(self, script_name, pos, size):\n    \"\"\"Initialize the automation controller.\n\n    Args:\n        script_name (str):\n            Base name used for the output subtitle file and for\n            referencing the script in error messages.\n        pos (wx.Point or None):\n            Screen position of the application window. Pass None if\n            the window position is not yet known.\n        size (wx.Size or None):\n            Dimensions of the application window. Pass None if unknown.\n\n    Raises:\n        ImportError:\n            If pyautogui is not installed. Install with:\n            ``pip install pyautogui``.\n    \"\"\"\n    #: str: Base name for subtitle file (script_name.srt).\n    self.script_name = script_name\n    #: wx.Point or None: Application window screen position.\n    self.pos = pos\n    #: wx.Size or None: Application window dimensions.\n    self.size = size\n    #: int: Sequential subtitle counter, starting at 1.\n    self.subtitle_id = 1\n    #: datetime: Reference timestamp for subtitle timing offsets.\n    self.start_time = datetime.datetime.now()\n    #: float: Base delay per dot character in script lines.\n    self.time_unit = 0.5\n    #: float: Delay between system UI operations.\n    self.sys_time_unit = 0.5\n    self._window_cache = {}\n\n    # Lazy-import pyautogui with a clear error message if missing.\n    try:\n        import pyautogui\n\n        self.pyautogui = pyautogui\n    except ImportError as e:\n        raise ImportError(\n            \"pyautogui is required for WxAuto. Install it with: \"\n            \"pip install pyautogui\"\n        ) from e\n</code></pre>"},{"location":"api/wxauto/#pytigon_gui.wxauto.WxAuto-functions","title":"Methods:","text":""},{"location":"api/wxauto/#pytigon_gui.wxauto.WxAuto.auto_click","title":"auto_click  <code>async</code>","text":"<pre><code>auto_click(window_name)\n</code></pre> <p>Move the mouse to a named window and perform a left click.</p> <p>This is a convenience wrapper around :meth:<code>auto_move_and_focus</code> followed by <code>pyautogui.click()</code>.</p> <p>Parameters:</p> Name Type Description Default <code>window_name</code> <code>str</code> <p>Internal name of the wx.Window to click on.</p> required Source code in <code>pytigon_gui/wxauto.py</code> <pre><code>async def auto_click(self, window_name):\n    \"\"\"Move the mouse to a named window and perform a left click.\n\n    This is a convenience wrapper around :meth:`auto_move_and_focus`\n    followed by ``pyautogui.click()``.\n\n    Args:\n        window_name (str):\n            Internal name of the wx.Window to click on.\n    \"\"\"\n    await self.auto_move_and_focus(window_name)\n    self.pyautogui.click()\n</code></pre>"},{"location":"api/wxauto/#pytigon_gui.wxauto.WxAuto.auto_click_on_img","title":"auto_click_on_img  <code>async</code>","text":"<pre><code>auto_click_on_img(image_name)\n</code></pre> <p>Locate an image on screen and click on its center.</p> <p>Convenience wrapper: calls :meth:<code>auto_focus_on_img</code> and then <code>pyautogui.click()</code>.</p> <p>Parameters:</p> Name Type Description Default <code>image_name</code> <code>str</code> <p>Path to the PNG image file to locate and click.</p> required Source code in <code>pytigon_gui/wxauto.py</code> <pre><code>async def auto_click_on_img(self, image_name):\n    \"\"\"Locate an image on screen and click on its center.\n\n    Convenience wrapper: calls :meth:`auto_focus_on_img` and then\n    ``pyautogui.click()``.\n\n    Args:\n        image_name (str):\n            Path to the PNG image file to locate and click.\n    \"\"\"\n    await self.auto_focus_on_img(image_name)\n    self.pyautogui.click()\n</code></pre>"},{"location":"api/wxauto/#pytigon_gui.wxauto.WxAuto.auto_focus_on_img","title":"auto_focus_on_img  <code>async</code>","text":"<pre><code>auto_focus_on_img(image_name)\n</code></pre> <p>Locate an image on screen and move the mouse to its center.</p> <p>Uses pyautogui's <code>locateOnScreen</code> to find the given image within the application region (see :meth:<code>get_region</code>). This is useful for clicking buttons or icons that cannot be found by window name.</p> <p>Parameters:</p> Name Type Description Default <code>image_name</code> <code>str</code> <p>Path to the PNG image file to search for on screen.</p> required <p>Raises:</p> Type Description <code>TypeError</code> <p>If the image cannot be found within the search region.</p> Source code in <code>pytigon_gui/wxauto.py</code> <pre><code>async def auto_focus_on_img(self, image_name):\n    \"\"\"Locate an image on screen and move the mouse to its center.\n\n    Uses pyautogui's ``locateOnScreen`` to find the given image within\n    the application region (see :meth:`get_region`). This is useful for\n    clicking buttons or icons that cannot be found by window name.\n\n    Args:\n        image_name (str):\n            Path to the PNG image file to search for on screen.\n\n    Raises:\n        TypeError:\n            If the image cannot be found within the search region.\n    \"\"\"\n    await sleep(self.sys_time_unit / 2)\n\n    region = self.get_region()\n    location = self.pyautogui.locateOnScreen(image_name, region=region)\n    if location is None:\n        raise TypeError(\n            \"Image '%s' not found on screen (region: %s)\" % (image_name, region)\n        )\n\n    # Calculate the geometric center of the matched region.\n    x, y = self.pyautogui.center(location)\n    self.pyautogui.moveTo(x, y)\n    await sleep(self.sys_time_unit / 2)\n</code></pre>"},{"location":"api/wxauto/#pytigon_gui.wxauto.WxAuto.auto_move_and_focus","title":"auto_move_and_focus  <code>async</code>","text":"<pre><code>auto_move_and_focus(window_name, set_focus=True)\n</code></pre> <p>Move the mouse cursor to the center of a named wx.Window.</p> <p>Uses <code>wx.Window.FindWindowByName</code> to locate the window by its internal name (set via <code>wx.Window.SetName</code>). After moving the mouse, optionally sets keyboard focus to the window.</p> <p>Delays are inserted before and after each UI operation to give the window system time to process events.</p> <p>Parameters:</p> Name Type Description Default <code>window_name</code> <code>str</code> <p>Internal name of the wx.Window to find.</p> required <code>set_focus</code> <code>bool</code> <p>If True, call <code>SetFocus()</code> on the found window before moving the mouse. Default True.</p> <code>True</code> <p>Returns:</p> Type Description <p>wx.Window or None: The found window, or None if no window with the given name exists.</p> Source code in <code>pytigon_gui/wxauto.py</code> <pre><code>async def auto_move_and_focus(self, window_name, set_focus=True):\n    \"\"\"Move the mouse cursor to the center of a named wx.Window.\n\n    Uses ``wx.Window.FindWindowByName`` to locate the window by its\n    internal name (set via ``wx.Window.SetName``). After moving the\n    mouse, optionally sets keyboard focus to the window.\n\n    Delays are inserted before and after each UI operation to give\n    the window system time to process events.\n\n    Args:\n        window_name (str):\n            Internal name of the wx.Window to find.\n        set_focus (bool):\n            If True, call ``SetFocus()`` on the found window before\n            moving the mouse. Default True.\n\n    Returns:\n        wx.Window or None:\n            The found window, or None if no window with the given\n            name exists.\n    \"\"\"\n    await sleep(self.sys_time_unit / 3)\n\n    win = self._window_cache.get(window_name)\n    if win is None or not win:\n        win = wx.Window.FindWindowByName(window_name)\n        if win:\n            self._window_cache[window_name] = win\n    if win and set_focus:\n        win.SetFocus()\n        await sleep(self.sys_time_unit / 3)\n\n    if not win:\n        logger.warning(\"window '%s' not found\", window_name)\n        return None\n\n    # Calculate the center of the window in screen coordinates.\n    pos = win.GetScreenPosition()\n    size = win.GetSize()\n    self.pyautogui.moveTo(\n        pos[0] + int(size.GetWidth() / 2),\n        pos[1] + int(size.GetHeight() / 2),\n    )\n    await sleep(self.sys_time_unit / 3)\n    return win\n</code></pre>"},{"location":"api/wxauto/#pytigon_gui.wxauto.WxAuto.dropdown","title":"dropdown  <code>async</code>","text":"<pre><code>dropdown(delta)\n</code></pre> <p>Simulate opening a dropdown combo and selecting an item.</p> The sequence is <ol> <li>Press Alt+Down to open the dropdown.</li> <li>Press Down/Up arrow keys <code>abs(delta)</code> times to navigate.</li> <li>Press Enter to confirm the selection.</li> </ol> <p>Parameters:</p> Name Type Description Default <code>delta</code> <code>str or int</code> <p>Number of items to move down (positive) or up (negative) from the top of the dropdown list.</p> required Source code in <code>pytigon_gui/wxauto.py</code> <pre><code>async def dropdown(self, delta):\n    \"\"\"Simulate opening a dropdown combo and selecting an item.\n\n    The sequence is:\n      1. Press Alt+Down to open the dropdown.\n      2. Press Down/Up arrow keys ``abs(delta)`` times to navigate.\n      3. Press Enter to confirm the selection.\n\n    Args:\n        delta (str or int):\n            Number of items to move down (positive) or up (negative)\n            from the top of the dropdown list.\n    \"\"\"\n    delta2 = int(delta)\n\n    # Open the dropdown.\n    self.pyautogui.keyDown(\"alt\")\n    self.pyautogui.press(\"down\")\n    self.pyautogui.keyUp(\"alt\")\n    await sleep(1)\n\n    # Navigate through the dropdown items.\n    if delta2 &gt; 0:\n        for _ in range(0, delta2):\n            self.pyautogui.press(\"down\")\n            await sleep(0.1)\n    else:\n        for _ in range(0, -delta2):\n            self.pyautogui.press(\"up\")\n            await sleep(0.1)\n\n    # Confirm selection.\n    self.pyautogui.press(\"enter\")\n    await sleep(1)\n</code></pre>"},{"location":"api/wxauto/#pytigon_gui.wxauto.WxAuto.get_region","title":"get_region","text":"<pre><code>get_region()\n</code></pre> <p>Return the screen region occupied by the application window.</p> <p>The region is used by pyautogui's <code>locateOnScreen</code> to restrict image searches to the application area, improving both performance and accuracy.</p> <p>Returns:</p> Type Description <p>tuple or None: <code>(x, y, width, height)</code> if both position and size are known, <code>None</code> otherwise.</p> Source code in <code>pytigon_gui/wxauto.py</code> <pre><code>def get_region(self):\n    \"\"\"Return the screen region occupied by the application window.\n\n    The region is used by pyautogui's ``locateOnScreen`` to restrict\n    image searches to the application area, improving both performance\n    and accuracy.\n\n    Returns:\n        tuple or None:\n            ``(x, y, width, height)`` if both position and size are known,\n            ``None`` otherwise.\n    \"\"\"\n    if self.pos is None or self.size is None:\n        return None\n    return (\n        self.pos.x,\n        self.pos.y,\n        self.size.GetWidth(),\n        self.size.GetHeight(),\n    )\n</code></pre>"},{"location":"api/wxauto/#pytigon_gui.wxauto.WxAuto.process","title":"process  <code>async</code>","text":"<pre><code>process(txt, argv=None, change_tab=None)\n</code></pre> <p>Interpret and execute an automation script.</p> <p>This is the main entry point for running automation scripts. It can process either inline script text or load commands from a file (when <code>txt</code> starts with <code>@</code>).</p> <p>When loading from a file, Django template rendering is optionally applied using the <code>argv</code> dictionary as template context.</p> <p>The <code>change_tab</code> parameter allows placeholder substitution before script execution. Replacements are specified as <code>key:value</code> pairs separated by <code>;;</code> (or passed as a list). Each <code>$key</code> in the script text is replaced by its corresponding value.</p> <p>Parameters:</p> Name Type Description Default <code>txt</code> <code>str</code> <p>Script content. If it starts with <code>@</code>, the rest is treated as a filename to load. Otherwise, the string itself is the script.</p> required <code>argv</code> <code>dict or None</code> <p>Optional template context variables. Only used when loading a script from file (<code>txt</code> starts with <code>@</code>).</p> <code>None</code> <code>change_tab</code> <code>str or list or None</code> <p>Optional placeholder replacements. - If a string: use <code>;;</code> as separator between   <code>key:value</code> pairs, <code>\\n</code> as pair separator. - If a list: each element should be a <code>key:value</code> string.</p> <code>None</code>"},{"location":"api/wxauto/#pytigon_gui.wxauto.WxAuto.process--script-command-reference","title":"Script Command Reference","text":"<p>See the module-level documentation for the full script syntax.</p> Source code in <code>pytigon_gui/wxauto.py</code> <pre><code>async def process(self, txt, argv=None, change_tab=None):\n    \"\"\"Interpret and execute an automation script.\n\n    This is the main entry point for running automation scripts. It can\n    process either inline script text or load commands from a file\n    (when ``txt`` starts with ``@``).\n\n    When loading from a file, Django template rendering is optionally\n    applied using the ``argv`` dictionary as template context.\n\n    The ``change_tab`` parameter allows placeholder substitution before\n    script execution. Replacements are specified as ``key:value`` pairs\n    separated by ``;;`` (or passed as a list). Each ``$key`` in the\n    script text is replaced by its corresponding value.\n\n    Args:\n        txt (str):\n            Script content. If it starts with ``@``, the rest is treated\n            as a filename to load. Otherwise, the string itself is the\n            script.\n        argv (dict or None):\n            Optional template context variables. Only used when loading\n            a script from file (``txt`` starts with ``@``).\n        change_tab (str or list or None):\n            Optional placeholder replacements.\n            - If a string: use ``;;`` as separator between\n              ``key:value`` pairs, ``\\\\n`` as pair separator.\n            - If a list: each element should be a ``key:value`` string.\n\n    Script Command Reference\n    ------------------------\n    See the module-level documentation for the full script syntax.\n    \"\"\"\n    # ---- Step 1: Load script content (from string or file) ------------\n    if txt.startswith(\"@\"):\n        # Load commands from an external file.\n        try:\n            with open(txt[1:], \"rt\") as f:\n                txt2 = f.read()\n                # Optionally render Django template variables.\n                if argv:\n                    t = Template(txt2)\n                    c = Context(argv)\n                    txt2 = t.render(c)\n        except OSError as e:\n            logger.error(\"Error reading script file '%s': %s\", txt[1:], e)\n            return\n        except Exception as e:\n            logger.error(\"Error rendering template '%s': %s\", txt[1:], e)\n            return\n    else:\n        txt2 = txt\n\n    # ---- Step 2: Apply placeholder substitutions ----------------------\n    if change_tab:\n        # Normalize to a list of \"key:value\" strings.\n        if isinstance(change_tab, str):\n            change_tab2 = change_tab.replace(\";;\", \"\\n\").split(\"\\n\")\n        else:\n            change_tab2 = list(change_tab)\n\n        # Process in reverse so that nested $references work correctly\n        # (later replacements don't interfere with earlier ones).\n        change_tab2.reverse()\n        for item in change_tab2:\n            if \":\" in item:\n                key, value = item.split(\":\", 1)\n                txt2 = txt2.replace(\"$\" + key, value)\n\n    # ---- Step 3: Interpret script line by line ------------------------\n    for line in txt2.split(\"\\n\"):\n        stripped = line.strip()\n\n        # Skip empty lines entirely.\n        if not stripped:\n            continue\n\n        # --- Pause: '.' lines ---\n        # Each character adds time_unit seconds of delay.\n        if stripped.startswith(\".\"):\n            await sleep(len(stripped) * self.time_unit)\n\n        # --- Method call: '^method' or '^method:arg' ---\n        # Calls a method on this WxAuto instance dynamically.\n        elif stripped.startswith(\"^\"):\n            parts = stripped.split(\":\")\n            try:\n                if len(parts) &gt; 1:\n                    # ^method_name:argument\n                    method_name = parts[0][1:]  # strip leading '^'\n                    await getattr(self, method_name)(parts[1])\n                else:\n                    # ^method_name (no argument)\n                    method_name = parts[0][1:]\n                    await getattr(self, method_name)()\n            except AttributeError as e:\n                logger.error(\"Unknown automation command: %s (%s)\", parts[0], e)\n            except Exception as e:\n                logger.error(\"Error executing command '%s': %s\", stripped, e)\n\n        # --- Comment: '#' lines ---\n        # Ignored - used for documentation within scripts.\n        elif stripped.startswith(\"#\"):\n            pass\n\n        # --- Raw keyboard: '&lt;&lt;key1 key2 ...' ---\n        # Sends raw key events via pyautogui.\n        elif stripped.startswith(\"&lt;&lt;\"):\n            tokens = stripped[2:].split(\" \")\n            for token in tokens:\n                token = token.strip()\n                if not token:\n                    continue\n                if token.startswith(\"^\"):\n                    # Release (keyUp) the key.\n                    self.pyautogui.keyUp(token[1:])\n                elif token.startswith(\"_\"):\n                    # Press and hold (keyDown) the key.\n                    self.pyautogui.keyDown(token[1:])\n                elif token.startswith(\"-\"):\n                    # Dash-based timing: each dash after the first\n                    # adds time_unit/10 seconds of delay.\n                    await sleep((len(token) - 1) * self.time_unit / 10)\n                else:\n                    # Press and release a regular key.\n                    self.pyautogui.press(token)\n\n        # --- Proxy dispatch: 'name &lt;&lt; value' or plain text ---\n        else:\n            if \"&lt;&lt;\" in stripped:\n                # Split on '&lt;&lt;' to get proxy_name and value.\n                parts = stripped.split(\"&lt;&lt;\")\n                proxy_name = parts[0].strip()\n                value = parts[1].strip()\n                # Dynamically create a ControlProxy or ImgProxy.\n                await (getattr(self, proxy_name) &lt;&lt; value)\n            else:\n                # Plain text: write as subtitle entry.\n                await (self &lt;&lt; stripped)\n</code></pre>"},{"location":"api/wxauto/#pytigon_gui.wxauto-functions","title":"Functions:","text":""},{"location":"api/wxauto/#pytigon_gui.wxauto.autoit","title":"autoit","text":"<pre><code>autoit(win)\n</code></pre> <p>Entry point for wxPython automation from the main application frame.</p> <p>This function is called by the Pytigon application after the main window has been created. It sets up a :class:<code>WxAuto</code> instance and launches an async coroutine to execute the automation script.</p> <p>Two script types are supported:</p> <ol> <li> <p>Plain text (<code>.txt</code> files):    The file is loaded and processed line by line by    :meth:<code>WxAuto.process</code>.</p> </li> <li> <p>Python module (<code>.py</code> files, or dotted module names):    The module is imported and its <code>wxauto(wx_auto, pyautogui, wx)</code>    function is called. This gives full programmatic control over    the automation.</p> </li> </ol> <p>Errors during script execution are caught and printed via <code>traceback.format_exc()</code> to avoid crashing the application.</p> <p>Parameters:</p> Name Type Description Default <code>win</code> <code>Window or None</code> <p>The main application window. Used to capture the screen position and size for region-constrained image searches. May be None if the window hasn't been created yet.</p> required Source code in <code>pytigon_gui/wxauto.py</code> <pre><code>def autoit(win):\n    \"\"\"Entry point for wxPython automation from the main application frame.\n\n    This function is called by the Pytigon application after the main\n    window has been created. It sets up a :class:`WxAuto` instance and\n    launches an async coroutine to execute the automation script.\n\n    Two script types are supported:\n\n    1. **Plain text** (``.txt`` files):\n       The file is loaded and processed line by line by\n       :meth:`WxAuto.process`.\n\n    2. **Python module** (``.py`` files, or dotted module names):\n       The module is imported and its ``wxauto(wx_auto, pyautogui, wx)``\n       function is called. This gives full programmatic control over\n       the automation.\n\n    Errors during script execution are caught and printed via\n    ``traceback.format_exc()`` to avoid crashing the application.\n\n    Args:\n        win (wx.Window or None):\n            The main application window. Used to capture the screen\n            position and size for region-constrained image searches.\n            May be None if the window hasn't been created yet.\n    \"\"\"\n    # Capture window geometry for region-constrained image matching.\n    if win:\n        pos = win.GetScreenPosition()\n        size = win.GetSize()\n    else:\n        pos = None\n        size = None\n\n    wx_auto = WxAuto(SCRIPT, pos, size)\n\n    async def astart():\n        \"\"\"Async entry point that runs the automation script.\"\"\"\n        try:\n            # Give the application a moment to fully render.\n            await sleep(1)\n\n            if SCRIPT.endswith(\".txt\"):\n                # Plain text script: load and process line by line.\n                await wx_auto.process(\"@\" + SCRIPT)\n            else:\n                # Python module: import and call wxauto().\n                sys.path.insert(0, os.getcwd())\n                module_name = SCRIPT.rsplit(\".\", 1)[0]\n                scr = importlib.import_module(module_name)\n                await scr.wxauto(wx_auto, wx_auto.pyautogui, wx)\n        except Exception:\n            logger.error(\"Automation error: %s\", traceback.format_exc())\n\n    # Schedule the automation coroutine on the wx event loop.\n    wx.GetApp().StartCoroutine(astart, win)\n</code></pre>"},{"location":"getting-started/overview/","title":"Overview","text":""},{"location":"getting-started/overview/#what-is-pytigon-gui","title":"What is Pytigon GUI?","text":"<p>Pytigon GUI is the wxPython-based desktop interface for the Pytigon framework. It renders UI defined in an XML-like tag syntax and connects to the Django backend via an embedded HTTP client.</p>"},{"location":"getting-started/overview/#architecture","title":"Architecture","text":"<pre><code>pytigon_gui/\n\u251c\u2500\u2500 pytigon.py            # Main application (50K lines)\n\u251c\u2500\u2500 wxauto.py             # wxPython automation utilities\n\u251c\u2500\u2500 guictrl/              # GUI control system\n\u2502   \u251c\u2500\u2500 tag*.py           # Tag parser \u2192 wxPython widget bridge\n\u2502   \u251c\u2500\u2500 button/           # Button controls\n\u2502   \u251c\u2500\u2500 grid/             # Data grid with table proxies\n\u2502   \u251c\u2500\u2500 input/            # Form input controls\n\u2502   \u2514\u2500\u2500 popup/            # Popup dialogs (HTML, select2)\n\u251c\u2500\u2500 guiframe/             # Frame management\n\u2502   \u251c\u2500\u2500 appframe.py       # Main application frame\n\u2502   \u251c\u2500\u2500 form.py           # Dynamic form generation\n\u2502   \u251c\u2500\u2500 notebook.py       # Tabbed notebook\n\u2502   \u251c\u2500\u2500 notebookpage.py   # Individual notebook pages\n\u2502   \u2514\u2500\u2500 page.py           # Base page component\n\u251c\u2500\u2500 guilib/               # Shared GUI utilities\n\u2502   \u251c\u2500\u2500 image.py          # Image loading/caching\n\u2502   \u251c\u2500\u2500 signal.py         # Event signaling\n\u2502   \u251c\u2500\u2500 threadwindow.py   # Threaded window operations\n\u2502   \u251c\u2500\u2500 websocket.py      # Django Channels bridge\n\u2502   \u2514\u2500\u2500 tools.py          # General utilities\n\u2514\u2500\u2500 toolbar/              # Toolbar system\n    \u251c\u2500\u2500 moderntoolbar.py  # Modern ribbon-style toolbar\n    \u251c\u2500\u2500 standardtoolbar.py# Classic toolbar\n    \u2514\u2500\u2500 treetoolbar.py    # Tree-based toolbar\n</code></pre>"},{"location":"getting-started/overview/#ui-tag-system","title":"UI Tag System","text":"<p>Pytigon GUI uses a custom XML-like syntax to define UI layout:</p> <pre><code>&lt;panel&gt;\n  &lt;toolbar type=\"modern\" /&gt;\n  &lt;form&gt;\n    &lt;field name=\"title\" type=\"char\" /&gt;\n    &lt;button action=\"save\" /&gt;\n  &lt;/form&gt;\n  &lt;grid table=\"mytable\" /&gt;\n&lt;/panel&gt;\n</code></pre> <p>Tags are parsed by <code>guictrl.tag_parsers</code> and <code>guictrl.tag_preprocess</code>, then instantiated as wxPython widgets by <code>guictrl.tag_ctrltag</code>.</p>"},{"location":"getting-started/overview/#dependencies","title":"Dependencies","text":"<ul> <li>wxPython 4.x+ \u2013 Desktop GUI toolkit</li> <li>Pillow \u2013 Image handling</li> <li>Django \u2013 Backend communication</li> <li>pytigon_lib \u2013 Core library (HTTP client, HTML rendering)</li> </ul>"},{"location":"getting-started/quick-import/","title":"Quick Import Reference","text":""},{"location":"getting-started/quick-import/#main-application","title":"Main Application","text":"<pre><code>from pytigon_gui.pytigon import main\nmain()\n</code></pre>"},{"location":"getting-started/quick-import/#frames","title":"Frames","text":"<pre><code>from pytigon_gui.guiframe.appframe import AppFrame\nfrom pytigon_gui.guiframe.form import Form\nfrom pytigon_gui.guiframe.notebook import NoteBook\nfrom pytigon_gui.guiframe.page import Page\n</code></pre>"},{"location":"getting-started/quick-import/#controls","title":"Controls","text":"<pre><code>from pytigon_gui.guictrl.ctrl import Ctrl\nfrom pytigon_gui.guictrl.factory import CtrlFactory\nfrom pytigon_gui.guictrl.grids import Grids\nfrom pytigon_gui.guictrl.panels import Panels\n</code></pre>"},{"location":"getting-started/quick-import/#grid","title":"Grid","text":"<pre><code>from pytigon_gui.guictrl.grid.grid import Grid\nfrom pytigon_gui.guictrl.grid.gridpanel import GridPanel\nfrom pytigon_gui.guictrl.grid.gridtable_base import GridTableBase\nfrom pytigon_gui.guictrl.grid.gridtable_from_proxy import GridTableFromProxy\n</code></pre>"},{"location":"getting-started/quick-import/#input-controls","title":"Input Controls","text":"<pre><code>from pytigon_gui.guictrl.input.text import TextCtrl\nfrom pytigon_gui.guictrl.input.choice import ChoiceCtrl\nfrom pytigon_gui.guictrl.input.combo import ComboCtrl\nfrom pytigon_gui.guictrl.input.numeric import NumericCtrl\nfrom pytigon_gui.guictrl.input.datetime import DateTimeCtrl\n</code></pre>"},{"location":"getting-started/quick-import/#toolbars","title":"Toolbars","text":"<pre><code>from pytigon_gui.toolbar.moderntoolbar import ModernToolBar\nfrom pytigon_gui.toolbar.standardtoolbar import StandardToolBar\nfrom pytigon_gui.toolbar.treetoolbar import TreeToolBar\nfrom pytigon_gui.toolbar.standardtoolbarbuttons import StandardToolBarButtons\n</code></pre>"},{"location":"getting-started/quick-import/#library-utilities","title":"Library Utilities","text":"<pre><code>from pytigon_gui.guilib.image import Image\nfrom pytigon_gui.guilib.signal import Signal\nfrom pytigon_gui.guilib.threadwindow import ThreadWindow\nfrom pytigon_gui.guilib.websocket import WebSocketClient\n</code></pre>"}]}