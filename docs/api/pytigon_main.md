# pytigon – Main Application

Main wxPython application entry point. Handles app initialization, frame creation,
Django embedding, toolbar setup, and the main event loop.

::: pytigon_gui.pytigon
    options:
      show_submodules: false
      members: true
