# guictrl.grid – Grid Subpackage

Data grid components: grid widget, panel wrapper, table base classes
(from HTML tables and data proxies), cell editors, and renderers.

::: pytigon_gui.guictrl.grid
    options:
      show_submodules: true
      members: true
