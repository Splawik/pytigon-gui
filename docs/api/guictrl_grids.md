# guictrl.grids – Grid Container

Grid layout container for arranging child controls in a
wxGrid-compatible structure.

::: pytigon_gui.guictrl.grids
    options:
      show_submodules: false
      members: true
