# guictrl.tag_parsers – Tag Parsers

Individual tag parsers for `<ctrl>`, `<button>`, `<grid>`, `<input>`,
and other UI elements in the Pytigon tag language.

::: pytigon_gui.guictrl.tag_parsers
    options:
      show_submodules: false
      members: true
